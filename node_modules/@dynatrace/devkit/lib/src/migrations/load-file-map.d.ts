import type { FileMap } from './file-map';
/** Loads a set of files or directories recursive into a fileMap */
export declare function loadFileMap(dirsOrFiles: string[], options?: {
    relativeTo?: string;
    fileNameVisitor?: (key: string, relativeTo?: string) => string;
}): FileMap;
