import type { FileMode } from './file-mode';
/** A single file inside a file map */
export interface File {
    content: Buffer;
    filename?: string;
    mode?: FileMode;
}
/** An in memory file tree representation interface */
export type FileMap = Record<string, File>;
