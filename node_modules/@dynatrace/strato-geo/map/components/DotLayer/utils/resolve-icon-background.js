"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var resolve_icon_background_exports = {};
__export(resolve_icon_background_exports, {
  resolveIconBackground: () => resolveIconBackground
});
module.exports = __toCommonJS(resolve_icon_background_exports);
var import_lodash_es = require("lodash");
var import_charts = require("@dynatrace/strato-components/charts");
var import_constants = require("../../../constants.js");
var import_calculate_hovered_color = require("../../../utils/calculate-hovered-color.js");
var import_define_color = require("../../../utils/define-color.js");
const resolveIconBackground = (datapoint, backgroundColor) => {
  let parsedBackgroundColor = (0, import_charts._getCanvasColor)(import_constants.DEFAULT_ICON_BACKGROUND_COLOR);
  let parsedHoveredBackgroundColor = (0, import_calculate_hovered_color.calculateHoveredColor)(
    parsedBackgroundColor
  );
  if (typeof backgroundColor !== "boolean") {
    const definedBgColor = (0, import_define_color.defineColor)(backgroundColor, datapoint);
    if (!(0, import_lodash_es.isEmpty)(definedBgColor)) {
      parsedBackgroundColor = (0, import_charts._getCanvasColor)(
        definedBgColor,
        import_constants.DEFAULT_ICON_BACKGROUND_COLOR
      );
      parsedHoveredBackgroundColor = (0, import_calculate_hovered_color.calculateHoveredColor)(
        parsedBackgroundColor
      );
    }
  }
  return {
    parsedBackgroundColor,
    parsedHoveredBackgroundColor
  };
};
