import { Location } from '../../../types/location.js';
interface ResolvedIconBackgroundColor {
    parsedBackgroundColor: string;
    parsedHoveredBackgroundColor: string;
}
export declare const resolveIconBackground: <T extends Location>(datapoint: T, backgroundColor: boolean | string | ((item: T) => string)) => ResolvedIconBackgroundColor;
export {};
