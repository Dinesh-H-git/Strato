"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var fetch_base_layer_features_exports = {};
__export(fetch_base_layer_features_exports, {
  _clearCache: () => _clearCache,
  fetchCountryFeatures: () => fetchCountryFeatures,
  fetchWorldViewFeatures: () => fetchWorldViewFeatures
});
module.exports = __toCommonJS(fetch_base_layer_features_exports);
var import_lodash_es = require("lodash");
var import_constants = require("../constants.js");
const cache = /* @__PURE__ */ new Map();
const MAPS_WITH_DISPUTED_BORDERS = ["IN", "IL", "CN", "MA"];
const getWorldViewParam = (countryCode) => MAPS_WITH_DISPUTED_BORDERS.includes(countryCode) ? `${countryCode}/` : "";
const fetchWorldViewFeatures = async (tenantCountryCode) => {
  const worldViewParam = getWorldViewParam(tenantCountryCode);
  const cacheKey = worldViewParam || "default";
  let features = cache.get(cacheKey);
  if ((0, import_lodash_es.isUndefined)(features)) {
    const res = await fetch(
      `${import_constants.CDN_BASE_PATH}${worldViewParam}WORLD-COUNTRIES.json`
    );
    features = await res.json();
    cache.set(cacheKey, features);
  }
  return features;
};
const fetchCountryFeatures = async (tenantCountryCode, regionCode) => {
  const worldViewParam = getWorldViewParam(tenantCountryCode);
  const cacheKey = `${worldViewParam || "default"}-${regionCode}`;
  let features = cache.get(cacheKey);
  if ((0, import_lodash_es.isUndefined)(features)) {
    const res = await fetch(
      `${import_constants.CDN_BASE_PATH}${worldViewParam}${regionCode}.json`
    );
    features = await res.json();
    cache.set(cacheKey, features);
  }
  return features;
};
const _clearCache = () => {
  cache.clear();
};
