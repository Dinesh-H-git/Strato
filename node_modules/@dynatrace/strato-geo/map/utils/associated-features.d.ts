/**
 * Iterates over the associated feature identifiers to check if a feature is an associated feature of another one.
 * @param featureId -
 */
export declare const isAssociatedFeature: (featureId: string | number) => boolean;
/**
 * Checks whether a feature has associated features (as for now, just connection features have)
 * @param featureId -
 */
export declare const hasAssociatedFeatures: (featureId: string) => boolean;
export declare const getAssociatedFeatures: (associatedFeatureIdentifier: string) => string[];
