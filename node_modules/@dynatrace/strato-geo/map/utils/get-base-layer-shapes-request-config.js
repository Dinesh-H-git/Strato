"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_base_layer_shapes_request_config_exports = {};
__export(get_base_layer_shapes_request_config_exports, {
  getBaseLayerShapesRequestConfig: () => getBaseLayerShapesRequestConfig
});
module.exports = __toCommonJS(get_base_layer_shapes_request_config_exports);
var import_lodash_es = require("lodash");
var import_constants = require("../constants.js");
const REGION_CODE_REGEX = /(?<code>.*)-.*/;
const hasIncludeAllWildCard = (include) => include.includes(import_constants.INCLUDE_ALL_WILDCARD_RULE);
const hasCountryCode = (include) => include.find((value) => !value.includes("-"));
const getBaseLayerShapesRequestConfig = (include) => {
  let includeWorldView = false;
  let countryRegions = [];
  if (hasIncludeAllWildCard(include) || hasCountryCode(include)) {
    includeWorldView = true;
  }
  const countryCodes = /* @__PURE__ */ new Set();
  for (let i = 0; i < include.length; i++) {
    const countryCode = REGION_CODE_REGEX.exec(include[i])?.groups?.code;
    if (!(0, import_lodash_es.isUndefined)(countryCode)) {
      countryCodes.add(countryCode);
    }
  }
  countryRegions = Array.from(countryCodes);
  return { includeWorldView, countryRegions };
};
