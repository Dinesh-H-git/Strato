"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var apply_feature_filter_rules_exports = {};
__export(apply_feature_filter_rules_exports, {
  applyFeatureFilterRules: () => applyFeatureFilterRules
});
module.exports = __toCommonJS(apply_feature_filter_rules_exports);
var import_constants = require("../constants.js");
const applyFeatureFilterRules = (rules, regionPrefix) => (value) => {
  const { include, exclude } = rules;
  let filteredFeatures = value.features;
  const includeAllRule = regionPrefix ? `${regionPrefix}-${import_constants.INCLUDE_ALL_WILDCARD_RULE}` : import_constants.INCLUDE_ALL_WILDCARD_RULE;
  if (!include.includes(includeAllRule) && include.length > 0) {
    filteredFeatures = filteredFeatures.filter(
      (feature) => include.includes(feature.id)
    );
  }
  if (exclude.length > 0) {
    filteredFeatures = filteredFeatures.filter(
      (feature) => !exclude.includes(feature.id)
    );
  }
  return { ...value, features: filteredFeatures };
};
