"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var create_categorical_color_scheme_from_legend_props_exports = {};
__export(create_categorical_color_scheme_from_legend_props_exports, {
  createCategoricalColorSchemeFromLegendProps: () => createCategoricalColorSchemeFromLegendProps
});
module.exports = __toCommonJS(create_categorical_color_scheme_from_legend_props_exports);
var import_lodash_es = require("lodash");
var import_charts = require("@dynatrace/strato-components/charts");
var import_constants = require("../constants.js");
const createCategoricalColorSchemeFromLegendProps = (colorPalette, categories) => {
  let obj = {};
  if ((0, import_charts._isCustomColorPalette)(colorPalette)) {
    categories.forEach((categoryName, index) => {
      obj = {
        ...obj,
        [categoryName]: colorPalette[index] ?? import_constants.DEFAULT_BASIC_SHAPE_COLOR_TOKEN.Default
      };
    });
  } else {
    const colorsList = Object.values(import_charts._colorPaletteMap[colorPalette]);
    categories.forEach((categoryName, i) => {
      const colorIndex = i % colorsList.length;
      const colorToken = import_charts._colorPaletteMap[colorPalette][(0, import_lodash_es.capitalize)(categoryName)] ?? colorsList[colorIndex];
      if (!(0, import_lodash_es.isUndefined)(colorToken)) {
        obj = {
          ...obj,
          [categoryName]: colorToken.Default
        };
      }
    });
  }
  return obj;
};
