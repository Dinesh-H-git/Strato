import type { Feature, FeatureCollection } from 'geojson';
import type { BaseLayerRules } from '../types/base-layer.js';
export declare const applyFeatureFilterRules: (rules: BaseLayerRules, regionPrefix?: string) => (value: FeatureCollection) => {
    features: Feature<import("geojson").Geometry, import("geojson").GeoJsonProperties>[];
    type: "FeatureCollection";
    bbox?: import("geojson").BBox | undefined;
};
