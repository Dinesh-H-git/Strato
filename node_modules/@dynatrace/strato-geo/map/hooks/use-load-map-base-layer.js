"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_load_map_base_layer_exports = {};
__export(use_load_map_base_layer_exports, {
  useLoadMapBaseLayer: () => useLoadMapBaseLayer
});
module.exports = __toCommonJS(use_load_map_base_layer_exports);
var import_react = require("react");
var import_use_map_view_provider_context = require("./use-map-view-provider-context.js");
var import_apply_feature_filter_rules = require("../utils/apply-feature-filter-rules.js");
var import_base_layer_rules = require("../utils/base-layer-rules.js");
var import_fetch_base_layer_features = require("../utils/fetch-base-layer-features.js");
var import_fetch_client_config_map_enabled = require("../utils/fetch-client-config-map-enabled.js");
var import_get_base_layer_shapes_request_config = require("../utils/get-base-layer-shapes-request-config.js");
var import_get_tenant_country_code = require("../utils/get-tenant-country-code.js");
var import_merge_feature_collections = require("../utils/merge-feature-collections.js");
const useLoadMapBaseLayer = (baseLayerConfig) => {
  const [isFetchingFeatures, setIsFetchingFeatures] = (0, import_react.useState)(true);
  const [isMapEnabled, setIsMapEnabled] = (0, import_react.useState)(false);
  const [baseFeatureCollection, setBaseFeatureCollection] = (0, import_react.useState)(void 0);
  const [error, setError] = (0, import_react.useState)(false);
  const {
    countryCode: providerCountryCode,
    displayWorldMap: providerDisplayWorldMap
  } = (0, import_use_map_view_provider_context.useMapViewProviderContext)();
  const getData = (0, import_react.useCallback)(async () => {
    setIsFetchingFeatures(true);
    const isMapEnabledResponse = providerDisplayWorldMap ?? await (0, import_fetch_client_config_map_enabled.fetchClientConfigMapEnabled)();
    setIsMapEnabled(isMapEnabledResponse);
    try {
      if (isMapEnabledResponse) {
        const tenantCountryCode = providerCountryCode ?? await (0, import_get_tenant_country_code.getTenantCountryCode)();
        const { includeWorldView, countryRegions } = (0, import_get_base_layer_shapes_request_config.getBaseLayerShapesRequestConfig)(baseLayerConfig.include);
        const featuresRequests = [];
        if (includeWorldView) {
          const worldViewRules = (0, import_base_layer_rules.getWorldViewRules)(baseLayerConfig);
          const worldViewRequest = (0, import_fetch_base_layer_features.fetchWorldViewFeatures)(
            tenantCountryCode
          ).then((0, import_apply_feature_filter_rules.applyFeatureFilterRules)(worldViewRules));
          featuresRequests.push(worldViewRequest);
        }
        if (countryRegions.length > 0) {
          const regionsRules = (0, import_base_layer_rules.getRegionsRules)(baseLayerConfig);
          countryRegions.forEach((countryCode) => {
            const countryRules = (0, import_base_layer_rules.getCountryRegionsRules)(
              regionsRules,
              countryCode
            );
            const countryRequest = (0, import_fetch_base_layer_features.fetchCountryFeatures)(
              tenantCountryCode,
              countryCode
            ).then((0, import_apply_feature_filter_rules.applyFeatureFilterRules)(countryRules, countryCode));
            featuresRequests.push(countryRequest);
          });
        }
        const featureCollections = await Promise.all(featuresRequests);
        setBaseFeatureCollection((0, import_merge_feature_collections.mergeFeatureCollections)(featureCollections));
      }
    } catch {
      setError(true);
    }
    setIsFetchingFeatures(false);
  }, [
    baseLayerConfig.include,
    baseLayerConfig.exclude,
    providerCountryCode,
    providerDisplayWorldMap
  ]);
  (0, import_react.useEffect)(() => {
    getData();
  }, [getData]);
  return { isFetchingFeatures, baseFeatureCollection, isMapEnabled, error };
};
