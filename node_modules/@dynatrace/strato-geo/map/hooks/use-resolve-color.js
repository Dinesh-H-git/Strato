"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_resolve_color_exports = {};
__export(use_resolve_color_exports, {
  useResolveChoroplethColor: () => useResolveChoroplethColor,
  useResolveColor: () => useResolveColor,
  useResolveConnectionColor: () => useResolveConnectionColor,
  useResolveLocationColor: () => useResolveLocationColor
});
module.exports = __toCommonJS(use_resolve_color_exports);
var import_lodash_es = require("lodash");
var import_core = require("@dynatrace/strato-components/core");
var import_use_color_scale = require("./use-color-scale.js");
var import_constants = require("../constants.js");
var import_use_map_config = require("./use-map-config.js");
var import_coloring = require("../types/coloring.js");
var import_build_scale_from_legend_config = require("../utils/build-scale-from-legend-config.js");
const useResolveColor = (defaultColor, props, colorParser) => {
  const legendColoring = (0, import_use_color_scale.useColorScale)();
  const { legend } = (0, import_use_map_config.useMapConfig)();
  if (!(0, import_coloring.isLegendColoring)(props)) {
    return props.color ?? defaultColor;
  }
  const fallbackColor = !(0, import_lodash_es.isUndefined)(legend) && ((0, import_build_scale_from_legend_config.isSequentialLegend)(legend) || (0, import_build_scale_from_legend_config.isThresholdLegend)(legend)) ? import_constants.DEFAULT_RANGE_COLOR : defaultColor;
  let colorLegend = fallbackColor;
  if (!(0, import_lodash_es.isUndefined)(legendColoring) && !(0, import_lodash_es.isUndefined)(colorParser)) {
    colorLegend = (item) => {
      const value = (0, import_core._accessValue)(item, props.valueAccessor);
      if ((0, import_lodash_es.isUndefined)(value)) {
        return fallbackColor;
      }
      const parsedValue = typeof value === "number" ? value + Number.MIN_VALUE : value;
      return colorParser(parsedValue) ?? fallbackColor;
    };
  }
  return colorLegend;
};
const useResolveLocationColor = (defaultColor, props, colorParser) => {
  return useResolveColor(defaultColor, props, colorParser);
};
const useResolveConnectionColor = (defaultColor, props, colorParser) => {
  return useResolveColor(defaultColor, props, colorParser);
};
const useResolveChoroplethColor = (defaultColor, props, colorParser) => {
  return useResolveColor(defaultColor, props, colorParser);
};
