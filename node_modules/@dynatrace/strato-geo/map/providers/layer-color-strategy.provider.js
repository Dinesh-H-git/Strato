"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var layer_color_strategy_provider_exports = {};
__export(layer_color_strategy_provider_exports, {
  LayerColorStrategyProvider: () => LayerColorStrategyProvider,
  buildCategoricalScale: () => buildCategoricalScale,
  buildClampedScale: () => buildClampedScale,
  buildNumericScale: () => buildNumericScale
});
module.exports = __toCommonJS(layer_color_strategy_provider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_d3_array = require("d3-array");
var import_lodash_es = require("lodash");
var import_layer_coloring_strategy_context = require("../contexts/layer-coloring-strategy.context.js");
var import_use_color_scale = require("../hooks/use-color-scale.js");
var import_use_map_config = require("../hooks/use-map-config.js");
var import_build_scale_from_legend_config = require("../utils/build-scale-from-legend-config.js");
function buildCategoricalScale(scale) {
  return (value) => {
    const categories = scale.domain();
    if (categories.includes(value)) {
      return scale(value);
    }
  };
}
function buildNumericScale(scale) {
  const scaleDomain = scale.domain();
  const [min, max] = (0, import_d3_array.extent)(scaleDomain);
  if ((0, import_lodash_es.isUndefined)(min) || (0, import_lodash_es.isUndefined)(max)) {
    return (_) => void 0;
  }
  return (value) => {
    if (value && value >= min && value <= max) {
      return scale(value);
    }
  };
}
function buildClampedScale(legend, scale) {
  let colorParser;
  if ((0, import_build_scale_from_legend_config.isSequentialLegend)(legend)) {
    colorParser = buildNumericScale(scale);
  } else if ((0, import_build_scale_from_legend_config.isThresholdLegend)(legend)) {
    colorParser = buildNumericScale(scale);
  } else {
    colorParser = buildCategoricalScale(scale);
  }
  return colorParser;
}
const LayerColorStrategyProvider = ({ children }) => {
  const scale = (0, import_use_color_scale.useColorScale)();
  const { legend } = (0, import_use_map_config.useMapConfig)();
  if ((0, import_lodash_es.isUndefined)(legend) || (0, import_lodash_es.isUndefined)(scale)) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children });
  }
  const clampedScale = buildClampedScale(legend, scale);
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_layer_coloring_strategy_context.LayerColoringStrategyContext.Provider, { value: clampedScale, children });
};
