import { createContext } from "react";
import { createStore } from "@dynatrace/strato-components/core";
const initialSelection = { highlightedSeries: void 0, visible: void 0 };
const initialState = {
  tooltip: {
    enabled: true,
    hoveredLayerId: void 0,
    location: void 0
  },
  overlay: {
    mouseInBounds: false
  },
  selection: initialSelection,
  zoom: {
    zoomInEnabled: true,
    zoomOutEnabled: true,
    zoomToFitEnabled: true,
    resetEnabled: false
  }
};
const MapStore = createContext(
  createStore(initialState)
);
MapStore["displayName"] = "MapStore";
export {
  MapStore,
  initialState
};
//# sourceMappingURL=map-store.context.js.map
