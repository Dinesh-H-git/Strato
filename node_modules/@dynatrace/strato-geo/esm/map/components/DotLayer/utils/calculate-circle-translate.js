const calculateCircleTranslate = (iconAnchor, radius) => {
  if (iconAnchor === "bottom") {
    const offsetTop = radius / 2;
    return [0, -offsetTop];
  }
  return [0, 0];
};
export {
  calculateCircleTranslate
};
//# sourceMappingURL=calculate-circle-translate.js.map
