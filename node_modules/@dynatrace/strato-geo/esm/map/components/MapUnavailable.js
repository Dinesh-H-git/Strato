import { jsx } from "react/jsx-runtime";
import { BaseLayerDataError } from "./BaseLayerDataError.js";
import { DisabledState } from "../slots/states/DisabledState.js";
const MapUnavailable = ({
  chartHeight,
  error,
  isDisabled,
  isFetchingFeatures
}) => {
  if (error) {
    return /* @__PURE__ */ jsx(BaseLayerDataError, { height: chartHeight });
  }
  if (!isFetchingFeatures && isDisabled) {
    return /* @__PURE__ */ jsx(DisabledState, { height: chartHeight });
  }
};
MapUnavailable.displayName = "MapUnavailable";
export {
  MapUnavailable
};
//# sourceMappingURL=MapUnavailable.js.map
