import { isNil } from "lodash-es";
import { useCallback } from "react";
const useSetMapInitialConfiguration = () => useCallback((mapRef) => {
  if (isNil(mapRef)) {
    return;
  }
  mapRef.getMap()?.touchZoomRotate?.disableRotation();
}, []);
export {
  useSetMapInitialConfiguration
};
//# sourceMappingURL=use-set-map-initial-configuration.js.map
