import { isString } from "lodash-es";
const ASSOCIATED_FEATURES_MAP = {
  connection: ["direction-start", "direction-end"]
};
const isAssociatedFeature = (featureId) => {
  if (!isString(featureId)) {
    return false;
  }
  const associatedFeatureIdentifiers = ASSOCIATED_FEATURES_MAP.connection;
  for (const associatedFeatureIdentifier of associatedFeatureIdentifiers) {
    if (featureId.includes(associatedFeatureIdentifier)) {
      return true;
    }
  }
  return false;
};
const hasAssociatedFeatures = (featureId) => {
  return featureId.includes("connection");
};
const getAssociatedFeatures = (associatedFeatureIdentifier) => ASSOCIATED_FEATURES_MAP[associatedFeatureIdentifier] ?? [];
export {
  getAssociatedFeatures,
  hasAssociatedFeatures,
  isAssociatedFeature
};
//# sourceMappingURL=associated-features.js.map
