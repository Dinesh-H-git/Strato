var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var escaped_characters_exports = {};
__export(escaped_characters_exports, {
  EscapedCharacterDecorationPlugin: () => EscapedCharacterDecorationPlugin,
  createEscapedCharacterDecorations: () => createEscapedCharacterDecorations
});
module.exports = __toCommonJS(escaped_characters_exports);
var import_state = require("@codemirror/state");
var import_view = require("@codemirror/view");
var import_decoration_marks = require("./decoration-marks.js");
var import_escape_suggestion = require("../utils/escape-suggestion.js");
function createEscapedCharacterDecorations(view) {
  const builder = new import_state.RangeSetBuilder();
  for (const { from, to } of view.visibleRanges) {
    const stringInput = view.state.sliceDoc(from, to);
    const escapedCharacters = stringInput.matchAll(import_escape_suggestion.ESCAPED_CHARACTERS_REGEX);
    for (const match of escapedCharacters) {
      const { index } = match;
      builder.add(index, index + 2, import_decoration_marks.escapedCharacterMark);
    }
  }
  return builder.finish();
}
const EscapedCharacterDecorationPlugin = import_view.ViewPlugin.fromClass(
  class {
    decorations;
    constructor(view) {
      this.decorations = createEscapedCharacterDecorations(view);
    }
    update(update) {
      if (update.docChanged) {
        this.decorations = createEscapedCharacterDecorations(update.view);
      }
    }
  },
  {
    decorations: (view) => view.decorations
  }
);
