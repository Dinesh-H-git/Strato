import { ViewPlugin } from '@codemirror/view';
import type { DecorationSet, EditorView, ViewUpdate } from '@codemirror/view';
/**
 * Create a range set with decorations for all the filter statements.
 * @internal
 */
export declare function createFilterStatementDeleteWidgets(view: EditorView): DecorationSet;
/**
 * View plugin to wrap all filter statements in spans.
 * @internal
 */
export declare const FilterStatementDeleteWidgetPlugin: ViewPlugin<{
    decorations: DecorationSet;
    update(update: ViewUpdate): void;
}, undefined>;
