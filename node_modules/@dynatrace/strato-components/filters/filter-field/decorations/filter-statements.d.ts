import type { Decoration, DecorationSet, EditorView, ViewUpdate } from '@codemirror/view';
import { ViewPlugin } from '@codemirror/view';
/**
 * Create a range set with decorations for all the filter statements.
 * @internal
 */
export declare function createFilterStatementDecorations(view: EditorView): import("@codemirror/state").RangeSet<Decoration>;
/** View plugin to wrap all filter statements in spans. */
export declare const FilterStatementDecorationPlugin: ViewPlugin<{
    decorations: DecorationSet;
    update(update: ViewUpdate): void;
}, undefined>;
