var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var filter_variable_exports = {};
__export(filter_variable_exports, {
  FilterVariableDecorationPlugin: () => FilterVariableDecorationPlugin,
  createFilterVariableDecorations: () => createFilterVariableDecorations
});
module.exports = __toCommonJS(filter_variable_exports);
var import_language = require("@codemirror/language");
var import_state = require("@codemirror/state");
var import_view = require("@codemirror/view");
var import_decoration_marks = require("./decoration-marks.js");
function createFilterVariableDecorations(view) {
  const builder = new import_state.RangeSetBuilder();
  for (const { from, to } of view.visibleRanges) {
    const tree = (0, import_language.syntaxTree)(view.state);
    tree.iterate({
      from,
      to,
      enter: (node) => {
        if (node.name === "Variable") {
          const { from: valueFrom, to: valueTo } = node;
          builder.add(valueFrom, valueTo, import_decoration_marks.variableMark);
        }
      }
    });
  }
  return builder.finish();
}
const FilterVariableDecorationPlugin = import_view.ViewPlugin.fromClass(
  class {
    decorations;
    constructor(view) {
      this.decorations = createFilterVariableDecorations(view);
    }
    update(update) {
      if (update.docChanged) {
        this.decorations = createFilterVariableDecorations(update.view);
      }
    }
  },
  {
    decorations: (view) => view.decorations
  }
);
