var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var filter_search_exports = {};
__export(filter_search_exports, {
  FilterSearchDecorationPlugin: () => FilterSearchDecorationPlugin,
  createFilterSearchDecorations: () => createFilterSearchDecorations
});
module.exports = __toCommonJS(filter_search_exports);
var import_language = require("@codemirror/language");
var import_state = require("@codemirror/state");
var import_view = require("@codemirror/view");
var import_decoration_marks = require("./decoration-marks.js");
function createFilterSearchDecorations(view) {
  const builder = new import_state.RangeSetBuilder();
  for (const { from, to } of view.visibleRanges) {
    (0, import_language.syntaxTree)(view.state).iterate({
      from,
      to,
      enter: (node) => {
        if (node.name === "SearchOperator") {
          const { from: keyFrom, to: keyTo } = node;
          builder.add(keyFrom, keyTo, import_decoration_marks.keyMark);
          builder.add(keyFrom + 1, keyTo, import_decoration_marks.searchMark);
          return false;
        }
      }
    });
  }
  return builder.finish();
}
const FilterSearchDecorationPlugin = import_view.ViewPlugin.fromClass(
  class {
    decorations;
    constructor(view) {
      this.decorations = createFilterSearchDecorations(view);
    }
    update(update) {
      if (update.docChanged) {
        this.decorations = createFilterSearchDecorations(update.view);
      }
    }
  },
  {
    decorations: (view) => view.decorations
  }
);
