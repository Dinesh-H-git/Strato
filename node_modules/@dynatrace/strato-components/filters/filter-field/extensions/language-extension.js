var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var language_extension_exports = {};
__export(language_extension_exports, {
  FilterFieldLanguageExtension: () => FilterFieldLanguageExtension
});
module.exports = __toCommonJS(language_extension_exports);
var import_autocomplete = require("@codemirror/autocomplete");
var import_editor_config = require("../editor-state-configs/editor-config.js");
var import_filterfield_language = require("../parser/filterfield-language.js");
const FilterFieldLanguageExtension = (() => [
  /**
   * Filter field language + syntax highlighting.
   *
   * Error highlighting is also done with the highlight extensions, since using an actual
   * linter, potential errors only show up after a short (configurable) delay. As soon as
   * you type something again, the errors disappear and then show up again if the input is
   * still invalid. Using the decoration plugins, we can immediately show errors.
   * The actual linter extension is only used to show tooltips with proper error messages.
   */
  // The extension for parsing the filter field language into a syntax tree.
  import_filterfield_language.filterFieldLanguageExtension,
  // Automatically close brackets.
  (0, import_autocomplete.closeBrackets)(),
  import_editor_config.usedValidatorMap,
  import_editor_config.usedErrors,
  import_editor_config.pastingMode
])();
