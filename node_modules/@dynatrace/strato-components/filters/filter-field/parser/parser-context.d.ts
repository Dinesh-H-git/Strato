import { ContextTracker } from '@lezer/lr';
declare class LastAcceptedStatement {
    lastToken: string | null;
    pos: number | null;
    validExpression: boolean | undefined;
    constructor(lastToken: string | null, pos: number | null, validExpression?: boolean);
}
export declare const trackLastToken: ContextTracker<LastAcceptedStatement>;
export {};
