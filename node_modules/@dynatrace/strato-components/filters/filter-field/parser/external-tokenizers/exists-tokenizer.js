var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var exists_tokenizer_exports = {};
__export(exists_tokenizer_exports, {
  existsTokenizer: () => existsTokenizer
});
module.exports = __toCommonJS(exists_tokenizer_exports);
var import_lr = require("@lezer/lr");
var import_character_codes = require("../character-codes.js");
var import_parser_terms = require("../parser.terms.js");
const { ASTERISK, BANG, EOF, EQUALS, SPACE, CLOSING_BRACKET, BACKSLASH } = import_character_codes.characterCodes;
const existsTokenizer = new import_lr.ExternalTokenizer((inputStream) => {
  let currCharCode = inputStream.peek(0);
  if (currCharCode === BANG) {
    currCharCode = inputStream.advance();
  }
  if (currCharCode !== EQUALS) {
    return;
  }
  if (inputStream.peek(1) === SPACE) {
    inputStream.advance();
  }
  const endOfExists = inputStream.peek(2);
  if (inputStream.peek(1) === ASTERISK && (endOfExists === EOF || endOfExists === SPACE || endOfExists === CLOSING_BRACKET || endOfExists === BACKSLASH && inputStream.peek(3) === EOF)) {
    inputStream.advance(2);
    inputStream.acceptToken(import_parser_terms.ExistsOperator);
  }
});
