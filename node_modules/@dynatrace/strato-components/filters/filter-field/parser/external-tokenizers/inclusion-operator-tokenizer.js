var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var inclusion_operator_tokenizer_exports = {};
__export(inclusion_operator_tokenizer_exports, {
  inclusionOperatorTokenizer: () => inclusionOperatorTokenizer
});
module.exports = __toCommonJS(inclusion_operator_tokenizer_exports);
var import_lr = require("@lezer/lr");
var import_character_codes = require("../character-codes.js");
var import_parser_terms = require("../parser.terms.js");
const { EOF, SPACE, OPENING_BRACKET, CLOSING_BRACKET, DOUBLEQUOTE, BACKSLASH } = import_character_codes.characterCodes;
const validAfterInclusionOperator = [
  OPENING_BRACKET,
  CLOSING_BRACKET,
  DOUBLEQUOTE,
  BACKSLASH,
  SPACE,
  EOF
];
const inclusionOperatorTokenizer = new import_lr.ExternalTokenizer(
  (inputStream) => {
    const char = String.fromCharCode(inputStream.next).toLowerCase();
    let tokenOffset = 0;
    switch (char) {
      case "i":
        if (String.fromCharCode(inputStream.peek(1)).toLowerCase() === "n") {
          tokenOffset = 2;
        }
        break;
      case "n":
        if (
          // TODO: switch to match sequence.
          // matchCharacterSequence({inputStream, characterSequence: 'not in'})
          String.fromCharCode(inputStream.peek(1)).toLowerCase() === "o" && String.fromCharCode(inputStream.peek(2)).toLowerCase() === "t" && String.fromCharCode(inputStream.peek(3)).toLowerCase() === " " && String.fromCharCode(inputStream.peek(4)).toLowerCase() === "i" && String.fromCharCode(inputStream.peek(5)).toLowerCase() === "n"
        ) {
          tokenOffset = 6;
        }
        break;
      default:
        return;
    }
    inputStream.advance(tokenOffset);
    if (validAfterInclusionOperator.includes(inputStream.next)) {
      inputStream.acceptToken(import_parser_terms.InclusionOperator);
    }
  }
);
