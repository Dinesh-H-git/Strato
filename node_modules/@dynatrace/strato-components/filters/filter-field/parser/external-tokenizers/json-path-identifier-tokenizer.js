var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var json_path_identifier_tokenizer_exports = {};
__export(json_path_identifier_tokenizer_exports, {
  jsonPathIdentifierTokenizer: () => jsonPathIdentifierTokenizer
});
module.exports = __toCommonJS(json_path_identifier_tokenizer_exports);
var import_lr = require("@lezer/lr");
var import_character_codes = require("../character-codes.js");
var import_parser_terms = require("../parser.terms.js");
const { BACKSLASH } = import_character_codes.characterCodes;
const disallowedJsonPathIdentifierChars = new RegExp(/[,()!<>="$:[\]~ .]/);
const jsonPathIdentifierTokenizer = new import_lr.ExternalTokenizer(
  (inputStream, stack) => {
    if (stack.context.lastToken !== "JsonPathBase" || inputStream.next === -1) {
      return;
    }
    const start = inputStream.pos;
    while (!disallowedJsonPathIdentifierChars.test(
      String.fromCharCode(inputStream.next)
    ) && inputStream.next !== -1) {
      if (inputStream.next === BACKSLASH) {
        inputStream.advance();
      }
      inputStream.advance();
    }
    if (inputStream.pos > start) {
      inputStream.acceptToken(import_parser_terms.JsonPathIdentifier);
    }
  }
);
