var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var search_operator_tokenizer_exports = {};
__export(search_operator_tokenizer_exports, {
  searchOperatorTokenizer: () => searchOperatorTokenizer
});
module.exports = __toCommonJS(search_operator_tokenizer_exports);
var import_lr = require("@lezer/lr");
var import_character_codes = require("../character-codes.js");
var import_parser_terms = require("../parser.terms.js");
const {
  ASTERISK,
  CLOSING_BRACKET,
  DOUBLEQUOTE,
  EOF,
  OPENING_BRACKET,
  SPACE,
  TILDE
} = import_character_codes.characterCodes;
const validBeforeSearchOperator = [
  OPENING_BRACKET,
  CLOSING_BRACKET,
  DOUBLEQUOTE,
  SPACE,
  EOF
];
const searchOperatorTokenizer = new import_lr.ExternalTokenizer(
  (inputStream, stack) => {
    if (inputStream.next !== ASTERISK) {
      return;
    }
    const previousToken = stack.context.lastToken;
    const previousChar = inputStream.peek(-1);
    if (previousToken !== "expression" && previousToken !== null && !validBeforeSearchOperator.includes(previousChar)) {
      return;
    }
    if (inputStream.peek(1) === SPACE) {
      inputStream.advance();
    }
    const nextChar = inputStream.advance();
    if (nextChar !== TILDE) {
      return;
    }
    inputStream.advance();
    inputStream.acceptToken(import_parser_terms.SearchOperator);
  }
);
