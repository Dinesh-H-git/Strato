var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var position_operator_tokenizer_exports = {};
__export(position_operator_tokenizer_exports, {
  positionOperatorTokenizer: () => positionOperatorTokenizer
});
module.exports = __toCommonJS(position_operator_tokenizer_exports);
var import_lr = require("@lezer/lr");
var import_character_codes = require("../character-codes.js");
var import_parser_terms = require("../parser.terms.js");
const { ASTERISK } = import_character_codes.characterCodes;
const positionOperatorTokenizer = new import_lr.ExternalTokenizer(
  (inputStream, stack) => {
    if (inputStream.next !== ASTERISK) {
      return;
    }
    if (stack.context.lastToken === null || stack.context.lastToken === "expression") {
      return;
    }
    inputStream.advance();
    let additionalOperators = 0;
    while (inputStream.next === ASTERISK) {
      inputStream.advance();
      additionalOperators += 1;
    }
    inputStream.acceptToken(
      additionalOperators === 0 ? import_parser_terms.PositionOperator : import_parser_terms.InvalidPositionOperator
    );
  }
);
