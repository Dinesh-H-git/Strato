import type { Transaction, TransactionSpec } from '@codemirror/state';
import type { EditorView } from '@codemirror/view';
import { type MutableRefObject } from 'react';
/**
 * Encapsulates EditorView methods to apply ErrorBoundary aware error handling
 * on main methods
 * @internal
 */
export declare const useErrorAwareViewRef: (viewRef: MutableRefObject<EditorView | null>) => {
    viewDispatch: (args: Transaction | TransactionSpec | TransactionSpec[]) => void;
};
