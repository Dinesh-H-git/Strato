var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var usePinnedFilters_exports = {};
__export(usePinnedFilters_exports, {
  usePinnedFilterMutation: () => usePinnedFilterMutation,
  usePinnedFilters: () => usePinnedFilters
});
module.exports = __toCommonJS(usePinnedFilters_exports);
var import_usePersistedFilter = require("./usePersistedFilter.js");
var import_useRecentFiltersStore = require("./useRecentFiltersStore.js");
function usePinnedFilters(name = null) {
  const setFilters = (0, import_useRecentFiltersStore.useRecentAndPinnedFiltersStore)(
    (state) => state.setPinnedFilters,
    name
  );
  const filters = (0, import_useRecentFiltersStore.useRecentAndPinnedFiltersStore)(
    (state) => state.pinnedFilters,
    name
  );
  (0, import_usePersistedFilter.usePersistedFilter)(
    name,
    `strato-FilterField-pinnedFilters-${name}`,
    setFilters
  );
  return filters;
}
function usePinnedFilterMutation(name = null) {
  const { mutate: pinFilter } = (0, import_usePersistedFilter.usePersistedFilterMutation)(
    `strato-FilterField-pinnedFilters-${name}`,
    name,
    "pin"
  );
  const { mutate: unpinFilter } = (0, import_usePersistedFilter.usePersistedFilterMutation)(
    `strato-FilterField-pinnedFilters-${name}`,
    name,
    "unpin"
  );
  return {
    pinFilter,
    unpinFilter
  };
}
