import { type StoredFilter } from '../../state/recent-pinned-store.js';
/**
 * Helper function to read a persisted stored filter from the app state.
 * @internal
 */
export declare function usePersistedFilter(name: string | null, key: string, saveFn: (filters: Array<StoredFilter>) => void): import("@tanstack/react-query").UseQueryResult<any, Error>;
/**
 * Helper function to mutate the persisted stored filters in the app state.
 * Depending on the actionType, it can pin, unpin or save filter.
 * @internal
 */
export declare function usePersistedFilterMutation(key: string, name: string | null, actionType: 'pin' | 'unpin' | 'save'): import("@tanstack/react-query").UseMutationResult<void | StoredFilter[], Error, string, unknown>;
