var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useCachedTreeConversion_exports = {};
__export(useCachedTreeConversion_exports, {
  useCachedTreeConversion: () => useCachedTreeConversion
});
module.exports = __toCommonJS(useCachedTreeConversion_exports);
var import_lodash_es = require("lodash");
var import_react = require("react");
var import_tree_conversion = require("../utils/conversion/tree-conversion.js");
function useCachedTreeConversion() {
  const prevDoc = (0, import_react.useRef)(void 0);
  const cachedTree = (0, import_react.useRef)(void 0);
  const cachedIsValid = (0, import_react.useRef)(true);
  const cachedIsValidSearch = (0, import_react.useRef)(true);
  const prevInternalValidatorMap = (0, import_react.useRef)(
    void 0
  );
  const prevParserConfig = (0, import_react.useRef)(void 0);
  function getSyntaxTree(args) {
    const { doc, internalValidatorMap, parserConfig } = args;
    if (prevDoc.current === doc?.toString() && // The internalValidatorMap is derived from the validatorMap prop, which is normalized with useMemo in FilterField.tsx
    // whenever the validatorMap changes. It has a stable reference as long as the raw validatorMap doesn't change.
    // Therefore, we can rely on reference equality here for caching.
    internalValidatorMap === prevInternalValidatorMap.current && (0, import_lodash_es.isEqual)(parserConfig, prevParserConfig.current) && cachedTree.current) {
      return {
        tree: cachedTree.current,
        isValid: cachedIsValid.current,
        isValidSearch: cachedIsValidSearch.current
      };
    }
    prevDoc.current = doc?.toString();
    prevInternalValidatorMap.current = internalValidatorMap;
    prevParserConfig.current = parserConfig;
    const filter = (0, import_tree_conversion.convertLezerTree)(args);
    cachedTree.current = filter.tree;
    cachedIsValid.current = filter.isValid;
    cachedIsValidSearch.current = filter.isValidSearch;
    return filter;
  }
  return { getSyntaxTree };
}
