var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var filter_field_state_reducer_exports = {};
__export(filter_field_state_reducer_exports, {
  FilterFieldActions: () => FilterFieldActions,
  filterFieldReducer: () => filterFieldReducer
});
module.exports = __toCommonJS(filter_field_state_reducer_exports);
var FilterFieldActions = /* @__PURE__ */ ((FilterFieldActions2) => {
  FilterFieldActions2["ADDSUGGESTION"] = "add";
  FilterFieldActions2["REMOVESUGGESTION"] = "remove";
  return FilterFieldActions2;
})(FilterFieldActions || {});
function filterFieldReducer(state, action) {
  const { type, payload } = action;
  const newState = { ...state };
  if (type === "add" /* ADDSUGGESTION */) {
    const { id, applySuggestion } = payload;
    newState.suggestions.set(id, applySuggestion);
    return newState;
  }
  if (type === "remove" /* REMOVESUGGESTION */) {
    const { id } = payload;
    newState.suggestions.delete(id);
    return newState;
  }
  return state;
}
