var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var suggestions_callback_exports = {};
__export(suggestions_callback_exports, {
  getFilterStatement: () => getFilterStatement
});
module.exports = __toCommonJS(suggestions_callback_exports);
var import_tree_node_guards = require("../tree-node-guards.js");
function findStatement(cursorPosition, node) {
  if (!node || !(node.range.from <= cursorPosition && node.range.to >= cursorPosition)) {
    return null;
  }
  if (node.type === "Group") {
    if (node.children.length === 0 || node.children[0].type === "Error") {
      return {};
    }
    let newNode = null;
    node.children.forEach((childNode) => {
      const temp = findStatement(cursorPosition, childNode);
      if (temp) {
        newNode = temp;
      }
    });
    return newNode;
  }
  if (node.type === "Statement") {
    return node;
  }
  return null;
}
function getFilterStatement(tree, cursorPosition, doc) {
  if (!doc) {
    return;
  }
  let statementNode;
  if (!tree) {
    return;
  }
  for (const node of tree.children) {
    const temp = findStatement(cursorPosition, node);
    if (temp) {
      statementNode = temp;
    }
  }
  if (!statementNode || !(0, import_tree_node_guards.isFilterFieldStatementNode)(statementNode)) {
    return;
  }
  return statementNode;
}
