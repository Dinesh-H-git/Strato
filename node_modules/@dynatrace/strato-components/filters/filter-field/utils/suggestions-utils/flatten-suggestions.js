var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var flatten_suggestions_exports = {};
__export(flatten_suggestions_exports, {
  flattenSuggestions: () => flattenSuggestions
});
module.exports = __toCommonJS(flatten_suggestions_exports);
var import_react = require("react");
var import_is_slot = require("../../../../core/utils/is-slot.js");
var import_SuggestionGroup = require("../../suggestions/SuggestionGroup.js");
var import_SuggestionGroupLabel = require("../../suggestions/SuggestionGroupLabel.js");
var import_tree_node_guards = require("../tree-node-guards.js");
function flattenSuggestions(items) {
  const flattenedSuggestionItems = [];
  const groupMetadata = [];
  let isFirstGroup = true;
  for (let index = 0; index < items.length; index++) {
    const item = items[index];
    if ((0, import_react.isValidElement)(item)) {
      if ((0, import_is_slot.isSlot)(item, import_SuggestionGroup.SuggestionGroup)) {
        const {
          label,
          suggestions,
          children,
          id,
          "aria-label": ariaLabel
        } = item.props;
        const groupId = id ?? `__group_${index}`;
        const childArray = import_react.Children.toArray(children);
        const labelSlot = (0, import_is_slot.isSlot)(childArray[0], import_SuggestionGroupLabel.SuggestionGroupLabel) ? childArray[0] : void 0;
        const groupedSuggestionChildren = labelSlot ? childArray.slice(1) : childArray;
        const suggestionCount = (suggestions?.length ?? 0) + groupedSuggestionChildren.length;
        if (suggestionCount === 0) {
          continue;
        }
        const hasLabel = labelSlot !== void 0 || label !== void 0;
        const hasAriaLabel = ariaLabel !== void 0;
        const shouldAddHeader = hasLabel || hasAriaLabel || !isFirstGroup;
        isFirstGroup = false;
        if (shouldAddHeader) {
          if (labelSlot) {
            flattenedSuggestionItems.push({
              kind: "group-header",
              label: labelSlot,
              groupId
            });
            groupMetadata.push({ groupId, label: labelSlot });
          } else if (label) {
            flattenedSuggestionItems.push({
              kind: "group-header",
              label,
              groupId
            });
            groupMetadata.push({ groupId, label });
          } else {
            flattenedSuggestionItems.push({ kind: "group-header", groupId });
            if (hasAriaLabel) {
              groupMetadata.push({ groupId, label: ariaLabel });
            }
          }
        }
        if (suggestions) {
          for (const s of suggestions) {
            flattenedSuggestionItems.push({
              kind: "suggestion",
              data: s,
              groupId
            });
          }
        }
        for (const child of groupedSuggestionChildren) {
          flattenedSuggestionItems.push({
            kind: "react-node",
            node: child,
            groupId
          });
        }
        continue;
      }
      flattenedSuggestionItems.push({ kind: "react-node", node: item });
      continue;
    }
    if ((0, import_tree_node_guards.isFilterFieldSuggestionGroup)(item)) {
      const groupId = item.id ?? `__group_${index}`;
      if (!item.suggestions || item.suggestions.length === 0) {
        continue;
      }
      const hasLabel = item.label !== void 0;
      const hasAriaLabel = item["aria-label"] !== void 0;
      const shouldAddHeader = hasLabel || hasAriaLabel || !isFirstGroup;
      isFirstGroup = false;
      if (shouldAddHeader) {
        flattenedSuggestionItems.push({
          kind: "group-header",
          label: item.label,
          groupId
        });
        if (hasLabel || hasAriaLabel) {
          groupMetadata.push({
            groupId,
            label: item.label ?? item["aria-label"]
          });
        }
      }
      for (const s of item.suggestions) {
        flattenedSuggestionItems.push({ kind: "suggestion", data: s, groupId });
      }
      continue;
    }
    if ((0, import_tree_node_guards.isFilterFieldSuggestion)(item)) {
      flattenedSuggestionItems.push({ kind: "suggestion", data: item });
    }
  }
  return { flattenedSuggestionItems, groupMetadata };
}
