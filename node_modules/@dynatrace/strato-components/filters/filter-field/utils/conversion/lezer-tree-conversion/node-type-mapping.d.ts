import type { FilterFieldLezerNodeType, FilterFieldNode } from '../../../types/tree-nodes.js';
/**
 * The type of the node how it is called in the exposed tree.
 * @internal
 */
export declare const nodeTypeOverrides: {
    [key in FilterFieldLezerNodeType]?: Exclude<FilterFieldNode['type'], 'Filter'>;
};
