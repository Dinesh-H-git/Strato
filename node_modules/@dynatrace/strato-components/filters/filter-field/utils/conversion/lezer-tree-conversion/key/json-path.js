var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var json_path_exports = {};
__export(json_path_exports, {
  convertJsonPath: () => convertJsonPath
});
module.exports = __toCommonJS(json_path_exports);
var import_get_text = require("../../../get-text.js");
var import_escape_characters = require("../../escape-characters.js");
var import_node_mutations = require("../node-mutations.js");
var import_identifier = require("../value/identifier.js");
function convertJsonPath(node, doc, parserConfig) {
  const { from, to } = node;
  const cursor = node.node.cursor();
  let convertedNode = {
    type: "Error",
    range: {
      from,
      to
    },
    textValue: "",
    value: ""
  };
  const errors = [];
  const {
    convertedNode: { textValue, value }
  } = (0, import_identifier.convertIdentifier)(node, doc);
  (0, import_node_mutations.updateNodeValues)(convertedNode, textValue, textValue);
  let isValidJsonPath = true;
  if (parserConfig?.jsonPathConversion) {
    convertedNode = {
      ...convertedNode,
      type: "JSONPath"
    };
    let path = "$";
    let pushToPath = false;
    cursor.iterate(
      (jsonPathChildNode) => {
        const nodeName = jsonPathChildNode.name;
        if (nodeName === "JsonPathBase") {
          const text = (0, import_get_text.getTextFromRange)(jsonPathChildNode, doc);
          const cleanedUpRoot = (0, import_escape_characters.removeEscapeCharacters)(
            text.slice(0, -1).replace(/^"(.*(?<=^|[^\\])(?:\\\\)*)"$/, "$1")
          );
          convertedNode.root = cleanedUpRoot;
          return;
        }
        if (!pushToPath) {
          return;
        }
        if (nodeName === "JsonPathIdentifier" || nodeName === "JsonPathArray" || nodeName === "String" || nodeName === "JsonPathNameSelector") {
          const text = (0, import_get_text.getTextFromRange)(jsonPathChildNode, doc);
          switch (nodeName) {
            case "JsonPathIdentifier":
              path += `.${text}`;
              break;
            case "JsonPathArray":
              path += `${text}`;
              break;
            case "String":
              path += `[${text}]`;
              break;
          }
        }
        if (nodeName === "\u26A0") {
          isValidJsonPath = false;
        }
      },
      (node2) => {
        if (node2.name === "JsonPathBase") {
          pushToPath = true;
        }
      }
    );
    convertedNode.path = path;
  }
  if (!parserConfig?.jsonPathConversion || !isValidJsonPath) {
    convertedNode = {
      type: "Error",
      range: convertedNode.range,
      value,
      textValue
    };
    errors.push({ range: { from, to }, message: "invalidKey" });
  }
  return { convertedNode, errors };
}
