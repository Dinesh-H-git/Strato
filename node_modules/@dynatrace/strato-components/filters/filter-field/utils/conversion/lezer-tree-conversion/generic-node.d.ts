import type { SyntaxNodeRef } from '@lezer/common';
import type { FilterFieldLezerNodeType } from '../../../types/tree-nodes.js';
/**
 * Convert a default node with string value and text value.
 * @internal
 */
export declare function convertNode(node: SyntaxNodeRef, doc: string): {
    convertedNode: {
        type: "Error" | "Group" | "Key" | FilterFieldLezerNodeType | "Statement" | "JSONPath" | "IPAddress" | "UID" | "SmartscapeId" | "Timestamp" | "BigInt";
        textValue: string;
        value: string;
        range: {
            from: number;
            to: number;
        };
    };
    errors: never[];
};
