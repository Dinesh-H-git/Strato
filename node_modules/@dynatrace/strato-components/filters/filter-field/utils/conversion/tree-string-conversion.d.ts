import type { SimpleFilterFieldTree } from '../../types/conversion-tree-nodes.js';
/**
 * Conversion util that converts a filter field tree into a string.
 * @param tree - Filter field tree
 * @public
 */
export declare function convertFilterFieldTreeToString(tree?: SimpleFilterFieldTree): string;
