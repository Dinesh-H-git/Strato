import type { PropsWithChildren } from 'react';
import type { FilterFieldSuggestion } from './Suggestion.types.js';
import type { AriaLabelingProps } from '../../../core/types/a11y-props.js';
import type { BehaviorTrackingProps } from '../../../core/types/behavior-tracking-props.js';
import type { DataTestId } from '../../../core/types/data-props.js';
import type { DOMProps } from '../../../core/types/dom.js';
import type { StylingProps } from '../../../core/types/styling-props.js';
/**
 * FilterFieldSuggestionGroup properties
 * @public
 */
export type FilterFieldSuggestionGroup = DOMProps & AriaLabelingProps & {
    /**
     * Label rendered for the suggestion group.
     * If a `FilterField.SuggestionGroupLabel` is present, it will override the label prop.
     * @defaultValue undefined
     */
    label?: string;
    /**
     * Suggestions rendered inside the group.
     * If both the suggestions prop and children are present, they will both be rendered.
     * @defaultValue undefined
     */
    suggestions?: FilterFieldSuggestion[];
};
/**
 * Accepted properties for FilterField suggestion group.
 * @public
 */
export type FilterFieldSuggestionGroupProps = PropsWithChildren<FilterFieldSuggestionGroup & AriaLabelingProps & StylingProps & DataTestId & BehaviorTrackingProps>;
