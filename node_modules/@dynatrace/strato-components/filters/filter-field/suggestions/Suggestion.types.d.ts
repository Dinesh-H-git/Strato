import type { PropsWithChildren, ReactNode } from 'react';
import type { AriaLabelingProps } from '../../../core/types/a11y-props.js';
import type { BehaviorTrackingProps } from '../../../core/types/behavior-tracking-props.js';
import type { DataTestId } from '../../../core/types/data-props.js';
import type { DOMProps } from '../../../core/types/dom.js';
import type { StylingProps } from '../../../core/types/styling-props.js';
import type { FilterFieldNodeBase } from '../types/tree-nodes.js';
import type { FilterFieldPrimitive } from '../types/validation.js';
/**
 * FilterFieldSuggestion properties.
 * @public
 */
export type FilterFieldSuggestion = {
    /**
     * The value used when applying the suggestion.
     * @defaultValue undefined
     */
    value?: string;
    /**
     * The display value to show for the suggestion.
     */
    displayValue?: ReactNode;
    /**
     * Additional information to render for the suggestion.
     */
    details?: ReactNode;
    /**
     * The display value to show for the suggestion.
     */
    icon?: ReactNode;
    /**
     * The characters to add to the suggestion when applying.
     * @defaultValue space
     */
    trailing?: string;
    /**
     * Insertion mode used when applying the suggestion.
     * @defaultValue 'replace-token'
     */
    insertionStrategy?: 'replace-token' | 'replace-statement' | 'replace-all' | 'insert';
    /**
     * If provided, the suggestion will be inserted at the given range,
     * overriding all existing content in that range.
     * @defaultValue undefined
     */
    insertionRange?: FilterFieldNodeBase['range'];
    /**
     * Whether the suggestion should be escaped when inserted.
     * @defaultValue undefined
     */
    escapeSuggestion?: boolean;
    /**
     * Whether the cursor should be set before the trailing characters.
     * @defaultValue false
     */
    cursorBeforeTrailing?: boolean;
    /**
     * Defines the type of the suggestion and provides a type indication by default.
     * When used with the `ValidatorMap` it may be derived from their predicates.
     */
    valueType?: FilterFieldPrimitive | string;
    /**
     * Defines the suggestions category.
     */
    suggestionType?: 'validatorMap' | 'hint-useAsKey' | 'hint-startTyping' | 'hint-useAsValue' | 'JSONPath' | 'hint-wildcard' | 'pastedContent';
};
/**
 * Accepted properties for FilterFieldSuggestion.
 * @public
 */
export type FilterFieldSuggestionProps = PropsWithChildren<StylingProps & DataTestId & DOMProps & AriaLabelingProps & BehaviorTrackingProps & FilterFieldSuggestion>;
/**
 * Single suggestion item rendered in a virtualized list of suggestions.
 * Groups are decomposed into individual header + suggestion items.
 * @internal
 */
export type FlattenedSuggestionItem = {
    kind: 'suggestion';
    data: FilterFieldSuggestion;
    groupId?: string;
} | {
    kind: 'group-header';
    label?: ReactNode;
    groupId: string;
} | {
    kind: 'react-node';
    node: ReactNode;
    groupId?: string;
};
/**
 * Group id and label rendered visually hidden to properly announce group headers to
 * screen readers when navigating through suggestions.
 * @internal
 */
export type GroupMetadata = {
    groupId: string;
    label: ReactNode;
};
