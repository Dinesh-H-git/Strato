import type { FilterFieldStatementNode, FilterFieldTree, FilterFieldValueNode } from './tree-nodes.js';
import type { FilterFieldSuggestionGroup } from '../suggestions/SuggestionGroup.types.js';
/**
 * Suggestions grouped by category.
 * @public
 */
export type FilterFieldGroupedSuggestions = {
    [key in 'value' | 'key' | 'comparisonOperator' | (string & {})]?: FilterFieldSuggestionGroup;
};
/**
 * Callback triggered when the FilterField suggestions change.
 * @public
 */
export type FilterFieldSuggestionsCallback = (
/** Contains the new state of FilterField suggestions. It includes information about the last token before the cursor position, the current value, an
 * array of default suggestions, the current syntax tree, the current cursor position, the suggestion type expected for the current cursor position, and
 * the filter statement at the current cursor position, if any.
 */
state?: {
    /** The types of suggestions that should be shown for the current cursor position. */
    suggestionTypes: ('key' | 'value' | 'comparisonOperator' | 'pastedContent' | 'none')[];
    /** The token at the current cursor position for the respective type of suggestion. */
    currentTokens: {
        type: NonNullable<Parameters<FilterFieldSuggestionsCallback>[0]>['suggestionTypes'][number];
        token: FilterFieldValueNode;
    }[];
    /** The current value of the filter field. */
    value: string;
    /** Suggestions to display for the current cursor position, grouped by suggestion type. */
    groupedSuggestions: FilterFieldGroupedSuggestions;
    /** Syntax tree of the current value. */
    syntaxTree: FilterFieldTree;
    /** The current position of the cursor. */
    cursorPosition: number;
    /** The filter statement at the current cursor position. */
    currentStatement?: FilterFieldStatementNode;
}) => void;
/** @internal */
export type FilterFieldSuggestionType = NonNullable<Parameters<FilterFieldSuggestionsCallback>[0]>['suggestionTypes'];
