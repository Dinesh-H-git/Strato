import type { Transaction } from '@codemirror/state';
import type { ViewUpdate, EditorView } from '@codemirror/view';
import type { MutableRefObject } from 'react';
import { type PastingInfo } from './editor-config.js';
/**
 * Update handler to manage pasting transactions.
 * @internal
 */
export declare function handlePastingTransaction(update: ViewUpdate, pastingInfoRef: MutableRefObject<PastingInfo>, editorView: EditorView, editorViewRef: MutableRefObject<EditorView | null>): void;
/**
 * function that returns the changed range of a pasting viewupdate
 * @internal
 */
export declare function getChangedRange(update: ViewUpdate): {
    from: number;
    to: number;
} | null;
/**
 * We have to handle controlled pasting separately as we don't have a ViewUpdate
 * and can't directly dispatch the updates to the statefield as we would trigger
 * a transaction within a transaction, which would crash CodeMirror.
 * @internal
 */
export declare function handleControlledPasting(transaction: Transaction, pastingInfoRef: MutableRefObject<PastingInfo>): void;
