import type { ReactElement, ReactNode } from 'react';
import type { BehaviorTrackingProps } from '../../../../core/types/behavior-tracking-props.js';
/**
 * @public
 */
export type TimeframeSelectorDisplayValueProps = BehaviorTrackingProps & {
    children?: ReactNode | ((customTriggerProps: {
        displayValue: ReactNode;
    }) => ReactElement);
};
/**
 * Typeguard function that will check if a Node is a TimeframeSelector.DisplayValue
 * @internal
 */
export declare function isTimeframeSelectorDisplayValueElement(child: ReactNode): child is ReactElement<TimeframeSelectorDisplayValueProps>;
/**
 * `TimeframeSelector.DisplayValue` defines a custom display value within the Trigger. A TimeframeSelector.DisplayValue component
 * is only valid within a TimeframeSelector.Trigger.
 * @public
 */
export declare const DisplayValue: {
    (props: TimeframeSelectorDisplayValueProps): import("react/jsx-runtime").JSX.Element | null;
    displayName: string;
};
