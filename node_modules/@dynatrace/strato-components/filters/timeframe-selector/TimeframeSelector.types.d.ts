import type { PropsWithChildren } from 'react';
import type { RequireAtLeastOne } from 'type-fest';
import type { BehaviorTrackingProps } from '../../core/types/behavior-tracking-props.js';
import type { DataTestId } from '../../core/types/data-props.js';
import type { FormControlProps } from '../../core/types/form-control-props.js';
import type { MaskingProps } from '../../core/types/masking-props.js';
import type { StylingProps } from '../../core/types/styling-props.js';
import type { TimeDetails, Timeframe, TimeValue } from '../../core/types/time.js';
/**
 * Accepted properties for the TimeframeSelector.
 * @public
 */
export type TimeframeSelectorProps = FormControlProps<RequireAtLeastOne<{
    from: string;
    to: string;
}> | RequireAtLeastOne<Timeframe> | null, (value: Timeframe | null) => void> & StylingProps & DataTestId & PropsWithChildren & MaskingProps & BehaviorTrackingProps & {
    /**
     * Shows the "Clear selection" button if set to true.
     * @defaultValue false
     * @deprecated The `clearable` prop is deprecated in favor of the new clear button directly located in the input.
     */
    clearable?: boolean;
    /**
     * The ISODatetime string of the earliest datetime that can be configured.
     * @defaultValue '1677-09-21T00:12:43.145224192Z'
     */
    min?: string;
    /**
     * The ISODatetime string of the latest datetime that can be configured.
     * @defaultValue '2262-04-11T23:47:16.854775807Z'
     */
    max?: string;
    /**
     * The precision of the time shown in the display value.
     * @defaultValue 'minutes'
     */
    precision?: 'minutes' | 'seconds' | 'milliseconds';
    /**
     * Whether the TimeframeStepper is shown.
     * @defaultValue true
     */
    stepper?: boolean;
};
/**
 * @deprecated The `TimeframeDetails` interface will be removed, please use `Timeframe` instead.
 * @public
 */
export interface TimeframeDetails {
    /**
     * Lower boundary of the timeframe in detailed format.
     * Contains information about the from date in Date and normalized string format, as well as
     * the type of the given time, which can either be an ISO string
     * or a relative time expression (e.g. Last 30 minutes).
     */
    from: TimeDetails;
    /**
     * Upper boundary of the timeframe in detailed format.
     * Contains information about the to date in Date and normalized string format, as well as
     * the type of the given time, which can either be an ISO string
     * or a relative time expression (e.g. Last 30 minutes).
     */
    to: TimeDetails;
}
/** @internal */
export interface TimeframeValueDetails {
    /**
     * Lower boundary of the timeframe in detailed format.
     * Contains information about the from date in Date and normalized string format, as well as
     * the type of the given time, which can either be an ISO string
     * or a relative time expression (e.g. Last 30 minutes).
     */
    from: TimeValue;
    /**
     * Upper boundary of the timeframe in detailed format.
     * Contains information about the to date in Date and normalized string format, as well as
     * the type of the given time, which can either be an ISO string
     * or a relative time expression (e.g. Last 30 minutes).
     */
    to: TimeValue;
}
