var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useTooltipHandler_exports = {};
__export(useTooltipHandler_exports, {
  useTooltipHandler: () => useTooltipHandler
});
module.exports = __toCommonJS(useTooltipHandler_exports);
var import_react = require("react");
var import_useSegments = require("./useSegments.js");
const useTooltipHandler = (readableTrigger) => {
  const [ref, setRef] = (0, import_react.useState)(null);
  const [isOpen, setIsOpen] = (0, import_react.useState)(false);
  const { segments } = (0, import_useSegments.useSegments)();
  const textEllipsisRef = (0, import_react.useCallback)((ref2) => {
    if (ref2) {
      setRef(ref2);
    }
  }, []);
  const onOpenTooltip = (0, import_react.useCallback)(
    (open) => {
      if (!open) {
        setIsOpen(false);
        return;
      }
      const noSelection = !segments || segments.length === 0;
      const contentFits = !(ref && ref.scrollWidth !== ref.clientWidth);
      const moreSegmentsSelected = segments.length > 1;
      const singleSegmentWithVariables = segments.length === 1 && (segments?.[0]?.variables?.[0]?.values.length ?? 0) > 1;
      setIsOpen(
        noSelection || !readableTrigger || moreSegmentsSelected || !contentFits || singleSegmentWithVariables
      );
    },
    [segments, readableTrigger, ref]
  );
  return {
    onOpenTooltip,
    isOpen,
    textEllipsisRef
  };
};
