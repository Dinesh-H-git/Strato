import type { BehaviorTrackingProps } from '../../core/types/behavior-tracking-props.js';
import type { DataTestId } from '../../core/types/data-props.js';
import type { DOMProps } from '../../core/types/dom.js';
import type { FormControlBaseProps } from '../../core/types/form-control-props.js';
import type { MaskingProps } from '../../core/types/masking-props.js';
import type { StylingProps } from '../../core/types/styling-props.js';
import type { WithChildren } from '../../core/types/with-children.js';
import { FormControlWithOverlayRef } from '../../forms/shared-types.js';
/**
 * Accepted properties for SegmentSelector
 * @public
 */
export type SegmentSelectorProps = DOMProps & WithChildren & StylingProps & DataTestId & MaskingProps & BehaviorTrackingProps & Omit<FormControlBaseProps, 'onChange' | 'controlState'> & {
    /**
     * Configures the style of the trigger.
     * @defaultValue 'default
     */
    variant?: 'default' | 'compact';
    /** Callback that is triggered when the open state of the SegmentSelector's overlay changes its open state. */
    onOpenChange?: (isOpen: boolean) => void;
};
/**
 * Ref for SegmentSelector.
 * @public
 */
export interface SegmentSelectorRef<T extends HTMLElement = HTMLDivElement> extends FormControlWithOverlayRef<T> {
    /** Validates the form control and sets the validity state. */
    validate: () => Promise<void>;
}
