import { type ElementType } from 'react';
import { ButtonProps } from '../../../../buttons/button/Button.js';
/**
 * The copy permissions button shown in the SegmentSelector if the user doesn't have permissions to read segments.
 * @internal
 */
export declare const CopyPermissionsButton: (props: Pick<ButtonProps<ElementType>, "color" | "variant"> & import("react").RefAttributes<HTMLButtonElement>) => import("react").ReactElement | null;
