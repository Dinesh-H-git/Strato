var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var SegmentsUIContext_exports = {};
__export(SegmentsUIContext_exports, {
  UISegmentsContext: () => UISegmentsContext,
  UISegmentsProvider: () => UISegmentsProvider,
  useSegmentsUIContext: () => useSegmentsUIContext
});
module.exports = __toCommonJS(SegmentsUIContext_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_SegmentsUIDispatchContext = require("./SegmentsUIDispatchContext.js");
var import_SegmentsUIReducer = require("./SegmentsUIReducer.js");
var import_useSegments = require("../../hooks/useSegments.js");
const UISegmentsContext = (0, import_react.createContext)({
  uiSegments: [],
  hasSelection: false,
  readOnly: false,
  isTouched: false
});
const useSegmentsUIContext = () => {
  const { dispatch } = (0, import_react.useContext)(import_SegmentsUIDispatchContext.UISegmentsDispatchContext);
  function addUISegment(newSegment, isPlaceholder = false) {
    dispatch({
      type: "add",
      payload: { segments: newSegment, isPlaceholder }
    });
  }
  function removeUISegment(id) {
    if (id) {
      dispatch({ type: "remove", payload: { id } });
    }
  }
  function removeAllUISegments() {
    dispatch({ type: "removeAll" });
  }
  function updateUISegment(oldSegment, newSegment) {
    dispatch({ type: "update", payload: { newSegment, oldSegment } });
  }
  function clearPlaceholder() {
    dispatch({ type: "clearPlaceholder" });
  }
  function setUISegments(uiSegments, hasSelection) {
    dispatch({ type: "setUISegments", payload: { uiSegments, hasSelection } });
  }
  function setArrayIdentifierError(hasError, id) {
    dispatch({ type: "setArrayIdentifierError", payload: { hasError, id } });
  }
  return {
    removeAllUISegments,
    removeUISegment,
    addUISegment,
    updateUISegment,
    clearPlaceholder,
    setUISegments,
    setArrayIdentifierError
  };
};
const UISegmentsProvider = (props) => {
  const { children, readOnly, overlayState } = props;
  const { segments } = (0, import_useSegments.useSegments)();
  const hasSelection = segments.length > 0;
  const id = segments[0]?.id ?? crypto.randomUUID();
  const [state, dispatch] = (0, import_react.useReducer)(import_SegmentsUIReducer.segmentsUIReducer, {
    uiSegments: hasSelection ? segments : [{ id, isPlaceholder: true }],
    hasSelection,
    readOnly,
    focusedSegmentId: id,
    isTouched: false
  });
  const dispatchContext = (0, import_react.useMemo)(
    () => ({ dispatch, overlayState }),
    [dispatch, overlayState]
  );
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UISegmentsContext.Provider, { value: state, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_SegmentsUIDispatchContext.UISegmentsDispatchContext.Provider, { value: dispatchContext, children }) });
};
