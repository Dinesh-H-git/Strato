import type { ChangeEvent, Dispatch } from 'react';
import type { FilterSegment } from '@dynatrace-sdk/client-query';
import type { SegmentSelectorUIState } from './SegmentsUIReducer.js';
import { type FormFieldMessagesReducerAction } from '../../../../forms/forms-core/state/formfield-messages-state-reducer.js';
import type { FormFieldMessageContextState } from '../../../../forms/forms-core/types/formfield-messages.types.js';
import { type CachedSegment } from '../SegmentsCache/SegmentsCache.js';
/** @internal */
export declare function getErrorKey(uiSegment: (FilterSegment | CachedSegment['segment']) & {
    isPlaceholder?: boolean;
    errorKey?: 'unavailable' | 'needs-variables' | 'needs-update';
}): Promise<'unavailable' | 'needs-update' | 'needs-variables' | null>;
/**
 * Helper function that checks every segment for errors and returns a boolean indicating
 * whether the segments are valid or not.
 * If the user has no access rights, it returns undefined.
 * @internal
 */
export declare function areUiSegmentsValid(uiSegments?: SegmentSelectorUIState['uiSegments']): Promise<boolean | undefined>;
/**
 * Iterates over selected segments and checks their availability.
 * If they are no longer available it adds an error.
 * Similarly it removes the error if it no longer applies.
 */
export declare function validateSegments(segments: FilterSegment[], onChange: (event?: ChangeEvent<HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement>) => void, formFieldMessagesDispatch: Dispatch<FormFieldMessagesReducerAction> | undefined, formControlMessages: FormFieldMessageContextState['formControlMessages'], errorId: string, showError?: boolean): Promise<void>;
