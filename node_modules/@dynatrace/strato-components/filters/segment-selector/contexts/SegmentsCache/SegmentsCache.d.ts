import { QueryClient } from '@tanstack/react-query';
import { type FilterSegment, type DetailedFilterSegment } from '@dynatrace-sdk/client-filter-segment-management';
export type CachedSegment = {
    segment: Omit<DetailedFilterSegment | FilterSegment, 'uid'> & {
        id: string;
    };
    temporarilyApplied: boolean;
};
/**
 * @internal
 */
export declare const segmentsClient: {
    queryClient: QueryClient;
    hasAccessRights: boolean | undefined;
};
/**
 * Returns a Promise<CachedSegment>.
 * @internal
 */
export declare function getSegment(uid?: string): Promise<CachedSegment>;
/**
 * Returns a Promise for all Cached Segments.
 * @internal
 */
declare function getAllSegments(): Promise<Map<string, CachedSegment>>;
/**
 * Hook to access cached segments.
 * @internal
 * @returns
 */
export declare function useSegmentsCache(): {
    getAllSegments: typeof getAllSegments;
    getSegment: typeof getSegment;
};
export {};
