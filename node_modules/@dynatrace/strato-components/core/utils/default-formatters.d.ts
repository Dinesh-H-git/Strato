import { FormatDateOptions } from '@dynatrace-sdk/units';
/**
 * Default datetime format options (date + time),
 * e.g. "12/21/2024, 5:45:30 PM" for US English locale.
 * @internal
 */
export declare const DATETIME_DEFAULT_FORMAT_OPTIONS: FormatDateOptions;
/**
 * Formats the date with the default configuration
 * @internal
 */
export declare function formatDatetimeDefault(value: number | Date): string;
/**
 * Formats a given number with default currency configuration
 * (no currency symbol, but two fraction digits, e.g. 12345.6789 → 12,345.68).
 *
 * @param value - The number to be formatted as currency.
 * @param formatOptions - The format options configured by the consumer.
 *
 * @internal
 */
export declare function formatCurrencyDefault(value: number, formatOptions?: Omit<Intl.NumberFormatOptions, 'currency' | 'style'> & {
    locale?: string | string[];
    abbreviate?: boolean;
}): string;
