var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var threshold_utils_exports = {};
__export(threshold_utils_exports, {
  _checkCombinedThresholdRules: () => _checkCombinedThresholdRules,
  _matchesThreshold: () => _matchesThreshold,
  getThresholdColor: () => getThresholdColor
});
module.exports = __toCommonJS(threshold_utils_exports);
function _matchesThreshold(value, threshold) {
  if (typeof value === "string") {
    if (threshold.comparator === "equal-to") {
      return value === threshold.value;
    } else if (threshold.comparator === "not-equal-to") {
      return value !== threshold.value;
    }
    console.warn(
      `Threshold comparator ${threshold.comparator} is not supported for type string`
    );
    return false;
  }
  const comparator = threshold.comparator;
  switch (comparator) {
    case "greater-than":
      return value > threshold.value;
    case "less-than":
      return value < threshold.value;
    case "greater-than-or-equal-to":
      return value >= threshold.value;
    case "less-than-or-equal-to":
      return value <= threshold.value;
    case "equal-to":
      return value === threshold.value;
    case "not-equal-to":
      return value !== threshold.value;
    default:
      console.log(`Threshold comparator not supported: ${comparator}`);
      return false;
  }
}
function getThresholdColor(value, thresholds, forBackgroundColor = false, colorFn) {
  for (let i = thresholds.length - 1; i >= 0; i--) {
    const threshold = thresholds[i];
    if (_matchesThreshold(value, threshold)) {
      const color = colorFn ? colorFn(threshold) : forBackgroundColor ? threshold.backgroundColor : threshold.color;
      return color ?? null;
    }
  }
  return null;
}
function _checkCombinedThresholdRules(combinedRules, allIds, values, color, backgroundColor) {
  let thresholdColor;
  let thresholdBackgroundColor;
  for (let rule = combinedRules.length - 1; rule >= 0; rule--) {
    const currentRule = combinedRules[rule];
    const foundId = allIds.find((key) => currentRule.id === key);
    if (foundId) {
      const passedThreshold = _matchesThreshold(values[foundId], currentRule);
      if (passedThreshold) {
        thresholdColor = color;
        thresholdBackgroundColor = backgroundColor;
      } else {
        return {
          thresholdColor: void 0,
          thresholdBackgroundColor: void 0
        };
      }
    } else {
      return { thresholdColor: void 0, thresholdBackgroundColor: void 0 };
    }
  }
  return { thresholdColor, thresholdBackgroundColor };
}
