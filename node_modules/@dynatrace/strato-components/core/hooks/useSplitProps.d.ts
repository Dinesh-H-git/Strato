type Matcher<T> = (key: keyof T, value: T[keyof T]) => boolean;
/**
 * Split props so we can provide them for different components.
 * @internal
 */
export declare function useSplitProps<T extends Record<string, unknown>>(props: T, matchers: Matcher<T>[]): {
    matchedProps: Partial<T>;
    remainingProps: Partial<T>;
};
export {};
