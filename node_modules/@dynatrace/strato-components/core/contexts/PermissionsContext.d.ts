export interface PermissionsContextValue {
    isAllowed: boolean;
    isLoading: boolean;
    tooltipText: string;
    deniedPermissions: string[];
}
export declare const DEFAULT_TOOLTIP_TEXT = "You don't have the required permissions";
export declare const defaultPermissionsContextValue: PermissionsContextValue;
/**
 * Context that broadcasts the resolved permission state to descendant form components.
 * When no PermissionsProvider is present, the default value leaves components unaffected
 * (isAllowed: true).
 * @internal
 */
export declare const PermissionsContext: import("react").Context<PermissionsContextValue>;
