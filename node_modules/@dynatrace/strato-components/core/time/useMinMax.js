var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useMinMax_exports = {};
__export(useMinMax_exports, {
  useMinMax: () => useMinMax,
  useMinMaxStoreSync: () => useMinMaxStoreSync
});
module.exports = __toCommonJS(useMinMax_exports);
var import_react = require("react");
var import_untilMidnight = require("./untilMidnight.js");
var import_useIntervalCallback = require("./useIntervalCallback.js");
var import_timeParser = require("../../forms/forms-core/time/timeParser.js");
function useMinMax(propMin, propMax, precision) {
  const { value: min } = (0, import_useIntervalCallback.useIntervalCallback)(
    () => (0, import_timeParser.getTimeValue)(propMin, precision) ?? null,
    [propMin, precision],
    (0, import_untilMidnight.untilMidnight)(propMin),
    import_untilMidnight.ONE_DAY_IN_MILLISECONDS
  );
  const { value: max } = (0, import_useIntervalCallback.useIntervalCallback)(
    () => (0, import_timeParser.getTimeValue)(propMax, precision) ?? null,
    [propMax, precision],
    (0, import_untilMidnight.untilMidnight)(propMax),
    import_untilMidnight.ONE_DAY_IN_MILLISECONDS
  );
  return { min: min ?? null, max: max ?? null };
}
function useMinMaxStoreSync(store, min, max, applyChange) {
  (0, import_react.useEffect)(() => {
    store.setState((state) => ({
      ...state,
      min,
      max,
      ...applyChange(state, min, max)
    }));
  }, [min, max, store, applyChange]);
}
