var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useTimeframeStore_exports = {};
__export(useTimeframeStore_exports, {
  useCalendarStore: () => useCalendarStore,
  useDateTimePickerStore: () => useDateTimePickerStore,
  useDateTimeRangePickerStore: () => useDateTimeRangePickerStore,
  useTimeframeSelectorStore: () => useTimeframeSelectorStore
});
module.exports = __toCommonJS(useTimeframeStore_exports);
var import_react = require("react");
var import_zustand = require("zustand");
var import_timeframeStoreProvider = require("./timeframeStoreProvider.js");
function useCalendarStore(selector) {
  const store = (0, import_react.useContext)(import_timeframeStoreProvider.CalendarStoreCtx);
  if (!store) {
    throw new Error(
      "useCalendarStore must be used within a CalendarStoreProvider"
    );
  }
  return (0, import_zustand.useStore)(store, selector);
}
function useDateTimePickerStore(selector) {
  const store = (0, import_react.useContext)(import_timeframeStoreProvider.DateTimePickerStoreCtx);
  if (!store) {
    throw new Error(
      "useDateTimePickerStore must be used within a DateTimePickerStoreProvider"
    );
  }
  return (0, import_zustand.useStore)(store, selector);
}
function useDateTimeRangePickerStore(selector) {
  const store = (0, import_react.useContext)(import_timeframeStoreProvider.DateTimeRangePickerStoreCtx);
  if (!store) {
    throw new Error(
      "useDateTimeRangePickerStore must be used within a DateTimeRangePickerStoreProvider"
    );
  }
  return (0, import_zustand.useStore)(store, selector);
}
function useTimeframeSelectorStore(selector) {
  const store = (0, import_react.useContext)(import_timeframeStoreProvider.TimeframeSelectorStoreCtx);
  if (!store) {
    throw new Error(
      "useTimeframeSelectorStore must be used within a TimeframeSelectorStoreProvider"
    );
  }
  return (0, import_zustand.useStore)(store, selector);
}
