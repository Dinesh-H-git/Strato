var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var untilMidnight_exports = {};
__export(untilMidnight_exports, {
  ONE_DAY_IN_MILLISECONDS: () => ONE_DAY_IN_MILLISECONDS,
  untilMidnight: () => untilMidnight
});
module.exports = __toCommonJS(untilMidnight_exports);
var import_tz = require("@date-fns/tz");
var import_units = require("@dynatrace-sdk/units");
var import_user_preferences = require("@dynatrace-sdk/user-preferences");
const ONE_DAY_IN_MILLISECONDS = 1e3 * 60 * 60 * 24;
function untilMidnight(isoString, relativeNow) {
  const timezone = (0, import_user_preferences.getTimezone)();
  const date = new import_tz.TZDateMini(
    (0, import_units.parseTimeAsTimeValue)(isoString, relativeNow)?.absoluteDate ?? "",
    timezone
  );
  if (Number.isNaN(date.getTime())) {
    return ONE_DAY_IN_MILLISECONDS;
  }
  const midnight = new import_tz.TZDateMini(
    date.getFullYear(),
    date.getMonth(),
    date.getDate() + 1,
    timezone
  );
  return midnight.getTime() - date.getTime();
}
