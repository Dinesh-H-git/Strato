var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ModalContextProvider_exports = {};
__export(ModalContextProvider_exports, {
  ModalContextProvider: () => ModalContextProvider
});
module.exports = __toCommonJS(ModalContextProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_ModalContext = require("../contexts/ModalContext.js");
function ModalContextProvider(props) {
  const { children } = props;
  const parent = (0, import_react.useContext)(import_ModalContext.ModalContext);
  const [modalCount, setModalCount] = (0, import_react.useState)(0);
  const contextValue = (0, import_react.useMemo)(
    () => ({
      parent,
      modalCount,
      addModal() {
        setModalCount((count) => count + 1);
        if (parent) {
          parent.addModal();
        }
      },
      removeModal() {
        setModalCount((count) => count - 1);
        if (parent) {
          parent.removeModal();
        }
      }
    }),
    [parent, modalCount]
  );
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_ModalContext.ModalContext.Provider, { value: contextValue, children });
}
