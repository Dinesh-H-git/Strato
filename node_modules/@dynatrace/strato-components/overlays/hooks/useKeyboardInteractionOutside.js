var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useKeyboardInteractionOutside_exports = {};
__export(useKeyboardInteractionOutside_exports, {
  useKeyboardInteractionOutside: () => useKeyboardInteractionOutside
});
module.exports = __toCommonJS(useKeyboardInteractionOutside_exports);
var import_react = require("react");
function isValidEvent(event, ref) {
  if (event.target) {
    const target = event.target;
    const ownerDocument = target.ownerDocument;
    if (!ownerDocument || !ownerDocument.documentElement.contains(target)) {
      return false;
    }
  }
  return ref.current && !ref.current.contains(event.target);
}
function useKeyboardInteractionOutside(props) {
  const { ref, onInteractOutside, isDisabled } = props;
  (0, import_react.useEffect)(() => {
    if (isDisabled) {
      return;
    }
    const onKeyUp = (event) => {
      if (onInteractOutside && isValidEvent(event, ref)) {
        onInteractOutside(event);
      }
    };
    document.addEventListener("keyup", onKeyUp, true);
    return () => {
      document.removeEventListener("keyup", onKeyUp, true);
    };
  }, [isDisabled, ref, onInteractOutside]);
}
