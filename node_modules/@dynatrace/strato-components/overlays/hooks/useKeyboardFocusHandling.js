var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useKeyboardFocusHandling_exports = {};
__export(useKeyboardFocusHandling_exports, {
  useKeyboardFocusHandling: () => useKeyboardFocusHandling
});
module.exports = __toCommonJS(useKeyboardFocusHandling_exports);
var import_react = require("react");
var import_get_last_focusable_child = require("../../core/utils/focus-management/get-last-focusable-child.js");
function useKeyboardFocusHandling(closeButtonRef, containerRef) {
  const prevFocusedElement = (0, import_react.useRef)(null);
  const closedOnKeydown = (0, import_react.useRef)(false);
  const focusCloseButton = (0, import_react.useCallback)(
    (e) => {
      if (e.ctrlKey && e.key === "F6" || e.metaKey && e.key === "F6") {
        if (closeButtonRef.current) {
          e.preventDefault();
          prevFocusedElement.current = document.activeElement;
          closeButtonRef.current?.focus();
        }
      }
    },
    [closeButtonRef]
  );
  const handleFocusOnKeydown = () => {
    if (closedOnKeydown.current) {
      const lastFocusableInContainer = containerRef?.current ? (0, import_get_last_focusable_child.getLastFocusableChild)(containerRef.current) : null;
      if (lastFocusableInContainer) {
        lastFocusableInContainer.focus();
      } else if (prevFocusedElement.current) {
        prevFocusedElement.current.focus();
        prevFocusedElement.current = null;
      } else {
        (0, import_get_last_focusable_child.getLastFocusableChild)(document.body)?.focus();
      }
      closedOnKeydown.current = false;
    }
  };
  (0, import_react.useEffect)(() => {
    window.addEventListener("keydown", focusCloseButton);
    return () => window.removeEventListener("keydown", focusCloseButton);
  }, [focusCloseButton]);
  return [handleFocusOnKeydown, closedOnKeydown];
}
