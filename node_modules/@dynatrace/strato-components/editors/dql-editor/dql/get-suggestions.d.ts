import { AutocompleteSuggestion } from '@dynatrace-sdk/client-query';
export type SuggestionCache = Map<string, {
    suggestions: AutocompleteSuggestion[];
    ttl?: number;
}>;
/** Gets memoized suggestions. */
export declare const getSuggestions: (query: string, cursor?: number, maxDataSuggestions?: number, cache?: SuggestionCache) => Promise<AutocompleteSuggestion[]>;
