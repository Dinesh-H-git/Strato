var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var key_maps_exports = {};
__export(key_maps_exports, {
  getKeyMaps: () => getKeyMaps
});
module.exports = __toCommonJS(key_maps_exports);
var import_autocomplete = require("@codemirror/autocomplete");
var import_commands = require("@codemirror/commands");
var import_view = require("@codemirror/view");
var import_indent_with_spaces = require("./indent-with-spaces.js");
const getKeyMaps = (viewRef) => {
  return [
    import_view.keymap.of([
      {
        key: "Enter",
        run: (target) => {
          const _selectedCompletion = (0, import_autocomplete.selectedCompletion)(target.state);
          const textToCursor = target.state.doc.slice(0, target.state.selection.main.to).toString();
          if (_selectedCompletion !== null && textToCursor.endsWith(_selectedCompletion?.label)) {
            (0, import_autocomplete.acceptCompletion)(target);
            return false;
          }
          return (0, import_autocomplete.acceptCompletion)(target);
        }
      },
      ...import_autocomplete.completionKeymap,
      ...import_autocomplete.closeBracketsKeymap,
      ...import_commands.standardKeymap,
      ...import_commands.historyKeymap,
      {
        key: "Mod-7",
        run: import_commands.toggleLineComment
      },
      {
        key: "Mod-/",
        run: import_commands.toggleLineComment
      },
      {
        key: "Shift-Alt-a",
        run: import_commands.toggleBlockComment
      },
      {
        key: "Shift-Alt-ArrowUp",
        run: import_commands.copyLineUp
      },
      {
        key: "Shift-Alt-ArrowDown",
        run: import_commands.copyLineDown
      },
      {
        key: "Tab",
        run: import_indent_with_spaces.indentWithSpaces,
        // SHIFT+TAB fix
        shift: import_commands.indentLess
      },
      {
        key: "Escape",
        run: (target) => {
          viewRef.current.dom.querySelector(".cm-scroller")?.focus();
          return true;
        }
      }
    ])
  ];
};
