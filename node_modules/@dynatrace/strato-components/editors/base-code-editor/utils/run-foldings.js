var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var run_foldings_exports = {};
__export(run_foldings_exports, {
  runFoldings: () => runFoldings
});
module.exports = __toCommonJS(run_foldings_exports);
var import_language = require("@codemirror/language");
var import_state = require("@codemirror/state");
var import_is_node_environment = require("../../../core/utils/is-node-environment.js");
function runFoldings(foldings, editorView) {
  const selection = editorView.state.selection;
  for (const foldPosition of foldings) {
    if (editorView.state.doc.length <= foldPosition) {
      if ((0, import_is_node_environment.isDev)()) {
        console.warn(
          `Editor folding position [${foldPosition}] exceeds text length [${editorView.state.doc.length}]. Folding will be ignored.`
        );
      }
      continue;
    }
    const transaction = editorView.state.update({
      selection: import_state.EditorSelection.cursor(foldPosition)
    });
    if (transaction) {
      editorView.dispatch(transaction);
    }
    (0, import_language.foldCode)(editorView);
  }
  const resetCursorTransaction = editorView.state.update({ selection });
  editorView.dispatch(resetCursorTransaction);
}
