import { AriaDisabledProps } from '../../../../core/types/a11y-props.js';
import type { BehaviorTrackingProps } from '../../../../core/types/behavior-tracking-props.js';
import { DataTestId } from '../../../../core/types/data-props.js';
import { DOMProps } from '../../../../core/types/dom.js';
import { MaskingProps } from '../../../../core/types/masking-props.js';
import { StylingProps } from '../../../../core/types/styling-props.js';
import { WithChildren } from '../../../../core/types/with-children.js';
import type { SharedFormFieldProps } from '../../types/formfield.types.js';
/**
 * @public
 */
export interface FormFieldProps extends WithChildren, SharedFormFieldProps, StylingProps, DataTestId, MaskingProps, DOMProps, AriaDisabledProps, BehaviorTrackingProps {
}
/**
 * The `FormField` component is the outermost layout component of an input
 * component (for example `TextInput`), providing support for displaying a
 * sibling `label` element alongside.
 *
 * It also features responsive design: small containers show the label elements
 * aligned on top of the input. In comparison, wider containers show input elements
 * with their label aligned to the left.
 *
 * The form field is child-agnostic.
 * @public
 */
export declare const FormField: (props: FormFieldProps & import("react").RefAttributes<HTMLDivElement>) => import("react").ReactElement | null;
