import type { BehaviorTrackingProps } from '../../../../core/types/behavior-tracking-props.js';
import { DataTestId } from '../../../../core/types/data-props.js';
import { DOMProps } from '../../../../core/types/dom.js';
import { StylingProps } from '../../../../core/types/styling-props.js';
import { WithChildren } from '../../../../core/types/with-children.js';
/**
 * @public
 */
export interface FormFieldMessageBaseItemProps extends WithChildren, StylingProps, DOMProps, DataTestId, BehaviorTrackingProps {
    /**
     * Determines the style of the message (text color, icon).
     * Error messages additionally prevent the form component
     * from being submitted in a form.
     */
    variant?: 'error' | 'hint';
    /**
     * Whether the connected input is currently disabled.
     * @defaultValue false
     */
    disabled?: boolean;
}
/**
 * Renders a Form Field Message.
 * @internal
 */
export declare const InternalBaseItem: {
    (props: FormFieldMessageBaseItemProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
