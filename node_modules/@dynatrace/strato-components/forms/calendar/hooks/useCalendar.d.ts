import type { CalendarProps, CalendarStrictValueType } from '../Calendar.types.js';
import { type CalendarType } from '../types/calendarConfiguration.js';
import type { CalendarState } from '../types/shared-types.js';
/** @internal */
export type UseCalendarProps<Type extends CalendarType> = Pick<CalendarProps<Type>, 'onChange' | 'type' | 'precision'> & {
    value: CalendarStrictValueType<Type>;
};
/**
 * Calendar hook to initialize the calendar state and update it to prop changes.
 * @internal
 */
export declare function useCalendar<Type extends CalendarType>({ type, }: UseCalendarProps<Type>): {
    state: CalendarState;
    dispatch: import("react").Dispatch<import("../types/shared-types.js").CalendarReducerAction>;
    uniqueId: string;
};
