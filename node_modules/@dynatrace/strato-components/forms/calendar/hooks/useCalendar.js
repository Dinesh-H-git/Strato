var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useCalendar_exports = {};
__export(useCalendar_exports, {
  useCalendar: () => useCalendar
});
module.exports = __toCommonJS(useCalendar_exports);
var import_tz = require("@date-fns/tz");
var import_date_fns = require("date-fns");
var import_react = require("react");
var import_user_preferences = require("@dynatrace-sdk/user-preferences");
var import_useId = require("../../../core/hooks/useId.js");
var import_helpers = require("../../../core/time/helpers.js");
var import_useTimeframeStore = require("../../../core/time/timeframeStore/useTimeframeStore.js");
var import_CalendarContext = require("../contexts/CalendarContext.js");
var import_calendar_state_reducer = require("../state/calendar-state-reducer.js");
function useCalendar({
  // eslint-disable-next-line @typescript-eslint/prefer-ts-expect-error, @typescript-eslint/ban-ts-comment
  // @ts-ignore Strict mode transition - needs to be investigated
  type = "single"
}) {
  const uniqueId = (0, import_useId.useId)();
  const configuredTimezone = (0, import_user_preferences.getTimezone)();
  const calendarValue = (0, import_useTimeframeStore.useCalendarStore)((state2) => state2.calendarValue);
  const activeISODate = (0, import_helpers.isTimeValue)(calendarValue) ? calendarValue?.absoluteDate : calendarValue?.from?.absoluteDate;
  const today = (0, import_date_fns.startOfDay)(import_tz.TZDateMini.tz(configuredTimezone));
  const parsedActiveDate = new import_tz.TZDateMini(
    activeISODate ?? today.toISOString(),
    configuredTimezone
  );
  const initialState = {
    ...import_CalendarContext.initialState,
    type,
    activeDate: (0, import_date_fns.isValid)(parsedActiveDate) ? parsedActiveDate : today
  };
  const [state, dispatch] = (0, import_react.useReducer)(
    import_calendar_state_reducer.reducer,
    // activeDate might be undefined but will be set in the initializer, to ensure the correct timezone.
    initialState,
    (state2) => {
      return {
        ...state2,
        activeDate: state2.activeDate ?? today,
        today
      };
    }
  );
  return { state, dispatch, uniqueId };
}
