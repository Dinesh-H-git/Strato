import { type ReactElement, type ReactNode } from 'react';
import type { NumberSpinButtonProps, NumberSpinButtonRef } from './types.js';
/**
 * Use the NumberSpinButton component to show a number that can be changed using arrow and page keys.
 * @internal
 */
export declare const NumberSpinButton: (props: NumberSpinButtonProps & import("react").RefAttributes<NumberSpinButtonRef>) => import("react").ReactElement | null;
/**
 * Allows to check if a ReactNode is a NumberSpinButton element.
 * @internal
 */
export declare function isNumberSpinButton(node: ReactNode): node is ReactElement<NumberSpinButtonProps>;
