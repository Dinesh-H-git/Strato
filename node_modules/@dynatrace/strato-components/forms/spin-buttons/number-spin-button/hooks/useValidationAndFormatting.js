var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useValidationAndFormatting_exports = {};
__export(useValidationAndFormatting_exports, {
  useValidationAndFormatting: () => useValidationAndFormatting
});
module.exports = __toCommonJS(useValidationAndFormatting_exports);
var import_lodash_es = require("lodash");
var import_react = require("react");
var import_constants = require("../constants.js");
const reParseValue = new RegExp("(-?\\d+)$");
const useValidationAndFormatting = (displayFormat, required, minValue = import_constants.NUMBER_SPIN_BUTTON_DEFAULTS.MIN_VALUE, maxValue = import_constants.NUMBER_SPIN_BUTTON_DEFAULTS.MAX_VALUE) => {
  const formatLength = displayFormat?.length ?? 0;
  const formatChars = displayFormat?.chars ?? import_constants.NUMBER_SPIN_BUTTON_DEFAULTS.PADDING_CHARS;
  const parse = (0, import_react.useCallback)((input) => {
    if (input === "") {
      return null;
    }
    const match = input.match(reParseValue);
    if (!match) {
      return null;
    }
    const [value] = match;
    return parseInt(value);
  }, []);
  const validate = (0, import_react.useCallback)(
    (value) => {
      if (required && value === null) {
        return false;
      }
      return value === null || value === void 0 || !Number.isNaN(value) && value >= minValue && value <= maxValue;
    },
    [maxValue, minValue, required]
  );
  const format = (0, import_react.useCallback)(
    (input) => {
      if (input === null || input === "") {
        return "";
      }
      const text = typeof input === "string" ? input : (0, import_lodash_es.isNil)(input) || Number.isNaN(input) ? "" : input.toString();
      if (formatLength === 0) {
        return text;
      }
      return text.padStart(formatLength, formatChars);
    },
    [formatLength, formatChars]
  );
  return { validate, parse, format };
};
