var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useValueNavigation_exports = {};
__export(useValueNavigation_exports, {
  useValueNavigation: () => useValueNavigation
});
module.exports = __toCommonJS(useValueNavigation_exports);
var import_react = require("react");
var import_useValidationAndFormatting = require("./useValidationAndFormatting.js");
var import_helper = require("../../helper.js");
var import_useSelection = require("../../hooks/useSelection.js");
var import_constants = require("../constants.js");
const useValueNavigation = (ref, props, applyText) => {
  const {
    disabled,
    readOnly,
    onChange,
    displayFormat,
    required,
    minValue,
    maxValue,
    value,
    isCyclic
  } = props;
  const { selectAll } = (0, import_useSelection.useSelection)(ref);
  const { format } = (0, import_useValidationAndFormatting.useValidationAndFormatting)(
    displayFormat,
    required,
    minValue,
    maxValue
  );
  const {
    onArrowUp = (value2, e) => {
      e.preventDefault();
      return (value2 ?? 0) + import_constants.NUMBER_SPIN_BUTTON_DEFAULTS.SMALL_STEP;
    },
    onArrowDown = (value2, e) => {
      e.preventDefault();
      return (value2 ?? 0) - import_constants.NUMBER_SPIN_BUTTON_DEFAULTS.SMALL_STEP;
    },
    onPageUp = (value2, e) => {
      e.preventDefault();
      return (value2 ?? 0) + import_constants.NUMBER_SPIN_BUTTON_DEFAULTS.BIG_STEP;
    },
    onPageDown = (value2, e) => {
      e.preventDefault();
      return (value2 ?? 0) - import_constants.NUMBER_SPIN_BUTTON_DEFAULTS.BIG_STEP;
    },
    onHome = (value2, e) => {
      if (minValue !== void 0) {
        e.preventDefault();
        return minValue;
      }
      return value2;
    },
    onEnd = (value2, e) => {
      if (maxValue !== void 0) {
        e.preventDefault();
        return maxValue;
      }
      return value2;
    }
  } = props;
  const onKeyDownHandler = (0, import_react.useCallback)(
    (e) => {
      if (disabled || readOnly) {
        return;
      }
      if (e.shiftKey || e.key === "Shift") {
        return;
      }
      const safeValue = value !== null && value !== void 0 && !Number.isNaN(value) ? value : null;
      const updateValue = (newValue) => {
        const sanitizedValue = (0, import_helper.constrainValue)(
          newValue,
          minValue,
          maxValue,
          isCyclic
        );
        if (safeValue === sanitizedValue) {
          return;
        }
        const newText = format(sanitizedValue);
        applyText?.(newText);
        onChange?.(sanitizedValue);
        selectAll();
      };
      switch (e.key) {
        case "ArrowUp":
          updateValue(onArrowUp(safeValue ?? 0, e));
          break;
        case "ArrowDown":
          updateValue(onArrowDown(safeValue ?? 0, e));
          break;
        case "PageUp":
          updateValue(onPageUp(safeValue ?? 0, e));
          break;
        case "PageDown":
          updateValue(onPageDown(safeValue ?? 0, e));
          break;
        case "Home":
          updateValue(onHome(safeValue ?? 0, e));
          break;
        case "End":
          updateValue(onEnd(safeValue ?? 0, e));
          break;
      }
    },
    [
      disabled,
      readOnly,
      minValue,
      maxValue,
      isCyclic,
      value,
      format,
      applyText,
      onChange,
      selectAll,
      onArrowUp,
      onArrowDown,
      onPageUp,
      onPageDown,
      onHome,
      onEnd
    ]
  );
  return { onKeyDownHandler };
};
