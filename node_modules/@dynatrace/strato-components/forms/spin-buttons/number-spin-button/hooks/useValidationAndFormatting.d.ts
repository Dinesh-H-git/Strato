import { NumberSpinButtonDisplayFormat } from '../types.js';
/**
 * This hook provides helper functions to parse, validate and format input values.
 */
export declare const useValidationAndFormatting: (displayFormat?: NumberSpinButtonDisplayFormat, required?: boolean, minValue?: number, maxValue?: number) => {
    validate: (value: number | null | undefined) => boolean;
    parse: (input: string) => number | null;
    format: (input: string | number | null) => string;
};
