type LocaleSeparators = {
    decimal: string;
    group: string | null;
};
/**
 * Parses a value into a JavaScript number (Replace commas with dots).
 * @internal
 */
export declare function parseNumber(value: string | number): number;
/**
 * Resolves decimal and grouping separators for a given locale.
 * @internal
 */
export declare function getLocaleSeparators(locale: string, options?: Intl.NumberFormatOptions): LocaleSeparators;
/**
 * Normalizes numeric text to locale separators and removes grouping separators.
 * @internal
 */
export declare function checkFormat(value: string): string;
export {};
