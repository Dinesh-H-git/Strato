var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_select_value_exports = {};
__export(use_select_value_exports, {
  _extractSelectedOptions: () => _extractSelectedOptions,
  useSelectValue: () => useSelectValue
});
module.exports = __toCommonJS(use_select_value_exports);
var import_lodash_es = require("lodash");
var import_use_select_options = require("./use-select-options.js");
function _extractSelectedOptions(value, options, multiple) {
  if (multiple === false) {
    for (const option of options) {
      if ((0, import_use_select_options._isOption)(option) && (0, import_lodash_es.isEqual)(option.value, value)) {
        return option;
      }
      if ((0, import_use_select_options._isGroup)(option)) {
        for (const nestedOption of option.options) {
          if ((0, import_use_select_options._isOption)(nestedOption) && (0, import_lodash_es.isEqual)(nestedOption.value, value)) {
            return nestedOption;
          }
        }
      }
    }
  }
  if (multiple === true && Array.isArray(value)) {
    const selectedOptions = [];
    for (const option of options) {
      if ((0, import_use_select_options._isOption)(option)) {
        for (const [index, partialValue] of value.entries()) {
          if ((0, import_lodash_es.isEqual)(option.value, partialValue)) {
            selectedOptions[index] = option;
          }
        }
      }
      if ((0, import_use_select_options._isGroup)(option)) {
        for (const nestedOption of option.options) {
          if ((0, import_use_select_options._isOption)(nestedOption)) {
            for (const [index, partialValue] of value.entries()) {
              if ((0, import_lodash_es.isEqual)(nestedOption.value, partialValue)) {
                selectedOptions[index] = nestedOption;
              }
            }
          }
        }
      }
    }
    if (selectedOptions.length > 0) {
      return selectedOptions;
    }
  }
  if (multiple === true) {
    return [];
  } else {
    return null;
  }
}
function useSelectValue({
  value,
  defaultValue,
  options,
  multiple
}) {
  let controlledValues = void 0;
  let uncontrolledValues = void 0;
  if (value !== void 0) {
    controlledValues = _extractSelectedOptions(
      value,
      options,
      multiple
    );
  } else if (defaultValue !== void 0) {
    uncontrolledValues = defaultValue !== void 0 ? _extractSelectedOptions(
      defaultValue,
      options,
      multiple
    ) : void 0;
  }
  return {
    value: controlledValues,
    defaultValue: uncontrolledValues
  };
}
