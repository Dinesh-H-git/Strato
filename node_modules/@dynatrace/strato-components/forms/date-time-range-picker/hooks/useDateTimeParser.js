var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useDateTimeParser_exports = {};
__export(useDateTimeParser_exports, {
  useDateTimeParser: () => useDateTimeParser
});
module.exports = __toCommonJS(useDateTimeParser_exports);
var import_react = require("react");
var import_useTimeframeStore = require("../../../core/time/timeframeStore/useTimeframeStore.js");
var import_useDateTimeFormat = require("../../date-time-picker/hooks/useDateTimeFormat.js");
const useDateTimeParser = ({
  type = "datetime"
}) => {
  const precision = (0, import_useTimeframeStore.useDateTimePickerStore)((state) => state.precision);
  const { emptyParts } = (0, import_useDateTimeFormat.useDateTimeFormat)(type, precision);
  const { maxValue, regexDateTimeStart, types } = (0, import_react.useMemo)(() => {
    const types2 = emptyParts.map(({ type: type2 }) => type2);
    const maxValue2 = types2[0] === "day" ? 31 : types2[0] === "month" ? 12 : 9999;
    const maxDigits = Math.ceil(Math.log10(maxValue2));
    const delimiter = emptyParts.find(({ type: type2 }) => type2 === "literal")?.value ?? ".";
    const regexDateTimeStart2 = new RegExp(
      `^(\\d{1,${maxDigits}})${delimiter.trim().replaceAll(".", "[.]")}$`
    );
    return {
      maxValue: maxValue2,
      regexDateTimeStart: regexDateTimeStart2,
      types: types2
    };
  }, [emptyParts]);
  return {
    maxValue,
    regexDateTimeStart,
    types
  };
};
