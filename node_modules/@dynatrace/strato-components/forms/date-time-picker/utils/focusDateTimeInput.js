var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var focusDateTimeInput_exports = {};
__export(focusDateTimeInput_exports, {
  focusDateTimeInput: () => focusDateTimeInput,
  focusElementWithRetry: () => focusElementWithRetry,
  focusTimeInput: () => focusTimeInput
});
module.exports = __toCommonJS(focusDateTimeInput_exports);
const focusDateTimeInput = (event, refFrom, refTo) => {
  const spinButtonHandlersTo = refTo?.current?.spinButtonHandlers;
  const spinButtonHandlersFrom = refFrom?.current?.spinButtonHandlers;
  const isTargetValid = (target) => {
    const spinButtonsFrom = Array.from(
      spinButtonHandlersTo?.values() || []
    ).map((handler) => handler?.element.current);
    const spinButtonsTo = Array.from(
      spinButtonHandlersFrom?.values() || []
    ).map((handler) => handler?.element.current);
    const allSpinButtons = [
      ...spinButtonsFrom,
      ...spinButtonsTo,
      ...[refTo?.current?.expressionInputRef],
      ...[refFrom?.current?.expressionInputRef]
    ];
    return target !== void 0 && allSpinButtons.includes(target);
  };
  const isAnySpinButtonEmpty = (handlers) => {
    if (!handlers) {
      return false;
    }
    let hasEmpty = false;
    let allEmpty = true;
    for (const [, handler] of handlers) {
      const textContent = handler?.element.current?.textContent;
      if (textContent === "") {
        hasEmpty = true;
      } else {
        allEmpty = false;
      }
    }
    return hasEmpty && !allEmpty;
  };
  const findLastEntryWithTextContent = (handlers) => {
    if (!handlers) {
      return null;
    }
    let firstSpinButtonHandler = null;
    for (const [, handler] of handlers) {
      if (!firstSpinButtonHandler) {
        firstSpinButtonHandler = handler;
      }
      if (!handler?.element.current?.textContent) {
        firstSpinButtonHandler = handler;
        break;
      }
    }
    return firstSpinButtonHandler;
  };
  if (isTargetValid(event?.target)) {
    return;
  }
  const emptyToSpinButton = isAnySpinButtonEmpty(spinButtonHandlersTo);
  const emptyFromSpinButton = isAnySpinButtonEmpty(spinButtonHandlersFrom);
  const firstFromSpinButtonHandler = findLastEntryWithTextContent(
    spinButtonHandlersFrom
  );
  const firstToSpinButtonHandler = findLastEntryWithTextContent(spinButtonHandlersTo);
  if (emptyToSpinButton && !emptyFromSpinButton) {
    const firstToSpinButtonHandler2 = findLastEntryWithTextContent(spinButtonHandlersTo);
    if (firstToSpinButtonHandler2) {
      firstToSpinButtonHandler2?.element.current?.focus();
      return;
    }
  }
  if (emptyFromSpinButton || firstFromSpinButtonHandler?.element.current?.textContent !== "" && (refTo?.current?.expressionInputRef?.textContent !== "" || firstToSpinButtonHandler?.element.current?.textContent !== "")) {
    if (firstFromSpinButtonHandler) {
      firstFromSpinButtonHandler?.element.current?.focus();
      return;
    }
  }
  if (refFrom?.current?.expressionInputRef?.textContent !== "" && refTo?.current?.expressionInputRef?.textContent === "" && firstToSpinButtonHandler?.element.current?.textContent === "") {
    refTo?.current?.expressionInputRef?.focus?.();
    return;
  }
  refFrom?.current?.expressionInputRef?.focus();
};
const focusTimeInput = (dateTimeFromRef) => {
  const hourElement = dateTimeFromRef.current?.spinButtonHandlers.get("hour")?.element.current;
  if (hourElement) {
    hourElement.focus();
    return;
  }
  focusDateTimeInput(null, dateTimeFromRef);
};
const MAX_FOCUS_ATTEMPTS = 3;
const focusElementWithRetry = (getElement, dateTimeRef) => {
  let attempts = 0;
  const tryFocus = () => {
    attempts++;
    const element = getElement();
    if (element) {
      element.focus();
      if (document.activeElement === element) {
        return;
      }
    }
    if (attempts < MAX_FOCUS_ATTEMPTS) {
      requestAnimationFrame(tryFocus);
      return;
    }
    if (dateTimeRef) {
      focusDateTimeInput(null, dateTimeRef);
    }
  };
  requestAnimationFrame(tryFocus);
};
