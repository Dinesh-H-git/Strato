import { AriaDisabledProps } from '../../core/types/a11y-props.js';
import type { BehaviorTrackingProps } from '../../core/types/behavior-tracking-props.js';
import { DataTestId } from '../../core/types/data-props.js';
import type { FormControlProps } from '../../core/types/form-control-props.js';
import { MaskingProps } from '../../core/types/masking-props.js';
import { StylingProps } from '../../core/types/styling-props.js';
import type { TimeValue } from '../../core/types/time.js';
/**
 * Determines what datetime parts to show.
 * @public
 */
export type DateTimePickerType = 'time' | 'date' | 'datetime';
/**
 * Determines the level of precision of the time component.
 * @public
 */
export type DateTimePickerPrecision = 'minutes' | 'seconds' | 'milliseconds';
/**
 * Props to define what datetime parts should be shown.
 * @public
 */
export type DateTimePickerTypeProps = {
    type: Extract<DateTimePickerType, 'date'>;
    precision?: never;
} | {
    type?: Exclude<DateTimePickerType, 'date'>;
    precision?: DateTimePickerPrecision;
};
/**
 * @public
 */
export type DateTimePickerProps = FormControlProps<TimeValue | string | null, (value: TimeValue | string | null) => void> & StylingProps & DataTestId & MaskingProps & DateTimePickerTypeProps & BehaviorTrackingProps & {
    /**
     * Earliest allowed ISOString and timestamp for validation.
     */
    min?: string;
    /**
     * Latest allowed ISOString for validation.
     */
    max?: string;
    /**
     * The placeholder that's shown in the input field if the content is empty.
     */
    placeholder?: string;
};
/**
 * Props or the DateSelector component.
 * @internal
 */
export interface DateSelectorProps extends StylingProps, DataTestId, MaskingProps, AriaDisabledProps {
    /** Determine if the trigger is disabled or not. */
    disabled: boolean;
    /** Determine if the trigger is in error state or not. */
    error: boolean;
    /** Determines whether the input is in read-only mode. */
    readOnly?: boolean;
}
/**
 * Imperative handle to provide methods to interact with the calendar widget.
 * @internal
 */
export interface DateSelectorRef {
    /** Open the calendar widget. */
    open(): void;
    /** Close the calendar widget. */
    close(): void;
}
