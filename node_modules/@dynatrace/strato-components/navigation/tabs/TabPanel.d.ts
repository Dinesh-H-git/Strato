import { AriaLabelingProps } from '../../core/types/a11y-props.js';
import { DataTestId } from '../../core/types/data-props.js';
import { DOMProps } from '../../core/types/dom.js';
import { StylingProps } from '../../core/types/styling-props.js';
import { WithChildren } from '../../core/types/with-children.js';
interface TabPanelProps extends WithChildren, DOMProps, AriaLabelingProps, StylingProps, DataTestId {
    /**
     * Whether the tab panel is disabled.
     * If true, its content is not rendered.
     */
    disabled: boolean;
    /** The overflow behavior of the tab panel. */
    panelOverflow: 'hidden' | 'scroll-y';
}
/**
 * TabPanel component, used to display the content of a tab.
 */
export declare const TabPanel: {
    (props: TabPanelProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
export {};
