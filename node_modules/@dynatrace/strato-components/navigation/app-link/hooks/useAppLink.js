var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useAppLink_exports = {};
__export(useAppLink_exports, {
  useAppLink: () => useAppLink
});
module.exports = __toCommonJS(useAppLink_exports);
var import_react = require("react");
var import_navigation = require("@dynatrace-sdk/navigation");
function useAppLink({
  appId,
  disabled,
  pageToken
}) {
  const href = (0, import_react.useMemo)(
    () => (0, import_navigation.getAppLink)(appId, pageToken),
    // temporarily workaround for type of pageToken should be fixed https://dev-jira.dynatrace.org/browse/UIP-2887
    [appId, pageToken]
  );
  const onClick = (0, import_react.useCallback)(
    (event) => {
      const shouldBrowserHandleClick = event.button !== 0 || event.metaKey || event.ctrlKey || event.shiftKey;
      if (!disabled && shouldBrowserHandleClick) {
        return;
      }
      event.preventDefault();
      event.stopPropagation();
      if (disabled) {
        return;
      }
      (0, import_navigation.openApp)(appId, pageToken);
    },
    [appId, disabled, pageToken]
  );
  return {
    onClick,
    href
  };
}
