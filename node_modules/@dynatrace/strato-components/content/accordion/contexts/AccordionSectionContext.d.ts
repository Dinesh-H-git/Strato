import { type Context } from 'react';
/** @internal */
type AccordionSectionContextProps = {
    labelId: string;
    contentId: string;
    isExpanded: boolean;
    keepMounted: boolean;
    size: 'default' | 'condensed';
};
/** @internal */
export declare const AccordionSectionContext: Context<AccordionSectionContextProps>;
export {};
