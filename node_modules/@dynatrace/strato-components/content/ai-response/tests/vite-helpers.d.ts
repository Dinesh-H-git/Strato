import { type RenderOptions, type RenderResult } from '@testing-library/react';
import { type ReactElement } from 'react';
export declare function setupMatchMediaMock(isMatching?: boolean): void;
export declare function clearMatchMediaMock(): void;
export declare function render(ui: ReactElement, { locale, timeZone, ...renderOptions }?: {
    locale?: string;
    timeZone?: string;
} & Omit<RenderOptions, 'queries'>): RenderResult;
