var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var component_mappings_exports = {};
__export(component_mappings_exports, {
  streamdownComponents: () => streamdownComponents
});
module.exports = __toCommonJS(component_mappings_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_Markdown = require("../../markdown/Markdown.js");
const streamdownComponents = {
  ...import_Markdown.componentMappings,
  code: ({ children, node, ...restProps }) => {
    const isInline = node?.position?.start?.line === node?.position?.end?.line;
    const MarkdownCode = import_Markdown.componentMappings.code;
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MarkdownCode, { inline: isInline, ...restProps, children });
  }
  // The markdownComponentMappings are typed for react-markdown, but they're compatible with
  // Streamdown so we can cast them here to avoid unnecessary duplication of the mapping logic.
};
