import type { DataTableRowData } from '../public.api.js';
/**
 * This function converts a JSONpath like string to an accessor function.
 * @internal
 */
export declare function makeAccessorFunction<TData extends DataTableRowData, TValue>(accessor: string): (row: TData) => TValue;
