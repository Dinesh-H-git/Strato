var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useBuiltInColumns_exports = {};
__export(useBuiltInColumns_exports, {
  useBuiltInColumns: () => useBuiltInColumns
});
module.exports = __toCommonJS(useBuiltInColumns_exports);
var import_react = require("react");
var import_RowDetails = require("../features/RowDetails/RowDetails.js");
var import_RowOrder = require("../features/RowOrder/RowOrder.js");
var import_RowSelection = require("../features/RowSelection/RowSelection.js");
var import_RowActions = require("../features/UserActions/RowActions.js");
const useBuiltInColumns = (options) => {
  return (0, import_react.useMemo)(() => {
    const cols = [...options.columns];
    const rowDetailsColumn = import_RowDetails.ROW_DETAILS_COLUMN;
    if (options.enableRowDetails === true && !cols.includes(rowDetailsColumn)) {
      cols.unshift(rowDetailsColumn);
    }
    const rowSelectionColumn = import_RowSelection.ROW_SELECTION_COLUMN;
    if (options.enableRowSelection !== false && !cols.includes(rowSelectionColumn)) {
      cols.unshift(rowSelectionColumn);
    }
    const rowDragColumn = import_RowOrder.ROW_DRAG_COLUMN;
    if (options.enableRowOrderDragAndDrop !== false && !cols.includes(rowDragColumn)) {
      cols.unshift(rowDragColumn);
    }
    const rowActionsColumn = import_RowActions.ROW_ACTIONS_COLUMN;
    if (options.enableRowActions === true && !cols.includes(rowActionsColumn)) {
      const spacerIndex = cols.findIndex(
        (column) => column.meta?.spacer === true
      );
      cols.splice(spacerIndex, 0, rowActionsColumn);
    }
    return cols;
  }, [
    options.columns,
    options.enableRowOrderDragAndDrop,
    options.enableRowDetails,
    options.enableRowSelection,
    options.enableRowActions
  ]);
};
