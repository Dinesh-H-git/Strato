import { type ColumnDef } from './useTable/types.js';
import type { DataTableColumnDef, DataTableDisplayColumnDef, DataTableGroupColumnDef, DataTableRowData } from '../public.api.js';
/**
 * @public
 * Type guard to determine if a given column definition is a `DataTableGroupColumnDef`.
 */
export declare function isDataTableGroupColumnDef<TData extends DataTableRowData, TValue = unknown>(columnDef: unknown): columnDef is DataTableGroupColumnDef<TData, TValue>;
/**
 * @public
 * Type guard to determine if a given column definition is a `DataTableDisplayColumnDef`.
 */
export declare function isDataTableDisplayColumnDef<TData extends DataTableRowData, TValue = unknown>(columnDef: unknown): columnDef is DataTableDisplayColumnDef<TData, TValue>;
/**
 * Helper hook that will calculate which columns are enabled within the DataTable
 * @internal
 */
export declare function useColumns<TData extends DataTableRowData, TValue = unknown>(propColumns: DataTableColumnDef<TData, TValue>[]): readonly [ColumnDef<TData>[], Map<string, DataTableColumnDef<TData, TValue>>];
