var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var core_row_model_exports = {};
__export(core_row_model_exports, {
  getCoreRowModel: () => getCoreRowModel
});
module.exports = __toCommonJS(core_row_model_exports);
var import_src = require("./tanstack-table-core-override/src/index.js");
function getCoreRowModel() {
  return (table) => (0, import_src.memo)(
    // Picked base row model from the tanstack/table-core but added table.options.columns
    // here to make sure the table re-inits when columns are changed.
    () => [table.options.data, table.options.columns],
    (data) => {
      const rowModel = {
        rows: [],
        flatRows: [],
        rowsById: {}
      };
      const accessRows = (originalRows, depth = 0, parentRow) => {
        const rows = [];
        for (let i = 0; i < originalRows.length; i++) {
          const row = (0, import_src.createRow)(
            table,
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            table._getRowId(originalRows[i], i, parentRow),
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            originalRows[i],
            i,
            depth,
            void 0,
            parentRow?.id
          );
          rowModel.flatRows.push(row);
          rowModel.rowsById[row.id] = row;
          rows.push(row);
          if (table.options.getSubRows) {
            row.originalSubRows = table.options.getSubRows(
              // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
              originalRows[i],
              i
            );
            if (row.originalSubRows?.length) {
              row.subRows = accessRows(row.originalSubRows, depth + 1, row);
            }
          }
        }
        return rows;
      };
      rowModel.rows = accessRows(data);
      return rowModel;
    },
    (0, import_src.getMemoOptions)(
      table.options,
      "debugTable",
      "getCoreRowModel",
      () => table._autoResetPageIndex()
    )
  );
}
