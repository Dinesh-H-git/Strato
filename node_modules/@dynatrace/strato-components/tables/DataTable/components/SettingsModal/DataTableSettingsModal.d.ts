import { type ReactElement } from 'react';
import { type ModalProps } from '../../../../overlays/modal/Modal.js';
/**
 * @internal
 */
export interface DataTableSettingsModalProps extends Omit<ModalProps, 'footer' | 'size'> {
    /** The callback that is called for resetting changes. */
    onReset?: () => void;
    /** The callback that is called for applying changes  */
    onApply?: () => void;
    /** The title of the modal. */
    title: string | ReactElement;
    /** Labels for the reset, cancel and apply buttons. */
    controlLabels: {
        reset?: string;
        cancel?: string;
        apply?: string;
    };
    /** Determines whether the reset and/or the apply buttons are disabled. */
    disableButtons?: {
        reset?: boolean;
        apply?: boolean;
    };
    /** Whether a status message for the screen reader should be displayed on successful reset. */
    showResetStatus?: boolean;
}
/**
 * Modal that allows to apply, reset or cancel changes on the given settings.
 * @internal
 */
export declare const DataTableSettingsModal: (props: DataTableSettingsModalProps & import("react").RefAttributes<HTMLDivElement>) => import("react").ReactElement | null;
