import type { DataTestId } from '../../../../../core/types/data-props.js';
import type { StylingProps } from '../../../../../core/types/styling-props.js';
import type { DataTablePinnedColumnState } from '../../../features/ColumnPinning/column-pinning-types.js';
import type { DataTableColumnVisibilityState } from '../../../features/ColumnVisibility/column-visibility-types.js';
import type { Column } from '../../../hooks/useTable/types.js';
import type { DataTableColumnSettingsConfig } from '../../../public.api.js';
import type { ColumnPinningPosition } from '../../../tanstack-table-core-override/src/index.js';
/**
 * Props for the DataTableColumnSettingsTrigger component.
 * @public
 */
export interface DataTableColumnSettingsTriggerProps extends StylingProps, DataTestId {
    /** Custom label to be displayed in the column settings trigger. */
    children?: (params: {
        hiddenColumnCount: number;
        totalColumnCount: number;
        defaultLabel: string;
    }) => string;
}
/** @internal */
export type ColumnListItem = Column<unknown, unknown> & {
    key: string;
    pinned: ColumnPinningPosition;
};
/**
 * ref type of the column settings modal
 * @internal
 */
export interface DataTableColumnSettingsModalRef {
    /**
     * Opens the column settings modal.
     *
     * @param columnSettings - specifies which column settings are available in the modal.
     * Every setting not explicitly specified defaults to its respective slot in the `DataTableToolbar`.
     */
    openColumnSettings: (columnSettings?: DataTableColumnSettingsConfig) => void;
}
/**
 * Props for the DataTableColumnVisibilitySettings component.
 * @public
 */
export interface DataTableColumnVisibilitySettingsProps {
    /**
     * The default column visibility state to reset to when 'Reset to default'
     * is triggered in the column settings modal.
     */
    resetColumnVisibility?: DataTableColumnVisibilityState;
}
/**
 * Props for the DataTableColumnOrderSettings component.
 * @public
 */
export interface DataTableColumnOrderSettingsProps {
    /**
     * The default column order to reset to when 'Reset to default' is triggered
     * in the column settings modal.
     */
    resetColumnOrder?: string[];
}
/**
 * Props for the DataTableColumnPinningSettings component.
 * @public
 */
export interface DataTableColumnPinningSettingsProps {
    /**
     * The default column pinning state to reset to when 'Reset to default'
     * is triggered in the column settings modal.
     */
    resetColumnPinning?: DataTablePinnedColumnState;
}
