import { type ReactNode, type ReactElement } from 'react';
import type { BehaviorTrackingProps } from '../../../../core/types/behavior-tracking-props.js';
import type { DataTestId } from '../../../../core/types/data-props.js';
import type { StylingProps } from '../../../../core/types/styling-props.js';
import type { DataTableRowData } from '../../public.api.js';
/**
 * @public
 */
export interface DataTableSelectedRowsActionsProps<TData extends DataTableRowData> extends StylingProps, DataTestId, BehaviorTrackingProps {
    children: (rows: {
        rowId: string;
        originalRow: TData;
    }[]) => ReactNode;
}
/**
 * @internal
 */
export declare function _isDataTableSelectedRowsActions<TData extends DataTableRowData>(child: ReactElement | null): child is ReactElement<DataTableSelectedRowsActionsProps<TData>>;
/**
 * Selected rows actions component used for configuring actions
 * that can be applied on one or multiple rows in the DataTable simultaneously.
 * Also, it gets called by DataTable as rows get selected/unselected.
 *
 * @param props - contains children or self
 *
 * @returns DataTableSelectedRowsActions containing children.
 * @public
 */
export declare const DataTableSelectedRowsActions: <TData extends DataTableRowData>(props: DataTableSelectedRowsActionsProps<TData> & import("react").RefAttributes<HTMLDivElement>) => import("react").ReactElement | null;
