import { Virtualizer } from '@tanstack/react-virtual';
import { type ReactElement } from 'react';
import { type Row } from '../hooks/useTable/types.js';
import type { DataTableRowData } from '../public.api.js';
export declare function DataTableBody<TData extends DataTableRowData>(props: Readonly<{
    rows: Row<TData>[];
    rowVirtualizer: Virtualizer<HTMLDivElement, Element>;
    columnVirtualizer: Virtualizer<HTMLDivElement, Element>;
}>): ReactElement | null;
export declare namespace DataTableBody {
    var displayName: string;
}
