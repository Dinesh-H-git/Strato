var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getFacetedRowModel_exports = {};
__export(getFacetedRowModel_exports, {
  getFacetedRowModel: () => getFacetedRowModel
});
module.exports = __toCommonJS(getFacetedRowModel_exports);
var import_utils = require("../utils");
var import_filterRowsUtils = require("./filterRowsUtils");
function getFacetedRowModel() {
  return (table, columnId) => (0, import_utils.memo)(
    () => [
      table.getPreFilteredRowModel(),
      table.getState().columnFilters,
      table.getState().globalFilter,
      table.getFilteredRowModel()
    ],
    (preRowModel, columnFilters, globalFilter) => {
      if (!preRowModel.rows.length || !columnFilters?.length && !globalFilter) {
        return preRowModel;
      }
      const filterableIds = [
        ...columnFilters.map((d) => d.id).filter((d) => d !== columnId),
        globalFilter ? "__global__" : void 0
      ].filter(Boolean);
      const filterRowsImpl = (row) => {
        for (let i = 0; i < filterableIds.length; i++) {
          if (row.columnFilters[filterableIds[i]] === false) {
            return false;
          }
        }
        return true;
      };
      return (0, import_filterRowsUtils.filterRows)(preRowModel.rows, filterRowsImpl, table);
    },
    (0, import_utils.getMemoOptions)(table.options, "debugTable", "getFacetedRowModel")
  );
}
