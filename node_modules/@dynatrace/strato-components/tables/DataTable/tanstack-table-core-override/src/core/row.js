var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var row_exports = {};
__export(row_exports, {
  createRow: () => createRow,
  getRowProto: () => getRowProto
});
module.exports = __toCommonJS(row_exports);
var import_utils = require("../utils");
var import_cell = require("./cell");
const rowProtosByTable = /* @__PURE__ */ new WeakMap();
function getRowProto(table) {
  let rowProto = rowProtosByTable.get(table);
  if (!rowProto) {
    const proto = {};
    proto.clone = function() {
      return Object.assign(Object.create(Object.getPrototypeOf(this)), this);
    };
    proto.subRows = [];
    proto.getValue = function(columnId) {
      if (this._valuesCache.hasOwnProperty(columnId)) {
        return this._valuesCache[columnId];
      }
      const column = table.getColumn(columnId);
      if (!column?.accessorFn) {
        return void 0;
      }
      this._valuesCache[columnId] = column.accessorFn(
        this.original,
        this.index
      );
      return this._valuesCache[columnId];
    };
    proto.getUniqueValues = function(columnId) {
      if (!this.hasOwnProperty("_uniqueValuesCache")) {
        this._uniqueValuesCache = {};
      }
      if (this._uniqueValuesCache.hasOwnProperty(columnId)) {
        return this._uniqueValuesCache[columnId];
      }
      const column = table.getColumn(columnId);
      if (!column?.accessorFn) {
        return void 0;
      }
      if (!column.columnDef.getUniqueValues) {
        this._uniqueValuesCache[columnId] = [this.getValue(columnId)];
        return this._uniqueValuesCache[columnId];
      }
      this._uniqueValuesCache[columnId] = column.columnDef.getUniqueValues(
        this.original,
        this.index
      );
      return this._uniqueValuesCache[columnId];
    };
    proto.renderValue = function(columnId) {
      return this.getValue(columnId) ?? table.options.renderFallbackValue;
    };
    proto.getLeafRows = function() {
      return (0, import_utils.flattenBy)(this.subRows, (d) => d.subRows);
    };
    proto.getParentRow = function() {
      return this.parentId ? table.getRow(this.parentId, true) : void 0;
    };
    proto.getParentRows = function() {
      let parentRows = [];
      let currentRow = this;
      while (true) {
        const parentRow = currentRow.getParentRow();
        if (!parentRow) break;
        parentRows.push(parentRow);
        currentRow = parentRow;
      }
      return parentRows.reverse();
    };
    proto.getAllCells = (0, import_utils.memo)(
      function() {
        return [this, table.getAllLeafColumns()];
      },
      (row, leafColumns) => {
        return leafColumns.map((column) => {
          return (0, import_cell.createCell)(table, row, column, column.id);
        });
      },
      (0, import_utils.getMemoOptions)(table.options, "debugRows", "getAllCells")
    );
    proto._getAllCellsByColumnId = (0, import_utils.memo)(
      function() {
        return [this.getAllCells()];
      },
      (allCells) => {
        return allCells.reduce((acc, cell) => {
          acc[cell.column.id] = cell;
          return acc;
        }, {});
      },
      (0, import_utils.getMemoOptions)(table.options, "debugRows", "getAllCellsByColumnId")
    );
    rowProtosByTable.set(table, proto);
    rowProto = proto;
  }
  return rowProto;
}
const createRow = (table, id, original, rowIndex, depth, subRows, parentId) => {
  const row = Object.create(getRowProto(table));
  Object.assign(row, {
    id,
    index: rowIndex,
    original,
    depth,
    parentId,
    _valuesCache: {}
  });
  if (subRows) {
    row.subRows = subRows;
  }
  for (let i = 0; i < table._features.length; i++) {
    const feature = table._features[i];
    feature?.createRow?.(row, table);
  }
  return row;
};
