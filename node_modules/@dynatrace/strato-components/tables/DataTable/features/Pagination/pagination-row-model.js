var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var pagination_row_model_exports = {};
__export(pagination_row_model_exports, {
  getPaginationRowModel: () => getPaginationRowModel
});
module.exports = __toCommonJS(pagination_row_model_exports);
var import_utils = require("../../hooks/useTable/utils.js");
var import_src = require("../../tanstack-table-core-override/src/index.js");
function getPaginationRowModel() {
  return (table) => (0, import_src.memo)(
    () => [
      table.getState().pagination,
      table.getPrePaginationRowModel(),
      table.options.paginateExpandedRows ? void 0 : table.getState().expanded
    ],
    (pagination, rowModel) => {
      if (!rowModel.rows.length || table.options.enablePagination !== true) {
        return rowModel;
      }
      const { pageSize, pageIndex } = pagination;
      const { rowsById } = rowModel;
      const pageStart = pageSize * pageIndex;
      const pageEnd = pageStart + pageSize;
      const rows = rowModel.rows.slice(pageStart, pageEnd);
      const paginatedRowModel = {
        rows,
        flatRows: [],
        rowsById
      };
      const addedFlatRowsSet = /* @__PURE__ */ new Set();
      const handleRow = (row) => {
        if (addedFlatRowsSet.has(row.id)) {
          return;
        }
        paginatedRowModel.flatRows.push(row);
        addedFlatRowsSet.add(row.id);
        if (row.subRows.length) {
          row.subRows.forEach(handleRow);
        }
      };
      paginatedRowModel.rows.forEach(handleRow);
      return paginatedRowModel;
    },
    (0, import_utils.getMemoOptions)(table.options, "debugTable", "getPaginationRowModel")
  );
}
