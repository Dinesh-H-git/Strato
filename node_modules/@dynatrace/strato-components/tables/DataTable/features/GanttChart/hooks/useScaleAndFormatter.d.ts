import { type ScaleLinear, type ScaleTime } from 'd3-scale';
import type { Formatter } from '../../../../../charts/core/types/formatter.js';
import type { MinMax } from '../../../../../charts/core/types/min-max.js';
import type { DataTableGanttColumnConfig } from '../../ColumnTypes/column-types-types.js';
type Scale = ScaleTime<number, number, never> | ScaleLinear<number, number, never>;
export declare const useScaleAndFormatter: (width: number, config: DataTableGanttColumnConfig, min: MinMax["min"], max: MinMax["max"]) => {
    scale: Scale;
    formatter?: Formatter;
};
export {};
