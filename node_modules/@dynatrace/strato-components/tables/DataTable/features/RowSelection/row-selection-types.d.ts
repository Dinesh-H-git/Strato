import type { DataTableRowData } from '../../row-data-types.js';
/**
 * @public
 */
export interface DataTableRowSelectionBaseProps<TData extends DataTableRowData> {
    /**
     * Enables row selection. Either accepts a boolean to enable the
     * row selection on the entire table or a config object.
     * The config object accepts a function that will get the row data as
     * the parameter and should return true or false if the row should be
     * selectable or not. In the config object, the 'select all' behavior
     * can also be configured and a selection 'limit' can also be set.
     *
     * Note: For the case where this prop is a config object, it needs
     * to be memoized, to avoid updates if it is identical. Same as for all
     * for other props with type object.
     *
     * @defaultValue false
     */
    selectableRows?: boolean | {
        /**
         * Allows to disable the selection of certain rows.
         */
        disableRowSelection?: (row: TData) => boolean;
        /**
         * Allows to configure if the select all will only select
         * the current page or the entire.
         * @defaultValue 'page'
         */
        selectAllBehavior?: 'page' | 'all';
        /**
         * Allows to set a number limit on how many items are allowed
         * to be selected. Limit number needs to be a positive integer.
         */
        limit?: number;
    };
    /**
     * Callback triggered when selection is changed.
     */
    onRowSelectionChange?: (selectedRows: Record<string, boolean>) => void;
}
/**
 * @public
 */
export interface DataTableRowSelectionControlledProps<TData extends DataTableRowData> extends DataTableRowSelectionBaseProps<TData> {
    /**
     * Controlled property to define the selected rows.
     **/
    selectedRows?: Record<string, boolean>;
    /**
     * Uncontrolled property to define the selected rows.
     **/
    defaultSelectedRows?: never;
}
/**
 * @public
 */
export interface DataTableRowSelectionUncontrolledProps<TData extends DataTableRowData> extends DataTableRowSelectionBaseProps<TData> {
    /**
     * Controlled property to define the selected rows.
     **/
    defaultSelectedRows?: Record<string, boolean>;
    /**
     * Uncontrolled property to define the selected rows.
     **/
    selectedRows?: never;
}
/**
 * Options for DataTable row selection
 * @public
 */
export type DataTableRowSelectionProps<TData extends DataTableRowData> = DataTableRowSelectionControlledProps<TData> | DataTableRowSelectionUncontrolledProps<TData>;
/**
 * Extension interface for the table options
 * @internal
 */
export interface DataTableRowSelectionOptions<TRow = unknown> {
    /**
     * Defines the select all mode for the row selection in the header.
     * @defaultValue 'page'
     */
    rowSelectionSelectAllMode?: 'page' | 'all';
    /**
     * Defines a limit of the maximum amount of elements that should be selectable.
     */
    rowSelectionLimit?: number;
    /**
     * - Enables/disables row selection for all rows in the table OR
     * - A function that given a row, returns whether to enable/disable row selection for that row
     */
    enableRowSelection?: boolean | ((row: TRow) => boolean);
}
/**
 * Extension interface for the table instance
 * @internal
 */
export interface DataTableRowSelectionInstance {
    /**
     * Selects/deselects all rows on the current page.
     */
    toggleAllPageRowsSelected: (value?: boolean) => void;
    /**
     * Selects/deselects all rows in the table.
     */
    toggleAllRowsSelected: (value?: boolean) => void;
    /**
     * Returns a count of currently selected elements.
     */
    getSelectionCount: () => number;
    /**
     * Returns a handler that can be used to toggle all rows on the current page respecting the configured limit.
     */
    getToggleAllLimitedPageRowsSelectedHandler: () => (event: unknown) => void;
    /**
     * Returns a handler that can be used to toggle all rows in the table respecting the configured limit.
     */
    getToggleAllLimitedRowsSelectedHandler: () => (event: unknown) => void;
    /**
     * Returns a handler that can be used to toggle all rows on the current page.
     */
    getToggleAllPageRowsSelectedHandler: () => (event: unknown) => void;
    /**
     * Returns a handler that can be used to toggle all rows in the table.
     */
    getToggleAllRowsSelectedHandler: () => (event: unknown) => void;
    /**
     * Selects/deselects all rows on the current page respecting the configured limit.
     */
    toggleAllLimitedPageRowsSelected: (value?: boolean) => void;
    /**
     * Selects/deselects all rows in the table respecting the configured limit.
     */
    toggleAllLimitedRowsSelected: (value?: boolean) => void;
    /**
     *  Returns whether row selection limit is reached.
     */
    getIsRowSelectionLimitReached: (rowSelectionState?: Record<string, boolean>) => boolean;
    /**
     * Returns true, if row selection is disabled for all rows on the current page, false otherwise.
     */
    getIsAllPageRowsSelectionDisabled: () => boolean;
    /**
     * Returns true, if row selection is disabled for all rows in the table, false otherwise.
     */
    getIsAllRowsSelectionDisabled: () => boolean;
    /**
     * Returns whether all rows on the current page are selected.
     */
    getIsAllPageRowsSelected: () => boolean;
    /**
     * Returns whether all rows in the table are selected.
     */
    getIsAllRowsSelected: () => boolean;
    /**
     * Returns whether any rows on the current page are selected.
     */
    getIsSomePageRowsSelected: () => boolean;
    /**
     * Returns whether any rows in the table are selected.
     */
    getIsSomeRowsSelected: () => boolean;
    /**
     * Resets the batch selection state when clicking outside the checkboxes.
     */
    resetBatchSelection: (event: MouseEvent) => void;
}
/**
 * Extension interface to extend the types of the `Row` from the tanstack table.
 * @internal
 */
export interface DataTableRowSelectionRow {
    /**
     * Selects/deselects one or multiple rows. When the shift key is pressed while selecting, all rows between this and the previous selection are selected/deselected.
     */
    batchSelect: (value?: boolean, shiftKey?: boolean) => void;
    /**
     * Selects/deselects the row.
     */
    toggleSelected: (value?: boolean) => void;
    /**
     *  Returns whether row selection should be disabled.
     */
    getShouldDisableRowSelection: () => boolean;
}
