import { type JSX } from 'react';
import type { Column, TableOptions } from '../../hooks/useTable/types.js';
import type { DataTableColumnDef, DataTableRowData } from '../../public.api.js';
type MeterBarChartRendererType<TData, TValue> = {
    value: number | {
        name: string;
        value: number;
        color?: string;
    }[];
    column: Column<TData, TValue>;
};
/**
 * Type guard to check value against MultiMeterBar values.
 * @internal
 */
export declare function isMultiMeterBarData(value: unknown): value is Array<{
    name: string;
    value: number;
    color?: string;
}>;
/**
 * Intended for configuring and rendering the meterbar chart component.
 */
export declare function MeterBarChartRenderer<TData, TValue>({ value, column, }: Readonly<MeterBarChartRendererType<TData, TValue>>): JSX.Element;
/**
 * Gets the highest total max values in a meterbar column.
 *
 * @param data - table data
 * @param columns - table column definition
 * @returns record of highest total max value per column
 * @internal
 */
export declare function getTotalMaxFromMeterbarChartData<TData extends DataTableRowData>(data: TData[], columns: DataTableColumnDef<TData>[]): Record<string, number>;
/**
 * Configuration hook for the DataTable Meterbar Chart feature.
 * @internal
 */
export declare function useMeterBarChartMaxValuesCalc<TData extends DataTableRowData>(props: {
    data: TData[];
    columns: DataTableColumnDef<TData>[];
}, options: TableOptions<TData>): void;
export {};
