import type { RowModel, Table } from '../../hooks/useTable/types.js';
import type { DataTableRowData } from '../../public.api.js';
/**
 * Row model which reflects the order given by the rowOrder set.
 * @returns new row model reflecting the order.
 * @internal
 */
export declare function getRowOrderRowModel<TData extends DataTableRowData>(): (table: Table<TData>) => () => RowModel<TData>;
