var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var download_utils_exports = {};
__export(download_utils_exports, {
  convertArrayToCsv: () => convertArrayToCsv,
  formatDateToISOString: () => formatDateToISOString
});
module.exports = __toCommonJS(download_utils_exports);
function convertArrayToCsv(dataRows) {
  const lines = new Array(dataRows.length);
  for (let i = 0; i < dataRows.length; i++) {
    lines[i] = dataRows[i].join(",");
  }
  return lines.join("\n") + "\n";
}
function formatDateToISOString(dateValue) {
  if (dateValue instanceof Date) {
    return dateValue.toISOString();
  }
  if (isValidTimeStamp(dateValue)) {
    if (!isNaN(Number(dateValue))) {
      return new Date(Number(dateValue)).toISOString();
    }
    return new Date(dateValue).toISOString();
  }
}
function isValidTimeStamp(dateValue) {
  if (typeof dateValue === "number") {
    return !!new Date(dateValue).getTime();
  } else if (typeof dateValue === "string") {
    if (!isNaN(Number(dateValue))) {
      return !!new Date(Number(dateValue)).getTime();
    } else {
      return !!new Date(dateValue).getTime();
    }
  }
  return false;
}
