var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useLockedRowOrder_exports = {};
__export(useLockedRowOrder_exports, {
  useLockedRowOrder: () => useLockedRowOrder
});
module.exports = __toCommonJS(useLockedRowOrder_exports);
var import_react = require("react");
function useLockedRowOrder(props) {
  const { initialRowOrder, lockedRows, position = "start" } = props;
  const getOrderedRows = (0, import_react.useCallback)(
    (rows) => {
      if (!position) {
        return rows;
      }
      const { locked, unlocked } = rows.reduce(
        (acc, row) => {
          lockedRows.includes(row) ? acc.locked.push(row) : acc.unlocked.push(row);
          return acc;
        },
        { locked: [], unlocked: [] }
      );
      return position === "start" ? [...locked, ...unlocked] : [...unlocked, ...locked];
    },
    [lockedRows, position]
  );
  const [rowOrder, setRowOrderState] = (0, import_react.useState)(
    getOrderedRows(initialRowOrder)
  );
  const setRowOrder = (0, import_react.useCallback)(
    (updater) => setRowOrderState(updater),
    []
  );
  (0, import_react.useEffect)(() => {
    setRowOrder(getOrderedRows(rowOrder));
  }, [lockedRows, position]);
  const disableRowDragAndDrop = (0, import_react.useCallback)(
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (row) => lockedRows.includes(row.id),
    [lockedRows]
  );
  const rowOrdering = (0, import_react.useMemo)(
    () => ({
      enableDragAndDrop: true,
      disableRowDragAndDrop,
      lockDisabledRows: position
    }),
    [disableRowDragAndDrop, position]
  );
  return {
    rowOrdering,
    rowOrder,
    setRowOrder
  };
}
