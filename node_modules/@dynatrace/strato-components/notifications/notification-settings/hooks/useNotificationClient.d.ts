/** @internal */
export interface UseNotificationClientReturn {
    toggleNotifications: () => Promise<void>;
    enabled: boolean;
    loading: boolean;
    hasNotFoundError: boolean;
    hasReadPermission: boolean;
    hasWritePermission: boolean;
}
/**
 * Base client shared for all notifications services.
 *
 * @internal
 * */
export declare function useNotificationClient<NotificationConfigType, NotificationType extends {
    id: string;
}, PaginatedNotificationType extends {
    count: number;
    results: Array<NotificationType>;
}, ErrorEnvelopeError extends {
    body: {
        error: {
            details?: {
                errorCode?: string;
            };
        };
    };
}>(notificationType: 'event' | 'resource' | 'self', config: NotificationConfigType, connectors: {
    list: (config: NotificationConfigType, abortSignal: AbortSignal) => Promise<PaginatedNotificationType>;
    create: (config: NotificationConfigType) => Promise<NotificationType>;
    destroy: (id: string) => Promise<void>;
    isErrorEnvelopeError: (e: unknown) => e is ErrorEnvelopeError;
}): UseNotificationClientReturn;
