var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var global_font_size_provider_exports = {};
__export(global_font_size_provider_exports, {
  GlobalFontSizeProvider: () => GlobalFontSizeProvider
});
module.exports = __toCommonJS(global_font_size_provider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_global_font_size_context = require("../../core/components/single-value-renderer/contexts/global-font-size.context.js");
const GlobalFontSizeProvider = ({
  children,
  valueCount,
  labelCount,
  trendCount
}) => {
  const valueFontSizes = (0, import_react.useRef)(/* @__PURE__ */ new Map());
  const labelFontSizes = (0, import_react.useRef)(/* @__PURE__ */ new Map());
  const trendFontSizes = (0, import_react.useRef)(/* @__PURE__ */ new Map());
  const isValueReady = (0, import_react.useRef)(false);
  const isLabelReady = (0, import_react.useRef)(labelCount === 0);
  const isTrendReady = (0, import_react.useRef)(trendCount === 0);
  const [fontSizes, setFontSizes] = (0, import_react.useState)();
  const reset = () => {
    valueFontSizes.current.clear();
    labelFontSizes.current.clear();
    trendFontSizes.current.clear();
    isValueReady.current = false;
    isLabelReady.current = labelCount === 0;
    isTrendReady.current = trendCount === 0;
  };
  reset();
  const updateFontSizes = () => {
    if (isValueReady.current && isLabelReady.current && isTrendReady.current) {
      const value = Math.min(...valueFontSizes.current.values());
      if (value > 0) {
        const label = Math.min(...labelFontSizes.current.values());
        const trend = Math.min(...trendFontSizes.current.values());
        setFontSizes({
          label,
          value,
          trend
        });
      }
    }
  };
  const updateValueFontSize = (id, fontSize) => {
    valueFontSizes.current.set(id, fontSize);
    if (valueFontSizes.current.size === valueCount) {
      isValueReady.current = true;
      updateFontSizes();
    }
  };
  const updateLabelFontSize = (id, fontSize) => {
    labelFontSizes.current.set(id, fontSize);
    if (labelFontSizes.current.size === labelCount) {
      isLabelReady.current = true;
      updateFontSizes();
    }
  };
  const updateTrendFontSize = (id, fontSize) => {
    trendFontSizes.current.set(id, fontSize);
    if (trendFontSizes.current.size === trendCount) {
      isTrendReady.current = true;
      updateFontSizes();
    }
  };
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_global_font_size_context.GlobalFontSizeContext.Provider,
    {
      value: {
        valueFontSize: fontSizes?.value,
        labelFontSize: fontSizes?.label,
        trendFontSize: fontSizes?.trend,
        updateValueFontSize,
        updateLabelFontSize,
        updateTrendFontSize
      },
      children
    }
  );
};
