var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_config_from_accessors_exports = {};
__export(get_config_from_accessors_exports, {
  getConfigFromAccessors: () => getConfigFromAccessors
});
module.exports = __toCommonJS(get_config_from_accessors_exports);
var import_access_value = require("../../../core/utils/accessors/access-value.js");
function getConfigFromAccessors(data, accessors) {
  const accessedConfig = {};
  if (accessors.colorAccessor) {
    accessedConfig.color = (0, import_access_value.accessValue)(data, accessors.colorAccessor);
  }
  if (accessors.value) {
    accessedConfig.value = {};
    if (accessors.value.formatterAccessor) {
      accessedConfig.value.formatter = (0, import_access_value.accessValue)(data, accessors.value.formatterAccessor);
    }
    if (accessors.value.unitAccessor) {
      accessedConfig.value.unit = (0, import_access_value.accessValue)(
        data,
        accessors.value.unitAccessor
      );
    }
  }
  if (accessors.trend) {
    accessedConfig.trend = {};
    if (accessors.trend.formatterAccessor) {
      accessedConfig.trend.formatter = (0, import_access_value.accessValue)(data, accessors.trend.formatterAccessor);
    }
    if (accessors.trend.inverseTrendAccessor) {
      accessedConfig.trend.inverseTrend = (0, import_access_value.accessValue)(
        data,
        accessors.trend.inverseTrendAccessor
      );
    }
  }
  if (accessors.sparkline) {
    accessedConfig.sparkline = {};
    if (accessors.sparkline.colorAccessor) {
      accessedConfig.sparkline.color = (0, import_access_value.accessValue)(
        data,
        accessors.sparkline.colorAccessor
      );
    }
  }
  return accessedConfig;
}
