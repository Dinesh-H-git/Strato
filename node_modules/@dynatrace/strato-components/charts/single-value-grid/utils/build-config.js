var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_config_exports = {};
__export(build_config_exports, {
  buildConfig: () => buildConfig
});
module.exports = __toCommonJS(build_config_exports);
var import_lodash_es = require("lodash");
var import_defaults = require("../defaults.js");
function buildConfig(propsConfig, accessorsConfig = {}) {
  const defaultProps = {
    ...import_defaults.gridDefaultProps
  };
  if (propsConfig.sparkline || accessorsConfig.sparkline) {
    defaultProps.sparkline = import_defaults.sparklineDefaultProps;
  }
  if (propsConfig.trend || accessorsConfig.trend) {
    defaultProps.trend = import_defaults.trendDefaultProps;
  }
  return (0, import_lodash_es.merge)({}, defaultProps, propsConfig, accessorsConfig, {
    color: accessorsConfig.color ?? propsConfig.color
  });
}
