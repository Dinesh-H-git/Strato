/**
 * Interface for the categorical custom color palette, expecting an object with
 * the shape \{<label>: <color>\}
 */
export interface CustomCategoricalColorPalette {
    [key: string]: string;
}
