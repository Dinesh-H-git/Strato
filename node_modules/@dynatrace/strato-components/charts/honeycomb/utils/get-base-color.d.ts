import { ScaleOrdinal, ScaleQuantize, ScaleThreshold } from 'd3-scale';
import { MinMax } from '../../core/types/min-max.js';
interface GetBaseColorProps {
    scale: ScaleQuantize<string, never> | ScaleThreshold<number, string, never> | ScaleOrdinal<string, string, string>;
    value: string | number;
}
export declare function getBaseColor(params: GetBaseColorProps, extent?: Partial<MinMax>): string | undefined;
export {};
