var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_categorical_scale_exports = {};
__export(build_categorical_scale_exports, {
  buildCategoricalScaleFromColorPalette: () => buildCategoricalScaleFromColorPalette,
  buildCategoricalScaleFromCustomPalette: () => buildCategoricalScaleFromCustomPalette
});
module.exports = __toCommonJS(build_categorical_scale_exports);
var import_d3_scale = require("d3-scale");
var import_lodash_es = require("lodash");
var import_filter_custom_categories = require("./filter-custom-categories.js");
var import_get_colors_list = require("./get-colors-list.js");
var import_color_palettes_definitions = require("../../core/types/color-palettes-definitions.js");
var import_constants = require("../constants.js");
const buildCategoricalScaleFromColorPalette = (colorPalette, filter) => {
  const categories = filter ?? Object.keys(import_color_palettes_definitions.colorPaletteMap[colorPalette]).map((category) => category);
  const colors = [];
  const colorsList = (0, import_get_colors_list.getColorsList)(colorPalette);
  categories.forEach((dataName, i) => {
    const index = i % colorsList.length;
    const colorToken = import_color_palettes_definitions.colorPaletteMap[colorPalette][dataName.toLowerCase()] ?? colorsList[index];
    if (!(0, import_lodash_es.isUndefined)(colorToken)) {
      colors.push(colorToken.Default);
    }
  });
  return (0, import_d3_scale.scaleOrdinal)(categories, colors).unknown(import_constants.DEFAULT_HONEYCOMB_TILE_COLOR);
};
const buildCategoricalScaleFromCustomPalette = (colorScheme, filter) => {
  const categories = filter ?? Object.keys(colorScheme);
  const filteredCategories = (0, import_filter_custom_categories.filterCustomCategories)(colorScheme, categories);
  const labels = Object.keys(filteredCategories);
  const colors = Object.values(filteredCategories);
  return (0, import_d3_scale.scaleOrdinal)(labels, colors).unknown(import_constants.DEFAULT_HONEYCOMB_TILE_COLOR);
};
