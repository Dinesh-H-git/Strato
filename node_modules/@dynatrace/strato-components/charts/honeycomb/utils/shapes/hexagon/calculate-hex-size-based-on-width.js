var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_hex_size_based_on_width_exports = {};
__export(calculate_hex_size_based_on_width_exports, {
  calculateHexSizeBasedOnHeight: () => calculateHexSizeBasedOnHeight,
  calculateHexSizeBasedOnWidth: () => calculateHexSizeBasedOnWidth
});
module.exports = __toCommonJS(calculate_hex_size_based_on_width_exports);
const calculateHexSizeBasedOnWidth = (width) => {
  const hexSize = width / Math.sqrt(3);
  const height = hexSize * 2;
  const hexQuarterHeight = height / 4;
  const hexRadius = width / 2;
  return { hexSize, height, hexQuarterHeight, hexRadius };
};
const calculateHexSizeBasedOnHeight = (height) => {
  const hexSize = height / 2;
  const width = Math.sqrt(3) * hexSize;
  const hexQuarterHeight = height / 4;
  const hexRadius = width / 2;
  return { hexSize, width, hexQuarterHeight, hexRadius };
};
