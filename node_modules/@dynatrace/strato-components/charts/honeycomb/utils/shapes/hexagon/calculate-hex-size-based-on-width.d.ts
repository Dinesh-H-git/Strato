/**
 * Calculate sizes needed to operate with a hex
 * more info: https://www.redblobgames.com/grids/hexagons/#basics
 * @param width -
 */
export declare const calculateHexSizeBasedOnWidth: (width: number) => {
    hexSize: number;
    height: number;
    hexQuarterHeight: number;
    hexRadius: number;
};
export declare const calculateHexSizeBasedOnHeight: (height: number) => {
    hexSize: number;
    width: number;
    hexQuarterHeight: number;
    hexRadius: number;
};
