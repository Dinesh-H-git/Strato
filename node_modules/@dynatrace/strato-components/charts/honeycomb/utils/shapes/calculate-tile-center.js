var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_tile_center_exports = {};
__export(calculate_tile_center_exports, {
  calculateTileCenter: () => calculateTileCenter
});
module.exports = __toCommonJS(calculate_tile_center_exports);
var import_get_sizes_abstract = require("./get-sizes-abstract.js");
var import_constants = require("../../constants.js");
var import_hive_helpers = require("../hive-helpers.js");
const calculateTileCenter = (shape, width, gridPosition, gap, offset = { top: 0, left: 0 }) => {
  const { halfWidth, halfHeight, height } = (0, import_get_sizes_abstract.getSizesAbstract)(shape, width);
  const adjustedHorizontalPosition = gridPosition.x - 1;
  const adjustedVerticalPosition = gridPosition.y - 1;
  const horizontalOffsetOddLines = width * adjustedHorizontalPosition + offset.left;
  const horizontalOffset = (0, import_hive_helpers.isEven)(gridPosition.y) ? horizontalOffsetOddLines + halfWidth + gap / 2 : horizontalOffsetOddLines;
  const heightWithoutFirstLine = adjustedVerticalPosition * import_constants.NON_OVERLAPPING_HEIGHT[shape];
  const verticalOffset = height * heightWithoutFirstLine + offset.top;
  return {
    y: verticalOffset + halfHeight + gap * adjustedVerticalPosition,
    x: horizontalOffset + halfWidth + gap * adjustedHorizontalPosition
  };
};
