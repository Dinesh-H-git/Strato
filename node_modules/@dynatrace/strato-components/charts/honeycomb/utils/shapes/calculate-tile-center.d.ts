import type { Point } from '../../../core/types/point.js';
import type { HoneycombTileShape } from '../../types/honeycomb.js';
export declare const calculateTileCenter: (shape: HoneycombTileShape, width: number, gridPosition: Point, gap: number, offset?: {
    top: number;
    left: number;
}) => {
    y: number;
    x: number;
};
