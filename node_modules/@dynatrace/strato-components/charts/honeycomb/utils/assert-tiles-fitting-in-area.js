var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var assert_tiles_fitting_in_area_exports = {};
__export(assert_tiles_fitting_in_area_exports, {
  assertTilesFittingInArea: () => assertTilesFittingInArea
});
module.exports = __toCommonJS(assert_tiles_fitting_in_area_exports);
var import_hive_helpers = require("./hive-helpers.js");
var import_constants = require("../constants.js");
const assertTilesFittingInArea = (tileWidth, tileHeight, ctnArea, shape, totalElements, gap) => {
  const { height: ctnHeight, width: ctnWidth } = ctnArea;
  const tilesPerOddRow = Math.floor(ctnWidth / (tileWidth + gap));
  const tilesPerEvenRow = tilesPerOddRow - 1;
  const nonOverlappingHeight = tileHeight * import_constants.NON_OVERLAPPING_HEIGHT[shape];
  let drawableElements = 0;
  let rows = 0;
  let hiveHeight = 0;
  let heightWithNewRow = tileHeight;
  do {
    ++rows;
    drawableElements += (0, import_hive_helpers.isEven)(rows) ? tilesPerEvenRow : tilesPerOddRow;
    hiveHeight = heightWithNewRow;
    if (drawableElements >= totalElements) {
      break;
    }
    heightWithNewRow = hiveHeight + gap + nonOverlappingHeight;
  } while (heightWithNewRow <= ctnHeight);
  return { drawableElements, hiveHeight };
};
