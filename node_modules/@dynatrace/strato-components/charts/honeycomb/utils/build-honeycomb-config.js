var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_honeycomb_config_exports = {};
__export(build_honeycomb_config_exports, {
  buildHoneycombConfig: () => buildHoneycombConfig,
  checkNumericalDataset: () => checkNumericalDataset,
  getDefaultColorScheme: () => getDefaultColorScheme
});
module.exports = __toCommonJS(build_honeycomb_config_exports);
var import_lodash_es = require("lodash");
var import_is_honeycomb_tile_categorical_data = require("./is-honeycomb-tile-categorical-data.js");
var import_is_honeycomb_tile_numeric_data = require("./is-honeycomb-tile-numeric-data.js");
var import_ColorRule = require("../../core/slots/ColorRule.js");
var import_constants = require("../constants.js");
const checkCategoricalDataset = (data) => Array.isArray(data) && ((0, import_lodash_es.isString)(data[0]) || (0, import_is_honeycomb_tile_categorical_data.isHoneycombTileCategoricalData)(data[0] ?? {}));
const checkNumericalDataset = (props) => Array.isArray(props.data) && ((0, import_lodash_es.isNumber)(props.data[0]) || (0, import_is_honeycomb_tile_numeric_data.isHoneycombTileNumericData)(props.data[0] ?? {}));
const getDefaultColorScheme = (props, contextConfig) => {
  const currentColorScheme = props.colorScheme || contextConfig.colorScheme;
  const isCategoricalDataset = checkCategoricalDataset(props.data);
  const isColorPaletteName = typeof currentColorScheme === "string";
  const isCustomNumericColorScheme = Array.isArray(currentColorScheme);
  const isInvalidColorScheme = (0, import_lodash_es.isUndefined)(currentColorScheme) || (0, import_lodash_es.isEmpty)(currentColorScheme);
  if (isInvalidColorScheme) {
    return isCategoricalDataset ? import_constants.DEFAULT_HONEYCOMB_CATEGORICAL_COLOR_PALETTE : import_constants.DEFAULT_HONEYCOMB_CONFIG.colorScheme;
  } else if (isColorPaletteName || !isCategoricalDataset || isCustomNumericColorScheme) {
    return currentColorScheme;
  } else {
    const capitalizedCategories = {};
    Object.keys(currentColorScheme).forEach((key) => {
      capitalizedCategories[key] = currentColorScheme[key];
    });
    return capitalizedCategories;
  }
};
const buildHoneycombConfig = (props, contextConfig, slots) => {
  const shape = props.shape ?? contextConfig.shape ?? import_constants.DEFAULT_HONEYCOMB_CONFIG.shape;
  const colorScheme = getDefaultColorScheme(props, contextConfig);
  const isToolbarSet = !!(slots.toolbar || contextConfig.toolbar);
  const toolbar = isToolbarSet ? (0, import_lodash_es.merge)(
    {},
    import_constants.DEFAULT_HONEYCOMB_CONFIG.toolbar,
    contextConfig.toolbar,
    slots.toolbar
  ) : void 0;
  const unit = props.unit ?? contextConfig.unit ?? void 0;
  const truncationMode = props.truncationMode ?? contextConfig.truncationMode ?? import_constants.DEFAULT_HONEYCOMB_CONFIG.truncationMode;
  const formatter = props.formatter ?? contextConfig.formatter;
  const showLabels = props.showLabels ?? contextConfig.showLabels ?? false;
  const legend = (0, import_lodash_es.merge)(
    {},
    import_constants.DEFAULT_HONEYCOMB_CONFIG.legend,
    contextConfig.legend,
    slots.legend
  );
  let min, max;
  if (checkNumericalDataset(props)) {
    min = props.min ?? contextConfig.min ?? void 0;
    max = props.max ?? contextConfig.max ?? void 0;
  }
  const colorRules = (contextConfig.colorRules || []).filter(import_ColorRule.isValidColorRule).concat(slots.colorRules || []);
  return {
    shape,
    colorScheme,
    toolbar,
    legend,
    unit,
    formatter,
    truncationMode,
    min,
    max,
    showLabels,
    colorRules,
    tooltip: slots.tooltip ?? contextConfig.tooltip
  };
};
