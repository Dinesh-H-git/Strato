var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_hexagon_shapes_exports = {};
__export(get_hexagon_shapes_exports, {
  getHexagonShapes: () => getHexagonShapes
});
module.exports = __toCommonJS(get_hexagon_shapes_exports);
var import_calculate_hex_size_based_on_width = require("./shapes/hexagon/calculate-hex-size-based-on-width.js");
const getHexagonShapes = (x, y, width) => {
  const { hexSize, hexQuarterHeight, hexRadius } = (0, import_calculate_hex_size_based_on_width.calculateHexSizeBasedOnWidth)(width);
  const topLeftVertex = { x: x - hexRadius, y: y - hexQuarterHeight };
  const topCenterVertex = { x, y: y - hexSize };
  const topRightVertex = { x: x + hexRadius, y: y - hexQuarterHeight };
  const bottomRightVertex = { x: x + hexRadius, y: y + hexQuarterHeight };
  const bottomCenterVertex = { x, y: y + hexSize };
  const bottomLeftVertex = { x: x - hexRadius, y: y + hexQuarterHeight };
  const halfWidth = width / 2;
  const leftBoundary = x - halfWidth;
  const rightBoundary = x + halfWidth;
  const textArea = {
    x,
    y,
    width,
    height: hexSize,
    leftBoundary,
    rightBoundary
  };
  const hexagonVertexes = [
    topLeftVertex,
    topCenterVertex,
    topRightVertex,
    bottomRightVertex,
    bottomCenterVertex,
    bottomLeftVertex
  ];
  return { hexagonVertexes, textArea };
};
