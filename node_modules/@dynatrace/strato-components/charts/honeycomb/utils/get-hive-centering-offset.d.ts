import { Dimensions } from '../../core/types/dimensions.js';
export declare const getHiveCenteringOffset: (graphSize: Dimensions, cols: number, tileWidth: number, hiveHeight: number, gap: number) => {
    left: number;
    top: number;
};
