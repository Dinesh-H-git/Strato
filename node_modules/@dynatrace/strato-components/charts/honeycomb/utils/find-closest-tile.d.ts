import type { Point } from '../../core/types/point.js';
import { HoneycombTileShape } from '../types/honeycomb.js';
type FindClosestTileFn = (tileWidth: number, tilesCenters: Point[], mousePosition: Point) => number | undefined;
export declare const findClosestTileStrategy: Record<HoneycombTileShape, FindClosestTileFn>;
export {};
