var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var filter_custom_categories_exports = {};
__export(filter_custom_categories_exports, {
  filterCustomCategories: () => filterCustomCategories
});
module.exports = __toCommonJS(filter_custom_categories_exports);
var import_get_lower_cased_categories = require("./get-lower-cased-categories.js");
var import_constants = require("../constants.js");
const filterCustomCategories = (colorScheme, filter) => {
  const lowerCasedCategories = (0, import_get_lower_cased_categories.getLowerCasedCategories)(colorScheme);
  const chosenCategories = {};
  filter.forEach((category) => {
    if (Object.keys(lowerCasedCategories).includes(category.toLowerCase())) {
      chosenCategories[category] = lowerCasedCategories[category.toLowerCase()];
    } else {
      chosenCategories[category] = import_constants.DEFAULT_HONEYCOMB_TILE_COLOR;
    }
  });
  return chosenCategories;
};
