var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var normalize_tiles_data_exports = {};
__export(normalize_tiles_data_exports, {
  normalizeTilesData: () => normalizeTilesData
});
module.exports = __toCommonJS(normalize_tiles_data_exports);
var import_lodash_es = require("lodash");
var import_constants = require("../constants.js");
const normalizeTile = (data, index) => {
  const isOnlyValue = (0, import_lodash_es.isString)(data) || (0, import_lodash_es.isNumber)(data);
  const value = isOnlyValue ? data : data.value;
  let name = "";
  if (isOnlyValue || !data.name) {
    name = `${import_constants.DEFAULT_TILE_NAME}`;
    if (index !== void 0) {
      name += `-${index + 1}`;
    }
  } else {
    name = data.name;
  }
  return {
    ...isOnlyValue ? {} : data,
    value,
    name
  };
};
const normalizeTilesData = (data) => {
  const dataArray = (0, import_lodash_es.isArray)(data) ? data : [data];
  const normalizedData = [];
  dataArray.forEach((item, index) => {
    const nameIndex = dataArray.length > 1 ? index : void 0;
    const normalizedTile = normalizeTile(item, nameIndex);
    if (!(0, import_lodash_es.isUndefined)(normalizedTile.value)) {
      normalizedData.push(normalizedTile);
    }
  });
  return normalizedData;
};
