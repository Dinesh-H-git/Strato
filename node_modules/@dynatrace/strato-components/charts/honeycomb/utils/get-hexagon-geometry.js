var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_hexagon_geometry_exports = {};
__export(get_hexagon_geometry_exports, {
  getHexagonGeometry: () => getHexagonGeometry
});
module.exports = __toCommonJS(get_hexagon_geometry_exports);
var import_get_hexagon_shapes = require("./get-hexagon-shapes.js");
var import_get_shape_lines_bisect = require("../../core/utils/get-shape-lines-bisect.js");
const getHexagonGeometry = (x, y, width) => {
  const { hexagonVertexes, textArea } = (0, import_get_hexagon_shapes.getHexagonShapes)(x, y, width);
  const hexagonLinesBisect = (0, import_get_shape_lines_bisect.getShapeLinesBisect)(hexagonVertexes);
  const hexagonGeometry = [];
  for (let i = 0; i < 6; i++) {
    hexagonGeometry.push(hexagonVertexes[i]);
    hexagonGeometry.push(hexagonLinesBisect[i]);
  }
  hexagonGeometry.push(hexagonVertexes[0]);
  hexagonGeometry.push(hexagonLinesBisect[0]);
  return { hexagonGeometry, textArea };
};
