import type { Point } from '../../core/types/point.js';
import { HoneycombTileData } from '../types/honeycomb.js';
import { TileColorMetadata, TilePositionMetadata } from '../types/rendering.js';
export declare const useBuildRenderMetadata: (elementsByOddRow: number, tileWidth: number, hiveHeight: number, gap?: number) => {
    renderMetadataByTileData: WeakMap<HoneycombTileData, TilePositionMetadata & TileColorMetadata>;
    tilesCenters: Point[];
};
