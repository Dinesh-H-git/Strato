var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_build_metadata_exports = {};
__export(use_build_metadata_exports, {
  useBuildMetadata: () => useBuildMetadata
});
module.exports = __toCommonJS(use_build_metadata_exports);
var import_lodash_es = require("lodash");
var import_use_honeycomb_base_scale = require("./use-honeycomb-base-scale.js");
var import_use_honeycomb_extent = require("./use-honeycomb-extent.js");
var import_use_honeycomb_formatter_config = require("./use-honeycomb-formatter-config.js");
var import_use_honeycomb_tick_formatter = require("./use-honeycomb-tick-formatter.js");
var import_builder = require("../../core/color-rule/builder.js");
var import_ColorRuleContext = require("../../core/context/ColorRuleContext.js");
var import_getValidColor = require("../../core/utils/getValidColor.js");
var import_get_base_color = require("../utils/get-base-color.js");
const useBuildMetadata = (data) => {
  const scale = (0, import_use_honeycomb_base_scale.useHoneycombBaseScale)();
  const extent = (0, import_use_honeycomb_extent.useHoneycombExtent)();
  const honeycombDataWithColoringMetadata = /* @__PURE__ */ new WeakMap();
  const honeycombDataWithTooltipMetadata = /* @__PURE__ */ new WeakMap();
  const { unit } = (0, import_use_honeycomb_formatter_config.useHoneycombFormatterConfig)();
  const formatter = (0, import_use_honeycomb_tick_formatter.useHoneycombTickFormatter)();
  const colorRules = (0, import_ColorRuleContext.useColorRuleConfig)();
  const ruleColor = (0, import_builder.buildColorRule)(colorRules)(data, "honeycomb");
  for (let i = 0; i < data.length; i++) {
    const index = i;
    const { value, name } = data[i];
    const baseColor = (0, import_get_base_color.getBaseColor)({ scale, value }, extent);
    const validColor = ruleColor.for(data[i])?.Default ?? (0, import_getValidColor.getValidColor)(baseColor);
    const tileColorData = {
      index,
      baseColor: validColor
    };
    const honeycombTooltipMetadata = {
      id: `${name ?? "unknown"}-${i}`,
      ...data[i],
      name: name ?? "",
      color: { Default: validColor }
    };
    if ((0, import_lodash_es.isNumber)(value) && !(0, import_lodash_es.isUndefined)(formatter)) {
      honeycombTooltipMetadata.value = formatter(value, unit);
    }
    honeycombDataWithColoringMetadata.set(data[i], tileColorData);
    honeycombDataWithTooltipMetadata.set(data[i], honeycombTooltipMetadata);
  }
  return {
    honeycombDataWithColoringMetadata,
    honeycombDataWithTooltipMetadata
  };
};
