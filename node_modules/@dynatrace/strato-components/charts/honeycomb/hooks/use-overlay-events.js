var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_overlay_events_exports = {};
__export(use_overlay_events_exports, {
  useOverlayEvents: () => useOverlayEvents
});
module.exports = __toCommonJS(use_overlay_events_exports);
var import_use_hive_rendering_metadata = require("./use-hive-rendering-metadata.js");
var import_use_hive_tile_best_fit = require("./use-hive-tile-best-fit.js");
var import_use_honeycomb_data = require("./use-honeycomb-data.js");
var import_use_honeycomb_shape = require("./use-honeycomb-shape.js");
var import_use_honeycomb_tooltip_metadata = require("./use-honeycomb-tooltip-metadata.js");
var import_useOverlayEventHandlers = require("../../core/hooks/useOverlayEventHandlers.js");
var import_store = require("../store/store.js");
var import_build_overlay_tooltip_data = require("../utils/build-overlay-tooltip-data.js");
var import_find_closest_tile = require("../utils/find-closest-tile.js");
const useOverlayEvents = (overlay) => {
  const setOverlayState = (0, import_store.useSetStateOverlay)();
  const honeycombData = (0, import_use_honeycomb_data.useHoneycombData)();
  const { tilesCenters } = (0, import_use_hive_rendering_metadata.useHiveRenderingMetadata)();
  const { tileWidth } = (0, import_use_hive_tile_best_fit.useHiveTileBestFit)();
  const shape = (0, import_use_honeycomb_shape.useHoneycombShape)();
  const { tooltipMetadata } = (0, import_use_honeycomb_tooltip_metadata.useHoneycombTooltipMetadata)();
  const getTooltipDataFromEvent = (event) => {
    const position = overlay.getAdjustedPositionsFromAbsolute(
      event.clientX,
      event.clientY
    );
    const relativePosition = {
      x: position.relativeX ?? 0,
      y: position.relativeY ?? 0
    };
    const closestTile = import_find_closest_tile.findClosestTileStrategy[shape](
      tileWidth,
      tilesCenters,
      relativePosition
    );
    return closestTile !== void 0 ? (0, import_build_overlay_tooltip_data.buildOverlayTooltipData)(closestTile, honeycombData, tooltipMetadata) : void 0;
  };
  return (0, import_useOverlayEventHandlers.useOverlayEventHandlers)(overlay, {
    onToolbarVisibilityChange: (visible) => setOverlayState({ toolbarVisible: visible }),
    getTooltipData: getTooltipDataFromEvent
  });
};
