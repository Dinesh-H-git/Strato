var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_build_render_metadata_exports = {};
__export(use_build_render_metadata_exports, {
  useBuildRenderMetadata: () => useBuildRenderMetadata
});
module.exports = __toCommonJS(use_build_render_metadata_exports);
var import_use_honeycomb_color_metadata = require("./use-honeycomb-color-metadata.js");
var import_use_honeycomb_shape = require("./use-honeycomb-shape.js");
var import_useGraphSize = require("../../core/components/chart-layout/hooks/useGraphSize.js");
var import_safe_get = require("../../core/utils/safe-get.js");
var import_get_hive_centering_offset = require("../utils/get-hive-centering-offset.js");
var import_calculate_tile_center = require("../utils/shapes/calculate-tile-center.js");
var import_unflatten_position = require("../utils/unflatten-position.js");
const useBuildRenderMetadata = (elementsByOddRow, tileWidth, hiveHeight, gap = 0) => {
  const graphSize = (0, import_useGraphSize.useGraphSize)();
  const shape = (0, import_use_honeycomb_shape.useHoneycombShape)();
  const { tileData, colorMetadata } = (0, import_use_honeycomb_color_metadata.useHoneycombColorMetadata)();
  const centeringOffset = (0, import_get_hive_centering_offset.getHiveCenteringOffset)(
    graphSize,
    elementsByOddRow,
    tileWidth,
    hiveHeight,
    gap
  );
  const renderMetadataByTileData = /* @__PURE__ */ new WeakMap();
  const tilesCenters = [];
  for (let i = 0; i < tileData.length; i++) {
    const hivePosition = (0, import_unflatten_position.unflattenPosition)(i, elementsByOddRow);
    const tileCenter = (0, import_calculate_tile_center.calculateTileCenter)(
      shape,
      tileWidth,
      hivePosition,
      gap,
      centeringOffset
    );
    const { index, baseColor } = (0, import_safe_get.safeGet)(colorMetadata, tileData[i]);
    const tileWithRenderingMetadata = {
      tileCenter,
      hivePosition,
      index,
      baseColor
    };
    tilesCenters.push(tileCenter);
    renderMetadataByTileData.set(tileData[i], tileWithRenderingMetadata);
  }
  return { renderMetadataByTileData, tilesCenters };
};
