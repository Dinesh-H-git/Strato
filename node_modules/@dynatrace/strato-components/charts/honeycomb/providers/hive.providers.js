var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var hive_providers_exports = {};
__export(hive_providers_exports, {
  HiveProviders: () => HiveProviders
});
module.exports = __toCommonJS(hive_providers_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_useGraphSize = require("../../core/components/chart-layout/hooks/useGraphSize.js");
var import_hive_rendering_metadata_context = require("../context/hive-rendering-metadata.context.js");
var import_hive_tile_best_fit_context = require("../context/hive-tile-best-fit.context.js");
var import_use_build_render_metadata = require("../hooks/use-build-render-metadata.js");
var import_use_honeycomb_color_metadata = require("../hooks/use-honeycomb-color-metadata.js");
var import_use_honeycomb_shape = require("../hooks/use-honeycomb-shape.js");
var import_calculate_optimal_tile_size = require("../utils/shapes/calculate-optimal-tile-size.js");
const HiveProviders = ({ children }) => {
  const graphSize = (0, import_useGraphSize.useGraphSize)();
  const shape = (0, import_use_honeycomb_shape.useHoneycombShape)();
  const { tileData } = (0, import_use_honeycomb_color_metadata.useHoneycombColorMetadata)();
  const { tileWidth, hiveHeight, gap } = (0, import_calculate_optimal_tile_size.calculateOptimalTileSize)(
    shape,
    graphSize,
    tileData.length
  );
  const elementsByOddRow = Math.min(
    Math.floor(graphSize.width / (tileWidth + gap)),
    tileData.length
  );
  const { renderMetadataByTileData, tilesCenters } = (0, import_use_build_render_metadata.useBuildRenderMetadata)(
    elementsByOddRow,
    tileWidth,
    hiveHeight,
    gap
  );
  const providerValue = (0, import_react.useMemo)(
    () => ({
      tileData,
      renderMetadata: renderMetadataByTileData,
      tilesCenters
    }),
    [graphSize, tileData]
  );
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_hive_tile_best_fit_context.HiveTileBestFitContext.Provider, { value: { tileWidth, hiveHeight, gap }, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_hive_rendering_metadata_context.HiveRenderingMetadataContext.Provider, { value: providerValue, children }) });
};
