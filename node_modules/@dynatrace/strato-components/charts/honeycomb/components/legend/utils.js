var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var utils_exports = {};
__export(utils_exports, {
  buildCategoricalItemsFromScale: () => buildCategoricalItemsFromScale
});
module.exports = __toCommonJS(utils_exports);
var import_lodash_es = require("lodash");
var import_builder = require("../../../core/color-rule/builder.js");
var import_getValidColor = require("../../../core/utils/getValidColor.js");
const buildCategoricalItemsFromScale = (scale, colorRules) => {
  const categories = scale.domain();
  const colorRule = (0, import_builder.buildColorRule)(colorRules)(categories, "categoricalLegend");
  const legendItems = [];
  categories.forEach((category) => {
    const color = scale(category);
    if (!(0, import_lodash_es.isUndefined)(color)) {
      legendItems.push({
        id: crypto.randomUUID(),
        color: colorRule.for(category) ?? { Default: (0, import_getValidColor.getValidColor)(color) },
        name: category
      });
    }
  });
  return legendItems;
};
