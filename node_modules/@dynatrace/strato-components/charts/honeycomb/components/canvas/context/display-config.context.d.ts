import { LegendItem } from '../../../../core/components/legend/categorical/types/legend.js';
export type DisplayConfigProps = {
    hovered?: LegendItem | undefined;
    showLabels?: boolean;
};
export declare const DisplayConfigContext: import("react").Context<DisplayConfigProps>;
