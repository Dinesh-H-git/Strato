var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CanvasHexagonRenderer_exports = {};
__export(CanvasHexagonRenderer_exports, {
  CanvasHexagonRenderer: () => CanvasHexagonRenderer
});
module.exports = __toCommonJS(CanvasHexagonRenderer_exports);
var import_react = require("react");
var import_useCanvas = require("../../../../core/components/canvas/hooks/useCanvas.js");
var import_get_hexagon_geometry = require("../../../utils/get-hexagon-geometry.js");
var import_draw_label_and_value = require("../utils/draw-label-and-value.js");
var import_fill_shape = require("../utils/fill-shape.js");
var import_remove_transparent_border = require("../utils/remove-transparent-border.js");
const createHexagonPath = (ctx, hexagonGeometry, radius) => {
  ctx.beginPath();
  ctx.moveTo(hexagonGeometry[0].x, hexagonGeometry[0].y);
  for (let i = 0; i < hexagonGeometry.length; i += 2) {
    const vertex = hexagonGeometry[i];
    const lineBisect = hexagonGeometry[i + 1];
    ctx.arcTo(vertex.x, vertex.y, lineBisect.x, lineBisect.y, radius);
  }
  ctx.closePath();
};
const CanvasHexagonRenderer = (props) => {
  const {
    x,
    y,
    name,
    value,
    width,
    fillColor,
    strokeColor = fillColor,
    radius = 0,
    showLabels,
    truncationMode
  } = props;
  const { hexagonGeometry, textArea } = (0, import_get_hexagon_geometry.getHexagonGeometry)(x, y, width);
  const draw = (0, import_react.useCallback)(
    (ctx) => {
      createHexagonPath(ctx, hexagonGeometry, radius);
      (0, import_remove_transparent_border.removeTransparentBorder)(ctx, strokeColor);
      (0, import_fill_shape.fillShape)(ctx, fillColor);
      if (showLabels) {
        (0, import_draw_label_and_value.drawLabelAndValue)(
          ctx,
          value,
          textArea,
          fillColor,
          name,
          truncationMode
        );
      }
      return false;
    },
    [
      x,
      y,
      width,
      fillColor,
      strokeColor,
      showLabels,
      truncationMode,
      name,
      value
    ]
  );
  (0, import_useCanvas.useCanvas)("honeycomb-hexagon", draw);
  return null;
};
CanvasHexagonRenderer["displayName"] = "CanvasHexagonRenderer";
