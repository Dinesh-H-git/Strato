var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var draw_label_and_value_exports = {};
__export(draw_label_and_value_exports, {
  drawLabelAndValue: () => drawLabelAndValue
});
module.exports = __toCommonJS(draw_label_and_value_exports);
var import_get_truncation_method = require("./get-truncation-method.js");
var import_measure_node_text_content = require("./measure-node-text-content.js");
var import_render_text_to_canvas = require("./render-text-to-canvas.js");
var import_color_contrast = require("../../../../core/utils/color-contrast.js");
var import_constants = require("../../../constants.js");
const topAndBottomPadding = 2;
const drawLabelAndValue = (ctx, value, textArea, backgroundColor, name = "", truncationMode = "middle") => {
  const { x, y } = textArea;
  const cropNameToFit = (0, import_get_truncation_method.getTruncationMethod)(
    ctx,
    textArea,
    name,
    truncationMode
  );
  const cropValueToFit = typeof value === "string" ? (0, import_get_truncation_method.getTruncationMethod)(ctx, textArea, value, truncationMode, "value") : () => value.toString();
  const {
    valueHeight,
    nameHeight,
    ableToRenderNothing,
    ableToRenderBoth,
    valueWidth,
    nameWidth,
    ableToRenderName,
    ableToRenderValue
  } = (0, import_measure_node_text_content.measureNodeTextContent)(ctx, value, name, textArea);
  if (ableToRenderNothing) {
    return;
  } else {
    const textColor = (0, import_color_contrast.getColorByContrast)(backgroundColor);
    ctx.textAlign = import_constants.HONEYCOMB_DEFAULT_TEXT_ALIGNMENT;
    ctx.fillStyle = textColor;
    const availableWidth = textArea.width - topAndBottomPadding * 2;
    const renderedName = availableWidth > nameWidth ? name : cropNameToFit();
    const renderedValue = availableWidth >= valueWidth ? value.toString() : cropValueToFit();
    if (ableToRenderBoth) {
      (0, import_render_text_to_canvas.renderTextToCanvas)(ctx, renderedName, "label", x, y - 2);
      const valueY = y + nameHeight + nameHeight * 0.5;
      (0, import_render_text_to_canvas.renderTextToCanvas)(ctx, renderedValue, "value", x, valueY);
    } else if (ableToRenderName) {
      const nameCenteredY = y + nameHeight / 2;
      (0, import_render_text_to_canvas.renderTextToCanvas)(ctx, renderedName, "label", x, nameCenteredY);
    } else if (ableToRenderValue) {
      const valueCenteredY = y + valueHeight / 2;
      availableWidth > valueWidth && (0, import_render_text_to_canvas.renderTextToCanvas)(ctx, renderedValue, "value", x, valueCenteredY);
    }
  }
};
