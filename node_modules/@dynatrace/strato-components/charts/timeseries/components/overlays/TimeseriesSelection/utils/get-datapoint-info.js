var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_datapoint_info_exports = {};
__export(get_datapoint_info_exports, {
  getDatapointInfo: () => getDatapointInfo
});
module.exports = __toCommonJS(get_datapoint_info_exports);
var import_constants = require("../../../../../core/components/data-point/constants.js");
var import_sanitize_zero_values = require("../../../../../core/utils/sanitize-zero-values.js");
var import_timeseries2 = require("../../../../utils/timeseries.js");
var import_yAxis = require("../../../../utils/yAxis.js");
var import_datapoint_map_band = require("../../../types/datapoint-map-band.js");
const getDatapointInfo = (currentDatapoint, xScale, yScales) => {
  const { start, center, end, unit, cumulativeValue, value, seriesId, color } = currentDatapoint;
  const datapointTimestamp = (0, import_timeseries2.getDatapointTime)(start, end, center);
  const datapointXPosition = xScale(datapointTimestamp);
  const yScale = (0, import_yAxis.getYScaleByUnit)(unit, yScales);
  if ((0, import_datapoint_map_band.isDatapointMapBand)(currentDatapoint)) {
    return [
      {
        id: seriesId,
        color,
        x: datapointXPosition + import_constants.OVERLAY_PADDING,
        y: (0, import_sanitize_zero_values.sanitizeZeroValues)(yScale, currentDatapoint.y0) + import_constants.OVERLAY_PADDING
      },
      {
        id: seriesId,
        color,
        x: datapointXPosition + import_constants.OVERLAY_PADDING,
        y: (0, import_sanitize_zero_values.sanitizeZeroValues)(yScale, currentDatapoint.y1) + import_constants.OVERLAY_PADDING
      }
    ];
  }
  const y = (0, import_sanitize_zero_values.sanitizeZeroValues)(
    yScale,
    cumulativeValue ? cumulativeValue : value ?? 0
  );
  return {
    id: seriesId,
    color,
    x: datapointXPosition + import_constants.OVERLAY_PADDING,
    y: y + import_constants.OVERLAY_PADDING
  };
};
