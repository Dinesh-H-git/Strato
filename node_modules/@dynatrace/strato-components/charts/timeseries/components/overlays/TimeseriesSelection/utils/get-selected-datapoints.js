var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_selected_datapoints_exports = {};
__export(get_selected_datapoints_exports, {
  getSelectedDatapoints: () => getSelectedDatapoints
});
module.exports = __toCommonJS(get_selected_datapoints_exports);
var import_is_datapoint_within_selection = require("./is-datapoint-within-selection.js");
var import_safe_get = require("../../../../../core/utils/safe-get.js");
var import_timeseries2 = require("../../../../utils/timeseries.js");
const getSelectedDatapoints = (allDatapoints, datapointsMap, range) => {
  const [from, to] = range;
  const selectedDatapoints = /* @__PURE__ */ new Map();
  for (const datapoint of allDatapoints) {
    const datapointDate = new Date(
      (0, import_timeseries2.getDatapointTime)(datapoint.start, datapoint.end)
    );
    if ((0, import_is_datapoint_within_selection.isDatapointWithinSelection)(datapointDate, from, to)) {
      const { start, end, center, unit, value, seriesName } = (0, import_safe_get.safeGet)(
        datapointsMap,
        datapoint
      );
      const datapointKey = seriesName;
      const dataPoint = {
        start,
        end,
        center,
        // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
        value,
        unit
      };
      selectedDatapoints.set(
        datapointKey,
        selectedDatapoints.has(datapointKey) ? (
          // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
          [...selectedDatapoints.get(datapointKey), dataPoint]
        ) : [dataPoint]
      );
    }
  }
  return selectedDatapoints;
};
