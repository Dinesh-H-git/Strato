var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var magnifiers_utils_exports = {};
__export(magnifiers_utils_exports, {
  generateXMagnifierLabels: () => generateXMagnifierLabels,
  generateYMagnifierLabels: () => generateYMagnifierLabels
});
module.exports = __toCommonJS(magnifiers_utils_exports);
var import_get_magnifier_label = require("../../../../core/components/crosshair/utils/get-magnifier-label.js");
function generateYMagnifierLabels(yScales, mousePosition, units, yFormatters) {
  const [leftAxisUnit, rightAxisUnit] = units;
  const [leftFormatter, rightFormatter] = yFormatters;
  const yLabelLeft = (0, import_get_magnifier_label.getMagnifierLabel)(
    leftAxisUnit,
    "y",
    leftFormatter,
    mousePosition,
    yScales.left?.scale
  );
  const yLabelRight = (0, import_get_magnifier_label.getMagnifierLabel)(
    rightAxisUnit,
    "y",
    rightFormatter,
    mousePosition,
    yScales.right?.scale
  );
  return {
    left: yLabelLeft,
    right: yLabelRight
  };
}
function generateXMagnifierLabels(formatter, date) {
  return formatter(date);
}
