var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_points_parsed_exports = {};
__export(get_points_parsed_exports, {
  getPointsParsed: () => getPointsParsed
});
module.exports = __toCommonJS(get_points_parsed_exports);
var import_constants = require("./tooltip/constants.js");
var import_find_closest_points = require("./tooltip/find-closest-points.js");
var import_timeseries_tooltip = require("./tooltip/timeseries-tooltip.js");
var import_constants2 = require("../../../../core/components/data-point/constants.js");
var import_group_by_threshold = require("../../../../core/utils/group-by-threshold.js");
const getPointsParsed = (mousePosition, allDatapoints, xScale, yScale, datapointsMap, width, height) => {
  const xPoints = mousePosition && allDatapoints.length > 0 ? (0, import_find_closest_points.findClosestPoints)(
    mousePosition.x,
    allDatapoints,
    xScale,
    yScale,
    datapointsMap
  ).map((point) => ({
    ...point,
    x: point.x + import_constants2.OVERLAY_PADDING,
    y: point.y + import_constants2.OVERLAY_PADDING
  })).filter((0, import_find_closest_points.filterOutOfBounds)(width, height)) : [];
  const closestPoint = xPoints.length > 0 ? (0, import_find_closest_points.getClosestPoint)(xPoints, mousePosition?.y) : import_constants.EMPTY_DATAPOINT;
  const filteredPoints = (0, import_timeseries_tooltip.filterTimestampsAndTimeframes)(xPoints, closestPoint);
  const groupedPoints = (0, import_group_by_threshold.getGroupedDataPointsByCoordinate)(xPoints, "y");
  return { closestPoint, filteredPoints, groupedPoints };
};
