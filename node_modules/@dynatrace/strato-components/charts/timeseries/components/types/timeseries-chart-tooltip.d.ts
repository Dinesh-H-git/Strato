import { GroupDataPoint } from '../../../core/types/data-point.js';
import { TimeseriesSlotProps } from '../../types/timeseries.js';
import { TimeseriesClosestPoint } from '../overlays/utils/tooltip/find-closest-points.js';
export interface TimeseriesChartTooltipProps {
    series: TimeseriesSlotProps[];
    /** Datapoints on an X coordinate */
    xPoints: TimeseriesClosestPoint[];
    /** Closest point to the mouse position & the x and y position of the tooltip */
    closestPoint: TimeseriesClosestPoint;
    groupedPoints: GroupDataPoint;
}
