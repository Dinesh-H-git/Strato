var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var accessors_exports = {};
__export(accessors_exports, {
  definedAccessor: () => definedAccessor,
  xAxisAccessor: () => xAxisAccessor,
  y0: () => y0,
  y0AxisAccessor: () => y0AxisAccessor,
  y1: () => y1,
  y1AxisAccessor: () => y1AxisAccessor
});
module.exports = __toCommonJS(accessors_exports);
var import_safe_get = require("../../../../core/utils/safe-get.js");
var import_timeseries2 = require("../../../utils/timeseries.js");
var import_clamp_value = require("../shared/utils/clamp-value.js");
const definedAccessor = (dataPoint, datapointsMap) => !(0, import_safe_get.safeGet)(datapointsMap, dataPoint).isGap;
const xAxisAccessor = (xScale, dataPointsMap, clampedScale) => (dataPoint) => {
  const { start, end, center } = (0, import_safe_get.safeGet)(dataPointsMap, dataPoint);
  const timestamp = (0, import_timeseries2.getDatapointTime)(start, end, center);
  return (0, import_clamp_value.getValueWithBoundaries)(timestamp, xScale, clampedScale);
};
const y0AxisAccessor = (yScale, dataPointsMap) => (dataPoint) => {
  const dataPointMetadata = (0, import_safe_get.safeGet)(dataPointsMap, dataPoint);
  const value = (dataPointMetadata.cumulativeValue ?? 0) - (dataPointMetadata.value ?? 0);
  return yScale(value);
};
const y1AxisAccessor = (yScale, dataPointsMap) => (dataPoint) => {
  const value = (0, import_safe_get.safeGet)(dataPointsMap, dataPoint).cumulativeValue ?? 0;
  return yScale(value);
};
function y1(yScaleByUnit, seriesMetadata, datapointsMap) {
  return (dataPoint) => y1AxisAccessor(
    yScaleByUnit(seriesMetadata.unit),
    datapointsMap
  )(dataPoint) ?? 0;
}
function y0(yScaleByUnit, seriesMetadata, datapointsMap) {
  return (dataPoint) => y0AxisAccessor(
    yScaleByUnit(seriesMetadata.unit),
    datapointsMap
  )(dataPoint) ?? 0;
}
