import type { ScaleTime } from 'd3-scale';
import type { TimeseriesDatapoint } from '../../../../core/types/timeseries.js';
import type { TimeseriesBandDataPoint } from '../../../types/timeseries-band.js';
import type { TimeseriesSlotProps } from '../../../types/timeseries.js';
export type DataPointDates = Pick<TimeseriesDatapoint, 'start' | 'center' | 'end'>;
/**
 * Common props used in the ChartDisplay
 */
export interface BaseDisplayProps {
    series: TimeseriesSlotProps[];
    xScale: ScaleTime<number, number>;
}
export type TimeseriesDataPointAllTypes = TimeseriesDatapoint | TimeseriesBandDataPoint;
