import type { ResultRecord, ResultRecordValue } from '@dynatrace-sdk/client-query';
import type { FieldEntry } from './getFieldsArrayForRecordIndex.js';
/**
 * @public
 */
export interface Dimension {
    field: FieldEntry;
    value: ResultRecordValue | undefined;
}
/**
 * Generates a key for a record based on its fields
 * @public
 */
export declare function getDimensions(record: ResultRecord, fieldsArray: FieldEntry[], isTimeseriesFormat: boolean): Array<Dimension>;
/**
 * Gets the value dimensions from field entries.
 * @param fieldsArray - Field entries
 * @param isTimeseriesFormat - Boolean indicating if field entry is in timeseries format
 * @internal
 */
export declare function getValueDimensions(fieldsArray: FieldEntry[], isTimeseriesFormat: boolean): FieldEntry[];
