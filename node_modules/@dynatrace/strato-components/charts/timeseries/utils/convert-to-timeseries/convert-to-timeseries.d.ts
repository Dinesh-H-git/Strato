import type { Metadata, QueryResult, RangedFieldTypes } from '@dynatrace-sdk/client-query';
import type { TypedQueryResult, UseDqlMetadata } from '@dynatrace-sdk/react-hooks';
import type { TimeseriesWithDimensions } from '../../types/convert-to-timeseries.js';
/**
 * Converts Grail records to Timeseries that can be used for charting
 * @param queryResult - The result of the query
 * @returns the computed Timeseries
 * @throws error if the data is malformed

 * @public
 */
export declare function convertQueryResultToTimeseries(queryResult: QueryResult | TypedQueryResult): TimeseriesWithDimensions[];
/**
 * Converts Grail records to Timeseries that can be used for charting
 * @param records - The input records from the query
 * @param fieldTypes - Types of fields
 * @param hiddenFieldIds - The list of field ids which should not be converted
 * @returns the computed Timeseries
 * @throws error if the data is malformed
 * @public
 */
export declare function convertToTimeseries(records: QueryResult['records'] | TypedQueryResult['records'], fieldTypes: RangedFieldTypes[], hiddenFieldIds?: string[], metadata?: Metadata | UseDqlMetadata): TimeseriesWithDimensions[];
