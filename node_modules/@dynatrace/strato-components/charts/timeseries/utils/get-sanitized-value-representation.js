var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_sanitized_value_representation_exports = {};
__export(get_sanitized_value_representation_exports, {
  getSanitizedValueRepresentation: () => getSanitizedValueRepresentation
});
module.exports = __toCommonJS(get_sanitized_value_representation_exports);
var import_detectInconsistencies = require("./detectInconsistencies.js");
function getSanitizedValueRepresentation(data, valueRepPerVariant) {
  const { area } = data;
  if (area && area.length > 0) {
    const valueRepresentation = valueRepPerVariant.get("area") ?? "absolute";
    const misalignedDatapoints = (0, import_detectInconsistencies.detectMisalignedDatapoints)(area);
    const isMisaligned = misalignedDatapoints && valueRepresentation === "relative";
    valueRepPerVariant.set(
      "area",
      isMisaligned ? "absolute" : valueRepresentation
    );
  }
  return valueRepPerVariant;
}
