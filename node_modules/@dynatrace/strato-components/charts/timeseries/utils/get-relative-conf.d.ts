import { TimeseriesBandSlotProps, ChartVariantInternal, TimeseriesVariantSlotProps, ValueRepresentationPerVariant } from '../types/timeseries.js';
export declare function getRelativeConf(series: TimeseriesBandSlotProps[] | TimeseriesVariantSlotProps[], variant: ChartVariantInternal, valueRepPerVariant: ValueRepresentationPerVariant, nonStackedMap?: {
    groupedValues: Map<string, number>;
    maxValuesByUnit: Map<string, number>;
    uniqueUnits: Map<string, boolean>;
}): {
    groupedValues: Map<string, number>;
    maxValuesByUnit: Map<string, number>;
    uniqueUnits: Map<string, boolean>;
} | undefined;
