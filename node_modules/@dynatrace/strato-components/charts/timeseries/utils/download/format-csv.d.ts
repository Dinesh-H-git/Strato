import { TimeseriesRawDataPerVariant } from '../../context/TimeseriesRawData.context.js';
import { TimeseriesVariantSlotProps, type TimeseriesBandSlotProps } from '../../types/timeseries.js';
/**
 * Populates array of names for each name of the series
 *
 * @param maxNameCount -
 * @param series -
 *
 * @returns array of values
 */
export declare function fillRowNames(maxNameCount: number, series: TimeseriesVariantSlotProps | TimeseriesBandSlotProps): string[];
export declare function formatCSV(rawData: TimeseriesRawDataPerVariant): string;
