import { TimeseriesRawDataPerVariant } from '../../context/TimeseriesRawData.context.js';
/**
 * Checks whether dataset contains Band geometry data
 *
 * @param rawData - dataset
 *
 * @returns boolean
 */
export declare function containsBandData(rawData: TimeseriesRawDataPerVariant): boolean;
/**
 * Checks whether dataset contains Line, Area or Bar geometry data
 * @param rawData -
 *
 * @returns boolean
 */
export declare function containsSeriesData(rawData: TimeseriesRawDataPerVariant): boolean;
/**
 * Gets maximum number of names that a series have
 *
 * @param rawData - series
 *
 * @returns number
 */
export declare function getNameCount(rawData: TimeseriesRawDataPerVariant): number;
/**
 * Generates a name column for each name, and the series column
 *
 * @param nameCount - number of name columns
 *
 * @returns name columns
 */
export declare function generateNameColumns(nameCount: number): string[];
/**
 * Generate all CSV columns for a given set of series
 *
 * @param rawData - series
 * @param nameCount - number of name columns
 *
 * @returns all columns
 */
export declare function generateColumns(rawData: TimeseriesRawDataPerVariant, nameCount: number): string[];
