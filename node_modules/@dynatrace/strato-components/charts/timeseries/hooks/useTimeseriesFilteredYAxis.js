var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useTimeseriesFilteredYAxis_exports = {};
__export(useTimeseriesFilteredYAxis_exports, {
  useTimeseriesFilteredYAxis: () => useTimeseriesFilteredYAxis
});
module.exports = __toCommonJS(useTimeseriesFilteredYAxis_exports);
var import_useTimeseriesData = require("./useTimeseriesData.js");
var import_useTimeseriesFilteredData = require("./useTimeseriesFilteredData.js");
var import_useYAxisConfig = require("./useYAxisConfig.js");
const useTimeseriesFilteredYAxis = () => {
  const { units: initialUnits } = (0, import_useTimeseriesData.useTimeseriesData)();
  const { units, originalUnits } = (0, import_useTimeseriesFilteredData.useTimeseriesFilteredData)();
  const [initialLeftUnit] = initialUnits;
  const [leftUnit] = units;
  const [originalLeftUnit] = originalUnits;
  const yAxis = (0, import_useYAxisConfig.useYAxisConfig)();
  if (initialUnits.length === 1 && yAxis.right && !yAxis.left) {
    return { right: yAxis.right };
  }
  if (initialLeftUnit !== (originalLeftUnit ?? leftUnit)) {
    return { left: yAxis.right };
  }
  if (initialUnits.length !== units.length || units.length === 1) {
    return { left: yAxis.left };
  }
  return yAxis;
};
