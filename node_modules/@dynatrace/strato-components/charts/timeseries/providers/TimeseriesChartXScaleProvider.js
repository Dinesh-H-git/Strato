var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TimeseriesChartXScaleProvider_exports = {};
__export(TimeseriesChartXScaleProvider_exports, {
  TimeseriesChartXScaleProvider: () => TimeseriesChartXScaleProvider
});
module.exports = __toCommonJS(TimeseriesChartXScaleProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_d3_scale = require("d3-scale");
var import_useGridPlotSize = require("../../core/components/chart-grid/hooks/useGridPlotSize.js");
var import_TimeseriesChartXScale_context = require("../context/TimeseriesChartXScale.context.js");
var import_selectors = require("../hooks/store/selectors.js");
var import_useMinMaxXAxis = require("../hooks/useMinMaxXAxis.js");
var import_useTimeseriesZoomRanges = require("../hooks/useTimeseriesZoomRanges.js");
const TimeseriesChartXScaleProvider = ({ children }) => {
  const { min, max } = (0, import_useMinMaxXAxis.useMinMaxXAxis)();
  const currentDomain = (0, import_selectors.useCurrentDomainState)();
  const { width } = (0, import_useGridPlotSize.useGridPlotSize)();
  const { initialZoom } = (0, import_useTimeseriesZoomRanges.useTimeseriesZoomRanges)();
  const [initialZoomMin, initialZoomMax] = initialZoom ?? [];
  const scaleDomain = [
    new Date(initialZoomMin ?? min),
    new Date(initialZoomMax ?? max)
  ];
  const currentScale = (0, import_d3_scale.scaleTime)().domain(currentDomain ?? scaleDomain).range([0, width]);
  const initialScale = (0, import_d3_scale.scaleTime)().domain(scaleDomain).range([0, width]);
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_TimeseriesChartXScale_context.TimeseriesChartXScaleContext.Provider,
    {
      value: { initialScale, currentScale },
      children
    }
  );
};
TimeseriesChartXScaleProvider["displayName"] = "TimeseriesChartXScaleProvider";
