var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TimeseriesFormatterProvider_exports = {};
__export(TimeseriesFormatterProvider_exports, {
  TimeseriesFormatterProvider: () => TimeseriesFormatterProvider
});
module.exports = __toCommonJS(TimeseriesFormatterProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_useGridPlotSize = require("../../core/components/chart-grid/hooks/useGridPlotSize.js");
var import_defaultUnitFormatter = require("../../core/utils/defaultUnitFormatter.js");
var import_format_date_based_on_domain = require("../../core/utils/format-date-based-on-domain.js");
var import_formatter_by_ticks = require("../../core/utils/formatter-by-ticks.js");
var import_ticks = require("../../core/utils/ticks.js");
var import_TimeseriesTickAxis_context = require("../context/TimeseriesTickAxis.context.js");
var import_TimeseriesXFormatter_context = require("../context/TimeseriesXFormatter.context.js");
var import_TimeseriesYFormatter_context = require("../context/TimeseriesYFormatter.context.js");
var import_useTimeseriesFilteredYAxis = require("../hooks/useTimeseriesFilteredYAxis.js");
var import_useTimeseriesUnits = require("../hooks/useTimeseriesUnits.js");
var import_useValueRepresentation = require("../hooks/useValueRepresentation.js");
var import_useXScale = require("../hooks/useXScale.js");
var import_useYScales = require("../hooks/useYScales.js");
const TimeseriesFormatterProvider = ({
  children
}) => {
  const { height, width } = (0, import_useGridPlotSize.useGridPlotSize)();
  const { left, right } = (0, import_useYScales.useYScales)();
  const { currentScale } = (0, import_useXScale.useXScales)();
  const yAxes = (0, import_useTimeseriesFilteredYAxis.useTimeseriesFilteredYAxis)();
  const [leftUnit, rightUnit] = (0, import_useTimeseriesUnits.useTimeseriesUnits)();
  const valueRepresentation = (0, import_useValueRepresentation.useValueRepresentation)();
  const isRelative = valueRepresentation === "relative" && leftUnit === "percent";
  const verticalNumTicks = (0, import_ticks.getVerticalNumTicks)(height);
  const horizontalNumTicks = (0, import_ticks.getHorizontalNumTicks)(width);
  const leftFormatter = isRelative ? (0, import_defaultUnitFormatter.defaultUnitFormatter)(leftUnit) : (0, import_formatter_by_ticks.getFormatterByTicks)(
    verticalNumTicks,
    left?.scale,
    leftUnit,
    yAxes.left?.formatter
  );
  const isOnlyRightAxisConfigured = !yAxes.left && yAxes.right && !rightUnit;
  const resolvedRightUnit = isOnlyRightAxisConfigured ? leftUnit : rightUnit;
  const rightFormatter = isRelative ? (0, import_defaultUnitFormatter.defaultUnitFormatter)(resolvedRightUnit) : (0, import_formatter_by_ticks.getFormatterByTicks)(
    verticalNumTicks,
    right?.scale,
    resolvedRightUnit,
    yAxes.right?.formatter
  );
  const leftTicks = (0, import_formatter_by_ticks.getFormattedTicks)(
    verticalNumTicks,
    left?.scale,
    leftUnit,
    leftFormatter
  );
  const rightTicks = (0, import_formatter_by_ticks.getFormattedTicks)(
    verticalNumTicks,
    right?.scale,
    isOnlyRightAxisConfigured ? leftUnit : rightUnit,
    rightFormatter
  );
  const bottomTicks = (0, import_formatter_by_ticks.getFormattedTicks)(
    horizontalNumTicks,
    currentScale,
    void 0,
    void 0
  );
  const bottomFormatter = (time) => (0, import_format_date_based_on_domain.formatDateBasedOnDomain)(time, currentScale.domain(), width);
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_TimeseriesYFormatter_context.TimeseriesYFormatterContext.Provider,
    {
      value: [leftFormatter, rightFormatter],
      children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_TimeseriesXFormatter_context.TimeseriesXFormatterContext.Provider, { value: bottomFormatter, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        import_TimeseriesTickAxis_context.TimeseriesTickAxisContext.Provider,
        {
          value: {
            left: leftTicks,
            right: rightTicks,
            bottom: bottomTicks
          },
          children
        }
      ) })
    }
  );
};
