var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TimeseriesMinMaxXAxisProvider_exports = {};
__export(TimeseriesMinMaxXAxisProvider_exports, {
  TimeseriesMinMaxXAxisProvider: () => TimeseriesMinMaxXAxisProvider
});
module.exports = __toCommonJS(TimeseriesMinMaxXAxisProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_convert_dates = require("../../core/utils/convert-dates.js");
var import_is_date = require("../../core/utils/is-date.js");
var import_TimeseriesMinMaxXAxis_context = require("../context/TimeseriesMinMaxXAxis.context.js");
var import_useTimeseriesData = require("../hooks/useTimeseriesData.js");
var import_useXAxisConfig = require("../hooks/useXAxisConfig.js");
var import_x_axis = require("../utils/x-axis.js");
const TimeseriesMinMaxXAxisProvider = ({ children }) => {
  const { dataPerVariant: data } = (0, import_useTimeseriesData.useTimeseriesData)();
  const { min: minFromData, max: maxFromData } = (0, import_x_axis.getXAxisRange)(data);
  const { min: minFromConfig, max: maxFromConfig } = (0, import_useXAxisConfig.useXAxisConfig)() ?? {};
  const min = (0, import_is_date.isDateCompatibleToTimestamp)(minFromConfig) ? minFromConfig : minFromData;
  const max = (0, import_is_date.isDateCompatibleToTimestamp)(maxFromConfig) ? maxFromConfig : maxFromData;
  const minConverted = (0, import_convert_dates.convertDateCompatibleToTimestamp)(min);
  const maxConverted = (0, import_convert_dates.convertDateCompatibleToTimestamp)(max);
  const minMaxValues = { min: minConverted, max: maxConverted };
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_TimeseriesMinMaxXAxis_context.TimeseriesMinMaxXAxisContext.Provider, { value: minMaxValues, children });
};
TimeseriesMinMaxXAxisProvider["displayName"] = "TimeseriesMinMaxXAxisProvider";
