var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var StoreProvider_exports = {};
__export(StoreProvider_exports, {
  StoreProvider: () => StoreProvider
});
module.exports = __toCommonJS(StoreProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_store = require("../../../core/store/store.js");
var import_types = require("../../core/components/interactions-overlay/types.js");
var import_get_initial_zoom_out = require("../../core/components/zoom/utils/get-initial-zoom-out.js");
var import_constants = require("../constants.js");
var import_TimeseriesChartState_context = require("../context/TimeseriesChartState.context.js");
var import_x_axis = require("../utils/x-axis.js");
const StoreProvider = ({
  data,
  currentDomain,
  initialDomain,
  infiniteZoom,
  children
}) => {
  const storeRef = (0, import_react.useRef)(void 0);
  const { min: minFromData, max: maxFromData } = (0, import_x_axis.getXAxisRange)(data);
  const initialDomainRange = initialDomain ?? [minFromData, maxFromData];
  const lastInteraction = storeRef?.current?.getState().interactionMode;
  storeRef.current = (0, import_store.createStore)({
    ...import_constants.initialState,
    currentDomain,
    interactionMode: lastInteraction || import_types.INTERACTION_MODE.Explore,
    zoom: {
      ...import_constants.initialState.zoom,
      zoomOutEnabled: infiniteZoom || (0, import_get_initial_zoom_out.getInitialZoomOut)(currentDomain, initialDomainRange)
    }
  });
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_TimeseriesChartState_context.TimeseriesChartStateContext.Provider, { value: storeRef.current, children });
};
