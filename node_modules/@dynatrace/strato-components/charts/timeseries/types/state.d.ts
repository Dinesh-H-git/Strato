import type { ScaleLinear, ScaleSymLog } from 'd3-scale';
import type { AnnotationsIntegrationSelectionState } from '../../core/components/annotations/components/indicators/types.js';
import type { InteractionState } from '../../core/components/interactions-state-helpers/types.js';
import type { ChartUnit } from '../../core/types/chart-unit.js';
export type YAxisScale = {
    scale: ScaleLinear<number, number> | ScaleSymLog<number, number>;
    unit?: ChartUnit;
};
type LeftYScale = {
    left: YAxisScale;
    right?: YAxisScale;
};
type RightYScale = {
    left?: YAxisScale;
    right: YAxisScale;
};
export type YAxisScales = LeftYScale | RightYScale;
export interface TimeseriesThresholdsState {
    hoveredThresholdId: string | undefined;
}
export interface TimeseriesChartState extends InteractionState<[Date, Date] | [number, number]> {
    annotations: AnnotationsIntegrationSelectionState;
    thresholds: TimeseriesThresholdsState;
    filteredSeriesIds: Set<string>;
}
export {};
