var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var HighlightedRectangleCanvas_exports = {};
__export(HighlightedRectangleCanvas_exports, {
  HighlightedRectangleCanvas: () => HighlightedRectangleCanvas
});
module.exports = __toCommonJS(HighlightedRectangleCanvas_exports);
var import_react = require("react");
var import_colors = __toESM(require("@dynatrace/strato-design-tokens/colors"));
var import_useCanvas = require("../../../../core/components/canvas/hooks/useCanvas.js");
var import_canvas_color = require("../../../../core/components/canvas/utils/canvas-color.js");
var import_constants = require("../../../../core/constants.js");
const OUTER_OFFSET = 2;
const OUTER_SHRINK = 3;
const INNER_OFFSET = 3;
const INNER_SHRINK = 5;
const BORDER_WIDTH = 1;
const HighlightedRectangleCanvas = ({
  points
}) => {
  const draw = (0, import_react.useCallback)(
    (ctx) => {
      for (const point of points) {
        ctx.beginPath();
        ctx.lineWidth = BORDER_WIDTH;
        ctx.strokeStyle = (0, import_canvas_color.getCanvasColor)(import_colors.default.Text.Neutral.Default);
        ctx.rect(
          Number(point.x) + OUTER_OFFSET,
          Number(point.y) + OUTER_OFFSET,
          Math.max(0, Number(point.width) - OUTER_SHRINK),
          Math.max(0, Number(point.height) - OUTER_SHRINK)
        );
        ctx.stroke();
        ctx.closePath();
        const innerX = Number(point.x) + INNER_OFFSET;
        const innerY = Number(point.y) + INNER_OFFSET;
        const innerWidth = Number(point.width) - INNER_SHRINK;
        const innerHeight = Number(point.height) - INNER_SHRINK;
        if (innerWidth > 0 && innerHeight > 0) {
          ctx.beginPath();
          ctx.lineWidth = BORDER_WIDTH;
          ctx.globalAlpha = import_constants.DEFAULT_HIGHLIGHTED_OPACITY;
          ctx.strokeStyle = (0, import_canvas_color.getCanvasColor)(import_colors.default.Background.Base.Default);
          ctx.rect(innerX, innerY, innerWidth, innerHeight);
          ctx.stroke();
          ctx.globalAlpha = 1;
          ctx.closePath();
        }
      }
      return false;
    },
    [points]
  );
  (0, import_useCanvas.useCanvas)("Highlighted-rects", draw);
  return null;
};
