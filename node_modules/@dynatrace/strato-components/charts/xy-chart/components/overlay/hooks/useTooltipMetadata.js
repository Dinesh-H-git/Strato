var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useTooltipMetadata_exports = {};
__export(useTooltipMetadata_exports, {
  useTooltipMetadata: () => useTooltipMetadata
});
module.exports = __toCommonJS(useTooltipMetadata_exports);
var import_selectors = require("../../../../core/components/overlay-tooltip/selectors.js");
var import_state = require("../../../types/state.js");
const VariantToGeometryMap = {
  linear: /* @__PURE__ */ new Set(["dot", "line", "area"]),
  rectangle: /* @__PURE__ */ new Set(["rect", "bar"])
};
const useTooltipMetadata = (variant) => {
  (0, import_selectors.useSelector)(({ data: data2 }) => {
    const metadata = data2?.metadata || import_state.UndefinedContent;
    if (VariantToGeometryMap[variant].has(metadata.type)) {
      return (metadata || import_state.UndefinedContent || void 0)?.datapoint;
    }
  });
  const { data } = (0, import_selectors.useTooltipSnapshot)();
  return data.metadata || import_state.UndefinedContent;
};
