var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useApplyZoomDelta_exports = {};
__export(useApplyZoomDelta_exports, {
  useApplyZoomDelta: () => useApplyZoomDelta
});
module.exports = __toCommonJS(useApplyZoomDelta_exports);
var import_useTooltipActions = require("../../../../core/components/overlay-tooltip/useTooltipActions.js");
var import_useSynchronizedSelection = require("../../../../core/components/shared-chart-interactions/hooks/useSynchronizedSelection.js");
var import_get_zoom_step = require("../../../../core/components/zoom/utils/get-zoom-step.js");
var import_useXYChartData = require("../../../hooks/useXYChartData.js");
var import_useXYChartScales = require("../../../hooks/useXYChartScales.js");
var import_reducer_zoom = require("../../../store/reducer-zoom.js");
var import_selectors = require("../../../store/selectors.js");
var import_axis_interactions = require("../../../utils/axis-interactions.js");
var import_get_scale = require("../../../utils/get-scale.js");
var import_zoom_change_handler = require("../utils/zoom-change-handler.js");
const useApplyZoomDelta = () => {
  const setState = (0, import_selectors.useSetState)();
  const axisIdToScales = (0, import_useXYChartScales.useXYChartScales)();
  const { axes } = (0, import_useXYChartData.useXYChartData)();
  const { resetSelectionState } = (0, import_useSynchronizedSelection.useSynchronizedSelection)();
  const { reset } = (0, import_useTooltipActions.useTooltipActions)();
  return (deltaStart, deltaEnd, type) => {
    const newCurrentDomain = /* @__PURE__ */ new Map();
    let zoomInEnabled = false;
    let zoomOutEnabled = false;
    const allAxesHaveInfiniteZoom = axes.filter(import_axis_interactions.isZoomEnabledFor).every((axis) => (0, import_axis_interactions.isInfiniteZoomEnabledFor)(axis));
    for (const axis of axes) {
      if ((0, import_axis_interactions.isZoomEnabledFor)(axis)) {
        const currentScale = (0, import_get_scale.getCurrentScale)(axisIdToScales, axis.id);
        const initialScale = (0, import_get_scale.getInitialScale)(axisIdToScales, axis.id);
        const initialDomain = initialScale.domain();
        const {
          nextDomain,
          zoomInEnabled: inEnabled,
          zoomOutEnabled: outEnabled
        } = (0, import_get_zoom_step.getZoomStep)(
          currentScale,
          [+initialDomain[0], +initialDomain[1]],
          allAxesHaveInfiniteZoom,
          deltaStart,
          deltaEnd
        );
        zoomInEnabled ||= inEnabled;
        zoomOutEnabled ||= outEnabled;
        newCurrentDomain.set(
          axis.id,
          nextDomain ? { min: nextDomain[0].valueOf(), max: nextDomain[1].valueOf() } : void 0
        );
        (0, import_zoom_change_handler.handleZoomChange)(axis, nextDomain[0], nextDomain[1], type);
      }
    }
    resetSelectionState?.();
    setState((0, import_reducer_zoom.reduceZoomDomain)(newCurrentDomain, zoomInEnabled, zoomOutEnabled));
    reset();
  };
};
