var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_axis_formatter_with_time_fallback_exports = {};
__export(get_axis_formatter_with_time_fallback_exports, {
  getAxisFormatterWithTimeFallback: () => getAxisFormatterWithTimeFallback
});
module.exports = __toCommonJS(get_axis_formatter_with_time_fallback_exports);
var import_format_date_based_on_domain = require("../../../../core/utils/format-date-based-on-domain.js");
var import_safe_get = require("../../../../core/utils/safe-get.js");
function getAxisFormatterWithTimeFallback({
  axisConfig,
  axisId,
  scale,
  axesIdToFormatter,
  width
}) {
  if (!axisConfig) {
    return (0, import_safe_get.safeGet)(axesIdToFormatter, axisId);
  }
  const { formatter, type } = axisConfig;
  if (type === "time" && !formatter) {
    return (value) => {
      const dateValue = value instanceof Date ? value : new Date(value);
      return (0, import_format_date_based_on_domain.formatDateBasedOnDomain)(
        dateValue,
        scale?.domain(),
        width
      );
    };
  } else {
    return (0, import_safe_get.safeGet)(axesIdToFormatter, axisId);
  }
}
