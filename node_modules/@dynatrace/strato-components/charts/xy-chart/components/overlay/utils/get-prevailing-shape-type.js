var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_prevailing_shape_type_exports = {};
__export(get_prevailing_shape_type_exports, {
  getPrevailingShapeType: () => getPrevailingShapeType
});
module.exports = __toCommonJS(get_prevailing_shape_type_exports);
var import_constants = require("../../../../core/constants.js");
var import_xy_chart_internals = require("../../../types/xy-chart-internals.js");
function getPrevailingShapeType(dotDistance = Infinity, lineDistance = Infinity, areaDistance = Infinity, rectPresent = false, barPresent = false) {
  if (dotDistance === Infinity && lineDistance === Infinity && areaDistance === Infinity && !rectPresent && !barPresent) {
    return void 0;
  }
  const threshold = import_constants.DEFAULT_DATA_POINT_RADIUS * 2;
  const distances = [
    { type: import_xy_chart_internals.LINE_VARIANT, distance: lineDistance },
    { type: import_xy_chart_internals.DOT_VARIANT, distance: dotDistance },
    { type: import_xy_chart_internals.AREA_VARIANT, distance: areaDistance }
  ];
  const closestContinuous = distances.reduce(
    (closest, current) => current.distance < closest.distance ? current : closest,
    distances[0]
  );
  if (barPresent) {
    if (closestContinuous.distance < threshold) {
      return closestContinuous.type;
    }
    return import_xy_chart_internals.BAR_VARIANT;
  }
  if (rectPresent) {
    if (closestContinuous.distance < threshold) {
      return closestContinuous.type;
    }
    return import_xy_chart_internals.RECT_VARIANT;
  }
  return closestContinuous.type;
}
