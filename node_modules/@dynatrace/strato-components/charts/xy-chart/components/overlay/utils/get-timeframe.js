var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_timeframe_exports = {};
__export(get_timeframe_exports, {
  getTimeframe: () => getTimeframe
});
module.exports = __toCommonJS(get_timeframe_exports);
var import_detect_scale = require("../../../../core/utils/detect-scale.js");
const getInvertedValue = (selectPosition, scale) => {
  if (!scale || (0, import_detect_scale.isBandScale)(scale)) {
    return void 0;
  }
  const inverted = scale.invert?.(selectPosition);
  return inverted !== void 0 ? new Date(inverted) : void 0;
};
const getTimeframe = (start, end, isAxisNotReversed, scale) => {
  return {
    start: getInvertedValue(isAxisNotReversed ? start : end, scale),
    end: getInvertedValue(isAxisNotReversed ? end : start, scale)
  };
};
