var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var draw_shape_exports = {};
__export(draw_shape_exports, {
  drawShapeFill: () => drawShapeFill,
  drawShapeStroke: () => drawShapeStroke
});
module.exports = __toCommonJS(draw_shape_exports);
var import_colors = __toESM(require("@dynatrace/strato-design-tokens/colors"));
var import_canvas_color = require("../../../../core/components/canvas/utils/canvas-color.js");
const TRIANGLE_RADIUS_ADJUSTMENT = 1;
const drawShapeFill = (ctx, point, radius) => {
  drawShape(ctx, point, radius, true, false);
};
const drawShapeStroke = (ctx, point, radius, strokeColor) => {
  drawShape(ctx, point, radius, false, true, strokeColor);
};
const createPathCircle = (ctx, point, radius) => {
  ctx.arc(point.x, point.y, radius, 0, 2 * Math.PI);
};
const createPathSquare = (radius, ctx, point) => {
  const s = radius;
  ctx.rect(point.x - s, point.y - s, 2 * s, 2 * s);
};
const createPathTriangle = (radius, ctx, point) => {
  const r = radius - TRIANGLE_RADIUS_ADJUSTMENT;
  const R = 2 * r;
  const side = r * 2 * Math.sqrt(3);
  ctx.beginPath();
  ctx.moveTo(point.x, point.y - R);
  ctx.lineTo(point.x + side / 2, point.y + r);
  ctx.lineTo(point.x - side / 2, point.y + r);
  ctx.closePath();
};
const createPathDiamond = (radius, ctx, point) => {
  const s = radius * Math.sqrt(2);
  ctx.beginPath();
  ctx.moveTo(point.x, point.y - s);
  ctx.lineTo(point.x + s, point.y);
  ctx.lineTo(point.x, point.y + s);
  ctx.lineTo(point.x - s, point.y);
  ctx.closePath();
};
const drawShape = (ctx, point, radius, fillShape, strokeShape, strokeColor) => {
  ctx.beginPath();
  switch (point.shape) {
    case "circle":
      createPathCircle(ctx, point, radius);
      break;
    case "square": {
      createPathSquare(radius, ctx, point);
      break;
    }
    case "triangle": {
      createPathTriangle(radius, ctx, point);
      break;
    }
    case "diamond": {
      createPathDiamond(radius, ctx, point);
      break;
    }
    default:
      createPathCircle(ctx, point, radius);
  }
  if (fillShape) {
    ctx.fillStyle = (0, import_canvas_color.getCanvasColor)(point.color.Default);
    ctx.fill();
  }
  if (strokeShape) {
    ctx.globalAlpha = 0.7;
    ctx.fillStyle = (0, import_canvas_color.getCanvasColor)(import_colors.default.Background.Base.Default);
    ctx.fill();
    ctx.globalAlpha = 1;
    ctx.lineWidth = 1;
    ctx.strokeStyle = (0, import_canvas_color.getCanvasColor)(strokeColor);
    ctx.stroke();
  }
  ctx.closePath();
};
