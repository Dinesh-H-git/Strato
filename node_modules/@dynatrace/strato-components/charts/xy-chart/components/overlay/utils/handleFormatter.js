var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var handleFormatter_exports = {};
__export(handleFormatter_exports, {
  handleFormatter: () => handleFormatter
});
module.exports = __toCommonJS(handleFormatter_exports);
var import_units = require("@dynatrace-sdk/units");
var import_defaultUnitFormatter = require("../../../../core/utils/defaultUnitFormatter.js");
var import_format_date_based_on_domain = require("../../../../core/utils/format-date-based-on-domain.js");
var import_safe_get = require("../../../../core/utils/safe-get.js");
const handleFormatter = (axesConfig, axesIdToFormatter, scale, xMousePosition, width) => {
  const correspondentToDomain = scale.invert(xMousePosition);
  const isDate = correspondentToDomain instanceof Date;
  const formatterFromConfig = (0, import_safe_get.safeGet)(axesIdToFormatter, axesConfig.id);
  if (isDate) {
    if (axesConfig.formatter) {
      const dateTimeFormatter = formatterFromConfig;
      return dateTimeFormatter(new Date(correspondentToDomain));
    } else {
      return (0, import_format_date_based_on_domain.formatDateBasedOnDomain)(
        correspondentToDomain,
        scale?.domain(),
        width
      );
    }
  } else {
    if (axesConfig.isRelative) {
      return (0, import_defaultUnitFormatter.defaultUnitFormatter)(import_units.units.percentage.percent)(
        correspondentToDomain
      );
    }
    return formatterFromConfig(correspondentToDomain);
  }
};
