var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_crosshair_coordinates_exports = {};
__export(calculate_crosshair_coordinates_exports, {
  calculateCrosshairCoordinates: () => calculateCrosshairCoordinates
});
module.exports = __toCommonJS(calculate_crosshair_coordinates_exports);
var import_get_scale = require("../../../utils/get-scale.js");
var import_area_accessors = require("../../displays/area-accessors.js");
var import_dot_accessors = require("../../displays/dot-accessors.js");
function calculateCrosshairCoordinates({
  tooltipContentSeries,
  datapoint,
  axisIdToScales,
  datapointMetadataMap,
  xRelAccessor,
  yRelAccessor,
  isArea,
  isHorizontal,
  isStacked
}) {
  const xScale = (0, import_get_scale.getCurrentScale)(axisIdToScales, tooltipContentSeries.xAxisId);
  const yScale = (0, import_get_scale.getCurrentScale)(axisIdToScales, tooltipContentSeries.yAxisId);
  let x;
  if (isArea) {
    x = isHorizontal ? (0, import_area_accessors.x1Accessor)(
      xScale,
      datapointMetadataMap,
      xRelAccessor,
      isStacked
    )(datapoint) : (0, import_area_accessors.xAccessor)(xScale, xRelAccessor)(datapoint);
  } else {
    x = (0, import_dot_accessors.xAccessor)(
      xScale,
      xRelAccessor
    )(datapoint);
  }
  let y;
  if (isArea) {
    y = isHorizontal ? (0, import_area_accessors.yAccessor)(yScale, yRelAccessor)(datapoint) : (0, import_area_accessors.y1Accessor)(
      yScale,
      datapointMetadataMap,
      yRelAccessor,
      isStacked
    )(datapoint);
  } else {
    y = (0, import_dot_accessors.yAccessor)(
      yScale,
      yRelAccessor
    )(datapoint);
  }
  return { x, y };
}
