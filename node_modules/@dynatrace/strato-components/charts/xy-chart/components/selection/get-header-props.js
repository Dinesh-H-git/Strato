var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_header_props_exports = {};
__export(get_header_props_exports, {
  getHeaderProps: () => getHeaderProps
});
module.exports = __toCommonJS(get_header_props_exports);
var import_get_duration_content = require("../../../core/components/overlay-tooltip/components/header/utils/get-duration-content.js");
var import_scale = require("../../../core/utils/scale.js");
var import_guards = require("../../types/guards.js");
var import_get_timeframe = require("../overlay/utils/get-timeframe.js");
const getHeaderProps = ({
  axisConfig,
  scale,
  formatter,
  startPixels,
  endPixels,
  suffix
}) => {
  const config = {
    axisType: axisConfig.type,
    axisScale: scale
  };
  const timeframe = (0, import_get_timeframe.getTimeframe)(
    startPixels,
    endPixels,
    !axisConfig?.reversed,
    scale
  );
  if ((0, import_guards.isAnyNumericalScale)(config) && !(0, import_guards.isTimeScale)(config)) {
    const from = config.axisScale.invert(startPixels);
    const to = config.axisScale.invert(endPixels);
    return {
      [`header${suffix}`]: [formatter(from), formatter(to)],
      [`duration${suffix}`]: formatter(
        (0, import_get_duration_content.getDurationContent)(true, [from, to])
      )
    };
  }
  if ((0, import_guards.isTimeScale)(config)) {
    return {
      [`header${suffix}`]: [timeframe.start, timeframe.end],
      [`showRangeDuration${suffix}`]: true
    };
  }
  if ((0, import_guards.isCategoricalScale)(config)) {
    return {
      [`header${suffix}`]: [
        (0, import_scale.scaleBandInvert)(config.axisScale)(startPixels),
        (0, import_scale.scaleBandInvert)(config.axisScale)(endPixels)
      ]
    };
  }
};
