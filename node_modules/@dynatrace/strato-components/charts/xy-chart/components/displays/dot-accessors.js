var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var dot_accessors_exports = {};
__export(dot_accessors_exports, {
  xAccessor: () => xAccessor,
  yAccessor: () => yAccessor
});
module.exports = __toCommonJS(dot_accessors_exports);
var import_get_band_centered_point = require("../../utils/get-band-centered-point.js");
var import_get_center_date = require("../../utils/get-center-date.js");
var import_type_guards = require("../../utils/type-guards.js");
const getAccessor = (accessorKeys) => {
  return (scale, relAccessor) => {
    return (datapoint) => {
      const args = {
        scale,
        v0: datapoint[accessorKeys[0]],
        v1: datapoint[accessorKeys[1]]
      };
      if ((0, import_type_guards.isNumericalDotScale)(args)) {
        const { scale: scale2, v0 } = args;
        return scale2(relAccessor ? relAccessor(v0) : v0);
      }
      if ((0, import_type_guards.isTimeDotScale)(args)) {
        const value = (0, import_get_center_date.getCenterDate)(args.v0, args.v1);
        return args.scale(value);
      }
      if ((0, import_type_guards.isCategoricalDotScale)(args)) {
        const { scale: scale2, v0 } = args;
        return (0, import_get_band_centered_point.getBandCenteredPoint)(scale2, v0);
      }
      throw Error("unknown Accessor value for Dot series");
    };
  };
};
const xAccessor = (scale, relAccessor) => getAccessor(["x0", "x1"])(scale, relAccessor);
const yAccessor = (scale, relAccessor) => getAccessor(["y0", "y1"])(scale, relAccessor);
