var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var BarLabelOverlay_exports = {};
__export(BarLabelOverlay_exports, {
  BarLabelOverlay: () => BarLabelOverlay
});
module.exports = __toCommonJS(BarLabelOverlay_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_colors = __toESM(require("@dynatrace/strato-design-tokens/colors"));
var import_BarDisplay = require("./BarDisplay.js");
var import_BarLabelOverlay_css = require("./BarLabelOverlay.sty.js");
var import_HorizontalBarLabel = require("./HorizontalBarLabel.js");
var import_VerticalBarLabel = require("./VerticalBarLabel.js");
var import_canvas_color = require("../../../../core/components/canvas/utils/canvas-color.js");
var import_useGridPlotSize = require("../../../../core/components/chart-grid/hooks/useGridPlotSize.js");
var import_color_contrast = require("../../../../core/utils/color-contrast.js");
var import_safe_get = require("../../../../core/utils/safe-get.js");
var import_useBarData = require("../../../hooks/useBarData.js");
var import_useXYChartData = require("../../../hooks/useXYChartData.js");
var import_useXYChartTruncationMode = require("../../../hooks/useXYChartTruncationMode.js");
var import_axis = require("../../../types/axis.js");
var import_get_scale = require("../../../utils/get-scale.js");
var import_value_representation = require("../../../utils/value-representation.js");
var import_bar_accessors = require("../bar-accessors.js");
function filterToScaleDomain(data, scale, isVertical) {
  const domainFn = scale?.domain;
  if (typeof domainFn !== "function") {
    return data;
  }
  const domain = domainFn.call(scale);
  if (!Array.isArray(domain)) {
    return data;
  }
  const allowed = new Set(domain);
  return data.filter((d) => {
    const key = isVertical ? d.x0 : d.y0;
    return typeof key === "string" ? allowed.has(key) : true;
  });
}
function BarLabelOverlay({
  axisIdToScales,
  series,
  metadata
}) {
  const { width: canvasWidth, height: canvasHeight } = (0, import_useGridPlotSize.useGridPlotSize)();
  const { datapointMetadataMap, axisIdToScaleDomain } = (0, import_useXYChartData.useXYChartData)();
  const { seriesToMetadata } = (0, import_useBarData.useBarData)();
  const { truncationMode } = (0, import_useXYChartTruncationMode.useXYChartTruncationMode)();
  if (!series.hasLabels) {
    return null;
  }
  const xScale = (0, import_get_scale.getCurrentScale)(
    axisIdToScales,
    series.xAxisId,
    (0, import_BarDisplay.isXBaselineAccessor)(series.data[0]) ? series.type : void 0
  );
  const dimensionXScale = (0, import_get_scale.getDimensionScale)(axisIdToScales, series.xAxisId);
  const yScale = (0, import_get_scale.getCurrentScale)(
    axisIdToScales,
    series.yAxisId,
    (0, import_BarDisplay.isYBaselineAccessor)(series.data[0]) ? series.type : void 0
  );
  const dimensionYScale = (0, import_get_scale.getDimensionScale)(axisIdToScales, series.yAxisId);
  const { xAxis, yAxis } = (0, import_safe_get.safeGet)(seriesToMetadata, series);
  const isVertical = xAxis.type === import_axis.XYAxisTypeCategorical;
  const isRTL = !isVertical && Boolean(xAxis.reversed);
  const isTTB = isVertical && Boolean(yAxis.reversed);
  const xFn = (0, import_bar_accessors.xAccessor)(
    xScale,
    datapointMetadataMap,
    dimensionXScale,
    series.id,
    (0, import_value_representation.relAccessor)(xAxis, axisIdToScaleDomain)
  );
  const yFn = (0, import_bar_accessors.yAccessor)(
    yScale,
    datapointMetadataMap,
    dimensionYScale,
    series.id,
    (0, import_value_representation.relAccessor)(yAxis, axisIdToScaleDomain)
  );
  const wFn = (0, import_bar_accessors.widthAccessor)(
    xScale,
    datapointMetadataMap,
    dimensionXScale,
    (0, import_value_representation.relAccessor)(xAxis, axisIdToScaleDomain)
  );
  const hFn = (0, import_bar_accessors.heightAccessor)(
    yScale,
    datapointMetadataMap,
    dimensionYScale,
    (0, import_value_representation.relAccessor)(yAxis, axisIdToScaleDomain)
  );
  const fillHex = (0, import_canvas_color.getCanvasColor)(metadata.color.Default);
  const insideColor = (0, import_color_contrast.getColorByContrast)(fillHex);
  const outsideColor = import_colors.default.Text.Neutral.Default;
  const data = filterToScaleDomain(
    series.data,
    isVertical ? xScale : yScale,
    isVertical
  );
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: import_BarLabelOverlay_css.overlayContainer, children: data.map((datum, index) => {
    const text = datum.label;
    if (!text) {
      return null;
    }
    const x = xFn(datum);
    const y = yFn(datum);
    const w = wFn(datum);
    const h = hFn(datum);
    const barWidth = Math.abs(w);
    const barHeight = Math.abs(h);
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: isVertical ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      import_VerticalBarLabel.VerticalBarLabel,
      {
        x,
        y,
        barWidth,
        barHeight,
        canvasHeight,
        isReversed: isTTB,
        text,
        insideColor,
        outsideColor,
        truncationMode
      }
    ) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      import_HorizontalBarLabel.HorizontalBarLabel,
      {
        x,
        y,
        barWidth,
        barHeight,
        canvasWidth,
        isReversed: isRTL,
        text,
        insideColor,
        outsideColor,
        truncationMode
      }
    ) }, index);
  }) });
}
BarLabelOverlay["displayName"] = "BarLabelOverlay";
