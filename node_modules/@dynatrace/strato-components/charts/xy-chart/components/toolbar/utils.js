var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var utils_exports = {};
__export(utils_exports, {
  allowWhenKeyIsEnter: () => allowWhenKeyIsEnter,
  areAllDomainsUnchanged: () => areAllDomainsUnchanged
});
module.exports = __toCommonJS(utils_exports);
var import_safe_get = require("../../../core/utils/safe-get.js");
var import_zoom = require("../../../core/utils/zoom.js");
var import_axis_interactions = require("../../utils/axis-interactions.js");
const allowWhenKeyIs = (key) => (action) => (event) => {
  if (event.key === key) {
    action();
  }
};
const allowWhenKeyIsEnter = allowWhenKeyIs("Enter");
function areAllDomainsUnchanged(axes, axisIdToScale) {
  return axes.every((axis) => {
    if ((0, import_axis_interactions.isZoomEnabledFor)(axis)) {
      const { currentScale, initialScale } = (0, import_safe_get.safeGet)(axisIdToScale, axis.id);
      return (0, import_zoom.isSameDomain)(
        currentScale.domain(),
        initialScale.domain()
      );
    }
    return true;
  });
}
