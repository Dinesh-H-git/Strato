var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var XYChartLegend_exports = {};
__export(XYChartLegend_exports, {
  XYChartLegend: () => XYChartLegend
});
module.exports = __toCommonJS(XYChartLegend_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_RectRangeLegend = require("./RectRangeLegend/RectRangeLegend.js");
var import_XYLegend = require("./XYLegend/XYLegend.js");
var import_safe_get = require("../../../core/utils/safe-get.js");
var import_useXYChartData = require("../../hooks/useXYChartData.js");
var import_is_range_legend = require("../../utils/is-range-legend.js");
var import_type_guards = require("../../utils/type-guards.js");
function buildLegendIdentityKey(series, seriesToMetadata) {
  return series.map((s) => {
    const metadata = (0, import_safe_get.safeGet)(seriesToMetadata, s);
    const color = "color" in metadata ? metadata.color.Default : "";
    return `${s.id}:${color}`;
  }).join(",");
}
const XYChartLegend = () => {
  const { seriesWithDatapoints, seriesToMetadata } = (0, import_useXYChartData.useXYChartData)();
  const identityKey = buildLegendIdentityKey(
    seriesWithDatapoints,
    seriesToMetadata
  );
  const stableSeries = (0, import_react.useMemo)(() => seriesWithDatapoints, [identityKey]);
  const stableMetadata = (0, import_react.useMemo)(() => seriesToMetadata, [identityKey]);
  const firstSeries = stableSeries[0];
  if ((0, import_type_guards.isRectSeries)(firstSeries)) {
    const { valueType: firstSeriesValueType } = (0, import_safe_get.safeGet)(
      stableMetadata,
      firstSeries
    );
    if (!firstSeriesValueType) {
      return null;
    }
    if ((0, import_is_range_legend.isRangeLegend)(stableMetadata, firstSeries)) {
      return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_RectRangeLegend.RectRangeLegend, { series: firstSeries });
    }
  }
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_XYLegend.XYLegend, { series: stableSeries, seriesToMetadata: stableMetadata });
};
