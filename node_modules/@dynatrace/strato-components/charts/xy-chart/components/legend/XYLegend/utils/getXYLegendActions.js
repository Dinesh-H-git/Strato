var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getXYLegendActions_exports = {};
__export(getXYLegendActions_exports, {
  getXYLegendActions: () => getXYLegendActions
});
module.exports = __toCommonJS(getXYLegendActions_exports);
var import_react = require("react");
var import_ChartSeriesAction = require("../../../../../core/components/series-action/ChartSeriesAction.js");
var import_safe_get = require("../../../../../core/utils/safe-get.js");
var import_xy_chart_internals = require("../../../../types/xy-chart-internals.js");
function filterRectDatapointsByCategory(series, categoryName) {
  return series.data.filter((datapoint) => datapoint.value === categoryName);
}
function getXYLegendActions(seriesWithDatapoints, seriesToMetadata) {
  return (item) => {
    if (!item) {
      return;
    }
    const currentSeries = seriesWithDatapoints.find((s) => {
      if (s.type === import_xy_chart_internals.RECT_VARIANT) {
        return item.id === `${s.id}--${item.name}`;
      }
      return s.id === item.id;
    });
    if (currentSeries) {
      const metadata = (0, import_safe_get.safeGet)(seriesToMetadata, currentSeries);
      const datapoints = currentSeries.type === import_xy_chart_internals.RECT_VARIANT ? filterRectDatapointsByCategory(currentSeries, item.name) : currentSeries.data;
      const isAreaOrBarSeries = currentSeries.type === import_xy_chart_internals.AREA_VARIANT || currentSeries.type === import_xy_chart_internals.BAR_VARIANT;
      const firstDatapoint = datapoints[0];
      const datapointElement = isAreaOrBarSeries ? { datapoint: firstDatapoint, name: item.name } : firstDatapoint;
      const seriesActions = metadata.actions?.(
        datapointElement,
        { datapoints, name: item.name }
      );
      if (seriesActions) {
        if (seriesActions instanceof Promise || seriesActions.type === import_ChartSeriesAction.ChartSeriesAction || seriesActions.type === import_react.Fragment) {
          return seriesActions;
        } else {
          throw new Error("Invalid type passed for getXYLegendActions!");
        }
      }
    }
  };
}
