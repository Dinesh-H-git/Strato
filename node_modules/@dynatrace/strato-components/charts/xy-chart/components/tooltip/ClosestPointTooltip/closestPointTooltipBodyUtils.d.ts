import type { IntlShape } from 'react-intl';
import type { AxisIdToFormatter, AxisIdToScaleDomain, MapDatapoints } from '../../../types/xy-chart-internals.js';
import { type ClosestDatapoint, type ClosestSeries, type ClosestSeriesToMetadata } from '../../../utils/type-guards.js';
import type { XYChartTooltipBodyProps } from '../XYChartTooltipBody/XYChartTooltipBody.js';
export interface BuildClosestPointTooltipBodyPropsArgs<T extends ClosestSeries, K extends ClosestDatapoint<T>> {
    datapoint: K;
    series: T;
    seriesToMetadata: ClosestSeriesToMetadata<T>;
    axisIdToFormatter: AxisIdToFormatter;
    axisIdToScaleDomain: AxisIdToScaleDomain;
    datapointMetadataMap: MapDatapoints;
    intl: IntlShape;
}
export declare const buildClosestPointTooltipBodyProps: <T extends ClosestSeries, K extends ClosestDatapoint<T>>({ datapoint, series, seriesToMetadata, axisIdToFormatter, axisIdToScaleDomain, datapointMetadataMap, intl, }: BuildClosestPointTooltipBodyPropsArgs<T, K>) => XYChartTooltipBodyProps;
