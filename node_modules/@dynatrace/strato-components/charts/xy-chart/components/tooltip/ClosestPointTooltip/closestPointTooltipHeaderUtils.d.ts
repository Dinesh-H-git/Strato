import type { AreaSeriesWithDatapoints, MapDatapoints } from '../../../types/xy-chart-internals.js';
import { type ClosestDatapoint, type ClosestSeries, type ClosestSeriesToMetadata } from '../../../utils/type-guards.js';
import type { XYChartTooltipHeaderProps } from '../XYChartTooltipHeader/XYChartTooltipHeader.js';
export interface BuildClosestPointTooltipHeaderPropsArgs<T extends ClosestSeries, K extends ClosestDatapoint<T>> {
    datapoint: K;
    series: T;
    seriesToMetadata: ClosestSeriesToMetadata<T>;
    seriesWithDatapoints: AreaSeriesWithDatapoints[];
    datapointMetadataMap: MapDatapoints;
    truncationMode?: XYChartTooltipHeaderProps['truncationMode'];
    idFactory?: () => string;
}
export declare const buildClosestPointTooltipHeaderProps: <T extends ClosestSeries, K extends ClosestDatapoint<T>>({ datapoint, series, seriesToMetadata, seriesWithDatapoints, datapointMetadataMap, truncationMode, idFactory, }: BuildClosestPointTooltipHeaderPropsArgs<T, K>) => XYChartTooltipHeaderProps;
