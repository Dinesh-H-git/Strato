var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getClosestPointTooltipBodyContent_exports = {};
__export(getClosestPointTooltipBodyContent_exports, {
  getClosestPointTooltipBodyContent: () => getClosestPointTooltipBodyContent
});
module.exports = __toCommonJS(getClosestPointTooltipBodyContent_exports);
var import_resolution_formatter = require("../../../../../core/components/tooltip/utils/resolution-formatter.js");
var import_type_guards = require("../../../../utils/type-guards.js");
function getClosestPointTooltipBodyContent([v0, v1], formatter, toPercentage) {
  let text = "";
  let duration;
  const displayValue = v0 ?? v1;
  if (displayValue === void 0) {
    return { text, duration };
  }
  const hasRange = v0 !== void 0 && v1 !== void 0 && v0 !== v1;
  const args = {
    formatter,
    v0: displayValue,
    v1
  };
  if ((0, import_type_guards.isCategoricalTooltip)(args)) {
    if (hasRange) {
      text = `${v0} - ${v1}`;
    } else {
      text = args.v0;
    }
  } else if ((0, import_type_guards.isNumericalToolTip)(args)) {
    if (hasRange) {
      text = `${args.formatter(args.v0)}${toPercentage ? " (" + toPercentage(args.v0) + "%)" : ""} - ${args.formatter(args.v1)}${toPercentage ? " (" + toPercentage(args.v1) + "%)" : ""}`;
    } else {
      text = `${args.formatter(args.v0)}${toPercentage ? " (" + toPercentage(args.v0) + "%)" : ""}`;
    }
  } else if ((0, import_type_guards.isTimeTooltip)(args)) {
    text = `${args.formatter(args.v0, args.v1)}`;
    duration = (0, import_resolution_formatter.formatResolution)(args.v0, args.v1);
  }
  return { duration, text };
}
