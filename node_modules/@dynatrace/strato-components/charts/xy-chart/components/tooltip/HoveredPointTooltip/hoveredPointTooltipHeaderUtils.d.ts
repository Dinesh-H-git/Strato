import type { SeriesIdToColorScale } from '../../../context/XYChartColorScales.context.js';
import type { BarSeriesWithDatapoints, RectDatapointInternal, MapDatapoints } from '../../../types/xy-chart-internals.js';
import type { BarDatapoint } from '../../../types/xy-chart.js';
import { type HoveredDatapoint, type HoveredSeries, type HoveredSeriesToMetadata } from '../../../utils/type-guards.js';
import type { XYChartTooltipHeaderProps } from '../XYChartTooltipHeader/XYChartTooltipHeader.js';
export interface BuildHoveredPointTooltipHeaderPropsArgs<T extends HoveredSeries, K extends HoveredDatapoint<T>> {
    datapoint: K;
    series: T;
    seriesToMetadata: HoveredSeriesToMetadata<T>;
    seriesWithDatapoints: BarSeriesWithDatapoints[];
    datapointMetadataMap: MapDatapoints;
    colorScaleMap: SeriesIdToColorScale;
    truncationMode?: XYChartTooltipHeaderProps['truncationMode'];
    idFactory?: () => string;
}
export declare const buildHoveredPointTooltipHeaderProps: <T extends HoveredSeries, K extends T extends BarSeriesWithDatapoints ? BarDatapoint : RectDatapointInternal>({ datapoint, series, seriesToMetadata, seriesWithDatapoints, datapointMetadataMap, colorScaleMap, truncationMode, idFactory, }: BuildHoveredPointTooltipHeaderPropsArgs<T, K>) => XYChartTooltipHeaderProps;
