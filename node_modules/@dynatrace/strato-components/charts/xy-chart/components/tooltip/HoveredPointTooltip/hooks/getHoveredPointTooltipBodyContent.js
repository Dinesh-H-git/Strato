var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getHoveredPointTooltipBodyContent_exports = {};
__export(getHoveredPointTooltipBodyContent_exports, {
  getHoveredPointTooltipBodyContent: () => getHoveredPointTooltipBodyContent
});
module.exports = __toCommonJS(getHoveredPointTooltipBodyContent_exports);
var import_resolution_formatter = require("../../../../../core/components/tooltip/utils/resolution-formatter.js");
var import_messages = require("../../../../messages/messages.js");
var import_type_guards = require("../../../../utils/type-guards.js");
function getHoveredPointTooltipBodyContent(intl, v0, v1, formatter, toPercentage) {
  const separator = intl.formatMessage(import_messages.XYCHART_MESSAGES.separatorDash);
  let duration;
  let text = "";
  const primaryValue = v0 ?? v1;
  const secondaryValue = v0 !== void 0 && v1 !== void 0 ? v1 : void 0;
  if (primaryValue === void 0) {
    return { duration, text };
  }
  const args = {
    formatter,
    v0: primaryValue,
    v1: secondaryValue
  };
  if ((0, import_type_guards.isCategoricalTooltip)(args)) {
    const format = formatter;
    const formatValue = (v) => format ? format(v) : v;
    const startText = formatValue(String(args.v0));
    const endText = args.v1 === void 0 ? startText : formatValue(String(args.v1));
    text = args.v1 === void 0 ? startText : `${startText} ${separator} ${endText}`;
    return { duration, text };
  }
  if ((0, import_type_guards.isNumericalToolTip)(args) && formatter) {
    const format = formatter;
    const percentageSuffix = (v) => toPercentage ? ` (${toPercentage(v)}%)` : "";
    const formattedStart = `${format(args.v0)}${percentageSuffix(
      args.v0
    )}`;
    const formattedEnd = args.v1 === void 0 ? formattedStart : `${format(args.v1)}${percentageSuffix(args.v1)}`;
    text = args.v1 === void 0 ? formattedStart : `${formattedStart} ${separator} ${formattedEnd}`;
    return { duration, text };
  }
  if ((0, import_type_guards.isTimeTooltip)(args) && formatter) {
    const format = formatter;
    if (args.v0 instanceof Date) {
      text = format(args.v0, args.v1 instanceof Date ? args.v1 : void 0);
      if (args.v1 instanceof Date) {
        duration = (0, import_resolution_formatter.formatResolution)(args.v0, args.v1);
      }
    }
  }
  return { duration, text };
}
