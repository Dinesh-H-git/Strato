import type { IntlShape } from 'react-intl';
import type { FormatCategory } from '../../../hooks/useFormatCategory.js';
import type { AxisIdToFormatter, AxisIdToScaleDomain, MapDatapoints } from '../../../types/xy-chart-internals.js';
import type { HoveredDatapoint, HoveredSeries, HoveredSeriesToMetadata } from '../../../utils/type-guards.js';
import type { XYChartTooltipBodyProps } from '../XYChartTooltipBody/XYChartTooltipBody.js';
export interface BuildHoveredPointTooltipBodyPropsArgs<T extends HoveredSeries, K extends HoveredDatapoint<T>> {
    datapoint: K;
    series: T;
    seriesToMetadata: HoveredSeriesToMetadata<T>;
    axisIdToFormatter: AxisIdToFormatter;
    axisIdToScaleDomain: AxisIdToScaleDomain;
    datapointMetadataMap: MapDatapoints;
    formatCategory: FormatCategory;
    intl: IntlShape;
}
export declare const buildHoveredPointTooltipBodyProps: <T extends HoveredSeries, K extends HoveredDatapoint<T>>({ datapoint, series, seriesToMetadata, axisIdToFormatter, axisIdToScaleDomain, datapointMetadataMap, formatCategory, intl, }: BuildHoveredPointTooltipBodyPropsArgs<T, K>) => XYChartTooltipBodyProps;
