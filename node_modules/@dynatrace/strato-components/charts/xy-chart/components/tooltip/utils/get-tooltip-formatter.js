var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_tooltip_formatter_exports = {};
__export(get_tooltip_formatter_exports, {
  getTooltipFormatter: () => getTooltipFormatter
});
module.exports = __toCommonJS(get_tooltip_formatter_exports);
var import_units = require("@dynatrace-sdk/units");
var import_safe_get = require("../../../../core/utils/safe-get.js");
var import_messages = require("../../../messages/messages.js");
var import_xy_chart = require("../../../types/xy-chart.js");
function getTooltipFormatter(axisConfig, axisIdToFormatter, intl) {
  const separator = intl.formatMessage(import_messages.XYCHART_MESSAGES.separatorDash);
  const getFormatter = () => (0, import_safe_get.safeGet)(axisIdToFormatter, axisConfig.id);
  const formatTimeValue = (from, to) => {
    if (to) {
      const timeframeParts = (0, import_units.formatTimeframeToParts)(
        from,
        to,
        intl,
        true,
        import_messages.XYCHART_MESSAGES.separatorDash
      );
      return typeof timeframeParts === "string" ? timeframeParts : timeframeParts.join(" ");
    }
    return (0, import_units.formatTimeframeToParts)(from, from, intl)[0];
  };
  if (axisConfig.type === import_xy_chart.TIME_SCALE_TYPE) {
    if (!axisConfig.formatter) {
      return formatTimeValue;
    }
    return (from, to) => {
      const formatter = getFormatter();
      if (to) {
        return `${formatter(from)} ${separator} ${formatter(to)}`;
      }
      return formatter(from);
    };
  }
  return getFormatter();
}
