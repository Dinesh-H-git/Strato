var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var XYChartAxesTicksProvider_exports = {};
__export(XYChartAxesTicksProvider_exports, {
  XYChartAxesTicksProvider: () => XYChartAxesTicksProvider,
  getTicksForAxis: () => getTicksForAxis
});
module.exports = __toCommonJS(XYChartAxesTicksProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_units = require("@dynatrace-sdk/units");
var import_useGridPlotSize = require("../../core/components/chart-grid/hooks/useGridPlotSize.js");
var import_formatter_by_ticks = require("../../core/utils/formatter-by-ticks.js");
var import_safe_get = require("../../core/utils/safe-get.js");
var import_ticks = require("../../core/utils/ticks.js");
var import_XYChartAxesTicks_context = require("../context/axes/XYChartAxesTicks.context.js");
var import_useAxesFormatter = require("../hooks/useAxesFormatter.js");
var import_useFormatCategory = require("../hooks/useFormatCategory.js");
var import_useVisibleAxesConfig = require("../hooks/useVisibleAxesConfig.js");
var import_useVisibleAxesScales = require("../hooks/useVisibleAxesScales.js");
var import_guards = require("../types/guards.js");
var import_generate_nice_unit_ticks = require("../utils/generate-nice-unit-ticks.js");
var import_get_ticks_formatted_by_config = require("../utils/get-ticks-formatted-by-config.js");
function getFormatter(numTicks, axisIdToFormatter, config, scale, formatCategory) {
  if ((0, import_guards.isCategorical)(config.type)) {
    const stringFormatter = (raw) => formatCategory(config, raw) ?? raw;
    return stringFormatter;
  }
  return config?.isRelative ? (0, import_formatter_by_ticks.getFormatterByTicks)(numTicks, scale, import_units.units.percentage.percent) : (0, import_safe_get.safeGet)(axisIdToFormatter, config.id);
}
function getTicksForAxis(config, numTicks, scale, formatter, availableSize, orientation) {
  if (config && !("categories" in config) && (0, import_generate_nice_unit_ticks.shouldUseNiceUnitTicks)(config) && scale !== void 0 && formatter !== void 0) {
    const domain = scale.domain();
    const niceTicks = (0, import_generate_nice_unit_ticks.computeNiceUnitTicks)(
      domain,
      config.formatter,
      availableSize,
      orientation,
      formatter,
      scale
    );
    if (niceTicks !== null) {
      return niceTicks;
    }
  }
  return (0, import_get_ticks_formatted_by_config.getTicksFormattedByConfig)(
    config,
    numTicks,
    scale,
    formatter,
    orientation === "horizontal" ? availableSize : void 0
  );
}
function XYChartAxesTicksProvider({
  children
}) {
  const { width, height } = (0, import_useGridPlotSize.useGridPlotSize)();
  const axisIdToFormatter = (0, import_useAxesFormatter.useAxesFormatter)();
  const config = (0, import_useVisibleAxesConfig.useVisibleAxesConfig)();
  const scales = (0, import_useVisibleAxesScales.useVisibleAxesScales)();
  const horizontalNumTicks = (0, import_ticks.getHorizontalNumTicks)(width);
  const verticalNumTicks = (0, import_ticks.getVerticalNumTicks)(height);
  const formatCategory = (0, import_useFormatCategory.useFormatCategory)();
  const topScale = scales.top;
  const topConfig = config.top;
  const topFormatter = topConfig ? getFormatter(
    horizontalNumTicks,
    axisIdToFormatter,
    topConfig,
    topScale,
    formatCategory
  ) : void 0;
  const topTicks = getTicksForAxis(
    topConfig,
    horizontalNumTicks,
    topScale,
    topFormatter,
    width,
    "horizontal"
  );
  const bottomScale = scales.bottom;
  const bottomConfig = config.bottom;
  const bottomFormatter = bottomConfig ? getFormatter(
    horizontalNumTicks,
    axisIdToFormatter,
    bottomConfig,
    bottomScale,
    formatCategory
  ) : void 0;
  const bottomTicks = getTicksForAxis(
    bottomConfig,
    horizontalNumTicks,
    bottomScale,
    bottomFormatter,
    width,
    "horizontal"
  );
  const leftScale = scales.left;
  const leftConfig = config.left;
  const leftFormatter = leftConfig ? getFormatter(
    verticalNumTicks,
    axisIdToFormatter,
    leftConfig,
    leftScale,
    formatCategory
  ) : void 0;
  const leftTicks = getTicksForAxis(
    leftConfig,
    verticalNumTicks,
    leftScale,
    leftFormatter,
    height,
    "vertical"
  );
  const rightScale = scales.right;
  const rightConfig = config.right;
  const rightFormatter = rightConfig ? getFormatter(
    verticalNumTicks,
    axisIdToFormatter,
    rightConfig,
    rightScale,
    formatCategory
  ) : void 0;
  const rightTicks = getTicksForAxis(
    rightConfig,
    verticalNumTicks,
    rightScale,
    rightFormatter,
    height,
    "vertical"
  );
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_XYChartAxesTicks_context.XYChartAxesTicksContext.Provider,
    {
      value: { topTicks, bottomTicks, leftTicks, rightTicks },
      children
    }
  );
}
