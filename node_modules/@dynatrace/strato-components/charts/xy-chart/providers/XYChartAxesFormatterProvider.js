var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var XYChartAxesFormatterProvider_exports = {};
__export(XYChartAxesFormatterProvider_exports, {
  XYChartAxesFormatterProvider: () => XYChartAxesFormatterProvider
});
module.exports = __toCommonJS(XYChartAxesFormatterProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_useGridPlotSize = require("../../core/components/chart-grid/hooks/useGridPlotSize.js");
var import_formatter_by_ticks = require("../../core/utils/formatter-by-ticks.js");
var import_get_time_formatter = require("../../core/utils/get-time-formatter.js");
var import_ticks = require("../../core/utils/ticks.js");
var import_XYChartAxesFormatter_context = require("../context/axes/XYChartAxesFormatter.context.js");
var import_useXYChartData = require("../hooks/useXYChartData.js");
var import_useXYChartScales = require("../hooks/useXYChartScales.js");
var import_guards = require("../types/guards.js");
var import_get_scale = require("../utils/get-scale.js");
var import_is_axis = require("../utils/is-axis.js");
function XYChartAxesFormatterProvider({
  children
}) {
  const { width, height } = (0, import_useGridPlotSize.useGridPlotSize)();
  const { axes } = (0, import_useXYChartData.useXYChartData)();
  const axisIdToScales = (0, import_useXYChartScales.useXYChartScales)();
  const axisIdToFormatter = /* @__PURE__ */ new Map();
  const horizontalNumTicks = (0, import_ticks.getHorizontalNumTicks)(width);
  const verticalNumTicks = (0, import_ticks.getVerticalNumTicks)(height);
  for (let i = 0; i < axes.length; i++) {
    const axis = axes[i];
    const isXCoordinate = (0, import_is_axis.isXAxis)(axis);
    const numTicks = isXCoordinate ? horizontalNumTicks : verticalNumTicks;
    const scale = (0, import_get_scale.getCurrentScale)(axisIdToScales, axis.id);
    if ((0, import_guards.isNumericalAxis)(axis)) {
      const formatter = (0, import_formatter_by_ticks.getFormatterByTicks)(
        numTicks,
        scale,
        axis.unit,
        axis.formatter
      );
      axisIdToFormatter.set(axis.id, formatter);
    } else if ((0, import_guards.isTimeAxis)(axis)) {
      const formatter = (0, import_get_time_formatter.getTimeFormatter)(axis.formatter);
      axisIdToFormatter.set(axis.id, formatter);
    } else {
      const formatter = (0, import_formatter_by_ticks.getFormatterByTicks)(numTicks, scale);
      axisIdToFormatter.set(axis.id, formatter);
    }
  }
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_XYChartAxesFormatter_context.XYChartAxesFormatterContext.Provider, { value: axisIdToFormatter, children });
}
