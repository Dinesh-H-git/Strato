var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_calculated_linear_point_exports = {};
__export(get_calculated_linear_point_exports, {
  getCalculatedLinearPoint: () => getCalculatedLinearPoint,
  getCalculatedLinearPointForArea: () => getCalculatedLinearPointForArea
});
module.exports = __toCommonJS(get_calculated_linear_point_exports);
var import_guards = require("../../../types/guards.js");
var import_get_center_date = require("../../../utils/get-center-date.js");
function getCalculatedLinearPoint(datapoint, xAxis, yAxis) {
  let x0 = datapoint.x0 ?? datapoint.x1;
  let y0 = datapoint.y1 ?? datapoint.y0;
  if ((0, import_guards.isTimeAxis)(xAxis)) {
    x0 = (0, import_get_center_date.getCenterDate)(
      datapoint.x0,
      datapoint.x1
    );
  }
  if ((0, import_guards.isTimeAxis)(yAxis)) {
    y0 = (0, import_get_center_date.getCenterDate)(
      datapoint.y0,
      datapoint.y1
    );
  }
  return { x0, y0 };
}
function getCalculatedLinearPointForArea(datapoint, xAxis, yAxis, chartOrientation, variant) {
  if (variant === "area") {
    if (chartOrientation === "vertical") {
      let x02 = datapoint.x0 ?? datapoint.x1;
      let y12 = datapoint.y1 ?? datapoint.y0;
      if ((0, import_guards.isTimeAxis)(xAxis)) {
        x02 = (0, import_get_center_date.getCenterDate)(
          datapoint.x0,
          datapoint.x1
        );
      }
      if ((0, import_guards.isTimeAxis)(yAxis)) {
        y12 = (0, import_get_center_date.getCenterDate)(
          datapoint.y0,
          datapoint.y1
        );
      }
      return { x0: x02, y1: y12 };
    }
    let y02 = datapoint.y0 ?? datapoint.y1;
    let x1 = datapoint.x1 ?? datapoint.x0;
    if ((0, import_guards.isTimeAxis)(yAxis)) {
      y02 = (0, import_get_center_date.getCenterDate)(
        datapoint.y0,
        datapoint.y1
      );
    }
    if ((0, import_guards.isTimeAxis)(xAxis)) {
      x1 = (0, import_get_center_date.getCenterDate)(
        datapoint.x0,
        datapoint.x1
      );
    }
    return { y0: y02, x1 };
  }
  if (chartOrientation === "vertical") {
    let x02 = datapoint.x0;
    const y02 = datapoint.y0;
    const y12 = datapoint.y1;
    if ((0, import_guards.isTimeAxis)(xAxis)) {
      x02 = (0, import_get_center_date.getCenterDate)(
        datapoint.x0,
        datapoint.x1
      );
    }
    return { x0: x02, y0: y02, y1: y12 };
  }
  if (chartOrientation === "horizontal") {
    const x02 = datapoint.x0;
    const x1 = datapoint.x1;
    let y02 = datapoint.y0;
    if ((0, import_guards.isTimeAxis)(yAxis)) {
      y02 = (0, import_get_center_date.getCenterDate)(
        datapoint.y0,
        datapoint.y1
      );
    }
    return { y0: y02, x0: x02, x1 };
  }
  let x0 = datapoint.x0;
  const y0 = datapoint.y0;
  const y1 = datapoint.y1;
  if ((0, import_guards.isTimeAxis)(xAxis)) {
    x0 = (0, import_get_center_date.getCenterDate)(
      datapoint.x0,
      datapoint.x1
    );
  }
  return { x0, y0, y1 };
}
