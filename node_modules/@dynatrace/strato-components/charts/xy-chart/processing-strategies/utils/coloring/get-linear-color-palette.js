var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_linear_color_palette_exports = {};
__export(get_linear_color_palette_exports, {
  getLinearColor: () => getLinearColor
});
module.exports = __toCommonJS(get_linear_color_palette_exports);
var import_get_valid_color_palette = require("./get-valid-color-palette.js");
var import_get_color_palette = require("../../../../core/utils/get-color-palette.js");
var import_defaults = require("../../../constants/defaults.js");
var import_get_color_selection = require("../../../utils/coloring/get-color-selection.js");
function getLinearColor(datum, seriesProps, configColorPalette, seriesName, seriesIndex) {
  const validatedColorPalette = (0, import_get_color_selection.getColorPaletteSelection)(seriesProps.colorPalette, datum) ?? configColorPalette ?? import_defaults.DEFAULT_COLOR_PALETTE;
  const assignedColor = (0, import_get_color_palette.getColorPalette)(
    seriesName,
    seriesIndex,
    // @ts-expect-error - todo
    validatedColorPalette
  );
  const colorSelection = (0, import_get_color_selection.getColorSelection)(seriesProps.color, datum) ?? assignedColor;
  const validColorPalette = (0, import_get_valid_color_palette.getValidColorPalette)(
    configColorPalette,
    validatedColorPalette
  );
  const validColor = colorSelection ? colorSelection : (0, import_get_color_palette.getColorPalette)(
    seriesName,
    seriesIndex,
    // @ts-expect-error - todo
    validColorPalette
  );
  return {
    color: validColor,
    colorPalette: validColorPalette,
    colorIsFromSlot: !!seriesProps.color || validatedColorPalette === seriesProps.colorPalette
  };
}
