var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_value_scale_domain_exports = {};
__export(get_value_scale_domain_exports, {
  getValueScaleDomain: () => getValueScaleDomain
});
module.exports = __toCommonJS(get_value_scale_domain_exports);
const valueScaleDomainByType = {
  number: (args) => {
    const minMaxValue = args.seriesIdToValueScaleDomain.get(args.seriesId) ?? {
      min: Infinity,
      max: -Infinity
    };
    minMaxValue.min = Math.min(minMaxValue.min, args.value);
    minMaxValue.max = Math.max(minMaxValue.max, args.value);
    return minMaxValue;
  },
  string: (args) => {
    const categorySet = args.seriesIdToValueScaleDomain.get(args.seriesId) ?? /* @__PURE__ */ new Set();
    categorySet.add(args.value);
    return categorySet;
  }
};
function getValueScaleDomain(value, seriesId, seriesIdToValueScaleDomain) {
  return valueScaleDomainByType[typeof value]({
    seriesIdToValueScaleDomain,
    seriesId,
    value
  });
}
