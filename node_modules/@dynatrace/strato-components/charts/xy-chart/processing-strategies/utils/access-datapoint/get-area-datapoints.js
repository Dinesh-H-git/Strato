var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_area_datapoints_exports = {};
__export(get_area_datapoints_exports, {
  getAreaDatapoints: () => getAreaDatapoints
});
module.exports = __toCommonJS(get_area_datapoints_exports);
var import_get_datapoints = require("./get-datapoints.js");
var import_number_of_coordinates_by_dimension = require("./number-of-coordinates-by-dimension.js");
function getAreaDatapoints(datum, seriesProps, xAxis, yAxis) {
  const xAxisType = (0, import_get_datapoints.getAxisType)("x", xAxis, yAxis);
  const yAxisType = (0, import_get_datapoints.getAxisType)("y", xAxis, yAxis);
  const point = {
    x0: (0, import_get_datapoints.getPoint)(datum, seriesProps.x0Accessor, xAxisType),
    x1: (0, import_get_datapoints.getPoint)(datum, seriesProps.x1Accessor, xAxisType),
    y0: (0, import_get_datapoints.getPoint)(datum, seriesProps.y0Accessor, yAxisType),
    y1: (0, import_get_datapoints.getPoint)(datum, seriesProps.y1Accessor, yAxisType)
  };
  const { xCoordinateCount, yCoordinateCount } = (0, import_number_of_coordinates_by_dimension.numberOfCoordinatesByDimension)(point);
  if (xCoordinateCount === 0 || yCoordinateCount === 0) {
    return void 0;
  }
  if (xCoordinateCount + yCoordinateCount === 2) {
    const invalidCombinations = point.x0 !== void 0 && point.y0 !== void 0 || point.x1 !== void 0 && point.y1 !== void 0;
    if (invalidCombinations) {
      return void 0;
    }
  }
  return {
    ...datum,
    ...point
  };
}
