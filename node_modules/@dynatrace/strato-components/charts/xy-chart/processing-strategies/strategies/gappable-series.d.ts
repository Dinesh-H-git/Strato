import type { AreaSeriesWithDatapoints, LineSeriesWithDatapoints, XYChartAxisInternal } from '../../types/xy-chart-internals.js';
import type { AreaDatapoint, LineDatapoint } from '../../types/xy-chart.js';
export declare class GappableSeriesHelper<T extends AreaDatapoint | LineDatapoint> {
    private readonly lastDatapointPerSeries;
    constructor();
    /** Given a current series, it detects gaps between consecutive datapoints */
    handleGapDetection(datapoint: T, currentSeries: LineSeriesWithDatapoints | AreaSeriesWithDatapoints, xAxis: XYChartAxisInternal, yAxis: XYChartAxisInternal): T | undefined;
}
