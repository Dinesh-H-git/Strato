var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var gappable_series_exports = {};
__export(gappable_series_exports, {
  GappableSeriesHelper: () => GappableSeriesHelper
});
module.exports = __toCommonJS(gappable_series_exports);
var import_get_gap_policy_information = require("../utils/gap/get-gap-policy-information.js");
class GappableSeriesHelper {
  lastDatapointPerSeries;
  constructor() {
    this.lastDatapointPerSeries = /* @__PURE__ */ new Map();
  }
  /** Given a current series, it detects gaps between consecutive datapoints */
  handleGapDetection(datapoint, currentSeries, xAxis, yAxis) {
    const lastDatapoint = this.lastDatapointPerSeries.get(currentSeries.id);
    let gappedDatapoint;
    if (lastDatapoint) {
      const isGap = (0, import_get_gap_policy_information.detectGapBetweenDatapoints)(
        lastDatapoint,
        datapoint,
        xAxis,
        yAxis
      );
      if (isGap) {
        gappedDatapoint = { ...datapoint };
      }
    }
    this.lastDatapointPerSeries.set(currentSeries.id, datapoint);
    return gappedDatapoint;
  }
}
