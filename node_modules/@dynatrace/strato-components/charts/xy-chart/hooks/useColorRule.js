var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useColorRule_exports = {};
__export(useColorRule_exports, {
  useColorRule: () => useColorRule
});
module.exports = __toCommonJS(useColorRule_exports);
var import_ColorRuleContext = require("../../core/context/ColorRuleContext.js");
var import_safe_get = require("../../core/utils/safe-get.js");
var import_validators = require("../../core/utils/validators.js");
var import_xy_chart_internals = require("../types/xy-chart-internals.js");
var import_apply_color_rule_to_rect_series = require("../utils/coloring/apply-color-rule-to-rect-series.js");
var import_apply_color_rule_to_series_metadata = require("../utils/coloring/apply-color-rule-to-series-metadata.js");
var import_has_series_matching_datapoint = require("../utils/coloring/has-series-matching-datapoint.js");
const useColorRule = ({
  seriesWithDatapoints,
  seriesToMetadata,
  datapointMetadataMap
}) => {
  const colorRules = (0, import_ColorRuleContext.useColorRuleConfig)();
  if (!colorRules.length) {
    return seriesToMetadata;
  }
  const reversedRules = colorRules.toReversed();
  const isValid = (0, import_validators.buildValidator)(import_validators.VALUE_MATCHERS);
  seriesWithDatapoints.forEach((series, index) => {
    const seriesMetadata = (0, import_safe_get.safeGet)(seriesToMetadata, series);
    if (seriesMetadata.colorIsFromSlot) {
      return;
    }
    if (series.type === import_xy_chart_internals.RECT_VARIANT) {
      const categoricalColorOverrides = (0, import_apply_color_rule_to_rect_series.applyColorRuleToRectSeries)(
        series,
        reversedRules,
        isValid,
        datapointMetadataMap
      );
      if (categoricalColorOverrides) {
        seriesToMetadata.set(series, {
          ...seriesMetadata,
          categoricalColorOverrides
        });
      }
    } else {
      const validColorRule = reversedRules.find(
        (rule) => (0, import_has_series_matching_datapoint.hasSeriesMatchingDatapoint)(series, rule, isValid)
      );
      if (validColorRule) {
        (0, import_apply_color_rule_to_series_metadata.applyColorRuleToSeriesMetadata)(
          seriesToMetadata,
          series,
          seriesMetadata,
          validColorRule,
          index
        );
      }
    }
  });
  return seriesToMetadata;
};
