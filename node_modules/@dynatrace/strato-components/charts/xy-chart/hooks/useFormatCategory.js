var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useFormatCategory_exports = {};
__export(useFormatCategory_exports, {
  useFormatCategory: () => useFormatCategory
});
module.exports = __toCommonJS(useFormatCategory_exports);
var import_react = require("react");
var import_units = require("@dynatrace-sdk/units");
var import_useVisibleAxesConfig = require("./useVisibleAxesConfig.js");
var import_formatter = require("../../core/types/formatter.js");
var import_XYChartData_context = require("../context/XYChartData.context.js");
var import_guards = require("../types/guards.js");
const useFormatCategory = () => {
  const config = (0, import_useVisibleAxesConfig.useVisibleAxesConfig)();
  const context = (0, import_react.useContext)(import_XYChartData_context.XYChartDataContext);
  if (context === void 0) {
    throw new Error(
      "useFormatCategory must be used within a XYChartDataContext"
    );
  }
  const { axisIdToCategoryDatum } = context;
  return (0, import_react.useCallback)(
    (axisOrPosition, raw) => {
      if (typeof raw !== "string") {
        return raw;
      }
      const axis = typeof axisOrPosition === "string" ? config[axisOrPosition] : axisOrPosition;
      if (!axis || !(0, import_guards.isCategorical)(axis.type)) {
        return raw;
      }
      const formatter = axis.formatter;
      if (!formatter) {
        return raw;
      }
      const numericFormatter = formatter;
      if ((0, import_formatter.isFormatterOptions)(numericFormatter)) {
        return (0, import_units.format)(Number(raw), numericFormatter);
      }
      const datum = axisIdToCategoryDatum.get(axis.id)?.get(raw) ?? {};
      return formatter(raw, datum);
    },
    [config, axisIdToCategoryDatum]
  );
};
