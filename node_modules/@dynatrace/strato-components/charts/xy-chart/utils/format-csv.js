var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var format_csv_exports = {};
__export(format_csv_exports, {
  formatCSV: () => formatCSV
});
module.exports = __toCommonJS(format_csv_exports);
var import_sanitize_and_parse_data = require("../../core/utils/sanitize-and-parse-data.js");
function formatCSV(data, isValidDatapoint, getSeriesName) {
  const csvColumns = ["series", "type", "x0", "x1", "y0", "y1", "value"];
  let csv = `${csvColumns.join(",")}
`;
  for (const series of data) {
    const seriesRow = [getSeriesName(series), series.type];
    for (const datapoint of series.data) {
      if (!isValidDatapoint(series.type, datapoint)) {
        continue;
      }
      const parsedX0 = (0, import_sanitize_and_parse_data.sanitizeAndParseData)(datapoint.x0);
      const parsedX1 = (0, import_sanitize_and_parse_data.sanitizeAndParseData)(datapoint.x1);
      const parsedY0 = (0, import_sanitize_and_parse_data.sanitizeAndParseData)(datapoint.y0);
      const parsedY1 = (0, import_sanitize_and_parse_data.sanitizeAndParseData)(datapoint.y1);
      const parsedValue = (0, import_sanitize_and_parse_data.sanitizeAndParseData)(
        "value" in datapoint ? datapoint.value : void 0
      );
      const datapointsRow = [
        parsedX0,
        parsedX1,
        parsedY0,
        parsedY1,
        parsedValue
      ];
      csv += `${seriesRow.concat(datapointsRow.toString()).join(",")}
`;
    }
  }
  return csv;
}
