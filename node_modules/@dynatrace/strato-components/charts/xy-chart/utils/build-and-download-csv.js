var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_and_download_csv_exports = {};
__export(build_and_download_csv_exports, {
  buildAndDownloadCsv: () => buildAndDownloadCsv,
  getSeriesName: () => getSeriesName
});
module.exports = __toCommonJS(build_and_download_csv_exports);
var import_format_csv = require("./format-csv.js");
var import_validation_datapoint = require("./validation-datapoint.js");
var import_download = require("../../../core/utils/download.js");
var import_constants = require("../../core/constants.js");
const getSeriesName = (metadata) => {
  return (series) => {
    return metadata.get(series)?.name;
  };
};
const buildAndDownloadCsv = (data, seriesMetadata, datapointMetadata, downloadDataCallback) => {
  if (downloadDataCallback) {
    downloadDataCallback();
  } else {
    const csvString = (0, import_format_csv.formatCSV)(
      data,
      (0, import_validation_datapoint.isValidDatapointForDownload)(datapointMetadata),
      getSeriesName(seriesMetadata)
    );
    (0, import_download.downloadCSV)(csvString, import_constants.CSS_DOWNLOAD_FILENAME);
  }
};
