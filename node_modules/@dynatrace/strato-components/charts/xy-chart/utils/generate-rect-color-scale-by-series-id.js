var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var generate_rect_color_scale_by_series_id_exports = {};
__export(generate_rect_color_scale_by_series_id_exports, {
  generateRectColorScaleBySeriesId: () => generateRectColorScaleBySeriesId
});
module.exports = __toCommonJS(generate_rect_color_scale_by_series_id_exports);
var import_is_value_domain = require("./is-value-domain.js");
var import_constants = require("../../core/constants.js");
var import_colored_range = require("../../core/types/colored-range.js");
var import_build_categorical_color_scale = require("../../core/utils/build-categorical-color-scale.js");
var import_build_quantize_scale_from_color_palette = require("../../core/utils/build-quantize-scale-from-color-palette.js");
var import_build_threshold_scale_from_ranges = require("../../core/utils/build-threshold-scale-from-ranges.js");
var import_safe_get = require("../../core/utils/safe-get.js");
var import_defaults = require("../constants/defaults.js");
const fitColorRangeToMinMax = (coloredRange, limits) => {
  const { from, to } = coloredRange;
  const { min, max } = limits;
  const isOutOfBounds = max !== void 0 && from > max || min !== void 0 && to < min;
  const returnObject = coloredRange;
  if (isOutOfBounds) {
    return null;
  }
  if (max !== void 0 && max < to) {
    returnObject.to = max;
  }
  if (min !== void 0 && min > from) {
    returnObject.from = min;
  }
  return returnObject;
};
const fitColorRangePaletteToMinMax = (colorPalette, limits) => {
  const temporalColorRanges = [];
  limits.min !== void 0 && limits.max !== void 0 && limits.min > limits.max ? limits.min = limits.min + limits.max - (limits.max = limits.min) : colorPalette.forEach((colorRange, index) => {
    const newRange = fitColorRangeToMinMax(colorRange, limits);
    if (newRange) {
      temporalColorRanges.push(newRange);
    }
  });
  colorPalette.length = 0;
  if (temporalColorRanges.length === 0) {
    temporalColorRanges.push({
      from: limits.min !== void 0 ? limits.min : -Infinity,
      to: limits.max !== void 0 ? limits.max : Infinity
    });
  }
  colorPalette.push(...temporalColorRanges);
};
function generateRectColorScaleBySeriesId(seriesRect, rectSeriesToMetadata, seriesIdToValueScaleDomain) {
  const map = /* @__PURE__ */ new Map();
  if (seriesIdToValueScaleDomain.size === 0) {
    return map;
  }
  seriesRect.forEach((series) => {
    const scaleDomain = (0, import_safe_get.safeGet)(seriesIdToValueScaleDomain, series.id);
    const { colorPalette, valueMin, valueMax } = (0, import_safe_get.safeGet)(
      rectSeriesToMetadata,
      series
    );
    if ((0, import_is_value_domain.isMinMaxDomain)(scaleDomain)) {
      if ((0, import_colored_range.isColoredRangeArray)(colorPalette)) {
        fitColorRangePaletteToMinMax(colorPalette, {
          min: valueMin,
          max: valueMax
        });
        const scale = (0, import_build_threshold_scale_from_ranges.buildThresholdScaleFromRanges)(
          colorPalette,
          import_constants.SCALE_FALLBACK_COLOR
        );
        map.set(series.id, scale);
      } else {
        const scale = (0, import_build_quantize_scale_from_color_palette.buildQuantizeScaleFromColorPalette)(
          valueMin ?? scaleDomain.min,
          valueMax ?? scaleDomain.max,
          colorPalette
        );
        map.set(series.id, scale);
      }
    } else if ((0, import_is_value_domain.isCategoricalDomain)(scaleDomain)) {
      const categoricalColorPalette = !(0, import_colored_range.isColoredRangeArray)(colorPalette) ? colorPalette : import_defaults.DEFAULT_COLOR_PALETTE_RECT;
      const scale = (0, import_build_categorical_color_scale.buildCategoricalColorScale)(categoricalColorPalette, [
        ...scaleDomain.values()
      ]);
      map.set(series.id, scale);
    }
  });
  return map;
}
