import type { RectSeriesWithDatapoints, RectSeriesIdToValueScaleDomain, XYChartColorScale, RectSeriesToMetadata } from '../types/xy-chart-internals.js';
/**
 * Generates a rect color scale for each series ID.
 *
 * @param seriesRect - Array of rect series with datapoints.
 * @param rectSeriesToMetadata - Metadata for each rect series.
 * @param seriesIdToValueScaleDomain - Mapping of series ID to their value scale domains.
 * @returns - A map of series IDs to their corresponding color scales.
 */
export declare function generateRectColorScaleBySeriesId(seriesRect: RectSeriesWithDatapoints[], rectSeriesToMetadata: RectSeriesToMetadata, seriesIdToValueScaleDomain: RectSeriesIdToValueScaleDomain): Map<string, XYChartColorScale>;
