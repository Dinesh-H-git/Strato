import { type ReactNode } from 'react';
import { type XYChartSlots } from '../types/xy-chart-internals.js';
/**
 * Extracts XY chart's slots
 */
export declare const iterateSlots: (children: ReactNode) => XYChartSlots;
