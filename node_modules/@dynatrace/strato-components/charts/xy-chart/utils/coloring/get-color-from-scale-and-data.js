var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_color_from_scale_and_data_exports = {};
__export(get_color_from_scale_and_data_exports, {
  getColorFromScaleAndData: () => getColorFromScaleAndData
});
module.exports = __toCommonJS(get_color_from_scale_and_data_exports);
var import_get_color_from_numeric_scale = require("./get-color-from-numeric-scale.js");
var import_constants = require("../../../core/constants.js");
var import_guards = require("../../types/guards.js");
const getColorFromScaleAndData = (colorScale, value, colorOverride) => {
  if (colorOverride) {
    return colorOverride;
  }
  if ((0, import_guards.isString)(value)) {
    return colorScale(value);
  }
  if ((0, import_guards.isNumericColoringScale)(colorScale)) {
    return (0, import_get_color_from_numeric_scale.getColorFromNumericScale)(colorScale, value);
  }
  return import_constants.SCALE_FALLBACK_COLOR;
};
