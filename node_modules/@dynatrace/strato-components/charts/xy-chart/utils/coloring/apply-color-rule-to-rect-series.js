var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var apply_color_rule_to_rect_series_exports = {};
__export(apply_color_rule_to_rect_series_exports, {
  applyColorRuleToRectSeries: () => applyColorRuleToRectSeries
});
module.exports = __toCommonJS(apply_color_rule_to_rect_series_exports);
var import_get_color_from_rule = require("./get-color-from-rule.js");
var import_rect_datapoint_matches_color_rule = require("./rect-datapoint-matches-color-rule.js");
var import_safe_get = require("../../../core/utils/safe-get.js");
var import_guards = require("../../types/guards.js");
const applyColorRuleToRectSeries = (series, rules, isValid, datapointMetadataMap) => {
  const categoricalColorOverrides = /* @__PURE__ */ new Map();
  series.data.forEach((datapoint) => {
    const matchingRule = rules.find(
      (rule) => (0, import_rect_datapoint_matches_color_rule.rectDatapointMatchesColorRule)(datapoint, rule, isValid)
    );
    if (matchingRule) {
      const color = (0, import_get_color_from_rule.getColorFromRule)(matchingRule);
      if (color) {
        const metadata = (0, import_safe_get.safeGet)(datapointMetadataMap, datapoint);
        if (metadata) {
          datapointMetadataMap.set(datapoint, {
            ...metadata,
            colorOverride: color
          });
        }
        if (typeof matchingRule.matchValue === "string" && (0, import_guards.isValidCategoryValue)(matchingRule.matchValue)) {
          categoricalColorOverrides.set(matchingRule.matchValue, color);
        }
      }
    }
  });
  return categoricalColorOverrides;
};
