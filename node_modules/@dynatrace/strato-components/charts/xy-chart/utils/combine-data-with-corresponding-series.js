var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var combine_data_with_corresponding_series_exports = {};
__export(combine_data_with_corresponding_series_exports, {
  combineDataWithCorrespondingSeries: () => combineDataWithCorrespondingSeries
});
module.exports = __toCommonJS(combine_data_with_corresponding_series_exports);
function combineDataWithCorrespondingSeries(rootData, seriesWithAxes) {
  const combinedData = [
    {
      data: rootData,
      series: []
    }
  ];
  const ROOT_DATA_POSITION = 0;
  for (const series of seriesWithAxes) {
    const { seriesProps } = series;
    if (seriesProps.data) {
      combinedData.push({
        data: seriesProps.data,
        series: [{ ...series }]
      });
    } else {
      combinedData[ROOT_DATA_POSITION].series.push({ ...series });
    }
  }
  return combinedData;
}
