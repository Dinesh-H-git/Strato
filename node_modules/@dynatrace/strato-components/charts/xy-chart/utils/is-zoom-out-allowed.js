var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var is_zoom_out_allowed_exports = {};
__export(is_zoom_out_allowed_exports, {
  isZoomOutAllowed: () => isZoomOutAllowed
});
module.exports = __toCommonJS(is_zoom_out_allowed_exports);
var import_axis_interactions = require("./axis-interactions.js");
var import_get_initial_zoom_out = require("../../core/components/zoom/utils/get-initial-zoom-out.js");
var import_safe_get = require("../../core/utils/safe-get.js");
function getMinMax(axisIdToScaleDomain, axisId) {
  const aux = (0, import_safe_get.safeGet)(axisIdToScaleDomain, axisId);
  return [aux.min, aux.max];
}
function isZoomOutAllowed(axes, axisIdToScaleDomain) {
  const zoomableAxes = axes.filter(import_axis_interactions.isZoomEnabledFor);
  if (zoomableAxes.length === 0) {
    return false;
  }
  return zoomableAxes.every((axis) => {
    if (axis.interactions.infinite) {
      return true;
    }
    return (0, import_get_initial_zoom_out.getInitialZoomOut)(
      axis.interactions.current,
      axis.interactions.initial ?? getMinMax(axisIdToScaleDomain, axis.id)
    );
  });
}
