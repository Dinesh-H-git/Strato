import type { RectDatapointInternal, XYChartAxisInternal, XYChartSeriesWithDatapoints } from '../types/xy-chart-internals.js';
import type { AreaDatapoint, BarDatapoint, DotDatapoint, LineDatapoint } from '../types/xy-chart.js';
/**
 * Counts the total datapoints, series, and categories in a selection.
 *
 * @param selectedDatapoints - Array of selected datapoints per series
 * @param seriesWithDatapoints - Array of series with their datapoints
 * @param axes - Array of chart axes
 * @returns Object containing total counts for datapoints, series, and categories
 */
export declare const countDatapointsWithSeriesAndCategories: (selectedDatapoints: (DotDatapoint[] | RectDatapointInternal[] | LineDatapoint[] | BarDatapoint[] | AreaDatapoint[] | undefined)[], seriesWithDatapoints: XYChartSeriesWithDatapoints[], axes: XYChartAxisInternal[]) => {
    selectedDatapoints: number;
    totalSeries: number;
    totalCategories: number;
};
