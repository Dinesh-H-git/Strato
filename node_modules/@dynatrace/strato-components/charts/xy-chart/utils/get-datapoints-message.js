var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_datapoints_message_exports = {};
__export(get_datapoints_message_exports, {
  getDatapointsMessage: () => getDatapointsMessage
});
module.exports = __toCommonJS(get_datapoints_message_exports);
var import_messages = require("../messages/messages.js");
const getDatapointsMessage = (selectedDatapoints, seriesCount, totalChartSeries, categoriesCount = 0) => {
  if (!selectedDatapoints) {
    return import_messages.XYCHART_MESSAGES.noDatapoints;
  }
  const isSingleSeriesWithoutCategories = totalChartSeries === 1 && !categoriesCount;
  const isMixedSelection = seriesCount > 0 && categoriesCount > 0;
  if (isSingleSeriesWithoutCategories || isMixedSelection) {
    if (selectedDatapoints === 1) {
      return import_messages.XYCHART_MESSAGES.datapoint;
    } else {
      return import_messages.XYCHART_MESSAGES.datapoints;
    }
  }
  if (seriesCount) {
    return import_messages.XYCHART_MESSAGES.seriesDatapoints;
  }
  return import_messages.XYCHART_MESSAGES.categoriesDatapoints;
};
