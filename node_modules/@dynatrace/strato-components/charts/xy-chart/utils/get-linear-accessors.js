var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_linear_accessors_exports = {};
__export(get_linear_accessors_exports, {
  getLinearAccessors: () => getLinearAccessors
});
module.exports = __toCommonJS(get_linear_accessors_exports);
var import_resolve_area_accessor_key = require("./resolve-area-accessor-key.js");
var import_type_guards = require("./type-guards.js");
var import_displays = require("../components/displays/index.js");
var import_xy_chart_internals = require("../types/xy-chart-internals.js");
function getLinearAccessors(seriesType, metadata, datapointToMetadata, xRelAccessor, yRelAccessor) {
  let accessKey;
  if (seriesType === import_xy_chart_internals.AREA_VARIANT) {
    const { orientation, variant } = metadata;
    accessKey = (0, import_resolve_area_accessor_key.resolveAreaAccessKey)(variant, orientation);
  } else {
    accessKey = seriesType;
  }
  const pair = import_displays.LINEAR_ACCESSORS[accessKey];
  if ((0, import_type_guards.isHorizontalAreaAccessors)(pair)) {
    const { isStacked } = metadata;
    return {
      xAccessor: (scale) => pair.x(scale, datapointToMetadata, xRelAccessor, isStacked),
      yAccessor: (scale) => pair.y(scale, yRelAccessor)
    };
  } else if ((0, import_type_guards.isVerticalAreaAccessors)(pair)) {
    const { isStacked } = metadata;
    return {
      xAccessor: (scale) => pair.x(scale, xRelAccessor),
      yAccessor: (scale) => pair.y(scale, datapointToMetadata, yRelAccessor, isStacked)
    };
  } else {
    return {
      xAccessor: (scale) => pair.x(scale, xRelAccessor),
      yAccessor: (scale) => pair.y(scale, yRelAccessor)
    };
  }
}
