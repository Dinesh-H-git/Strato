var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var value_representation_exports = {};
__export(value_representation_exports, {
  getRelativeAccessor: () => getRelativeAccessor,
  relAccessor: () => relAccessor,
  toAbsolute: () => toAbsolute,
  toRelative: () => toRelative
});
module.exports = __toCommonJS(value_representation_exports);
var import_get_relative_value = require("../../core/utils/get-relative-value.js");
var import_safe_get = require("../../core/utils/safe-get.js");
const toRelative = (currentValue, minMaxDomain) => {
  return (0, import_get_relative_value.getRelativeValue)(
    currentValue,
    Math.max(Math.abs(minMaxDomain.max), Math.abs(minMaxDomain.min)),
    true
  );
};
const toAbsolute = (currentValue, minMaxDomain) => {
  const isNegativeValue = currentValue < 0;
  const total = Math.max(
    Math.abs(minMaxDomain.max),
    Math.abs(minMaxDomain.min)
  );
  const math = Math[isNegativeValue ? "ceil" : "floor"];
  return math(total * currentValue) / 100;
};
const getRelativeAccessor = (axis, domain) => {
  if (axis.isRelative) {
    return (value) => toRelative(value, domain);
  }
  return void 0;
};
const relAccessor = (axis, axisIdToScaleDomain) => {
  const minMaxDomain = (0, import_safe_get.safeGet)(axisIdToScaleDomain, axis.id);
  return getRelativeAccessor(axis, minMaxDomain);
};
