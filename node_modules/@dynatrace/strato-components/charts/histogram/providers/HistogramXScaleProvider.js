var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var HistogramXScaleProvider_exports = {};
__export(HistogramXScaleProvider_exports, {
  HistogramXScaleProvider: () => HistogramXScaleProvider
});
module.exports = __toCommonJS(HistogramXScaleProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_useGridPlotSize = require("../../core/components/chart-grid/hooks/useGridPlotSize.js");
var import_core_scale = require("../../core/utils/core-scale.js");
var import_HistogramXScale_context = require("../contexts/HistogramXScale.context.js");
var import_selectors = require("../hooks/store/selectors.js");
var import_useHistogramFilteredData = require("../hooks/useHistogramFilteredData.js");
var import_useHistogramZoomRanges = require("../hooks/useHistogramZoomRanges.js");
var import_useXAxisConfig = require("../hooks/useXAxisConfig.js");
const HistogramXScaleProvider = ({ children }) => {
  const { min: minConfig, max: maxConfig, scale } = (0, import_useXAxisConfig.useXAxisConfig)();
  const { minMaxXValue } = (0, import_useHistogramFilteredData.useHistogramFilteredData)();
  const { initialZoom } = (0, import_useHistogramZoomRanges.useHistogramZoomRanges)();
  const [initialZoomMin, initialZoomMax] = initialZoom ?? [];
  const { width } = (0, import_useGridPlotSize.useGridPlotSize)();
  const configMinMax = {
    min: initialZoomMin ?? minConfig,
    max: initialZoomMax ?? maxConfig
  };
  const initialXScale = (0, import_core_scale.buildScale)(
    scale,
    [0, width],
    minMaxXValue,
    configMinMax
  );
  const [min, max] = initialXScale.domain();
  const minMaxDomain = { min, max };
  const domain = (0, import_selectors.useCurrentDomainState)();
  const xScale = domain ? initialXScale.copy().domain(domain) : initialXScale;
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_HistogramXScale_context.HistogramXScale.Provider,
    {
      value: {
        initialXScale,
        xScale,
        minMaxBoundaries: minMaxDomain
      },
      children
    }
  );
};
HistogramXScaleProvider["displayName"] = "HistogramXScaleProvider";
