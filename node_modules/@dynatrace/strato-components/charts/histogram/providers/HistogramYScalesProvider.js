var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var HistogramYScalesProvider_exports = {};
__export(HistogramYScalesProvider_exports, {
  HistogramYScalesProvider: () => HistogramYScalesProvider
});
module.exports = __toCommonJS(HistogramYScalesProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_useGridPlotSize = require("../../core/components/chart-grid/hooks/useGridPlotSize.js");
var import_core_scale = require("../../core/utils/core-scale.js");
var import_HistogramYScales_context = require("../contexts/HistogramYScales.context.js");
var import_useHistogramFilteredData = require("../hooks/useHistogramFilteredData.js");
var import_useYAxisConfig = require("../hooks/useYAxisConfig.js");
const HistogramYScalesProvider = ({ children }) => {
  const { minMaxPerUnit, units } = (0, import_useHistogramFilteredData.useHistogramFilteredData)();
  const { height } = (0, import_useGridPlotSize.useGridPlotSize)();
  const { left: leftYAxis, right: rightYAxis } = (0, import_useYAxisConfig.useYAxisConfig)();
  const yScales = units.map((unit, i) => {
    if (i === 0) {
      const leftMinMaxValue = minMaxPerUnit.get(unit) ?? { min: 0, max: 0 };
      return (0, import_core_scale.buildScale)(
        leftYAxis?.scale ?? "linear",
        [height, 0],
        leftMinMaxValue,
        { min: leftYAxis?.min, max: leftYAxis?.max }
      );
    } else if (i === 1) {
      const rightMinMaxValue = minMaxPerUnit.get(unit) ?? { min: 0, max: 0 };
      return (0, import_core_scale.buildScale)(
        rightYAxis?.scale ?? "linear",
        [height, 0],
        rightMinMaxValue,
        { min: rightYAxis?.min, max: rightYAxis?.max }
      );
    } else {
      const minMaxValue = minMaxPerUnit.get(unit) ?? { min: 0, max: 0 };
      return (0, import_core_scale.buildScale)("linear", [height, 0], minMaxValue);
    }
  });
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_HistogramYScales_context.HistogramYScales.Provider, { value: yScales, children });
};
HistogramYScalesProvider["displayName"] = "HistogramYScalesProvider";
