import { type PropsWithChildren } from 'react';
interface HistogramStateSyncProviderProps extends PropsWithChildren {
    currentDomain?: [number, number];
    initialDomain?: [number, number];
    infiniteZoom?: boolean;
}
export declare const HistogramStateSyncProvider: {
    ({ currentDomain, initialDomain, infiniteZoom, children, }: HistogramStateSyncProviderProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
export {};
