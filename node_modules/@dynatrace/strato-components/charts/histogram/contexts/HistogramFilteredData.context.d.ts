import { ChartUnit } from '../../core/types/chart-unit.js';
import { WeakMapSeries, WeakMapBins, HistogramSeries } from '../types/histogram.js';
export interface HistogramFilteredData {
    series: HistogramSeries[];
    seriesMap: WeakMapSeries;
    binsMap: WeakMapBins;
    units: ChartUnit[];
    minMaxPerUnit: Map<ChartUnit, {
        min: number;
        max: number;
    }>;
    minMaxXValue: {
        min: number;
        max: number;
    };
}
export declare const HistogramFilteredDataContext: import("react").Context<HistogramFilteredData>;
