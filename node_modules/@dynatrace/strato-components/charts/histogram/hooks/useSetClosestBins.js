var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useSetClosestBins_exports = {};
__export(useSetClosestBins_exports, {
  useSetClosestBins: () => useSetClosestBins
});
module.exports = __toCommonJS(useSetClosestBins_exports);
var import_lodash_es = require("lodash");
var import_reducers = require("./store/reducers.js");
var import_state_setters = require("./store/state-setters.js");
var import_useHistogramFilteredData = require("./useHistogramFilteredData.js");
var import_useHistogramTooltip = require("./useHistogramTooltip.js");
var import_useHistogramXScale = require("./useHistogramXScale.js");
var import_useHistogramYScales = require("./useHistogramYScales.js");
var import_find_closest_bins = require("../components/overlays/utils/find-closest-bins.js");
const useSetClosestBins = () => {
  const setState = (0, import_state_setters.useSetState)();
  const { xScale } = (0, import_useHistogramXScale.useHistogramXScale)();
  const yScales = (0, import_useHistogramYScales.useHistogramYScales)();
  const { series, binsMap, seriesMap } = (0, import_useHistogramFilteredData.useHistogramFilteredData)();
  const { variant } = (0, import_useHistogramTooltip.useHistogramTooltip)();
  return (position) => {
    if (!(0, import_lodash_es.isUndefined)(position)) {
      const mouseX = xScale.invert(position.relative.x);
      const closestBinsX = (0, import_find_closest_bins.findClosestBinX)(
        mouseX,
        series,
        yScales,
        xScale,
        seriesMap,
        binsMap
      );
      const { closestBin, closestBinsGrouped, closestBinsByClosestOne } = (0, import_find_closest_bins.getClosestBins)(position.relative.y, closestBinsX);
      const closestBinsByVariant = variant === "grouped" ? (
        // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
        closestBinsGrouped.find(
          (innerArray) => innerArray.some((item) => item.id === closestBin?.id)
        )
      ) : closestBinsByClosestOne;
      setState(
        (prev) => (0, import_reducers.reduceClosestBins)(prev, {
          closestBin,
          closestBinsGrouped,
          closestBinsByVariant
        })
      );
      return closestBin?.id;
    }
    setState((prev) => (0, import_reducers.reduceClosestBins)(prev, void 0));
    return void 0;
  };
};
