import { HistogramToolbarSlotProps } from '../types/toolbar.js';
/**
 * Slot for configuring the toolbar within a HistogramChart.
 * @public
 */
export declare const Toolbar: {
    (props: HistogramToolbarSlotProps): null;
    displayName: string;
};
