/**
 * Props for the Pan slot component.
 * @public
 */
export interface PanSlotProps {
    /**
     * Whether the pan is disabled.
     */
    disabled?: boolean;
}
/**
 * Pan slot component.
 * @public
 */
export declare const Pan: {
    (props: PanSlotProps): null;
    displayName: string;
};
