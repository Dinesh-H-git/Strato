var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var accessors_exports = {};
__export(accessors_exports, {
  height: () => height,
  width: () => width,
  x: () => x,
  y: () => y
});
module.exports = __toCommonJS(accessors_exports);
var import_detect_scale = require("../../../core/utils/detect-scale.js");
var import_sanitize_zero_values = require("../../../core/utils/sanitize-zero-values.js");
var import_HistogramDataProvider = require("../../providers/HistogramDataProvider.js");
function width(xScale, binsMap) {
  return (bin) => Math.abs(
    xScale((0, import_HistogramDataProvider.safeGet)(binsMap, bin).to) - xScale((0, import_HistogramDataProvider.safeGet)(binsMap, bin).from)
  );
}
function height(scale, binsMap) {
  return (bin) => {
    const { value, cumulativeValue = 0 } = (0, import_HistogramDataProvider.safeGet)(binsMap, bin);
    const isLogValue = (0, import_detect_scale.isLogScale)(scale) && cumulativeValue !== value;
    if (isLogValue) {
      const baseCumulative = (0, import_sanitize_zero_values.sanitizeZeroValues)(scale, cumulativeValue - value);
      const cumulativeCoordinate = (0, import_sanitize_zero_values.sanitizeZeroValues)(scale, cumulativeValue);
      return baseCumulative - cumulativeCoordinate;
    }
    const valueCoordinate = (0, import_sanitize_zero_values.sanitizeZeroValues)(scale, value);
    const zeroCoordinate = (0, import_sanitize_zero_values.sanitizeZeroValues)(scale, 0);
    return zeroCoordinate - valueCoordinate;
  };
}
function x(xScale, binsMap) {
  return (bin) => xScale((0, import_HistogramDataProvider.safeGet)(binsMap, bin).from);
}
function y(yScale, binsMap) {
  return (bin) => {
    const c = yScale((0, import_HistogramDataProvider.safeGet)(binsMap, bin).cumulativeValue ?? 0);
    const max = yScale.invert(0);
    return yScale(max) + c;
  };
}
