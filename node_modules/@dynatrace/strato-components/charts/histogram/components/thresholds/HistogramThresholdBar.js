var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var HistogramThresholdBar_exports = {};
__export(HistogramThresholdBar_exports, {
  HistogramThresholdBar: () => HistogramThresholdBar
});
module.exports = __toCommonJS(HistogramThresholdBar_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_ThresholdBar = require("../../../core/components/threshold-bar/ThresholdBar.js");
var import_threshold_to_marker = require("../../../core/utils/threshold-to-marker.js");
var import_state_setters = require("../../hooks/store/state-setters.js");
var import_useHistogramThresholds = require("../../hooks/useHistogramThresholds.js");
var import_useHistogramXScale = require("../../hooks/useHistogramXScale.js");
var import_useHistogramYFormatter = require("../../hooks/useHistogramYFormatter.js");
var import_useHistogramYScales = require("../../hooks/useHistogramYScales.js");
const HistogramThresholdBar = (props) => {
  const { position, length } = props;
  const [leftScale, rightScale] = (0, import_useHistogramYScales.useHistogramYScales)();
  const { xScale } = (0, import_useHistogramXScale.useHistogramXScale)();
  const { leftThreshold, rightThreshold } = (0, import_useHistogramThresholds.useHistogramThresholds)();
  const [leftFormatter, rightFormatter] = (0, import_useHistogramYFormatter.useHistogramYFormatter)();
  const setState = (0, import_state_setters.useSetState)();
  const thresholds = position === "left" ? leftThreshold : rightThreshold;
  if (!thresholds || !thresholds.length) {
    return null;
  }
  const yScale = position === "left" ? leftScale : rightScale;
  const isFlipped = position !== "left";
  const axisFormatter = position === "left" ? leftFormatter : rightFormatter;
  const markers = thresholds.reduce(
    (0, import_threshold_to_marker.transformThresholdsToMarkers)(position, xScale, yScale, axisFormatter),
    []
  );
  const handleHover = (id) => {
    setState((prev) => ({
      ...prev,
      hoveredThresholdId: id
    }));
  };
  return markers.length ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_ThresholdBar.ThresholdBar,
    {
      markers,
      length,
      layout: "vertical",
      isFlipped,
      onHover: handleHover
    }
  ) : null;
};
HistogramThresholdBar["displayName"] = "HistogramThresholdBar";
