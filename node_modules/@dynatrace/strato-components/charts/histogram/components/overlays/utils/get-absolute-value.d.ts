import { Unit } from '@dynatrace-sdk/units';
import { TooltipValueMetadata } from '../../../types/histogram.js';
export declare function getAbsoluteValue(absoluteMetadata: TooltipValueMetadata, formatter: (value: number, unit?: string | Unit) => string): string | number;
