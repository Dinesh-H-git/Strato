var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_datapoints_details_exports = {};
__export(get_datapoints_details_exports, {
  getDataPointDetails: () => getDataPointDetails
});
module.exports = __toCommonJS(get_datapoints_details_exports);
var import_constants = require("../../../../core/components/data-point/constants.js");
var import_HistogramDataProvider = require("../../../providers/HistogramDataProvider.js");
const getDataPointDetails = (yScale, xScale, bin, binsMap) => {
  const { cumulativeValue, value, seriesId, color } = (0, import_HistogramDataProvider.safeGet)(binsMap, bin);
  const rangeValue = cumulativeValue ?? value ?? 0;
  const domainValue = yScale(rangeValue) ?? 0;
  const y = domainValue + import_constants.OVERLAY_PADDING;
  const x = xScale(bin.from + (bin.to - bin.from) / 2) + import_constants.OVERLAY_PADDING;
  return {
    id: seriesId,
    x,
    y,
    color
  };
};
