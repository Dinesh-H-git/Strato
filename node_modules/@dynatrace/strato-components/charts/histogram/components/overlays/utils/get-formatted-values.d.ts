import { Unit } from '@dynatrace-sdk/units';
import { BinMetadata } from '../../../types/histogram.js';
export declare function getFormattedValues(formatter: (value: number, unit?: string | Unit) => string, binMetadata: BinMetadata): string[];
