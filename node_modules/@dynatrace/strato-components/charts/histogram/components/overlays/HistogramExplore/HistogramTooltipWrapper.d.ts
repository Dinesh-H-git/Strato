import { type PropsWithChildren, type MouseEvent as ReactMouseEvent } from 'react';
import { TooltipItem } from '../../../../core/types/tooltip.js';
interface HistogramTooltipWrapperProps {
    tooltipItems: TooltipItem[];
    onMouseLeave: (event?: ReactMouseEvent) => void;
    onScrollContent: (event?: Event) => void;
}
export declare const HistogramTooltipWrapper: ({ tooltipItems, onMouseLeave, onScrollContent, children, }: PropsWithChildren<HistogramTooltipWrapperProps>) => import("react/jsx-runtime").JSX.Element | null;
export {};
