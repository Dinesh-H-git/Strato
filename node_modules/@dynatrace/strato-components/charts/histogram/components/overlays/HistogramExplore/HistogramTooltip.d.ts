import { type HistogramTooltipDataPoint } from '../utils/bins-to-tooltip.js';
interface HistogramTooltipProps {
    points: HistogramTooltipDataPoint[];
    closestPoint?: HistogramTooltipDataPoint;
}
/**
 * Tooltip specific to the Histogram.
 */
export declare const HistogramTooltip: ({ points, closestPoint, }: HistogramTooltipProps) => import("react/jsx-runtime").JSX.Element;
export {};
