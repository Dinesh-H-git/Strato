import type { HistogramSeries } from '../types/histogram.js';
/**
 * Return max series value
 * @param histogramSeries - given histogramSeries
 * @internal
 */
export declare const getMaxSeriesValue: (histogramSeries: HistogramSeries) => number;
