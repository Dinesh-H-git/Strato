import { HistogramSeries } from '../types/histogram.js';
/**
 * Builds CSV from Histogram raw data and downloads the file
 *
 * @param series - Histogram series
 */
export declare const buildAndDownloadCsv: (series: HistogramSeries[]) => void;
