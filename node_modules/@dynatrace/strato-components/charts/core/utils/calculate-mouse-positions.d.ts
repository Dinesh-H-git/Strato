import { type MouseEvent } from 'react';
type ClientPosition = Pick<MouseEvent, 'clientY' | 'clientX'>;
export declare const calculateMousePositions: ({ clientY, clientX }: ClientPosition, target: EventTarget & Element) => {
    absolute: {
        x: number;
        y: number;
    };
    relative: {
        x: number;
        y: number;
    };
};
export {};
