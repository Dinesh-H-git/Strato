var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var format_date_based_on_domain_exports = {};
__export(format_date_based_on_domain_exports, {
  formatDateBasedOnDomain: () => formatDateBasedOnDomain
});
module.exports = __toCommonJS(format_date_based_on_domain_exports);
var import_format_dates = require("./format-dates.js");
const THIRTY_MINUTES = 1e3 * 60 * 30;
const FIVE_MINUTES = 1e3 * 60 * 5;
const FIVE_SECONDS = 1e3 * 5;
const FIFTY_MILLISECONDS = 50;
function formatDateBasedOnDomain(date, domain, width) {
  if (width <= 0) {
    return "";
  }
  const [start, end] = domain;
  const timeInMs = date.getTime();
  const msPerPixel = (Math.max(start.getTime(), end.getTime()) - Math.min(start.getTime(), end.getTime())) / width;
  if (msPerPixel < FIFTY_MILLISECONDS) {
    return (0, import_format_dates.formatSeconds)(timeInMs);
  } else if (msPerPixel <= FIVE_SECONDS) {
    return (0, import_format_dates.formatMinute)(timeInMs);
  } else if (msPerPixel <= FIVE_MINUTES) {
    return (0, import_format_dates.formatHour)(timeInMs);
  } else if (msPerPixel <= THIRTY_MINUTES) {
    return (0, import_format_dates.formatDay)(timeInMs);
  } else {
    return (0, import_format_dates.formatWeekday)(timeInMs);
  }
}
