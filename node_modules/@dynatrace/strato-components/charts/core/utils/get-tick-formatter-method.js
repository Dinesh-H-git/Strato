var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_tick_formatter_method_exports = {};
__export(get_tick_formatter_method_exports, {
  getTickFormatterMethod: () => getTickFormatterMethod
});
module.exports = __toCommonJS(get_tick_formatter_method_exports);
var import_units = require("@dynatrace-sdk/units");
var import_defaultUnitFormatter = require("./defaultUnitFormatter.js");
var import_format_value = require("./format-value.js");
var import_formatter = require("../types/formatter.js");
const getTickFormatterMethod = (values, formatter, unit) => {
  let tickFormatter;
  const isDefaultFormatter = !formatter;
  const isCustomFormatter = !(0, import_formatter.isFormatterOptions)(formatter);
  const isCustomFormatterOrHasPrecisionDefined = isCustomFormatter || formatter.maximumFractionDigits || formatter.minimumFractionDigits;
  if (isDefaultFormatter) {
    const precisionFormatter = (value, unit2, precision2) => (0, import_defaultUnitFormatter.defaultUnitFormatter)(unit2, precision2)(value);
    const precision = (0, import_format_value.getFormatterPrecision)(values, precisionFormatter, unit);
    tickFormatter = (0, import_defaultUnitFormatter.defaultUnitFormatter)(unit, precision);
  } else if (isCustomFormatterOrHasPrecisionDefined) {
    tickFormatter = isCustomFormatter ? formatter : (value) => (0, import_units.format)(value, formatter);
  } else {
    const precisionFormatter = (value, unit2, precision2) => (0, import_units.format)(value, {
      maximumFractionDigits: precision2,
      minimumFractionDigits: precision2,
      ...formatter
    });
    const precision = (0, import_format_value.getFormatterPrecision)(values, precisionFormatter, unit);
    tickFormatter = (value) => (0, import_units.format)(value, {
      maximumFractionDigits: precision,
      ...formatter
    });
  }
  return tickFormatter;
};
