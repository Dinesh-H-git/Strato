var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var measure_font_baseline_exports = {};
__export(measure_font_baseline_exports, {
  measureFontBaseline: () => measureFontBaseline
});
module.exports = __toCommonJS(measure_font_baseline_exports);
var import_constants = require("../constants.js");
const getFont = (fontStyle, fontWeight, fontFamily) => ({
  fontStyle,
  fontWeight,
  fontSize: `${import_constants.FONT_BASE}px`,
  fontFamily
});
const getFontString = (textFont) => {
  const { fontStyle, fontWeight, fontSize, fontFamily } = textFont;
  return [fontStyle, fontWeight, fontSize, fontFamily].filter((value) => !!value).join(" ");
};
const getTextBBox = (text, textFont) => {
  const context = document.createElement("canvas").getContext("2d");
  if (context) {
    context.imageSmoothingEnabled = false;
    context.imageSmoothingQuality = "low";
    context.font = getFontString(textFont);
    const textMetrics = context.measureText(text);
    const actualTextWidth = Math.abs(textMetrics.actualBoundingBoxLeft) + Math.abs(textMetrics.actualBoundingBoxRight);
    const actualTextHeight = Math.abs(textMetrics.actualBoundingBoxAscent) + Math.abs(textMetrics.actualBoundingBoxDescent);
    return {
      width: Math.ceil(actualTextWidth),
      height: Math.ceil(actualTextHeight)
    };
  }
};
const measureFontBaseline = ({
  text,
  fontStyle,
  fontWeight,
  fontFamily
}) => {
  const font = getFont(fontStyle, fontWeight, fontFamily);
  return getTextBBox(text, font);
};
