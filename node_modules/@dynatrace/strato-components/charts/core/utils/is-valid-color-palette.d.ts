import { ColorPalette } from '../types/color-palette.js';
/**
 * Returns true if colorPalette is a valid ColorPalette
 * @param colorPalette - color palette
 */
export declare const isValidColorPalette: (colorPalette: unknown) => colorPalette is ColorPalette;
