import { type ColorPalette, type ColorToken, type CustomColorPalette, type ColorPaletteMode } from '../types/color-palette.js';
/**
 * Decides whether to assign a default palette or a custom one
 * @param name - name of the category as string
 * @param index - position of the category name
 * @param colorPalette - type of palette being passed in
 * @returns The proper color palette to be assigned
 */
export declare const getColorPalette: (name: string, index: number, colorPalette: ColorPalette | CustomColorPalette, colorPaletteMode?: ColorPaletteMode) => ColorToken;
