var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var formatter_by_ticks_exports = {};
__export(formatter_by_ticks_exports, {
  getCategoricalTicks: () => getCategoricalTicks,
  getDateNumericalFormattedTicks: () => getDateNumericalFormattedTicks,
  getFormattedTicks: () => getFormattedTicks,
  getFormatterByTicks: () => getFormatterByTicks
});
module.exports = __toCommonJS(formatter_by_ticks_exports);
var import_scale = require("@visx/scale");
var import_defaultUnitFormatter = require("./defaultUnitFormatter.js");
var import_format_value = require("./format-value.js");
var import_get_tick_formatter_method = require("./get-tick-formatter-method.js");
var import_tick_formatters = require("../components/axes/utils/tick-formatters.js");
function getFormatterByTicks(numTicks, scale, unit, formatter) {
  if (!scale) {
    return (0, import_defaultUnitFormatter.defaultUnitFormatter)(unit);
  }
  const ticks = (0, import_scale.getTicks)(scale, numTicks);
  return (0, import_get_tick_formatter_method.getTickFormatterMethod)(ticks, formatter, unit);
}
function getFormattedTicks(numTicks, scale, unit, formatter) {
  if (!scale) {
    return { ticks: [], formattedTicks: [] };
  }
  const ticks = (0, import_scale.getTicks)(scale, numTicks);
  const formattedTicks = (0, import_format_value.getTickValues)(
    ticks,
    (0, import_tick_formatters.getYAxisTickFormatter)(scale, unit, formatter, numTicks)
  );
  return { ticks, formattedTicks };
}
function getDateNumericalFormattedTicks(numTicks, scale, unit, formatter) {
  if (!scale || !formatter) {
    return { ticks: [], formattedTicks: [] };
  }
  const ticks = (0, import_scale.getTicks)(scale, numTicks);
  const formattedTicks = (0, import_format_value.getTickValues)(
    ticks,
    (value) => formatter(value, unit)
  );
  return { ticks, formattedTicks };
}
function getCategoricalTicks(scale, formatLabel) {
  if (!scale) {
    return { ticks: [], formattedTicks: [] };
  }
  const ticks = scale.domain();
  const formattedTicks = formatLabel ? ticks.map(formatLabel) : ticks;
  return { ticks, formattedTicks };
}
