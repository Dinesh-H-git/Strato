import type { ColorPalette, ColorPaletteDefinition } from '../types/color-palette.js';
/**
 * Maps all the basic color palettes, creating an inverted one for each of them.
 * @param coloringMap - All the basic color palettes without the inverted.
 * @returns - The whole color palette object
 */
export declare const prepareInvertedColorPalette: (coloringMap: Record<ColorPalette, ColorPaletteDefinition | Record<string, string>>) => any;
