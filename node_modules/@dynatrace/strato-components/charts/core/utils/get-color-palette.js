var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_color_palette_exports = {};
__export(get_color_palette_exports, {
  getColorPalette: () => getColorPalette
});
module.exports = __toCommonJS(get_color_palette_exports);
var import_constants = require("../constants.js");
var import_getValidColor = require("./getValidColor.js");
var import_color_palette = require("../types/color-palette.js");
var import_color_palettes_definitions = require("../types/color-palettes-definitions.js");
function getCustomRecordColor(name, colorPalette, defaultColor) {
  const lowerCasedName = name.toLowerCase();
  const colorKey = Object.keys(colorPalette).find(
    (key) => key.toLowerCase() === lowerCasedName
  ) ?? "";
  const color = colorPalette[colorKey];
  return { Default: (0, import_getValidColor.getValidColor)(color, defaultColor) };
}
const getColorPalette = (name, index, colorPalette, colorPaletteMode = "multi-color") => {
  if ((0, import_color_palette.isCustomColorPalette)(colorPalette)) {
    if ((0, import_color_palette.isCustomColorRecordPalette)(colorPalette)) {
      return getCustomRecordColor(
        colorPaletteMode === "single-color" ? Object.keys(colorPalette)[0] : name,
        colorPalette,
        import_constants.SCALE_FALLBACK_COLOR
      );
    }
    return {
      Default: colorPalette[index % colorPalette.length] ?? import_constants.SCALE_FALLBACK_COLOR
    };
  } else {
    const colorPaletteTokenMap = import_color_palettes_definitions.colorPaletteMap[colorPalette];
    const paletteKeys = Object.keys(colorPaletteTokenMap);
    const palette = paletteKeys[index % paletteKeys.length];
    if ((0, import_color_palette.shouldApplyAutomatically)(colorPalette)) {
      const colorMap = import_color_palettes_definitions.automaticColoringMap[colorPalette];
      return getCustomRecordColor(
        colorPaletteMode === "single-color" ? "" : name,
        colorMap,
        colorPaletteTokenMap[palette].Default
      );
    }
    return colorPaletteTokenMap[palette];
  }
};
