/**
 * Gets color if it's a valid CSS color, otherwise returns the default.
 *
 * @param color - color to validate
 */
export declare function getValidColor(color?: string, defaultColor?: string): string;
export declare const isValidCSSColor: (color: string) => boolean;
