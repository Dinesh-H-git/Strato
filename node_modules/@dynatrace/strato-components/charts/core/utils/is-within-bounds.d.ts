import type { ViewportEventPosition } from '../types/pointer-events.js';
export declare const isWithinBounds: (position: ViewportEventPosition, bounds: Pick<DOMRect, "top" | "left" | "bottom" | "right"> | undefined) => boolean;
