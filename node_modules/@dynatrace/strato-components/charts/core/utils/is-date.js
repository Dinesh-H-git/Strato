var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var is_date_exports = {};
__export(is_date_exports, {
  isDateCompatibleToTimestamp: () => isDateCompatibleToTimestamp,
  isDateString: () => isDateString,
  isParseableDate: () => isParseableDate,
  isValidTimestamp: () => isValidTimestamp
});
module.exports = __toCommonJS(is_date_exports);
var import_lodash_es = require("lodash");
const isDateString = (dateString) => {
  if (typeof dateString !== "string") {
    return false;
  }
  return isValidTimestamp(dateString);
};
const isValidTimestamp = (timestamp) => !isNaN(new Date(timestamp).getTime());
const isDateCompatibleToTimestamp = (input) => !(0, import_lodash_es.isUndefined)(input) && (isDateString(input) || (0, import_lodash_es.isDate)(input) || isValidTimestamp(input));
const isParseableDate = (value) => {
  if (value instanceof Date) {
    return true;
  }
  if (typeof value === "number") {
    return false;
  } else if (typeof value === "string") {
    if (!isNaN(Number(value))) {
      return !!new Date(Number(value)).getTime();
    } else {
      return !!new Date(value).getTime();
    }
  }
  return false;
};
