var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var normalize_colored_ranges_exports = {};
__export(normalize_colored_ranges_exports, {
  normalizeColoredRange: () => normalizeColoredRange,
  normalizeColoredRanges: () => normalizeColoredRanges
});
module.exports = __toCommonJS(normalize_colored_ranges_exports);
var import_getValidColor = require("./getValidColor.js");
const isOverlappingRange = (a, b) => b.from >= a.from && b.from < a.to;
const isContinuousRange = (a, b) => a.to === b.from;
const normalizeColoredRanges = (ranges, defaultColor) => {
  let [a] = ranges;
  a = normalizeColoredRange(a);
  const normalizedRange = [a];
  for (let i = 1; i < ranges.length; i++) {
    const b = normalizeColoredRange(ranges[i]);
    if (!isOverlappingRange(a, b)) {
      if (isContinuousRange(a, b)) {
        normalizedRange.push(b);
      } else {
        normalizedRange.push({
          from: a.to,
          to: b.from,
          color: defaultColor
        });
        normalizedRange.push(b);
      }
      a = b;
    }
  }
  return normalizedRange;
};
const normalizeColoredRange = (range) => {
  range.color = (0, import_getValidColor.getValidColor)(range.color);
  return range;
};
