var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_time_formatter_exports = {};
__export(get_time_formatter_exports, {
  getTimeFormatter: () => getTimeFormatter
});
module.exports = __toCommonJS(get_time_formatter_exports);
var import_lodash_es = require("lodash");
var import_units = require("@dynatrace-sdk/units");
var import_format_date_ticks = require("./format-date-ticks.js");
const getTimeFormatter = (axisFormatter) => {
  if ((0, import_lodash_es.isUndefined)(axisFormatter)) {
    return (value) => (0, import_format_date_ticks.formatDateTicks)(new Date(value));
  } else if ((0, import_lodash_es.isFunction)(axisFormatter)) {
    return (value) => axisFormatter(new Date(value));
  } else {
    return (value) => (0, import_units.formatDate)(value, axisFormatter);
  }
};
