var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_quantize_scale_from_color_palette_exports = {};
__export(build_quantize_scale_from_color_palette_exports, {
  buildQuantizeScaleFromColorPalette: () => buildQuantizeScaleFromColorPalette
});
module.exports = __toCommonJS(build_quantize_scale_from_color_palette_exports);
var import_d3_scale = require("d3-scale");
var import_color_palette = require("../types/color-palette.js");
var import_color_palettes_definitions = require("../types/color-palettes-definitions.js");
const buildQuantizeScaleFromColorPalette = (min, max, colorPalette) => {
  let colors;
  if ((0, import_color_palette.isCustomColorPalette)(colorPalette)) {
    if (Array.isArray(colorPalette)) {
      colors = colorPalette;
    } else {
      colors = Object.values(colorPalette);
    }
  } else {
    colors = Object.entries(import_color_palettes_definitions.colorPaletteMap[colorPalette]).map(([_, color]) => color.Default).reverse();
  }
  return (0, import_d3_scale.scaleQuantize)([min, max], colors);
};
