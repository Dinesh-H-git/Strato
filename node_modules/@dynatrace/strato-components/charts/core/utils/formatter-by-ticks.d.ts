import { AxisScale } from '@visx/axis';
import { type ScaleBand, type ScaleLinear, type ScaleLogarithmic, type ScaleSymLog, type ScaleTime } from 'd3-scale';
import { ConvertibleUnit, FormatOptions, Unit } from '@dynatrace-sdk/units';
import { ChartUnit } from '../types/chart-unit.js';
import { DateTimeFormatter, Formatter, FormatterWithPrecision } from '../types/formatter.js';
export interface TickAxis {
    ticks: string[];
    formattedTicks: string[];
    numericTicks?: number[];
}
export declare function getFormatterByTicks(numTicks: number, scale: ScaleTime<number, number> | ScaleLinear<number, number> | ScaleLogarithmic<number, number> | ScaleSymLog<number, number> | ScaleBand<string> | undefined, unit?: ChartUnit, formatter?: Formatter | FormatOptions<Unit, ConvertibleUnit> | undefined): FormatterWithPrecision;
export declare function getFormattedTicks(numTicks: number, scale: ScaleLogarithmic<number, number> | ScaleSymLog<number, number> | ScaleLinear<number, number> | ScaleTime<number, number> | undefined, unit?: ChartUnit, formatter?: Formatter | undefined): {
    ticks: any[];
    formattedTicks: string[];
};
export declare function getDateNumericalFormattedTicks(numTicks: number, scale: AxisScale | undefined, unit: ChartUnit | undefined, formatter: FormatterWithPrecision | DateTimeFormatter | undefined): {
    ticks: any[];
    formattedTicks: string[];
};
export declare function getCategoricalTicks(scale: AxisScale | undefined, formatLabel?: (raw: string) => string): {
    ticks: string[];
    formattedTicks: string[];
};
