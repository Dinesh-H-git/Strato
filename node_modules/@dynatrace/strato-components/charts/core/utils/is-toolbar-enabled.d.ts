import type { IntentProps } from '../../../core/slots/Intent/Intent.js';
export declare const isToolbarEnabled: <T extends {
    hidden?: boolean;
    downloadCSV?: {
        enabled?: boolean;
    };
    downloadData?: {
        enabled?: boolean;
    };
    intents?: Array<IntentProps>;
}>(config: T | undefined) => boolean;
