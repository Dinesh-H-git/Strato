var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getValidColor_exports = {};
__export(getValidColor_exports, {
  getValidColor: () => getValidColor,
  isValidCSSColor: () => isValidCSSColor
});
module.exports = __toCommonJS(getValidColor_exports);
var import_constants = require("../constants.js");
function getValidColor(color = "", defaultColor = import_constants.SCALE_FALLBACK_COLOR) {
  if (isValidCSSColor(color)) {
    return color;
  }
  return defaultColor;
}
const isValidCSSColor = (color) => {
  if (color === "initial" || color === "inherit") {
    return true;
  }
  const { style } = new Option();
  style.color = color.startsWith("var(") ? (
    /**
     * FYI: If the color starts with 'var(', extract the fallback color from the CSS variable definition
     * Example: 'var(--primary-color, #ff0000)' -> '#ff0000'
     */
    color.replace(/^\s*var\(--[0-9A-Za-z-]+,\s*([^)]+)\)\s*$/, "$1")
  ) : color;
  return style.color !== "";
};
