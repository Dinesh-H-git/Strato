import { DateString } from '../types/dates.js';
/**
 *
 * @param input - A DateString
 * @returns a Unix timestamp in miliseconds
 */
export declare const convertDateStringToTimestamp: (input: DateString) => number;
/**
 *
 * @param input - A Date
 * @returns a Unix timestamp in miliseconds
 */
export declare const convertDateToTimestamp: (input: Date) => number;
/**
 * Accepts any date format and transforms it into timestamp, making
 * it easier to handle in the charts with one single format.
 *
 * @param input - A DateString
 * @returns a Unix timestamp in miliseconds
 */
export declare const convertDateCompatibleToTimestamp: (input: number | DateString | Date) => number;
