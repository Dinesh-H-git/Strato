var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var convert_dates_exports = {};
__export(convert_dates_exports, {
  convertDateCompatibleToTimestamp: () => convertDateCompatibleToTimestamp,
  convertDateStringToTimestamp: () => convertDateStringToTimestamp,
  convertDateToTimestamp: () => convertDateToTimestamp
});
module.exports = __toCommonJS(convert_dates_exports);
var import_lodash_es = require("lodash");
var import_is_date = require("./is-date.js");
const convertDateStringToTimestamp = (input) => new Date(input).getTime();
const convertDateToTimestamp = (input) => input.getTime();
const convertDateCompatibleToTimestamp = (input) => {
  if ((0, import_is_date.isDateString)(input)) {
    return convertDateStringToTimestamp(input);
  }
  if ((0, import_lodash_es.isDate)(input)) {
    return convertDateToTimestamp(input);
  }
  return input;
};
