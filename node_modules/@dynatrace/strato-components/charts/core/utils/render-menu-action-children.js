var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var render_menu_action_children_exports = {};
__export(render_menu_action_children_exports, {
  renderMenuActionChildren: () => renderMenuActionChildren
});
module.exports = __toCommonJS(render_menu_action_children_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_react_is = require("react-is");
var import_is_slot = require("../../../core/utils/is-slot.js");
var import_Divider = require("../../../layouts/divider/Divider.js");
var import_ChartSeriesAction = require("../components/series-action/ChartSeriesAction.js");
const anyChildIsNotSeriesActionComponent = (children) => {
  return import_react.Children.toArray(children).some((child) => {
    return !Object.values(import_ChartSeriesAction.ChartSeriesAction).some(
      (ChartSeriesActionComponent) => (0, import_is_slot.isSlot)(child, ChartSeriesActionComponent)
    );
  });
};
const sortResults = (seriesActions, intents) => {
  if (seriesActions.length === 0) {
    return intents;
  }
  if (intents.length === 0) {
    return seriesActions;
  }
  return [...seriesActions, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_Divider.Divider, {}, "divider"), ...intents];
};
function renderMenuActionChildren(children, options) {
  if (!children) {
    return;
  }
  const { divideActionsAndIntents = true } = options || {};
  const seriesActions = [];
  const intents = [];
  if (anyChildIsNotSeriesActionComponent(children)) {
    return children;
  }
  import_react.Children.forEach(children, (child, index) => {
    if ((0, import_is_slot.isSlot)(child, import_ChartSeriesAction.ChartSeriesAction.Intent)) {
      intents.push(child);
    } else if ((0, import_react_is.isElement)(child)) {
      seriesActions.push(child);
    } else {
      seriesActions.push(/* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: child }, index));
    }
  });
  if (!divideActionsAndIntents) {
    return [...seriesActions, ...intents];
  }
  return intents.length > 0 || seriesActions.length > 0 ? sortResults(seriesActions, intents) : void 0;
}
