var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_categorical_color_scale_exports = {};
__export(build_categorical_color_scale_exports, {
  buildCategoricalColorScale: () => buildCategoricalColorScale
});
module.exports = __toCommonJS(build_categorical_color_scale_exports);
var import_d3_scale = require("d3-scale");
var import_getValidColor = require("./getValidColor.js");
var import_constants = require("../constants.js");
var import_color_palette = require("../types/color-palette.js");
var import_color_palettes_definitions = require("../types/color-palettes-definitions.js");
function assignColorsToCategories(colorScheme, categories) {
  const assignedColors = {};
  categories.forEach((category) => {
    assignedColors[category] = (0, import_getValidColor.getValidColor)(colorScheme[category]);
  });
  return assignedColors;
}
function buildCategoricalColorScale(colorPalette, filter = void 0) {
  if ((0, import_color_palette.isCustomColorPalette)(colorPalette)) {
    return buildScaleWithCustomPalette(colorPalette, filter);
  }
  return buildScaleWithPalette(colorPalette, filter);
}
function buildScaleWithPalette(colorPalette, filter) {
  const categories = filter ?? Object.keys(import_color_palettes_definitions.colorPaletteMap[colorPalette]);
  const colors = [];
  const colorsList = Object.values(import_color_palettes_definitions.colorPaletteMap[colorPalette]);
  categories.forEach((dataName, i) => {
    const index = i % colorsList.length;
    const colorToken = import_color_palettes_definitions.colorPaletteMap[colorPalette][dataName.toLowerCase()] ?? colorsList[index];
    if (colorToken !== void 0) {
      colors.push(colorToken.Default);
    }
  });
  return (0, import_d3_scale.scaleOrdinal)(categories, colors).unknown(import_constants.SCALE_FALLBACK_COLOR);
}
function buildScaleWithCustomPalette(colorPalette, filter) {
  if (Array.isArray(colorPalette)) {
    const categories2 = filter ?? Object.keys(colorPalette);
    return (0, import_d3_scale.scaleOrdinal)(categories2, colorPalette).unknown(import_constants.SCALE_FALLBACK_COLOR);
  }
  const categories = filter ?? Object.keys(colorPalette);
  const coloredCategories = assignColorsToCategories(colorPalette, categories);
  const labels = Object.keys(coloredCategories);
  const colors = Object.values(coloredCategories);
  return (0, import_d3_scale.scaleOrdinal)(labels, colors).unknown(import_constants.SCALE_FALLBACK_COLOR);
}
