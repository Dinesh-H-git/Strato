var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var format_date_ticks_exports = {};
__export(format_date_ticks_exports, {
  formatDateTicks: () => formatDateTicks
});
module.exports = __toCommonJS(format_date_ticks_exports);
var import_d3_time = require("d3-time");
var import_units = require("@dynatrace-sdk/units");
var import_user_preferences = require("@dynatrace-sdk/user-preferences");
var import_format_dates = require("./format-dates.js");
function formatDateTicks(date) {
  const timeInMs = date.getTime();
  if ((0, import_d3_time.timeSecond)(date) < date) {
    return (0, import_format_dates.formatSeconds)(timeInMs);
  } else if ((0, import_d3_time.timeMinute)(date) < date) {
    return (0, import_format_dates.formatMinute)(timeInMs);
  } else if ((0, import_d3_time.timeHour)(date) < date) {
    return (0, import_format_dates.formatHour)(timeInMs);
  } else if ((0, import_d3_time.timeDay)(date) < date) {
    const formattedDay = (0, import_format_dates.formatDay)(timeInMs);
    const locale = (0, import_user_preferences.getRegionalFormat)();
    if (formattedDay.match(/am|pm/i)) {
      return (0, import_units.formatDate)(timeInMs, {
        hour: "2-digit",
        locale
      });
    }
    return (0, import_units.formatDate)(timeInMs, {
      hour: "2-digit",
      minute: "numeric",
      hourCycle: "h24",
      locale
    });
  } else if ((0, import_d3_time.timeMonth)(date) < date) {
    return (0, import_format_dates.formatWeekday)(timeInMs);
  } else if ((0, import_d3_time.timeYear)(date) < date) {
    return (0, import_format_dates.formatMonth)(timeInMs);
  } else {
    return (0, import_format_dates.formatYear)(timeInMs);
  }
}
