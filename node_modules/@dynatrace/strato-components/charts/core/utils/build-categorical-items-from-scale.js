var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_categorical_items_from_scale_exports = {};
__export(build_categorical_items_from_scale_exports, {
  buildCategoricalItemsFromScale: () => buildCategoricalItemsFromScale
});
module.exports = __toCommonJS(build_categorical_items_from_scale_exports);
var import_lodash_es = require("lodash");
var import_getValidColor = require("./getValidColor.js");
const buildCategoricalItemsFromScale = (scale, overrides, id) => {
  const categories = scale.domain();
  const legendItems = [];
  categories.forEach((category) => {
    const color = overrides?.get(category) || scale(category);
    if (!(0, import_lodash_es.isUndefined)(color)) {
      legendItems.push({
        id: id ? id(category) : crypto.randomUUID(),
        color: { Default: (0, import_getValidColor.getValidColor)(color) },
        name: category
      });
    }
  });
  return legendItems;
};
