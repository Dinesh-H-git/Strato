var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var group_by_threshold_exports = {};
__export(group_by_threshold_exports, {
  getGroupedDataPointsByCoordinate: () => getGroupedDataPointsByCoordinate,
  groupByThreshold: () => groupByThreshold
});
module.exports = __toCommonJS(group_by_threshold_exports);
var import_constants = require("../components/data-point/constants.js");
const groupByThreshold = (list, threshold, accessor) => {
  const cloned = [...list];
  cloned.sort((a, b) => accessor(b) - accessor(a));
  const groups = [[]];
  let previousValue = null;
  for (const entry of cloned) {
    const value = accessor(entry);
    if (previousValue !== null && Math.abs(previousValue - value) > threshold) {
      groups.push([]);
    }
    const group = groups.at(-1);
    if (group !== void 0) {
      group.push(entry);
    }
    previousValue = value;
  }
  return groups.filter((group) => group.length > 0);
};
const getGroupedDataPointsByCoordinate = (xPoints, coordinate) => {
  const pointsGrouped = groupByThreshold(
    xPoints,
    import_constants.GROUP_DISTANCE_THRESHOLD,
    (closestPoint) => closestPoint[coordinate]
  );
  return pointsGrouped.filter((groupedPoint) => groupedPoint.length > 0);
};
