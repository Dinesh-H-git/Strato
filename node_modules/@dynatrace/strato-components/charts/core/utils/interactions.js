var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var interactions_exports = {};
__export(interactions_exports, {
  getConfigPlusDefaultOrEmpty: () => getConfigPlusDefaultOrEmpty,
  getInteractions: () => getInteractions
});
module.exports = __toCommonJS(interactions_exports);
var import_lodash_es = require("lodash");
const defaultInteractions = {
  pan: { enabled: true },
  zoom: { enabled: true },
  zoomX: { enabled: true }
};
const oldDefaultInteractions = {
  pan: { enabled: false },
  zoom: { enabled: false },
  zoomX: { enabled: false }
};
const getConfigPlusDefaultOrEmpty = (defaultConfig, config) => {
  const isEmptyConfig = (0, import_lodash_es.isEmpty)(config) || (0, import_lodash_es.isUndefined)(config);
  return isEmptyConfig ? {} : (0, import_lodash_es.merge)({}, defaultConfig, config);
};
const getSlotsApiValuesOrFallbackIfHasOldConfig = (slotsChartInteractions, chartConfigInteractions, oldApiValues) => {
  const hasEmptySlotsAndChartConfig = (0, import_lodash_es.isEmpty)(slotsChartInteractions) && (0, import_lodash_es.isEmpty)(chartConfigInteractions);
  const hasOldApiValues = !(0, import_lodash_es.isEmpty)(oldApiValues) && !(0, import_lodash_es.isUndefined)(oldApiValues);
  if (hasEmptySlotsAndChartConfig && hasOldApiValues) {
    return oldApiValues;
  }
  return {
    ...defaultInteractions,
    ...chartConfigInteractions,
    ...slotsChartInteractions
  };
};
function getInteractions(contextInteractions, slotsChartInteractions, propsInteractions = {}, chartConfigInteractions) {
  const oldApiValues = getConfigPlusDefaultOrEmpty(
    oldDefaultInteractions,
    contextInteractions
  );
  const slotsApiValues = getSlotsApiValuesOrFallbackIfHasOldConfig(
    slotsChartInteractions,
    chartConfigInteractions,
    oldApiValues
  );
  return (0, import_lodash_es.merge)(
    {},
    oldApiValues,
    slotsApiValues,
    propsInteractions
  );
}
