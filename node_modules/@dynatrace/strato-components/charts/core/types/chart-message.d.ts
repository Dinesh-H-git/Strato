/**
 * Messages emitted by the chart
 * @public
 */
export interface ChartMessage {
    /** Severity level of the emitted message */
    severityLevel: 'warning' | 'error';
    /** Human-readable message */
    message: string;
}
