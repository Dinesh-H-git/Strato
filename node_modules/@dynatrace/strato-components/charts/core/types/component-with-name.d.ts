/**
 * This type is responsible for adding the
 * displayName property to the root component
 * when required
 */
export type ComponentWithName<ComponentType> = ComponentType & {
    displayName: string;
};
