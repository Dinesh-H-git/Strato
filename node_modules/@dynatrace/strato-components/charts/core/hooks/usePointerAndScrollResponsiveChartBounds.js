var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var usePointerAndScrollResponsiveChartBounds_exports = {};
__export(usePointerAndScrollResponsiveChartBounds_exports, {
  usePointerAndScrollResponsiveChartBounds: () => usePointerAndScrollResponsiveChartBounds
});
module.exports = __toCommonJS(usePointerAndScrollResponsiveChartBounds_exports);
var import_react = require("react");
var import_useThrottledCallbackOnScroll = require("./useThrottledCallbackOnScroll.js");
var import_throttleRaf = require("../utils/throttleRaf.js");
const usePointerAndScrollResponsiveChartBounds = (chartRef, enabled) => {
  const getBoundingClientRect = () => () => chartRef?.current?.getBoundingClientRect();
  const [getChartBounds, setGetChartBounds] = (0, import_react.useState)(getBoundingClientRect);
  (0, import_useThrottledCallbackOnScroll.useThrottledCallbackOnScroll)(!enabled, () => {
    setGetChartBounds(getBoundingClientRect);
  });
  const throttledPointerUpdate = (0, import_react.useMemo)(
    () => (0, import_throttleRaf.throttleRaf)(() => {
      setGetChartBounds(getBoundingClientRect);
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    []
  );
  const throttledMoveUpdate = (0, import_react.useMemo)(
    () => (0, import_throttleRaf.throttleRaf)((event) => {
      if (!event.pressure || chartRef?.current?.contains(event.target)) {
        return;
      }
      setGetChartBounds(getBoundingClientRect);
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    []
  );
  (0, import_react.useEffect)(() => {
    window.addEventListener("pointermove", throttledMoveUpdate);
    window.addEventListener("pointerup", throttledPointerUpdate);
    return () => {
      window.removeEventListener("pointermove", throttledMoveUpdate);
      window.removeEventListener("pointerup", throttledPointerUpdate);
    };
  }, [throttledMoveUpdate, throttledPointerUpdate]);
  return getChartBounds;
};
