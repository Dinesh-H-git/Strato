var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useCustomSeriesActions_exports = {};
__export(useCustomSeriesActions_exports, {
  useCustomSeriesActions: () => useCustomSeriesActions
});
module.exports = __toCommonJS(useCustomSeriesActions_exports);
var import_react = require("react");
var import_sdk_logger = require("../../../core/sdk/sdk-logger.js");
const useCustomSeriesActions = (actions, isOpen) => {
  const [customActions, setCustomActions] = (0, import_react.useState)(null);
  const logger = (0, import_sdk_logger.getStratoLogger)("useCustomSeriesActions");
  (0, import_react.useEffect)(() => {
    if (actions && isOpen) {
      if (actions instanceof Promise) {
        actions.then((resolvedActions) => {
          setCustomActions(resolvedActions);
        }).catch((error) => {
          logger.error("Failed to load series actions", error);
        });
      } else {
        setCustomActions(actions);
      }
    } else {
      setCustomActions(null);
    }
  }, [actions, isOpen]);
  return customActions;
};
