import type { ReactNode } from 'react';
/**
 * @public
 */
export type TooltipProps = {
    hidden?: boolean;
    children?: (payload: Record<string, unknown>) => ReactNode;
};
export declare const Tooltip: <T extends TooltipProps>(prefix: string) => {
    (props: T): null;
    displayName: string;
};
