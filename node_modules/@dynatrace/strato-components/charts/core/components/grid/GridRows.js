var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var GridRows_exports = {};
__export(GridRows_exports, {
  GridRows: () => GridRows
});
module.exports = __toCommonJS(GridRows_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_grid = require("@visx/grid");
var import_GridLines = require("./GridLines.js");
var import_should_highlight_zero_line = require("./utils/should-highlight-zero-line.js");
var import_detect_scale = require("../../utils/detect-scale.js");
const GridRows = ({
  scale,
  highlightZeroLine = true,
  ticks,
  ...restProps
}) => {
  if (ticks) {
    const typedScale = scale;
    const lines = ticks.map((tick, index) => ({
      from: { x: 0, y: Math.max(0, typedScale(tick) ?? 0) },
      to: { x: restProps.width, y: Math.max(0, typedScale(tick) ?? 0) },
      index
    }));
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      import_GridLines.GridLines,
      {
        lines,
        highlightAccessor: (from, to) => highlightZeroLine && !(0, import_detect_scale.isBandScale)(scale) && (0, import_should_highlight_zero_line.shouldHighlightZeroLine)(from, to, scale)
      }
    );
  }
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_grid.GridRows, { scale, ...restProps, children: ({ lines }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_GridLines.GridLines,
    {
      lines,
      highlightAccessor: (from, to) => highlightZeroLine && !(0, import_detect_scale.isBandScale)(scale) && (0, import_should_highlight_zero_line.shouldHighlightZeroLine)(from, to, scale)
    }
  ) });
};
