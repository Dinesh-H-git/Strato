var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var symbol_canvas_exports = {};
__export(symbol_canvas_exports, {
  drawSymbol: () => drawSymbol
});
module.exports = __toCommonJS(symbol_canvas_exports);
const TRIANGLE_RADIUS_ADJUSTMENT = 0.5;
function drawSymbol(ctx, cx, cy, radius, shape = "circle") {
  switch (shape) {
    case "circle":
      ctx.arc(cx, cy, radius, 0, 2 * Math.PI);
      break;
    case "square": {
      const s = radius;
      ctx.rect(cx - s, cy - s, 2 * s, 2 * s);
      break;
    }
    case "triangle": {
      const r = radius - TRIANGLE_RADIUS_ADJUSTMENT;
      const R = 2 * r;
      const side = r * 2 * Math.sqrt(3);
      ctx.moveTo(cx, cy - R);
      ctx.lineTo(cx + side / 2, cy + r);
      ctx.lineTo(cx - side / 2, cy + r);
      ctx.closePath();
      break;
    }
    case "diamond": {
      const s = radius * Math.sqrt(2);
      ctx.moveTo(cx, cy - s);
      ctx.lineTo(cx + s, cy);
      ctx.lineTo(cx, cy + s);
      ctx.lineTo(cx - s, cy);
      ctx.closePath();
      break;
    }
    default:
      ctx.arc(cx, cy, radius, 0, 2 * Math.PI);
  }
}
