var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var canvas_color_exports = {};
__export(canvas_color_exports, {
  getCanvasColor: () => getCanvasColor
});
module.exports = __toCommonJS(canvas_color_exports);
var import_lodash_es = require("lodash");
var import_constants = require("../../../constants.js");
var import_getTokenComputedStyle = require("../../../utils/getTokenComputedStyle.js");
var import_getValidColor = require("../../../utils/getValidColor.js");
const getCanvasColor = (0, import_lodash_es.memoize)(
  (color, fallbackColor = import_constants.SCALE_FALLBACK_COLOR) => {
    let transformedColor = color ?? fallbackColor;
    if (transformedColor.indexOf("var") >= 0) {
      transformedColor = (0, import_getTokenComputedStyle.getTokenComputedStyle)(transformedColor);
      return transformedColor ? transformedColor : (0, import_getTokenComputedStyle.getTokenComputedStyle)(fallbackColor);
    }
    if (!(0, import_getValidColor.isValidCSSColor)(transformedColor)) {
      return (0, import_getTokenComputedStyle.getTokenComputedStyle)(fallbackColor);
    }
    return transformedColor;
  }
);
