import { AreaCanvasInstance, Orientation, FnCanvasAccessor } from '../../types/canvas-shape.js';
type FnAreaCanvasStyle<Datum> = (ctx: CanvasRenderingContext2D, areaCanvas: AreaCanvasInstance<Datum>, x: FnCanvasAccessor<Datum>, y0: FnCanvasAccessor<Datum>, y1: FnCanvasAccessor<Datum>, orientation?: Orientation) => void;
/**
 * Builds style FN Area based on parameters.
 *
 * @param strokeWidth - stroke width
 * @param radius - scatter plot radius
 * @param lineStrokeWidth - upper line stroke width
 * @param enableDotOverflow - enable dots indicators to be drawn overflowing the plot area
 * @param overlayPadding - padding for overlay
 * @returns - fn print Area canvas
 */
export declare function printAreaBuilder<Datum>(strokeWidth: number, radius: number | undefined, lineStrokeWidth: number, enableDotOverflow: boolean, overlayPadding?: number): FnAreaCanvasStyle<Datum>;
export {};
