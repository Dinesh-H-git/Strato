import type { PointShapeType } from '../../../shape/type.js';
import { type DotCanvasInstance, FnCanvasAccessor } from '../../types/canvas-shape.js';
type FnDotCanvasStyle<Datum> = (ctx: CanvasRenderingContext2D, dotCanvas: DotCanvasInstance<Datum>, x: FnCanvasAccessor<Datum>, y: FnCanvasAccessor<Datum>) => void;
/**
 * Builds style FN Dot based on parameters.
 *
 * @param radius - scatter plot radius
 * @param enableDotOverflow - enable dots indicators to be drawn overflowing the plot area
 * @param shape - The shape to draw. One of 'circle', 'square', 'triangle', or 'diamond'.
 * @returns - fn print Line canvas
 */
export declare function printDotBuilder<Datum>(radius: number | undefined, enableDotOverflow: boolean, shape?: PointShapeType): FnDotCanvasStyle<Datum>;
export {};
