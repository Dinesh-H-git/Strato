var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var BarCanvas_exports = {};
__export(BarCanvas_exports, {
  BarCanvas: () => BarCanvas
});
module.exports = __toCommonJS(BarCanvas_exports);
var import_useCanvas = require("../hooks/useCanvas.js");
var import_bar_canvas = require("../utils/shapes/bar-canvas.js");
function BarCanvas(props) {
  const {
    strokeWidth,
    isVertical = true,
    enableDotOverflow = false,
    overlayPadding,
    ...propsBarCanvas
  } = props;
  const drawBars = (0, import_bar_canvas.printBarBuilder)(
    strokeWidth,
    enableDotOverflow,
    overlayPadding
  );
  const drawBarCanvas = (ctx, canvas) => {
    drawBars(ctx, canvas, canvas.x, canvas.y, canvas.width, canvas.height);
  };
  const barCanvas = {
    ...propsBarCanvas,
    isVertical
  };
  (0, import_useCanvas.useCanvas)(
    "bar",
    (ctx) => drawBarCanvas(ctx, barCanvas)
  );
  return null;
}
