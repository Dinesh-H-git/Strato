var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var RoundRectCanvas_exports = {};
__export(RoundRectCanvas_exports, {
  RoundRectCanvas: () => RoundRectCanvas
});
module.exports = __toCommonJS(RoundRectCanvas_exports);
var import_react = require("react");
var import_useCanvas = require("../hooks/useCanvas.js");
var import_canvas_color = require("../utils/canvas-color.js");
const RoundRectCanvas = (props) => {
  const {
    x,
    y,
    width,
    height,
    color,
    borderRadius = 0,
    borderColor = color,
    borderWidth = 0,
    opacity = 1
  } = props;
  const draw = (0, import_react.useCallback)(
    (ctx) => {
      ctx.beginPath();
      ctx.save();
      ctx.moveTo(x + borderRadius, y);
      ctx.lineTo(x + width - borderRadius, y);
      ctx.quadraticCurveTo(x + width, y, x + width, y + borderRadius);
      ctx.lineTo(x + width, y + height - borderRadius);
      ctx.quadraticCurveTo(
        x + width,
        y + height,
        x + width - borderRadius,
        y + height
      );
      ctx.lineTo(x + borderRadius, y + height);
      ctx.quadraticCurveTo(x, y + height, x, y + height - borderRadius);
      ctx.lineTo(x, y + borderRadius);
      ctx.quadraticCurveTo(x, y, x + borderRadius, y);
      ctx.globalAlpha = opacity;
      ctx.lineWidth = borderWidth;
      ctx.fillStyle = (0, import_canvas_color.getCanvasColor)(color);
      ctx.fill();
      ctx.restore();
      ctx.strokeStyle = (0, import_canvas_color.getCanvasColor)(borderColor);
      ctx.stroke();
      ctx.restore();
      ctx.closePath();
      return false;
    },
    [x, y, width, height, color, borderRadius, borderColor, borderWidth]
  );
  (0, import_useCanvas.useCanvas)("round-rect", draw);
  return null;
};
