var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var LineCanvas_exports = {};
__export(LineCanvas_exports, {
  LineCanvas: () => LineCanvas
});
module.exports = __toCommonJS(LineCanvas_exports);
var import_useCanvas = require("../hooks/useCanvas.js");
var import_line_canvas = require("../utils/shapes/line-canvas.js");
function LineCanvas(props) {
  const {
    radius,
    strokeWidth = 2,
    enableDotOverflow = false,
    overlayPadding,
    ...propsLineCanvas
  } = props;
  const drawLine = (0, import_line_canvas.printLineBuilder)(
    strokeWidth,
    enableDotOverflow,
    radius,
    overlayPadding
  );
  const drawLineCanvas = (ctx, canvas) => {
    drawLine(ctx, canvas, canvas.x, canvas.y);
  };
  (0, import_useCanvas.useCanvas)(
    "line",
    (ctx) => drawLineCanvas(ctx, propsLineCanvas)
  );
  return null;
}
