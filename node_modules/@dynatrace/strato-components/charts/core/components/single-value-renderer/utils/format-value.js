var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var format_value_exports = {};
__export(format_value_exports, {
  defaultFormatter: () => defaultFormatter,
  extractValueAndUnit: () => extractValueAndUnit,
  formatValue: () => formatValue
});
module.exports = __toCommonJS(format_value_exports);
var import_lodash_es = require("lodash");
var import_units = require("@dynatrace-sdk/units");
var import_normalize = require("./normalize.js");
var import_formatter = require("../../../types/formatter.js");
const defaultFormatter = (value, unit) => {
  const options = {
    maximumFractionDigits: 1
  };
  if (unit && typeof unit === "string") {
    return [(0, import_units.getFormatting)(value, options)[0].value, unit];
  }
  const parts = (0, import_units.getFormatting)(
    value,
    unit ? { ...options, input: unit } : options
  );
  const formattedUnit = parts[0].symbol.length > 0 ? parts[0].symbol : void 0;
  return [parts[0].value, formattedUnit];
};
function formatValue(value, unit, formatter) {
  if ((0, import_lodash_es.isNil)(value)) {
    return {
      value: ""
    };
  }
  if (typeof value === "number") {
    if (typeof unit === "string") {
      const [formattedValue2, formattedUnit2] = defaultFormatter(value, unit);
      return {
        value: formattedValue2,
        unit: formattedUnit2
      };
    }
    if ((0, import_formatter.isFormatterOptions)(formatter)) {
      const formattingParts = (0, import_units.getFormatting)(value, formatter);
      return {
        value: formattingParts[0]?.value,
        unit: formattingParts[0]?.symbol
      };
    }
    if (formatter) {
      return extractValueAndUnit(formatter(value));
    }
    const [formattedValue, formattedUnit] = defaultFormatter(value, unit);
    return {
      value: formattedValue,
      unit: formattedUnit
    };
  }
  return {
    value: (0, import_normalize.normalize)(`${value}`)
  };
}
function extractValueAndUnit(formattedValue) {
  const parts = formattedValue.split(" ");
  let value;
  let unit;
  if (parts[0] === "<") {
    value = `${parts[0]} ${parts[1]}`;
    unit = parts[2];
  } else {
    value = parts[0];
    unit = parts[1];
  }
  return {
    value,
    unit
  };
}
