var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var apply_color_rule_to_single_value_exports = {};
__export(apply_color_rule_to_single_value_exports, {
  applyColorRuleToSingleValue: () => applyColorRuleToSingleValue
});
module.exports = __toCommonJS(apply_color_rule_to_single_value_exports);
var import_lodash_es = require("lodash");
var import_get_color_palette = require("../../../utils/get-color-palette.js");
var import_getAccessor = require("../../../utils/getAccessor.js");
var import_getValidColor = require("../../../utils/getValidColor.js");
var import_validators = require("../../../utils/validators.js");
const getSubject = (rule, rawData) => {
  const valueAccessor = rule.valueAccessor;
  if (!valueAccessor || !rawData) {
    return null;
  }
  const accessor = (0, import_getAccessor.getAccessor)(valueAccessor);
  if (accessor) {
    const value = (0, import_lodash_es.get)(rawData, accessor);
    if (!(0, import_lodash_es.isNil)(value)) {
      return value;
    }
  }
  return null;
};
const applyColorRuleToSingleValue = (colorRules, value, dataItem) => {
  const isValid = (0, import_validators.buildValidator)(import_validators.VALUE_MATCHERS);
  for (let i = colorRules.length - 1; i >= 0; i--) {
    const rule = colorRules[i];
    const subject = getSubject(rule, dataItem) ?? value;
    if (!(0, import_lodash_es.isNil)(subject) && isValid(subject, rule.comparator, rule.matchValue)) {
      return rule.colorPalette ? (0, import_get_color_palette.getColorPalette)(`${subject}`, 0, rule.colorPalette).Default : (0, import_getValidColor.getValidColor)(rule.color ?? "");
    }
  }
};
