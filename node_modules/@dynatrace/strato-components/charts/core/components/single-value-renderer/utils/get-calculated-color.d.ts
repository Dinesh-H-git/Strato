import { type ColorRuleProps } from '../../../types/color-rule.js';
export declare const getCalculatedColor: (data: number | string, colorRules?: ColorRuleProps[], explicitColor?: string, dataItem?: Record<string, unknown>) => {
    color: string;
    ruleApplied: boolean;
};
