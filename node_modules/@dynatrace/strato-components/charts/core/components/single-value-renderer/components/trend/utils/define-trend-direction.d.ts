import type { TrendDirection } from '../types/trend-direction.js';
/**
 * Defines automatic behavior of trend direction if it is not configured
 *
 * @param directionProp -
 * @param valueProp -
 */
export declare function defineTrendDirection(directionProp?: TrendDirection, valueProp?: number): TrendDirection;
