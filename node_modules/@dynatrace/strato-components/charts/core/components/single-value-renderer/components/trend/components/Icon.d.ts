import type { TrendDirection } from '../types/trend-direction.js';
interface TrendIconProps {
    direction: TrendDirection;
    color: string;
}
export declare const Icon: ({ direction, color }: TrendIconProps) => import("react/jsx-runtime").JSX.Element;
export {};
