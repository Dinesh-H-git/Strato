var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var define_trend_colors_exports = {};
__export(define_trend_colors_exports, {
  defineTrendColors: () => defineTrendColors
});
module.exports = __toCommonJS(define_trend_colors_exports);
var import_lodash_es = require("lodash");
var import_constants = require("../../../constants.js");
const UPWARD_COLORS = [
  import_constants.UPWARD_ICON_DEFAULT_COLOR,
  import_constants.UPWARD_VALUE_DEFAULT_COLOR
];
const NEUTRAL_COLORS = [
  import_constants.NEUTRAL_ICON_DEFAULT_COLOR,
  import_constants.NEUTRAL_VALUE_DEFAULT_COLOR
];
const DOWNWARD_COLORS = [
  import_constants.DOWNWARD_ICON_DEFAULT_COLOR,
  import_constants.DOWNWARD_VALUE_DEFAULT_COLOR
];
const DEFAULT_TREND = {
  downward: DOWNWARD_COLORS,
  neutral: NEUTRAL_COLORS,
  upward: UPWARD_COLORS
};
const INVERSE_TREND = {
  downward: UPWARD_COLORS,
  neutral: NEUTRAL_COLORS,
  upward: DOWNWARD_COLORS
};
const defineTrendColors = (direction, inverseTrend, colorsOverride) => {
  const trendColors = inverseTrend === "color" ? { ...INVERSE_TREND } : { ...DEFAULT_TREND };
  if (!(0, import_lodash_es.isUndefined)(colorsOverride)) {
    Object.keys(colorsOverride).forEach((key) => {
      const trendDirection = key;
      if (!(0, import_lodash_es.isUndefined)(colorsOverride[trendDirection])) {
        trendColors[trendDirection] = [
          // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
          colorsOverride[trendDirection],
          // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
          colorsOverride[trendDirection]
        ];
      }
    });
  }
  return trendColors[direction];
};
