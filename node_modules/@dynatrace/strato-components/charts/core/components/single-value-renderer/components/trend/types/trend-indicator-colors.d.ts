/**
 * The object to override the trend colors
 * @public
 */
export interface TrendIndicatorColors {
    /** Color for when the trend goes up */
    upward?: string;
    /** Color for when the trend goes down */
    downward?: string;
    /** Color for when the trend stays neutral */
    neutral?: string;
}
