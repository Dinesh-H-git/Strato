import { type ReactElement, type ReactNode } from 'react';
import type { ConvertibleUnit, FormatOptions, Unit } from '@dynatrace-sdk/units';
import type { BehaviorTrackingProps } from '../../../../../core/types/behavior-tracking-props.js';
import type { DataTestId } from '../../../../../core/types/data-props.js';
import type { MaskingProps } from '../../../../../core/types/masking-props.js';
import type { StylingProps } from '../../../../../core/types/styling-props.js';
import type { CompactToolbarProps } from '../../../slots/toolbar/CompactToolbar.js';
import type { Formatter } from '../../../types/formatter.js';
/**
 * SingleValue unit
 *
 * @deprecated Use string | Unit directly instead of SingleValueUnit.
 *
 * @example
 * ```
 * // Before
 * unit: SingleValueUnit = 'ms'
 * // After
 * unit: string | Unit = 'ms'
 * ```
 * @public
 */
export type SingleValueUnit = Unit | (string & {});
/**
 * SingleValue Alignment
 * @public
 */
export type SingleValueAlignment = 'start' | 'center' | 'end';
/**
 * Base props for SingleValue instances
 * @public
 */
export interface SingleValueBaseProps extends MaskingProps, StylingProps, DataTestId, BehaviorTrackingProps {
    /** Optional label text to show above the value. */
    label?: string;
    /** Accessible name for the component, announced by screen readers instead of the default. */
    'aria-label'?: string;
    /** ID of an element that provides an additional accessible description. */
    'aria-describedby'?: string;
    /** Optional icon to show left of the value. */
    prefixIcon?: ReactNode;
    /** State of the value. This affects text and prefix icon color.
     * @defaultValue 'default'
     */
    color?: string;
    /** Optional unit to show in the right of the value */
    unit?: SingleValueUnit;
    /** Data to show within the chart. */
    data: string | number;
    /** If specified, value will be formatted with given formatter options. */
    formatter?: Formatter | FormatOptions<Unit, ConvertibleUnit>;
    /** Horizontal alignment of icon, value and unit. */
    alignment?: SingleValueAlignment;
    /** Threshold color will be preferred over color */
    applyThresholdBackground?: boolean;
}
/**
 * @internal
 */
export type PropsFromGrid = {
    toolbar: Required<CompactToolbarProps> & {
        draggable: false;
    };
    intentsAsyncTemplate?: () => Promise<ReactElement>;
};
