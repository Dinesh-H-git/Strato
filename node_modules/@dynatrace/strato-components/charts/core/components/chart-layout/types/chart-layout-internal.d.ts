import type { MutableRefObject } from 'react';
import type { Dimensions } from '../../../types/dimensions.js';
export interface UseResizableLayoutResponse {
    legendResizeLineRef: MutableRefObject<HTMLDivElement | null>;
    legendContainerRef: MutableRefObject<HTMLDivElement | null>;
    legendResizeContainerRef: MutableRefObject<HTMLDivElement | null>;
    legendWidth: number;
    legendHeight: number;
    graphSize: Dimensions;
}
export interface AreasSizes {
    legend: Dimensions;
    graph: Dimensions;
}
