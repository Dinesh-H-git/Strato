var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_areas_size_exports = {};
__export(get_areas_size_exports, {
  getAreasSizeHandlers: () => getAreasSizeHandlers
});
module.exports = __toCommonJS(get_areas_size_exports);
var import_calculate_graph_size = require("./calculate-graph-size.js");
var import_constants = require("../types/constants.js");
const getAreasWidthSizeHandler = (containerHeight, containerWidth, maxWidth, resizerWrapperSize, legendInitialWidth) => {
  const maxAllowedLegendWidth = containerWidth * maxWidth;
  const legendWidth = legendInitialWidth > maxAllowedLegendWidth ? maxAllowedLegendWidth : Math.max(legendInitialWidth, import_constants.MIN_LEGEND_WIDTH);
  const legend = {
    height: containerHeight,
    width: legendWidth
  };
  const graph = {
    width: (0, import_calculate_graph_size.calculateGraphSize)(containerWidth, legendWidth, resizerWrapperSize),
    height: containerHeight
  };
  return { legend, graph };
};
const getAreasHeightSizeHandler = (containerHeight, containerWidth, maxHeight, resizerWrapperSize, legendInitialHeight) => {
  const maxAllowedLegendHeight = containerHeight * maxHeight;
  const legendHeight = legendInitialHeight > maxAllowedLegendHeight ? maxAllowedLegendHeight : legendInitialHeight;
  const legend = {
    height: legendHeight,
    width: containerWidth
  };
  const graph = {
    width: containerWidth,
    height: (0, import_calculate_graph_size.calculateGraphSize)(
      containerHeight,
      legendHeight,
      resizerWrapperSize
    )
  };
  return {
    legend,
    graph
  };
};
const getAreasSizeHandlers = (containerHeight, containerWidth, resizerWrapperSize) => ({
  getAreasHeightSize: (maxHeight, legendInitialHeight) => getAreasHeightSizeHandler(
    containerHeight,
    containerWidth,
    maxHeight,
    resizerWrapperSize,
    legendInitialHeight
  ),
  getAreasWidthSize: (maxWidth, legendInitialWidth) => getAreasWidthSizeHandler(
    containerHeight,
    containerWidth,
    maxWidth,
    resizerWrapperSize,
    legendInitialWidth
  )
});
