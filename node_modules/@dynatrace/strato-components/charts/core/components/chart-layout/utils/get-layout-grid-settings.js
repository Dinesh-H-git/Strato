var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_layout_grid_settings_exports = {};
__export(get_layout_grid_settings_exports, {
  getGridTemplate: () => getGridTemplate,
  getLayoutGridSettings: () => getLayoutGridSettings,
  getTemplateArea: () => getTemplateArea
});
module.exports = __toCommonJS(get_layout_grid_settings_exports);
var import_coerce_width_value = require("./coerce-width-value.js");
var import_coerceSizeValue = require("../../../utils/coerceSizeValue.js");
var import_constants = require("../types/constants.js");
const getTemplateArea = (hasLegend, hasResizer) => {
  if (!hasLegend) {
    return import_constants.TEMPLATE_AREA_FOR_GRAPH;
  }
  if (hasLegend && hasResizer) {
    return import_constants.TEMPLATE_AREA_FOR_RESIZABLE_CHART_LAYOUT;
  }
  return import_constants.TEMPLATE_AREA_FOR_FIXED_CHART_LAYOUT;
};
const getGridTemplate = (hasLegend, hasResizer) => {
  let gridTemplate = "1fr";
  if (hasLegend) {
    gridTemplate += " min-content";
    if (hasResizer) {
      gridTemplate += " min-content";
    }
  }
  return gridTemplate;
};
const getLayoutGridSettings = (chartWidth, chartHeight, containerSizes, hasResizableLegend, showResizer) => {
  const templateAreas = getTemplateArea(hasResizableLegend, showResizer);
  const gridTemplate = getGridTemplate(hasResizableLegend, showResizer);
  const horizontalPadding = containerSizes.padding.left + containerSizes.padding.right;
  const layoutWidth = (0, import_coerce_width_value.coerceWidthValue)(chartWidth, horizontalPadding) ?? import_constants.DEFAULT_LAYOUT_WIDTH;
  const layoutHeight = (0, import_coerceSizeValue.coerceSizeValue)(chartHeight) ?? import_constants.DEFAULT_LAYOUT_HEIGHT;
  return {
    layoutWidth,
    layoutHeight,
    templateAreas,
    gridTemplate
  };
};
