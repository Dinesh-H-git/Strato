export declare const getAreasSizeHandlers: (containerHeight: number, containerWidth: number, resizerWrapperSize: number) => {
    getAreasHeightSize: (maxHeight: number, legendInitialHeight: number) => {
        legend: {
            height: number;
            width: number;
        };
        graph: {
            width: number;
            height: number;
        };
    };
    getAreasWidthSize: (maxWidth: number, legendInitialWidth: number) => {
        legend: {
            height: number;
            width: number;
        };
        graph: {
            width: number;
            height: number;
        };
    };
};
