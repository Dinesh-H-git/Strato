var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var orientation_exports = {};
__export(orientation_exports, {
  getLegendOrientation: () => getLegendOrientation,
  getOrientation: () => getOrientation,
  isVertical: () => isVertical
});
module.exports = __toCommonJS(orientation_exports);
var import_constants = require("../../../constants.js");
const LEGEND_MIN_WIDTH = 225;
const calculateAutoOrientation = (width, height) => {
  if (width > height + LEGEND_MIN_WIDTH) {
    return import_constants.ORIENTATION.Horizontal;
  }
  return import_constants.ORIENTATION.Vertical;
};
const getOrientation = (width, height, position) => {
  if (position === import_constants.LEGEND_POSITION.Bottom) {
    return import_constants.ORIENTATION.Vertical;
  }
  if (position === import_constants.LEGEND_POSITION.Right) {
    return import_constants.ORIENTATION.Horizontal;
  }
  return calculateAutoOrientation(width, height);
};
const isVertical = (orientation) => orientation === import_constants.ORIENTATION.Vertical;
const getLegendOrientation = (layoutOrientation) => isVertical(layoutOrientation) ? import_constants.ORIENTATION.Horizontal : import_constants.ORIENTATION.Vertical;
