var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var automatic_ratio_exports = {};
__export(automatic_ratio_exports, {
  getLabelsHeightForRangeBottom: () => getLabelsHeightForRangeBottom,
  getMaxItemSizeForCategoricalHorizontal: () => getMaxItemSizeForCategoricalHorizontal,
  getMaxLabelForRangeHorizontal: () => getMaxLabelForRangeHorizontal,
  getSizeForCategoricaVertical: () => getSizeForCategoricaVertical
});
module.exports = __toCommonJS(automatic_ratio_exports);
var import_constants = require("../../legend/categorical/constants.js");
var import_constants2 = require("../../legend/range/constants.js");
const getMaxItemSizeForCategoricalHorizontal = (node, height) => {
  const textWidths = [];
  const nrOfVisibleItems = Math.round(height / import_constants.ITEM_HEIGHT.value);
  const nodes = node.getElementsByClassName(import_constants.LABEL_ITEM_CLASSNAME);
  for (const node2 of Array.from(nodes).slice(0, nrOfVisibleItems)) {
    textWidths.push(
      node2.getBoundingClientRect().width + import_constants.LEGEND_BUTTON_WIDTH + import_constants.LEGEND_ICON_WIDTH
    );
  }
  return Math.max(...textWidths);
};
const getSizeForCategoricaVertical = () => {
  const oneAndHalfRow = import_constants.ITEM_HEIGHT.value + import_constants.ITEM_HEIGHT.value / 2;
  const resizerPadding = 10;
  return oneAndHalfRow + resizerPadding;
};
const getMaxLabelForRangeHorizontal = (node) => {
  const textWidths = [];
  const nodes = node.getElementsByTagName("g");
  for (const node2 of Array.from(nodes)) {
    textWidths.push(node2.getBoundingClientRect().width);
  }
  return Math.max(...textWidths) + import_constants2.CONTAINER_PADDING * 2;
};
const getLabelsHeightForRangeBottom = (node) => {
  const nodes = node.getElementsByTagName("g");
  const firstLabel = Array.from(nodes)?.[0];
  return firstLabel?.getBoundingClientRect().height + import_constants2.DEFAULT_ITEM_THICKNESS;
};
