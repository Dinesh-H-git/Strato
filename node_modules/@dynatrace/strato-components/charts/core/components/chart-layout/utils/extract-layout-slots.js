var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var extract_layout_slots_exports = {};
__export(extract_layout_slots_exports, {
  extractLayoutSlots: () => extractLayoutSlots
});
module.exports = __toCommonJS(extract_layout_slots_exports);
var import_react = require("react");
var import_is_slot = require("../../../../../core/utils/is-slot.js");
var import_Graph = require("../slots/Graph.js");
var import_Legend = require("../slots/Legend.js");
var import_Provider = require("../slots/Provider.js");
function extractLayoutSlots(children) {
  let areas = {
    provider: import_react.Fragment
  };
  import_react.Children.forEach(children, (child) => {
    if ((0, import_is_slot.isSlot)(child, import_Graph.Graph)) {
      areas.graphArea = child.props.children;
    } else if ((0, import_is_slot.isSlot)(child, import_Legend.Legend)) {
      const { ratio, initialRatio, ...rest } = child.props;
      areas.legendProps = {
        ...rest,
        ratio: initialRatio ?? ratio,
        initialRatio
      };
      areas.legendArea = child.props.children;
    } else if ((0, import_is_slot.isSlot)(child, import_Provider.Provider)) {
      areas = {
        ...extractLayoutSlots(child.props.children),
        provider: child.props.provider
      };
    }
  });
  return areas;
}
