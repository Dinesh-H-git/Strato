var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_legend_size_based_on_ratio_exports = {};
__export(calculate_legend_size_based_on_ratio_exports, {
  calculateLegendSizeBasedOnRatio: () => calculateLegendSizeBasedOnRatio
});
module.exports = __toCommonJS(calculate_legend_size_based_on_ratio_exports);
var import_orientation = require("./orientation.js");
var import_sanitize_legend_ratio = require("../../../utils/sanitize-legend-ratio.js");
const calculateLegendSizeBasedOnRatio = (orientation, containerWidth, containerHeight, ratio) => {
  const sanitizedRatio = (0, import_sanitize_legend_ratio.sanitizeLegendRatio)(ratio);
  const fraction = sanitizedRatio * 0.01;
  const width = (0, import_orientation.isVertical)(orientation) ? containerWidth : fraction * containerWidth;
  const height = !(0, import_orientation.isVertical)(orientation) ? containerHeight : fraction * containerHeight;
  return { width, height };
};
