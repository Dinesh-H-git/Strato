var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var sanitize_legend_ratio_exports = {};
__export(sanitize_legend_ratio_exports, {
  sanitizeLegendRatio: () => sanitizeLegendRatio
});
module.exports = __toCommonJS(sanitize_legend_ratio_exports);
var import_lodash_es = require("lodash");
var import_constants = require("../../../constants.js");
const isInTheRange = (value, min, max) => value >= min && value <= max;
const sanitizeLegendRatio = (legendRatio, ratioBoundaries, orientation) => {
  if ((0, import_lodash_es.isNumber)(legendRatio)) {
    const isHorizontal = orientation === import_constants.ORIENTATION.Horizontal;
    const ratioInFraction = legendRatio / 100;
    const min = isHorizontal ? ratioBoundaries.minWidth : ratioBoundaries.minHeight;
    const max = isHorizontal ? ratioBoundaries.maxWidth : ratioBoundaries.maxHeight;
    return isInTheRange(ratioInFraction, min, max) ? legendRatio : import_constants.defaultRatio;
  }
  return import_constants.defaultRatio;
};
