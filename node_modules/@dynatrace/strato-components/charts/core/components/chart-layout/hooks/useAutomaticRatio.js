var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useAutomaticRatio_exports = {};
__export(useAutomaticRatio_exports, {
  useAutomaticRatio: () => useAutomaticRatio
});
module.exports = __toCommonJS(useAutomaticRatio_exports);
var import_react = require("react");
var import_constants = require("../../../constants.js");
var import_constants2 = require("../../legend/categorical/constants.js");
var import_constants3 = require("../../legend/range/constants.js");
var import_automatic_ratio = require("../utils/automatic-ratio.js");
const useAutomaticRatio = (orientation, ratio, containerAvailableArea) => {
  const [readyForAutoRatio, setReadyForAutoRatio] = (0, import_react.useState)(false);
  const [idealLegendSize, setIdealLegendSize] = (0, import_react.useState)(
    void 0
  );
  const resetAutoRatio = () => {
    setReadyForAutoRatio(false);
    setIdealLegendSize(void 0);
  };
  (0, import_react.useEffect)(() => {
    resetAutoRatio();
  }, [orientation]);
  const legendRef = (0, import_react.useCallback)(
    (node) => {
      const { height: containerHeight } = containerAvailableArea;
      const hasAutoRatioEnabled = !ratio || ratio === "auto";
      if (!hasAutoRatioEnabled) {
        setReadyForAutoRatio(true);
        return;
      }
      if (node === null || containerHeight === 0 || readyForAutoRatio) {
        return;
      }
      let longestSeries;
      const isCategoricalLegend = Array.from(node.classList).includes(
        import_constants2.CATEGORICAL_LEGEND_CLASS
      );
      const isRangeLegend = Array.from(node.classList).includes(
        import_constants3.RANGE_LEGEND_CLASS
      );
      if (isCategoricalLegend) {
        longestSeries = orientation === import_constants.ORIENTATION.Horizontal ? (0, import_automatic_ratio.getMaxItemSizeForCategoricalHorizontal)(node, containerHeight) : (0, import_automatic_ratio.getSizeForCategoricaVertical)();
      }
      if (isRangeLegend) {
        const hasLoadedLabels = node.getElementsByTagName("g").length !== 0;
        if (!hasLoadedLabels) {
          return;
        }
        longestSeries = orientation === import_constants.ORIENTATION.Horizontal ? (0, import_automatic_ratio.getMaxLabelForRangeHorizontal)(node) : (0, import_automatic_ratio.getLabelsHeightForRangeBottom)(node);
      }
      if (!readyForAutoRatio) {
        setIdealLegendSize(
          longestSeries !== void 0 && longestSeries > 0 ? longestSeries : import_constants2.MIN_ITEM_WIDTH
        );
        setReadyForAutoRatio(true);
      }
    },
    [readyForAutoRatio, containerAvailableArea, ratio, orientation]
  );
  return {
    legendCandidateSize: idealLegendSize,
    legendRef,
    readyForAutoRatio,
    refreshAutoRatio: resetAutoRatio
  };
};
