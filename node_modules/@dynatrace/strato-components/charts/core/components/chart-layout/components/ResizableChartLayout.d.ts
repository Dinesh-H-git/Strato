import { type PropsWithChildren } from 'react';
interface ResizableChartLayoutProps {
    onResize?: (ratio: number) => void;
    showResizer: boolean;
    showLegend: boolean;
}
export declare const ResizableChartLayout: {
    ({ onResize, showResizer, showLegend, }: PropsWithChildren<ResizableChartLayoutProps>): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
export {};
