import { type PropsWithChildren, type JSX, type ReactNode } from 'react';
interface LayoutStateHandlerProps {
    empty: boolean;
    height: string | number;
    emptyState: ReactNode | undefined;
    errorState: ReactNode | ((message: string) => JSX.Element) | undefined;
    showLoader: boolean;
}
export declare const LayoutStateHandler: (props: PropsWithChildren<LayoutStateHandlerProps>) => import("react/jsx-runtime").JSX.Element;
export {};
