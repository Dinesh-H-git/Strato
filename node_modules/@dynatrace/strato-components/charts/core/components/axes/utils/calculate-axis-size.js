var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_axis_size_exports = {};
__export(calculate_axis_size_exports, {
  getXTickMaxSize: () => getXTickMaxSize,
  getYTickMaxSize: () => getYTickMaxSize
});
module.exports = __toCommonJS(calculate_axis_size_exports);
var import_lodash_es = require("lodash");
var import_get_tick_label_props = require("./get-tick-label-props.js");
var import_detect_scale = require("../../../utils/detect-scale.js");
var import_constants = require("../constants.js");
const getYTickMaxSize = (containerWidth, label) => {
  const labelArea = (0, import_lodash_es.isEmpty)(label) ? 0 : import_constants.Y_LABEL_AREA;
  const maxSize = containerWidth - (import_constants.Y_AXES_TICK_LINE_OFFSET + labelArea) + import_get_tick_label_props.ELLIPSIS_BUFFER;
  return Math.max(maxSize, 0);
};
const getXTickMaxSize = (containerHeight, containerWidth, isTickRotated, scale, label, numTicks) => {
  const labelArea = (0, import_lodash_es.isEmpty)(label) ? 0 : import_constants.X_LABEL_AREA;
  let maxSize = 0;
  if (isTickRotated) {
    maxSize = containerHeight - (import_constants.X_AXES_TICK_LINE_OFFSET + labelArea) + import_get_tick_label_props.ELLIPSIS_BUFFER;
  } else {
    if (scale && (0, import_detect_scale.isBandScale)(scale)) {
      maxSize = scale.step();
    } else if (numTicks !== void 0 && numTicks > 0 && scale && !(0, import_detect_scale.isSymlogScale)(scale)) {
      maxSize = containerWidth / numTicks;
    } else {
      maxSize = containerWidth;
    }
  }
  return Math.max(maxSize, 0);
};
