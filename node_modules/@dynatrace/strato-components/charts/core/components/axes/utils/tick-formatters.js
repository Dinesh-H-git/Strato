var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var tick_formatters_exports = {};
__export(tick_formatters_exports, {
  getYAxisTickFormatter: () => getYAxisTickFormatter
});
module.exports = __toCommonJS(tick_formatters_exports);
var import_detect_scale = require("../../../utils/detect-scale.js");
var import_format_value = require("../../../utils/format-value.js");
const sanitizeEpsilonValue = (value) => value === Number.EPSILON ? 0 : value;
function getYAxisTickFormatter(scale, unit, formatter, numTicks, precision) {
  const valueFormatter = formatter ?? import_format_value.formatValue;
  const logFormatter = (scale2) => (value) => {
    const isSymlogMajorTick = (v) => {
      const abs = Math.abs(v);
      if (abs === 0) {
        return true;
      }
      const log = Math.log10(abs);
      return Math.abs(log - Math.round(log)) < 1e-9;
    };
    const isMajorValue = (0, import_detect_scale.isSymlogScale)(scale2) ? isSymlogMajorTick(value) : scale2.tickFormat(numTicks)(value) !== "";
    const nonEpsilonValue = sanitizeEpsilonValue(value);
    return isMajorValue ? valueFormatter(nonEpsilonValue, unit, precision) : "";
  };
  const linearFormatter = (value) => valueFormatter(value, unit, precision);
  const yAxisTickFormatter = (0, import_detect_scale.isLogScale)(scale) || (0, import_detect_scale.isSymlogScale)(scale) ? logFormatter(scale) : linearFormatter;
  return (0, import_format_value.tickValueFormatter)(yAxisTickFormatter, unit);
}
