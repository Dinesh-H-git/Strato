import type { DateString } from '../../../../types/dates.js';
export declare function getVisibleArrows(minMaxBoundaries: {
    min: number | DateString | Date;
    max: number | DateString | Date;
}, initialZoom: Date[] | number[] | undefined, currentDomain: Date[] | number[] | undefined, infiniteZoom: boolean): {
    left: boolean;
    right: boolean;
};
