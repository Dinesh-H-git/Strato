import type { DateThresholdData } from '../../../types/thresholds.js';
/**
 * Sparkline's threshold subcomponent props
 * @public @deprecated Use Sparkline.ThresholdIndicatorProps instead.
 */
export interface SparklineThresholdProps {
    /** The threshold data to be graphed by the component */
    data: DateThresholdData;
    /** The unique color picked for this timeseries representation in HEX, RGB, Color Token or HSL.*/
    color: string;
    /** Whereas to show the threshold ranges filled or only the strokes */
    strokeOnly?: boolean;
}
/**
 * Sparkline's thresholdIndicator subcomponent props
 * @public
 */
export interface SparklineThresholdIndicatorProps {
    /** The threshold data to be graphed by the component */
    data: DateThresholdData;
    /** The unique color picked for this timeseries representation in HEX, RGB, Color Token or HSL.*/
    color: string;
    /** Whereas to show the threshold ranges filled or only the strokes */
    strokeOnly?: boolean;
}
