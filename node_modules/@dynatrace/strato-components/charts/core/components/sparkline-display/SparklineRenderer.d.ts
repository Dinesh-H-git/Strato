import type { ScaleLinear, ScaleSymLog, ScaleTime } from 'd3-scale';
import type { SparklineDatapointMetadata, SparklineGapPolicy, SparklineVariant } from './types/sparkline-props.js';
import type { SeriesCurve } from '../../types/timeseries.js';
import type { CanvasGradientGenerator } from '../canvas/utils/create-canvas-gradient.js';
interface SparklineDisplayProps {
    data: SparklineDatapointMetadata[];
    width: number;
    height: number;
    xScale: ScaleTime<number, number>;
    yScale: ScaleLinear<number, number> | ScaleSymLog<number, number>;
    strokeColor: string;
    fillColor: string | CanvasGradientGenerator;
    variant: SparklineVariant;
    strokeWidth: number;
    curve?: SeriesCurve;
    radius?: number;
    gapPolicy: SparklineGapPolicy;
}
export declare const SparklineRenderer: ({ data, width, height, xScale, yScale, fillColor, strokeColor, variant, strokeWidth, curve, radius, gapPolicy, }: SparklineDisplayProps) => import("react/jsx-runtime").JSX.Element;
export {};
