var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_context_value_layout_exports = {};
__export(get_context_value_layout_exports, {
  getContextValueLayout: () => getContextValueLayout
});
module.exports = __toCommonJS(get_context_value_layout_exports);
var import_chars_font_width_map = require("../../text-ellipsis/chars-font-width-map.js");
var import_truncate_string = require("../../text-ellipsis/truncate-string.js");
var import_constants = require("../constants.js");
function getContextValueLayout({
  containerHeight,
  containerWidth,
  showContextValues,
  formattedContextMaxValue,
  formattedContextMinValue,
  labelsAlignment,
  labelsFixedWidth
}) {
  const fontSizePx = containerHeight >= import_constants.FONT_SIZE_BREAKING_POINT ? "12px" : "9px";
  const fontMap = containerHeight >= import_constants.FONT_SIZE_BREAKING_POINT ? import_chars_font_width_map.charsFontWidth12pixelsSize500Map : import_chars_font_width_map.charsFontWidth9pixelsSize500Map;
  let contextValueWidth = labelsFixedWidth;
  if (contextValueWidth === void 0) {
    const stripSign = (s) => s.startsWith(import_constants.NEGATIVE_SIGN) ? s.slice(1) : s;
    const maxDigitStr = stripSign(formattedContextMaxValue);
    const minDigitStr = stripSign(formattedContextMinValue);
    const maxContextValueWidth = showContextValues ? Math.min(
      Math.ceil((0, import_truncate_string.calculateWidth)(maxDigitStr, fontMap)),
      import_constants.CONTEXT_VALUE_MAX_WIDTH
    ) : 0;
    const minContextValueWidth = showContextValues ? Math.min(
      Math.ceil((0, import_truncate_string.calculateWidth)(minDigitStr, fontMap)),
      import_constants.CONTEXT_VALUE_MAX_WIDTH
    ) : 0;
    const signColumnReservation = showContextValues ? containerHeight > import_constants.CONTEXT_VALUE_SIGN_COLUMN_HEIGHT_THRESHOLD ? import_constants.CONTEXT_VALUE_SIGN_COLUMN_LARGE : import_constants.CONTEXT_VALUE_SIGN_COLUMN_SMALL : 0;
    contextValueWidth = Math.max(maxContextValueWidth, minContextValueWidth) + signColumnReservation + import_constants.CONTEXT_VALUE_TRAILING_PADDING;
  }
  const chartLeftOffset = showContextValues && labelsAlignment === "left" ? contextValueWidth : 0;
  const chartWidth = showContextValues ? containerWidth - contextValueWidth : containerWidth;
  return {
    fontSizePx,
    contextValueWidth,
    chartLeftOffset,
    chartWidth
  };
}
