import type { ConvertibleUnit, FormatOptions, Unit } from '@dynatrace-sdk/units';
import type { ChartUnit } from '../../../types/chart-unit.js';
import type { Formatter } from '../../../types/formatter.js';
interface GetFormattedContextValuesProps {
    unit: ChartUnit;
    formatter?: Formatter | FormatOptions<Unit, ConvertibleUnit>;
    domainValues: {
        min: number;
        max: number;
    };
}
interface FormattedContextValues {
    formattedContextMaxValue: string;
    formattedContextMinValue: string;
}
export declare function getFormattedContextValues({ unit, formatter, domainValues, }: GetFormattedContextValuesProps): FormattedContextValues;
export {};
