var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var iterate_sparkline_slots_exports = {};
__export(iterate_sparkline_slots_exports, {
  iterateSparklineSlots: () => iterateSparklineSlots
});
module.exports = __toCommonJS(iterate_sparkline_slots_exports);
var import_react = require("react");
var import_is_slot = require("../../../../../core/utils/is-slot.js");
var import_ThresholdIndicator = require("../slots/ThresholdIndicator.js");
var import_XAxis = require("../slots/XAxis.js");
var import_YAxis = require("../slots/YAxis.js");
function iterateSparklineSlots(children) {
  const thresholds = [];
  let xAxis = void 0;
  let yAxis = void 0;
  import_react.Children.forEach(children, (child) => {
    if ((0, import_is_slot.isSlot)(child, import_ThresholdIndicator.ThresholdIndicator)) {
      const thresholdProps = child.props;
      thresholds.push(thresholdProps);
    } else if ((0, import_is_slot.isSlot)(child, import_XAxis.XAxis)) {
      xAxis = {
        ...xAxis,
        ...child.props
      };
    } else if ((0, import_is_slot.isSlot)(child, import_YAxis.YAxis)) {
      yAxis = {
        ...yAxis,
        ...child.props
      };
    }
  });
  return {
    thresholds,
    xAxis,
    yAxis
  };
}
