var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_x_axis_config_exports = {};
__export(get_x_axis_config_exports, {
  getXAxisConfig: () => getXAxisConfig
});
module.exports = __toCommonJS(get_x_axis_config_exports);
var import_constants = require("../constants.js");
const getXAxisConfigDate = (value) => {
  if (typeof value === "number") {
    return new Date(value);
  }
  return value;
};
const getXAxisConfig = (xAxis) => {
  if (!xAxis) {
    return import_constants.defaultXAxisConfig;
  }
  const defaultMin = xAxis.min === "auto" || xAxis.min === "data-min" || xAxis.min === void 0;
  const defaultMax = xAxis.max === "auto" || xAxis.max === "data-max" || xAxis.max === void 0;
  return {
    min: defaultMin ? "data-min" : getXAxisConfigDate(xAxis.min),
    max: defaultMax ? "data-max" : getXAxisConfigDate(xAxis.max)
  };
};
