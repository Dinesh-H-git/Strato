var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_formatted_context_values_exports = {};
__export(get_formatted_context_values_exports, {
  getFormattedContextValues: () => getFormattedContextValues
});
module.exports = __toCommonJS(get_formatted_context_values_exports);
var import_format_value = require("../../../utils/format-value.js");
var import_get_formatter = require("../../../utils/get-formatter.js");
function getFormattedContextValues({
  unit,
  formatter,
  domainValues
}) {
  const resolvedFormatter = (0, import_get_formatter.getFormatter)(formatter);
  const contextFormatter = resolvedFormatter ?? import_format_value.formatValue;
  const wrappedFormatter = (0, import_format_value.tickValueFormatter)(
    (value) => contextFormatter(value, unit),
    unit
  );
  return {
    formattedContextMaxValue: wrappedFormatter(domainValues.max),
    formattedContextMinValue: wrappedFormatter(domainValues.min)
  };
}
