var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_sparkline_y_scale_exports = {};
__export(get_sparkline_y_scale_exports, {
  getSparklineYScale: () => getSparklineYScale
});
module.exports = __toCommonJS(get_sparkline_y_scale_exports);
var import_lodash_es = require("lodash");
var import_core_scale = require("../../../utils/core-scale.js");
const VERTICAL_PADDING = 4;
const getSparklineYScale = (domainValues, height, yAxis, isBarVariant) => {
  const { min: dataMin, max: dataMax } = domainValues;
  const scaleType = yAxis.scale ?? "linear";
  let yAxisMin = dataMin;
  let yAxisMax = dataMax;
  if (isBarVariant) {
    yAxisMin = dataMin > 0 ? 0 : dataMin;
    yAxisMax = dataMax < 0 ? 0 : dataMax;
  }
  if ((0, import_lodash_es.isNumber)(yAxis.max)) {
    yAxisMax = yAxis.max;
  }
  if ((0, import_lodash_es.isNumber)(yAxis.min)) {
    yAxisMin = yAxis.min;
  }
  return (0, import_core_scale.getScale)(
    scaleType,
    [yAxisMin, yAxisMax],
    [height - VERTICAL_PADDING, VERTICAL_PADDING]
  );
};
