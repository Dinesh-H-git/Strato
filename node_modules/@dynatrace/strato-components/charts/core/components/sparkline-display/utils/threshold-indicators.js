var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var threshold_indicators_exports = {};
__export(threshold_indicators_exports, {
  mapToThresholdsIndicators: () => mapToThresholdsIndicators
});
module.exports = __toCommonJS(threshold_indicators_exports);
var import_type_guards = require("../../../types/type-guards.js");
var import_thresholds_to_indicator = require("../../../utils/thresholds-to-indicator.js");
function getMinMax(y0, y1) {
  return [y0, y1].sort((a, b) => a - b);
}
function mapToThresholdsIndicators(thresholdsProps, width, height, xScale, yScale) {
  const thresholdIndicators = [];
  thresholdsProps.forEach((thresholdProps) => {
    const { data, color, strokeOnly } = thresholdProps;
    const indicatorData = (0, import_thresholds_to_indicator.transformThresholdIntoIndicator)(data, width, yScale, xScale);
    const showStroke = strokeOnly || (0, import_type_guards.isDynamic)(data) || (0, import_type_guards.isStatic)(data);
    const isValueInBounds = indicatorData.some((value) => {
      const [minValue, maxValue] = getMinMax(value.y0, value.y1);
      return maxValue <= height && maxValue >= 0 || minValue >= 0 && minValue <= height || maxValue > height && minValue < 0;
    });
    if (isValueInBounds) {
      thresholdIndicators.push({
        id: crypto.randomUUID(),
        indicatorData,
        color,
        strokeOnly: showStroke
      });
    }
  });
  return thresholdIndicators;
}
