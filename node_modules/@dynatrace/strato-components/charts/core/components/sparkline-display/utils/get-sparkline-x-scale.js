var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_sparkline_x_scale_exports = {};
__export(get_sparkline_x_scale_exports, {
  getSparklineXScale: () => getSparklineXScale
});
module.exports = __toCommonJS(get_sparkline_x_scale_exports);
var import_d3_scale = require("d3-scale");
var import_constants = require("../constants.js");
const getSparklineXScale = (data, width, padding, isBarVariant) => {
  const rangeMax = width - padding - (isBarVariant ? import_constants.TIMESTAMP_BAR_X_DEVIATION : 0);
  const xAxisDomain = {
    min: Infinity,
    max: -Infinity
  };
  data.datapoints.forEach((datapoint) => {
    const start = datapoint.start.getTime();
    const end = datapoint.end?.getTime() ?? start;
    const timestamp = isBarVariant ? start : end;
    xAxisDomain.min = timestamp < xAxisDomain.min ? timestamp : xAxisDomain.min;
    xAxisDomain.max = end > xAxisDomain.max ? end : xAxisDomain.max;
  });
  return (0, import_d3_scale.scaleTime)().domain([xAxisDomain.min, xAxisDomain.max]).range([padding, rangeMax]);
};
