import { type ReactNode } from 'react';
export declare function getChartStatesTemplates(children: ReactNode): {
    emptyState: ReactNode;
    errorState: ReactNode | ((errorMessage: string) => import("react").JSX.Element);
};
