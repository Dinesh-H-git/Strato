var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var SelectionArea_exports = {};
__export(SelectionArea_exports, {
  SelectionArea: () => SelectionArea
});
module.exports = __toCommonJS(SelectionArea_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_selection_area_path_ref_context = require("./context/selection-area-path-ref.context.js");
var import_Selection = require("./Selection.js");
var import_SelectionArea_css = require("./SelectionArea.sty.js");
var import_calculate_selection_delta = require("./utils/calculate-selection-delta.js");
var import_clamp_selection_area = require("./utils/clamp-selection-area.js");
var import_move_selection_area = require("./utils/move-selection-area.js");
var import_ZoomSelection = require("./ZoomSelection.js");
var import_constants = require("../interactions-overlay/constants.js");
var import_adjust_position = require("../interactions-overlay/utils/adjust-position.js");
var import_normalize_selection_area = require("../interactions-overlay/utils/normalize-selection-area.js");
const SelectionArea = (0, import_react.forwardRef)((props, forwardedRef) => {
  const {
    bounds,
    selection,
    variant,
    focusable,
    interactable,
    bottomFormatter,
    topFormatter,
    onKeyDown: handleWrapperKeyDown,
    onSelectionChange,
    isBottomLabelRotated = false,
    isTopLabelRotated = false
  } = props;
  const [start, end] = selection;
  const { width, height } = bounds;
  const startX = start.relative.x;
  const endX = end.relative.x;
  const range = [0, width + import_constants.SELECTION_RIGHT_OVERFLOW];
  const handleSelectionAreaKeyDown = (event) => {
    const delta = (0, import_calculate_selection_delta.calculateSelectionDelta)(event, width);
    const nextSelection = (0, import_clamp_selection_area.clampSelectionArea)(
      (0, import_move_selection_area.moveSelectionArea)(selection, delta),
      range
    );
    onSelectionChange?.({ selection: nextSelection, accessible: true });
  };
  const handleSelectionAreaStartKeyDown = (event) => {
    const delta = (0, import_calculate_selection_delta.calculateSelectionDelta)(event, width);
    const adjustedStart = (0, import_adjust_position.adjustPosition)(start, delta);
    const nextSelection = (0, import_clamp_selection_area.clampSelectionArea)(
      (0, import_normalize_selection_area.normalizeSelectionArea)([adjustedStart, end]),
      range
    );
    onSelectionChange?.({ selection: nextSelection, accessible: true });
  };
  const handleSelectionAreaEndKeyDown = (event) => {
    const delta = (0, import_calculate_selection_delta.calculateSelectionDelta)(event, width);
    const adjustedEnd = (0, import_adjust_position.adjustPosition)(end, delta);
    const nextSelection = (0, import_clamp_selection_area.clampSelectionArea)(
      (0, import_normalize_selection_area.normalizeSelectionArea)([start, adjustedEnd]),
      range
    );
    onSelectionChange?.({ selection: nextSelection, accessible: true });
  };
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    "svg",
    {
      className: import_SelectionArea_css.brushOverlay,
      onKeyDown: handleWrapperKeyDown,
      onBlur: (event) => event.stopPropagation(),
      children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_selection_area_path_ref_context.SelectionAreaPathRefContext.Provider, { value: forwardedRef, children: variant === "zoom" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        import_ZoomSelection.ZoomSelection,
        {
          bounds,
          focusable,
          interactable,
          bottomFormatter,
          topFormatter,
          startX,
          endX,
          height,
          handleSelectionAreaKeyDown,
          handleSelectionAreaEndKeyDown,
          handleSelectionAreaStartKeyDown,
          isBottomLabelRotated,
          isTopLabelRotated
        }
      ) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        import_Selection.Selection,
        {
          bounds,
          focusable,
          interactable,
          bottomFormatter,
          topFormatter,
          startX,
          endX,
          height,
          width,
          handleSelectionAreaEndKeyDown,
          handleSelectionAreaStartKeyDown,
          start,
          end,
          onSelectionChange,
          isBottomLabelRotated,
          isTopLabelRotated
        }
      ) })
    }
  );
});
