import { type KeyboardEventHandler } from 'react';
interface SelectionEdgeProps {
    x: number;
    y: number;
    focusable: boolean;
    interactable: boolean;
    onKeyDown: KeyboardEventHandler;
    onDragMove: (x: number) => void;
    onDragStart: () => void;
    onDragEnd: () => void;
    topLabel?: string;
    bottomLabel?: string;
    chartBounds: DOMRect;
    isBottomLabelRotated?: boolean;
    isTopLabelRotated?: boolean;
    edgeId: string;
    hoveredEdge: string;
    pointerHandlers: {
        onPointerEnter: () => void;
        onPointerLeave: () => void;
    };
}
export declare const SelectionEdge: ({ x, y, focusable, interactable, onKeyDown, onDragMove, onDragStart, onDragEnd, topLabel, bottomLabel, chartBounds, isBottomLabelRotated, isTopLabelRotated, edgeId, hoveredEdge, pointerHandlers, }: SelectionEdgeProps) => import("react/jsx-runtime").JSX.Element;
export {};
