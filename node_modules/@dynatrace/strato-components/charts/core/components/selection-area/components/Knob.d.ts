import { type KeyboardEventHandler } from 'react';
interface KnobProps {
    /** Coordinate x for knob */
    x: number;
    /** Coordinate x for knob */
    y: number;
    onKeyDown: KeyboardEventHandler;
    interactable: boolean;
    onDragMove: (x: number) => void;
    onDragStart: () => void;
    onDragEnd: () => void;
    /** Makes the element accessible through keyboard */
    focusable?: boolean;
    edgeId: string;
    hoveredEdge: string;
    pointerHandlers: {
        onPointerEnter: () => void;
        onPointerLeave: () => void;
    };
}
export declare const Knob: ({ y, x, interactable, onKeyDown, focusable, onDragMove, onDragStart, onDragEnd, edgeId, hoveredEdge, pointerHandlers, }: KnobProps) => import("react/jsx-runtime").JSX.Element;
export {};
