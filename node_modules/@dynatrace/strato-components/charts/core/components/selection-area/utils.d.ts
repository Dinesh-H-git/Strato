import type { RelativeAbsolutePosition } from '../../types/pointer-events.ts';
export type CurrentRange = [number, number];
export type NextRange = [
    Pick<RelativeAbsolutePosition, 'relative'>,
    Pick<RelativeAbsolutePosition, 'relative'>
];
export declare const LEFT_EDGE_ID = "left-edge";
export declare const RIGHT_EDGE_ID = "right-edge";
export declare const changedEdge: (currentRange: CurrentRange, nextRange: NextRange) => false | "left-edge" | "right-edge";
