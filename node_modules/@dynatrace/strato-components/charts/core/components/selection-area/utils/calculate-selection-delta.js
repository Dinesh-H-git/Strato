var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_selection_delta_exports = {};
__export(calculate_selection_delta_exports, {
  calculateSelectionDelta: () => calculateSelectionDelta
});
module.exports = __toCommonJS(calculate_selection_delta_exports);
var import_constants = require("../../interactions-overlay/constants.js");
const calculateSelectionDelta = (event, width) => {
  switch (event.key) {
    case import_constants.KeyboardControls.LEFT_ARROW: {
      const factor = event.shiftKey ? -import_constants.LARGE_STEP_FACTOR : -import_constants.SMALL_STEP_FACTOR;
      return width * factor;
    }
    case import_constants.KeyboardControls.RIGHT_ARROW: {
      const factor = event.shiftKey ? import_constants.LARGE_STEP_FACTOR : import_constants.SMALL_STEP_FACTOR;
      return width * factor;
    }
    default:
      return 0;
  }
};
