import type { RelativeAbsolutePosition } from '../../../types/pointer-events.js';
/**
 * Takes care of moving the whole selection area to the left or to the right
 * @param selection -
 * @param delta -
 */
export declare const moveSelectionArea: (selection: [RelativeAbsolutePosition, RelativeAbsolutePosition], delta: number) => [RelativeAbsolutePosition, RelativeAbsolutePosition];
