import type { RelativeAbsolutePosition } from '../../../types/pointer-events.js';
/**
 * Adjusts the selection area to ensure it does not go beyond the specified bounds.
 * @param selection -
 * @param bounds - in pixels
 */
export declare const clampSelectionArea: (selection: [RelativeAbsolutePosition, RelativeAbsolutePosition], bounds: [number, number]) => [RelativeAbsolutePosition, RelativeAbsolutePosition];
