var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useToolbar_exports = {};
__export(useToolbar_exports, {
  useToolbar: () => useToolbar
});
module.exports = __toCommonJS(useToolbar_exports);
var import_react = require("react");
const useToolbar = (collapsed, menuDisplayMode) => {
  const [isExpanded, setIsExpanded] = (0, import_react.useState)(!collapsed);
  const [lastItemSelected, setLastItemSelected] = (0, import_react.useState)(void 0);
  const [fitsBounds, setFitsBounds] = (0, import_react.useState)(true);
  (0, import_react.useEffect)(() => {
    toggleExpanded(fitsBounds && !collapsed);
  }, [collapsed, fitsBounds]);
  const updateLastItemSelected = (lastItemSelected2) => {
    setLastItemSelected(lastItemSelected2);
  };
  const toggleExpanded = (state) => {
    setIsExpanded(state);
  };
  const updateFitsBounds = (isFitting) => setFitsBounds(isFitting);
  const isMenuShowable = menuDisplayMode === "always" || menuDisplayMode === "collapsed" && !isExpanded || menuDisplayMode === "expanded" && isExpanded;
  const shouldShowMenu = !isMenuShowable && !fitsBounds || isMenuShowable;
  const isCollapsable = menuDisplayMode !== "expanded" && menuDisplayMode !== "never";
  return {
    isExpanded,
    lastItemSelected,
    updateLastItemSelected,
    toggleExpanded,
    isCollapsable,
    fitsBounds,
    updateFitsBounds,
    shouldShowMenu
  };
};
