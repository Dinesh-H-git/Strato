import { type ReactNode } from 'react';
export declare const CONTROL_WIDTH = 37.5;
/**
 * Estimates toolbar size depending on defined children count
 *
 * @param children -
 */
export declare const estimateToolbarWidth: (children: ReactNode) => number;
