import { RelativePosition } from '../../../hooks/useDragElement.js';
import { ToolbarPlacement } from '../../../types/toolbar.js';
/**
 * Gets toolbar top and right position with a given placement and a given boundaries
 *
 * @param placement -
 * @param maxBoundaries -
 */
export declare function getToolbarPlacement(placement: ToolbarPlacement, maxBoundaries: RelativePosition): {
    top: number;
    right: number;
    left?: undefined;
} | {
    top: number;
    right: number;
    left: number;
};
