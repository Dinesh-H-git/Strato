export type ChartLoaderOverlayProps = {
    /** Whether the chart is loading. This helps the Overlay to take specific actions while loading, like prevent browser zoom. */
    loading: boolean;
    /** Determines whether the zoom interaction is enabled or not by a boolean.*/
    zoomEnabled: boolean;
};
