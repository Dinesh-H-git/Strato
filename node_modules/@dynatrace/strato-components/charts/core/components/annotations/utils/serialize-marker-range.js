var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var serialize_marker_range_exports = {};
__export(serialize_marker_range_exports, {
  deserializeMarkerRange: () => deserializeMarkerRange,
  serializeMarkerRange: () => serializeMarkerRange
});
module.exports = __toCommonJS(serialize_marker_range_exports);
var import_lodash_es = require("lodash");
var import_is_numerical_marker = require("./is-numerical-marker.js");
const serializeNumericalRange = ({ start, end }) => [start, end ?? start].join("_");
const serializeTimeRange = ({ start, end }) => [start.getTime(), end?.getTime() ?? start.getTime()].join("_");
const serializeMarkerRange = (marker, trackIndex) => {
  const markerString = (0, import_is_numerical_marker.isNumericalMarker)(marker) ? serializeNumericalRange(marker) : serializeTimeRange(marker);
  return `${trackIndex}_${markerString}`;
};
const deserializeMarkerRange = (range) => {
  const [trackIndex, from, to] = range.split("_").map(import_lodash_es.toFinite);
  return [from, to, trackIndex];
};
