var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var aggregate_markers_coords_exports = {};
__export(aggregate_markers_coords_exports, {
  aggregateMarkersCoords: () => aggregateMarkersCoords
});
module.exports = __toCommonJS(aggregate_markers_coords_exports);
var import_react = require("react");
var import_react_is = require("react-is");
var import_colors = __toESM(require("@dynatrace/strato-design-tokens/colors"));
var import_calculate_expand_marker_coordinates = require("./calculate-expand-marker-coordinates.js");
var import_is_slot = require("../../../../../core/utils/is-slot.js");
var import_color_contrast = require("../../../utils/color-contrast.js");
var import_getTokenComputedStyle = require("../../../utils/getTokenComputedStyle.js");
var import_MarkerAtom = require("../slots/MarkerAtom.js");
var import_TrackAtom = require("../slots/TrackAtom.js");
function aggregateMarkersCoords(children, trackWidth) {
  const markersPerTrack = [];
  const markerEventHandlers = {};
  const trackEventHandlers = {};
  const tracksConfig = [];
  const icons = [];
  import_react.Children.forEach(children, (track, trackIndex) => {
    if ((0, import_is_slot.isSlot)(track, import_TrackAtom.TrackAtom)) {
      markersPerTrack[trackIndex] = [];
      trackEventHandlers[trackIndex] = {
        onMouseEnter: track.props.onMouseEnter
      };
      const markersByTrackConfig = [];
      import_react.Children.forEach(track.props.children, (marker) => {
        if ((0, import_is_slot.isSlot)(marker, import_MarkerAtom.MarkerAtom)) {
          let markerAtomCoords;
          markersByTrackConfig.push(marker.props);
          const { textOverflow, content, x, width } = marker.props;
          if (marker.props.width === 0) {
            markerAtomCoords = (0, import_calculate_expand_marker_coordinates.calculateValueMarkerCoordinates)(
              content,
              x,
              trackWidth
            );
          } else {
            markerAtomCoords = (0, import_calculate_expand_marker_coordinates.calculateRangeMarkerCoordinates)(
              content,
              x,
              width,
              trackWidth,
              textOverflow
            );
          }
          markerEventHandlers[`${trackIndex}_${markerAtomCoords.join("_")}`] = {
            onMouseEnter: marker.props.onMouseEnter,
            onMouseMove: marker.props.onMouseMove,
            onMouseLeave: marker.props.onMouseLeave,
            onClick: marker.props.onClick
          };
          markersPerTrack[trackIndex].push(markerAtomCoords);
          if ((0, import_react_is.isElement)(marker.props.content)) {
            const markerColor = (0, import_getTokenComputedStyle.getTokenComputedStyle)(marker.props.color);
            const lightTextColor = (0, import_color_contrast.getColorTokenDefaultValue)(
              import_colors.default.Text.Neutral.OnAccent.Default
            );
            const darkTextColor = (0, import_color_contrast.getColorTokenDefaultValue)(
              import_colors.default.Text.Neutral.Default
            );
            const contrastTextColor = (0, import_color_contrast.getColorByContrast)(
              markerColor,
              lightTextColor,
              darkTextColor
            );
            icons.push([marker.props.content, contrastTextColor]);
          }
        }
      });
      tracksConfig.push({ ...track.props, markers: markersByTrackConfig });
    }
  });
  return {
    markersPerTrack,
    markerEventHandlers,
    trackEventHandlers,
    icons,
    tracksConfig
  };
}
