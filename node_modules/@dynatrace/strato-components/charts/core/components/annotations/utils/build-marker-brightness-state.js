var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_marker_brightness_state_exports = {};
__export(build_marker_brightness_state_exports, {
  buildMarkerBrightnessState: () => buildMarkerBrightnessState
});
module.exports = __toCommonJS(build_marker_brightness_state_exports);
var import_lodash_es = require("lodash");
const buildMarkerBrightnessState = ({ data, hoveredGroup, hoveredListItem, pinnedGroup }, markerId, isHoveringListItem) => {
  let state = "default";
  if ((pinnedGroup ?? hoveredGroup) && data) {
    const markersIdWithinGroup = data.map(({ key }) => key);
    if (markersIdWithinGroup.includes(markerId)) {
      if ((0, import_lodash_es.isUndefined)(pinnedGroup)) {
        state = "hovered";
      } else if ((0, import_lodash_es.isUndefined)(hoveredListItem)) {
        state = "active";
      } else {
        state = isHoveringListItem ? "active" : "inactive";
      }
    }
  }
  return state;
};
