var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_annotations_visible_area_exports = {};
__export(get_annotations_visible_area_exports, {
  getAnnotationsVisibleArea: () => getAnnotationsVisibleArea
});
module.exports = __toCommonJS(get_annotations_visible_area_exports);
var import_lodash_es = require("lodash");
var import_get_annotations_tracks_height = require("./get-annotations-tracks-height.js");
const DEFAULT_VISIBLE_ANNOTATIONS_TRACKS = 3;
const getAnnotationsVisibleArea = (numTracks, visibleTracksAmountLimit = DEFAULT_VISIBLE_ANNOTATIONS_TRACKS, numHiddenTracks = 0) => {
  const sanitizedNumHiddenTracks = numHiddenTracks < 0 ? 0 : numHiddenTracks;
  const totalTracksHeight = (0, import_get_annotations_tracks_height.getAnnotationsTracksHeight)(
    numTracks - sanitizedNumHiddenTracks
  );
  const isVisibleTracksAmountLimitDefined = !(0, import_lodash_es.isUndefined)(
    visibleTracksAmountLimit
  );
  return isVisibleTracksAmountLimitDefined ? Math.min(
    (0, import_get_annotations_tracks_height.getAnnotationsTracksHeight)(visibleTracksAmountLimit),
    totalTracksHeight
  ) : totalTracksHeight;
};
