var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var define_marker_content_exports = {};
__export(define_marker_content_exports, {
  defineMarkerContent: () => defineMarkerContent
});
module.exports = __toCommonJS(define_marker_content_exports);
var import_lodash_es = require("lodash");
var import_sanitize_symbol = require("../../../../../core/utils/sanitize-symbol.js");
const defineMarkerContent = (markers) => {
  let markerContent;
  if (markers.length > 1) {
    markerContent = markers.length.toString();
  } else {
    const marker = markers[0];
    const { symbol, title } = marker;
    if (!(0, import_lodash_es.isUndefined)(symbol)) {
      if ((0, import_lodash_es.isString)(symbol) || (0, import_lodash_es.isNumber)(symbol)) {
        markerContent = (0, import_sanitize_symbol._sanitizeSymbol)(`${symbol}`);
      } else {
        markerContent = symbol;
      }
    } else {
      markerContent = title ?? "";
    }
  }
  return markerContent;
};
