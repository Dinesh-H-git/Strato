var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_marker_dimension_exports = {};
__export(calculate_marker_dimension_exports, {
  calculateMarkerDimension: () => calculateMarkerDimension
});
module.exports = __toCommonJS(calculate_marker_dimension_exports);
var import_lodash_es = require("lodash");
var import_is_range_marker = require("./is-range-marker.js");
var import_constants = require("../constants.js");
const calculateMarkerDimension = (marker, scale) => {
  let x;
  const isReversed = scale.domain()[0] > scale.domain()[1];
  const finishMarker = marker.end ?? marker.start;
  const start = isReversed ? finishMarker : marker.start;
  const end = isReversed ? marker.start : finishMarker;
  if ((0, import_lodash_es.isNumber)(start)) {
    x = scale(start) ?? 0;
  } else {
    x = scale(start.getTime()) ?? 0;
  }
  let width = 0;
  if ((0, import_is_range_marker.isRangeMarker)(marker)) {
    let x1;
    if ((0, import_lodash_es.isNumber)(end)) {
      x1 = scale(end) ?? 0;
    } else {
      x1 = scale(end.getTime()) ?? 0;
    }
    width = calculateMarkerWidth(x, x1);
  }
  return { x, width };
};
const calculateMarkerWidth = (x0, x1) => {
  const width = x1 - x0;
  if (width < import_constants.MINIMUM_MARKER_WIDTH) {
    return import_constants.MINIMUM_MARKER_WIDTH;
  }
  return width;
};
