var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_expand_marker_coordinates_exports = {};
__export(calculate_expand_marker_coordinates_exports, {
  calculateMarkerExpandToSideCoordinates: () => calculateMarkerExpandToSideCoordinates,
  calculateRangeMarkerCoordinates: () => calculateRangeMarkerCoordinates,
  calculateValueMarkerCoordinates: () => calculateValueMarkerCoordinates
});
module.exports = __toCommonJS(calculate_expand_marker_coordinates_exports);
var import_chars_font_width_map = require("../../text-ellipsis/chars-font-width-map.js");
var import_truncate_string = require("../../text-ellipsis/truncate-string.js");
var import_constants = require("../components/canvas/renderers/value-marker-renderer/constants.js");
var import_constants2 = require("../constants.js");
const calculateValueMarkerCoordinates = (content, originalX, trackWidth) => {
  let expandedContentWidth = 0;
  if (typeof content === "string") {
    expandedContentWidth = (0, import_truncate_string.calculateWidth)(
      content,
      import_chars_font_width_map.charsFontWidth12pixelsSize500Map
    );
  }
  const widthNeededToRenderText = expandedContentWidth + import_constants2.MARKER_CONTENT_TO_BORDER_PADDING * 2;
  if (widthNeededToRenderText > import_constants.VALUE_MARKER_WIDTH) {
    const halfWidthText = widthNeededToRenderText / 2;
    const x02 = originalX - halfWidthText;
    const x12 = originalX + halfWidthText;
    const overflowsLeft2 = x02 < 0;
    const overflowsRight2 = x12 > trackWidth;
    const canExpandCenter = !overflowsLeft2 && !overflowsRight2;
    if (canExpandCenter) {
      return [x02, x12];
    }
    return calculateMarkerExpandToSideCoordinates(
      originalX,
      widthNeededToRenderText,
      trackWidth
    );
  }
  const overflowsLeft = originalX - import_constants2.MARKER_RADIUS < 0;
  const overflowsRight = originalX + import_constants2.MARKER_RADIUS > trackWidth;
  let x0 = originalX - import_constants2.MARKER_RADIUS;
  let x1 = originalX + import_constants2.MARKER_RADIUS;
  if (overflowsLeft) {
    x0 = 0;
    x1 = import_constants.VALUE_MARKER_WIDTH;
  } else if (overflowsRight) {
    x0 = trackWidth - import_constants.VALUE_MARKER_WIDTH;
    x1 = trackWidth;
  }
  return [x0, x1];
};
const calculateRangeMarkerCoordinates = (content, originalX, originalWidth, trackWidth, textOverflow) => {
  if (textOverflow === "expand") {
    let expandedContentWidth = 0;
    if (typeof content === "string") {
      expandedContentWidth = (0, import_truncate_string.calculateWidth)(
        content,
        import_chars_font_width_map.charsFontWidth12pixelsSize500Map
      );
    }
    const widthNeededToRenderText = expandedContentWidth + import_constants2.MARKER_CONTENT_TO_BORDER_PADDING * 2;
    if (widthNeededToRenderText > originalWidth) {
      const halfWidthText = widthNeededToRenderText / 2;
      const center = originalWidth / 2 + originalX;
      const x0 = center - halfWidthText;
      const x1 = center + halfWidthText;
      const overflowsLeft = x0 < 0;
      const overflowsRight = x1 > trackWidth;
      const canExpandCenter = !overflowsLeft && !overflowsRight;
      if (canExpandCenter) {
        return [x0, x1];
      }
      return calculateMarkerExpandToSideCoordinates(
        originalX,
        widthNeededToRenderText,
        trackWidth
      );
    }
  }
  return [originalX, originalX + originalWidth];
};
const calculateMarkerExpandToSideCoordinates = (originalX, contentWidth, trackWidth) => {
  const leftSpace = originalX;
  const rightSpace = trackWidth - originalX;
  let x0;
  let x1;
  if (leftSpace > rightSpace) {
    x0 = Math.max(originalX - contentWidth, 0);
    x1 = originalX;
  } else {
    x0 = originalX;
    x1 = Math.min(originalX + contentWidth, trackWidth);
  }
  return [x0, x1];
};
