import type { ScaleLinear, ScaleLogarithmic, ScaleSymLog, ScaleTime } from 'd3-scale';
import type { AnnotationsMarkerConfig } from '../types/annotations-marker-props.js';
export declare const calculateMarkerDimension: (marker: AnnotationsMarkerConfig, scale: ScaleTime<number, number> | ScaleLinear<number, number> | ScaleLogarithmic<number, number> | ScaleSymLog<number, number>) => {
    x: number;
    width: number;
};
