import { type ReactNode } from 'react';
import { MarkerCoords } from '../types/marker-coords.js';
export declare const calculateValueMarkerCoordinates: (content: ReactNode, originalX: number, trackWidth: number) => MarkerCoords;
export declare const calculateRangeMarkerCoordinates: (content: ReactNode, originalX: number, originalWidth: number, trackWidth: number, textOverflow: "expand" | "truncate") => MarkerCoords;
export declare const calculateMarkerExpandToSideCoordinates: (originalX: number, contentWidth: number, trackWidth: number) => MarkerCoords;
