var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_min_max_as_number_exports = {};
__export(get_min_max_as_number_exports, {
  getMinMaxAsNumber: () => getMinMaxAsNumber
});
module.exports = __toCommonJS(get_min_max_as_number_exports);
var import_lodash_es = require("lodash");
var import_parse_date_number_to_number = require("./parse-date-number-to-number.js");
const getMinMaxAsNumber = (firstValue, secondValue) => {
  const first = (0, import_parse_date_number_to_number.parseDateNumberToNumber)(firstValue);
  const second = (0, import_lodash_es.isUndefined)(secondValue) ? first : (0, import_parse_date_number_to_number.parseDateNumberToNumber)(secondValue);
  return {
    min: Math.min(first, second),
    max: Math.max(first, second)
  };
};
