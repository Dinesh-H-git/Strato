var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useOverlayEvents_exports = {};
__export(useOverlayEvents_exports, {
  useOverlayEvents: () => useOverlayEvents
});
module.exports = __toCommonJS(useOverlayEvents_exports);
var import_lodash_es = require("lodash");
var import_react = require("react");
var import_selectors = require("./store/selectors.js");
var import_state_setters = require("./store/state-setters.js");
var import_useAnnotationsHandlers = require("./useAnnotationsHandlers.js");
var import_useMarkersInteractions = require("./useMarkersInteractions.js");
var import_constants = require("../constants.js");
var import_MarkerCoordinates_context = require("../contexts/MarkerCoordinates.context.js");
var import_AnnotationsDataInitialState = require("../providers/AnnotationsDataInitialState.js");
var import_in_marker_bounds = require("../utils/in-marker-bounds.js");
const useOverlayEvents = () => {
  const markerCoordsPerTrack = (0, import_react.useContext)(import_MarkerCoordinates_context.MarkerCoordinatesContext);
  const {
    hoverTrack,
    blurTrack,
    hoverMarker,
    blurMarker,
    clickMarker,
    currentHoveredTrack,
    currentHoveredMarker
  } = (0, import_useAnnotationsHandlers.useAnnotationsHandlers)();
  const { pinnedGroup } = (0, import_selectors.useAnnotationsSelectionState)();
  const updateTooltipState = (0, import_state_setters.useSetStateTooltip)();
  const updateTooltipSelection = (0, import_state_setters.useSetStateSelection)();
  const onMarkersInteraction = (0, import_useMarkersInteractions.useMarkersInteractions)();
  const handleMouseMove = (e) => {
    const { clientX, clientY, currentTarget } = e;
    const { top, left } = currentTarget.getBoundingClientRect();
    const mousePosition = {
      x: clientX - left,
      y: clientY - top
    };
    let trackIndex = Math.max(0, Math.floor(mousePosition.y / import_constants.TRACK_HEIGHT));
    if (trackIndex !== currentHoveredTrack.current) {
      hoverTrack(e, trackIndex);
    }
    if (markerCoordsPerTrack !== void 0) {
      let isHoveringMarker = false;
      if (trackIndex >= markerCoordsPerTrack.length) {
        trackIndex = markerCoordsPerTrack.length - 1;
      }
      const markerCoords = markerCoordsPerTrack[trackIndex];
      for (let markerIndex = markerCoords.length - 1; markerIndex >= 0; markerIndex--) {
        const marker = markerCoords[markerIndex];
        if ((0, import_in_marker_bounds.inMarkerBounds)(mousePosition, trackIndex, marker)) {
          const markerId = `${trackIndex}_${marker.join("_")}`;
          hoverMarker(e, markerId);
          isHoveringMarker = true;
          break;
        }
      }
      if (!isHoveringMarker && currentHoveredMarker.current !== void 0) {
        blurMarker(e, currentHoveredMarker.current);
        hoverTrack(e, trackIndex);
      }
    }
  };
  const handleClick = (e) => {
    if (currentHoveredMarker.current !== void 0) {
      clickMarker(e, currentHoveredMarker.current);
      return;
    }
    if (!(0, import_lodash_es.isUndefined)(pinnedGroup)) {
      updateTooltipState(import_AnnotationsDataInitialState.initialState.tooltip);
      updateTooltipSelection(import_AnnotationsDataInitialState.initialState.selection);
      onMarkersInteraction?.(import_AnnotationsDataInitialState.initialState.selection);
    }
  };
  const handleMouseLeave = (e) => {
    if (currentHoveredTrack.current !== void 0) {
      blurTrack();
    }
    if (currentHoveredMarker.current !== void 0) {
      blurMarker(e, currentHoveredMarker.current);
    }
  };
  return {
    handleMouseMove,
    handleClick,
    handleMouseLeave
  };
};
