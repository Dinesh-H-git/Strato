import { AnnotationsMarkerGroup } from './annotations.js';
import type { Point } from '../../../types/point.js';
export interface AnnotationState {
    tooltip: TooltipState;
    selection: SelectionState;
}
export type TooltipState = {
    visibility: boolean;
    data?: AnnotationsMarkerGroup[];
    pinned: boolean;
    position?: Point;
    relativePosition: Point;
};
export interface SelectionState {
    data?: AnnotationsMarkerGroup[];
    /**
     * hover effect
     */
    hoveredGroup?: string;
    /**
     * pinned effect
     */
    pinnedGroup?: string;
    /**
     * emphasize tooltip item
     */
    hoveredItem?: string;
    /**
     * active/inactive markers within marker group
     */
    hoveredListItem?: string;
    /**
     * check the difference with hoveredGroup
     */
    hoverGroupListKey?: string;
}
/**
 * The annotations marker interactions' handler
 * @internal
 */
export type AnnotationsMarkerInteractionsHandler = ((event: Partial<SelectionState>) => void) | undefined;
