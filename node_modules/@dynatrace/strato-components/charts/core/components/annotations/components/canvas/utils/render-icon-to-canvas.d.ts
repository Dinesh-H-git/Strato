import { type ReactElement } from 'react';
export declare const renderIconContent: (ctx: CanvasRenderingContext2D, icon: ReactElement, iconImages: Map<ReactElement, HTMLImageElement>, x: number, y: number) => void;
