var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var RangeMarkerRenderer_exports = {};
__export(RangeMarkerRenderer_exports, {
  RangeMarkerRenderer: () => RangeMarkerRenderer
});
module.exports = __toCommonJS(RangeMarkerRenderer_exports);
var import_react = require("react");
var import_react_is = require("react-is");
var import_constants = require("./value-marker-renderer/constants.js");
var import_useCanvas = require("../../../../canvas/hooks/useCanvas.js");
var import_drawRoundRect = require("../../../../canvas/utils/drawRoundRect.js");
var import_constants2 = require("../../../constants.js");
var import_useAnnotationsIconImages = require("../../../hooks/useAnnotationsIconImages.js");
var import_constants3 = require("../constants.js");
var import_apply_marker_styles = require("../utils/apply-marker-styles.js");
var import_render_icon_to_canvas = require("../utils/render-icon-to-canvas.js");
var import_render_text_in_canvas = require("../utils/render-text-in-canvas.js");
const CONTENT_OFFSET_Y = 10;
const RangeMarkerRenderer = (props) => {
  const {
    height,
    width,
    x,
    y,
    fillColor,
    children,
    opacity = import_constants2.MARKER_OPACITY,
    brightness,
    strokeColor
  } = props;
  const iconImages = (0, import_useAnnotationsIconImages.useAnnotationsIconImages)();
  const draw = (0, import_react.useCallback)(
    (ctx) => {
      ctx.beginPath();
      const rectX = x;
      const rectY = y - import_constants2.MARKER_STROKE_WIDTH - import_constants3.CANVAS_PIXEL_OFFSET;
      const rectWidth = width;
      const rectHeight = height + import_constants2.MARKER_STROKE_WIDTH * 2 - import_constants3.CANVAS_PIXEL_OFFSET;
      (0, import_drawRoundRect.drawRoundRect)(
        ctx,
        rectX,
        rectY,
        rectWidth,
        rectHeight,
        import_constants2.MARKER_BORDER_RADIUS
      );
      (0, import_apply_marker_styles.applyMarkerStyles)(ctx, opacity, brightness, fillColor, strokeColor);
      ctx.closePath();
      if ((0, import_react_is.isElement)(children)) {
        const iconX = x + width / 2 - import_constants.MARKER_HALF_WIDTH + import_constants2.MARKER_ICON_OFFSET;
        const iconY = y + import_constants2.MARKER_ICON_OFFSET;
        (0, import_render_icon_to_canvas.renderIconContent)(ctx, children, iconImages, iconX, iconY);
      } else if (typeof children === "string") {
        const textX = x + width / 2;
        (0, import_render_text_in_canvas.renderTextContent)(
          ctx,
          children,
          textX,
          y + CONTENT_OFFSET_Y,
          "center",
          fillColor
        );
      }
      return false;
    },
    [
      x,
      y,
      width,
      height,
      opacity,
      brightness,
      fillColor,
      strokeColor,
      children,
      iconImages
    ]
  );
  (0, import_useCanvas.useCanvas)("annotations-range-marker", draw);
  return null;
};
RangeMarkerRenderer["displayName"] = "RangeMarkerRenderer";
