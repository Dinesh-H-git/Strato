import { ValueMarkerPointerPosition } from '../types.js';
export declare const calculateCenteredText: (x: number, width: number, orientation: ValueMarkerPointerPosition, contentWidth: number) => number;
