var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var filter_indicators_out_of_bounds_exports = {};
__export(filter_indicators_out_of_bounds_exports, {
  filterIndicatorsOutOfBounds: () => filterIndicatorsOutOfBounds
});
module.exports = __toCommonJS(filter_indicators_out_of_bounds_exports);
var import_is_indicator_partially_or_within_domain = require("./is-indicator-partially-or-within-domain.js");
var import_is_range_marker = require("../../../utils/is-range-marker.js");
function filterIndicatorsOutOfBounds(indicators, [domainStart, domainEnd]) {
  const filteredIndicators = [];
  indicators.forEach((track, index) => {
    filteredIndicators[index] = track.filter((indicator) => {
      const { start } = indicator;
      const indicatorStart = start.valueOf();
      if ((0, import_is_range_marker.isRangeMarker)(indicator)) {
        const { end } = indicator;
        const indicatorEnd = end.valueOf();
        return (0, import_is_indicator_partially_or_within_domain.isIndicatorPartiallyOrWithinDomain)(
          indicatorStart,
          indicatorEnd,
          domainStart,
          domainEnd
        );
      } else {
        return indicatorStart >= domainStart && indicatorStart <= domainEnd;
      }
    });
  });
  return filteredIndicators;
}
