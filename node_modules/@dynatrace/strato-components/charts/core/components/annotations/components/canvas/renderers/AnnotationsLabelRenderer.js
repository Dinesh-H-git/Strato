var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var AnnotationsLabelRenderer_exports = {};
__export(AnnotationsLabelRenderer_exports, {
  AnnotationsLabelRenderer: () => AnnotationsLabelRenderer
});
module.exports = __toCommonJS(AnnotationsLabelRenderer_exports);
var import_react = require("react");
var import_colors = __toESM(require("@dynatrace/strato-design-tokens/colors"));
var import_typography = __toESM(require("@dynatrace/strato-design-tokens/typography"));
var import_getTokenComputedStyle = require("../../../../../utils/getTokenComputedStyle.js");
var import_useCanvas = require("../../../../canvas/hooks/useCanvas.js");
var import_chars_font_width_map = require("../../../../text-ellipsis/chars-font-width-map.js");
var import_truncate_string = require("../../../../text-ellipsis/truncate-string.js");
const getLabelFont = () => {
  const fontSize = (0, import_getTokenComputedStyle.getTokenComputedStyle)(import_typography.default.Text.Small.Default.Size);
  const fontFamily = (0, import_getTokenComputedStyle.getTokenComputedStyle)(
    import_typography.default.Text.Small.Default.Family
  );
  const fontWeight = (0, import_getTokenComputedStyle.getTokenComputedStyle)(
    import_typography.default.Text.Small.Default.Weight
  );
  return [fontWeight, fontSize, fontFamily];
};
const AnnotationsLabelRenderer = (props) => {
  const {
    text: originalText,
    x,
    y,
    maxWidth,
    textAlign = "right",
    ellipsis
  } = props;
  const color = (0, import_getTokenComputedStyle.getTokenComputedStyle)(import_colors.default.Text.Neutral.Subdued);
  const [fontWeight, fontSize, fontFamily] = getLabelFont();
  const font = [fontWeight, fontSize, fontFamily].join(" ");
  const draw = (0, import_react.useCallback)(
    (ctx) => {
      ctx.font = font;
      const textWidth = (0, import_truncate_string.calculateWidth)(
        originalText,
        import_chars_font_width_map.charsFontWidth12pixelsSize400Map
      );
      const applyEllipsis = ellipsis && maxWidth && textWidth > maxWidth;
      const textToRender = applyEllipsis ? (0, import_truncate_string.truncateString)(
        originalText,
        maxWidth,
        import_chars_font_width_map.charsFontWidth12pixelsSize400Map,
        "end"
      ) : originalText;
      ctx.textAlign = textAlign;
      ctx.fillStyle = color;
      ctx.textBaseline = "middle";
      ctx.fillText(textToRender, x, y, maxWidth);
      return false;
    },
    [originalText, x, y, maxWidth]
  );
  (0, import_useCanvas.useCanvas)("annotations-label", draw);
  return null;
};
AnnotationsLabelRenderer["displayName"] = "AnnotationsLabelRenderer";
