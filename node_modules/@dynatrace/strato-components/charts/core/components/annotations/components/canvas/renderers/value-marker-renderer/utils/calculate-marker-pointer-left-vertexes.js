var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_marker_pointer_left_vertexes_exports = {};
__export(calculate_marker_pointer_left_vertexes_exports, {
  calculateLeftMarkerVertexes: () => calculateLeftMarkerVertexes
});
module.exports = __toCommonJS(calculate_marker_pointer_left_vertexes_exports);
var import_constants = require("../constants.js");
const calculateLeftMarkerVertexes = (x, y, width) => {
  const leftXValues = x;
  const rightXValues = x + width;
  const bottomYValues = y + import_constants.SQUARE_HEIGHT;
  const bottomLeftVertex = { x, y: bottomYValues + import_constants.TRIANGLE_HEIGHT };
  const topLeftVertex = { x: leftXValues, y };
  const topRightVertex = { x: rightXValues, y };
  const bottomRightVertex = { x: rightXValues, y: bottomYValues };
  const triangleBottomRightJoint = {
    x: x + import_constants.MARKER_HALF_WIDTH,
    y: bottomYValues
  };
  return [
    bottomLeftVertex,
    topLeftVertex,
    topRightVertex,
    bottomRightVertex,
    triangleBottomRightJoint
  ];
};
