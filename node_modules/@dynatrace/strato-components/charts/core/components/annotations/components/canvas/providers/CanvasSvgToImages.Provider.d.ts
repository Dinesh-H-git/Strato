import { type PropsWithChildren, type ReactElement } from 'react';
interface CanvasSvgToImagesProviderProps {
    icons: Array<[ReactElement, string]>;
}
export declare const CanvasSvgToImagesProvider: {
    ({ icons, children, }: PropsWithChildren<CanvasSvgToImagesProviderProps>): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
export {};
