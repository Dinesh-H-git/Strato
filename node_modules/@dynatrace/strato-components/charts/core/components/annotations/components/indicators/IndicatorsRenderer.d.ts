import type { ScaleLinear, ScaleLogarithmic, ScaleSymLog, ScaleTime } from 'd3-scale';
import { MarkerInteractionState } from '../../types/marker.js';
export interface IndicatorsRendererProps {
    start: Date | number;
    end?: Date | number;
    color: string;
    state: MarkerInteractionState;
    trackIndex: number;
    isInverted?: boolean;
    scale: ScaleTime<number, number> | ScaleLinear<number, number> | ScaleLogarithmic<number, number> | ScaleSymLog<number, number>;
}
export declare const IndicatorsRenderer: ({ start, end, color, state, trackIndex, scale, isInverted, }: IndicatorsRendererProps) => import("react/jsx-runtime").JSX.Element;
