var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var IndicatorsRenderer_exports = {};
__export(IndicatorsRenderer_exports, {
  IndicatorsRenderer: () => IndicatorsRenderer
});
module.exports = __toCommonJS(IndicatorsRenderer_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_AreaIndicator = require("./AreaIndicator.js");
var import_LineIndicator = require("./LineIndicator.js");
var import_useCurrentTheme = require("../../../../../../core/hooks/useCurrentTheme.js");
var import_BrightnessFilter = require("../../../BrightnessFilter.js");
var import_useGridPlotSize = require("../../../chart-grid/hooks/useGridPlotSize.js");
var import_get_marker_brightness_based_on_state = require("../../utils/get-marker-brightness-based-on-state.js");
var import_is_range_marker = require("../../utils/is-range-marker.js");
var import_serialize_marker_range = require("../../utils/serialize-marker-range.js");
const IndicatorsRenderer = ({
  start,
  end,
  color,
  state,
  trackIndex,
  scale,
  isInverted = false
}) => {
  const { height } = (0, import_useGridPlotSize.useGridPlotSize)();
  const theme = (0, import_useCurrentTheme.useCurrentTheme)();
  const slope = (0, import_get_marker_brightness_based_on_state.getMarkerBrightnessBasedOnState)(theme, state);
  let ShapeComponent;
  const brightnessFilterId = `brightness_${(0, import_serialize_marker_range.serializeMarkerRange)(
    {
      start: start.valueOf(),
      end: end ? end.valueOf() : void 0
    },
    trackIndex
  )}`;
  const filter = `url(#${brightnessFilterId})`;
  const range = { start, end };
  if ((0, import_is_range_marker.isRangeMarker)(range)) {
    const x0 = scale(isInverted ? range.end : range.start) ?? 0;
    const x1 = scale(isInverted ? range.start : range.end) ?? 0;
    const width = x1 - x0;
    ShapeComponent = (0, import_AreaIndicator.AreaIndicator)({
      x: x0,
      width,
      height,
      color,
      filter
    });
  } else {
    const x0 = scale(range.start) ?? 0;
    ShapeComponent = (0, import_LineIndicator.LineIndicator)({ x: x0, height, color, filter });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_BrightnessFilter.BrightnessFilter, { id: brightnessFilterId, slope }),
    ShapeComponent
  ] });
};
