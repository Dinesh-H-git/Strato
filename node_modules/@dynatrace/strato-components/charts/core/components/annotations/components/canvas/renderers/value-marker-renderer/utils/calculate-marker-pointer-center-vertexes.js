var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_marker_pointer_center_vertexes_exports = {};
__export(calculate_marker_pointer_center_vertexes_exports, {
  calculateCenterMarkerVertexes: () => calculateCenterMarkerVertexes
});
module.exports = __toCommonJS(calculate_marker_pointer_center_vertexes_exports);
var import_constants = require("../constants.js");
const calculateCenterMarkerVertexes = (x, y, width) => {
  const halfWidth = width / 2;
  const leftXValues = x - halfWidth;
  const rightXValues = x + halfWidth;
  const bottomYValues = y + import_constants.SQUARE_HEIGHT;
  const bottomCenterVertex = { x, y: bottomYValues + import_constants.TRIANGLE_HEIGHT };
  const triangleBottomLeftJoint = {
    x: x - import_constants.MARKER_HALF_WIDTH,
    y: bottomYValues
  };
  const topLeftVertex = { x: leftXValues, y };
  const topRightVertex = { x: rightXValues, y };
  const triangleBottomRightJoint = {
    x: x + import_constants.MARKER_HALF_WIDTH,
    y: bottomYValues
  };
  const shape = [
    bottomCenterVertex,
    triangleBottomLeftJoint,
    topLeftVertex,
    topRightVertex,
    triangleBottomRightJoint
  ];
  if (width !== import_constants.VALUE_MARKER_WIDTH) {
    const bottomLeftVertex = { x: leftXValues, y: bottomYValues };
    const bottomRightVertex = { x: rightXValues, y: bottomYValues };
    shape.splice(2, 0, bottomLeftVertex);
    shape.splice(5, 0, bottomRightVertex);
  }
  return shape;
};
