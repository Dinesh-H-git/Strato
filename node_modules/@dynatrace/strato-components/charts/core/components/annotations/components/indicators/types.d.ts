import type { AnnotationsMarkerConfig } from '../../types/annotations-marker-props.js';
import type { AnnotationsTrackConfig } from '../../types/annotations-track-props.js';
import type { AnnotationsMarkerGroup } from '../../types/annotations.js';
import type { MarkerInteractionState } from '../../types/marker.js';
import type { SelectionState } from '../../types/state.js';
/**
 * Defines the display behavior of marker's indicators.
 * - `'always'`: Always show indicators regardless of hover state.
 * - `'never'`: Never show indicators.
 * - `'auto'`: Show indicators when hovering over the respective marker.
 * @public
 */
export type IndicatorsDisplay = 'auto' | 'always' | 'never';
/**
 * @internal
 */
export interface IndicatorData {
    start: Date | number;
    end?: Date | number;
    color: string;
    state: MarkerInteractionState;
    priority?: number;
}
/**
 * @internal
 */
export type AnnotationsIntegrationTrackConfig = AnnotationsTrackConfig & {
    markers: AnnotationsIntegrationMarkerConfig[];
    indicatorsDisplay?: IndicatorsDisplay;
};
/**
 * @internal
 */
export type AnnotationsIntegrationMarkerConfig = AnnotationsMarkerConfig & {
    indicatorsDisplay?: IndicatorsDisplay;
};
/**
 * @internal
 */
export interface AnnotationsIntegrationMarkerGroup extends AnnotationsMarkerGroup {
    markers: AnnotationsIntegrationMarkerConfig[];
}
/**
 * @internal
 */
export interface AnnotationsIntegrationSelectionState extends SelectionState {
    data?: AnnotationsIntegrationMarkerGroup[];
}
