var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_shown_indicators_exports = {};
__export(get_shown_indicators_exports, {
  getShownIndicators: () => getShownIndicators
});
module.exports = __toCommonJS(get_shown_indicators_exports);
var import_constants = require("../../../constants.js");
var import_serialize_marker_range = require("../../../utils/serialize-marker-range.js");
var import_indicator_visibility_helpers = require("./indicator-visibility-helpers.js");
const getShownIndicators = (tracks, selection) => tracks.map(
  (currentTrack, trackIndex) => currentTrack.markers.reduce(
    (indicators, currentMarker) => {
      const {
        indicatorsDisplay: trackIndicatorsVisibility,
        color: trackColor
      } = currentTrack;
      const {
        color: markerColor,
        start: markerStart,
        end: markerEnd,
        indicatorsDisplay: markerIndicatorVisibility,
        priority
      } = currentMarker;
      if ((0, import_indicator_visibility_helpers.isAlwaysHidden)(markerIndicatorVisibility, trackIndicatorsVisibility)) {
        return indicators;
      }
      const markerId = (0, import_serialize_marker_range.serializeMarkerRange)(currentMarker, trackIndex);
      const displayIndicator = (0, import_indicator_visibility_helpers.isAlwaysShown)(
        markerIndicatorVisibility,
        trackIndicatorsVisibility
      ) || (0, import_indicator_visibility_helpers.isShownByInteraction)(
        selection.data,
        markerId,
        markerIndicatorVisibility
      );
      if (displayIndicator) {
        const color = markerColor ?? trackColor ?? import_constants.DEFAULT_MARKER_COLOR;
        const indicator = (0, import_indicator_visibility_helpers.buildIndicator)(
          color,
          selection,
          markerId,
          markerStart,
          markerEnd,
          priority
        );
        return [...indicators, indicator];
      }
      return indicators;
    },
    []
  ),
  []
);
