import { type PropsWithChildren } from 'react';
interface AnnotationsScrollWrapperProps {
    height: number | string;
    width: number | string;
}
export declare const AnnotationsScrollWrapper: ({ height, width, children, }: PropsWithChildren<AnnotationsScrollWrapperProps>) => import("react/jsx-runtime").JSX.Element;
export {};
