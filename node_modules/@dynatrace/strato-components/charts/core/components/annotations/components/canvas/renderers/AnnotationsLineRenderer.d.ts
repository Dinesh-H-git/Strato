import { Point } from '@visx/brush/lib/types';
interface AnnotationsLineRendererProps {
    source: Point;
    destination: Point;
    strokeColor: string;
}
export declare const AnnotationsLineRenderer: {
    (props: AnnotationsLineRendererProps): null;
    displayName: string;
};
export {};
