var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_marker_pointer_right_vertexes_exports = {};
__export(calculate_marker_pointer_right_vertexes_exports, {
  calculateRightMarkerVertexes: () => calculateRightMarkerVertexes
});
module.exports = __toCommonJS(calculate_marker_pointer_right_vertexes_exports);
var import_constants = require("../constants.js");
const calculateRightMarkerVertexes = (x, y, width) => {
  const leftXValues = x - width;
  const rightXValues = x;
  const bottomYValues = y + import_constants.SQUARE_HEIGHT;
  const bottomRightVertex = {
    x: rightXValues,
    y: bottomYValues + import_constants.TRIANGLE_HEIGHT
  };
  const triangleBottomLeftJoint = {
    x: rightXValues - import_constants.MARKER_HALF_WIDTH,
    y: bottomYValues
  };
  const bottomLeftVertex = { x: leftXValues, y: bottomYValues };
  const topLeftVertex = { x: leftXValues, y };
  const topRightVertex = { x: rightXValues, y };
  return [
    bottomRightVertex,
    triangleBottomLeftJoint,
    bottomLeftVertex,
    topLeftVertex,
    topRightVertex
  ];
};
