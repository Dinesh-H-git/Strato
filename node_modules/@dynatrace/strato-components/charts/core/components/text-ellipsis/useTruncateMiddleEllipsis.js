var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useTruncateMiddleEllipsis_exports = {};
__export(useTruncateMiddleEllipsis_exports, {
  useTruncateMiddleEllipsis: () => useTruncateMiddleEllipsis
});
module.exports = __toCommonJS(useTruncateMiddleEllipsis_exports);
var import_react = require("react");
var import_truncate_string = require("./truncate-string.js");
const useTruncateMiddleEllipsis = (text, charsFontWidthMap, containerRef) => {
  const [displayText, setDisplayText] = (0, import_react.useState)(text);
  (0, import_react.useEffect)(() => {
    const containerEl = containerRef.current;
    if (!containerEl) {
      return;
    }
    const checkOverflow = (availableWidth) => {
      const truncatedString = (0, import_truncate_string.truncateString)(
        text,
        availableWidth,
        charsFontWidthMap
      );
      setDisplayText(truncatedString);
    };
    const resizeObserver = new ResizeObserver((entries) => {
      const entry = entries[0];
      const containerWidth = entry.contentRect.width;
      requestAnimationFrame(() => checkOverflow(containerWidth));
    });
    resizeObserver.observe(containerEl);
    return () => {
      resizeObserver.disconnect();
    };
  }, [text, charsFontWidthMap]);
  return displayText;
};
