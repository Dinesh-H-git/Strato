import type { AnyNumberScale } from '../../../types/scales.js';
import type { ZoomBehavior } from '../../interactions-overlay/types.js';
export declare const getZoomStep: (currentScale: AnyNumberScale, initialDomain: [number, number], infiniteZoom: boolean | undefined, deltaStart: number, deltaEnd: number, behaviour?: ZoomBehavior | undefined) => {
    nextDomain: [number, number] | [Date, Date];
    zoomInEnabled: boolean;
    zoomOutEnabled: boolean;
};
