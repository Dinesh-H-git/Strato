var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var SynchronizedSelectionProvider_exports = {};
__export(SynchronizedSelectionProvider_exports, {
  SynchronizedSelectionProvider: () => SynchronizedSelectionProvider
});
module.exports = __toCommonJS(SynchronizedSelectionProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_SynchronizedSelectionContext = require("../contexts/SynchronizedSelectionContext.js");
const getSelectionValue = (position, scale) => {
  const value = position.relative.x;
  return scale.invert(value).valueOf();
};
const getSelectionValues = (selection, scale) => {
  const [start, end] = selection;
  return [getSelectionValue(start, scale), getSelectionValue(end, scale)];
};
const initialState = {
  disabled: false,
  selectedChartId: void 0,
  startX: void 0,
  endX: void 0
};
const SynchronizedSelectionProvider = ({
  children
}) => {
  const [state, setState] = (0, import_react.useState)(initialState);
  const { selectedChartId, startX, endX, disabled } = state;
  const resetSelectionState = () => {
    setState(initialState);
  };
  const updateSelectionState = (0, import_react.useCallback)(
    (props) => {
      if (!props) {
        resetSelectionState();
      } else {
        const { selection, chartId, scale } = props;
        const [startX2, endX2] = getSelectionValues(selection, scale);
        setState((prevState) => {
          return {
            ...prevState,
            startX: startX2,
            endX: endX2,
            selectedChartId: chartId
          };
        });
      }
    },
    []
  );
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_SynchronizedSelectionContext.SynchronizedSelectionContext.Provider,
    {
      value: {
        selectedChartId,
        startX,
        endX,
        disabled,
        updateSelectionState,
        resetSelectionState
      },
      children
    }
  );
};
