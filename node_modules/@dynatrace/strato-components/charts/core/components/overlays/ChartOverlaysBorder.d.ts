interface ChartOverlaysBorderProps {
    left?: boolean;
    right?: boolean;
    bottom?: boolean;
    top?: boolean;
}
/**
 * A component to render a mimic axis borders conditionally
 **/
export declare const ChartOverlaysBorder: ({ left, right, bottom, top, }: ChartOverlaysBorderProps) => import("react/jsx-runtime").JSX.Element;
export {};
