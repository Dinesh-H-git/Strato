/** @internal */
export interface TimeFrameHeaderProps {
    from: Date;
    to: Date;
    showDuration?: boolean;
    limit?: number;
    clampFutureToDateToNow?: boolean;
    className?: string;
}
/** @internal */
export declare const TimeFrameHeader: {
    ({ from, to, showDuration, limit, clampFutureToDateToNow, className, }: TimeFrameHeaderProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
