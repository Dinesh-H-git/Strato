import { type ReactNode } from 'react';
import type { SeriesActionsTemplate } from '../../../../types/series-actions-template.js';
/**
 * Props of the KeyValue tooltip template
 * @internal
 */
export interface KeyValueProps {
    /** Icon element */
    icon: ReactNode;
    /** Primary Atom name */
    name: string;
    /** Value content */
    value: string;
    /** Series action */
    actions?: () => SeriesActionsTemplate;
}
/**
 *  @internal
 *  Tooltip template
 * */
export declare const KeyValue: {
    ({ icon, name, value, actions }: KeyValueProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
