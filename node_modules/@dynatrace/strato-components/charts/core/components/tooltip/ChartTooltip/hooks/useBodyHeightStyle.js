var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useBodyHeightStyle_exports = {};
__export(useBodyHeightStyle_exports, {
  useBodyHeightStyle: () => useBodyHeightStyle
});
module.exports = __toCommonJS(useBodyHeightStyle_exports);
var import_lodash_es = require("lodash");
var import_react = require("react");
var import_constants = require("../../constants.js");
var import_ChartTooltipMetadataContext = require("../../context/ChartTooltipMetadataContext.js");
var import_TooltipHeader = require("../../section-components/TooltipHeader/TooltipHeader.js");
const useBodyHeightStyle = (totalSize, bodyItemsCount) => {
  const [headerElementRect, setHeaderElementRect] = (0, import_react.useState)();
  const { tooltipRef } = (0, import_ChartTooltipMetadataContext.useChartTooltipMetadata)();
  (0, import_react.useEffect)(() => {
    if (!tooltipRef?.current) {
      return;
    }
    const tooltipContent = tooltipRef.current?.children?.[0]?.children;
    const hasHeader = tooltipContent[0]?.className.includes(import_TooltipHeader.HEADER_SECTION_NAME);
    if (hasHeader) {
      setHeaderElementRect(tooltipContent[0].getBoundingClientRect());
    }
  }, [tooltipRef]);
  const isVirtualized = !(0, import_lodash_es.isUndefined)(totalSize) && !(0, import_lodash_es.isUndefined)(bodyItemsCount);
  return {
    height: isVirtualized ? totalSize || `${import_constants.ESTIMATED_ITEM_SIZE * bodyItemsCount}px` : "100%",
    maxHeight: `calc(${import_constants.CONTAINER_CSS_PROPS.maxHeight} - ${headerElementRect?.height ?? 0}px)`
  };
};
