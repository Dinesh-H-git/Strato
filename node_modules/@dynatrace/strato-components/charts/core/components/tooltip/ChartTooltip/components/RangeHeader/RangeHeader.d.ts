import type { Formatter } from '../../../../../types/formatter.js';
/** @internal */
export interface RangeHeaderProps {
    from: number;
    to: number;
    formatter: Formatter;
    absoluteRange?: boolean;
    className?: string;
}
/**
 * @internal
 */
export declare const RangeHeader: ({ from, to, formatter, absoluteRange, className, }: RangeHeaderProps) => import("react/jsx-runtime").JSX.Element;
