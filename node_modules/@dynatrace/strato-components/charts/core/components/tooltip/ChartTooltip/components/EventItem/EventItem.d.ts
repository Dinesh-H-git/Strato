import { type HTMLAttributes, type PropsWithChildren, type ReactElement, type ReactNode } from 'react';
import type { SeriesActionsTemplate } from '../../../../../types/series-actions-template.js';
import type { TooltipItemProps } from '../../../types/tooltip.js';
/** @internal */
export interface EvenItemProps extends HTMLAttributes<HTMLDivElement>, Pick<TooltipItemProps, 'showDivider'> {
    title?: string;
    description?: string;
    icon?: ReactNode | string;
    actions?: () => SeriesActionsTemplate;
    defaultActions?: ReactElement<PropsWithChildren>;
}
/** @internal */
export declare const EventItem: {
    ({ description, icon, title, actions, className, showDivider, ...rest }: EvenItemProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
