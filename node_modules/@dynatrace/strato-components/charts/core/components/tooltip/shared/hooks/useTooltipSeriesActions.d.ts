import type { SeriesActions } from '../../../../types/series-actions-template.js';
import { TooltipItem } from '../../../../types/tooltip.js';
export declare function findSeriesActions<T>(series: T[], getId: (series: T) => string, tooltipItem: TooltipItem): T | undefined;
/**
 * Return the series action template with the original series entity
 * @param series -
 * @param getId -
 */
export declare function useTooltipSeriesActions<T>(series: T[], getId: (series: T) => string, ...rest: unknown[]): SeriesActions<TooltipItem>;
