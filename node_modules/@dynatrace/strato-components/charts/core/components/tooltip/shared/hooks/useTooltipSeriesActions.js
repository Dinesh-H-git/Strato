var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useTooltipSeriesActions_exports = {};
__export(useTooltipSeriesActions_exports, {
  findSeriesActions: () => findSeriesActions,
  useTooltipSeriesActions: () => useTooltipSeriesActions
});
module.exports = __toCommonJS(useTooltipSeriesActions_exports);
var import_react = require("react");
var import_ChartSeriesAction = require("../../../series-action/ChartSeriesAction.js");
var import_ChartSeriesActionContext = require("../../../series-action/ChartSeriesActionContext.js");
function findSeriesActions(series, getId, tooltipItem) {
  return series.find(
    (tooltipSeries) => tooltipItem.id.includes(getId(tooltipSeries))
  );
}
function useTooltipSeriesActions(series, getId, ...rest) {
  const { chartSeriesActions } = (0, import_ChartSeriesActionContext.useSeriesActions)();
  return (selectedSeries) => {
    if (selectedSeries === void 0) {
      return void 0;
    }
    const actionSeries = findSeriesActions(series, getId, selectedSeries);
    if (actionSeries) {
      const actions = chartSeriesActions?.(actionSeries, ...rest);
      if (actions) {
        if (actions instanceof Promise || actions.type === import_ChartSeriesAction.ChartSeriesAction || actions.type === import_react.Fragment) {
          return actions;
        } else {
          throw new Error("Invalid type passed for ChartSeriesActions!");
        }
      }
    }
  };
}
