import { type ReactNode } from 'react';
type FunctionChildrenProps = {
    children: (() => ReactNode)[];
};
/**
 * Interactive chart tooltip header component.
 */
export declare const Body: {
    ({ children }: FunctionChildrenProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
export {};
