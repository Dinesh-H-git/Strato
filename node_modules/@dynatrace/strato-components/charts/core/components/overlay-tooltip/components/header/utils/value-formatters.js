var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var value_formatters_exports = {};
__export(value_formatters_exports, {
  defaultLabelFormatterStrategy: () => defaultLabelFormatterStrategy,
  emptyHeaderFormatter: () => emptyHeaderFormatter,
  formatValueRange: () => formatValueRange,
  getSingleValue: () => getSingleValue
});
module.exports = __toCommonJS(value_formatters_exports);
var import_lodash_es = require("lodash");
var import_format_date_range = require("./format-date-range.js");
var import_type_guards = require("./type-guards.js");
const getSingleValue = (value) => {
  const rawValue = Array.isArray(value) ? value[0] : value;
  return (0, import_lodash_es.isUndefined)(rawValue) ? "" : rawValue.toString();
};
const defaultLabelFormatterStrategy = (value, label) => {
  if ((0, import_type_guards.isSingleValue)(value)) {
    const returnValue = getSingleValue(value);
    return label ? `${label}: ${returnValue}` : returnValue.toString();
  }
  return formatValueRange(value);
};
const emptyHeaderFormatter = (_) => {
  return null;
};
const formatValueRange = (values) => {
  if ((0, import_type_guards.isNonEmptyRange)(values)) {
    let base = `${values[0]} - ${values[1]}`;
    if ((0, import_type_guards.isDateRange)(values)) {
      base = (0, import_format_date_range.formatDateRange)(values[0], values[1]);
    }
    return base;
  }
  if (!Array.isArray(values)) {
    return !(0, import_lodash_es.isUndefined)(values) ? defaultLabelFormatterStrategy(values) : "";
  }
  if (values.some((i) => !(0, import_lodash_es.isUndefined)(i))) {
    return defaultLabelFormatterStrategy(values[0] ?? values[1]);
  }
  return "";
};
