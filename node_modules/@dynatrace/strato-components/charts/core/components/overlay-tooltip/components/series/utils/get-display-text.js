var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_display_text_exports = {};
__export(get_display_text_exports, {
  getDisplayText: () => getDisplayText
});
module.exports = __toCommonJS(get_display_text_exports);
var import_is_valid_number = require("./is-valid-number.js");
var import_chars_font_width_map = require("../../../../text-ellipsis/chars-font-width-map.js");
var import_truncate_string = require("../../../../text-ellipsis/truncate-string.js");
var import_width_calculation = require("../../../utils/width-calculation.js");
const getDisplayText = (text, availableWidth, truncationMode) => {
  if (!text || text.trim() === "") {
    return text;
  }
  if (!(0, import_is_valid_number.isValidNumber)(availableWidth)) {
    return text;
  }
  const textWidth = (0, import_width_calculation.calculateTotalWidth400)(text);
  if (textWidth <= availableWidth) {
    return text;
  }
  return (0, import_truncate_string.truncateString)(
    text,
    availableWidth,
    import_chars_font_width_map.charsFontWidth12pixelsSize400Map,
    truncationMode
  );
};
