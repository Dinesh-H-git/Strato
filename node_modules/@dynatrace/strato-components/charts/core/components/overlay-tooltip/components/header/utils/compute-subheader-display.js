var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var compute_subheader_display_exports = {};
__export(compute_subheader_display_exports, {
  computeSubheaderDisplay: () => computeSubheaderDisplay
});
module.exports = __toCommonJS(compute_subheader_display_exports);
var import_get_value = require("./get-value.js");
var import_text_truncation_header = require("../../../utils/text-truncation-header.js");
const computeSubheaderDisplay = ({
  subheader,
  formatStrategy,
  availableWidth,
  truncationMode
}) => {
  const subheaderValue = (0, import_get_value.getValue)(subheader);
  const subheaderContent = subheaderValue ? formatStrategy(subheaderValue) : void 0;
  const shownSubheaderText = subheaderContent ? (0, import_text_truncation_header.getHeaderTruncatedText)(subheaderContent, availableWidth, truncationMode) : void 0;
  return {
    subheaderContent,
    shownSubheaderText,
    isSubheaderTruncated: !!subheaderContent && shownSubheaderText !== subheaderContent
  };
};
