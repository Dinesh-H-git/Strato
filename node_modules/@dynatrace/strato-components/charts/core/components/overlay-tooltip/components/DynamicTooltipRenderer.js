var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var DynamicTooltipRenderer_exports = {};
__export(DynamicTooltipRenderer_exports, {
  DynamicTooltipRenderer: () => DynamicTooltipRenderer
});
module.exports = __toCommonJS(DynamicTooltipRenderer_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_lodash_es = require("lodash");
var import_DynamicTooltip = require("./DynamicTooltip.js");
var import_LegacyTemplateTooltip = require("../components/LegacyTemplateTooltip.js");
var import_constants = require("../constants.js");
var import_ExternalConfigContext = require("../contexts/ExternalConfigContext.js");
var import_useDefaultConfig = require("../hooks/useDefaultConfig.js");
var import_selectors = require("../selectors.js");
var import_build_content = require("../utils/build-content.js");
var import_utils = require("../utils.js");
const DynamicTooltipRenderer = (props) => {
  (0, import_selectors.useSelector)(
    ({ absoluteX: absoluteX2, absoluteY: absoluteY2, inBounds: inBounds2, pinned: pinned2 }) => `${absoluteX2}-${absoluteY2}-${inBounds2}-${pinned2}`
  );
  const { absoluteX, absoluteY, inBounds, pinned, data } = (0, import_selectors.useTooltipSnapshot)();
  const defaultConfig = (0, import_useDefaultConfig.useDefaultConfig)();
  if (!inBounds || (0, import_lodash_es.isNull)(absoluteX) || (0, import_lodash_es.isNull)(absoluteY) || (0, import_utils.isTooltipDataEmpty)(data)) {
    return;
  }
  const { config, content } = (0, import_build_content.buildContent)(props.children, defaultConfig, {
    userSymbolPosition: props.symbolAlignment,
    userTruncationMode: props.truncation ?? import_constants.DEFAULT_TRUNCATION_MODE,
    customTooltip: !!props.legacyTemplate
  });
  if (props.legacyTemplate) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_ExternalConfigContext.ExternalConfigContext.Provider, { value: config, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      import_LegacyTemplateTooltip.LegacyTemplateTooltip,
      {
        pinned,
        absoluteX,
        absoluteY,
        children: props.legacyTemplate
      }
    ) });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_DynamicTooltip.DynamicTooltip, { pinned, absoluteX, absoluteY, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_ExternalConfigContext.ExternalConfigContext.Provider, { value: config, children: content }) });
};
