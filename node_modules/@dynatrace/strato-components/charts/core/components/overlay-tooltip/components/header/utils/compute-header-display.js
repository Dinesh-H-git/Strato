var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var compute_header_display_exports = {};
__export(compute_header_display_exports, {
  computeHeaderDisplay: () => computeHeaderDisplay
});
module.exports = __toCommonJS(compute_header_display_exports);
var import_get_duration_content = require("./get-duration-content.js");
var import_get_shown_header_text = require("./get-shown-header-text.js");
var import_get_value = require("./get-value.js");
var import_width_calculation = require("../../../utils/width-calculation.js");
const buildContent = (formattedValue, headerLabel) => {
  if (!formattedValue) {
    return "";
  }
  return headerLabel ? `${headerLabel}: ${formattedValue}` : formattedValue;
};
const computeHeaderDisplay = ({
  header,
  label,
  duration,
  formatStrategy,
  labelFor,
  isSingleLine,
  isTruncationEnd,
  availableWidth,
  showedLines,
  truncationMode,
  showRangeDuration
}) => {
  const value = (0, import_get_value.getValue)(header);
  const formattedValue = formatStrategy(value);
  const headerLabel = labelFor?.("headerLabel", label);
  const content = buildContent(formattedValue, headerLabel);
  const totalTextWidth = (0, import_width_calculation.calculateTotalWidth500)(content);
  const durationContent = duration ?? (0, import_get_duration_content.getDurationContent)(showRangeDuration, header);
  const durationSuffix = durationContent ? ` (${durationContent})` : "";
  const durationSuffixWidth = durationSuffix ? (0, import_width_calculation.calculateTotalWidth500)(durationSuffix) : 0;
  const isJsTruncation = isSingleLine || !isTruncationEnd;
  const effectiveWidth = isJsTruncation && durationSuffixWidth > 0 ? availableWidth - durationSuffixWidth : availableWidth;
  const shownText = (0, import_get_shown_header_text.getShownHeaderText)(
    content,
    isSingleLine,
    isTruncationEnd,
    effectiveWidth,
    showedLines,
    truncationMode
  );
  const isMultiLineCssClamped = !isSingleLine && isTruncationEnd && Math.ceil((totalTextWidth + durationSuffixWidth) / availableWidth) > showedLines;
  return {
    content,
    totalTextWidth,
    shownText,
    isTruncated: isMultiLineCssClamped || shownText !== content,
    durationContent,
    durationSuffix
  };
};
