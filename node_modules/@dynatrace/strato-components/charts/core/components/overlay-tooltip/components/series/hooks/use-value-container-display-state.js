var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_value_container_display_state_exports = {};
__export(use_value_container_display_state_exports, {
  useValueContainerDisplayText: () => useValueContainerDisplayText
});
module.exports = __toCommonJS(use_value_container_display_state_exports);
var import_react = require("react");
var import_constants = require("../../../constants.js");
var import_width_calculation = require("../../../utils/width-calculation.js");
var import_build_relative_value_text = require("../utils/build-relative-value-text.js");
var import_get_display_text = require("../utils/get-display-text.js");
const useValueContainerDisplayText = (value, valueUnit, relativeValue, relativeValueUnit, availableContainerWidth, truncationMode) => {
  return (0, import_react.useMemo)(() => {
    const relativeValueText = (0, import_build_relative_value_text.buildRelativeValueText)(
      relativeValue,
      relativeValueUnit
    );
    const valueUnitWithSpace = ` ${valueUnit}`;
    const fullValueText = `${value}${valueUnit ? valueUnitWithSpace : ""}`;
    if (!availableContainerWidth || availableContainerWidth <= 0) {
      const valueWidth2 = relativeValueText ? (0, import_width_calculation.calculateTotalWidth400)(fullValueText, relativeValueText) : (0, import_width_calculation.calculateTotalWidth400)(fullValueText);
      return {
        displayValueText: fullValueText,
        displayRelativeValueText: relativeValueText,
        actualValueWidth: valueWidth2,
        fullValueText,
        fullRelativeValueText: relativeValueText
      };
    }
    const maxValueContainerWidth = availableContainerWidth / 2 - import_constants.GAP_BETWEEN_COLUMNS;
    if (!relativeValueText) {
      const truncatedValueText = (0, import_get_display_text.getDisplayText)(
        fullValueText,
        maxValueContainerWidth,
        truncationMode
      );
      const actualValueWidth = (0, import_width_calculation.calculateTotalWidth400)(truncatedValueText);
      return {
        displayValueText: truncatedValueText,
        displayRelativeValueText: void 0,
        actualValueWidth,
        fullValueText,
        fullRelativeValueText: void 0
      };
    }
    const relativeValueWidth = (0, import_width_calculation.calculateTotalWidth400)(relativeValueText);
    const valueWidth = (0, import_width_calculation.calculateTotalWidth400)(fullValueText);
    const totalValueContainerWidth = (0, import_width_calculation.calculateTotalWidth400)(
      relativeValueText,
      fullValueText,
      " "
    );
    if (totalValueContainerWidth <= maxValueContainerWidth) {
      return {
        displayValueText: fullValueText,
        displayRelativeValueText: relativeValueText,
        actualValueWidth: totalValueContainerWidth,
        fullValueText,
        fullRelativeValueText: relativeValueText
      };
    }
    const maxRelativeValueWidth = Math.min(
      maxValueContainerWidth * 0.6,
      maxValueContainerWidth - import_constants.SPACE_CHARACTER_WIDTH
    );
    if (relativeValueWidth + import_constants.SPACE_CHARACTER_WIDTH >= maxRelativeValueWidth) {
      const truncatedRelativeValue = (0, import_get_display_text.getDisplayText)(
        relativeValueText,
        maxRelativeValueWidth,
        truncationMode
      );
      const actualRelativeValueWidth = (0, import_width_calculation.calculateTotalWidth400)(
        truncatedRelativeValue
      );
      const remainingSpaceForValue = maxValueContainerWidth - actualRelativeValueWidth - import_constants.SPACE_CHARACTER_WIDTH;
      const doesFitInAvailableSpace = valueWidth <= remainingSpaceForValue;
      const displayValueText = doesFitInAvailableSpace ? fullValueText : (0, import_get_display_text.getDisplayText)(fullValueText, remainingSpaceForValue, truncationMode);
      return {
        displayValueText,
        displayRelativeValueText: truncatedRelativeValue,
        actualValueWidth: (0, import_width_calculation.calculateTotalWidth400)(
          displayValueText,
          truncatedRelativeValue,
          " "
        ),
        fullValueText,
        fullRelativeValueText: relativeValueText
      };
    } else {
      const actualRelativeValueWidth = (0, import_width_calculation.calculateTotalWidth400)(relativeValueText);
      const remainingSpaceForValue = maxValueContainerWidth - actualRelativeValueWidth - import_constants.SPACE_CHARACTER_WIDTH;
      const doesFitInAvailableSpace = valueWidth <= remainingSpaceForValue;
      const displayValueText = doesFitInAvailableSpace ? fullValueText : (0, import_get_display_text.getDisplayText)(fullValueText, remainingSpaceForValue, truncationMode);
      return {
        displayValueText,
        displayRelativeValueText: relativeValueText,
        actualValueWidth: (0, import_width_calculation.calculateTotalWidth400)(
          displayValueText,
          relativeValueText,
          " "
        ),
        fullValueText,
        fullRelativeValueText: relativeValueText
      };
    }
  }, [
    value,
    valueUnit,
    relativeValue,
    relativeValueUnit,
    availableContainerWidth,
    truncationMode
  ]);
};
