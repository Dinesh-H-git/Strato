import type { HeaderData, TooltipValue } from '../../../types.js';
/**
 * Primary header data for the tooltip, containing the main value and an optional subheader.
 * @public
 */
export interface HeaderDataInformation {
    /** The primary value displayed in the tooltip header row. */
    header1?: HeaderData;
    header2?: HeaderData;
    subheader?: HeaderData;
}
/**
 * Extended header data that additionally supports a duration display and label override.
 * @public
 */
export interface HeaderDataWithDuration extends HeaderDataInformation {
    /** When `true`, renders the elapsed duration between the header range values. */
    showRangeDuration1?: boolean;
    /**
     * Controls whether duration should be computed and displayed for header2.
     * When true and header2 is a range, the computed duration (e.g., "10" for [5, 15])
     * will be shown in parentheses after the header2 text.
     * @defaultValue false
     */
    showRangeDuration2?: boolean;
    /** Optional label text shown above the header value. */
    label?: string;
    /**
     * Label to prepend to header2 content when formatting.
     * Formatted as "label: value" (e.g., "Y Axis: 20").
     */
    label2?: string;
    /** Maximum number of lines before the header text is truncated. */
    maxLines?: number;
    /** Preformatted duration string to display instead of the computed range duration. */
    duration1?: string;
    /**
     * Explicit duration string to display for header2.
     * Shown in parentheses (e.g., "(10min)").
     */
    duration2?: string;
}
/**
 * Props for the `ChartTooltip.Header` slot, controlling the tooltip header content.
 * @public
 */
export type HeaderProps = HeaderDataWithDuration;
export type HeaderValueFormatter = (value: TooltipValue | [TooltipValue, TooltipValue] | undefined) => string | undefined;
