var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_duration_content_exports = {};
__export(get_duration_content_exports, {
  getDurationContent: () => getDurationContent
});
module.exports = __toCommonJS(get_duration_content_exports);
var import_type_guards = require("./type-guards.js");
var import_resolution_formatter = require("../../../../tooltip/utils/resolution-formatter.js");
const DISPLAY_UNITS_LIMIT = 3;
const getDurationContent = (showRangeDuration, value) => {
  if (!showRangeDuration) {
    return null;
  }
  if ((0, import_type_guards.isDateRange)(value)) {
    return (0, import_resolution_formatter.formatResolution)(value[0], value[1], DISPLAY_UNITS_LIMIT);
  }
  if ((0, import_type_guards.isNumericRange)(value)) {
    return Math.abs(value[1] - value[0]);
  }
  return null;
};
