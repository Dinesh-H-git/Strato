var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_series_item_display_state_exports = {};
__export(use_series_item_display_state_exports, {
  useSeriesItemDisplayState: () => useSeriesItemDisplayState
});
module.exports = __toCommonJS(use_series_item_display_state_exports);
var import_dynamic = require("@vanilla-extract/dynamic");
var import_react = require("react");
var import_use_value_container_display_state = require("./use-value-container-display-state.js");
var import_constants = require("../../../constants.js");
var import_ExternalConfigContext = require("../../../contexts/ExternalConfigContext.js");
var import_tooltip_css = require("../../../tooltip.sty.js");
var import_width_calculation = require("../../../utils/width-calculation.js");
var import_format_series_name_for_display = require("../utils/format-series-name-for-display.js");
var import_get_display_name_text = require("../utils/get-display-name-text.js");
var import_get_display_text = require("../utils/get-display-text.js");
var import_get_is_truncated = require("../utils/get-is-truncated.js");
var import_get_name_text_style = require("../utils/get-name-text-style.js");
const resolveDisplayText = (text, shouldUseMultiLineCss, availableWidth, validatedMaxLines, truncationMode) => {
  if (shouldUseMultiLineCss) {
    if (truncationMode === "end") {
      return text;
    }
    const wrapWidth = availableWidth !== void 0 ? availableWidth * validatedMaxLines : void 0;
    return (0, import_get_display_text.getDisplayText)(text, wrapWidth, truncationMode) || "";
  }
  return (0, import_get_display_text.getDisplayText)(text, availableWidth, truncationMode) || "";
};
const calculateAvailableNameWidth = (maxWidth, actualValueWidth) => {
  if (!maxWidth) {
    return void 0;
  }
  const valueOffset = actualValueWidth > 0 ? actualValueWidth + 6 : 0;
  return maxWidth - valueOffset - import_constants.SYMBOL_AND_SPACING_WIDTH;
};
const calculateNameText = (name, multiLine, shouldUseMultiLineCss, availableNameWidth, truncationMode, validatedMaxLines) => {
  if (Array.isArray(name)) {
    if (multiLine) {
      return name.map(
        (item) => resolveDisplayText(
          item,
          shouldUseMultiLineCss,
          availableNameWidth,
          validatedMaxLines,
          truncationMode
        )
      );
    }
    return (0, import_format_series_name_for_display.formatSeriesNameForDisplay)(name);
  }
  return resolveDisplayText(
    name,
    shouldUseMultiLineCss,
    availableNameWidth,
    validatedMaxLines,
    truncationMode
  );
};
const useSeriesItemDisplayState = (props) => {
  const {
    name,
    description,
    value,
    valueUnit,
    relativeValue,
    relativeValueUnit,
    maxWidth,
    maxLines = import_constants.DEFAULT_SERIES_NAME_LINES,
    multiLine = false
  } = props;
  const { truncationMode } = (0, import_ExternalConfigContext.useExternalConfig)();
  const validatedMaxLines = Math.min(maxLines, import_constants.MAX_SERIES_NAME_LINES);
  const isSingleLine = validatedMaxLines === 1;
  const isTruncationEnd = truncationMode === "end";
  const {
    displayValueText,
    displayRelativeValueText,
    actualValueWidth,
    fullValueText,
    fullRelativeValueText
  } = (0, import_use_value_container_display_state.useValueContainerDisplayText)(
    value?.toString(),
    valueUnit,
    relativeValue,
    relativeValueUnit,
    maxWidth,
    truncationMode
  );
  const availableNameWidth = calculateAvailableNameWidth(
    maxWidth,
    actualValueWidth
  );
  const shouldUseMultiLineCss = !isSingleLine;
  const nameText = calculateNameText(
    name,
    multiLine,
    shouldUseMultiLineCss,
    availableNameWidth,
    truncationMode,
    validatedMaxLines
  );
  const multiLineMode = shouldUseMultiLineCss && truncationMode !== "end" ? "wrap" : "clamp";
  const displayNameText = (0, import_get_display_name_text.getDisplayNameText)(name, multiLine);
  const totalTextWidth = (0, import_react.useMemo)(() => {
    return Array.isArray(displayNameText) ? (0, import_width_calculation.calculateTotalWidth400)(...displayNameText) : (0, import_width_calculation.calculateTotalWidth400)(displayNameText);
  }, [displayNameText]);
  const isMultiLineCssClamped = isTruncationEnd && shouldUseMultiLineCss && !!availableNameWidth && Math.ceil(totalTextWidth / availableNameWidth) > validatedMaxLines;
  const isTruncated = (0, import_get_is_truncated.getIsTruncated)(
    isMultiLineCssClamped,
    name,
    multiLine,
    nameText,
    displayNameText
  );
  const nameTextStyle = (0, import_get_name_text_style.getNameTextStyle)(
    isSingleLine,
    multiLine,
    multiLineMode
  );
  const nameStyle = shouldUseMultiLineCss ? (0, import_dynamic.assignInlineVars)({ [import_tooltip_css.seriesNameMaxLinesVar]: String(validatedMaxLines) }) : {};
  const descriptionText = typeof description === "string" && description.length > 0 ? resolveDisplayText(
    description,
    shouldUseMultiLineCss,
    availableNameWidth,
    validatedMaxLines,
    truncationMode
  ) : void 0;
  const isDescriptionTruncated = typeof description === "string" && descriptionText !== description;
  return {
    nameText,
    displayNameText,
    descriptionText,
    fullDescriptionText: description,
    isDescriptionTruncated,
    descriptionShouldUseMultiLineCss: shouldUseMultiLineCss,
    descriptionMultiLineMode: multiLineMode,
    displayValueText,
    displayRelativeValueText,
    fullValueText,
    fullRelativeValueText,
    shouldUseMultiLineCss,
    nameTextStyle,
    nameStyle,
    isTruncated,
    actualValueWidth
  };
};
