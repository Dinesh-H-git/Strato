var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useDefaultLabels_exports = {};
__export(useDefaultLabels_exports, {
  useDefaultLabels: () => useDefaultLabels
});
module.exports = __toCommonJS(useDefaultLabels_exports);
var import_lodash_es = require("lodash");
var import_react_intl = require("react-intl");
var import_defaults = require("../defaults.js");
var import_selectors = require("../selectors.js");
var import_utils = require("../utils.js");
const useDefaultLabels = () => {
  const data = (0, import_selectors.useTooltipSnapshot)().data;
  const intl = (0, import_react_intl.useIntl)();
  if ((0, import_utils.isTooltipDataEmpty)(data)) {
    return null;
  }
  const defaults = import_defaults.DEFAULTS[data.geometry];
  return (key, label) => {
    if ((0, import_lodash_es.isNil)(label)) {
      return defaults[key] && intl.formatMessage(defaults[key]);
    }
    return label;
  };
};
