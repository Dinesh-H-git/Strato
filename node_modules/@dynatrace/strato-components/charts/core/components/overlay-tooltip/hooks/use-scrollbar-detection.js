var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_scrollbar_detection_exports = {};
__export(use_scrollbar_detection_exports, {
  useScrollbarDetection: () => useScrollbarDetection
});
module.exports = __toCommonJS(use_scrollbar_detection_exports);
var import_react = require("react");
var import_needs_scrollbar_fix = require("../utils/needs-scrollbar-fix.js");
const useScrollbarDetection = () => {
  const checkScrollbar = (0, import_react.useCallback)((element) => {
    const hasVerticalScrollbar = element.scrollHeight > element.clientHeight;
    const needsFix = hasVerticalScrollbar && (0, import_needs_scrollbar_fix.needsScrollbarFix)();
    element.style.setProperty("--needs-scrollbar-fix", needsFix ? "1" : "0");
  }, []);
  const refCallback = (0, import_react.useCallback)(
    (element) => {
      if (!element) {
        return;
      }
      checkScrollbar(element);
    },
    [checkScrollbar]
  );
  return refCallback;
};
