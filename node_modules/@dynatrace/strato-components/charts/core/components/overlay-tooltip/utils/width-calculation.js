var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var width_calculation_exports = {};
__export(width_calculation_exports, {
  calculateTotalWidth400: () => calculateTotalWidth400,
  calculateTotalWidth500: () => calculateTotalWidth500
});
module.exports = __toCommonJS(width_calculation_exports);
var import_chars_font_width_map = require("../../text-ellipsis/chars-font-width-map.js");
var import_truncate_string = require("../../text-ellipsis/truncate-string.js");
const calculateTotalWidth400 = (...texts) => {
  let totalWidth = 0;
  for (const text of texts) {
    if (text) {
      const width = (0, import_truncate_string.calculateWidth)(text, import_chars_font_width_map.charsFontWidth12pixelsSize400Map);
      totalWidth += isFinite(width) ? width : 0;
    }
  }
  return totalWidth;
};
const calculateTotalWidth500 = (...texts) => {
  let totalWidth = 0;
  for (const text of texts) {
    if (text) {
      const width = (0, import_truncate_string.calculateWidth)(text, import_chars_font_width_map.charsFontWidth12pixelsSize500Map);
      totalWidth += isFinite(width) ? width : 0;
    }
  }
  return totalWidth;
};
