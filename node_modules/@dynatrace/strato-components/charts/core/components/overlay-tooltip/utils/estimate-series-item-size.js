var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var estimate_series_item_size_exports = {};
__export(estimate_series_item_size_exports, {
  estimateSeriesItemSize: () => estimateSeriesItemSize
});
module.exports = __toCommonJS(estimate_series_item_size_exports);
var import_chars_font_width_map = require("../../text-ellipsis/chars-font-width-map.js");
var import_truncate_string = require("../../text-ellipsis/truncate-string.js");
var import_constants = require("../constants.js");
const estimateSeriesItemSize = (item, maxContainerWidth) => {
  const baseHeight = 24;
  const multiLine = "multiLine" in item ? item.multiLine : false;
  const maxLines = "maxLines" in item ? item.maxLines : import_constants.DEFAULT_SERIES_NAME_LINES;
  const containerWidth = Math.max(maxContainerWidth, import_constants.MIN_TOOLTIP_WIDTH) - (import_constants.SYMBOL_AND_SPACING_WIDTH + import_constants.TOOLTIP_WRAPPER_PADDING);
  let lineCount = 1;
  if (Array.isArray(item.name)) {
    lineCount = item.name.length;
  } else if (typeof item.name === "string" && multiLine) {
    const textWidth = (0, import_truncate_string.calculateWidth)(
      item.name,
      import_chars_font_width_map.charsFontWidth12pixelsSize400Map
    );
    if (textWidth > containerWidth) {
      lineCount = Math.min(Math.ceil(textWidth / containerWidth), maxLines);
    }
  }
  let descriptionLineCount = 0;
  const description = "description" in item ? item.description : void 0;
  if (typeof description === "string" && description.length > 0) {
    descriptionLineCount = 1;
    if (multiLine) {
      const descriptionWidth = (0, import_truncate_string.calculateWidth)(
        description,
        import_chars_font_width_map.charsFontWidth12pixelsSize400Map
      );
      if (descriptionWidth > containerWidth) {
        descriptionLineCount = Math.min(
          Math.ceil(descriptionWidth / containerWidth),
          maxLines
        );
      }
    }
  }
  return baseHeight + (lineCount + descriptionLineCount - 1) * 16;
};
