var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var mouseInteractions_exports = {};
__export(mouseInteractions_exports, {
  exploreMouseClickOrTap: () => exploreMouseClickOrTap,
  exploreMouseHover: () => exploreMouseHover,
  exploreMouseWheel: () => exploreMouseWheel
});
module.exports = __toCommonJS(mouseInteractions_exports);
var import_lodash_es = require("lodash");
const exploreMouseHover = ({
  event,
  pinned,
  selectionArea,
  lastMousePositionRef,
  setCursorMode,
  onHover
}) => {
  const canExplore = !pinned && (0, import_lodash_es.isUndefined)(selectionArea);
  const { position } = event;
  if (canExplore) {
    onHover?.({ triggeredBy: "pointer", position });
  }
  lastMousePositionRef.current = position;
  setCursorMode("explore");
};
const exploreMouseClickOrTap = ({
  event,
  selectionArea,
  onHover,
  onTogglePin,
  pinned,
  onSelectEnd,
  lastMousePositionRef
}) => {
  const { position } = event;
  if ((0, import_lodash_es.isUndefined)(selectionArea)) {
    onHover?.({
      triggeredBy: "pointer",
      position
    });
    onTogglePin?.({
      triggeredBy: "pointer",
      pin: !pinned,
      position
    });
  } else {
    onSelectEnd?.({ cancelled: true });
    onHover?.({
      triggeredBy: "pointer",
      position
    });
  }
  lastMousePositionRef.current = position;
};
const exploreMouseWheel = ({
  event,
  onPanChange
}) => {
  const isTemporaryPanMode = (event2) => event2.altKey;
  if (isTemporaryPanMode(event)) {
    const horizontal = event.deltaX && event.deltaX !== 0;
    if (horizontal) {
      onPanChange?.({ triggeredBy: "pointer", delta: event.deltaX });
    }
  }
};
