import type { MutableRefObject } from 'react';
import type { RelativeAbsolutePosition } from '../../../../types/pointer-events.js';
import type { InteractionTriggers, ZoomBehavior } from '../../types.js';
export declare const zoomKeyboardEscape: ({ selectionArea, eventCancelled, onSelectEnd, lastMousePositionRef, pinned, onHover, onTogglePin, }: {
    pinned: boolean;
    eventCancelled: MutableRefObject<boolean>;
    lastMousePositionRef: MutableRefObject<RelativeAbsolutePosition | undefined>;
    selectionArea?: [RelativeAbsolutePosition, RelativeAbsolutePosition];
    onHover?: (event: {
        triggeredBy: InteractionTriggers;
        position: RelativeAbsolutePosition;
    }) => void;
    onTogglePin?: (event: {
        triggeredBy: InteractionTriggers;
        position: RelativeAbsolutePosition | undefined;
        pin: boolean;
    }) => void;
    onSelectEnd?: (event: {
        triggeredBy: InteractionTriggers;
        start: RelativeAbsolutePosition;
        end: RelativeAbsolutePosition;
    } | {
        cancelled: true;
    }) => void;
}) => void;
export declare const zoomKeyboardEnter: ({ selectionArea, lastMousePositionRef, width, onSelectEnd, onZoomChange, onHover, }: {
    lastMousePositionRef: MutableRefObject<RelativeAbsolutePosition | undefined>;
    width: number;
    selectionArea?: [RelativeAbsolutePosition, RelativeAbsolutePosition];
    onSelectEnd?: (event: {
        triggeredBy: InteractionTriggers;
        start: RelativeAbsolutePosition;
        end: RelativeAbsolutePosition;
    } | {
        cancelled: true;
    }) => void;
    onZoomChange?: (event: {
        triggeredBy: InteractionTriggers;
        deltaStart: number;
        deltaEnd: number;
        behavior: ZoomBehavior;
    }) => void;
    onHover?: (event: {
        triggeredBy: InteractionTriggers;
        position: RelativeAbsolutePosition;
    }) => void;
}) => void;
export declare const zoomKeyboardUp: ({ width, onZoomChange, }: {
    width: number;
    onZoomChange?: (event: {
        triggeredBy: InteractionTriggers;
        deltaStart: number;
        deltaEnd: number;
        behavior: ZoomBehavior;
    }) => void;
}) => void;
export declare const zoomKeyboardDown: ({ width, onZoomChange, }: {
    width: number;
    onZoomChange?: (event: {
        triggeredBy: InteractionTriggers;
        deltaStart: number;
        deltaEnd: number;
        behavior: ZoomBehavior;
    }) => void;
}) => void;
