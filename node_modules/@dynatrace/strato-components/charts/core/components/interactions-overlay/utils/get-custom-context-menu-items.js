var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_custom_context_menu_items_exports = {};
__export(get_custom_context_menu_items_exports, {
  getCustomContextMenuItems: () => getCustomContextMenuItems
});
module.exports = __toCommonJS(get_custom_context_menu_items_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_is_slot = require("../../../../../core/utils/is-slot.js");
var import_OverlayTooltip = require("../../overlay-tooltip/OverlayTooltip.js");
var import_ChartSeriesAction = require("../../series-action/ChartSeriesAction.js");
const renderActionItem = (item) => {
  const itemProps = {
    disabled: item.props.disabled,
    onSelect: item.props.onSelect,
    label: "",
    icon: ""
  };
  import_react.Children.forEach(item.props.children, (itemPart) => {
    if ((0, import_is_slot.isSlot)(itemPart, import_ChartSeriesAction.ChartSeriesAction.ItemIcon)) {
      itemProps.icon = itemPart.props.children;
    } else {
      itemProps.label = itemPart;
    }
  });
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_OverlayTooltip.OverlayTooltip.Action,
    {
      label: itemProps.label,
      icon: itemProps.icon,
      onSelect: itemProps.onSelect,
      disabled: itemProps.disabled
    }
  );
};
const getCustomContextMenuItems = (customItem) => {
  let actions = [];
  let intents = [];
  import_react.Children.forEach(customItem, (action) => {
    if ((0, import_is_slot.isSlot)(action, import_ChartSeriesAction.ChartSeriesAction)) {
      const { actions: recursiveActions, intents: recursiveIntents } = getCustomContextMenuItems(action.props.children);
      actions = [...actions, ...recursiveActions];
      intents = [...intents, ...recursiveIntents];
    } else if ((0, import_is_slot.isSlot)(action, import_ChartSeriesAction.ChartSeriesAction.Item)) {
      actions = [...actions, renderActionItem(action)];
    } else if ((0, import_is_slot.isSlot)(action, import_ChartSeriesAction.ChartSeriesAction.Intent)) {
      intents = [...intents, action];
    }
  });
  return { actions, intents };
};
