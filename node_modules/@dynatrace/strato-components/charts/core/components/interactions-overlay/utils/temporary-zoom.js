var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var temporary_zoom_exports = {};
__export(temporary_zoom_exports, {
  temporaryZoom: () => temporaryZoom
});
module.exports = __toCommonJS(temporary_zoom_exports);
var import_calculate_mouse_positions = require("../../../utils/calculate-mouse-positions.js");
var import_constants = require("../constants.js");
const temporaryZoom = ({
  event,
  lastMousePositionRef,
  width,
  onZoomChange
}) => {
  const vertical = event.deltaY && event.deltaY !== 0;
  const wheelPosition = (0, import_calculate_mouse_positions.calculateMousePositions)(event, event.currentTarget);
  lastMousePositionRef.current = wheelPosition;
  if (vertical) {
    const wheelUp = event.deltaY < 0;
    const delta = wheelUp ? Math.min(event.deltaY, -import_constants.MIN_ZOOM_STEP) : Math.max(event.deltaY, import_constants.MIN_ZOOM_STEP);
    const ratio = wheelPosition.relative.x / width;
    const deltaStart = -delta * ratio;
    const deltaEnd = delta * (1 - ratio);
    onZoomChange?.({
      triggeredBy: "pointer",
      deltaStart,
      deltaEnd,
      behavior: wheelUp ? "zoom-in" : "zoom-out"
    });
  }
};
