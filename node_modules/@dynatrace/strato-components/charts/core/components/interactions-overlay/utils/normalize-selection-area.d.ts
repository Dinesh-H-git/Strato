import type { RelativeAbsolutePosition } from '../../../types/pointer-events.js';
/**
 * Takes care of reordering the selection area if needed, ensuring that start is always before end
 * @param positionA -
 * @param positionB -
 */
export declare const normalizeSelectionArea: ([positionA, positionB]: [RelativeAbsolutePosition, RelativeAbsolutePosition]) => [RelativeAbsolutePosition, RelativeAbsolutePosition];
