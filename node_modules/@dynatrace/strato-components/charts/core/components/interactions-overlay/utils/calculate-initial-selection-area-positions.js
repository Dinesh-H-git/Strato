var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_initial_selection_area_positions_exports = {};
__export(calculate_initial_selection_area_positions_exports, {
  calculateInitialSelectionAreaPositions: () => calculateInitialSelectionAreaPositions
});
module.exports = __toCommonJS(calculate_initial_selection_area_positions_exports);
var import_calculate_center_positions = require("./calculate-center-positions.js");
const calculateInitialSelectionAreaPositions = (bounds) => {
  const { height, left, top, width } = bounds;
  const areaWidth = width / 3;
  const middlePosition = (0, import_calculate_center_positions.calculateCenterPositions)(width, height, top, left);
  const selectionStart = {
    relative: {
      x: middlePosition.relative.x - areaWidth / 2,
      y: middlePosition.relative.y
    },
    absolute: {
      x: middlePosition.absolute.x - areaWidth / 2,
      y: middlePosition.absolute.y
    }
  };
  const selectionEnd = {
    relative: {
      x: middlePosition.relative.x + areaWidth / 2,
      y: middlePosition.relative.y
    },
    absolute: {
      x: middlePosition.absolute.x + areaWidth / 2,
      y: middlePosition.absolute.y
    }
  };
  return [selectionStart, selectionEnd];
};
