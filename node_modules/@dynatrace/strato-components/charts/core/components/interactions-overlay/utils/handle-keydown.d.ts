import type { KeyboardEvent, MutableRefObject } from 'react';
import type { RelativeAbsolutePosition } from '../../../types/pointer-events.js';
import type { InteractionsHandlers, InteractionsOverlayProps } from '../types.js';
type KeyboardInteractions = Pick<InteractionsOverlayProps, 'selectionArea' | 'onSelectEnd' | 'onModeChange'> & {
    lastMousePositionRef: MutableRefObject<RelativeAbsolutePosition | undefined>;
    panDeltaRef: MutableRefObject<number | undefined>;
    eventCancelled: MutableRefObject<boolean>;
    interaction: InteractionsHandlers;
    event: KeyboardEvent;
};
export declare const handleKeyDown: ({ event, selectionArea, eventCancelled, onSelectEnd, onModeChange, interaction, panDeltaRef, lastMousePositionRef, }: KeyboardInteractions) => void;
export {};
