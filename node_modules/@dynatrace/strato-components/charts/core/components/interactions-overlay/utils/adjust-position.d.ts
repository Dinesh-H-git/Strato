import type { RelativeAbsolutePosition } from '../../../types/pointer-events.js';
export declare const adjustPosition: (position: RelativeAbsolutePosition, delta: number) => RelativeAbsolutePosition;
