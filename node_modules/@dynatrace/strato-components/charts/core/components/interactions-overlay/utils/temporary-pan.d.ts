import type { InteractionTriggers } from '../types.js';
export declare const temporaryPan: ({ deltaX, onPanChange, }: {
    deltaX: number;
    onPanChange?: (event: {
        triggeredBy: InteractionTriggers;
        delta: number;
    }) => void;
}) => void;
