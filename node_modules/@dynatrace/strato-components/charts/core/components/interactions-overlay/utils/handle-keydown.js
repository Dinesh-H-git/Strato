var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var handle_keydown_exports = {};
__export(handle_keydown_exports, {
  handleKeyDown: () => handleKeyDown
});
module.exports = __toCommonJS(handle_keydown_exports);
var import_lodash_es = require("lodash");
var import_constants = require("../constants.js");
var import_reset = require("./reset.js");
const isTemporaryZoomMode = (event) => event.metaKey || event.ctrlKey;
const handleKeyDown = ({
  event,
  selectionArea,
  eventCancelled,
  onSelectEnd,
  onModeChange,
  interaction,
  panDeltaRef,
  lastMousePositionRef
}) => {
  switch (event.key) {
    case import_constants.KeyboardControls.KEYBOARD_EXPLORE_MODE_SHORTCUT:
    case import_constants.KeyboardControls.KEYBOARD_EXPLORE_MODE_SHORTCUT_ALTERNATIVE: {
      (0, import_reset.reset)(lastMousePositionRef, panDeltaRef);
      if (!(0, import_lodash_es.isUndefined)(selectionArea)) {
        eventCancelled.current = true;
        onSelectEnd?.({ cancelled: true });
      }
      onModeChange?.({ mode: "explore", keyboard: true });
      break;
    }
    case import_constants.KeyboardControls.KEYBOARD_PAN_MODE_SHORTCUT:
    case import_constants.KeyboardControls.KEYBOARD_PAN_MODE_SHORTCUT_ALTERNATIVE: {
      (0, import_reset.reset)(lastMousePositionRef, panDeltaRef);
      onModeChange?.({ mode: "pan", keyboard: true });
      break;
    }
    case import_constants.KeyboardControls.KEYBOARD_ZOOM_MODE_SHORTCUT:
    case import_constants.KeyboardControls.KEYBOARD_ZOOM_MODE_SHORTCUT_ALTERNATIVE: {
      (0, import_reset.reset)(lastMousePositionRef, panDeltaRef);
      if (!(0, import_lodash_es.isUndefined)(selectionArea)) {
        eventCancelled.current = true;
        onSelectEnd?.({ cancelled: true });
      }
      onModeChange?.({ mode: "zoom", keyboard: true });
      break;
    }
    case import_constants.KeyboardControls.KEYBOARD_RESET_SHORTCUT:
    case import_constants.KeyboardControls.KEYBOARD_RESET_SHORTCUT_ALTERNATIVE:
      interaction.onDoubleClickOrTap("keyboard");
      break;
    case import_constants.KeyboardControls.UP_ARROW: {
      event.preventDefault();
      interaction.onKeyUp(isTemporaryZoomMode(event));
      break;
    }
    case import_constants.KeyboardControls.DOWN_ARROW: {
      event.preventDefault();
      interaction.onKeyDown(isTemporaryZoomMode(event));
      break;
    }
    case import_constants.KeyboardControls.LEFT_ARROW: {
      interaction.onKeyLeft(event.shiftKey);
      break;
    }
    case import_constants.KeyboardControls.RIGHT_ARROW: {
      interaction.onKeyRight(event.shiftKey);
      break;
    }
    case import_constants.KeyboardControls.ENTER: {
      if (!(0, import_lodash_es.isUndefined)(selectionArea)) {
        interaction.onKeyEnter();
      }
      break;
    }
    case import_constants.KeyboardControls.ESCAPE: {
      interaction.onKeyEscape();
      (0, import_reset.reset)(lastMousePositionRef, panDeltaRef);
      break;
    }
  }
};
