var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useChartActionsMenu_exports = {};
__export(useChartActionsMenu_exports, {
  useChartActionsMenu: () => useChartActionsMenu
});
module.exports = __toCommonJS(useChartActionsMenu_exports);
var import_lodash_es = require("lodash");
var import_react = require("react");
var import_is_mouse_outside_element = require("../../../utils/is-mouse-outside-element.js");
const DEBOUNCE_TIME_MS = 5;
function useChartActionsMenu() {
  const [selectedItem, setSelectedItem] = (0, import_react.useState)("");
  const [isScrolling, setIsScrolling] = (0, import_react.useState)(false);
  const actionsMenuRef = (0, import_react.useRef)(null);
  const onLeave = (event) => {
    if (!event || (0, import_is_mouse_outside_element.isMouseOutsideElement)(event, actionsMenuRef)) {
      setSelectedItem("");
    }
  };
  const onScrollEnd = (0, import_lodash_es.debounce)(() => setIsScrolling(false), DEBOUNCE_TIME_MS);
  const onScroll = (event) => {
    setSelectedItem("");
    setIsScrolling(true);
    onScrollEnd();
  };
  const onUpdateSelectedItem = (itemId) => {
    if (selectedItem !== itemId) {
      setSelectedItem(itemId);
    }
  };
  return {
    selectedItem,
    isScrolling,
    onLeave,
    onScroll,
    updateSelectedItem: onUpdateSelectedItem,
    actionsMenuRef
  };
}
