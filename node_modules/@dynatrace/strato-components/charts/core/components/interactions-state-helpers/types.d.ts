import type { RelativeAbsolutePosition } from '../../types/pointer-events.js';
import type { InteractionMode } from '../interactions-overlay/types.js';
import type { SelectionAreaVariant } from '../selection-area/types.js';
export interface ExploreState {
    visible: boolean;
    pinned: boolean;
    position?: RelativeAbsolutePosition;
}
export interface ZoomState {
    zoomInEnabled: boolean;
    zoomOutEnabled: boolean;
}
export interface ToolbarState {
    visible: boolean;
}
export interface SelectState {
    start: RelativeAbsolutePosition;
    end: RelativeAbsolutePosition;
    finished: boolean;
    accessible: boolean;
    variant: SelectionAreaVariant;
    selectionPercentage?: [number, number];
    isKeyboardCreated?: boolean;
}
export type HighlightedSeriesId = string | undefined;
export interface InteractionState<T = unknown> {
    highlightedSeriesId: HighlightedSeriesId;
    currentDomain: T | undefined;
    interactionMode: InteractionMode;
    explore: ExploreState;
    zoom: ZoomState;
    select: SelectState | undefined;
}
