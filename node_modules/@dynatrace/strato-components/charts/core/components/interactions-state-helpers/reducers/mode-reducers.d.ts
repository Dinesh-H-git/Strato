import type { InteractionMode } from '../../interactions-overlay/types.js';
import { SelectionAreaVariant } from '../../selection-area/types.js';
import type { InteractionState } from '../types.js';
export declare function reduceInteractionMode<T extends InteractionState>(interactionMode: InteractionMode): (prev: T) => T;
export declare function reduceSelectionAreaVariant<T extends InteractionState>(variant: SelectionAreaVariant): (prev: T) => T;
