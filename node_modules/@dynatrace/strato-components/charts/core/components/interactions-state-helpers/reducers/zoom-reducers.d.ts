import type { InteractionState } from '../types.js';
export declare function reduceZoomDomain<T extends InteractionState>(domain: T['currentDomain'], zoomInEnabled: boolean, zoomOutEnabled: boolean): (prev: T) => T;
