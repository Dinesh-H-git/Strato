import { type ReactElement } from 'react';
import { ColorToken } from '../../../../types/color-palette.js';
/**
 * @internal
 */
export type LegendItem = {
    id: string;
    color: ColorToken;
    name: string;
    icon?: ReactElement;
};
export type ChartIcon = 'line' | 'area' | 'bar' | 'band';
