var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useLegendItemInteractions_exports = {};
__export(useLegendItemInteractions_exports, {
  useLegendItemInteractions: () => useLegendItemInteractions
});
module.exports = __toCommonJS(useLegendItemInteractions_exports);
const useLegendItemInteractions = (item, state, interactionsDisabled) => {
  const {
    updateVisible,
    updateHoveredState,
    onLegendIconClick,
    onLegendNameClick,
    onItemHover,
    visible
  } = state;
  const onIconButtonClick = () => {
    if (!interactionsDisabled) {
      updateVisible(!visible);
      onLegendIconClick(item);
    }
  };
  const onNameButtonClick = (event) => {
    if (!interactionsDisabled) {
      onLegendNameClick(item, event);
      updateVisible(true);
      handleLabelMouseEnter();
    }
  };
  const onNameButtonKeyDown = (event) => {
    if (event.code === "Enter") {
      event.preventDefault();
      const { ctrlKey, metaKey, shiftKey } = event;
      onNameButtonClick(
        new MouseEvent("click", { ctrlKey, metaKey, shiftKey })
      );
    }
  };
  const handleMouseEnter = () => {
    if (!interactionsDisabled) {
      onItemHover(visible ? item : void 0);
      updateHoveredState(true);
    }
  };
  const handleLabelMouseEnter = () => {
    handleMouseEnter();
  };
  const handleIconMouseEnter = () => {
    handleMouseEnter();
  };
  const handleMouseLeave = () => {
    onItemHover(void 0);
    updateHoveredState(false);
  };
  return {
    handleLabelMouseEnter,
    handleIconMouseEnter,
    handleMouseLeave,
    onNameButtonClick,
    onNameButtonKeyDown,
    onIconButtonClick
  };
};
