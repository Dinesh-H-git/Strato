var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useLegendItemState_exports = {};
__export(useLegendItemState_exports, {
  useLegendItemState: () => useLegendItemState
});
module.exports = __toCommonJS(useLegendItemState_exports);
var import_react = require("react");
var import_useMouseHover = require("../../../../hooks/useMouseHover.js");
const useLegendItemState = (item, interactionsDisabled, hiddenItems, numberOfItems) => {
  const refIconButton = (0, import_react.useRef)(null);
  const refNameButton = (0, import_react.useRef)(null);
  const [open, setOpen] = (0, import_react.useState)(false);
  const [isHovered, setIsHovered] = (0, import_react.useState)(false);
  const isItemHidden = hiddenItems.has(item.id);
  const [visible, setVisible] = (0, import_react.useState)(!isItemHidden);
  const shouldShowActionsButton = open || isHovered;
  const isIconHovered = (0, import_useMouseHover.useMouseHover)(refIconButton) && !interactionsDisabled && isHovered;
  const isLastSelectedItem = hiddenItems.size === numberOfItems - 1 && !isItemHidden;
  (0, import_react.useEffect)(() => setVisible(!isItemHidden), [isItemHidden]);
  return {
    refIconButton,
    refNameButton,
    open,
    shouldShowActionsButton,
    isIconHovered,
    visible,
    isLastItem: isLastSelectedItem,
    updateVisible: setVisible,
    updateOpenState: setOpen,
    updateHoveredState: setIsHovered
  };
};
