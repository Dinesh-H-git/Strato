import { LegendItem } from '../types/legend.js';
interface LegendInteractionsCallbacks {
    onHover?: (_: LegendItem | undefined) => void;
    onClick?: (_: LegendItem[]) => void;
}
export declare const useLegendInteractions: (items: LegendItem[], callbacks: LegendInteractionsCallbacks) => {
    onLegendIconClick: (item: LegendItem) => void;
    onItemHover: (item?: LegendItem) => void;
    onLegendNameClick: (item: LegendItem, event: MouseEvent) => void;
    hiddenItems: Set<string>;
};
export {};
