import type { LegendItem } from './legend.js';
import type { TruncationMode } from '../../../../../../typography/text-ellipsis/TextEllipsis.js';
import type { SeriesActions } from '../../../../types/series-actions-template.js';
export type LegendState = {
    hiddenItems: Set<string>;
    truncationMode: TruncationMode;
    onLegendIconClick(item: LegendItem): void;
    onItemHover(item: LegendItem | undefined, isIconHovered?: boolean): void;
    onLegendNameClick(item: LegendItem, event: MouseEvent): void;
    interactionsDisabled: boolean;
    onSeriesAction?: SeriesActions<LegendItem>;
};
/**
 * Settings for the grid legend layout
 */
export interface GridSettings {
    maxRows: number;
    maxColumns: number;
    canFitAllItemsWithoutScrollbar: boolean;
    hasVirtual: boolean;
    minItemWidth: number;
    maxItemWidth: number;
    longestSeries: number;
}
