import { GridSettings } from '../types/legend-internal.js';
import { LegendItem } from '../types/legend.js';
export declare const getMinMaxSeriesWidth: (items: LegendItem[]) => {
    shortestSeries: number;
    longestSeries: number;
};
export declare const buildGridSettings: (items: LegendItem[], width: number, height: number) => GridSettings;
