var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_legend_scale_exports = {};
__export(get_legend_scale_exports, {
  getLegendScale: () => getLegendScale
});
module.exports = __toCommonJS(get_legend_scale_exports);
var import_d3_scale = require("d3-scale");
var import_get_min_max_values = require("./get-min-max-values.js");
const getLegendScale = (items, containerSize, invertScale = false, min, max) => {
  const { max: parsedMax, min: parsedMin } = (0, import_get_min_max_values.getMinMaxValues)(items, min, max);
  const range = [0, containerSize];
  const domain = !invertScale ? [parsedMin, parsedMax] : [parsedMax, parsedMin];
  return (0, import_d3_scale.scaleLinear)().domain(domain).range(range).clamp(true);
};
