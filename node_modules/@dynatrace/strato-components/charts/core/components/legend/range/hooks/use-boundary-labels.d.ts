import { InternalRangeItem } from '../types/range-legend.internal.js';
export declare const useBoundaryLabels: (items: InternalRangeItem[], rangeStartPosition: number, rangeEndPosition: number, min?: number, max?: number) => {
    firstLabel: string;
    lastLabel: string;
};
