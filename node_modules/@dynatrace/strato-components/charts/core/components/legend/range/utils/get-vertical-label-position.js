var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_vertical_label_position_exports = {};
__export(get_vertical_label_position_exports, {
  getVerticalLabelPosition: () => getVerticalLabelPosition
});
module.exports = __toCommonJS(get_vertical_label_position_exports);
var import_constants = require("../constants.js");
const DEFAULT_POSITION_X_VERTICAL = 12;
const getVerticalLabelPosition = (labelHeight, itemStartPosition, itemEndPosition, alignment) => {
  const labelPositionX = DEFAULT_POSITION_X_VERTICAL;
  switch (alignment) {
    case import_constants.LABEL_ALIGNMENT.Start:
      return {
        x: labelPositionX,
        y: Math.floor(itemStartPosition + labelHeight)
      };
    case import_constants.LABEL_ALIGNMENT.End: {
      return {
        x: labelPositionX,
        y: itemEndPosition
      };
    }
    case import_constants.LABEL_ALIGNMENT.Center: {
      return {
        x: labelPositionX,
        y: Math.floor(itemEndPosition + labelHeight / 2)
      };
    }
  }
};
