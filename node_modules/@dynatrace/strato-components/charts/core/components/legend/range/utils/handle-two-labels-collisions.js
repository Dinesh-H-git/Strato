var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var handle_two_labels_collisions_exports = {};
__export(handle_two_labels_collisions_exports, {
  handleTwoLabelsCollisions: () => handleTwoLabelsCollisions
});
module.exports = __toCommonJS(handle_two_labels_collisions_exports);
var import_constants = require("../constants.js");
var import_labels_collision = require("./labels-collision.js");
const handleTwoLabelsCollisions = (firstLabel, lastLabel, truncationMode, bounds) => {
  const { startLabel, endLabel } = (0, import_labels_collision.getLabelsPosition)(
    firstLabel,
    lastLabel,
    bounds
  );
  let iteration = 0;
  let startLabelEndPosition = startLabel.end;
  let endLabelStartPosition = endLabel.start;
  const isTruncationNeeded = startLabelEndPosition >= endLabelStartPosition - import_constants.GAP_BETWEEN_LABELS;
  if (!isTruncationNeeded) {
    return { startLabel: firstLabel, endLabel: lastLabel };
  }
  let candidateStartLabel = firstLabel;
  let candidateEndLabel = lastLabel;
  do {
    candidateStartLabel = (0, import_labels_collision.truncateLabel)(candidateStartLabel, truncationMode);
    if (!candidateStartLabel.includes(import_constants.TEXT_ELLIPSIS)) {
      break;
    }
    candidateEndLabel = (0, import_labels_collision.truncateLabel)(candidateEndLabel, truncationMode);
    if (!candidateEndLabel.includes(import_constants.TEXT_ELLIPSIS)) {
      break;
    }
    const { startLabel: newStartLabel, endLabel: newEndLabel } = (0, import_labels_collision.getLabelsPosition)(candidateStartLabel, candidateEndLabel, bounds);
    startLabelEndPosition = newStartLabel.end;
    endLabelStartPosition = newEndLabel.start;
    iteration++;
    if (iteration >= Math.max(firstLabel.length, lastLabel.length)) {
      break;
    }
  } while (startLabelEndPosition + import_constants.GAP_BETWEEN_LABELS >= endLabelStartPosition);
  return {
    startLabel: candidateStartLabel,
    endLabel: candidateEndLabel
  };
};
