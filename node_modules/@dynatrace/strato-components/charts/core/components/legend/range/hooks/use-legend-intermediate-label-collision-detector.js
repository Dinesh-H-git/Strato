var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_legend_intermediate_label_collision_detector_exports = {};
__export(use_legend_intermediate_label_collision_detector_exports, {
  useLegendIntermediateLabelCollisionDetector: () => useLegendIntermediateLabelCollisionDetector
});
module.exports = __toCommonJS(use_legend_intermediate_label_collision_detector_exports);
var import_react = require("react");
var import_constants = require("../../../../constants.js");
var import_LegendSizeContext = require("../../shared/context/LegendSizeContext.js");
var import_constants2 = require("../constants.js");
var import_RangeLegendOrientationContext = require("../context/RangeLegendOrientationContext.js");
var import_RangeLegendScaleContext = require("../context/RangeLegendScaleContext.js");
var import_RangeLegendTruncationContext = require("../context/RangeLegendTruncationContext.js");
var import_handle_collision_in_horizontal_layout = require("../utils/handle-collision-in-horizontal-layout.js");
var import_handle_collision_in_vertical_layout = require("../utils/handle-collision-in-vertical-layout.js");
const useLegendIntermediateLabelCollisionDetector = (labelText, segmentStartPosition, segmentEndPosition, alignment, skipCollisionDetector) => {
  const [label, setLabel] = (0, import_react.useState)({ text: labelText });
  const truncationMode = (0, import_RangeLegendTruncationContext.useRangeLegendTruncation)();
  const { orientation } = (0, import_RangeLegendOrientationContext.useOrientation)();
  const { scale } = (0, import_RangeLegendScaleContext.useScale)();
  const { width: legendWidth, height: legendHeight } = (0, import_LegendSizeContext.useLegendSize)();
  const containerLegendHeight = legendHeight - import_constants2.CONTAINER_PADDING * 2;
  const containerLegendWidth = legendWidth - import_constants2.CONTAINER_PADDING * 2;
  const width = Math.min(containerLegendWidth, import_constants2.MAX_LEGEND_WIDTH);
  const height = Math.max(containerLegendHeight, import_constants2.MAX_LEGEND_HEIGHT);
  (0, import_react.useEffect)(() => {
    if (skipCollisionDetector || width <= 0 || height <= 0) {
      return;
    }
    const labelPosition = {
      start: segmentStartPosition,
      end: segmentEndPosition
    };
    if (orientation === import_constants.ORIENTATION.Horizontal) {
      const newLabel = (0, import_handle_collision_in_horizontal_layout.handleCollisionInHorizontalLayout)(
        labelText,
        labelPosition,
        alignment,
        truncationMode,
        scale.range()
      );
      setLabel(newLabel);
    } else {
      const newLabel = (0, import_handle_collision_in_vertical_layout.handleCollisionInVerticalLayout)(
        labelText,
        labelPosition,
        alignment,
        truncationMode,
        scale.range()
      );
      setLabel(newLabel);
    }
  }, [
    width,
    height,
    alignment,
    segmentStartPosition,
    segmentEndPosition,
    skipCollisionDetector,
    orientation,
    labelText,
    truncationMode,
    scale
  ]);
  return label;
};
