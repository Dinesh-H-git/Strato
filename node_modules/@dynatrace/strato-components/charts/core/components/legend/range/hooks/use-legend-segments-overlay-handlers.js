var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_legend_segments_overlay_handlers_exports = {};
__export(use_legend_segments_overlay_handlers_exports, {
  useLegendSegmentsOverlayHandlers: () => useLegendSegmentsOverlayHandlers
});
module.exports = __toCommonJS(use_legend_segments_overlay_handlers_exports);
var import_constants = require("../../../../constants.js");
var import_useChartLayoutLoading = require("../../../chart-layout/hooks/useChartLayoutLoading.js");
var import_RangeLegendOrientationContext = require("../context/RangeLegendOrientationContext.js");
var import_RangeLegendScaleContext = require("../context/RangeLegendScaleContext.js");
var import_use_set_hovered_marker_id = require("../hooks/store/use-set-hovered-marker-id.js");
const distance = (point, pointToCheck) => Math.sqrt(Math.pow(point - pointToCheck, 2));
const useLegendSegmentsOverlayHandlers = (itemMarkers) => {
  const { setHoveredMarkerId } = (0, import_use_set_hovered_marker_id.useSetHoveredMarkerId)();
  const { scale } = (0, import_RangeLegendScaleContext.useScale)();
  const { orientation } = (0, import_RangeLegendOrientationContext.useOrientation)();
  const isLoading = (0, import_useChartLayoutLoading.useChartLayoutLoading)();
  const isHorizontal = orientation === import_constants.ORIENTATION.Horizontal;
  const markersWithPosition = itemMarkers.map((marker) => ({
    marker,
    position: scale(marker)
  }));
  const onMouseMoveHandler = (event) => {
    const { clientX, clientY } = event;
    const { left, top } = event.currentTarget.getBoundingClientRect();
    const mousePosition = isHorizontal ? clientX - left : clientY - top;
    if (!isLoading) {
      const closest = markersWithPosition.reduce(
        (acc, cur) => distance(acc.position, mousePosition) < distance(cur.position, mousePosition) ? acc : cur,
        markersWithPosition[0]
      );
      setHoveredMarkerId(closest.marker);
    }
  };
  const onMouseLeaveHandler = () => setHoveredMarkerId(null);
  return {
    onMouseMoveHandler,
    onMouseLeaveHandler
  };
};
