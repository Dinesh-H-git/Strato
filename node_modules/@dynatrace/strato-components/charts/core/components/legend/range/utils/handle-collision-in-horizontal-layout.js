var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var handle_collision_in_horizontal_layout_exports = {};
__export(handle_collision_in_horizontal_layout_exports, {
  handleCollisionInHorizontalLayout: () => handleCollisionInHorizontalLayout
});
module.exports = __toCommonJS(handle_collision_in_horizontal_layout_exports);
var import_get_horizontal_label_position = require("./get-horizontal-label-position.js");
var import_labels_collision = require("./labels-collision.js");
var import_chars_font_width_map = require("../../../text-ellipsis/chars-font-width-map.js");
var import_truncate_string = require("../../../text-ellipsis/truncate-string.js");
const handleCollisionInHorizontalLayout = (labelText, segment, alignment, truncationMode, bounds) => {
  const labelWidth = (0, import_truncate_string.calculateWidth)(
    labelText,
    import_chars_font_width_map.charsFontWidth12pixelsSize500Map
  );
  const { x: labelX, y: labelY } = (0, import_get_horizontal_label_position.getHorizontalLabelPosition)(
    labelWidth,
    segment.start,
    segment.end,
    alignment
  );
  const isTruncationNeeded = bounds[0] + labelWidth > bounds[1];
  if (!isTruncationNeeded) {
    const candidateStartPos = labelX;
    const candidateEndPos = labelX + labelWidth;
    return {
      text: labelText,
      x: (0, import_labels_collision.calculateOptimalPlacement)(
        candidateStartPos,
        candidateEndPos,
        bounds[0],
        bounds[1]
      ),
      y: labelY
    };
  } else {
    const { text, x } = (0, import_labels_collision.calculateOptimalEllipsis)(
      labelText,
      labelX,
      labelX + labelWidth,
      truncationMode,
      bounds[0],
      bounds[1]
    );
    return { text, x, y: labelY };
  }
};
