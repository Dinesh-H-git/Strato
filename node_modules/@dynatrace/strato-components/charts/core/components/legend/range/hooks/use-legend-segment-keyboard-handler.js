var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_legend_segment_keyboard_handler_exports = {};
__export(use_legend_segment_keyboard_handler_exports, {
  useLegendSegmentKeyboardHandler: () => useLegendSegmentKeyboardHandler
});
module.exports = __toCommonJS(use_legend_segment_keyboard_handler_exports);
var import_react = require("react");
var import_use_set_hovered_marker_id = require("./store/use-set-hovered-marker-id.js");
var import_constants = require("../../../../constants.js");
var import_RangeLegendOrientationContext = require("../context/RangeLegendOrientationContext.js");
const useLegendSegmentKeyboardHandler = (rectEl, itemFrom, itemTo, startPosition) => {
  const isFirstSegment = startPosition === 0;
  const hoveredMarkerId = (0, import_react.useRef)(isFirstSegment ? itemFrom : itemTo);
  const { setHoveredMarkerId } = (0, import_use_set_hovered_marker_id.useSetHoveredMarkerId)();
  const { orientation } = (0, import_RangeLegendOrientationContext.useOrientation)();
  const moveFocusForward = (0, import_react.useCallback)(() => {
    rectEl?.parentElement?.nextElementSibling?.querySelector("rect")?.focus();
    rectEl?.blur();
  }, [rectEl]);
  const moveFocusBackward = (0, import_react.useCallback)(() => {
    rectEl?.parentElement?.previousElementSibling?.querySelector("rect")?.focus();
    rectEl?.blur();
  }, [rectEl]);
  const movePositionForward = (0, import_react.useCallback)(() => {
    hoveredMarkerId.current = itemTo;
    setHoveredMarkerId(hoveredMarkerId.current);
  }, [itemTo, setHoveredMarkerId]);
  const movePositionBackward = (0, import_react.useCallback)(() => {
    hoveredMarkerId.current = itemFrom;
    setHoveredMarkerId(hoveredMarkerId.current);
  }, [itemFrom, setHoveredMarkerId]);
  const KEYDOWN_HANDLERS = (0, import_react.useMemo)(
    () => ({
      [import_constants.ORIENTATION.Horizontal]: {
        ArrowRight: () => {
          const isFirstPosition = hoveredMarkerId.current === itemFrom && isFirstSegment;
          if (isFirstPosition) {
            movePositionForward();
          } else {
            moveFocusForward();
          }
        },
        ArrowLeft: () => {
          const isSecondPosition = hoveredMarkerId.current === itemTo && isFirstSegment;
          if (isSecondPosition) {
            movePositionBackward();
          } else {
            moveFocusBackward();
          }
        }
      },
      [import_constants.ORIENTATION.Vertical]: {
        ArrowDown: () => {
          const isFirstPosition = hoveredMarkerId.current === itemFrom && isFirstSegment;
          if (isFirstPosition) {
            movePositionForward();
          } else {
            moveFocusForward();
          }
        },
        ArrowUp: () => {
          const isSecondPosition = hoveredMarkerId.current === itemTo && isFirstSegment;
          if (isSecondPosition) {
            movePositionBackward();
          } else {
            moveFocusBackward();
          }
        }
      }
    }),
    [
      isFirstSegment,
      itemFrom,
      itemTo,
      moveFocusBackward,
      moveFocusForward,
      movePositionBackward,
      movePositionForward
    ]
  );
  const onFocusHandler = (0, import_react.useCallback)(
    () => setHoveredMarkerId(hoveredMarkerId.current),
    [setHoveredMarkerId]
  );
  const onBlurHandler = (0, import_react.useCallback)(
    () => setHoveredMarkerId(null),
    [setHoveredMarkerId]
  );
  const onKeyDownHandler = (0, import_react.useCallback)(
    (event) => {
      if (event.key === "Tab") {
        const isFirstPosition = hoveredMarkerId.current === itemFrom && isFirstSegment;
        if (isFirstPosition) {
          event.preventDefault();
          hoveredMarkerId.current = itemTo;
          setHoveredMarkerId(hoveredMarkerId.current);
        }
      }
      if (event.key in KEYDOWN_HANDLERS[orientation]) {
        KEYDOWN_HANDLERS[orientation][event.key]();
      }
    },
    [
      KEYDOWN_HANDLERS,
      isFirstSegment,
      itemFrom,
      itemTo,
      orientation,
      setHoveredMarkerId
    ]
  );
  (0, import_react.useLayoutEffect)(() => {
    if (isFirstSegment) {
      const parentSvg = rectEl?.closest("svg");
      if (parentSvg) {
        const eventListenerCallback = (event) => {
          event.preventDefault();
          hoveredMarkerId.current = itemFrom;
        };
        parentSvg.addEventListener("blur", eventListenerCallback);
        return () => parentSvg.removeEventListener("blur", eventListenerCallback);
      }
    }
  }, [isFirstSegment, itemFrom, rectEl]);
  return {
    onBlurHandler,
    onFocusHandler,
    onKeyDownHandler
  };
};
