var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_internal_range_items_exports = {};
__export(get_internal_range_items_exports, {
  getInternalRangeItems: () => getInternalRangeItems
});
module.exports = __toCommonJS(get_internal_range_items_exports);
var import_is_single_range_handler = require("./is-single-range-handler.js");
const getInternalRangeItems = (items) => {
  const isSingleRange = (0, import_is_single_range_handler.isSingleRangeHandler)(items);
  return items.map((item) => {
    let { from: itemFrom, to: itemTo } = item;
    if (isSingleRange) {
      const isInfiniteRange = itemFrom === -Infinity && itemTo === Infinity;
      const isSinglePointRange = itemFrom === itemTo;
      if (isInfiniteRange) {
        itemFrom = 0;
        itemTo = 1;
      }
      if (isSinglePointRange) {
        itemTo = itemFrom + 1;
      }
    }
    return {
      ...item,
      from: itemFrom,
      to: itemTo,
      labels: { from: item.from.toString(), to: item.to.toString() }
    };
  });
};
