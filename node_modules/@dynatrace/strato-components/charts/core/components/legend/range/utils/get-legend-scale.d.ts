import { ScaleLinear } from 'd3-scale';
import { type InternalRangeItem } from '../types/range-legend.internal.js';
export declare const getLegendScale: (items: InternalRangeItem[], containerSize: number, invertScale?: boolean, min?: number, max?: number) => ScaleLinear<number, number>;
