var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_boundary_labels_exports = {};
__export(use_boundary_labels_exports, {
  useBoundaryLabels: () => useBoundaryLabels
});
module.exports = __toCommonJS(use_boundary_labels_exports);
var import_lodash_es = require("lodash");
var import_constants = require("../../../../constants.js");
var import_FormatterContext = require("../context/FormatterContext.js");
var import_RangeLegendOrientationContext = require("../context/RangeLegendOrientationContext.js");
var import_RangeLegendScaleContext = require("../context/RangeLegendScaleContext.js");
const useBoundaryLabels = (items, rangeStartPosition, rangeEndPosition, min, max) => {
  const { scale } = (0, import_RangeLegendScaleContext.useScale)();
  const { formatter } = (0, import_FormatterContext.useFormatter)();
  const { orientation } = (0, import_RangeLegendOrientationContext.useOrientation)();
  const isReversed = orientation === import_constants.ORIENTATION.Vertical;
  const firstItem = items.find(
    (item) => scale(isReversed ? item.to : item.from) === rangeStartPosition
  );
  const lastItem = items.find(
    (item) => scale(isReversed ? item.from : item.to) === rangeEndPosition
  );
  const firstItemValue = firstItem?.labels[isReversed ? "to" : "from"] ?? "";
  const lastItemValue = lastItem?.labels[isReversed ? "from" : "to"] ?? "";
  if (isReversed) {
    const formattedFirstValue2 = (0, import_lodash_es.isNumber)(max) ? formatter(max) : firstItemValue;
    const formattedLastValue2 = (0, import_lodash_es.isNumber)(min) ? formatter(min) : lastItemValue;
    return {
      firstLabel: formattedFirstValue2,
      lastLabel: formattedLastValue2
    };
  }
  const formattedFirstValue = (0, import_lodash_es.isNumber)(min) ? formatter(min) : firstItemValue;
  const formattedLastValue = (0, import_lodash_es.isNumber)(max) ? formatter(max) : lastItemValue;
  return {
    firstLabel: formattedFirstValue,
    lastLabel: formattedLastValue
  };
};
