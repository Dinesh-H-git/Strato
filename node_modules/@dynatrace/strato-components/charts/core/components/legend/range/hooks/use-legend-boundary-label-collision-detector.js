var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_legend_boundary_label_collision_detector_exports = {};
__export(use_legend_boundary_label_collision_detector_exports, {
  useLegendBoundaryLabelCollisionDetector: () => useLegendBoundaryLabelCollisionDetector
});
module.exports = __toCommonJS(use_legend_boundary_label_collision_detector_exports);
var import_react = require("react");
var import_constants = require("../../../../constants.js");
var import_LegendSizeContext = require("../../shared/context/LegendSizeContext.js");
var import_constants2 = require("../constants.js");
var import_RangeLegendOrientationContext = require("../context/RangeLegendOrientationContext.js");
var import_RangeLegendTruncationContext = require("../context/RangeLegendTruncationContext.js");
var import_handle_collision_in_vertical_layout = require("../utils/handle-collision-in-vertical-layout.js");
var import_handle_two_labels_collisions = require("../utils/handle-two-labels-collisions.js");
var import_labels_collision = require("../utils/labels-collision.js");
const useLegendBoundaryLabelCollisionDetector = (startLabel, endLabel) => {
  const [labels, setLabels] = (0, import_react.useState)({ startLabel, endLabel });
  const truncationMode = (0, import_RangeLegendTruncationContext.useRangeLegendTruncation)();
  const { orientation } = (0, import_RangeLegendOrientationContext.useOrientation)();
  const { width: legendWidth, height: legendHeight } = (0, import_LegendSizeContext.useLegendSize)();
  const containerLegendHeight = legendHeight - import_constants2.CONTAINER_PADDING * 2;
  const containerLegendWidth = legendWidth - import_constants2.CONTAINER_PADDING * 2;
  const width = Math.min(containerLegendWidth, import_constants2.MAX_LEGEND_WIDTH);
  const height = Math.max(containerLegendHeight, import_constants2.MAX_LEGEND_HEIGHT);
  const handleLabelCollision = (0, import_react.useCallback)(
    (firstLabel, lastLabel, orientation2, width2) => {
      if (orientation2 === import_constants.ORIENTATION.Horizontal) {
        return (0, import_handle_two_labels_collisions.handleTwoLabelsCollisions)(
          firstLabel,
          lastLabel,
          truncationMode,
          [0, width2]
        );
      }
      const { startLabel: startLabel2, endLabel: endLabel2 } = (0, import_labels_collision.getLabelsPosition)(
        firstLabel,
        lastLabel,
        [0, width2]
      );
      const { text: startLabelTruncated } = (0, import_handle_collision_in_vertical_layout.handleCollisionInVerticalLayout)(
        firstLabel,
        startLabel2,
        "start",
        truncationMode,
        [0, width2]
      );
      const { text: endLabelTruncated } = (0, import_handle_collision_in_vertical_layout.handleCollisionInVerticalLayout)(
        lastLabel,
        endLabel2,
        "end",
        truncationMode,
        [0, width2]
      );
      return { startLabel: startLabelTruncated, endLabel: endLabelTruncated };
    },
    [truncationMode]
  );
  (0, import_react.useEffect)(() => {
    if (width <= 0 || height <= 0) {
      return;
    }
    const labelsWithTruncation = handleLabelCollision(
      startLabel,
      endLabel,
      orientation,
      width
    );
    setLabels({
      startLabel: labelsWithTruncation.startLabel,
      endLabel: labelsWithTruncation.endLabel
    });
  }, [handleLabelCollision, startLabel, endLabel, orientation, width, height]);
  return labels;
};
