var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_horizontal_label_position_exports = {};
__export(get_horizontal_label_position_exports, {
  getHorizontalLabelPosition: () => getHorizontalLabelPosition
});
module.exports = __toCommonJS(get_horizontal_label_position_exports);
var import_constants = require("../constants.js");
const DEFAULT_POSITION_Y_HORIZONTAL = 22;
const getHorizontalLabelPosition = (labelWidth, itemStartPosition, itemEndPosition, alignment) => {
  const labelPositionY = DEFAULT_POSITION_Y_HORIZONTAL;
  switch (alignment) {
    case import_constants.LABEL_ALIGNMENT.Start:
      return {
        x: itemStartPosition,
        y: labelPositionY
      };
    case import_constants.LABEL_ALIGNMENT.End: {
      return {
        x: Math.floor(itemEndPosition - labelWidth),
        y: labelPositionY
      };
    }
    case import_constants.LABEL_ALIGNMENT.Center: {
      return {
        x: Math.floor(itemEndPosition - labelWidth / 2),
        y: labelPositionY
      };
    }
  }
};
