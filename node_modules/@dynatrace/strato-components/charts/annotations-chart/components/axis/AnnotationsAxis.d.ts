import { ScaleLinear, ScaleTime } from 'd3-scale';
interface AnnotationsAxisProps {
    scale: ScaleTime<number, number> | ScaleLinear<number, number>;
    width: number;
    height: number;
    labelsOffset: number;
}
export declare const AnnotationsAxis: ({ scale, width, height, labelsOffset, }: AnnotationsAxisProps) => import("react/jsx-runtime").JSX.Element | null;
export {};
