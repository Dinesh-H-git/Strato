var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var populate_missing_axis_props_exports = {};
__export(populate_missing_axis_props_exports, {
  populateMissingAxisProps: () => populateMissingAxisProps
});
module.exports = __toCommonJS(populate_missing_axis_props_exports);
var import_lodash_es = require("lodash");
var import_constants = require("../../../constants.js");
var import_annotations_x_axis_props = require("../types/annotations-x-axis-props.js");
const DEFAULT_AXIS = {
  hidden: import_constants.DEFAULT_AXIS_HIDDEN
};
const DEFAULT_NUMERICAL_AXIS = {
  ...DEFAULT_AXIS,
  type: import_constants.DEFAULT_NUMERICAL_AXIS_TYPE
};
const parseConfigAxisToDate = (axisConfig) => {
  let configMin = axisConfig?.min;
  let configMax = axisConfig?.max;
  if (typeof axisConfig?.min === "string") {
    configMin = new Date(axisConfig.min);
  }
  if (typeof axisConfig?.max === "string") {
    configMax = new Date(axisConfig.max);
  }
  return {
    ...axisConfig,
    min: configMin,
    max: configMax
  };
};
const populateMissingAxisProps = (axis, hasNumericalData, axisConfig) => {
  if ((0, import_annotations_x_axis_props.isNumericalAxis)(axis)) {
    return (0, import_lodash_es.merge)({}, DEFAULT_NUMERICAL_AXIS, axisConfig, axis);
  }
  if (hasNumericalData && !(0, import_annotations_x_axis_props.isTimestampAxis)(axis)) {
    return (0, import_lodash_es.merge)({}, DEFAULT_NUMERICAL_AXIS, axisConfig, axis);
  }
  const parsedAxisConfig = axisConfig ? parseConfigAxisToDate(axisConfig) : axisConfig;
  return (0, import_lodash_es.merge)({}, DEFAULT_AXIS, parsedAxisConfig, axis);
};
