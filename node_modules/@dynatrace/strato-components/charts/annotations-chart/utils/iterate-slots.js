var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var iterate_slots_exports = {};
__export(iterate_slots_exports, {
  iterateSlots: () => iterateSlots
});
module.exports = __toCommonJS(iterate_slots_exports);
var import_lodash_es = require("lodash");
var import_react = require("react");
var import_is_slot = require("../../../core/utils/is-slot.js");
var import_populate_missing_marker_props = require("../../core/components/annotations/utils/populate-missing-marker-props.js");
var import_populate_missing_track_props = require("../../core/components/annotations/utils/populate-missing-track-props.js");
var import_Marker = require("../slots/Marker.js");
var import_Tooltip = require("../slots/Tooltip.js");
var import_Track = require("../slots/Track.js");
var import_XAxis = require("../slots/XAxis.js");
const reactNodeToArray = (children) => import_react.Children.toArray(children);
const isValidMarker = (marker) => {
  const valid = Number.isFinite(+marker.start);
  if (!(0, import_lodash_es.isUndefined)(marker.end)) {
    return valid && Number.isFinite(+marker.end);
  }
  return valid;
};
const iterateSlots = (children) => {
  const slots = reactNodeToArray(children);
  const tracks = [];
  const aggregatedMarkers = [];
  let tooltipProps = {};
  let xAxisProps = {};
  slots.forEach((child) => {
    if ((0, import_is_slot.isSlot)(child, import_XAxis.XAxis)) {
      xAxisProps = child.props;
    } else if ((0, import_is_slot.isSlot)(child, import_Track.Track)) {
      const markersChildren = reactNodeToArray(child.props.children);
      const { children: children2, ...trackProps } = child.props;
      const currentTrack = {
        ...trackProps,
        markers: []
      };
      markersChildren.forEach((markerChild) => {
        if ((0, import_is_slot.isSlot)(markerChild, import_Marker.Marker)) {
          const marker = (0, import_populate_missing_marker_props.populateMissingMarkerProps)(
            markerChild.props,
            trackProps
          );
          if (isValidMarker(marker)) {
            currentTrack.markers.push(marker);
            aggregatedMarkers.push(marker);
          }
        }
      });
      const track = (0, import_populate_missing_track_props.populateMissingTrackProps)(currentTrack);
      tracks.push(track);
    } else if ((0, import_is_slot.isSlot)(child, import_Tooltip.Tooltip)) {
      tooltipProps = child.props;
    }
  });
  return { tracks, aggregatedMarkers, xAxisProps, tooltipProps };
};
