import { type ReactNode } from 'react';
interface TopRightSlotProps {
    value: ReactNode;
    isToolbarEnabled: boolean;
}
interface TopRightSlotRef {
    showPopover: () => void;
    hidePopover: () => void;
}
export declare const MeterBarLayoutTopRightSlot: (props: TopRightSlotProps & import("react").RefAttributes<TopRightSlotRef>) => import("react").ReactElement | null;
export {};
