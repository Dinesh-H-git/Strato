var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useMultiMeterBarChartTooltipHandlers_exports = {};
__export(useMultiMeterBarChartTooltipHandlers_exports, {
  useMultiMeterBarChartTooltipHandlers: () => useMultiMeterBarChartTooltipHandlers
});
module.exports = __toCommonJS(useMultiMeterBarChartTooltipHandlers_exports);
var import_TooltipContext = require("../../../core/components/overlay-tooltip/contexts/TooltipContext.js");
var import_useTooltipActions = require("../../../core/components/overlay-tooltip/useTooltipActions.js");
var import_constants = require("../constants.js");
var import_MultiMeterBarChartTooltipContext = require("../context/MultiMeterBarChartTooltipContext.js");
const useMultiMeterBarChartTooltipHandlers = (data, setHoveredSegmentIndex) => {
  const tooltip = (0, import_TooltipContext.useTooltip)();
  const { hover, pin, unpin, outOfBounds } = (0, import_useTooltipActions.useTooltipActions)();
  const { tooltipConfig } = (0, import_MultiMeterBarChartTooltipContext.useMultiMeterBarChartTooltipData)();
  const isSharedTooltip = tooltipConfig.variant === import_constants.TOOLTIP_VARIANT.Shared || !!tooltipConfig.template;
  const buildSingleSeriesData = (segmentEl) => {
    const index = Number(segmentEl.getAttribute("data-segment-index"));
    const series = [data.series[index]];
    return {
      geometry: "multimeterbar",
      series
    };
  };
  const onClick = (event) => {
    if (tooltip.getState().pinned) {
      unpin();
    } else {
      if (isSharedTooltip) {
        pin(event, data);
      } else {
        const segmentEl = event.target.closest(
          "[data-segment-index]"
        );
        if (segmentEl) {
          pin(event, buildSingleSeriesData(segmentEl));
        }
      }
    }
  };
  const onPointerMove = (event) => {
    const segmentEl = event.target.closest("[data-segment-index]");
    if (isSharedTooltip) {
      if (!tooltip.getState().pinned) {
        setHoveredSegmentIndex(
          segmentEl ? Number(segmentEl.getAttribute("data-segment-index")) : void 0
        );
      }
      hover(event, data);
    } else if (segmentEl) {
      hover(event, buildSingleSeriesData(segmentEl));
    } else {
      outOfBounds();
    }
  };
  const onPointerLeave = () => {
    outOfBounds();
  };
  return { onClick, onPointerMove, onPointerLeave };
};
