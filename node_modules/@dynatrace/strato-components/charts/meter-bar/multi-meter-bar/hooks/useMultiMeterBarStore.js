var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useMultiMeterBarStore_exports = {};
__export(useMultiMeterBarStore_exports, {
  useMultiMeterBarStore: () => useMultiMeterBarStore
});
module.exports = __toCommonJS(useMultiMeterBarStore_exports);
var import_use_set_state = require("./store/use-set-state.js");
var import_build_segment_data = require("../utils/build-segment-data.js");
var import_get_segment_color = require("../utils/get-segment-color.js");
function useMultiMeterBarStore() {
  const setState = (0, import_use_set_state.useSetState)();
  const initSegmentsState = (data, seriesForColorRule, colorPalette, colorBuilder) => {
    const segments = data.map((segmentData, index) => {
      const colorAppliedByRule = colorBuilder.for(seriesForColorRule[index]);
      const color = (0, import_get_segment_color.getSegmentColor)(
        segmentData,
        colorPalette,
        index,
        colorAppliedByRule
      );
      return (0, import_build_segment_data.buildSegmentData)(segmentData, color);
    });
    const visibleSegments = new Map(
      segments.map((segment) => [
        segment.id,
        { ...segment, highlighted: true }
      ])
    );
    return { segments, visibleSegments };
  };
  const setVisibleSegments = (visibleSegments) => {
    setState((prev) => {
      const { segments, visibleSegments: visibleSegmentsStore } = prev;
      const segmentsWithIds = segments.reduce(
        (acc, curr) => ({ ...acc, [curr.id]: { ...curr } }),
        {}
      );
      visibleSegmentsStore.clear();
      visibleSegments.forEach(
        (segment) => visibleSegmentsStore.set(segment.id, {
          ...segmentsWithIds[segment.id]
        })
      );
      return {
        ...prev,
        visibleSegments: visibleSegmentsStore
      };
    });
  };
  const setHighlightedSegment = (highlightedSegment) => {
    setState((prev) => ({ ...prev, highlightedSegment }));
  };
  return {
    initSegmentsState,
    setHighlightedSegment,
    setVisibleSegments
  };
}
