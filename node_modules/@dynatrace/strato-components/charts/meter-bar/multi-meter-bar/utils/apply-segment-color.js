var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var apply_segment_color_exports = {};
__export(apply_segment_color_exports, {
  applySegmentColor: () => applySegmentColor
});
module.exports = __toCommonJS(apply_segment_color_exports);
var import_get_color_palette = require("../../../core/utils/get-color-palette.js");
var import_default = require("../default.js");
const parseColorPalette = (colorPalette) => {
  if (!colorPalette) {
    return import_default.DEFAULT_COLOR_PALETTE;
  }
  if (typeof colorPalette === "string" || Array.isArray(colorPalette)) {
    return colorPalette;
  }
  return Object.values(colorPalette);
};
const applySegmentColor = (segmentName, index, colorPalette, color) => {
  if (color) {
    return color;
  }
  const palette = parseColorPalette(colorPalette);
  const segmentColor = (0, import_get_color_palette.getColorPalette)(segmentName, index, palette);
  return segmentColor.Default;
};
