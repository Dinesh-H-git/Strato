var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_thresholds_exports = {};
__export(build_thresholds_exports, {
  buildThresholds: () => buildThresholds
});
module.exports = __toCommonJS(build_thresholds_exports);
var import_get_threshold_indicators = require("./get-threshold-indicators.js");
var import_find_matching_threshold = require("../../../core/utils/find-matching-threshold.js");
function buildThresholds(value, min, max, width, thresholdsConfig) {
  const matchingThreshold = thresholdsConfig !== void 0 ? (0, import_find_matching_threshold.findMatchingThreshold)(value, thresholdsConfig.data) : void 0;
  const thresholdIndicators = thresholdsConfig !== void 0 ? (0, import_get_threshold_indicators.getThresholdIndicators)(thresholdsConfig.data, min, max, width) : [];
  const legendItems = thresholdsConfig !== void 0 ? thresholdsConfig.data.map((threshold, index) => {
    const name = threshold.name ?? `Threshold ${index + 1}`;
    return {
      id: `item-${name}-${index}`,
      name,
      color: { Default: threshold.color }
    };
  }) : [];
  return { matchingThreshold, thresholdIndicators, legendItems };
}
