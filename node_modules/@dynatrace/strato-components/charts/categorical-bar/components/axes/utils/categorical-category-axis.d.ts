import { TickCategory } from '../../../../core/components/axes/types.js';
import { WeakMapCategory, CategoricalBarData } from '../../../types/categorical-bar-chart-data.js';
/**
 * get tick categories record id name
 * @param filteredCategories -
 *
 * @returns return Record key id, value name
 */
export declare const getTickCategories: (filteredCategories: CategoricalBarData[], categoryMap: WeakMapCategory) => TickCategory;
