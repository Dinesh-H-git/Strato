var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useCategoricalMouseEventHandlers_exports = {};
__export(useCategoricalMouseEventHandlers_exports, {
  useCategoricalMouseEventHandlers: () => useCategoricalMouseEventHandlers
});
module.exports = __toCommonJS(useCategoricalMouseEventHandlers_exports);
var import_calculate_mouse_positions = require("../../../../core/utils/calculate-mouse-positions.js");
var import_selectors = require("../../../hooks/store/selectors.js");
const useCategoricalMouseEventHandlers = () => {
  const setState = (0, import_selectors.useSetState)();
  const handleMouseEnter = (event) => {
    setState((prev) => ({
      ...prev,
      overlays: {
        ...prev.overlays,
        mouseInBounds: true,
        mousePosition: (0, import_calculate_mouse_positions.calculateMousePositions)(event, event.currentTarget),
        visibleTooltip: true
      }
    }));
  };
  const handleMouseMove = (event) => {
    setState((prev) => ({
      ...prev,
      overlays: {
        ...prev.overlays,
        mouseInBounds: true,
        mousePosition: (0, import_calculate_mouse_positions.calculateMousePositions)(event, event.currentTarget),
        visibleTooltip: true
      }
    }));
  };
  const handleMouseLeave = () => {
    setState((prev) => ({
      ...prev,
      overlays: {
        ...prev.overlays,
        mouseInBounds: false,
        mousePosition: void 0,
        visibleTooltip: prev.overlays.pinnedTooltip
      }
    }));
  };
  const handleFocus = () => {
    setState((prev) => ({
      ...prev,
      overlays: {
        ...prev.overlays,
        mouseInBounds: true
      }
    }));
  };
  const handleBlur = (event) => {
    const nextFocusElement = event.relatedTarget;
    if (!event.target?.contains(nextFocusElement)) {
      setState((prev) => ({
        ...prev,
        overlays: {
          ...prev.overlays,
          mouseInBounds: false
        }
      }));
    }
  };
  return {
    handleMouseEnter,
    handleMouseMove,
    handleMouseLeave,
    handleFocus,
    handleBlur
  };
};
