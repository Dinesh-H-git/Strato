var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var categorical_tooltip_exports = {};
__export(categorical_tooltip_exports, {
  getClosestCategory: () => getClosestCategory,
  getSortedTooltipItems: () => getSortedTooltipItems
});
module.exports = __toCommonJS(categorical_tooltip_exports);
var import_get_unit_as_string = require("../../../../core/utils/get-unit-as-string.js");
var import_safe_get = require("../../../../core/utils/safe-get.js");
const getClosestCategory = (categories, dimensionMap, closestPoint) => categories.find(
  (category) => category.dimensions.find(
    (dimension) => (0, import_safe_get.safeGet)(dimensionMap, dimension).id === closestPoint?.datapoint.id
  )
) ?? categories[0];
const getSortedTooltipItems = (dimensionMap, closestPoint, grouped, closestCategory, formatter, valueScale, unit) => {
  if (!closestPoint) {
    return [];
  }
  if (!grouped) {
    const { value, ...rest } = closestPoint.datapoint;
    return [
      {
        ...rest,
        values: [formatter(value, (0, import_get_unit_as_string.getUnitAsString)(unit))]
      }
    ];
  }
  return closestCategory.dimensions.map((dimension) => {
    const { id, color, value, name, cumulativeValue } = (0, import_safe_get.safeGet)(
      dimensionMap,
      dimension
    );
    return {
      ...dimension,
      id,
      name,
      color,
      values: [formatter(value, (0, import_get_unit_as_string.getUnitAsString)(unit))],
      position: valueScale((cumulativeValue ?? 0) + value) || 0
    };
  }).sort((item1, item2) => item1.position - item2.position);
};
