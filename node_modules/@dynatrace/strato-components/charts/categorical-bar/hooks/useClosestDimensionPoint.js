var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useClosestDimensionPoint_exports = {};
__export(useClosestDimensionPoint_exports, {
  useClosestDimensionPoint: () => useClosestDimensionPoint
});
module.exports = __toCommonJS(useClosestDimensionPoint_exports);
var import_useClosestDimensionPoints = require("./useClosestDimensionPoints.js");
var import_getClosestDistance = require("../utils/getClosestDistance.js");
var import_layout = require("../utils/layout.js");
const useClosestDimensionPoint = (layout, groupMode = "stacked", mousePosition) => {
  const closestDimensionPoints = (0, import_useClosestDimensionPoints.useClosestDimensionPoints)(
    layout,
    groupMode,
    mousePosition
  );
  const closestDimensionPoint = findClosestDimensionPoint(
    layout,
    mousePosition,
    closestDimensionPoints,
    groupMode
  );
  return {
    closestDimensionPoint: closestDimensionPoint ?? closestDimensionPoints[0],
    closestDimensionPoints
  };
};
const findClosestDimensionPoint = (layout, mousePosition, closestDimensionPoints, groupMode) => {
  if (mousePosition && closestDimensionPoints.length > 0) {
    return closestDimensionPoints.reduce(
      (p1, p2) => (0, import_getClosestDistance.getClosestDistance)(
        mousePosition,
        p1,
        p2,
        (0, import_layout.isVertical)(layout),
        (0, import_layout.isStacked)(groupMode)
      ),
      closestDimensionPoints[0]
    );
  }
  return void 0;
};
