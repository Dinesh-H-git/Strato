var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useClosestDimensionPoints_exports = {};
__export(useClosestDimensionPoints_exports, {
  useClosestDimensionPoints: () => useClosestDimensionPoints
});
module.exports = __toCommonJS(useClosestDimensionPoints_exports);
var import_react = require("react");
var import_useCategoricalBarChartScales = require("./useCategoricalBarChartScales.js");
var import_useCategoricalBarFilteredData = require("./useCategoricalBarFilteredData.js");
var import_safe_get = require("../../core/utils/safe-get.js");
var import_scale = require("../../core/utils/scale.js");
var import_categorical_layout = require("../utils/categorical-layout.js");
var import_layout = require("../utils/layout.js");
function useClosestDimensionPoints(layout, groupMode = "stacked", mousePosition) {
  const { filteredCategories, isSingleDimension, dimensionMap, categoryMap } = (0, import_useCategoricalBarFilteredData.useCategoricalBarFilteredData)();
  const { categoryScale, valueScale, dimensionScale } = (0, import_useCategoricalBarChartScales.useCategoricalBarChartScales)();
  const [closestDimensionPoints, setClosestDimensionPositions] = (0, import_react.useState)([]);
  const vertical = (0, import_layout.isVertical)(layout);
  const hoveredCategoryId = mousePosition ? (0, import_scale.scaleBandInvert)(categoryScale)(
    (vertical ? mousePosition.x : mousePosition.y) ?? 0
  ) : void 0;
  const categoryIndex = filteredCategories.findIndex(
    (category) => category.id === hoveredCategoryId
  );
  const closestCategory = filteredCategories[categoryIndex];
  (0, import_react.useEffect)(() => {
    const dimensionPoints = closestCategory?.dimensions.map((dimension) => {
      const { name, color, cumulativeValue, value, id, ...rest } = (0, import_safe_get.safeGet)(
        dimensionMap,
        dimension
      );
      const { id: categoryId } = (0, import_safe_get.safeGet)(categoryMap, closestCategory);
      const barSize = (0, import_categorical_layout.getTopValueCoord)(dimension, valueScale, dimensionMap);
      const categoryCoord = categoryScale(categoryId) ?? 0;
      const dimensionCoord = categoryCoord + (dimensionScale(name) ?? 0);
      const dimensionPoint = (0, import_layout.isStacked)(groupMode) || isSingleDimension ? categoryCoord + categoryScale.bandwidth() / 2 : dimensionCoord + dimensionScale.bandwidth() / 2;
      return {
        x: vertical ? dimensionPoint : barSize,
        y: vertical ? barSize : dimensionPoint,
        datapoint: { ...rest, cumulativeValue, value, name, color, id }
      };
    }) ?? [];
    setClosestDimensionPositions(dimensionPoints);
  }, [
    closestCategory,
    vertical,
    categoryScale,
    valueScale,
    groupMode,
    categoryIndex
  ]);
  return closestDimensionPoints;
}
