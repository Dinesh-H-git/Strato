var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useClosestDimensionInThresholdPoints_exports = {};
__export(useClosestDimensionInThresholdPoints_exports, {
  useClosestDimensionInThresholdPoints: () => useClosestDimensionInThresholdPoints
});
module.exports = __toCommonJS(useClosestDimensionInThresholdPoints_exports);
var import_lodash_es = require("lodash");
var import_useCategoricalBarChartScales = require("./useCategoricalBarChartScales.js");
var import_useCategoricalBarFilteredData = require("./useCategoricalBarFilteredData.js");
var import_useClosestDimensionPoints = require("./useClosestDimensionPoints.js");
var import_groupedDistancePoints = require("../utils/groupedDistancePoints.js");
var import_layout = require("../utils/layout.js");
var import_stackedDistancePoints = require("../utils/stackedDistancePoints.js");
const useClosestDimensionInThresholdPoints = (layout, groupMode = "stacked", mousePosition) => {
  const { isSingleDimension } = (0, import_useCategoricalBarFilteredData.useCategoricalBarFilteredData)();
  const { categoryScale, valueScale, dimensionScale } = (0, import_useCategoricalBarChartScales.useCategoricalBarChartScales)();
  const closestDimensionPoints = (0, import_useClosestDimensionPoints.useClosestDimensionPoints)(
    layout,
    groupMode,
    mousePosition
  );
  if ((0, import_lodash_es.isUndefined)(mousePosition)) {
    return;
  }
  return findClosestDimensionPointInThreshold(
    layout,
    mousePosition,
    closestDimensionPoints,
    groupMode,
    categoryScale,
    valueScale,
    dimensionScale,
    isSingleDimension
  );
};
const findClosestDimensionPointInThreshold = (layout, mousePosition, closestDimensionPoints, groupMode, categoryScale, valueScale, dimensionScale, isSingleDimension) => {
  if (mousePosition && closestDimensionPoints.length > 0) {
    let closestDimensionPointInThreshold;
    if ((0, import_layout.isStacked)(groupMode) || isSingleDimension) {
      closestDimensionPointInThreshold = (0, import_stackedDistancePoints.findClosestPointInStackedChart)(
        layout,
        mousePosition,
        closestDimensionPoints,
        valueScale,
        categoryScale
      );
    } else {
      closestDimensionPointInThreshold = (0, import_groupedDistancePoints.findClosestPointInGroupedChart)(
        layout,
        mousePosition,
        closestDimensionPoints,
        categoryScale,
        dimensionScale
      );
    }
    return closestDimensionPointInThreshold;
  }
  return void 0;
};
