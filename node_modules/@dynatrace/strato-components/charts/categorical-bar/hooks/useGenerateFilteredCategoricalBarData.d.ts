import type { ChartUnit } from '../../core/types/chart-unit.js';
import type { ValueRepresentation } from '../../core/types/scales.js';
import type { CategoricalBarData, WeakMapCategory, WeakMapDimension } from '../types/categorical-bar-chart-data.js';
export type MinMaxPerUnit = Map<ChartUnit, {
    min: number;
    max: number;
}>;
export declare const getMaxDimensionValue: (filteredCategories: CategoricalBarData[]) => number;
export declare const calculateTotalCategoryValue: (categoryData: CategoricalBarData) => number;
export declare const calculateRelativeValue: (value: unknown, relativeTotalValue?: number) => number;
/**
 *
 * @param filteredCategories - filtered categories that have new reference in memory, so would throw an error if we do safeGet on it
 * @param categoricalData - non filtered categories that have their obj reference in the category/dimension map
 * @param categoryMap - category map
 * @param dimensionMap  - dimension map
 * @returns
 */
export declare function useGenerateFilteredCategoricalBarData(filteredCategories: CategoricalBarData[], categoricalData: CategoricalBarData[], unit: ChartUnit, categoryMap: WeakMapCategory, dimensionMap: WeakMapDimension, valueRepresentation: ValueRepresentation): {
    categoryMap: WeakMapCategory;
    dimensionMap: WeakMapDimension;
    filteredUniqueDimensions: string[];
    minMaxPerUnit: MinMaxPerUnit;
    isSingleDimension: boolean;
};
