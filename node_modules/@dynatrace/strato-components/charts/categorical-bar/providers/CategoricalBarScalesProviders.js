var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CategoricalBarScalesProviders_exports = {};
__export(CategoricalBarScalesProviders_exports, {
  CategoricalBarScalesProvider: () => CategoricalBarScalesProvider
});
module.exports = __toCommonJS(CategoricalBarScalesProviders_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_d3_scale = require("d3-scale");
var import_useGridPlotSize = require("../../core/components/chart-grid/hooks/useGridPlotSize.js");
var import_constants = require("../../core/constants.js");
var import_CategoricalBarChartScalesContext = require("../contexts/CategoricalBarChartScalesContext.js");
var import_useCategoricalBarChartBaseConfig = require("../hooks/useCategoricalBarChartBaseConfig.js");
var import_useCategoricalBarChartValueAxisConfig = require("../hooks/useCategoricalBarChartValueAxisConfig.js");
var import_useCategoricalBarFilteredData = require("../hooks/useCategoricalBarFilteredData.js");
var import_get_value_scale = require("../utils/get-value-scale.js");
var import_layout = require("../utils/layout.js");
const CategoricalBarScalesProvider = ({
  children
}) => {
  const { filteredCategories, unit, filteredDimensions, minMaxPerUnit } = (0, import_useCategoricalBarFilteredData.useCategoricalBarFilteredData)();
  const { min, max, scale } = (0, import_useCategoricalBarChartValueAxisConfig.useCategoricalBarChartValueAxisConfig)();
  const { layout } = (0, import_useCategoricalBarChartBaseConfig.useCategoricalBarChartBaseConfig)();
  const isHorizontal = !(0, import_layout.isVertical)(layout);
  const { width, height } = (0, import_useGridPlotSize.useGridPlotSize)();
  const categoryScale = (0, import_d3_scale.scaleBand)().domain(filteredCategories.map(({ id }) => id)).range(isHorizontal ? [0, height] : [0, width]).paddingInner(import_constants.CATEGORICAL_BARS_INNER_PADDING).paddingOuter(import_constants.CATEGORICAL_BARS_OUTER_PADDING);
  const dimensionScale = (0, import_d3_scale.scaleBand)().domain(filteredDimensions).range([0, categoryScale.bandwidth()]).paddingInner(import_constants.CATEGORICAL_BARS_INNER_PADDING);
  const size = isHorizontal ? width : height;
  const domainMinMax = minMaxPerUnit.get(unit) ?? { min: 0, max: 0 };
  const valueScale = (0, import_get_value_scale.getCategoricalValueScale)(
    domainMinMax,
    size,
    isHorizontal,
    scale,
    { min, max }
  );
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_CategoricalBarChartScalesContext.CategoricalBarChartScalesContext.Provider,
    {
      value: { categoryScale, valueScale, dimensionScale },
      children
    }
  );
};
