var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CategoricalBarFilteredDataProvider_exports = {};
__export(CategoricalBarFilteredDataProvider_exports, {
  CategoricalBarFilteredDataProvider: () => CategoricalBarFilteredDataProvider
});
module.exports = __toCommonJS(CategoricalBarFilteredDataProvider_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_CategoricalBarFilteredData_context = require("../contexts/CategoricalBarFilteredData.context.js");
var import_selectors = require("../hooks/store/selectors.js");
var import_useCategoricalBarChartData = require("../hooks/useCategoricalBarChartData.js");
var import_useGenerateFilteredCategoricalBarData = require("../hooks/useGenerateFilteredCategoricalBarData.js");
const CategoricalBarFilteredDataProvider = ({
  children
}) => {
  const {
    categoricalData,
    unit,
    categoryMap: initialCategoryMap,
    dimensionMap: initialDimensionMap,
    valueRepresentation
  } = (0, import_useCategoricalBarChartData.useCategoricalBarChartData)();
  const filteredDimensions = (0, import_selectors.useFilteredDimensionsState)();
  const filteredCategories = filteredDimensions.size > 0 ? categoricalData.map((category) => ({
    ...category,
    dimensions: category.dimensions.filter(
      ({ name }) => filteredDimensions.has(name)
    )
  })) : categoricalData;
  const {
    categoryMap,
    dimensionMap,
    filteredUniqueDimensions,
    minMaxPerUnit,
    isSingleDimension
  } = (0, import_useGenerateFilteredCategoricalBarData.useGenerateFilteredCategoricalBarData)(
    filteredCategories,
    categoricalData,
    unit,
    initialCategoryMap,
    initialDimensionMap,
    valueRepresentation
  );
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    import_CategoricalBarFilteredData_context.CategoricalBarFilteredDataContext.Provider,
    {
      value: {
        filteredCategories,
        filteredDimensions: filteredUniqueDimensions,
        minMaxPerUnit,
        isSingleDimension,
        categoryMap,
        dimensionMap,
        unit
      },
      children
    }
  );
};
