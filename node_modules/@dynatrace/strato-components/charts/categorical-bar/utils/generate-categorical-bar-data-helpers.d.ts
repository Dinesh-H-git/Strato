import { ColorPalette, ColorPaletteMode, ColorToken, CustomColorPalette } from '../../core/types/color-palette.js';
import type { CategoricalBarData, WeakMapDimension } from '../types/categorical-bar-chart-data.js';
import { CategoricalBarChartData } from '../types/categorical-bar-chart.js';
/**
 * Series entry for ColorRule evaluation.
 * Internal fields track original data for mapping results back to dimensions.
 */
export type SeriesEntry = CategoricalBarChartData & {
    _originalData: CategoricalBarChartData;
    _dimensionKey?: string;
};
interface ColorRuleFor {
    for: (series: SeriesEntry) => ColorToken | null | undefined | ColorPalette;
}
export declare function getDimensionColor({ colorByDimension, categoryColor, categoryName, paletteColorPosition, colorPalette, colorPaletteMode, colorRuleResult, }: {
    colorByDimension: Map<string, ColorToken>;
    categoryColor?: string;
    categoryName: string;
    paletteColorPosition: number;
    colorPalette?: ColorPalette | CustomColorPalette;
    colorPaletteMode: ColorPaletteMode;
    colorRuleResult?: ColorToken | null | ColorPalette;
}): ColorToken;
export declare function handleSingleDimension({ categoryData, rest, categoryName, categoryColor, colorByDimension, colorRules, colorPalette, colorPaletteMode, paletteColorPosition, uniqueDimensions, dimensionMap, newCategory, seriesForColorRule, hasColorRules, }: {
    categoryData: CategoricalBarChartData;
    rest: Omit<CategoricalBarChartData, 'unit' | 'category' | 'value' | 'color'>;
    categoryName: string;
    categoryColor?: string;
    colorByDimension: Map<string, ColorToken>;
    colorRules: ColorRuleFor;
    colorPalette?: ColorPalette | CustomColorPalette;
    colorPaletteMode: ColorPaletteMode;
    paletteColorPosition: number;
    uniqueDimensions: Set<string>;
    dimensionMap: WeakMapDimension;
    newCategory: CategoricalBarData;
    seriesForColorRule: SeriesEntry[];
    hasColorRules: boolean;
}): void;
export declare function handleMultipleDimensions({ categoryData, rest, categoryName, colorByDimension, colorByCategory, colorRules, colorPalette, categoricalColorPalette, uniqueDimensions, dimensionMap, newCategory, seriesForColorRule, hasColorRules, }: {
    categoryData: CategoricalBarChartData;
    rest: Omit<CategoricalBarChartData, 'unit' | 'category' | 'value' | 'color'>;
    categoryName: string;
    colorByDimension: Map<string, ColorToken>;
    colorByCategory: Map<string, {
        [id: string]: ColorToken;
    }>;
    colorRules: ColorRuleFor;
    colorPalette?: ColorPalette | CustomColorPalette;
    categoricalColorPalette: boolean;
    uniqueDimensions: Set<string>;
    dimensionMap: WeakMapDimension;
    newCategory: CategoricalBarData;
    seriesForColorRule: SeriesEntry[];
    hasColorRules: boolean;
}): void;
export {};
