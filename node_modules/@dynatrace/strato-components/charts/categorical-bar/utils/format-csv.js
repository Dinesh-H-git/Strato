var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var format_csv_exports = {};
__export(format_csv_exports, {
  formatCSV: () => formatCSV
});
module.exports = __toCommonJS(format_csv_exports);
var import_download = require("../../../core/utils/download.js");
function formatCSV(categories) {
  const isSingleDimension = typeof categories[0].value === "number";
  let csv = "";
  let columns = ["category", "value"];
  if (!isSingleDimension) {
    columns = ["category", "key", "value"];
  }
  for (const category of categories) {
    if (isSingleDimension) {
      const sanitizedCategory = (0, import_download.escapeForCSV)(category.category);
      csv += `${sanitizedCategory},${category.value},
`;
    } else {
      for (const [dimensionName, dimensionValue] of Object.entries(
        category.value
      )) {
        const sanitizedCategory = (0, import_download.escapeForCSV)(category.category);
        const sanitizedName = (0, import_download.escapeForCSV)(dimensionName);
        csv += `${sanitizedCategory},${sanitizedName},${dimensionValue},
`;
      }
    }
  }
  csv = `${columns.join(",")}
${csv}`;
  return csv;
}
