import { Layout } from '../types/categorical-bar-chart.js';
/**
 * Returns true if layout is a valid Layout
 * @param layout - layout
 */
export declare const isValidLayout: (layout: "horizontal" | "vertical") => layout is Layout;
