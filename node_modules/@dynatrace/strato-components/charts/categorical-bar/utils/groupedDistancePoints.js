var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var groupedDistancePoints_exports = {};
__export(groupedDistancePoints_exports, {
  TOOLTIP_DISTANCE_THRESHOLD: () => TOOLTIP_DISTANCE_THRESHOLD,
  findClosestPointInGroupedChart: () => findClosestPointInGroupedChart
});
module.exports = __toCommonJS(groupedDistancePoints_exports);
var import_layout = require("./layout.js");
var import_scale = require("../../core/utils/scale.js");
const TOOLTIP_DISTANCE_THRESHOLD = 32;
function findClosestPointInGroupedChart(layout, mousePosition, closestDimensionPoints, categoryScale, dimensionScale) {
  const mouseCoordinate = (0, import_layout.isVertical)(layout) ? mousePosition.x : mousePosition.y;
  const closestCategory = (0, import_scale.scaleBandInvert)(categoryScale)(mouseCoordinate);
  const categoryCoord = categoryScale(closestCategory) ?? 0;
  const closestDimension = (0, import_scale.scaleBandInvert)(dimensionScale)(
    mouseCoordinate - categoryCoord
  );
  return closestDimensionPoints.find(
    (dimension) => closestDimension === dimension.datapoint.name
  );
}
