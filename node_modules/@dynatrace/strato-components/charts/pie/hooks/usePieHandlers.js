var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var usePieHandlers_exports = {};
__export(usePieHandlers_exports, {
  usePieHandlers: () => usePieHandlers
});
module.exports = __toCommonJS(usePieHandlers_exports);
var import_use_set_highlighted_ids_state = require("./store/use-set-highlighted-ids-state.js");
var import_use_set_visible_slices_state = require("./store/use-set-visible-slices-state.js");
var import_useTooltipActions = require("../../core/components/overlay-tooltip/useTooltipActions.js");
var import_get_slices_map = require("../utils/get-slices-map.js");
var import_plot_data_utils = require("../utils/plot-data.utils.js");
function usePieHandlers(data) {
  const setVisibleSlicesState = (0, import_use_set_visible_slices_state.useSetVisibleSlicesState)();
  const setHighlightedIdState = (0, import_use_set_highlighted_ids_state.useSetHighlightedIdState)();
  const { reset } = (0, import_useTooltipActions.useTooltipActions)();
  const slicesMap = (0, import_get_slices_map.getSlicesMap)(data);
  const onLegendItemHover = (hoveredSeries) => {
    setHighlightedIdState(hoveredSeries?.id);
  };
  const onLegendItemClicked = (visibleSlices) => {
    reset();
    const newVisibleSlices = visibleSlices.map(
      (legendItem) => ({
        ...slicesMap.get(legendItem.id)
      })
    );
    setVisibleSlicesState(
      (0, import_plot_data_utils.getSlicesWithRelativeValues)(newVisibleSlices)
    );
  };
  return { onLegendItemHover, onLegendItemClicked };
}
