import { type PropsWithChildren } from 'react';
import { PieChartProps, PieDonutConfig } from '../types/pie-chart.config.js';
export declare const usePieChartConfig: (props: PropsWithChildren<PieChartProps>) => PieDonutConfig;
