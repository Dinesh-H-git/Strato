import { type PropsWithChildren } from 'react';
import type { InnerWrapperProps } from '../../../../types/internal/pie-chart.js';
interface InnerContainerProps extends Pick<InnerWrapperProps, 'width' | 'height'> {
    donutInner: number;
}
export declare const InnerContainer: ({ donutInner, height, width, children, }: PropsWithChildren<InnerContainerProps>) => import("react/jsx-runtime").JSX.Element;
export {};
