import type { InnerWrapperProps } from '../../../../types/internal/pie-chart.js';
interface InnerDonutProps extends Pick<InnerWrapperProps, 'visibleIds'> {
    donutInner: number;
}
export declare const InnerDonut: ({ donutInner, visibleIds }: InnerDonutProps) => import("react/jsx-runtime").JSX.Element | null;
export {};
