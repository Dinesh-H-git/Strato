import { InternalSliceData, type ChartVariant } from '../types/internal/pie-chart.js';
export declare const buildChartSizes: (height: number, width: number, variant: ChartVariant, data: InternalSliceData[], hiddenLabels: boolean) => {
    topCenter: number;
    radius: number;
    innerRadius: number;
    leftCenter: number;
};
