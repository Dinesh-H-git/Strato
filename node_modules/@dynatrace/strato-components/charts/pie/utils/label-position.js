var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var label_position_exports = {};
__export(label_position_exports, {
  labelPosition: () => labelPosition
});
module.exports = __toCommonJS(label_position_exports);
var import_default = require("../types/default.js");
const labelPosition = ({
  outerRadius,
  innerRadius,
  centroidX,
  centroidY
}) => {
  const labelRadiusDistance = outerRadius + import_default.LABEL_FONT_SIZE / 2;
  const distanceAsPercentage = (
    // radius centroid / radius expected positon === centroid in percentage
    (outerRadius + innerRadius) / (2 * labelRadiusDistance)
  );
  return {
    // expand centroids X and Y proportionally so they expand up to the desired radius distance
    labelX: centroidX * (1 / distanceAsPercentage),
    labelY: centroidY * (1 / distanceAsPercentage)
  };
};
