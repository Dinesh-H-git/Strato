var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_chart_sizes_exports = {};
__export(build_chart_sizes_exports, {
  buildChartSizes: () => buildChartSizes
});
module.exports = __toCommonJS(build_chart_sizes_exports);
var import_constants = require("../constants.js");
var import_default = require("../types/default.js");
const buildChartSizes = (height, width, variant, data, hiddenLabels) => {
  const topCenter = height / 2;
  const labelsPadding = hiddenLabels ? 0 : import_default.LABEL_FONT_SIZE * import_default.LABEL_FONT_SIZE_MULTIPLIER;
  const radius = Math.min(width, height) / 2 - labelsPadding;
  const donutInnerRadius = radius * import_default.DONUT_INTERNAL_RADIUS;
  const pieInnerRadius = data.length > 1 ? import_default.PIE_INTERNAL_RADIUS : 0;
  const innerRadius = variant === import_constants.CHART_VARIANT.Donut ? donutInnerRadius : pieInnerRadius;
  const leftCenter = width / 2;
  return { topCenter, radius, innerRadius, leftCenter };
};
