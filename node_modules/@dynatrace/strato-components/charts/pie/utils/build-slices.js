var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_slices_exports = {};
__export(build_slices_exports, {
  buildSlices: () => buildSlices
});
module.exports = __toCommonJS(build_slices_exports);
var import_get_color_palette = require("../../core/utils/get-color-palette.js");
var import_plot_data_utils = require("../utils/plot-data.utils.js");
const DEFAULT_COLOR_PALETTE = "categorical";
const buildSlices = (data, colorPalette, unit) => {
  const totalAbsoluteValueSum = Object.values(data).reduce(
    (total, { value }) => total + Math.abs(value),
    0
  );
  return data.map((slice, index) => {
    const assignedPaletteColor = (0, import_get_color_palette.getColorPalette)(
      slice.category,
      index,
      colorPalette ?? DEFAULT_COLOR_PALETTE
    );
    const priorityColor = slice.color && { Default: slice.color };
    return {
      ...slice,
      id: `${slice.category}-${index}`,
      relativeValue: (0, import_plot_data_utils.getSlicePercentage)(slice.value, totalAbsoluteValueSum),
      color: priorityColor || assignedPaletteColor,
      _priorityColor: priorityColor || void 0,
      unit
    };
  });
};
