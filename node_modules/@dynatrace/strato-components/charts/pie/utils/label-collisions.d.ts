import type { InternalLabel } from '../types/internal/pie-chart.js';
export declare const collision: (a: InternalLabel, b: InternalLabel) => boolean;
export declare const isInVerticalBounds: (labelY: number, labelHeight: number, top: number) => boolean;
export declare const isInBounds: (label: InternalLabel, left: number, top: number) => boolean;
