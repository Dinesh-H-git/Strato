var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var label_collisions_exports = {};
__export(label_collisions_exports, {
  collision: () => collision,
  isInBounds: () => isInBounds,
  isInVerticalBounds: () => isInVerticalBounds
});
module.exports = __toCommonJS(label_collisions_exports);
var import_is_in_horizontal_bounds = require("../../core/utils/is-in-horizontal-bounds.js");
var import_default = require("../types/default.js");
const COLLISION_THRESHOLD = import_default.LABEL_FONT_SIZE / 2;
const oneWayCollision = (a, b) => a.boxX >= b.boxX && a.boxX <= b.boxX + b.boxWidth + COLLISION_THRESHOLD && (a.boxY >= b.boxY && a.boxY <= b.boxY + b.boxHeight + COLLISION_THRESHOLD || a.boxY + a.boxHeight >= b.boxY - COLLISION_THRESHOLD && a.boxY + a.boxHeight <= b.boxY + b.boxHeight);
const collision = (a, b) => oneWayCollision(a, b) || oneWayCollision(b, a);
const isInVerticalBounds = (labelY, labelHeight, top) => labelY > -top && labelY + labelHeight < top;
const isInBounds = (label, left, top) => (0, import_is_in_horizontal_bounds.isInHorizontalBounds)(label.boxX, label.boxWidth, left) && isInVerticalBounds(label.boxY, label.boxHeight, top);
