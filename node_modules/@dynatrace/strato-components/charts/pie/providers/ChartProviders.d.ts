import { type PropsWithChildren } from 'react';
import type { PieChartData, PieDonutConfig } from '../types/pie-chart.config.js';
export interface ChartProvidersProps extends PropsWithChildren {
    chartData: PieChartData;
    chartConfig: PieDonutConfig;
}
export declare const ChartProviders: (props: ChartProvidersProps) => import("react/jsx-runtime").JSX.Element;
