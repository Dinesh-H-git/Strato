var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var format_csv_exports = {};
__export(format_csv_exports, {
  formatCSV: () => formatCSV
});
module.exports = __toCommonJS(format_csv_exports);
var import_download = require("../../../../core/utils/download.js");
var import_get_unit_as_string = require("../../../core/utils/get-unit-as-string.js");
function formatCSV(rawData) {
  const columns = ["name", "unit", "from", "to", "value"];
  const binsColumns = columns.slice(columns.indexOf("from"));
  let csv = `${columns.join(",")}
`;
  for (const series of rawData) {
    const sanitizedSeriesName = (0, import_download.escapeForCSV)(series.name);
    const seriesRow = [sanitizedSeriesName, (0, import_get_unit_as_string.getUnitAsString)(series.unit)];
    for (const bins of series.bins) {
      const binsRow = [];
      for (const column of binsColumns) {
        const value = bins[column];
        const parsedValue = value?.toString() ?? "";
        binsRow.push(parsedValue);
      }
      csv += `${seriesRow.concat(binsRow).join(",")}
`;
    }
  }
  return csv;
}
