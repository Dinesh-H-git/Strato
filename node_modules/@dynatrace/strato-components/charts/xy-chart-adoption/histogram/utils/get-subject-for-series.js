var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_subject_for_series_exports = {};
__export(get_subject_for_series_exports, {
  HISTOGRAM_DEFAULT_VALUE_ACCESSOR: () => HISTOGRAM_DEFAULT_VALUE_ACCESSOR,
  getSubjectForSeries: () => getSubjectForSeries
});
module.exports = __toCommonJS(get_subject_for_series_exports);
var import_lodash_es = require("lodash");
var import_getAccessor = require("../../../core/utils/getAccessor.js");
const HISTOGRAM_DEFAULT_VALUE_ACCESSOR = "name";
const FORBIDDEN_ACCESSORS = /^name$/;
function getSubjectForSeries(rule, series) {
  const { valueAccessor } = rule;
  if (!valueAccessor || FORBIDDEN_ACCESSORS.test(valueAccessor)) {
    return series.name;
  }
  const accessor = (0, import_getAccessor.getAccessor)(valueAccessor);
  if (accessor) {
    const value = (0, import_lodash_es.get)(series, accessor);
    if (!(0, import_lodash_es.isNil)(value)) {
      return value;
    }
  }
  return null;
}
