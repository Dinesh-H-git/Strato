var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var resolve_histogram_color_rules_exports = {};
__export(resolve_histogram_color_rules_exports, {
  resolveHistogramColorRules: () => resolveHistogramColorRules
});
module.exports = __toCommonJS(resolve_histogram_color_rules_exports);
var import_lodash_es = require("lodash");
var import_get_subject_for_series = require("./get-subject-for-series.js");
var import_get_color_palette = require("../../../core/utils/get-color-palette.js");
var import_validators = require("../../../core/utils/validators.js");
function resolveHistogramColorRules(seriesData, colorRules) {
  const isValid = (0, import_validators.buildValidator)(import_validators.VALUE_MATCHERS);
  const resolved = [];
  for (const rule of colorRules) {
    if (!rule.colorPalette) {
      resolved.push({
        ...rule,
        valueAccessor: rule.valueAccessor ?? import_get_subject_for_series.HISTOGRAM_DEFAULT_VALUE_ACCESSOR
      });
      continue;
    }
    let paletteIndex = 0;
    for (const series of seriesData) {
      const subject = (0, import_get_subject_for_series.getSubjectForSeries)(rule, series);
      if (!(0, import_lodash_es.isNil)(subject) && isValid(subject, rule.comparator, rule.matchValue)) {
        try {
          const colorToken = (0, import_get_color_palette.getColorPalette)(
            series.name,
            paletteIndex,
            rule.colorPalette
          );
          resolved.push({
            comparator: "equals",
            matchValue: series.name,
            color: colorToken.Default,
            valueAccessor: import_get_subject_for_series.HISTOGRAM_DEFAULT_VALUE_ACCESSOR
          });
        } catch {
        }
        paletteIndex++;
      }
    }
  }
  return resolved;
}
