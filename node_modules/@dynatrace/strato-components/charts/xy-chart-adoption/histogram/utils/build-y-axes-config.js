var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_y_axes_config_exports = {};
__export(build_y_axes_config_exports, {
  buildYAxesConfigFromConfig: () => buildYAxesConfigFromConfig,
  mapYAxisToInternal: () => mapYAxisToInternal
});
module.exports = __toCommonJS(build_y_axes_config_exports);
const mapYAxisToInternal = (yAxis, position) => {
  if (!yAxis) {
    return void 0;
  }
  const { position: _position, scale, ...rest } = yAxis;
  return {
    ...rest,
    type: scale === "log" ? "log" : "numerical",
    position
  };
};
const buildYAxesConfigFromConfig = (config) => {
  const left = mapYAxisToInternal(config.yAxis.left, "left");
  const right = mapYAxisToInternal(config.yAxis.right, "right");
  const result = {};
  if (left) {
    result.left = left;
  }
  if (right) {
    result.right = right;
  }
  return result;
};
