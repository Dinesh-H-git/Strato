var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var transformer_selection_to_histogram_exports = {};
__export(transformer_selection_to_histogram_exports, {
  mapSelectedSeriesToHistogram: () => mapSelectedSeriesToHistogram,
  mapSelectionDomainToHistogramDomains: () => mapSelectionDomainToHistogramDomains
});
module.exports = __toCommonJS(transformer_selection_to_histogram_exports);
var import_parse_date_number_to_number = require("../../core/components/annotations/utils/parse-date-number-to-number.js");
const toNumber = (value) => typeof value === "string" ? Number(value) : (0, import_parse_date_number_to_number.parseDateNumberToNumber)(value);
const mapDatapointToHistogramBin = (datapoint) => {
  const barData = "datapoint" in datapoint ? datapoint.datapoint : datapoint;
  const { x0, x1, y1 } = barData;
  return {
    from: x0,
    to: x1,
    value: y1
  };
};
const mapSelectedSeriesToHistogram = (selectedSeries) => {
  return selectedSeries.flatMap(
    (series) => Object.entries(series).map(([name, datapoints]) => {
      const bins = datapoints.map(
        (datapoint) => mapDatapointToHistogramBin(datapoint)
      );
      if (!bins.length) {
        return null;
      }
      return {
        name,
        bins
      };
    }).filter((series2) => series2 !== null)
  );
};
const mapSelectionDomainToHistogramDomains = (selectionDomain) => {
  const [[, firstDomain]] = Object.entries(selectionDomain[0]);
  return [toNumber(firstDomain[0]), toNumber(firstDomain[1])];
};
