var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var iterate_histogram_slots_exports = {};
__export(iterate_histogram_slots_exports, {
  iterateHistogramSlots: () => iterateHistogramSlots
});
module.exports = __toCommonJS(iterate_histogram_slots_exports);
var import_react = require("react");
var import_react_is = require("react-is");
var import_Intent = require("../../../core/slots/Intent/Intent.js");
var import_is_slot = require("../../../core/utils/is-slot.js");
var import_populate_missing_marker_props = require("../../core/components/annotations/utils/populate-missing-marker-props.js");
var import_populate_missing_track_props = require("../../core/components/annotations/utils/populate-missing-track-props.js");
var import_ColorRule = require("../../core/slots/ColorRule.js");
var import_ChartToolbar = require("../../core/slots/toolbar/ChartToolbar.js");
var import_Annotations = require("../../histogram/slots/annotations/Annotations.js");
var import_Marker = require("../../histogram/slots/annotations/Marker.js");
var import_Tooltip = require("../../histogram/slots/annotations/Tooltip.js");
var import_Track = require("../../histogram/slots/annotations/Track.js");
var import_Legend = require("../../histogram/slots/Legend.js");
var import_Pan = require("../../histogram/slots/Pan.js");
var import_Select = require("../../histogram/slots/Select.js");
var import_ThresholdIndicator = require("../../histogram/slots/ThresholdIndicator.js");
var import_Toolbar = require("../../histogram/slots/Toolbar.js");
var import_XAxis = require("../../histogram/slots/XAxis.js");
var import_YAxis = require("../../histogram/slots/YAxis.js");
var import_Zoom = require("../../histogram/slots/Zoom.js");
const iterateAnnotationsChildren = (children) => {
  const tracks = [];
  let tooltip;
  import_react.Children.forEach(children, (child) => {
    if ((0, import_is_slot.isSlot)(child, import_Track.Track)) {
      const { children: trackChildren, ...trackProps } = child.props;
      const currentTrack = (0, import_populate_missing_track_props.populateMissingTrackProps)({
        ...trackProps,
        markers: []
      });
      import_react.Children.forEach(trackChildren, (markerChild) => {
        if ((0, import_react_is.isElement)(markerChild) && (0, import_is_slot.isSlot)(markerChild, import_Marker.Marker)) {
          currentTrack.markers.push(
            (0, import_populate_missing_marker_props.populateMissingMarkerProps)(markerChild.props, trackProps)
          );
        }
      });
      tracks.push(currentTrack);
    } else if ((0, import_is_slot.isSlot)(child, import_Tooltip.AnnotationsTooltip)) {
      const { children: tooltipChildren, hidden } = child.props;
      tooltip = { template: tooltipChildren, hidden: hidden ?? false };
    }
  });
  return { tracks, tooltip };
};
const iterateHistogramSlots = (children) => {
  let disableZoom = false;
  let disablePan = false;
  let legend = {};
  let selectActions;
  let intents;
  const colorRules = [];
  const thresholds = [];
  const yAxis = [];
  let xAxis = {};
  let toolbar = {};
  let deprecatedToolbar = {};
  let annotations;
  import_react.Children.forEach(children, (child) => {
    if ((0, import_is_slot.isSlot)(child, import_Zoom.Zoom)) {
      disableZoom = child.props.disabled === true;
    } else if ((0, import_is_slot.isSlot)(child, import_Pan.Pan)) {
      disablePan = child.props.disabled === true;
    } else if ((0, import_is_slot.isSlot)(child, import_Legend.Legend)) {
      legend = { ...child.props };
    } else if ((0, import_is_slot.isSlot)(child, import_YAxis.YAxis)) {
      yAxis.push({
        ...child.props
      });
    } else if ((0, import_is_slot.isSlot)(child, import_XAxis.XAxis)) {
      xAxis = {
        ...child.props
      };
    } else if ((0, import_is_slot.isSlot)(child, import_Select.Select)) {
      selectActions = child.props.actions;
    } else if ((0, import_is_slot.isSlot)(child, import_ColorRule.ColorRule) && (0, import_ColorRule.isValidColorRule)(child.props)) {
      colorRules.push(child.props);
    } else if ((0, import_is_slot.isSlot)(child, import_Intent.Intent)) {
      intents = intents ? [...intents, child.props] : [child.props];
    } else if ((0, import_is_slot.isSlot)(child, import_Toolbar.Toolbar)) {
      toolbar = child.props;
    } else if ((0, import_is_slot.isSlot)(child, import_ChartToolbar.ChartToolbar)) {
      deprecatedToolbar = {
        enabled: true,
        ...child.props
      };
    } else if ((0, import_is_slot.isSlot)(child, import_ThresholdIndicator.ThresholdIndicator)) {
      thresholds.push(child.props);
    } else if ((0, import_is_slot.isSlot)(child, import_Annotations.Annotations)) {
      const {
        children: annotationsChildren,
        showLabels,
        visibleTracksLimit,
        annotationsActions
      } = child.props;
      const { tracks, tooltip } = iterateAnnotationsChildren(annotationsChildren);
      annotations = {
        tracks,
        tooltip,
        showLabels,
        visibleTracksLimit,
        annotationsActions
      };
    }
  });
  const interactionSlots = {
    disableZoom,
    disablePan,
    legend,
    yAxis,
    xAxis,
    colorRules,
    thresholds,
    toolbar,
    deprecatedToolbar
  };
  if (selectActions) {
    interactionSlots.selectActions = selectActions;
  }
  if (intents) {
    interactionSlots.intents = intents;
  }
  if (annotations) {
    interactionSlots.annotations = annotations;
  }
  return interactionSlots;
};
