import { type ReactNode } from 'react';
import type { ChartVariantInternal, TimeseriesBand } from './types/timeseries.js';
import type { Timeseries } from '../../core/types/timeseries.js';
export type SeriesSlots = {
    line: Timeseries[];
    area: Timeseries[];
    bar: Timeseries[];
    band: TimeseriesBand[];
    shapePriority: ChartVariantInternal[];
};
export declare function iterateSeriesSlots(children: ReactNode): SeriesSlots;
