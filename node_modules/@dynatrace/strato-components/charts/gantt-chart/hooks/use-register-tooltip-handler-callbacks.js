var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var use_register_tooltip_handler_callbacks_exports = {};
__export(use_register_tooltip_handler_callbacks_exports, {
  useRegisterTooltipHandlerCallbacks: () => useRegisterTooltipHandlerCallbacks
});
module.exports = __toCommonJS(use_register_tooltip_handler_callbacks_exports);
var import_react = require("react");
var import_use_set_tooltip_state = require("./use-set-tooltip-state.js");
const useRegisterTooltipHandlerCallbacks = (ref) => {
  const { setTooltipStatePinned, setTooltipStateVisibility } = (0, import_use_set_tooltip_state.useSetTooltipState)();
  const isTooltipPinned = (0, import_use_set_tooltip_state.useTooltipPinnedStatus)();
  const hasContent = !!(0, import_use_set_tooltip_state.useTooltipContent)();
  (0, import_react.useLayoutEffect)(() => {
    const unpinTooltip = () => setTooltipStatePinned(false);
    const hideTooltip = () => setTooltipStateVisibility(false);
    const handleKeyDown = (event) => {
      if (isTooltipPinned && event.key === "Escape") {
        unpinTooltip();
        !hasContent && hideTooltip();
      }
    };
    const handleOutsideClick = (event) => {
      const currentTarget = event.target;
      const refCurrent = ref.current;
      if (isTooltipPinned && refCurrent && !refCurrent.contains(currentTarget)) {
        unpinTooltip();
        !hasContent && hideTooltip();
      }
    };
    if (isTooltipPinned) {
      document.addEventListener("keydown", handleKeyDown);
      document.addEventListener("click", handleOutsideClick);
    }
    return () => {
      document.removeEventListener("keydown", handleKeyDown);
      document.removeEventListener("click", handleOutsideClick);
    };
  }, [
    hasContent,
    isTooltipPinned,
    ref,
    setTooltipStatePinned,
    setTooltipStateVisibility
  ]);
  (0, import_react.useEffect)(() => {
    return () => {
      setTooltipStatePinned(false);
      setTooltipStateVisibility(false);
    };
  }, [setTooltipStatePinned, setTooltipStateVisibility]);
};
