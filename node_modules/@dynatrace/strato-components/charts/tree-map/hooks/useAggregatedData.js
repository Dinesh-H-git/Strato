var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useAggregatedData_exports = {};
__export(useAggregatedData_exports, {
  useAggregatedData: () => useAggregatedData
});
module.exports = __toCommonJS(useAggregatedData_exports);
var import_lodash_es = require("lodash");
var import_react = require("react");
var import_constants = require("../constants.js");
var import_NameAccessorContext = require("../context/NameAccessorContext.js");
var import_build_aggregated_leaf = require("../utils/build-aggregated-leaf.js");
var import_is_below_threshold = require("../utils/is-below-threshold.js");
var import_node_aggregations = require("../utils/node-aggregations.js");
const useAggregatedData = (data, treemapArea, hasParentLabels, formatter) => {
  const nameAccessor = (0, import_NameAccessorContext.useNameAccessor)();
  const areaPerValue = treemapArea / data.value;
  const [clustersToAggregate, remainingClusters] = data.children.length > 2 ? (0, import_lodash_es.partition)(
    data.children,
    (node) => (0, import_is_below_threshold.isBelowThreshold)(
      node,
      areaPerValue,
      hasParentLabels ? import_constants.DEFAULT_MIN_NODE_SIZE_PARENT_AND_LABELS : import_constants.DEFAULT_MIN_NODE_SIZE_PARENT
    )
  ) : [[], data.children];
  const clustersWithAggregatedLeafs = (0, import_react.useMemo)(
    () => remainingClusters.map((node) => {
      return (0, import_build_aggregated_leaf.buildAggregatedLeaf)(
        node,
        areaPerValue,
        import_constants.DEFAULT_MIN_NODE_SIZE,
        formatter,
        nameAccessor
      );
    }),
    [remainingClusters, areaPerValue, formatter, nameAccessor]
  );
  const aggregatedClusters = clustersToAggregate.length > 0 ? [(0, import_node_aggregations.getAggregatedCluster)(clustersToAggregate, formatter)] : [];
  const dataForRendering = {
    ...data,
    children: [...clustersWithAggregatedLeafs, ...aggregatedClusters]
  };
  const dataForHighlighting = {
    ...data,
    children: [
      ...clustersWithAggregatedLeafs,
      ...clustersToAggregate,
      ...aggregatedClusters
    ]
  };
  return { dataForRendering, dataForHighlighting };
};
