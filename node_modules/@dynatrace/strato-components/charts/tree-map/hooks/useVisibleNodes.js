var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useVisibleNodes_exports = {};
__export(useVisibleNodes_exports, {
  useVisibleNodes: () => useVisibleNodes
});
module.exports = __toCommonJS(useVisibleNodes_exports);
var import_selectors = require("../store/selectors.js");
var import_get_filtered_nodes = require("../utils/get-filtered-nodes.js");
var import_get_nodes_value_sum = require("../utils/get-nodes-value-sum.js");
const useVisibleNodes = (data) => {
  const groupsSelected = (0, import_selectors.useGroupsSelectedState)();
  const selectedIds = groupsSelected.map((group) => group.id);
  const filteredNodes = (0, import_get_filtered_nodes.getFilteredNodes)(data.tree.children ?? [], selectedIds);
  const rootNodeValue = (0, import_get_nodes_value_sum.getNodesValueSum)(filteredNodes);
  return {
    ...data.tree,
    children: filteredNodes,
    value: rootNodeValue
  };
};
