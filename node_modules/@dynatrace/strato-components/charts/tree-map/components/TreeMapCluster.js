var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TreeMapCluster_exports = {};
__export(TreeMapCluster_exports, {
  TreeMapCluster: () => TreeMapCluster
});
module.exports = __toCommonJS(TreeMapCluster_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_useCurrentTheme = require("../../../core/hooks/useCurrentTheme.js");
var import_constants = require("../constants.js");
var import_TreeMapClusterBorder = require("./canvas/TreeMapClusterBorder.js");
var import_TreeMapNodeLabel = require("./canvas/TreeMapNodeLabel.js");
var import_get_cluster_position = require("../utils/get-cluster-position.js");
var import_get_node_color = require("../utils/get-node-color.js");
var import_get_node_dimension = require("../utils/get-node-dimension.js");
var import_get_node_opacity = require("../utils/get-node-opacity.js");
const TreeMapCluster = (props) => {
  const { treeMapData, highlightedItem, highlightingMap } = props;
  const theme = (0, import_useCurrentTheme.useCurrentTheme)();
  const clusterNodes = (0, import_react.useMemo)(
    () => treeMapData.descendants().reverse().filter((entry) => entry.depth === 1),
    [treeMapData]
  );
  return clusterNodes.map((clusterNode) => {
    const { x0, y0 } = (0, import_get_cluster_position.getClusterPosition)(clusterNode);
    const { width, height } = (0, import_get_node_dimension.getNodeDimension)(clusterNode);
    const labelColor = theme === "light" ? import_constants.DARK_LABEL_TEXT : import_constants.LIGHT_LABEL_TEXT;
    const borderColor = (0, import_get_node_color.getNodeColor)(clusterNode);
    const opacity = (0, import_get_node_opacity.getNodeOpacity)(
      clusterNode,
      highlightingMap,
      highlightedItem
    );
    const doesLabelFit = height >= import_constants.MIN_CLUSTER_HEIGHT;
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        import_TreeMapClusterBorder.TreeMapClusterBorder,
        {
          x: x0,
          y: y0,
          color: borderColor,
          width,
          height,
          opacity
        }
      ),
      doesLabelFit && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        import_TreeMapNodeLabel.TreeMapNodeLabel,
        {
          x: x0 + width / 2,
          y: y0 + import_constants.CLUSTER_LABEL_OFFSET_Y,
          maxWidth: width - import_constants.CLUSTER_LABEL_OFFSET_X,
          label: clusterNode.data.name,
          color: labelColor,
          opacity
        }
      )
    ] }, `${clusterNode.data.name}}`);
  });
};
