var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TreeMapSquareRenderer_exports = {};
__export(TreeMapSquareRenderer_exports, {
  TreeMapSquareRenderer: () => TreeMapSquareRenderer
});
module.exports = __toCommonJS(TreeMapSquareRenderer_exports);
var import_react = require("react");
var import_canvas_label_utils = require("./utils/canvas-label-utils.js");
var import_utils = require("../../../../typography/utils.js");
var import_useCanvas = require("../../../core/components/canvas/hooks/useCanvas.js");
var import_canvas_color = require("../../../core/components/canvas/utils/canvas-color.js");
var import_chars_font_width_map = require("../../../core/components/text-ellipsis/chars-font-width-map.js");
var import_truncate_string = require("../../../core/components/text-ellipsis/truncate-string.js");
var import_color_contrast = require("../../../core/utils/color-contrast.js");
var import_get_font_details = require("../../../core/utils/get-font-details.js");
var import_constants = require("../../constants.js");
var import_useTruncationMode = require("../../hooks/useTruncationMode.js");
const textAlign = import_constants.DEFAULT_LABEL_ALIGN;
const textBaseline = import_constants.DEFAULT_LABEL_BASELINE;
const fontSize = import_constants.DEFAULT_LABEL_FONTSIZE;
const fontWeight = import_constants.DEFAULT_FONT_WEIGHT;
const padding = import_constants.DEFAULT_LABEL_PADDING;
const lineHeight = import_constants.DEFAULT_LABEL_LINE_HEIGHT;
const boldFontWeight = import_constants.INCREASED_FONT_WEIGHT;
const clusterFontSize = import_constants.DEFAULT_CLUSTER_LABEL_FONTSIZE;
const clusterLineHeight = import_constants.DEFAULT_CLUSTER_LABEL_LINE_HEIGHT;
function TreeMapSquareRenderer(props) {
  const {
    x,
    y,
    width,
    height,
    color,
    opacity,
    name,
    value,
    skipDrawing = false,
    withLabel = false,
    withValueOnly = false
  } = props;
  const truncationMode = (0, import_useTruncationMode.useTruncationMode)();
  const draw = (0, import_react.useCallback)(
    (ctx) => {
      if (skipDrawing) {
        return false;
      }
      if (withLabel) {
        ctx.textAlign = textAlign;
        ctx.textBaseline = textBaseline;
      }
      ctx.moveTo(x, y);
      ctx.beginPath();
      ctx.globalAlpha = opacity;
      ctx.fillStyle = (0, import_canvas_color.getCanvasColor)(color);
      ctx.filter = "none";
      ctx.roundRect(x, y, width, height, import_constants.RECT_BORDER_RADIUS);
      ctx.fill();
      if (withLabel && withValueOnly) {
        ctx.fillStyle = (0, import_color_contrast.getColorByContrast)(ctx.fillStyle);
        ctx.font = (0, import_get_font_details.getFontDetails)(
          boldFontWeight,
          clusterFontSize,
          clusterLineHeight
        );
        const maxWidth = width - padding * 2;
        const minHeight = padding * 2 + clusterFontSize;
        const { centerX, centerY } = (0, import_canvas_label_utils.getRectCenter)(width, height);
        if (height >= minHeight && maxWidth > 0) {
          const truncatedValue = (0, import_truncate_string.truncateString)(
            value,
            maxWidth,
            import_chars_font_width_map.charsFontWidth12pixelsSize500Map,
            truncationMode
          );
          ctx.fillText(truncatedValue, x + centerX, y + centerY);
        }
      }
      if (withLabel && !withValueOnly) {
        ctx.fillStyle = (0, import_color_contrast.getColorByContrast)(ctx.fillStyle);
        ctx.font = (0, import_get_font_details.getFontDetails)(fontWeight, fontSize, lineHeight);
        const nameWidth = (0, import_truncate_string.calculateWidth)(
          name,
          import_chars_font_width_map.charsFontWidth12pixelsSize400Map
        );
        ctx.font = (0, import_get_font_details.getFontDetails)(
          boldFontWeight,
          clusterFontSize,
          clusterLineHeight
        );
        const labelWidth = (0, import_canvas_label_utils.getLabelWidth)(
          nameWidth,
          value,
          import_chars_font_width_map.charsFontWidth12pixelsSize500Map
        );
        const labelFit = (0, import_canvas_label_utils.getLabelFit)(
          height,
          width,
          fontSize,
          padding,
          labelWidth
        );
        const withEllipsis = labelFit === import_constants.LABEL_OPTIONS.Ellipsis || import_constants.LABEL_OPTIONS.EllipsisWithValue;
        const maxWidth = width - padding;
        const truncatedName = withEllipsis ? (0, import_truncate_string.truncateString)(
          name,
          maxWidth,
          import_chars_font_width_map.charsFontWidth12pixelsSize400Map,
          truncationMode,
          "start"
        ) : name;
        const displayedName = truncatedName === import_utils.ELLIPSIS_CHARACTER ? "" : truncatedName;
        const labels = (0, import_canvas_label_utils.getAppliedLabels)(labelFit, displayedName, value);
        const { centerX, centerY } = (0, import_canvas_label_utils.getRectCenter)(width, height);
        labels.forEach((label, i, array) => {
          const isValueLabel = i === 1;
          if (isValueLabel) {
            ctx.font = (0, import_get_font_details.getFontDetails)(
              boldFontWeight,
              clusterFontSize,
              clusterLineHeight
            );
          } else {
            ctx.font = (0, import_get_font_details.getFontDetails)(fontWeight, fontSize, lineHeight);
          }
          const offsetY = (0, import_canvas_label_utils.getOffsetY)(array, i, fontSize);
          ctx.fillText(label, x + centerX, y + centerY + offsetY);
        });
      }
      return false;
    },
    [
      color,
      height,
      name,
      opacity,
      skipDrawing,
      truncationMode,
      value,
      width,
      withLabel,
      withValueOnly,
      x,
      y
    ]
  );
  (0, import_useCanvas.useCanvas)("treemap-square", draw);
  return null;
}
TreeMapSquareRenderer["displayName"] = "TreeMapSquareRenderer";
