var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var TreeMapCanvas_exports = {};
__export(TreeMapCanvas_exports, {
  TreeMapCanvas: () => TreeMapCanvas
});
module.exports = __toCommonJS(TreeMapCanvas_exports);
var import_jsx_runtime = require("react/jsx-runtime");
var import_react = require("react");
var import_constants = require("../constants.js");
var import_TreeMapSquareRenderer = require("./canvas/TreeMapSquareRenderer.js");
var import_LabelsDisplayContext = require("../context/LabelsDisplayContext.js");
var import_useSetHierarchyNodes = require("../hooks/useSetHierarchyNodes.js");
var import_get_node_color = require("../utils/get-node-color.js");
var import_get_node_dimension = require("../utils/get-node-dimension.js");
var import_get_node_opacity = require("../utils/get-node-opacity.js");
var import_get_node_position = require("../utils/get-node-position.js");
var import_should_draw_leaf_node = require("../utils/should-draw-leaf-node.js");
var import_split_nodes_by_depth = require("../utils/split-nodes-by-depth.js");
var import_with_parent_label = require("../utils/with-parent-label.js");
const TreeMapCanvas = ({
  treeMapData,
  highlightedItem,
  highlightingMap
}) => {
  const labelsDisplay = (0, import_LabelsDisplayContext.useLabelsDisplay)();
  const nodes = (0, import_react.useMemo)(
    () => treeMapData.descendants().reverse(),
    [treeMapData]
  );
  const { clusters, leafs } = (0, import_react.useMemo)(() => (0, import_split_nodes_by_depth.splitNodesByDepth)(nodes), [nodes]);
  (0, import_useSetHierarchyNodes.useSetHierarchyNodes)(clusters, leafs);
  const labelsToDisplay = [
    import_constants.LABELS_DISPLAY.Nodes,
    import_constants.LABELS_DISPLAY.All,
    import_constants.LABELS_DISPLAY.ClustersAndValues
  ];
  const showLabel = labelsToDisplay.includes(labelsDisplay);
  const withValueOnly = labelsDisplay === import_constants.LABELS_DISPLAY.ClustersAndValues;
  const squares = leafs.map((node) => {
    const { x, y } = (0, import_get_node_position.getNodePosition)(node);
    const { width, height } = (0, import_get_node_dimension.getNodeDimension)(node);
    const color = (0, import_get_node_color.getNodeColor)(node);
    const opacity = (0, import_get_node_opacity.getNodeOpacity)(node, highlightingMap, highlightedItem);
    const shouldDraw = (0, import_should_draw_leaf_node.shouldDrawLeafNode)(node);
    const hasParentLabels = (0, import_with_parent_label.withParentLabel)(labelsDisplay);
    const skipDrawing = !shouldDraw && hasParentLabels;
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      import_TreeMapSquareRenderer.TreeMapSquareRenderer,
      {
        x,
        y,
        width,
        height,
        color,
        opacity,
        name: node.data.name,
        value: node.data.formattedValue ?? "",
        skipDrawing,
        withLabel: showLabel,
        withValueOnly
      },
      node.data.id
    );
  });
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: squares });
};
