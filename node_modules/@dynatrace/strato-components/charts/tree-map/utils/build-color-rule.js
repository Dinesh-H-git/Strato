var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_color_rule_exports = {};
__export(build_color_rule_exports, {
  buildColorRangeLegendRule: () => buildColorRangeLegendRule,
  buildColorRule: () => buildColorRule
});
module.exports = __toCommonJS(build_color_rule_exports);
var import_lodash_es = require("lodash");
var import_is_valid_range_color_rule = require("./is-valid-range-color-rule.js");
var import_get_color_palette = require("../../core/utils/get-color-palette.js");
var import_getAccessor = require("../../core/utils/getAccessor.js");
var import_getValidColor = require("../../core/utils/getValidColor.js");
var import_validators = require("../../core/utils/validators.js");
var import_constants = require("../constants.js");
const getRuleColor = (rule, subject) => rule.colorPalette ? (0, import_get_color_palette.getColorPalette)(`${JSON.stringify(subject)}`, 0, rule.colorPalette).Default : (0, import_getValidColor.getValidColor)(rule.color ?? "");
const getSubject = (rule, node, defaultValueAccessor) => {
  const valueAccessor = rule.valueAccessor;
  if (!valueAccessor) {
    return defaultValueAccessor === import_constants.NAME_ACCESSOR.Value ? node.value : node.name;
  }
  if (valueAccessor === import_constants.NAME_ACCESSOR.Name) {
    return node.name;
  }
  if (valueAccessor === import_constants.NAME_ACCESSOR.Value) {
    return node.value;
  }
  const accessor = (0, import_getAccessor.getAccessor)(valueAccessor);
  if (accessor) {
    const value = (0, import_lodash_es.get)(node, accessor);
    if (!(0, import_lodash_es.isNil)(value)) {
      return value;
    }
  }
  return null;
};
const buildColorRule = (node, colorRules, defaultValueAccessor) => {
  if (!colorRules || colorRules.length === 0) {
    return void 0;
  }
  const isValid = (0, import_validators.buildValidator)(import_validators.VALUE_MATCHERS);
  for (let index = colorRules.length - 1; index >= 0; index--) {
    const rule = colorRules[index];
    const subject = getSubject(rule, node, defaultValueAccessor);
    if (!(0, import_lodash_es.isNil)(subject) && isValid(subject, rule.comparator, rule.matchValue)) {
      return getRuleColor(rule, subject);
    }
  }
  return void 0;
};
const buildColorRangeLegendRule = (legendColorMap, colorRules) => {
  if (!colorRules || colorRules.length === 0) {
    return void 0;
  }
  const colorRuleMap = /* @__PURE__ */ new Map();
  colorRules.forEach((rule) => {
    legendColorMap.forEach((_, key) => {
      const range = (0, import_lodash_es.pick)(key, ["from", "to"]);
      if (!(0, import_lodash_es.isNil)(range) && (0, import_is_valid_range_color_rule.isValidRangeColorRule)(range, rule)) {
        const color = getRuleColor(rule, range);
        colorRuleMap.set(key, {
          color: { Default: color }
        });
      }
    });
  });
  return {
    for: (range) => {
      return colorRuleMap.get(range);
    }
  };
};
