var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_tree_map_chart_config_exports = {};
__export(build_tree_map_chart_config_exports, {
  buildTreeMapChartConfig: () => buildTreeMapChartConfig,
  getLegendConfiguration: () => getLegendConfiguration
});
module.exports = __toCommonJS(build_tree_map_chart_config_exports);
var import_lodash_es = require("lodash");
var import_ColorRule = require("../../core/slots/ColorRule.js");
var import_defaultUnitFormatter = require("../../core/utils/defaultUnitFormatter.js");
var import_is_valid_legend_position = require("../../core/utils/is-valid-legend-position.js");
var import_defaults = require("../defaults.js");
const validateToolbarContextConfig = (contextConfig) => !(0, import_lodash_es.isUndefined)(contextConfig) && contextConfig.downloadData?.enabled;
const getToolbarConfiguration = (contextConfig, slots) => {
  const hasValidToolbarConfig = validateToolbarContextConfig(contextConfig) || !(0, import_lodash_es.isUndefined)(slots);
  return hasValidToolbarConfig ? (0, import_lodash_es.merge)({}, contextConfig, slots) : void 0;
};
function getLegendConfiguration(contextLegendConfig, slotsLegendConfig) {
  let legend = (0, import_lodash_es.merge)(
    {},
    import_defaults.defaultTreeMapConfig.legend,
    contextLegendConfig,
    slotsLegendConfig
  );
  const position = legend.position && (0, import_is_valid_legend_position.isValidLegendPosition)(legend.position) ? legend.position : import_defaults.defaultTreeMapConfig.legend.position;
  legend = {
    ...legend,
    position
  };
  return legend;
}
const buildTreeMapChartConfig = (props, config, slots) => {
  const colorPalette = props.colorPalette ?? config.colorPalette ?? import_defaults.defaultTreeMapConfig.colorPalette;
  const formatter = props.formatter ?? config.formatter ?? (0, import_defaultUnitFormatter.defaultUnitFormatter)(props.unit ?? props.data?.unit);
  const truncationMode = props.truncationMode ?? config.truncationMode ?? import_defaults.defaultTreeMapConfig.truncationMode;
  const loading = props.loading ?? config.loading ?? import_defaults.defaultTreeMapConfig.loading;
  const labelsDisplay = props.labelsDisplay ?? config.labelsDisplay ?? import_defaults.defaultTreeMapConfig.labelsDisplay;
  const nameAccessor = props.nameAccessor ?? config.nameAccessor ?? import_defaults.defaultTreeMapConfig.nameAccessor;
  const min = props.min ?? config.min;
  const max = props.max ?? config.max;
  const legend = getLegendConfiguration(config.legend, slots.legend);
  const toolbar = getToolbarConfiguration(config.toolbar, slots.toolbar);
  const colorRules = (config.colorRules ?? []).filter(import_ColorRule.isValidColorRule).concat(slots.colorRules?.filter(import_ColorRule.isValidColorRule) ?? []);
  return {
    colorPalette,
    formatter,
    truncationMode,
    loading,
    legend,
    toolbar,
    labelsDisplay,
    nameAccessor,
    min,
    max,
    colorRules,
    tooltip: slots.tooltip ?? config.tooltip
  };
};
