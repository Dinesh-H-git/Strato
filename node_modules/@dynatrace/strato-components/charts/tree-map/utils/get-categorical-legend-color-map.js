var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_categorical_legend_color_map_exports = {};
__export(get_categorical_legend_color_map_exports, {
  getCategoricalLegendColorMap: () => getCategoricalLegendColorMap
});
module.exports = __toCommonJS(get_categorical_legend_color_map_exports);
var import_build_color_rule = require("./build-color-rule.js");
var import_get_color_palette = require("../../core/utils/get-color-palette.js");
const getCategoricalLegendColorMap = (rootNode, colorPalette, nameAccessor, colorRules) => {
  const legendColorMap = /* @__PURE__ */ new Map();
  let colorIdx = 0;
  const legendTarget = nameAccessor ?? "name";
  const legendColorRules = colorRules.filter(
    (rule) => !rule.valueAccessor || rule.valueAccessor === legendTarget
  );
  rootNode.children?.forEach((cluster) => {
    if (nameAccessor) {
      cluster.children?.forEach((node) => {
        const labelKey = node[nameAccessor];
        const ruleColor = (0, import_build_color_rule.buildColorRule)(node, legendColorRules, nameAccessor);
        if (!legendColorMap.has(labelKey)) {
          const colorToken = ruleColor ? { Default: ruleColor } : (0, import_get_color_palette.getColorPalette)(labelKey, colorIdx, colorPalette);
          legendColorMap.set(labelKey, { color: colorToken });
          colorIdx++;
        }
      });
    } else {
      const labelKey = cluster.name;
      const ruleColor = (0, import_build_color_rule.buildColorRule)(cluster, legendColorRules);
      if (!legendColorMap.has(labelKey)) {
        const colorToken = ruleColor ? { Default: ruleColor } : (0, import_get_color_palette.getColorPalette)(labelKey, colorIdx, colorPalette);
        legendColorMap.set(labelKey, {
          color: colorToken,
          value: cluster.value
        });
        colorIdx++;
      }
    }
  });
  return legendColorMap;
};
