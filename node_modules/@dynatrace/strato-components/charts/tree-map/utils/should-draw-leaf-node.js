var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var should_draw_leaf_node_exports = {};
__export(should_draw_leaf_node_exports, {
  shouldDrawLeafNode: () => shouldDrawLeafNode
});
module.exports = __toCommonJS(should_draw_leaf_node_exports);
var import_constants = require("../constants.js");
const shouldDrawLeafNode = (leaf) => {
  const { y0 = 0, y1 = 0, x0 = 0, x1 = 0 } = leaf.parent ?? {};
  const nodeHeight = Math.abs(y1 - y0);
  const nodeWidth = Math.abs(x1 - x0);
  const fitHeight = nodeHeight + import_constants.RECT_STROKE_WIDTH >= import_constants.MIN_CLUSTER_HEIGHT_WITH_VISIBLE_CHILDREN;
  const fitWidth = nodeWidth + import_constants.RECT_STROKE_WIDTH >= import_constants.MIN_CLUSTER_WIDTH_WITH_VISIBLE_CHILDREN;
  return fitHeight && fitWidth;
};
