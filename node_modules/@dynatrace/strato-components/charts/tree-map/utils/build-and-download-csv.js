var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_and_download_csv_exports = {};
__export(build_and_download_csv_exports, {
  buildAndDownloadCsv: () => buildAndDownloadCsv
});
module.exports = __toCommonJS(build_and_download_csv_exports);
var import_format_csv = require("./format-csv.js");
var import_constants = require("../../../charts/core/constants.js");
var import_download = require("../../../core/utils/download.js");
const getRootNode = (name, value) => {
  const rootName = name ? `${name} - (Root)` : "Root";
  return { name: rootName, value, parent: "-" };
};
const getNodeList = (tree) => {
  const rootNode = getRootNode(tree?.name, tree.value);
  const nodes = [rootNode];
  tree.nodes?.forEach((cluster) => {
    nodes.push({ name: cluster.name, value: cluster.value, parent: "Root" });
    cluster.nodes?.forEach((leaf) => {
      nodes.push({ name: leaf.name, value: leaf.value, parent: cluster.name });
    });
  });
  return nodes;
};
const buildAndDownloadCsv = (tree) => {
  const nodeList = getNodeList(tree);
  const csvString = (0, import_format_csv.formatCSV)(nodeList);
  (0, import_download.downloadCSV)(csvString, import_constants.CSS_DOWNLOAD_FILENAME);
};
