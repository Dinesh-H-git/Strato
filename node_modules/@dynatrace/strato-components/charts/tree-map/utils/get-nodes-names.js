var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_nodes_names_exports = {};
__export(get_nodes_names_exports, {
  getNodeNames: () => getNodeNames
});
module.exports = __toCommonJS(get_nodes_names_exports);
var import_constants = require("../constants.js");
var import_node_guards = require("../types/guards/node-guards.js");
const getNodeNames = (node, nameAccessor, clusterName) => {
  const nodeName = node.name;
  const parentName = (0, import_node_guards.isLeaf)(node) && clusterName;
  const isOtherCluster = node.id === import_constants.CLUSTER_OTHER_ID;
  if (isOtherCluster) {
    return /* @__PURE__ */ new Set([...node.aggregatedNodes ?? []]);
  }
  const names = [nodeName];
  parentName && names.push(parentName);
  const leafNodes = (0, import_node_guards.isCluster)(node) ? node.children ?? [] : [node];
  const leafNames = nameAccessor && typeof nameAccessor === "string" ? leafNodes.map((leaf) => leaf[nameAccessor]) : [];
  const aggregatedNodeNames = leafNodes.flatMap(
    (leaf) => leaf.aggregatedNodes ?? []
  );
  return /* @__PURE__ */ new Set([
    ...names,
    ...leafNames,
    ...aggregatedNodeNames
  ]);
};
