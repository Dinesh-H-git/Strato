var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_highlighting_map_exports = {};
__export(get_highlighting_map_exports, {
  getHighlightingMap: () => getHighlightingMap
});
module.exports = __toCommonJS(get_highlighting_map_exports);
var import_constants = require("../constants.js");
var import_get_nodes_names = require("./get-nodes-names.js");
const getAggregatedClusterLeafNames = (map) => {
  const aggregatedClusterLeafNames = new Set(map[import_constants.CLUSTER_OTHER_ID]);
  map[import_constants.CLUSTER_OTHER_ID].forEach(
    (cluster) => map[`cluster-${cluster}`].forEach(
      (leaf) => aggregatedClusterLeafNames.add(leaf)
    )
  );
  return aggregatedClusterLeafNames;
};
const getHighlightingMap = (data, nameAccessor) => {
  const map = {};
  data.children.forEach((cluster) => {
    const clusterId = cluster.id;
    const clusterName = cluster.name;
    map[clusterId] = (0, import_get_nodes_names.getNodeNames)(cluster, nameAccessor, clusterName);
    cluster.children?.forEach((leaf) => {
      const leafId = leaf.id;
      if (leafId === import_constants.CLUSTER_OTHER_ID) {
        return;
      }
      map[leafId] = (0, import_get_nodes_names.getNodeNames)(leaf, nameAccessor, clusterName);
    });
  });
  if (map[import_constants.CLUSTER_OTHER_ID]) {
    map[import_constants.CLUSTER_OTHER_ID] = getAggregatedClusterLeafNames(map);
  }
  return map;
};
