var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var format_csv_exports = {};
__export(format_csv_exports, {
  formatCSV: () => formatCSV
});
module.exports = __toCommonJS(format_csv_exports);
var import_download = require("../../../core/utils/download.js");
var import_sanitize_and_parse_data = require("../../core/utils/sanitize-and-parse-data.js");
function formatCSV(data) {
  const columns = ["name", "value", "parent"];
  let csv = `${columns.join(",")}
`;
  for (const node of data) {
    const sanitizedName = (0, import_download.escapeForCSV)(node.name);
    const sanitizedValue = (0, import_sanitize_and_parse_data.sanitizeAndParseData)(node.value);
    const sanitizedParent = (0, import_download.escapeForCSV)(node.parent);
    csv += `${sanitizedName},${sanitizedValue},${sanitizedParent}`;
    csv += "\n";
  }
  return csv;
}
