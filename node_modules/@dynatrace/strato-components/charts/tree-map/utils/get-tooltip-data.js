var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_tooltip_data_exports = {};
__export(get_tooltip_data_exports, {
  getTooltipData: () => getTooltipData
});
module.exports = __toCommonJS(get_tooltip_data_exports);
var import_node_padding = require("./node-padding.js");
var import_should_draw_leaf_node = require("./should-draw-leaf-node.js");
const getTooltipData = (leafNodes, clusterNodes, relativePosition, hasParentLabels = false) => {
  const nodes = hasParentLabels ? [...leafNodes, ...clusterNodes] : leafNodes;
  return nodes.find((node) => {
    const padding = (0, import_node_padding.nodePadding)(node, hasParentLabels);
    const showLeafTooltip = (0, import_should_draw_leaf_node.shouldDrawLeafNode)(node);
    if (!showLeafTooltip && hasParentLabels) {
      return null;
    }
    return node.x0 - padding <= relativePosition.x && node.x1 + padding >= relativePosition.x && node.y0 - padding <= relativePosition.y && node.y1 + padding >= relativePosition.y;
  });
};
