var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_colored_internal_range_data_exports = {};
__export(get_colored_internal_range_data_exports, {
  getColoredInternalRangeData: () => getColoredInternalRangeData
});
module.exports = __toCommonJS(get_colored_internal_range_data_exports);
var import_lodash_es = require("lodash");
var import_build_color_rule = require("./build-color-rule.js");
var import_get_data_extent = require("./get-data-extent.js");
var import_get_range_colored_nodes = require("./get-range-colored-nodes.js");
var import_get_range_legend_color_map = require("./get-range-legend-color-map.js");
var import_get_range_legend_with_color_rules = require("./get-range-legend-with-color-rules.js");
var import_build_quantize_scale_from_color_palette = require("../../core/utils/build-quantize-scale-from-color-palette.js");
var import_build_threshold_scale_from_ranges = require("../../core/utils/build-threshold-scale-from-ranges.js");
var import_is_valid_color_palette = require("../../core/utils/is-valid-color-palette.js");
var import_constants = require("../constants.js");
function getColoredInternalRangeData(tree, colorPalette, minMaxConfig, colorRules, unit) {
  const hasCustomRanges = !(0, import_is_valid_color_palette.isValidColorPalette)(colorPalette);
  let { min, max } = minMaxConfig;
  if ((0, import_lodash_es.isUndefined)(min) || (0, import_lodash_es.isUndefined)(max)) {
    const [extentMin, extentMax] = (0, import_get_data_extent.getDataExtent)(tree);
    min ??= extentMin;
    max ??= extentMax;
  }
  const scale = !hasCustomRanges ? (0, import_build_quantize_scale_from_color_palette.buildQuantizeScaleFromColorPalette)(min, max, colorPalette) : (0, import_build_threshold_scale_from_ranges.buildThresholdScaleFromRanges)(
    colorPalette,
    import_constants.DEFAULT_RANGE_LEGEND_COLOR,
    minMaxConfig
  );
  const baseLegendColorMap = (0, import_get_range_legend_color_map.getRangeLegendColorMap)(
    scale,
    min,
    max,
    hasCustomRanges
  );
  const colorRulesMap = (0, import_build_color_rule.buildColorRangeLegendRule)(
    baseLegendColorMap,
    colorRules
  );
  const legendColorMap = (0, import_get_range_legend_with_color_rules.getRangeLegendWithColorRules)(
    baseLegendColorMap,
    colorRulesMap
  );
  const nodeArray = (0, import_get_range_colored_nodes.getRangeColoredNodes)(tree, scale, min, max, colorRules);
  const rootNodeName = tree.name ?? import_constants.DEFAULT_ROOT_NODE_NAME;
  const data = {
    tree: {
      ...tree,
      children: nodeArray,
      name: rootNodeName,
      id: import_constants.DEFAULT_ROOT_NODE_NAME
    },
    unit
  };
  return { data, legendColorMap };
}
