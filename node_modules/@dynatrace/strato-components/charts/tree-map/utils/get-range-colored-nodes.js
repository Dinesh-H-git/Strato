var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_range_colored_nodes_exports = {};
__export(get_range_colored_nodes_exports, {
  getRangeColoredNodes: () => getRangeColoredNodes
});
module.exports = __toCommonJS(get_range_colored_nodes_exports);
var import_build_color_rule = require("./build-color-rule.js");
var import_getTokenComputedStyle = require("../../core/utils/getTokenComputedStyle.js");
var import_constants = require("../constants.js");
const getColoredLeafNodes = (nodes, scale, min, max, colorRules) => nodes.map((node) => {
  const ruleColor = (0, import_build_color_rule.buildColorRule)(node, colorRules, "value");
  return {
    ...node,
    color: { Default: ruleColor ?? getNodeColor(node, scale, min, max) }
  };
});
const getNodeColor = (node, scale, min, max) => {
  if (node.value >= min && node.value <= max) {
    return (0, import_getTokenComputedStyle.getTokenComputedStyle)(scale(node.value));
  }
  return import_constants.OUT_OF_RANGE_NODE_COLOR;
};
const getRangeColoredNodes = (rootNode, scale, min, max, colorRules) => rootNode.children?.map((clusterNode) => ({
  ...clusterNode,
  children: getColoredLeafNodes(
    clusterNode.children || [],
    scale,
    min,
    max,
    colorRules
  )
}));
