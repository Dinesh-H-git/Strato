var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_tree_map_color_palette_exports = {};
__export(get_tree_map_color_palette_exports, {
  getTreeMapColorPalette: () => getTreeMapColorPalette
});
module.exports = __toCommonJS(get_tree_map_color_palette_exports);
var import_lodash_es = require("lodash");
var import_color_palette = require("../../core/types/color-palette.js");
var import_constants = require("../constants.js");
const categoricalPalettes = /* @__PURE__ */ new Set([
  ...import_color_palette.themedColoringOptions,
  ...import_color_palette.automaticColoringOptions
]);
const rangePalettes = /* @__PURE__ */ new Set([
  ...import_color_palette.sequentialColoringOptions,
  ...import_color_palette.divergingColoringOptions
]);
const isCategoricalColorPalette = (colorPalette) => {
  return (0, import_lodash_es.isString)(colorPalette) && categoricalPalettes.has(colorPalette);
};
const isRangeColorPalette = (colorPalette) => {
  return (0, import_lodash_es.isString)(colorPalette) && rangePalettes.has(colorPalette);
};
const isCustomRanges = (colorPalette) => {
  return Array.isArray(colorPalette) && !!colorPalette.length && colorPalette.every(
    (color) => typeof color === "object" && "from" in color && "to" in color
  );
};
const isCustomColoredArray = (colorPalette) => Array.isArray(colorPalette) && colorPalette.length && colorPalette.every((color) => typeof color === "string");
const getTreeMapColorPalette = (colorPalette) => {
  const isCategoricalPalette = isCategoricalColorPalette(colorPalette);
  const isRangePalette = isRangeColorPalette(colorPalette);
  const isValidPalette = isCategoricalPalette || isRangePalette;
  const isCustomRangePalette = isCustomRanges(colorPalette);
  const isCustomCategoricalPalette = !isCustomRangePalette && !Array.isArray(colorPalette) && (0, import_color_palette.isCustomColorPalette)(colorPalette);
  const isValidRangePalette = isValidPalette || isCustomRangePalette;
  const isValidCategoricalPalette = isValidPalette || isCustomCategoricalPalette || isCustomColoredArray(colorPalette);
  const categoricalColorPalette = isValidCategoricalPalette ? colorPalette : import_constants.DEFAULT_CATEGORICAL_COLOR_PALETTE;
  const rangeColorPalette = isValidRangePalette ? colorPalette : import_constants.DEFAULT_RANGE_COLOR_PALETTE;
  return { categoricalColorPalette, rangeColorPalette };
};
