var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_sorted_tree_exports = {};
__export(get_sorted_tree_exports, {
  getSortedTree: () => getSortedTree
});
module.exports = __toCommonJS(get_sorted_tree_exports);
const sortAscending = (a, b) => a.value - b.value;
const sortDescending = (a, b) => b.value - a.value;
const sortNodes = (nodes, order) => {
  const sortFunction = order === "asc" ? sortAscending : sortDescending;
  return nodes.sort(sortFunction);
};
const getSortedTree = (rootNode) => {
  const clusters = sortNodes(
    rootNode.children ?? [],
    "asc"
  );
  const children = clusters.map((cluster) => {
    const { children: children2, ...clusterRest } = cluster;
    return {
      ...clusterRest,
      children: sortNodes(cluster.children ?? [], "desc")
    };
  });
  return { ...rootNode, children };
};
