var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_range_legend_color_map_exports = {};
__export(get_range_legend_color_map_exports, {
  getRangeLegendColorMap: () => getRangeLegendColorMap
});
module.exports = __toCommonJS(get_range_legend_color_map_exports);
var import_getTokenComputedStyle = require("../../core/utils/getTokenComputedStyle.js");
const getRangeLegendColorMap = (scale, min, max, hasCustomRanges) => {
  const legendColorMap = /* @__PURE__ */ new Map();
  const ranges = hasCustomRanges ? scale.domain() : [min, ...scale.thresholds(), max];
  for (let i = 0; i < ranges.length - 1; i++) {
    const mapKey = { from: ranges[i], to: ranges[i + 1] };
    const colorToken = { Default: (0, import_getTokenComputedStyle.getTokenComputedStyle)(scale(ranges[i])) };
    legendColorMap.set(mapKey, { color: colorToken });
  }
  return legendColorMap;
};
