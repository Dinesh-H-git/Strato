import type { TreeMapRootNode } from '../types/treemap.js';
/**
 * Clusters single leaf nodes into a cluster node
 * @param tree
 */
export declare function clusterizeNodes(tree: TreeMapRootNode): TreeMapRootNode;
