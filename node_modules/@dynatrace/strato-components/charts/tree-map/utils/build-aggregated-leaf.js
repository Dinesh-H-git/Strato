var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_aggregated_leaf_exports = {};
__export(build_aggregated_leaf_exports, {
  buildAggregatedLeaf: () => buildAggregatedLeaf
});
module.exports = __toCommonJS(build_aggregated_leaf_exports);
var import_lodash_es = require("lodash");
var import_is_below_threshold = require("./is-below-threshold.js");
var import_node_aggregations = require("../utils/node-aggregations.js");
const buildAggregatedLeaf = (cluster, areaPerValue, minNodeSize, formatter, nameAccessor) => {
  if (cluster.children.length === 1) {
    return cluster;
  }
  const [nodesToAggregate, remainingNodes] = (0, import_lodash_es.partition)(
    cluster.children,
    (node) => (0, import_is_below_threshold.isBelowThreshold)(node, areaPerValue, minNodeSize)
  );
  const singleChildCluster = cluster.children.length === 1;
  if (!nodesToAggregate.length || singleChildCluster) {
    return cluster;
  }
  const clusterName = cluster.name;
  const clusterColorToken = cluster.color;
  const aggregatedNode = (0, import_node_aggregations.getAggregatedLeaf)(
    nodesToAggregate,
    clusterName,
    formatter,
    clusterColorToken,
    nameAccessor
  );
  if ((0, import_is_below_threshold.isBelowThreshold)(aggregatedNode, areaPerValue, minNodeSize)) {
    const smallestRemainingNode = remainingNodes.pop() ?? {
      name: "",
      value: 0,
      ...nameAccessor && { [nameAccessor]: "" }
    };
    aggregatedNode.value += smallestRemainingNode.value;
    nameAccessor && aggregatedNode.aggregatedNodes?.push(
      smallestRemainingNode[nameAccessor]
    );
  }
  return { ...cluster, children: [...remainingNodes, aggregatedNode] };
};
