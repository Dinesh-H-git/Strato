var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useGaugeChartMinMaxLabelsPosition_exports = {};
__export(useGaugeChartMinMaxLabelsPosition_exports, {
  useGaugeChartMinMaxLabelsPosition: () => useGaugeChartMinMaxLabelsPosition
});
module.exports = __toCommonJS(useGaugeChartMinMaxLabelsPosition_exports);
var import_constants = require("../constants.js");
var import_useGaugeChartAspectRatio = require("./useGaugeChartAspectRatio.js");
var import_useGaugeChartSizeContext = require("./useGaugeChartSizeContext.js");
var import_polar_to_cartesian = require("../utils/polar-to-cartesian.js");
const useGaugeChartMinMaxLabelsPosition = () => {
  const { arcMiddleRadius } = (0, import_useGaugeChartAspectRatio.useGaugeChartAspectRatio)();
  const dimensions = (0, import_useGaugeChartSizeContext.useGaugeChartSizeContext)();
  const width = dimensions?.width ?? 0;
  const height = dimensions?.height ?? 0;
  const minLabelPosition = (0, import_polar_to_cartesian.polarToCartesian)(arcMiddleRadius, import_constants.ARC_START_ANGLE);
  const maxLabelPosition = (0, import_polar_to_cartesian.polarToCartesian)(arcMiddleRadius, import_constants.ARC_END_ANGLE);
  const verticalOffset = height > width ? height * import_constants.GAUGE_VERTICAL_SMALL_OFFSET : height * import_constants.GAUGE_VERTICAL_DEFAULT_OFFSET;
  const availableAfterArcEnd = height / 2 - verticalOffset - maxLabelPosition.y;
  const minMaxPadding = Math.min(
    import_constants.MIN_MAX_PADDING,
    Math.max(0, availableAfterArcEnd - import_constants.MIN_MAX_LABEL_RESERVED_HEIGHT_IN_PX)
  );
  return {
    minLabelPosition,
    maxLabelPosition,
    minMaxPadding
  };
};
