var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useGaugeChartTooltipOverlayBounds_exports = {};
__export(useGaugeChartTooltipOverlayBounds_exports, {
  useGaugeChartTooltipOverlayBounds: () => useGaugeChartTooltipOverlayBounds
});
module.exports = __toCommonJS(useGaugeChartTooltipOverlayBounds_exports);
var import_constants = require("../constants.js");
var import_useGaugeChartSizeContext = require("./useGaugeChartSizeContext.js");
const ARC_ENDPOINT_Y_FACTOR = -Math.cos(import_constants.ARC_END_ANGLE);
const LABEL_HEIGHT_BUFFER = 20;
const useGaugeChartTooltipOverlayBounds = () => {
  const dimensions = (0, import_useGaugeChartSizeContext.useGaugeChartSizeContext)();
  if (!dimensions?.width || !dimensions?.height) {
    return void 0;
  }
  const { width, height } = dimensions;
  const verticalOffset = height > width ? height * import_constants.GAUGE_VERTICAL_SMALL_OFFSET : height * import_constants.GAUGE_VERTICAL_DEFAULT_OFFSET;
  const arcOuterRadius = Math.min(width, height) / 2 * import_constants.ARC_OUTER_RADIUS_PERCENTAGE;
  const centerY = height / 2 + verticalOffset;
  return {
    top: centerY - arcOuterRadius,
    left: width / 2 - arcOuterRadius,
    width: 2 * arcOuterRadius,
    height: arcOuterRadius * (1 + ARC_ENDPOINT_Y_FACTOR) + import_constants.MIN_MAX_PADDING + LABEL_HEIGHT_BUFFER
  };
};
