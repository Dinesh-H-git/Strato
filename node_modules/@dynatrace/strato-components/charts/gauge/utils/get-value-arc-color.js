var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var get_value_arc_color_exports = {};
__export(get_value_arc_color_exports, {
  getValueArcColor: () => getValueArcColor
});
module.exports = __toCommonJS(get_value_arc_color_exports);
var import_access_value = require("../../../core/utils/accessors/access-value.js");
var import_apply_color_rule_to_single_value = require("../../core/components/single-value-renderer/utils/apply-color-rule-to-single-value.js");
var import_constants = require("../../core/constants.js");
var import_find_matching_threshold = require("../../core/utils/find-matching-threshold.js");
var import_constants2 = require("../constants.js");
const getValueArcColor = (data, colorRules, explicitColor, thresholds, valueAccessor) => {
  const displayValue = typeof data === "number" ? data : (0, import_access_value.accessValue)(data, valueAccessor ?? import_constants.DEFAULT_VALUE_ACCESSOR) ?? 0;
  if (colorRules && colorRules.length > 0) {
    const ruleColor = (0, import_apply_color_rule_to_single_value.applyColorRuleToSingleValue)(
      colorRules,
      displayValue,
      typeof data === "number" ? {} : data
    );
    if (ruleColor) {
      return ruleColor;
    }
  }
  if (thresholds && thresholds.length > 0) {
    const matchingThreshold = (0, import_find_matching_threshold.findMatchingThreshold)(displayValue, thresholds);
    if (matchingThreshold?.color) {
      return matchingThreshold.color;
    }
  }
  if (explicitColor) {
    return explicitColor;
  }
  return import_constants2.ARC_DEFAULT_VALUE_COLOR;
};
