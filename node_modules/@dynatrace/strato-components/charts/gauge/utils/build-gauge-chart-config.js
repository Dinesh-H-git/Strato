var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var build_gauge_chart_config_exports = {};
__export(build_gauge_chart_config_exports, {
  buildGaugeChartConfig: () => buildGaugeChartConfig
});
module.exports = __toCommonJS(build_gauge_chart_config_exports);
var import_constants = require("../constants.js");
var import_apply_threshold_default = require("./apply-threshold-default.js");
var import_ColorRule = require("../../core/slots/ColorRule.js");
var import_CompactToolbar = require("../../core/slots/toolbar/CompactToolbar.js");
const buildGaugeChartConfig = (props, consumerConfig, slots) => {
  const min = props.min ?? consumerConfig.min ?? import_constants.GAUGE_DEFAULT_CONFIG.min;
  const max = props.max ?? consumerConfig.max ?? import_constants.GAUGE_DEFAULT_CONFIG.max;
  const color = props.color ?? consumerConfig.color ?? import_constants.GAUGE_DEFAULT_CONFIG.color;
  const formatter = props.formatter ?? consumerConfig.formatter;
  const loading = props.loading ?? consumerConfig.loading ?? import_constants.GAUGE_DEFAULT_CONFIG.loading;
  const thresholdsIndicators = slots.thresholdsIndicators.length === 0 ? consumerConfig.thresholdsIndicators?.map(import_apply_threshold_default.applyThresholdDefaults) : slots.thresholdsIndicators;
  const colorRules = (consumerConfig.colorRules || []).filter(import_ColorRule.isValidColorRule).concat(slots.colorRules || []);
  const toolbar = {
    ...(0, import_CompactToolbar.baseToolbarProps)(),
    ...consumerConfig.toolbar,
    ...slots.toolbar
  };
  const tooltip = slots.tooltip ?? consumerConfig.tooltip ?? import_constants.GAUGE_DEFAULT_CONFIG.tooltip;
  const valueAccessor = props.valueAccessor ?? consumerConfig.valueAccessor;
  const unit = props.unit ?? consumerConfig.unit;
  return {
    min,
    max,
    color,
    formatter,
    thresholdsIndicators,
    labelSlot: slots.label,
    colorRules,
    toolbar,
    tooltip,
    intents: slots.intents,
    valueAccessor,
    unit,
    loading
  };
};
