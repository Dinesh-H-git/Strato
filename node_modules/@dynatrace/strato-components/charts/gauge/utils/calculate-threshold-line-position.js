var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var calculate_threshold_line_position_exports = {};
__export(calculate_threshold_line_position_exports, {
  calculateThresholdLinePosition: () => calculateThresholdLinePosition
});
module.exports = __toCommonJS(calculate_threshold_line_position_exports);
var import_calculate_gauge_angle = require("./calculate-gauge-angle.js");
var import_polar_to_cartesian = require("./polar-to-cartesian.js");
const calculateThresholdLinePosition = (thresholdValue, min, max, arcOuterRadius, arcInnerRadius) => {
  if (thresholdValue < min || thresholdValue > max) {
    return void 0;
  }
  const thresholdValueAngle = (0, import_calculate_gauge_angle.calculateGaugeAngle)(thresholdValue, max, min);
  const proportionFactorInPercentage = 0.05;
  const adjustedInnerOffset = arcInnerRadius * proportionFactorInPercentage;
  const adjustedOuterOffset = arcOuterRadius * proportionFactorInPercentage;
  const { x: x1, y: y1 } = (0, import_polar_to_cartesian.polarToCartesian)(
    arcInnerRadius - adjustedInnerOffset,
    thresholdValueAngle
  );
  const { x: x2, y: y2 } = (0, import_polar_to_cartesian.polarToCartesian)(
    arcOuterRadius + adjustedOuterOffset,
    thresholdValueAngle
  );
  return { start: { x: x1, y: y1 }, end: { x: x2, y: y2 } };
};
