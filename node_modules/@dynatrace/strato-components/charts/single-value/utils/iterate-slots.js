var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var iterate_slots_exports = {};
__export(iterate_slots_exports, {
  iterateSlots: () => iterateSlots
});
module.exports = __toCommonJS(iterate_slots_exports);
var import_lodash_es = require("lodash");
var import_react = require("react");
var import_Intent = require("../../../core/slots/Intent/Intent.js");
var import_is_slot = require("../../../core/utils/is-slot.js");
var import_iterate_sparkline_slots = require("../../core/components/sparkline-display/utils/iterate-sparkline-slots.js");
var import_ColorRule = require("../../core/slots/ColorRule.js");
var import_Sparkline = require("../slots/Sparkline.js");
var import_Toolbar = require("../slots/Toolbar.js");
var import_Trend = require("../slots/Trend.js");
const iterateSlots = (children) => {
  const slots = {};
  let sparklineChildren;
  const colorRules = [];
  let intents = void 0;
  import_react.Children.forEach(children, (child) => {
    if ((0, import_is_slot.isSlot)(child, import_Sparkline.Sparkline)) {
      const { children: sparklineSlots, ...props } = child.props;
      const { thresholds, xAxis, yAxis } = (0, import_iterate_sparkline_slots.iterateSparklineSlots)(sparklineSlots);
      slots.sparkline = {
        ...props,
        ...thresholds.length > 0 ? { thresholds } : {},
        xAxis,
        yAxis
      };
      sparklineChildren = sparklineSlots;
    } else if ((0, import_is_slot.isSlot)(child, import_Trend.Trend)) {
      slots.trend = {
        ...child.props
      };
    } else if ((0, import_is_slot.isSlot)(child, import_ColorRule.ColorRule)) {
      const colorRule = child.props;
      if ((0, import_ColorRule.isValidColorRule)(colorRule)) {
        colorRules.push(colorRule);
      }
    } else if ((0, import_is_slot.isSlot)(child, import_Intent.Intent)) {
      intents = intents ? [...intents, child.props] : [child.props];
    } else if ((0, import_is_slot.isSlot)(child, import_Toolbar.Toolbar)) {
      slots.toolbar = child.props;
    }
  });
  if (colorRules.length > 0) {
    slots.colorRules = colorRules;
  }
  if (!(0, import_lodash_es.isUndefined)(intents)) {
    slots.toolbar = {
      ...slots.toolbar,
      intents,
      hidden: slots.toolbar?.hidden ?? false
    };
  }
  return { slots, sparklineChildren };
};
