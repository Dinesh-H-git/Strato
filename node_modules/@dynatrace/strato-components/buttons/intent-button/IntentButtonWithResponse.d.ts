import { type IntentWithResponseProps } from '../../core/slots/Intent/Intent.js';
import { ButtonOwnProps } from '../button/Button.js';
/**
 * Renders a button that sends an intent and handles the response.
 * Used when `responseProperties` and `onResponse` are provided.
 * @internal
 */
export declare const IntentButtonWithResponse: (props: Omit<IntentWithResponseProps, "onSelect"> & Pick<ButtonOwnProps, "color" | "textAlign" | "width" | "size" | "variant" | "aria-disabled" | "onClick"> & {
    disabled?: boolean;
} & import("react").RefAttributes<HTMLButtonElement>) => import("react").ReactElement | null;
