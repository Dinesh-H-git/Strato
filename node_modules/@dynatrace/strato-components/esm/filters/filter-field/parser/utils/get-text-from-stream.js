function getTextFromStream(inputStream, maxCharacters) {
  let text = "";
  let nextPeek = 0;
  while (nextPeek < maxCharacters && inputStream.peek(nextPeek) !== -1) {
    text += String.fromCharCode(inputStream.peek(nextPeek));
    nextPeek += 1;
  }
  return text;
}
export {
  getTextFromStream
};
//# sourceMappingURL=get-text-from-stream.js.map
