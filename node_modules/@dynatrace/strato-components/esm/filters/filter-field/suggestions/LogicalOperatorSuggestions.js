import { jsx } from "react/jsx-runtime";
import { useIntl } from "react-intl";
import { Suggestion } from "./Suggestion.js";
import { SuggestionGroup } from "./SuggestionGroup.js";
import { useFilterFieldContext } from "../hooks/useFilterFieldContext.js";
import { filterFieldMessages } from "../messages.js";
function LogicalOperatorSuggestionsGroup() {
  const { logicalOperatorSuggestionValues } = useFilterFieldContext();
  const intl = useIntl();
  if (logicalOperatorSuggestionValues.length === 0) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    SuggestionGroup,
    {
      "aria-label": intl.formatMessage(
        filterFieldMessages.suggestions.a11y.logicalOperators
      ),
      children: logicalOperatorSuggestionValues.map((suggestion, index) => /* @__PURE__ */ jsx(Suggestion, { ...suggestion }, suggestion.value ?? index))
    }
  );
}
export {
  LogicalOperatorSuggestionsGroup
};
//# sourceMappingURL=LogicalOperatorSuggestions.js.map
