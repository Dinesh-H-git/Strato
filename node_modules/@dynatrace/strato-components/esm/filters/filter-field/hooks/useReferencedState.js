import {
  useRef,
  useState
} from "react";
function useReferencedState(initialState) {
  const [state, setState] = useState(initialState);
  const stateRef = useRef(null);
  stateRef.current = state;
  return [state, setState, stateRef];
}
export {
  useReferencedState
};
//# sourceMappingURL=useReferencedState.js.map
