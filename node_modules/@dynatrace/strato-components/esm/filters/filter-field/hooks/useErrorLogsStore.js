import { create } from "zustand";
import { combine } from "zustand/middleware";
const initialState = {
  cursorPosition: 0,
  action: null,
  isControlled: false
};
const useErrorLogsStore = create(
  combine(initialState, (set) => ({
    setCursorPosition(pos) {
      set({
        cursorPosition: pos
      });
    },
    setAction(action) {
      set({
        action
      });
    },
    setIsControlled(value) {
      set({
        isControlled: value
      });
    }
  }))
);
export {
  useErrorLogsStore
};
//# sourceMappingURL=useErrorLogsStore.js.map
