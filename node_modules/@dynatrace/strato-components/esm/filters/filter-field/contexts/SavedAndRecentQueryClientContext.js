import { QueryClient } from "@tanstack/react-query";
import { createContext } from "react";
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      retry: false,
      // Data only changes via explicit mutations — we can set the staleTime to infinity to prevent unnecessary refetches.
      staleTime: Infinity
    },
    mutations: {
      retry: false
    }
  }
});
const RecentAndSavedFiltersQueryClientContext = createContext(queryClient);
export {
  RecentAndSavedFiltersQueryClientContext
};
//# sourceMappingURL=SavedAndRecentQueryClientContext.js.map
