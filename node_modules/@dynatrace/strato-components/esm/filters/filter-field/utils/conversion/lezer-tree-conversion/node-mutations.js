import { isFilterFieldValueNode } from "../../tree-node-guards.js";
function convertToErrorNode(node) {
  if (node === void 0) {
    return;
  }
  node.type = "Error";
  if (!Array.isArray(node.value)) {
    node.value = node.textValue;
  }
}
function updateNodeValues(node, text, value) {
  if (!isFilterFieldValueNode(node)) {
    return;
  }
  node.textValue = `${node.textValue ?? ""}${text}`;
  node.value = `${node.value ?? ""}${value ?? text}`;
}
export {
  convertToErrorNode,
  updateNodeValues
};
//# sourceMappingURL=node-mutations.js.map
