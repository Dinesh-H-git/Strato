import { getTextFromRange } from "../../../get-text.js";
function convertBoolean(node, doc) {
  const { from, to } = node;
  const textValue = getTextFromRange(node, doc);
  const convertedNode = {
    type: "Boolean",
    textValue,
    value: textValue === "true",
    range: { from, to }
  };
  return { convertedNode, errors: [] };
}
export {
  convertBoolean
};
//# sourceMappingURL=boolean.js.map
