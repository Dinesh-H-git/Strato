import { getTextFromRange } from "../../../get-text.js";
function convertExistsOperator(node, doc) {
  const { from, to } = node;
  const textValue = getTextFromRange(node, doc);
  const convertedNode = {
    type: "ExistsOperator",
    range: {
      from,
      to
    },
    textValue,
    value: textValue === "= *" || textValue === "=*"
  };
  return {
    convertedNode,
    errors: []
  };
}
export {
  convertExistsOperator
};
//# sourceMappingURL=exists.js.map
