import { jsx, jsxs } from "react/jsx-runtime";
import {
  forwardRef
} from "react";
import { useIntl } from "react-intl";
import { CopyIcon } from "@dynatrace/strato-icons";
import {
  getEnvironmentId,
  getAppName,
  getAppId,
  getCurrentUserDetails
} from "@dynatrace-sdk/app-environment";
import { Button } from "../../../../buttons/button/Button.js";
import { Text } from "../../../../typography/text/Text.js";
import { TextEllipsis } from "../../../../typography/text-ellipsis/TextEllipsis.js";
import { SEGMENTSELECTOR_MESSAGES } from "../../segmentMessages.js";
const CopyPermissionsButton = forwardRef(({ variant, color }, forwardedRef) => {
  const intl = useIntl();
  const environmentId = getEnvironmentId();
  const appName = getAppName();
  const appId = getAppId();
  const { id, email } = getCurrentUserDetails();
  const clipboardText = `Your assigned access control policies are missing the following permissions:
Missing permissions: storage:filter-segments:read;
Environment: ${environmentId}
App name: ${appName}
App ID: ${appId}
User email: ${email}
User ID: ${id}`;
  return /* @__PURE__ */ jsxs(
    Button,
    {
      variant,
      ref: forwardedRef,
      color,
      onClick: (e) => {
        e.stopPropagation();
        navigator.clipboard.writeText(clipboardText).catch(() => {
        });
      },
      children: [
        /* @__PURE__ */ jsx(Button.Prefix, { children: /* @__PURE__ */ jsx(CopyIcon, {}) }),
        /* @__PURE__ */ jsx(Text, { textStyle: "base-emphasized", children: /* @__PURE__ */ jsx(TextEllipsis, { children: intl.formatMessage(SEGMENTSELECTOR_MESSAGES.copyDetailsButton) }) })
      ]
    }
  );
});
export {
  CopyPermissionsButton
};
//# sourceMappingURL=CopyPermissionsButton.js.map
