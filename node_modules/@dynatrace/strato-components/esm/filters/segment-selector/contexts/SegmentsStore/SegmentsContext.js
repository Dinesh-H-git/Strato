import { jsx } from "react/jsx-runtime";
import { createContext, useRef } from "react";
import { createStore } from "../../../../core/store/store.js";
const SegmentsContext = createContext(
  createStore([])
);
const SegmentsProvider = (props) => {
  const { children, defaultSegments = [] } = props;
  const segmentsStoreRef = useRef(
    createStore(defaultSegments)
  );
  return /* @__PURE__ */ jsx(SegmentsContext.Provider, { value: segmentsStoreRef.current, children });
};
export {
  SegmentsContext,
  SegmentsProvider
};
//# sourceMappingURL=SegmentsContext.js.map
