import {
  isValidElement,
  cloneElement
} from "react";
import { isElement } from "react-is";
import { mergeProps } from "../../../../core/utils/merge-props.js";
import { useTimeframeSelectorContext } from "../../hooks/useTimeframeSelectorContext.js";
function isTimeframeSelectorCustomTriggerElement(child) {
  return isElement(child) && child.type === CustomTrigger;
}
const CustomTrigger = (props) => {
  const { children, ...remainingProps } = props;
  const { triggerProps, displayValue, hint, showError } = useTimeframeSelectorContext();
  const dtComponentName = triggerProps?.["data-dt-component"];
  if (isValidElement(children)) {
    return cloneElement(children, {
      ...mergeProps(
        triggerProps,
        { color: showError ? "critical" : void 0 },
        remainingProps,
        children.props,
        { "data-dt-component": dtComponentName }
      )
    });
  }
  if (typeof children === "function") {
    return children(
      { displayValue, isInvalid: showError, hint },
      mergeProps(triggerProps, remainingProps)
    );
  }
  return null;
};
CustomTrigger["displayName"] = "TimeframeSelector.CustomTrigger";
export {
  CustomTrigger,
  isTimeframeSelectorCustomTriggerElement
};
//# sourceMappingURL=CustomTrigger.js.map
