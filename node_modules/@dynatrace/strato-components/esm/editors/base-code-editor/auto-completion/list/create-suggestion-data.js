import {
  regexFromSuggestions,
  regexFromPrefixAndSuggestions
} from "../regex-utils.js";
async function createSuggestionData(autocompletionList) {
  try {
    const suggestions = typeof autocompletionList.autocompletionList === "function" ? await autocompletionList.autocompletionList() : autocompletionList.autocompletionList;
    const suggestionRegex = regexFromSuggestions(suggestions);
    return {
      suggestions,
      suggestionRegex,
      prefixRegex: regexFromPrefixAndSuggestions(
        autocompletionList.matchBefore,
        suggestionRegex
      )
    };
  } catch {
    return void 0;
  }
}
export {
  createSuggestionData
};
//# sourceMappingURL=create-suggestion-data.js.map
