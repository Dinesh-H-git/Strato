const escapeRegexSource = (source) => {
  return source.replace(/[-[\]{}()*+?.,\\^$|]/g, "\\$&");
};
const regexSourceFromSet = (s) => {
  return `[${escapeRegexSource(Array.from(s.keys()).join(""))}]`;
};
const regexFromSuggestions = (suggestions) => {
  const first = /* @__PURE__ */ new Set();
  const rest = /* @__PURE__ */ new Set();
  for (const s of suggestions) {
    if (s.label.length > 0) {
      first.add(s.label[0]);
      for (const char of s.label) {
        rest.add(char);
      }
    }
  }
  const source = `${regexSourceFromSet(first)}${regexSourceFromSet(rest)}*$`;
  return new RegExp(source, "i");
};
const regexFromPrefixAndSuggestions = (prefixRegexSource, suggestionRegexSource) => new RegExp(
  `${prefixRegexSource.source}(${suggestionRegexSource.source})?$`,
  prefixRegexSource.flags + "i"
);
export {
  regexFromPrefixAndSuggestions,
  regexFromSuggestions
};
//# sourceMappingURL=regex-utils.js.map
