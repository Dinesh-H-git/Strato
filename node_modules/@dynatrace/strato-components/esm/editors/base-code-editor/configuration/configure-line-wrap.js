import { EditorView } from "@codemirror/view";
const configureLineWrap = (lineWrap, viewRef, lineWrappingCompartment) => {
  const editorView = viewRef.current;
  editorView.dispatch({
    effects: lineWrappingCompartment.reconfigure(
      lineWrap ? EditorView.lineWrapping : []
    )
  });
};
export {
  configureLineWrap
};
//# sourceMappingURL=configure-line-wrap.js.map
