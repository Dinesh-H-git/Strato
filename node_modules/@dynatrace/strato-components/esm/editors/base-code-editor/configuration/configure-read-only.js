import { selectAll } from "@codemirror/commands";
import { EditorState } from "@codemirror/state";
import { EditorView, keymap } from "@codemirror/view";
import { getKeyMaps } from "../utils/key-maps.js";
const configureReadOnly = (readOnly, viewRef, keymapCompartment) => {
  const editorView = viewRef.current;
  editorView.dispatch({
    effects: keymapCompartment.reconfigure([
      EditorState.readOnly.of(readOnly),
      EditorView.editable.of(!readOnly),
      readOnly ? keymap.of([{ key: "Mod-a", run: selectAll }]) : getKeyMaps(viewRef)
    ])
  });
};
export {
  configureReadOnly
};
//# sourceMappingURL=configure-read-only.js.map
