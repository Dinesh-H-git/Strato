import { useCallback, useRef } from "react";
const useValidityInfo = (onValidityChange) => {
  const currentValidityRef = useRef([]);
  const onValidityChangeRef = useRef(void 0);
  onValidityChangeRef.current = onValidityChange;
  return useCallback(
    (validity) => {
      if (onValidityChangeRef.current && JSON.stringify(validity) !== JSON.stringify(currentValidityRef.current)) {
        currentValidityRef.current = validity;
        onValidityChangeRef.current(validity);
      }
    },
    // Avoid adding any dependencies to prevent reconfiguring the editor when currentDiagnostics changes
    []
  );
};
export {
  useValidityInfo
};
//# sourceMappingURL=useValidityInfo.js.map
