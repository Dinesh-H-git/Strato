import { TZDateMini } from "@date-fns/tz";
import { isAfter, isSameDay, isValid } from "date-fns";
import { defineMessages } from "react-intl";
import {
  parseTimeAsTimeValue,
  _getTimeframeExpression,
  _parseTimeExpression
} from "@dynatrace-sdk/units";
import { getTimezone } from "@dynatrace-sdk/user-preferences";
import {
  getFormattedTimeValue,
  getFormattedMinMax
} from "../../../core/time/helpers.js";
import { isDev } from "../../../core/utils/is-node-environment.js";
const messages = defineMessages({
  ms: {
    description: "Timeframe formatter: timeframe-millisecond",
    defaultMessage: "{prefix, select, past {Last} future {Next} relativeFuture {In} other {}} {offset, plural, one {# millisecond} other {# milliseconds}}{suffix}",
    id: "1ierRkr3xtpxyOXK"
  },
  s: {
    description: "Timeframe formatter: timeframe-second",
    defaultMessage: "{prefix, select, past {Last} future {Next} relativeFuture {In} other {}} {offset, plural, one {# second} other {# seconds}}{suffix}",
    id: "DMJP39IduNdB3wO3"
  },
  m: {
    description: "Timeframe formatter: timeframe-minute",
    defaultMessage: "{prefix, select, past {Last} future {Next} relativeFuture {In} other {}} {offset, plural, one {# minute} other {# minutes}}{suffix}",
    id: "SPSPRmp+gABk1LGQ"
  },
  h: {
    description: "Timeframe formatter: timeframe-hour",
    defaultMessage: "{prefix, select, past {Last} future {Next} relativeFuture {In} other {}} {offset, plural, one {# hour} other {# hours}}{suffix}",
    id: "aNWYfuuoyg/TEftI"
  },
  d: {
    description: "Timeframe formatter: timeframe-day",
    defaultMessage: "{prefix, select, past {Last} future {Next} relativeFuture {In} other {}} {offset, plural, one {# day} other {# days}}{suffix}",
    id: "ECZzXUfKYwLWko5n"
  },
  w: {
    description: "Timeframe formatter: timeframe-week",
    defaultMessage: "{prefix, select, past {Last} future {Next} relativeFuture {In} other {}} {offset, plural, one {# week} other {# weeks}}{suffix}",
    id: "p5Aa2CtWutuGwudB"
  },
  M: {
    description: "Timeframe formatter: timeframe-month",
    defaultMessage: "{prefix, select, past {Last} future {Next} relativeFuture {In} other {}} {offset, plural, one {# month} other {# months}}{suffix}",
    id: "jZte4Ia6a1UkBwpv"
  },
  q: {
    description: "Timeframe formatter: timeframe-quarter",
    defaultMessage: "{prefix, select, past {Last} future {Next} relativeFuture {In} other {}} {offset, plural, one {# quarter} other {# quarters}}{suffix}",
    id: "m5z+3UJ4Q3absAi5"
  },
  y: {
    description: "Timeframe formatter: timeframe-year",
    defaultMessage: "{prefix, select, past {Last} future {Next} relativeFuture {In} other {}} {offset, plural, one {# year} other {# years}}{suffix}",
    id: "ly03uOR7VGcWb+pG"
  },
  requiredTimeframe: {
    defaultMessage: "Empty timeframe.",
    id: "QsKlSIxE+Yp58CUG",
    description: "Timeframe selector required timeframe error hint."
  },
  requiredFrom: {
    defaultMessage: "The 'from' field is required.",
    id: "VlyElrRtYeguIlZS",
    description: "Timeframe selector required from field error hint."
  },
  requiredTo: {
    defaultMessage: "The 'to' field is required.",
    id: "d7CsKWrzLvzuObN6",
    description: "Timeframe selector required to field error hint."
  },
  invalid: {
    description: "Timeframe formatter: invalid",
    defaultMessage: "invalid timeframe",
    id: "eZkDqSXP6kwDUc5d"
  },
  invalidFrom: {
    defaultMessage: "The 'from' field is invalid.",
    description: "Error message shown if the from value of a timeframe is invalid",
    id: "VTvQ7p6QQyABl+XP"
  },
  invalidTo: {
    defaultMessage: "The 'to' field is invalid.",
    id: "rm7eAPvUS8J1G8TZ",
    description: "Error message shown if the to value of a timeframe is invalid"
  },
  invalidTimeframe: {
    defaultMessage: "Invalid timeframe",
    id: "U59PIse3upSvDblc",
    description: "Error message shown if the from and to values of a timeframe are both invalid"
  },
  invalidOrder: {
    defaultMessage: "'from' date should be before 'to' date",
    id: "DKFIWE64kHV1ohuk",
    description: "Message to display if the from and to dates are in the wrong order."
  },
  outsideRange: {
    defaultMessage: "Only dates between {min} \u2192 {max} are available.",
    id: "cBlxys3qfILuKMaG",
    description: "Hint to display in the timeframe selector if a min and a max date is set."
  },
  now: {
    description: "Timeframe formatter: timeframe-now",
    defaultMessage: "Now",
    id: "sAS3aHsvApXTajqr"
  },
  to: {
    description: "Timeframe formatter: timeframe-to",
    defaultMessage: "to",
    id: "ewgHQuXKDMK7CzyQ"
  },
  tomorrow: {
    description: "Timeframe formatter: timeframe-tomorrow",
    defaultMessage: "Tomorrow",
    id: "FbK3miEv64PNHk2r"
  },
  today: {
    description: "Timeframe formatter: timeframe-today",
    defaultMessage: "Today",
    id: "BlGZxVNh87aTe30G"
  },
  yesterday: {
    description: "Timeframe formatter: timeframe-yesterday",
    defaultMessage: "Yesterday",
    id: "r3yl8iiNQEDjjS+I"
  },
  "roundDown-s": {
    description: "Timeframe formatter: timeframe-roundDown-second",
    defaultMessage: "{prefix, select, previous {Previous} next {Next} other {This}} {amount, plural, one {second} other {# seconds}}",
    id: "znJQdrgHo0UbGJLV"
  },
  "roundDown-m": {
    description: "Timeframe formatter: timeframe-roundDown-minute",
    defaultMessage: "{prefix, select, previous {Previous} next {Next} other {This}} {amount, plural, one {minute} other {# minutes}}",
    id: "HpRsCrp1SmD+zRFu"
  },
  "roundDown-h": {
    description: "Timeframe formatter: timeframe-roundDown-hour",
    defaultMessage: "{prefix, select, previous {Previous} next {Next} other {This}} {amount, plural, one {hour} other {# hours}}",
    id: "vEoSirMyZ5RMVdsR"
  },
  "roundDown-d": {
    description: "Timeframe formatter: timeframe-roundDown-day",
    defaultMessage: "{prefix, select, previous {Previous} next {Next} other {This}} {amount, plural, one {day} other {# days}}",
    id: "V09xUXtpHEVFRBsh"
  },
  "roundDown-w": {
    description: "Timeframe formatter: timeframe-roundDown-week",
    defaultMessage: "{prefix, select, previous {Previous} next {Next} other {This}} {amount, plural, one {week} other {# weeks}}",
    id: "BfrmGp7APB2nlQRw"
  },
  "roundDown-M": {
    description: "Timeframe formatter: timeframe-roundDown-month",
    defaultMessage: "{prefix, select, previous {Previous} next {Next} other {This}} {amount, plural, one {month} other {# months}}",
    id: "Ro/9HWdwiItr3Ter"
  },
  "roundDown-q": {
    description: "Timeframe formatter: timeframe-roundDown-quarter",
    defaultMessage: "{prefix, select, previous {Previous} next {Next} other {This}} {amount, plural, one {quarter} other {# quarters}}",
    id: "V4fGRjKxu0XkK3nm"
  },
  "roundDown-y": {
    description: "Timeframe formatter: timeframe-roundDown-year",
    defaultMessage: "{prefix, select, previous {Previous} next {Next} other {This}} {amount, plural, one {year} other {# years}}",
    id: "CjpZq81r1HsdNxp0"
  },
  "full-day": {
    description: "Timeframe formatter: timeframe-full-day",
    defaultMessage: "Full day",
    id: "B/DeQ4xCT/IrvyuQ"
  },
  "remaining-day": {
    description: "Timeframe formatter: timeframe-remaining-day",
    defaultMessage: "Remaining day",
    id: "GixTwN/AsVWJprz2"
  }
});
function isExpressionString(candidate) {
  const [expression] = candidate.match(_getTimeframeExpression()) ?? [];
  return !!expression;
}
function getTimeValue(candidate, precision, relativeNow, type = "expression") {
  if (candidate === void 0) {
    return void 0;
  }
  if (candidate === null) {
    return null;
  }
  if (typeof candidate === "string" || candidate.type === "expression") {
    const stringCandidate = typeof candidate === "string" ? candidate : candidate.value;
    const parsed = parseTimeAsTimeValue(
      stringCandidate,
      relativeNow,
      precision
    );
    if (!parsed) {
      return { absoluteDate: "", value: stringCandidate, type };
    }
    return parsed;
  }
  return removePrecisionFromTimeValue(candidate, precision);
}
function mapToPartialTimeframe(value, precision) {
  if (!value || value.from === void 0 && value.to === void 0) {
    return null;
  }
  const relativeNow = TZDateMini.tz(getTimezone()).getTime();
  const from = getTimeValue(value.from, precision, relativeNow) ?? null;
  const to = getTimeValue(value.to, precision, relativeNow) ?? null;
  return {
    from,
    to
  };
}
function mapToTimeframe(value, precision) {
  if (!value || value.from === void 0 && value.to === void 0) {
    return null;
  }
  const relativeNow = TZDateMini.tz(getTimezone()).getTime();
  const from = getTimeValue(value.from, precision, relativeNow);
  const to = getTimeValue(value.to, precision, relativeNow);
  if (from === void 0 && to === void 0) {
    return null;
  }
  return {
    from,
    to
  };
}
function parseValue(value, fallbackToNow = false, precision) {
  const timezone = getTimezone();
  const nowDate = {
    value: "now()",
    absoluteDate: removePrecision(
      TZDateMini.tz(timezone).toISOString(),
      precision
    ),
    type: "expression"
  };
  if (!value) {
    return { from: null, to: fallbackToNow ? nowDate : null };
  }
  const { valueFrom, valueTo } = getFromToValues(value);
  const relativeNow = TZDateMini.tz(timezone).getTime();
  const parsedFrom = parseTimeAsTimeValue(valueFrom, relativeNow, precision);
  const parsedTo = fallbackToNow && !valueTo ? nowDate : parseTimeAsTimeValue(valueTo, relativeNow, precision);
  const invalidFrom = {
    type: "expression",
    value: valueFrom ?? "",
    absoluteDate: ""
  };
  const invalidTo = {
    type: "expression",
    value: valueTo ?? "",
    absoluteDate: ""
  };
  const from = parsedFrom ?? (valueFrom === void 0 ? null : invalidFrom);
  const to = parsedTo ?? (valueTo === void 0 ? null : invalidTo);
  return {
    from,
    to
  };
}
function formatTimeValue(input, precision = "minutes") {
  if (input === null) {
    return "";
  }
  if (input === "now()" || input === "now") {
    return "Now";
  }
  const timeValue = parseTimeAsTimeValue(
    typeof input == "string" ? input : input.value,
    Date.now(),
    precision
  );
  if (timeValue === null) {
    return "";
  }
  const { type, value } = timeValue;
  if (type === "iso8601") {
    return getFormattedTimeValue(timeValue, precision);
  }
  if (value === "now()") {
    return "Now";
  }
  return value.replaceAll("now()", "").replaceAll("now", "").trim();
}
function formatTimeframeExpressionInSpecialCases(from, to, intl) {
  const timeDetailsFrom = _parseTimeExpression(from);
  const timeDetailsTo = _parseTimeExpression(to);
  if (!timeDetailsFrom || !timeDetailsTo) {
    return;
  }
  let roundDownTranslationIndex = timeDetailsFrom.roundDownUnit;
  switch (timeDetailsFrom.roundDownUnit) {
    case "w0":
    case "w1":
    case "w2":
    case "w3":
    case "w4":
    case "w5":
    case "w6":
      roundDownTranslationIndex = "w";
      break;
    default:
      roundDownTranslationIndex = timeDetailsFrom.roundDownUnit;
  }
  if (!timeDetailsFrom.offsetAmount && timeDetailsFrom.roundDownUnit && !timeDetailsTo.offsetAmount && !timeDetailsTo.roundDownUnit && roundDownTranslationIndex) {
    if (timeDetailsFrom.roundDownUnit === "d") {
      return {
        displayValue: intl.formatMessage(messages.today),
        isInvalid: false,
        hint: ""
      };
    }
    return {
      displayValue: intl.formatMessage(
        messages[`roundDown-${roundDownTranslationIndex}`],
        {
          prefix: "this",
          amount: 1
          // to display the singular of a unit we need a 1 here
        }
      ),
      isInvalid: false,
      hint: ""
    };
  }
  if (timeDetailsFrom.offsetAmount === 1 && timeDetailsFrom.roundDownUnit && timeDetailsFrom.offsetUnit && timeDetailsTo.offsetAmount > 0 && timeDetailsTo.roundDownUnit && timeDetailsTo.offsetUnit && timeDetailsFrom.roundDownUnit === timeDetailsTo.roundDownUnit && roundDownTranslationIndex === timeDetailsTo.offsetUnit) {
    if (timeDetailsFrom.roundDownUnit === "d" && timeDetailsTo.offsetAmount === 2) {
      return {
        displayValue: intl.formatMessage(messages.tomorrow),
        isInvalid: false,
        hint: ""
      };
    }
    return {
      displayValue: intl.formatMessage(
        messages[`roundDown-${roundDownTranslationIndex}`],
        {
          prefix: "next",
          amount: Math.abs(timeDetailsTo.offsetAmount) - 1
        }
      ),
      isInvalid: false,
      hint: ""
    };
  }
  if (timeDetailsFrom.offsetAmount < 0 && timeDetailsFrom.roundDownUnit && timeDetailsFrom.offsetUnit && !timeDetailsTo.offsetAmount && timeDetailsTo.roundDownUnit && timeDetailsFrom.roundDownUnit === timeDetailsTo.roundDownUnit && timeDetailsFrom.offsetUnit === timeDetailsTo.roundDownUnit && roundDownTranslationIndex) {
    if (timeDetailsFrom.roundDownUnit === "d" && timeDetailsFrom.offsetAmount === -1) {
      return {
        displayValue: intl.formatMessage(messages.yesterday),
        isInvalid: false,
        hint: ""
      };
    }
    return {
      displayValue: intl.formatMessage(
        messages[`roundDown-${roundDownTranslationIndex}`],
        {
          prefix: "previous",
          amount: Math.abs(timeDetailsFrom.offsetAmount)
        }
      ),
      isInvalid: false,
      hint: ""
    };
  }
  if (!timeDetailsFrom.offsetAmount && timeDetailsFrom.roundDownUnit === "d" && timeDetailsTo.offsetAmount === 1 && timeDetailsTo.roundDownUnit === "d" && timeDetailsTo.offsetUnit === "d") {
    return {
      displayValue: intl.formatMessage(messages["full-day"]),
      isInvalid: false,
      hint: ""
    };
  }
  if (
    // It is not great that we parse the whole statement here. We could expose the expression in the
    // _parseTimeExpression function, but since it is in units sdk and would be a breaking change
    // we just wait for moving everything there.
    parseTimeAsTimeValue(from)?.value === "now()" && timeDetailsTo.offsetAmount === 1 && timeDetailsTo.roundDownUnit === "d" && timeDetailsTo.offsetUnit === "d"
  ) {
    return {
      displayValue: intl.formatMessage(messages["remaining-day"]),
      isInvalid: false,
      hint: ""
    };
  }
}
function formatTimeValues(from, to, intl, precision, fromDate, toDate, hint) {
  if (from.type === "expression" && to.type === "expression") {
    if (to.value === from.value) {
      return {
        displayValue: intl.formatMessage(messages.invalid),
        isInvalid: true,
        hint: intl.formatMessage(messages.invalidOrder)
      };
    }
    const specialCase = formatTimeframeExpressionInSpecialCases(
      from.value,
      to.value,
      intl
    );
    if (specialCase) {
      return specialCase;
    }
    const isFromNow = from.value === "now()";
    const isToNow = to.value === "now()";
    if (isToNow || isFromNow) {
      const displayValue = formatRelativeTimeframe(
        isToNow ? from.value : to.value,
        intl,
        isToNow ? "past" : "future",
        true
      );
      return {
        displayValue,
        isInvalid: displayValue === intl.formatMessage(messages.invalid),
        hint: ""
      };
    }
  }
  const isRelativeToNow = to.type === "expression" && to.value === "now()" || from.type === "expression" && from.value === "now()";
  const hideToDate = isSameDay(fromDate, toDate) && (from.type === "iso8601" && to.type === "iso8601" || from.type === "expression" && from.value === "now()");
  return {
    displayValue: `${formatDetails(
      from,
      intl,
      precision,
      false,
      isRelativeToNow
    )} \u2192 ${formatDetails(to, intl, precision, hideToDate, isRelativeToNow)}`,
    isInvalid: false,
    hint
  };
}
function getFromToValues(value) {
  if (!value) {
    return {};
  }
  const valueFrom = typeof value.from === "string" ? value.from : value.from?.value;
  const valueTo = typeof value.to === "string" ? value.to : value.to?.value;
  return { valueFrom, valueTo };
}
function formatTimeframe(value, intl, precision) {
  const { valueFrom, valueTo } = getFromToValues(value);
  const relativeNow = TZDateMini.tz(getTimezone()).getTime();
  const fromDetails = parseTimeAsTimeValue(valueFrom, relativeNow, precision);
  const toDetails = parseTimeAsTimeValue(valueTo, relativeNow, precision);
  return extractDisplayValueFromTimeValues(
    fromDetails,
    toDetails,
    intl,
    precision,
    valueFrom,
    valueTo
  );
}
function extractDisplayValueFromTimeValues(from, to, intl, precision = "minutes", valueFrom, valueTo) {
  let hint = "";
  if (!from || !to) {
    if (isDev()) {
      if (!from) {
        console.warn(
          `The from date could not be parsed. Please check if the provided value is valid and supported: ${valueFrom}.`
        );
      }
      if (!to) {
        console.warn(
          `The to date could not be parsed. Please check if the provided value is valid and supported: ${valueTo}.`
        );
      }
    }
    if (!from && !to) {
      hint = intl.formatMessage(messages.invalidTimeframe);
    } else if (!from) {
      hint = intl.formatMessage(messages.invalidFrom);
    } else {
      hint = intl.formatMessage(messages.invalidTo);
    }
    return {
      displayValue: intl.formatMessage(messages.invalid),
      isInvalid: true,
      hint
    };
  }
  const configuredTimezone = getTimezone();
  const fromDate = new TZDateMini(from.absoluteDate, configuredTimezone);
  const toDate = new TZDateMini(to.absoluteDate, configuredTimezone);
  const { isInvalid, hint: invalidItemHint } = checkInvalidItem(
    fromDate,
    toDate,
    from.value,
    to.value,
    intl
  );
  if (isInvalid) {
    return {
      displayValue: intl.formatMessage(messages.invalid),
      isInvalid: true,
      hint: invalidItemHint
    };
  }
  return formatTimeValues(from, to, intl, precision, fromDate, toDate, hint);
}
function checkInvalidItem(fromDate, toDate, from, to, intl) {
  if (!isValid(fromDate)) {
    if (isDev()) {
      console.warn(`The from date (${from}) is invalid.`);
    }
    return { isInvalid: true, hint: intl.formatMessage(messages.invalidFrom) };
  }
  if (!isValid(toDate)) {
    if (isDev()) {
      console.warn(`The to date (${to}) is invalid.`);
    }
    return { isInvalid: true, hint: intl.formatMessage(messages.invalidTo) };
  }
  if (!isAfter(toDate, fromDate)) {
    if (isDev()) {
      console.warn(
        `The from date - ${from} - should be before the to date - ${to}.`
      );
    }
    return { isInvalid: true, hint: intl.formatMessage(messages.invalidOrder) };
  }
  return { isInvalid: false, hint: "" };
}
function formatRelativeTimeframe(expression, intl, prefix, isRelativeToNow) {
  const timeDetails = _parseTimeExpression(expression);
  if (timeDetails) {
    const { offsetAmount, offsetUnit, roundDownUnit } = timeDetails;
    if (offsetAmount === 0 && !roundDownUnit) {
      return intl.formatMessage(messages.now);
    }
    if (roundDownUnit) {
      return expression.replaceAll("now()", "").replaceAll("now", "");
    }
    const timeExpression = messages[offsetUnit];
    if (timeExpression) {
      let suffix = "";
      prefix ??= offsetAmount > 0 ? "future" : "past";
      if (prefix === "future" && !isRelativeToNow) {
        prefix = "relativeFuture";
      }
      if (!isRelativeToNow && prefix === "past") {
        suffix = " ago";
        prefix = "other";
      }
      return intl.formatMessage(timeExpression, {
        prefix,
        offset: Math.abs(offsetAmount),
        suffix
      }).trim();
    }
  }
  return intl.formatMessage(messages.invalid);
}
function formatDetails(details, intl, precision = "minutes", showTimeOnly, isRelativeToNow) {
  return details.type === "expression" ? formatRelativeTimeframe(details.value, intl, void 0, isRelativeToNow) : getFormattedTimeValue(details, precision, showTimeOnly);
}
function getOutsideRangeMessage(intl, min, max, precision) {
  const { min: from, max: to } = getFormattedMinMax(min, max, precision);
  if (min && max) {
    return intl.formatMessage(
      {
        defaultMessage: "Only dates between {from} \u2192 {to} are available.",
        id: "Nlzd/zJ2XP7vZMvE",
        description: "Given dates are not within the valid range."
      },
      {
        from,
        to
      }
    );
  }
  if (min) {
    return intl.formatMessage(
      {
        defaultMessage: "Only dates after {from} are available.",
        id: "QXOCOrq5Seu+1A93",
        description: "Given dates are not within the valid range."
      },
      {
        from
      }
    );
  }
  if (max) {
    return intl.formatMessage(
      {
        defaultMessage: "Only dates before {to} are available.",
        id: "Jzdt1FVhHSO1Lmg9",
        description: "Given dates are not within the valid range."
      },
      {
        to
      }
    );
  }
  return intl.formatMessage({
    defaultMessage: "Selected time is outside of the valid range.",
    id: "r2DiyNP5gUAg0s5f",
    description: "Given dates are not within the valid range."
  });
}
function removePrecision(isoString, precision = "minutes") {
  const relativeDate = TZDateMini.tz(getTimezone()).getTime();
  const timeValue = parseTimeAsTimeValue(isoString, relativeDate, precision);
  if (!isNaN(new TZDateMini(isoString).getTime())) {
    return timeValue ? timeValue.absoluteDate : isoString;
  }
  return isoString;
}
const removePrecisionFromTimeFrame = (date, precision = "minutes") => {
  if (!date) {
    return null;
  }
  const { from, to } = parseValue(date);
  const relativeDate = TZDateMini.tz(getTimezone()).getTime();
  const parsedFrom = parseTimeAsTimeValue(from?.value, relativeDate, precision);
  const parsedTo = parseTimeAsTimeValue(to?.value, relativeDate, precision);
  return {
    from: parsedFrom || from,
    to: parsedTo || to
  };
};
const removePrecisionFromTimeValue = (date, precision = "minutes") => {
  if (!date) {
    return date;
  }
  const relativeDate = TZDateMini.tz(getTimezone()).getTime();
  if (date.value) {
    return parseTimeAsTimeValue(date.value, relativeDate, precision);
  }
  return date;
};
export {
  extractDisplayValueFromTimeValues,
  formatDetails,
  formatTimeValue,
  formatTimeframe,
  getFromToValues,
  getOutsideRangeMessage,
  getTimeValue,
  isExpressionString,
  mapToPartialTimeframe,
  mapToTimeframe,
  messages,
  parseValue,
  removePrecision,
  removePrecisionFromTimeFrame,
  removePrecisionFromTimeValue
};
//# sourceMappingURL=timeParser.js.map
