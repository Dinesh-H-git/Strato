import { jsx } from "react/jsx-runtime";
import { isFinite } from "lodash-es";
import { forwardRef } from "react";
import { NumberInputBase } from "./NumberInputBase.js";
import { useControllableState } from "../../core/hooks/useControllableState.js";
const COMPONENT_NAME = "NumberInput";
const NumberInput = /* @__PURE__ */ forwardRef((props, forwardedRef) => {
  const { defaultValue, value, onChange, ...remainingProps } = props;
  const [internalValue, setInternalValue] = useControllableState({
    defaultValue,
    value,
    onChange
  });
  const handleChange = (newValue) => {
    try {
      const newInternalValue = isFinite(newValue) ? newValue : null;
      setInternalValue(newInternalValue);
    } catch {
    }
  };
  return /* @__PURE__ */ jsx(
    NumberInputBase,
    {
      ref: forwardedRef,
      value: internalValue,
      onChange: handleChange,
      ...remainingProps,
      "data-dt-component": COMPONENT_NAME
    }
  );
});
NumberInput.displayName = COMPONENT_NAME;
export {
  NumberInput
};
//# sourceMappingURL=NumberInput.js.map
