import { useId } from "../../core/hooks/useId.js";
function useInputHint({
  id,
  hint,
  hasError,
  ariaDescribedby: ariaDescribedbyProp
}) {
  const _hintId = useId();
  const hintId = id ?? _hintId;
  const ariaDescribedby = [
    ariaDescribedbyProp,
    hint ? hintId : void 0
  ].filter((value) => !!value);
  return {
    hintProps: { id: hintId, "aria-live": "polite" },
    inputProps: {
      "aria-describedby": ariaDescribedby.length > 0 ? ariaDescribedby.join(" ") : void 0,
      "aria-invalid": hasError
    }
  };
}
export {
  useInputHint
};
//# sourceMappingURL=useInputHint.js.map
