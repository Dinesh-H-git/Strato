import { jsx } from "react/jsx-runtime";
import { useState, useMemo, createContext } from "react";
const DayViewContext = createContext({
  setHoveredDate: void 0,
  hoveredDate: null
});
const DayViewContextProvider = ({
  children
}) => {
  const [hoveredDate, setHoveredDate] = useState(null);
  const contextValue = useMemo(
    () => ({ hoveredDate, setHoveredDate }),
    [hoveredDate, setHoveredDate]
  );
  return /* @__PURE__ */ jsx(DayViewContext.Provider, { value: contextValue, children });
};
export {
  DayViewContext,
  DayViewContextProvider
};
//# sourceMappingURL=DayViewContext.js.map
