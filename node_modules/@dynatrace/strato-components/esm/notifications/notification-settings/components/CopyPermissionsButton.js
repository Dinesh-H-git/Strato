import { jsx, jsxs } from "react/jsx-runtime";
import { useIntl } from "react-intl";
import { CopyIcon } from "@dynatrace/strato-icons";
import { Button } from "../../../buttons/button/Button.js";
import { Text } from "../../../typography/text/Text.js";
import { notificationMessages } from "../messages.js";
const CopyPermissionsButton = (props) => {
  const intl = useIntl();
  return /* @__PURE__ */ jsxs(
    Button,
    {
      variant: "emphasized",
      onClick: async (e) => {
        e.stopPropagation();
        try {
          await navigator.clipboard.writeText(
            `notification:notifications:${props.readOrWrite}`
          );
        } catch {
        }
      },
      children: [
        /* @__PURE__ */ jsx(Button.Prefix, { children: /* @__PURE__ */ jsx(CopyIcon, {}) }),
        /* @__PURE__ */ jsx(Text, { textStyle: "base-emphasized", children: intl.formatMessage(notificationMessages.copyMissingPermissions) })
      ]
    }
  );
};
export {
  CopyPermissionsButton
};
//# sourceMappingURL=CopyPermissionsButton.js.map
