import { jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import {
  NotificationSettingsContext
} from "../contexts/NotificationSettingsContext.js";
import { useEventNotificationClient } from "../hooks/useEventNotificationClient.js";
function EventNotificationClient(props) {
  const {
    toggleNotifications,
    loading,
    hasNotFoundError,
    hasReadPermission,
    hasWritePermission,
    enabled
  } = useEventNotificationClient(props.config);
  const contextValue = useMemo(
    () => ({
      enabled,
      hasNotFoundError,
      hasReadPermission,
      hasWritePermission,
      loading,
      toggleNotifications
    }),
    [
      enabled,
      hasNotFoundError,
      hasReadPermission,
      hasWritePermission,
      loading,
      toggleNotifications
    ]
  );
  return /* @__PURE__ */ jsx(NotificationSettingsContext.Provider, { value: contextValue, children: props.children });
}
function isEventNotificationConfig(type, config) {
  return type === "event";
}
export {
  EventNotificationClient,
  isEventNotificationConfig
};
//# sourceMappingURL=EventNotificationClient.js.map
