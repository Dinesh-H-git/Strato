import { QueryClient } from "@tanstack/react-query";
import { createContext } from "react";
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      retry: false
    },
    mutations: {
      retry: false
    }
  }
});
const NotificationQueryClientContext = createContext(queryClient);
export {
  NotificationQueryClientContext
};
//# sourceMappingURL=notification-query-client.js.map
