import { jsx } from "react/jsx-runtime";
import {
  forwardRef
} from "react";
import { isElement } from "react-is";
import { Button } from "../../buttons/button/Button.js";
function isAppHeaderActionButton(node) {
  return node !== void 0 && node !== null && isElement(node) && node.type === ActionButton;
}
const ActionButton = forwardRef(
  (props, forwardedRef) => /* @__PURE__ */ jsx(Button, { ref: forwardedRef, ...props })
);
ActionButton.displayName = "AppHeader.Button";
export {
  ActionButton,
  isAppHeaderActionButton
};
//# sourceMappingURL=ActionButton.js.map
