import { useBreakpointCollapse } from "../shared/useBreakpointCollapse.js";
function useLayoutState({
  sidebar,
  containerRef
}) {
  const { containerWidth, collapsed, setCollapsed, isBelowBreakpoint } = useBreakpointCollapse({
    collapsed: {
      value: sidebar.config.collapsed,
      defaultValue: sidebar.config.defaultCollapsed ?? false,
      onChange: sidebar.config.onCollapsedChange
    },
    breakpoint: sidebar.config.breakpoint,
    containerRef
  });
  let mode = "split";
  if (collapsed) {
    mode = "collapsed";
  }
  if (isBelowBreakpoint) {
    mode = "drawer";
  }
  return {
    collapsed,
    containerWidth,
    isBelowBreakpoint,
    mode,
    setCollapsed
  };
}
export {
  useLayoutState
};
//# sourceMappingURL=useLayoutState.js.map
