import { useCallback, useRef } from "react";
function useButtonFocusHandover() {
  const pendingTarget = useRef(null);
  const setTarget = useCallback((target) => {
    pendingTarget.current = { target };
  }, []);
  const collapseButtonRef = useCallback((node) => {
    if (node && pendingTarget.current?.target === "collapse") {
      node.focus();
      pendingTarget.current = null;
    }
  }, []);
  const expandButtonRef = useCallback((node) => {
    if (node && pendingTarget.current?.target === "expand") {
      node.focus();
      pendingTarget.current = null;
    }
  }, []);
  return {
    setTarget,
    collapseButtonRef,
    expandButtonRef
  };
}
export {
  useButtonFocusHandover
};
//# sourceMappingURL=useFocusHandover.js.map
