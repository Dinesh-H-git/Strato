import { createContext, useContext } from "react";
const PageLayoutPortalContext = createContext(null);
function usePageLayoutPortalContext() {
  const ctx = useContext(PageLayoutPortalContext);
  if (ctx === null) {
    throw new Error(
      "PageLayout slot components (Header, Sidebar, Content, Details) must be rendered inside a <PageLayout>."
    );
  }
  return ctx;
}
export {
  PageLayoutPortalContext,
  usePageLayoutPortalContext
};
//# sourceMappingURL=PageLayoutPortalContext.js.map
