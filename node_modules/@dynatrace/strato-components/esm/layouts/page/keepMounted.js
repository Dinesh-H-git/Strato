import { jsx, jsxs } from "react/jsx-runtime";
import {
  createContext,
  useCallback,
  useContext,
  useEffect,
  useMemo,
  useRef,
  useState
} from "react";
import { createPortal } from "react-dom";
import { useId } from "../../core/hooks/useId.js";
import { OverlayStackingProvider } from "../../core/providers/OverlayStackingProvider.js";
const useKeepMounted = ({
  children,
  keepMounted = false
}) => {
  const id = useId();
  if (!keepMounted) {
    return /* @__PURE__ */ jsx("div", { style: { height: "100%" }, children });
  }
  return /* @__PURE__ */ jsx(KeepMounted, { id, style: { height: "100%" }, children });
};
const KeepMounted = (props) => {
  const { children, style, className, id } = props;
  const { getPortalElement } = useContext(keepMountedContext);
  const keepMountedRef = useRef(null);
  const appendPortalElement = useCallback(() => {
    const portalElement = getPortalElement(id, children);
    if (portalElement === void 0) {
      return;
    }
    keepMountedRef.current?.appendChild(portalElement);
  }, [children, getPortalElement, id]);
  useEffect(() => {
    appendPortalElement();
  }, [appendPortalElement]);
  return /* @__PURE__ */ jsx("div", { style, className, ref: keepMountedRef });
};
const keepMountedContext = createContext({
  getPortalElement: () => void 0
});
keepMountedContext.displayName = "KeepMountextContext";
const KeepMountedProvider = ({ children }) => {
  const [portals, setPortals] = useState(
    () => /* @__PURE__ */ new Map()
  );
  const getPortalElement = useCallback(
    (id, children2) => {
      const keepPortal = portals.get(id);
      if (!keepPortal) {
        const element = document.createElement("div");
        setPortals(new Map(portals.set(id, { children: children2, element })));
        return element;
      }
      if (keepPortal.children !== children2) {
        setPortals(new Map(portals.set(id, { ...keepPortal, children: children2 })));
      }
      return keepPortal?.element;
    },
    [portals]
  );
  const ctx = useMemo(() => ({ getPortalElement }), [getPortalElement]);
  return /* @__PURE__ */ jsxs(keepMountedContext.Provider, { value: ctx, children: [
    children,
    /* @__PURE__ */ jsx(OverlayStackingProvider, { children: Array.from(portals.entries()).map(([id, { children: children2, element }]) => {
      return createPortal(children2, element, id);
    }) })
  ] });
};
export {
  KeepMountedProvider,
  useKeepMounted
};
//# sourceMappingURL=keepMounted.js.map
