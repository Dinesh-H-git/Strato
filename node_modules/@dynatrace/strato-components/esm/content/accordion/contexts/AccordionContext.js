import { jsx } from "react/jsx-runtime";
import { createContext, useContext } from "react";
const AccordionContext = createContext({
  triggerPosition: "end",
  interactive: true,
  color: "neutral",
  size: "default",
  state: void 0,
  dispatch: void 0
});
function AccordionContextProvider(props) {
  return /* @__PURE__ */ jsx(AccordionContext.Provider, { value: props.value, children: props.children });
}
function useAccordionContext() {
  const { triggerPosition, interactive, color, size, state, dispatch } = useContext(AccordionContext);
  if (state === void 0 || dispatch === void 0) {
    throw new Error(
      "Accordion context is used outside of an Accordion. Please make sure you are using the correct Accordion components."
    );
  }
  return {
    triggerPosition,
    interactive,
    color,
    size,
    state,
    dispatch
  };
}
export {
  AccordionContextProvider,
  useAccordionContext
};
//# sourceMappingURL=AccordionContext.js.map
