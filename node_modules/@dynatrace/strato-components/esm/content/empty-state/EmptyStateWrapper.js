import { jsx } from "react/jsx-runtime";
import clsx from "clsx";
import { forwardRef, useEffect, useRef, useState } from "react";
import useResizeObserver from "use-resize-observer";
import { _MIN_CONTAINER_WIDTH } from "./empty-state-utils.js";
import { emptyStateContainerCss } from "./EmptyState.sty.js";
import { useMergeRefs } from "../../core/hooks/useMergeRefs.js";
import { Flex } from "../../layouts/flex/Flex.js";
const Monitored = /* @__PURE__ */ forwardRef((props, forwardedRef) => {
  const {
    size = "default",
    children,
    className: consumerClassName,
    style: consumerStyle,
    "data-testid": dataTestId,
    "data-dtrum-mask": dataDtrumMask,
    "data-dtrum-allow": dataDtrumAllow,
    "data-dt-component": dataDtComponent,
    ...remainingProps
  } = props;
  const emptyStateContainerRef = useRef(null);
  const emptyStateRef = useMergeRefs([emptyStateContainerRef, forwardedRef]);
  const [emptyStateParentContainer, setEmptyStateParentContainer] = useState(null);
  useEffect(() => {
    if (emptyStateContainerRef.current) {
      setEmptyStateParentContainer(
        emptyStateContainerRef.current.parentElement
      );
    }
  }, []);
  const parentContainerRef = useResizeObserver({
    ref: emptyStateParentContainer
  });
  const parentContainerWidth = parentContainerRef.width ?? 0;
  const isInResponsive = parentContainerWidth < _MIN_CONTAINER_WIDTH && size === "large";
  return /* @__PURE__ */ jsx(
    Flex,
    {
      ref: emptyStateRef,
      className: clsx([
        emptyStateContainerCss({
          size: isInResponsive ? "largeResponsive" : size
        }),
        consumerClassName
      ]),
      style: consumerStyle,
      "data-testid": dataTestId,
      "data-dtrum-mask": dataDtrumMask,
      "data-dtrum-allow": dataDtrumAllow,
      ...remainingProps,
      "data-dt-component": dataDtComponent,
      children
    }
  );
});
const Unmonitored = /* @__PURE__ */ forwardRef((props, forwardedRef) => {
  const {
    size = "default",
    children,
    className: consumerClassName,
    style: consumerStyle,
    "data-testid": dataTestId,
    "data-dtrum-mask": dataDtrumMask,
    "data-dtrum-allow": dataDtrumAllow,
    "data-dt-component": dataDtComponent,
    ...remainingProps
  } = props;
  return /* @__PURE__ */ jsx(
    Flex,
    {
      ref: forwardedRef,
      className: clsx([emptyStateContainerCss({ size }), consumerClassName]),
      style: consumerStyle,
      "data-testid": dataTestId,
      "data-dtrum-mask": dataDtrumMask,
      "data-dtrum-allow": dataDtrumAllow,
      ...remainingProps,
      "data-dt-component": dataDtComponent,
      children
    }
  );
});
const EmptyStateWrapper = /* @__PURE__ */ forwardRef((props, forwardedRef) => {
  const { size = "default" } = props;
  const Wrapper = size === "large" ? Monitored : Unmonitored;
  return /* @__PURE__ */ jsx(Wrapper, { ...props, ref: forwardedRef }, size);
});
export {
  EmptyStateWrapper
};
//# sourceMappingURL=EmptyStateWrapper.js.map
