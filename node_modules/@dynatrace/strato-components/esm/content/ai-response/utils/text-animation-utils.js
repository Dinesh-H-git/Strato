const findNextWordIndex = (text, startIndex) => {
  if (startIndex >= text.length) {
    return text.length;
  }
  const remainingText = text.slice(startIndex);
  const isWhitespace = /\s/.test(text[startIndex]);
  if (isWhitespace) {
    const nextWordIndex2 = remainingText.search(/\S/);
    return nextWordIndex2 === -1 ? text.length : startIndex + nextWordIndex2;
  }
  const nextWhitespaceIndex = remainingText.search(/\s/);
  if (nextWhitespaceIndex === -1) {
    return text.length;
  }
  const nextWordIndex = remainingText.slice(nextWhitespaceIndex).search(/\S/);
  return nextWordIndex === -1 ? text.length : startIndex + nextWhitespaceIndex + nextWordIndex;
};
const findFirstDiffIndex = (oldText, newText) => {
  const maxIndex = Math.min(oldText.length, newText.length);
  for (let i = 0; i < maxIndex; i++) {
    if (oldText[i] !== newText[i]) {
      return i;
    }
  }
  return maxIndex;
};
export {
  findFirstDiffIndex,
  findNextWordIndex
};
//# sourceMappingURL=text-animation-utils.js.map
