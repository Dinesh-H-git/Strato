import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { assignInlineVars } from "@vanilla-extract/dynamic";
import * as styles from "./DataTablePinningShadowCells.sty.js";
import {
  useDataTable,
  useDataTableContent
} from "../../contexts/DataTableContext.js";
function DataTablePinningShadowHeaderCells(props) {
  const {
    isLeftShadow,
    isRightShadow,
    leftPosition,
    rightPosition,
    gridPosition
  } = props;
  const tableContent = useDataTableContent();
  const table = useDataTable();
  const hasLeftPinnedColumns = table.getLeftVisibleLeafColumns().length > 0;
  const hasRightPinnedColumns = table.getRightVisibleLeafColumns().length > 0;
  const showLeftShadow = isLeftShadow && hasLeftPinnedColumns && tableContent.hasOverflowLeft;
  const showRightShadow = isRightShadow && hasRightPinnedColumns && tableContent.hasOverflowRight;
  const leftShadowCell = showLeftShadow ? /* @__PURE__ */ jsx(
    "div",
    {
      role: "presentation",
      className: styles.pinningShadowCell.left,
      style: {
        ...assignInlineVars({
          [styles.leftPositionVar]: `${leftPosition}px`
        }),
        ...gridPosition
      },
      "data-pinned-position": "left"
    },
    "left"
  ) : null;
  const rightShadowCell = showRightShadow ? /* @__PURE__ */ jsx(
    "div",
    {
      role: "presentation",
      className: styles.pinningShadowCell.right,
      style: {
        ...assignInlineVars({
          [styles.rightPositionVar]: `${rightPosition}px`
        }),
        ...gridPosition
      },
      "data-pinned-position": "right"
    },
    "right"
  ) : null;
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    leftShadowCell,
    rightShadowCell
  ] });
}
DataTablePinningShadowHeaderCells.displayName = "DataTablePinningShadowHeaderCells";
export {
  DataTablePinningShadowHeaderCells
};
//# sourceMappingURL=DataTablePinningShadowHeaderCells.js.map
