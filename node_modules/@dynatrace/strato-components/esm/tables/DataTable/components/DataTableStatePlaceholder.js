import { jsx } from "react/jsx-runtime";
import { useIntl } from "react-intl";
import { DataTableEmptyState } from "./DataTableEmptyState.js";
import { useDataTable } from "../contexts/DataTableContext.js";
import { LoadingState } from "../features/LoadingState/LoadingState.js";
import { dataTableMessages } from "../messages.js";
const DataTableStatePlaceholder = (props) => {
  const table = useDataTable();
  const intl = useIntl();
  const isLoading = table.getState().loading;
  if (isLoading) {
    return /* @__PURE__ */ jsx(LoadingState, { headerHeight: table.getTotalHeaderHeight() });
  }
  const { isColumnsEmpty, isDataEmpty, allColumnsHidden, emptyStateSlot } = props;
  if (isDataEmpty) {
    return emptyStateSlot ?? /* @__PURE__ */ jsx(DataTableEmptyState, {});
  }
  if (isColumnsEmpty) {
    return emptyStateSlot ?? /* @__PURE__ */ jsx(DataTableEmptyState, { children: intl.formatMessage(dataTableMessages.noColumnsAvailable) });
  }
  if (allColumnsHidden) {
    return /* @__PURE__ */ jsx(DataTableEmptyState, { children: intl.formatMessage(dataTableMessages.noColumnsVisible) });
  }
  return null;
};
export {
  DataTableStatePlaceholder
};
//# sourceMappingURL=DataTableStatePlaceholder.js.map
