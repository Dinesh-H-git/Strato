const DATA_TABLE_ROW_ID_SEPARATOR = "\u21B3";
function useRowIdentification(props, options) {
  const rowIdentificationFn = props.rowId;
  if (rowIdentificationFn) {
    options.rowIdSeparator = DATA_TABLE_ROW_ID_SEPARATOR;
    options.getRowId = (row, _index, parent) => {
      const rowId = rowIdentificationFn(row);
      if (parent) {
        const parentId = parent.id;
        return `${parentId}${options.rowIdSeparator}${rowId}`;
      }
      return rowId;
    };
  } else {
    options.getRowId = void 0;
  }
}
export {
  DATA_TABLE_ROW_ID_SEPARATOR,
  useRowIdentification
};
//# sourceMappingURL=RowIdentification.js.map
