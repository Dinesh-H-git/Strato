import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useIntl } from "react-intl";
import {
  ArrowSmallDownIcon,
  ArrowSmallUpIcon,
  SorterSmallDoubleIcon
} from "@dynatrace/strato-icons";
import { Menu } from "../../../../navigation/menu/Menu.js";
import {
  useDataTable,
  useDataTableColumn
} from "../../contexts/DataTableContext.js";
import { dataTableMessages } from "../../messages.js";
function SortingColumnActions({
  hasOtherColumnActions
}) {
  const intl = useIntl();
  const table = useDataTable();
  const column = useDataTableColumn();
  if (!column.getCanSort()) {
    return null;
  }
  const columnIsSorted = column.getIsSorted();
  const columnCanMultiSort = column.getCanMultiSort();
  const sortingState = table.getState().sorting ?? [];
  const columnSortIndex = column.getSortIndex();
  const tableIsSorted = sortingState.length > 0;
  const tableIsSingleSorted = sortingState.length === 1;
  const tableIsMultiSorted = sortingState.length > 1;
  const columnIsSingleSorted = tableIsSingleSorted && columnSortIndex === 0;
  const showSortAscending = columnIsSorted !== "asc" || tableIsMultiSorted;
  const showSortDescending = columnIsSorted !== "desc" || tableIsMultiSorted;
  const showClearSingleSort = !tableIsMultiSorted && !!columnIsSorted;
  const singleSortGroup = /* @__PURE__ */ jsxs(Menu.Group, { children: [
    showSortAscending && /* @__PURE__ */ jsxs(
      Menu.Item,
      {
        onSelect: () => column.toggleSorting(false, false),
        "data-dt-name": "ColumnActionSortAscending",
        children: [
          /* @__PURE__ */ jsx(Menu.Prefix, { children: /* @__PURE__ */ jsx(ArrowSmallUpIcon, {}) }),
          !tableIsMultiSorted && columnIsSorted === "desc" ? intl.formatMessage(dataTableMessages.changeToAscending) : intl.formatMessage(dataTableMessages.sortAscending)
        ]
      }
    ),
    showSortDescending && /* @__PURE__ */ jsxs(
      Menu.Item,
      {
        onSelect: () => column.toggleSorting(true, false),
        "data-dt-name": "ColumnActionSortDescending",
        children: [
          /* @__PURE__ */ jsx(Menu.Prefix, { children: /* @__PURE__ */ jsx(ArrowSmallDownIcon, {}) }),
          !tableIsMultiSorted && columnIsSorted === "asc" ? intl.formatMessage(dataTableMessages.changeToDescending) : intl.formatMessage(dataTableMessages.sortDescending)
        ]
      }
    ),
    showClearSingleSort && /* @__PURE__ */ jsxs(
      Menu.Item,
      {
        onSelect: () => column.clearSorting(),
        "data-dt-name": "ColumnActionClearSort",
        children: [
          /* @__PURE__ */ jsx(Menu.Prefix, { children: /* @__PURE__ */ jsx(SorterSmallDoubleIcon, {}) }),
          intl.formatMessage(dataTableMessages.clearSorting)
        ]
      }
    )
  ] });
  let multiSortGroup = null;
  const showMultiSortGroup = columnCanMultiSort && tableIsSorted && !columnIsSingleSorted;
  if (showMultiSortGroup) {
    const showAddAscending = columnIsSorted !== "asc";
    const showAddDescending = columnIsSorted !== "desc";
    const showClearMultiSort = !!columnIsSorted;
    multiSortGroup = /* @__PURE__ */ jsxs(Menu.Group, { children: [
      /* @__PURE__ */ jsx(
        Menu.Label,
        {
          "aria-label": intl.formatMessage(
            dataTableMessages.multiColumnSortingLabel
          ),
          children: intl.formatMessage(dataTableMessages.multiColumnSortingLabel)
        }
      ),
      showAddAscending && /* @__PURE__ */ jsxs(
        Menu.Item,
        {
          onSelect: () => column.toggleSorting(false, true),
          "data-dt-name": "ColumnActionMultiSortAscending",
          children: [
            /* @__PURE__ */ jsx(Menu.Prefix, { children: /* @__PURE__ */ jsx(ArrowSmallUpIcon, {}) }),
            columnIsSorted === "desc" ? intl.formatMessage(dataTableMessages.changeToAscending) : intl.formatMessage(dataTableMessages.addAsAscending)
          ]
        }
      ),
      showAddDescending && /* @__PURE__ */ jsxs(
        Menu.Item,
        {
          onSelect: () => column.toggleSorting(true, true),
          "data-dt-name": "ColumnActionMultiSortDescending",
          children: [
            /* @__PURE__ */ jsx(Menu.Prefix, { children: /* @__PURE__ */ jsx(ArrowSmallDownIcon, {}) }),
            columnIsSorted === "asc" ? intl.formatMessage(dataTableMessages.changeToDescending) : intl.formatMessage(dataTableMessages.addAsDescending)
          ]
        }
      ),
      showClearMultiSort && /* @__PURE__ */ jsxs(
        Menu.Item,
        {
          onSelect: () => column.clearSorting(),
          "data-dt-name": "ColumnActionClearSort",
          children: [
            /* @__PURE__ */ jsx(Menu.Prefix, { children: /* @__PURE__ */ jsx(SorterSmallDoubleIcon, {}) }),
            intl.formatMessage(dataTableMessages.clearSorting)
          ]
        }
      )
    ] });
  }
  if (hasOtherColumnActions) {
    return /* @__PURE__ */ jsxs(Menu.Sub, { children: [
      /* @__PURE__ */ jsx(Menu.SubTrigger, { children: intl.formatMessage(dataTableMessages.sortSubmenuTrigger) }),
      /* @__PURE__ */ jsxs(Menu.SubContent, { children: [
        singleSortGroup,
        multiSortGroup
      ] })
    ] });
  }
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    singleSortGroup,
    multiSortGroup
  ] });
}
export {
  SortingColumnActions
};
//# sourceMappingURL=SortingColumnActions.js.map
