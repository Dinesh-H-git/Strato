import { jsx } from "react/jsx-runtime";
import {
  Children
} from "react";
import { isElement } from "react-is";
import { isProduction } from "../../../core/utils/is-node-environment.js";
function DataTableDevTools({
  devTools
}) {
  return null;
}
function isDataTableDevTools(child) {
  return isElement(child) && child.type === DataTableDevTools;
}
function _getDevToolsSlot(props, table) {
  if (isProduction()) {
    return null;
  }
  const devToolsSlot = Children.toArray(props.children).find(
    isDataTableDevTools
  );
  if (devToolsSlot) {
    const DevTools = devToolsSlot.props.devTools;
    if (DevTools) {
      return /* @__PURE__ */ jsx(DevTools, { table });
    }
  }
  return null;
}
export {
  DataTableDevTools,
  _getDevToolsSlot
};
//# sourceMappingURL=DevTools.js.map
