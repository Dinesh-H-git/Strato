import {
  useCallback,
  useMemo,
  useRef
} from "react";
import {
  getToolbarConfig
} from "../components/Toolbar/DataTableToolbar.js";
function useToolbarSlot(toolbarProps) {
  const { children: toolbarContent, ...toolbarRemainingProps } = toolbarProps ?? {};
  const { slots: toolbarSlots, toolbarItemProps } = useMemo(
    () => getToolbarConfig(toolbarContent),
    [toolbarContent]
  );
  const columnSettingsModalRef = useRef(null);
  const openColumnSettings = useCallback(
    (columnSettings) => {
      columnSettingsModalRef.current?.openColumnSettings(columnSettings);
    },
    []
  );
  return {
    toolbarSlots,
    toolbarContent,
    toolbarItemProps,
    toolbarRemainingProps,
    openColumnSettings,
    columnSettingsModalRef
  };
}
export {
  useToolbarSlot
};
//# sourceMappingURL=useToolbarSlot.js.map
