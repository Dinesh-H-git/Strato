import { splitAccessorPath } from "../../../core/utils/accessors/split-accessor-path.js";
function makeAccessorFunction(accessor) {
  const accessPath = splitAccessorPath(accessor);
  return function(row) {
    let currentObj = row;
    for (const accessPathPart of accessPath) {
      currentObj = (currentObj ?? {})[accessPathPart];
    }
    return currentObj;
  };
}
export {
  makeAccessorFunction
};
//# sourceMappingURL=makeAccessorFunction.js.map
