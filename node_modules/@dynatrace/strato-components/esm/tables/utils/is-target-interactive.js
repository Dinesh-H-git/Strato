function isTargetInteractive(row, target) {
  if (row === target) {
    return false;
  }
  const interactiveTags = ["A", "BUTTON", "LABEL", "INPUT", "SELECT"];
  let currentElement = target;
  do {
    const hitInteractiveElement = currentElement?.getAttribute("tabindex") !== null || interactiveTags.includes(currentElement?.tagName);
    if (hitInteractiveElement) {
      return true;
    }
    currentElement = currentElement?.parentElement;
  } while (currentElement !== row && currentElement !== null);
  return false;
}
export {
  isTargetInteractive
};
//# sourceMappingURL=is-target-interactive.js.map
