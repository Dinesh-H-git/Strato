import { jsx } from "react/jsx-runtime";
import {
  useContext,
  useMemo,
  createContext
} from "react";
import { deserializeDataTableConfig } from "../utils/datatable-config-utils.js";
const DataTableConfigContext = createContext({});
function DataTableConfigProvider(props) {
  const { children, value } = props;
  const deserializedConfig = useMemo(() => {
    return typeof value === "string" ? deserializeDataTableConfig(value) : value;
  }, [value]);
  return /* @__PURE__ */ jsx(DataTableConfigContext.Provider, { value: deserializedConfig, children });
}
function useDataTableConfigElement(key) {
  const contextConfig = useContext(DataTableConfigContext);
  if (typeof contextConfig?.[key] === "undefined" || contextConfig?.[key] === null) {
    return void 0;
  }
  return contextConfig[key];
}
export {
  DataTableConfigProvider,
  useDataTableConfigElement
};
//# sourceMappingURL=useDataTableConfig.js.map
