import { jsx } from "react/jsx-runtime";
import {
  useContext,
  useMemo,
  useState
} from "react";
import { ModalContext } from "../contexts/ModalContext.js";
function ModalContextProvider(props) {
  const { children } = props;
  const parent = useContext(ModalContext);
  const [modalCount, setModalCount] = useState(0);
  const contextValue = useMemo(
    () => ({
      parent,
      modalCount,
      addModal() {
        setModalCount((count) => count + 1);
        if (parent) {
          parent.addModal();
        }
      },
      removeModal() {
        setModalCount((count) => count - 1);
        if (parent) {
          parent.removeModal();
        }
      }
    }),
    [parent, modalCount]
  );
  return /* @__PURE__ */ jsx(ModalContext.Provider, { value: contextValue, children });
}
export {
  ModalContextProvider
};
//# sourceMappingURL=ModalContextProvider.js.map
