import { useContext } from "react";
import { useStore } from "zustand";
import {
  CalendarStoreCtx,
  DateTimePickerStoreCtx,
  DateTimeRangePickerStoreCtx,
  TimeframeSelectorStoreCtx
} from "./timeframeStoreProvider.js";
function useCalendarStore(selector) {
  const store = useContext(CalendarStoreCtx);
  if (!store) {
    throw new Error(
      "useCalendarStore must be used within a CalendarStoreProvider"
    );
  }
  return useStore(store, selector);
}
function useDateTimePickerStore(selector) {
  const store = useContext(DateTimePickerStoreCtx);
  if (!store) {
    throw new Error(
      "useDateTimePickerStore must be used within a DateTimePickerStoreProvider"
    );
  }
  return useStore(store, selector);
}
function useDateTimeRangePickerStore(selector) {
  const store = useContext(DateTimeRangePickerStoreCtx);
  if (!store) {
    throw new Error(
      "useDateTimeRangePickerStore must be used within a DateTimeRangePickerStoreProvider"
    );
  }
  return useStore(store, selector);
}
function useTimeframeSelectorStore(selector) {
  const store = useContext(TimeframeSelectorStoreCtx);
  if (!store) {
    throw new Error(
      "useTimeframeSelectorStore must be used within a TimeframeSelectorStoreProvider"
    );
  }
  return useStore(store, selector);
}
export {
  useCalendarStore,
  useDateTimePickerStore,
  useDateTimeRangePickerStore,
  useTimeframeSelectorStore
};
//# sourceMappingURL=useTimeframeStore.js.map
