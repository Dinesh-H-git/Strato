import { getAccessorParts } from "./parse-accessor.js";
function accessValue(record, accessor) {
  const accessorParts = getAccessorParts(accessor);
  let value = record;
  for (let i = 0; i < accessorParts.length; i++) {
    const part = accessorParts[i];
    value = value?.[part];
    if (value === void 0) {
      return void 0;
    }
  }
  return value;
}
export {
  accessValue
};
//# sourceMappingURL=access-value.js.map
