function createDOMRect(position, size) {
  const { x = 0, y = 0 } = position;
  return () => {
    const values = {
      bottom: y,
      height: size ? size.height : 0,
      width: size ? size.width : 0,
      top: y,
      left: x,
      right: x,
      x,
      y
    };
    return {
      ...values,
      toJSON: () => JSON.parse(JSON.stringify(values))
    };
  };
}
export {
  createDOMRect
};
//# sourceMappingURL=create-dom-rect.js.map
