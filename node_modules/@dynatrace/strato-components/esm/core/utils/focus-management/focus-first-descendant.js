import { attemptFocus } from "./attempt-focus.js";
const focusFirstDescendant = (element, options) => {
  if (!element) {
    return false;
  }
  for (const child of element.children) {
    const childElement = child;
    if (attemptFocus(childElement, options) || focusFirstDescendant(childElement, options)) {
      return true;
    }
  }
  return false;
};
export {
  focusFirstDescendant
};
//# sourceMappingURL=focus-first-descendant.js.map
