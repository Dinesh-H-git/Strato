import { useMemo } from "react";
function useFormControlState(formControlState) {
  const [isError, hint] = useMemo(() => {
    const controlState = formControlState && typeof formControlState === "string" ? { state: formControlState } : formControlState;
    return [controlState?.state === "error", controlState?.hint];
  }, [formControlState]);
  return [isError, hint];
}
export {
  useFormControlState
};
//# sourceMappingURL=useFormControlState.js.map
