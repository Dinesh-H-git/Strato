import { useMemo } from "react";
import { accessValue } from "../../../core/utils/accessors/access-value.js";
function useLabelCount(data, labelAccessor) {
  return useMemo(
    () => data.reduce((total, record) => {
      if (typeof record === "object") {
        if (labelAccessor) {
          const label = accessValue(record, labelAccessor);
          if (label) {
            return total + 1;
          }
        }
      }
      return total;
    }, 0),
    [data, labelAccessor]
  );
}
export {
  useLabelCount
};
//# sourceMappingURL=use-label-count.js.map
