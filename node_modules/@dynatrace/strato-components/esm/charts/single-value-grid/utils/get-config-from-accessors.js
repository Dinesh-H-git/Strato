import { accessValue } from "../../../core/utils/accessors/access-value.js";
function getConfigFromAccessors(data, accessors) {
  const accessedConfig = {};
  if (accessors.colorAccessor) {
    accessedConfig.color = accessValue(data, accessors.colorAccessor);
  }
  if (accessors.value) {
    accessedConfig.value = {};
    if (accessors.value.formatterAccessor) {
      accessedConfig.value.formatter = accessValue(data, accessors.value.formatterAccessor);
    }
    if (accessors.value.unitAccessor) {
      accessedConfig.value.unit = accessValue(
        data,
        accessors.value.unitAccessor
      );
    }
  }
  if (accessors.trend) {
    accessedConfig.trend = {};
    if (accessors.trend.formatterAccessor) {
      accessedConfig.trend.formatter = accessValue(data, accessors.trend.formatterAccessor);
    }
    if (accessors.trend.inverseTrendAccessor) {
      accessedConfig.trend.inverseTrend = accessValue(
        data,
        accessors.trend.inverseTrendAccessor
      );
    }
  }
  if (accessors.sparkline) {
    accessedConfig.sparkline = {};
    if (accessors.sparkline.colorAccessor) {
      accessedConfig.sparkline.color = accessValue(
        data,
        accessors.sparkline.colorAccessor
      );
    }
  }
  return accessedConfig;
}
export {
  getConfigFromAccessors
};
//# sourceMappingURL=get-config-from-accessors.js.map
