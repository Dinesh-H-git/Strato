import { createContext, useContext } from "react";
import {
  buildSingleValueConfig,
  deserializeConfig,
  isSerializedConfig
} from "../utils/single-value-config.js";
const SingleValueConfigContext = createContext({});
const SingleValueConfig = SingleValueConfigContext.Provider;
const useSingleValueConfig = (props, slots) => {
  let contextConfig = useContext(SingleValueConfigContext);
  if (isSerializedConfig(contextConfig)) {
    contextConfig = deserializeConfig(contextConfig);
  }
  return buildSingleValueConfig(props, slots, contextConfig);
};
export {
  SingleValueConfig,
  SingleValueConfigContext,
  useSingleValueConfig
};
//# sourceMappingURL=use-single-value-config.js.map
