import {
  BAR_VARIANT_GROUP
} from "../../../types/xy-chart.js";
const isGrouping = (variant, chartOrientation, xAxis, yAxis) => {
  if (variant !== BAR_VARIANT_GROUP || !chartOrientation || chartOrientation === "both") {
    return false;
  }
  const primaryAxis = chartOrientation === "vertical" ? xAxis : yAxis;
  return primaryAxis.type === "categorical";
};
export {
  isGrouping
};
//# sourceMappingURL=group-bar-datapoints.js.map
