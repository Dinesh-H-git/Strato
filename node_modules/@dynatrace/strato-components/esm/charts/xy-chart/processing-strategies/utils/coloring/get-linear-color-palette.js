import { getValidColorPalette } from "./get-valid-color-palette.js";
import { getColorPalette } from "../../../../core/utils/get-color-palette.js";
import { DEFAULT_COLOR_PALETTE } from "../../../constants/defaults.js";
import {
  getColorPaletteSelection,
  getColorSelection
} from "../../../utils/coloring/get-color-selection.js";
function getLinearColor(datum, seriesProps, configColorPalette, seriesName, seriesIndex) {
  const validatedColorPalette = getColorPaletteSelection(seriesProps.colorPalette, datum) ?? configColorPalette ?? DEFAULT_COLOR_PALETTE;
  const assignedColor = getColorPalette(
    seriesName,
    seriesIndex,
    // @ts-expect-error - todo
    validatedColorPalette
  );
  const colorSelection = getColorSelection(seriesProps.color, datum) ?? assignedColor;
  const validColorPalette = getValidColorPalette(
    configColorPalette,
    validatedColorPalette
  );
  const validColor = colorSelection ? colorSelection : getColorPalette(
    seriesName,
    seriesIndex,
    // @ts-expect-error - todo
    validColorPalette
  );
  return {
    color: validColor,
    colorPalette: validColorPalette,
    colorIsFromSlot: !!seriesProps.color || validatedColorPalette === seriesProps.colorPalette
  };
}
export {
  getLinearColor
};
//# sourceMappingURL=get-linear-color-palette.js.map
