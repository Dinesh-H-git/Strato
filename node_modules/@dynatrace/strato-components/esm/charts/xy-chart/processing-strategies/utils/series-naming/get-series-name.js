import { XYCHART_MESSAGES } from "../../../messages/messages.js";
import { isValidName } from "../../../utils/type-guards.js";
import { getDatumValue } from "../access-datapoint/get-datapoints.js";
function getSeriesNameWithFallback(intl, seriesIndex, seriesNameFallback) {
  return isValidName(seriesNameFallback) ? String(seriesNameFallback) : intl.formatMessage(XYCHART_MESSAGES.unnamedSeriesMessage, {
    index: seriesIndex + 1
  });
}
function getSeriesNameWithAccessor(intl, seriesIndex, seriesNameFallback, seriesNameAccessor, datum) {
  const seriesNameFromAccessor = getDatumValue(datum, seriesNameAccessor);
  const finalSeriesName = seriesNameFromAccessor ?? seriesNameFallback;
  const seriesNameValidated = getSeriesNameWithFallback(
    intl,
    seriesIndex,
    finalSeriesName
  );
  return seriesNameValidated;
}
export {
  getSeriesNameWithAccessor,
  getSeriesNameWithFallback
};
//# sourceMappingURL=get-series-name.js.map
