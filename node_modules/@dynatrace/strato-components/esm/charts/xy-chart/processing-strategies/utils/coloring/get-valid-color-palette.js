import { isCustomColorPalette } from "../../../../core/types/color-palette.js";
import { isColoredRangeArray } from "../../../../core/types/colored-range.js";
import { isValidColorPalette } from "../../../../core/utils/is-valid-color-palette.js";
function getValidColorPalette(colorPalette, fallbackColorPalette) {
  return colorPalette && (isColoredRangeArray(colorPalette) || isCustomColorPalette(colorPalette) || isValidColorPalette(colorPalette)) ? colorPalette : fallbackColorPalette;
}
export {
  getValidColorPalette
};
//# sourceMappingURL=get-valid-color-palette.js.map
