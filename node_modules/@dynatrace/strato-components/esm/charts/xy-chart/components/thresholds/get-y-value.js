import { isDataRange } from "./type-guards.js";
const getYValue = (data, type) => {
  const key = type === "min" ? "yMin" : "yMax";
  if (isDataRange(data)) {
    return data[key];
  }
  return data.y;
};
export {
  getYValue
};
//# sourceMappingURL=get-y-value.js.map
