import { jsx } from "react/jsx-runtime";
import { HoveredPointTooltip } from "./HoveredPointTooltip/HoveredPointTooltip.js";
import { TopListTooltip } from "./TopListTooltip.js";
import { useBarData } from "../../hooks/useBarData.js";
function BarSeriesTooltip({
  datapoint,
  series
}) {
  const { seriesToMetadata } = useBarData();
  if (series.labelAccessor) {
    return /* @__PURE__ */ jsx(
      TopListTooltip,
      {
        datapoint,
        series,
        seriesToMetadata
      }
    );
  }
  return /* @__PURE__ */ jsx(
    HoveredPointTooltip,
    {
      datapoint,
      series,
      seriesToMetadata
    }
  );
}
export {
  BarSeriesTooltip
};
//# sourceMappingURL=BarSeriesTooltip.js.map
