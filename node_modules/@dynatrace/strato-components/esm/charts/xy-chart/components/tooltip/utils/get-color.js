import { safeGet } from "../../../../core/utils/safe-get.js";
import { getColorFromScaleAndData } from "../../../utils/coloring/get-color-from-scale-and-data.js";
import { isRectSeries } from "../../../utils/type-guards.js";
const getColor = (series, datapoint, metadata, colorScaleMap, colorOverride) => {
  if (isRectSeries(series)) {
    const colorScale = safeGet(colorScaleMap, series.id);
    return typeof datapoint.value === "string" || typeof datapoint.value === "number" ? getColorFromScaleAndData(colorScale, datapoint.value, colorOverride) : "";
  }
  return metadata.color.Default;
};
export {
  getColor
};
//# sourceMappingURL=get-color.js.map
