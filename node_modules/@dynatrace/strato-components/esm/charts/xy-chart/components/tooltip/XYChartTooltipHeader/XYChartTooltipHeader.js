import { jsx } from "react/jsx-runtime";
import { ChartTooltip } from "../../../../core/components/tooltip/ChartTooltip/ChartTooltip.js";
function XYChartTooltipHeader({
  color,
  name,
  actions,
  defaultActions,
  truncationMode,
  value,
  shape
}) {
  const variant = shape === "circle" ? "single" : shape;
  return /* @__PURE__ */ jsx(ChartTooltip.Body, { children: /* @__PURE__ */ jsx(
    ChartTooltip.SeriesItem,
    {
      color,
      name,
      actions,
      defaultActions,
      truncationMode,
      value,
      variant
    }
  ) });
}
XYChartTooltipHeader.displayName = "XYChartTooltipHeader";
export {
  XYChartTooltipHeader
};
//# sourceMappingURL=XYChartTooltipHeader.js.map
