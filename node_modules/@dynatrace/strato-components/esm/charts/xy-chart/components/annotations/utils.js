import { isAnyNumericalAxis } from "../../types/guards.js";
const isValidAnnotationsAxis = (xAxisId, axesConfig) => {
  const axis = axesConfig.find((axis2) => axis2.id === xAxisId);
  return !!axis && axis.coordinate === "x" && isAnyNumericalAxis(axis);
};
export {
  isValidAnnotationsAxis
};
//# sourceMappingURL=utils.js.map
