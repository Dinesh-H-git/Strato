import {
  useSelector,
  useTooltipSnapshot
} from "../../../../core/components/overlay-tooltip/selectors.js";
import { UndefinedContent } from "../../../types/state.js";
const VariantToGeometryMap = {
  linear: /* @__PURE__ */ new Set(["dot", "line", "area"]),
  rectangle: /* @__PURE__ */ new Set(["rect", "bar"])
};
const useTooltipMetadata = (variant) => {
  useSelector(({ data: data2 }) => {
    const metadata = data2?.metadata || UndefinedContent;
    if (VariantToGeometryMap[variant].has(metadata.type)) {
      return (metadata || UndefinedContent || void 0)?.datapoint;
    }
  });
  const { data } = useTooltipSnapshot();
  return data.metadata || UndefinedContent;
};
export {
  useTooltipMetadata
};
//# sourceMappingURL=useTooltipMetadata.js.map
