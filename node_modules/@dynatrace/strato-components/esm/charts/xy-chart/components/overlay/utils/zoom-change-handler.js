import { isTimeAxis } from "../../../types/guards.js";
function handleZoomChange(axis, min, max, zoomType) {
  const timeAxis = isTimeAxis(axis);
  axis.onZoomChange(
    timeAxis ? new Date(min) : min,
    timeAxis ? new Date(max) : max,
    zoomType
  );
}
export {
  handleZoomChange
};
//# sourceMappingURL=zoom-change-handler.js.map
