const getHighlightedSeries = (seriesWithDatapoints, highlightedSeriesId) => {
  return seriesWithDatapoints.find((s) => {
    if (s.type !== "rect") {
      return highlightedSeriesId === s.id;
    }
    return highlightedSeriesId?.includes(s.id);
  });
};
export {
  getHighlightedSeries
};
//# sourceMappingURL=get-highlighted-series.js.map
