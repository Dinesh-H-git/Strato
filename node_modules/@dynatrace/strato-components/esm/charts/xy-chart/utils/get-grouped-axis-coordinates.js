const getGroupedAxisCoordinates = (axisIdToDimension, axes) => new Set(
  [...axisIdToDimension.keys()].map((axisId) => {
    const axis = axes.find((a) => a.id === axisId);
    return axis?.coordinate;
  })
);
export {
  getGroupedAxisCoordinates
};
//# sourceMappingURL=get-grouped-axis-coordinates.js.map
