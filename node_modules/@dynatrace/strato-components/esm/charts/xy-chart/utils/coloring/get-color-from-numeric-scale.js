import { SCALE_FALLBACK_COLOR } from "../../../core/constants.js";
const getColorFromNumericScale = (colorScale, value) => {
  const colorDomain = colorScale.domain();
  const [min, max] = [
    colorDomain[0],
    colorDomain[colorDomain.length - 1]
  ];
  if (value >= min && value <= max) {
    const scale = colorScale;
    return scale(value);
  }
  return SCALE_FALLBACK_COLOR;
};
export {
  getColorFromNumericScale
};
//# sourceMappingURL=get-color-from-numeric-scale.js.map
