import { get, isNil } from "lodash-es";
import { getFallbackValueAccessor } from "../get-fallback-value-accessor.js";
const hasSeriesMatchingDatapoint = (series, rule, isValid) => {
  const accessor = rule.valueAccessor ?? getFallbackValueAccessor(series);
  return series.data.some((datapoint) => {
    const subject = get(datapoint, accessor);
    return !isNil(subject) && isValid(subject, rule.comparator, rule.matchValue);
  });
};
export {
  hasSeriesMatchingDatapoint
};
//# sourceMappingURL=has-series-matching-datapoint.js.map
