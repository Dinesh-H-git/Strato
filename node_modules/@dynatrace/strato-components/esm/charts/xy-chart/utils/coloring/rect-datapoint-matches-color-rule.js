import { get, isNil } from "lodash-es";
const rectDatapointMatchesColorRule = (datapoint, rule, isValid) => {
  const accessor = rule.valueAccessor ?? "value";
  const subject = get(datapoint, accessor);
  return !isNil(subject) && isValid(subject, rule.comparator, rule.matchValue);
};
export {
  rectDatapointMatchesColorRule
};
//# sourceMappingURL=rect-datapoint-matches-color-rule.js.map
