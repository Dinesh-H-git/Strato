import { getColorPalette } from "../../../core/utils/get-color-palette.js";
const applyColorRuleToSeriesMetadata = (seriesToMetadata, series, seriesMetadata, rule, index) => {
  const colorOverride = rule.colorPalette ? getColorPalette(seriesMetadata.name, index, rule.colorPalette) : { Default: rule.color };
  if (colorOverride) {
    seriesToMetadata.set(series, {
      ...seriesMetadata,
      color: colorOverride
    });
  }
};
export {
  applyColorRuleToSeriesMetadata
};
//# sourceMappingURL=apply-color-rule-to-series-metadata.js.map
