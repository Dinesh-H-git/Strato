import { safeGet } from "../../core/utils/safe-get.js";
import { BAR_VARIANT } from "../types/xy-chart-internals.js";
function getCurrentScale(axisIdToScales, axisId, seriesType) {
  const scales = safeGet(axisIdToScales, axisId);
  return seriesType === BAR_VARIANT && scales.categoricalBarScale ? scales.categoricalBarScale : scales.currentScale;
}
function getInitialScale(axisIdToScales, axisId) {
  return safeGet(axisIdToScales, axisId).initialScale;
}
function getDimensionScale(axisIdToScales, axisId) {
  return safeGet(axisIdToScales, axisId).dimensionScale;
}
export {
  getCurrentScale,
  getDimensionScale,
  getInitialScale
};
//# sourceMappingURL=get-scale.js.map
