import { OVERLAY_PADDING } from "../../core/components/data-point/constants.js";
const AXES = ["top", "bottom", "left", "right"];
const Paddings = () => ({
  top: 0,
  bottom: 0,
  left: 0,
  right: 0,
  horizontal: function() {
    return this.left + this.right;
  },
  vertical: function() {
    return this.top + this.bottom;
  }
});
const getPaddingByAxes = (axes, padding = OVERLAY_PADDING) => {
  return AXES.reduce((acc, axis) => {
    acc[axis] = axes[axis] && !axes[axis].hidden ? 0 : padding;
    return acc;
  }, Paddings());
};
export {
  getPaddingByAxes
};
//# sourceMappingURL=get-padding-by-axes.js.map
