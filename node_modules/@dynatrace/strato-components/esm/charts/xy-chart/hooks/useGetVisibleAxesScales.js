import { useVisibleAxesScales } from "./useVisibleAxesScales.js";
const useGetVisibleAxesScales = () => {
  const { top, bottom, left, right } = useVisibleAxesScales();
  const xScale = bottom || top;
  const yScale = left || right;
  const hasXAndYScale = !!(xScale && yScale);
  if (hasXAndYScale) {
    return {
      hasXAndYScale: true,
      xScale,
      yScale
    };
  }
  return {
    hasXAndYScale: false,
    xScale: null,
    yScale: null
  };
};
export {
  useGetVisibleAxesScales
};
//# sourceMappingURL=useGetVisibleAxesScales.js.map
