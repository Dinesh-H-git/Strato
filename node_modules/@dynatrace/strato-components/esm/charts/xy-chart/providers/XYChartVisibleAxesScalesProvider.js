import { jsx } from "react/jsx-runtime";
import { XYChartVisibleAxesScalesContext } from "../context/axes/XYChartVisibleAxesScales.context.js";
import { useVisibleAxesConfig } from "../hooks/useVisibleAxesConfig.js";
import { useXYChartScales } from "../hooks/useXYChartScales.js";
import { getCurrentScale } from "../utils/get-scale.js";
function XYChartVisibleAxesScalesProvider({
  children
}) {
  const axisIdToScales = useXYChartScales();
  const { top, bottom, left, right } = useVisibleAxesConfig();
  const scales = {
    top: top ? getCurrentScale(axisIdToScales, top.id) : void 0,
    bottom: bottom ? getCurrentScale(axisIdToScales, bottom.id) : void 0,
    left: left ? getCurrentScale(axisIdToScales, left.id) : void 0,
    right: right ? getCurrentScale(axisIdToScales, right.id) : void 0
  };
  return /* @__PURE__ */ jsx(XYChartVisibleAxesScalesContext.Provider, { value: scales, children });
}
export {
  XYChartVisibleAxesScalesProvider
};
//# sourceMappingURL=XYChartVisibleAxesScalesProvider.js.map
