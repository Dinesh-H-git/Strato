import { jsx } from "react/jsx-runtime";
import { useRef } from "react";
import { createStore } from "../../../core/store/store.js";
import { initialState } from "../constants/defaults.js";
import { XYChartStoreContext } from "../context/XYChartStore.context.js";
const XYChartStoreProvider = ({ children }) => {
  const storeRef = useRef(void 0);
  const lastInteraction = storeRef?.current?.getState().interactionMode;
  storeRef.current = createStore({
    ...initialState,
    interactionMode: lastInteraction || "explore"
  });
  return /* @__PURE__ */ jsx(XYChartStoreContext.Provider, { value: storeRef.current, children });
};
export {
  XYChartStoreProvider
};
//# sourceMappingURL=XYChartStoreProvider.js.map
