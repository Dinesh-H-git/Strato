import { jsx } from "react/jsx-runtime";
import { safeGet } from "../../core/utils/safe-get.js";
import { AreaDisplay } from "../components/displays/AreaDisplay.js";
import { BarDisplay } from "../components/displays/BarDisplay/BarDisplay.js";
import { DotDisplay } from "../components/displays/DotDisplay.js";
import { LineDisplay } from "../components/displays/LineDisplay.js";
import {
  AREA_VARIANT,
  BAR_VARIANT,
  DOT_VARIANT,
  LINE_VARIANT
} from "../types/xy-chart-internals.js";
function XYChartShiftedDisplay({
  series,
  axisIdToScales,
  seriesToMetadata
}) {
  switch (series.type) {
    case DOT_VARIANT:
      return /* @__PURE__ */ jsx(
        DotDisplay,
        {
          axisIdToScales,
          series,
          metadata: safeGet(
            seriesToMetadata,
            series
          )
        },
        series.id
      );
    case LINE_VARIANT:
      return /* @__PURE__ */ jsx(
        LineDisplay,
        {
          axisIdToScales,
          series,
          metadata: safeGet(
            seriesToMetadata,
            series
          )
        },
        series.id
      );
    case BAR_VARIANT:
      return /* @__PURE__ */ jsx(
        BarDisplay,
        {
          axisIdToScales,
          series,
          metadata: safeGet(
            seriesToMetadata,
            series
          )
        },
        series.id
      );
    case AREA_VARIANT:
      return /* @__PURE__ */ jsx(
        AreaDisplay,
        {
          axisIdToScales,
          series,
          metadata: safeGet(
            seriesToMetadata,
            series
          )
        },
        series.id
      );
    default:
      return null;
  }
}
export {
  XYChartShiftedDisplay
};
//# sourceMappingURL=XYChartShiftedDisplay.js.map
