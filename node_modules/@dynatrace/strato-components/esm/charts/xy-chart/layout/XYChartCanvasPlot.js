import { jsx } from "react/jsx-runtime";
import {
  forwardRef,
  useMemo
} from "react";
import {
  datapointCanvasPlot,
  rectangleCanvasPlot
} from "./XYChartLayout.sty.js";
import { Canvas } from "../../core/components/canvas/Canvas.js";
import { CanvasContext } from "../../core/components/canvas/CanvasContext.js";
import { useGridPlotSize } from "../../core/components/chart-grid/hooks/useGridPlotSize.js";
import { POINT_OVERLAY_PADDING } from "../../core/components/data-point/constants.js";
const XYChartCanvasPlot = forwardRef(
  ({ shifted, children }, canvasRefs) => {
    const { height, width } = useGridPlotSize();
    const plotStyles = shifted ? datapointCanvasPlot : rectangleCanvasPlot;
    const shiftCorrection = shifted ? POINT_OVERLAY_PADDING : 0;
    const shapes = useMemo(
      () => ({
        shapeRefs: /* @__PURE__ */ new Map()
      }),
      []
    );
    return /* @__PURE__ */ jsx(CanvasContext.Provider, { value: shapes, children: /* @__PURE__ */ jsx("div", { className: plotStyles, children: /* @__PURE__ */ jsx(
      Canvas,
      {
        ref: canvasRefs,
        width: width + shiftCorrection,
        height: height + shiftCorrection,
        children
      }
    ) }) });
  }
);
export {
  XYChartCanvasPlot
};
//# sourceMappingURL=XYChartCanvasPlot.js.map
