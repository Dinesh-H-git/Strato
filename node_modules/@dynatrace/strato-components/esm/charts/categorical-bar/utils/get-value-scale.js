import { buildScale } from "../../core/utils/core-scale.js";
const getRange = (size, inverted) => inverted ? [0, size] : [size, 0];
function getCategoricalValueScale(axisBoundaries, maxRange, inverted = false, scale = "linear", configMinMax) {
  const range = getRange(maxRange, inverted);
  const clamp = false;
  return buildScale(scale, range, axisBoundaries, configMinMax).clamp(clamp);
}
export {
  getCategoricalValueScale
};
//# sourceMappingURL=get-value-scale.js.map
