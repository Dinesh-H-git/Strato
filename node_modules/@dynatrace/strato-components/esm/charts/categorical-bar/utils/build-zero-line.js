import { isVertical } from "./layout.js";
function buildZeroLine(layout, zeroLineValue, chartWidth, chartHeight) {
  let from, to, show;
  if (isVertical(layout)) {
    show = zeroLineValue <= chartHeight;
    from = { x: 0, y: zeroLineValue };
    to = {
      x: chartWidth,
      y: zeroLineValue
    };
  } else {
    show = zeroLineValue >= 0;
    from = { x: zeroLineValue, y: 0 };
    to = {
      x: zeroLineValue,
      y: chartHeight
    };
  }
  return {
    show,
    from,
    to
  };
}
export {
  buildZeroLine
};
//# sourceMappingURL=build-zero-line.js.map
