const getDistance = (mousePosition, point) => mousePosition - point;
const getClosestDistance = (mousePosition, point1, point2, isVertical, isStacked) => {
  if (isVertical && !isStacked || !isVertical && isStacked) {
    return getDistance(mousePosition.x, point1.x) < getDistance(mousePosition.x, point2.x) ? point1 : point2;
  }
  return getDistance(mousePosition.y, point1.y) < getDistance(mousePosition.y, point2.y) ? point1 : point2;
};
export {
  getClosestDistance
};
//# sourceMappingURL=getClosestDistance.js.map
