import { useContext, createContext } from "react";
import {
  buildCategoricalBarChartConfig,
  deserializeConfig
} from "../utils/categorical-config.js";
import { iterateSlots } from "../utils/iterate-slots.js";
const CategoricalBarChartConfigContext = createContext({});
const CategoricalBarChartConfig = CategoricalBarChartConfigContext.Provider;
const useCategoricalBarChartConfig = (props) => {
  const { children } = props;
  const contextConfig = useContext(CategoricalBarChartConfigContext);
  const slots = iterateSlots(children);
  const isImportedConfig = typeof contextConfig === "string";
  const config = isImportedConfig ? deserializeConfig(contextConfig) : contextConfig;
  return buildCategoricalBarChartConfig(props, config, slots);
};
export {
  CategoricalBarChartConfig,
  CategoricalBarChartConfigContext,
  useCategoricalBarChartConfig
};
//# sourceMappingURL=useCategoricalBarChartConfig.js.map
