import { calculateMousePositions } from "../../../../core/utils/calculate-mouse-positions.js";
import { useSetState } from "../../../hooks/store/selectors.js";
const useCategoricalMouseEventHandlers = () => {
  const setState = useSetState();
  const handleMouseEnter = (event) => {
    setState((prev) => ({
      ...prev,
      overlays: {
        ...prev.overlays,
        mouseInBounds: true,
        mousePosition: calculateMousePositions(event, event.currentTarget),
        visibleTooltip: true
      }
    }));
  };
  const handleMouseMove = (event) => {
    setState((prev) => ({
      ...prev,
      overlays: {
        ...prev.overlays,
        mouseInBounds: true,
        mousePosition: calculateMousePositions(event, event.currentTarget),
        visibleTooltip: true
      }
    }));
  };
  const handleMouseLeave = () => {
    setState((prev) => ({
      ...prev,
      overlays: {
        ...prev.overlays,
        mouseInBounds: false,
        mousePosition: void 0,
        visibleTooltip: prev.overlays.pinnedTooltip
      }
    }));
  };
  const handleFocus = () => {
    setState((prev) => ({
      ...prev,
      overlays: {
        ...prev.overlays,
        mouseInBounds: true
      }
    }));
  };
  const handleBlur = (event) => {
    const nextFocusElement = event.relatedTarget;
    if (!event.target?.contains(nextFocusElement)) {
      setState((prev) => ({
        ...prev,
        overlays: {
          ...prev.overlays,
          mouseInBounds: false
        }
      }));
    }
  };
  return {
    handleMouseEnter,
    handleMouseMove,
    handleMouseLeave,
    handleFocus,
    handleBlur
  };
};
export {
  useCategoricalMouseEventHandlers
};
//# sourceMappingURL=useCategoricalMouseEventHandlers.js.map
