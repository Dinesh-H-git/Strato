import { isLinkAction } from "../../../../core/components/series-action/utils/is-link-action.js";
import { useSetState } from "../../../hooks/store/selectors.js";
const useSeriesActionClickHandler = (hasMultipleItems) => {
  const setState = useSetState();
  const multiItemsActionClickHandler = (e) => {
    if (!isLinkAction(e.target)) {
      e.preventDefault();
    }
    e.stopPropagation();
  };
  const singleItemActionClickHandler = (e) => {
    setState((prev) => ({
      ...prev,
      overlays: { ...prev.overlays, visibleTooltip: false }
    }));
  };
  return hasMultipleItems ? multiItemsActionClickHandler : singleItemActionClickHandler;
};
export {
  useSeriesActionClickHandler
};
//# sourceMappingURL=useSeriesActionClickHandler.js.map
