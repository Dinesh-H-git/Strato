import { jsx, jsxs } from "react/jsx-runtime";
import { CopyIcon } from "@dynatrace/strato-icons";
import { format, formatDate, units } from "@dynatrace-sdk/units";
import { useCopyToClipboard } from "../../../../core/utils/useCopyToClipboard.js";
import { ChartSeriesAction } from "../../../core/components/series-action/ChartSeriesAction.js";
import { ChartTooltip } from "../../../core/components/tooltip/ChartTooltip/ChartTooltip.js";
import { DEFAULT_GANTT_SEGMENT_NAME } from "../../constants/defaults.js";
import { useGanttSegmentsContext } from "../../context/GanttSegmentsContext.js";
import { getEntryColor } from "../../functions/get-entry-color.js";
const GanttChartTooltipBody = ({
  entry,
  row,
  parent,
  isDate
}) => {
  const {
    config: {
      nameAccessor = "name",
      formatter,
      tooltipActions,
      colorAccessor,
      colorPalette
    }
  } = useGanttSegmentsContext();
  const duration = entry.end !== void 0 ? entry.end - entry.start : void 0;
  const name = entry[nameAccessor] || DEFAULT_GANTT_SEGMENT_NAME;
  const [copyName] = useCopyToClipboard(name);
  const [copyStart] = useCopyToClipboard(`${entry.start}`);
  const [copyEnd] = useCopyToClipboard(`${entry.end}`);
  const [copyDuration] = useCopyToClipboard(`${duration}`);
  const formatValue = isDate ? formatDate : format;
  return /* @__PURE__ */ jsx(
    ChartTooltip.SeriesItem,
    {
      color: getEntryColor(entry, colorAccessor, colorPalette),
      name,
      actions: tooltipActions ? tooltipActions(entry, row, parent) : () => /* @__PURE__ */ jsxs(ChartSeriesAction, { children: [
        /* @__PURE__ */ jsxs(ChartSeriesAction.Item, { onSelect: copyName, children: [
          name,
          /* @__PURE__ */ jsx(ChartSeriesAction.ItemIcon, { children: /* @__PURE__ */ jsx(CopyIcon, {}) })
        ] }),
        /* @__PURE__ */ jsxs(ChartSeriesAction.Item, { onSelect: copyStart, children: [
          "Start: ",
          formatValue(entry.start, formatter),
          /* @__PURE__ */ jsx(ChartSeriesAction.ItemIcon, { children: /* @__PURE__ */ jsx(CopyIcon, {}) })
        ] }),
        entry.end && /* @__PURE__ */ jsxs(ChartSeriesAction.Item, { onSelect: copyEnd, children: [
          "End: ",
          formatValue(entry.end, formatter),
          /* @__PURE__ */ jsx(ChartSeriesAction.ItemIcon, { children: /* @__PURE__ */ jsx(CopyIcon, {}) })
        ] }),
        duration && /* @__PURE__ */ jsxs(ChartSeriesAction.Item, { onSelect: copyDuration, children: [
          "Duration:",
          " ",
          format(
            duration,
            isDate && !formatter ? { input: units.time.millisecond } : formatter
          ),
          /* @__PURE__ */ jsx(ChartSeriesAction.ItemIcon, { children: /* @__PURE__ */ jsx(CopyIcon, {}) })
        ] })
      ] })
    }
  );
};
export {
  GanttChartTooltipBody
};
//# sourceMappingURL=GanttChartTooltipBody.js.map
