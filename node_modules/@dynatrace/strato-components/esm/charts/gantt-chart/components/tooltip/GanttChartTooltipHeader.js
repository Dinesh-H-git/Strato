import { jsx } from "react/jsx-runtime";
import { format } from "@dynatrace-sdk/units";
import { ChartTooltip } from "../../../core/components/tooltip/ChartTooltip/ChartTooltip.js";
import { RangeHeader } from "../../../core/components/tooltip/ChartTooltip/components/RangeHeader/RangeHeader.js";
import { TextHeader } from "../../../core/components/tooltip/ChartTooltip/components/TextHeader.js";
const GanttChartTooltipHeader = ({
  segment,
  isDate,
  formatter
}) => {
  const [start, end] = [segment.start, segment.end];
  if (isDate) {
    return end === void 0 || start === end ? /* @__PURE__ */ jsx(ChartTooltip.TimestampHeader, { date: new Date(start) }) : /* @__PURE__ */ jsx(
      ChartTooltip.TimeFrameHeader,
      {
        showDuration: true,
        from: new Date(start),
        to: new Date(end)
      }
    );
  }
  return end === void 0 || start === end ? /* @__PURE__ */ jsx(TextHeader, { text: format(start, formatter) }) : /* @__PURE__ */ jsx(
    RangeHeader,
    {
      from: start,
      to: end,
      formatter: (value) => format(value, formatter)
    }
  );
};
export {
  GanttChartTooltipHeader
};
//# sourceMappingURL=GanttChartTooltipHeader.js.map
