import { DEFAULT_TOOLTIP_HIDDEN } from "../constants.js";
const populateMissingTooltipProps = (tooltipProps, contextConfig) => {
  const { children, ...restProps } = tooltipProps;
  const contextTemplate = contextConfig?.template ?? contextConfig?.children;
  const config = {
    hidden: contextConfig?.hidden ?? DEFAULT_TOOLTIP_HIDDEN
  };
  return {
    ...config,
    ...restProps,
    template: children ?? contextTemplate
  };
};
export {
  populateMissingTooltipProps
};
//# sourceMappingURL=populate-missing-tooltip-props.js.map
