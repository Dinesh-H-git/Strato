import { useContext } from "react";
import { AnnotationsChartConfigContext } from "../contexts/AnnotationsChartConfig.context.js";
import {
  buildAnnotationsChartConfig,
  deserializeConfig,
  isSerializedConfig
} from "../utils/annotations-chart-config.js";
const useAnnotationsChartConfig = (props, xAxis, tooltip, hasNumericalData) => {
  const contextConfig = useContext(AnnotationsChartConfigContext);
  const config = isSerializedConfig(contextConfig) ? deserializeConfig(contextConfig) : contextConfig;
  return buildAnnotationsChartConfig(
    props,
    config,
    xAxis,
    tooltip,
    hasNumericalData
  );
};
export {
  useAnnotationsChartConfig
};
//# sourceMappingURL=useAnnotationsChartConfig.js.map
