import { isNumber } from "lodash-es";
import {
  buildValidator,
  isRangeOperator,
  RANGE_MATCHERS
} from "../../core/utils/validators.js";
const isValidRangeColorRule = (range, rule) => {
  const hasValidValueAccessor = !rule.valueAccessor || ["name", "value"].includes(rule.valueAccessor);
  if (!isRangeOperator(rule.comparator) || !isNumber(rule.matchValue) || !hasValidValueAccessor) {
    return false;
  }
  return buildValidator(RANGE_MATCHERS)(
    range,
    rule.comparator,
    rule.matchValue
  );
};
export {
  isValidRangeColorRule
};
//# sourceMappingURL=is-valid-range-color-rule.js.map
