import { pick } from "lodash-es";
const getRangeLegendWithColorRules = (baseLegendColorMap, colorRulesMap) => {
  const legendColorMap = /* @__PURE__ */ new Map();
  baseLegendColorMap.forEach((value, key) => {
    const range = pick(key, ["from", "to"]);
    const colorRule = colorRulesMap?.for(key);
    legendColorMap.set(range, colorRule ?? value);
  });
  return legendColorMap;
};
export {
  getRangeLegendWithColorRules
};
//# sourceMappingURL=get-range-legend-with-color-rules.js.map
