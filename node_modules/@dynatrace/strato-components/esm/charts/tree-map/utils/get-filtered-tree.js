const hasValue = (node) => node.value > 0;
const getFilteredTree = (rootNode) => {
  const clusters = [...rootNode.children ?? []].filter(hasValue);
  const children = clusters.map((cluster) => {
    const { children: children2, ...clusterRest } = cluster;
    return {
      ...clusterRest,
      children: [...cluster.children ?? []].filter(hasValue)
    };
  });
  return { ...rootNode, children };
};
export {
  getFilteredTree
};
//# sourceMappingURL=get-filtered-tree.js.map
