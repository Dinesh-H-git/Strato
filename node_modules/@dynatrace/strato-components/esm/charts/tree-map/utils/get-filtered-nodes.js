const getFilteredNodes = (nodes, selectedIds) => {
  if (!selectedIds.length) {
    return nodes;
  }
  return nodes.filter((node) => selectedIds.includes(node.name));
};
export {
  getFilteredNodes
};
//# sourceMappingURL=get-filtered-nodes.js.map
