const splitNodesByDepth = (nodes) => {
  const splitNodes = { clusters: [], leafs: [] };
  return nodes.reduce((acc, node) => {
    if (node.depth === 1) {
      acc.clusters.push(node);
    }
    if (node.depth > 1) {
      acc.leafs.push(node);
    }
    return acc;
  }, splitNodes);
};
export {
  splitNodesByDepth
};
//# sourceMappingURL=split-nodes-by-depth.js.map
