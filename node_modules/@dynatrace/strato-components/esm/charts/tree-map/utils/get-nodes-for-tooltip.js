const getNodesForTooltip = (clusterNodes) => {
  const nodes = [];
  clusterNodes.forEach((node) => {
    nodes.push(node);
    node.children?.forEach((leaf) => {
      nodes.push(leaf);
    });
  });
  return nodes;
};
const getSeriesActionsNodes = (nodes) => {
  return nodes.map((node) => ({
    id: node.id,
    name: node.name,
    value: node.value
  }));
};
export {
  getNodesForTooltip,
  getSeriesActionsNodes
};
//# sourceMappingURL=get-nodes-for-tooltip.js.map
