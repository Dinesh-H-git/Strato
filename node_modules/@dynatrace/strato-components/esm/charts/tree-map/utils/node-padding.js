import {
  NODE_LEAF_PADDING,
  NODE_CLUSTER_PADDING,
  NODE_WITH_RECT_CLUSTER_PADDING,
  NODE_WITH_RECT_PADDING_TOP,
  ROOT_NODE_WITH_LABELS_OUTER_PADDING,
  NODE_WITH_RECT_OUTER_PADDING
} from "../constants.js";
function nodePadding(node, hasParentLabel) {
  if (node.height === 0) {
    return NODE_LEAF_PADDING;
  }
  if (hasParentLabel) {
    return node.depth > 0 ? NODE_CLUSTER_PADDING : NODE_WITH_RECT_CLUSTER_PADDING;
  }
  return NODE_CLUSTER_PADDING;
}
const nodePaddingTop = (node) => {
  return node.depth > 0 ? NODE_WITH_RECT_PADDING_TOP : ROOT_NODE_WITH_LABELS_OUTER_PADDING;
};
const nodePaddingOuter = (node) => {
  return node.depth > 0 ? NODE_WITH_RECT_OUTER_PADDING : ROOT_NODE_WITH_LABELS_OUTER_PADDING;
};
export {
  nodePadding,
  nodePaddingOuter,
  nodePaddingTop
};
//# sourceMappingURL=node-padding.js.map
