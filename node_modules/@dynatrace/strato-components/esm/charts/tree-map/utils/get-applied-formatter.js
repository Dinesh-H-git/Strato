import { format } from "@dynatrace-sdk/units";
import { isFormatterOptions } from "../../core/types/formatter.js";
function getAppliedFormatter(formatter) {
  if (isFormatterOptions(formatter)) {
    return (value) => format(value, formatter);
  }
  return formatter;
}
export {
  getAppliedFormatter
};
//# sourceMappingURL=get-applied-formatter.js.map
