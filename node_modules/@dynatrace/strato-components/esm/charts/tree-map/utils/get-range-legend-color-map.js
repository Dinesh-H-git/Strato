import { getTokenComputedStyle } from "../../core/utils/getTokenComputedStyle.js";
const getRangeLegendColorMap = (scale, min, max, hasCustomRanges) => {
  const legendColorMap = /* @__PURE__ */ new Map();
  const ranges = hasCustomRanges ? scale.domain() : [min, ...scale.thresholds(), max];
  for (let i = 0; i < ranges.length - 1; i++) {
    const mapKey = { from: ranges[i], to: ranges[i + 1] };
    const colorToken = { Default: getTokenComputedStyle(scale(ranges[i])) };
    legendColorMap.set(mapKey, { color: colorToken });
  }
  return legendColorMap;
};
export {
  getRangeLegendColorMap
};
//# sourceMappingURL=get-range-legend-color-map.js.map
