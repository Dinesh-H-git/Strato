import { jsx } from "react/jsx-runtime";
import { useRef } from "react";
import { createStore } from "../../../core/store/store.js";
import { TreeMapStateContext, initialTreeMapState } from "../store/store.js";
const StoreProvider = ({
  value,
  children
}) => {
  const storeRef = useRef(void 0);
  storeRef.current = createStore(value ?? initialTreeMapState);
  return /* @__PURE__ */ jsx(TreeMapStateContext.Provider, { value: storeRef.current, children });
};
export {
  StoreProvider
};
//# sourceMappingURL=StoreProvider.js.map
