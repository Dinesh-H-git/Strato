import { useEffect } from "react";
import { useSetState } from "../store/selectors.js";
const useSetHierarchyNodes = (clusters, leafs) => {
  const setState = useSetState();
  useEffect(() => {
    setState((prev) => ({
      ...prev,
      hierarchyLeafNodes: leafs,
      hierarchyClusterNodes: clusters
    }));
  }, [leafs, clusters, setState]);
};
export {
  useSetHierarchyNodes
};
//# sourceMappingURL=useSetHierarchyNodes.js.map
