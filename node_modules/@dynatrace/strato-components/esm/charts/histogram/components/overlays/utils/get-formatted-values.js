import { isUndefined } from "lodash-es";
import { getAbsoluteValue } from "./get-absolute-value.js";
const isRelativeValueRepresentation = (absoluteValueMetadata) => !isUndefined(absoluteValueMetadata);
function getFormattedValues(formatter, binMetadata) {
  const value = formatter(binMetadata.value, binMetadata.unit);
  return isRelativeValueRepresentation(binMetadata.absolute) ? [getAbsoluteValue(binMetadata.absolute, formatter).toString(), value] : [value.toString()];
}
export {
  getFormattedValues
};
//# sourceMappingURL=get-formatted-values.js.map
