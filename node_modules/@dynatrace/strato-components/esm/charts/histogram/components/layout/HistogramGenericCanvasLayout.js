import { jsx } from "react/jsx-runtime";
import { forwardRef } from "react";
import {
  Canvas
} from "../../../core/components/canvas/Canvas.js";
import { CanvasContext } from "../../../core/components/canvas/CanvasContext.js";
import { useGridPlotSize } from "../../../core/components/chart-grid/hooks/useGridPlotSize.js";
import { useHistogramXScale } from "../../hooks/useHistogramXScale.js";
import { BarChartDisplay } from "../displays/BarChartDisplay.js";
const HistogramGenericCanvasLayout = forwardRef((props, canvasRef) => {
  const { series } = props;
  const { width, height } = useGridPlotSize();
  const { xScale } = useHistogramXScale();
  return /* @__PURE__ */ jsx(
    CanvasContext.Provider,
    {
      value: {
        shapeRefs: /* @__PURE__ */ new Map()
      },
      children: /* @__PURE__ */ jsx(Canvas, { width, height, ref: canvasRef, children: /* @__PURE__ */ jsx(BarChartDisplay, { xScale, series }) })
    }
  );
});
export {
  HistogramGenericCanvasLayout
};
//# sourceMappingURL=HistogramGenericCanvasLayout.js.map
