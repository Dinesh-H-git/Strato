import { isEmpty, isNil, isUndefined, merge } from "lodash-es";
import { defaultToolbar } from "../constants/defaults.js";
const isUndefinedOrIsEmpty = (value) => {
  return isUndefined(value) && isEmpty(value);
};
const returnNullOrNegate = (value) => isNil(value) ? void 0 : !value;
function getToolbarConfiguration(contextToolbar, slotToolbar, slotDownloadCSV, contextDownloadCSV) {
  const hasNoDownloadCsvConfigured = isUndefinedOrIsEmpty(slotDownloadCSV) && isUndefinedOrIsEmpty(contextDownloadCSV);
  if (!contextToolbar && isEmpty(slotToolbar) && hasNoDownloadCsvConfigured) {
    return { ...defaultToolbar };
  }
  return merge({}, defaultToolbar, contextToolbar, slotToolbar, {
    downloadData: slotDownloadCSV ?? contextDownloadCSV,
    enabled: returnNullOrNegate(slotToolbar?.hidden) ?? returnNullOrNegate(contextToolbar?.hidden) ?? contextToolbar?.enabled
  });
}
export {
  getToolbarConfiguration
};
//# sourceMappingURL=get-toolbar-configuration.js.map
