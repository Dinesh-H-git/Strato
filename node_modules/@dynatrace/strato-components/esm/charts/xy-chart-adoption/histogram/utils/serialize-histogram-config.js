import { ChartType } from "../../../core/types/chart-types.js";
import { normalizeConfig } from "../../../histogram/utils/histogram-configuration.js";
const serializeHistogramConfig = (config) => JSON.stringify({
  type: ChartType.Histogram,
  ...normalizeConfig(config)
});
export {
  serializeHistogramConfig
};
//# sourceMappingURL=serialize-histogram-config.js.map
