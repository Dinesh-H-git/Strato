function yAxisConfigMapping(axes) {
  return axes.reduce((result, { position, ...rest }) => {
    const axisPosition = position === "right" ? "right" : "left";
    return {
      ...result,
      [axisPosition]: {
        ...rest,
        type: rest.scale === "log" ? "log" : "numerical"
      }
    };
  }, {});
}
export {
  yAxisConfigMapping
};
//# sourceMappingURL=axis-config-mapping.js.map
