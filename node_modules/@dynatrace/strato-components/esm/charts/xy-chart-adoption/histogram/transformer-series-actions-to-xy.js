function mapHistogramSeriesActionsToXY(seriesActions, originalData) {
  return (datapoint, _series) => {
    const bin = {
      from: datapoint.datapoint.x0,
      to: datapoint.datapoint.x1,
      value: datapoint.datapoint.y1
    };
    const histogramSeries = originalData.find(
      (s) => s.name === datapoint.name
    ) ?? {
      name: datapoint.name,
      bins: [bin]
    };
    return seriesActions(histogramSeries, bin);
  };
}
export {
  mapHistogramSeriesActionsToXY
};
//# sourceMappingURL=transformer-series-actions-to-xy.js.map
