import { jsx } from "react/jsx-runtime";
import { useRef } from "react";
import { createStore } from "../../../core/store/store.js";
import { initialState } from "../constants.js";
import { HoneycombStoreContext } from "../context/honeycomb-store.context.js";
const HoneycombStoreProvider = ({ children }) => {
  const storeRef = useRef(null);
  storeRef.current = createStore(initialState);
  return /* @__PURE__ */ jsx(HoneycombStoreContext.Provider, { value: storeRef.current, children });
};
export {
  HoneycombStoreProvider
};
//# sourceMappingURL=honeycomb-store.provider.js.map
