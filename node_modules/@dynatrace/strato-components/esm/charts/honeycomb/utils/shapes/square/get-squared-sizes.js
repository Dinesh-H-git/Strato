const getSquaredSizes = (width) => {
  const half = width / 2;
  return {
    height: width,
    halfWidth: half,
    halfHeight: half
  };
};
export {
  getSquaredSizes
};
//# sourceMappingURL=get-squared-sizes.js.map
