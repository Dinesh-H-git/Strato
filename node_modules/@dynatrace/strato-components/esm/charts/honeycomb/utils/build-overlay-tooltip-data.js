import { safeGet } from "../../core/utils/safe-get.js";
const buildOverlayTooltipData = (tileIndex, honeycombData, tooltipMetadata) => {
  const tileData = honeycombData[tileIndex];
  const metadata = safeGet(tooltipMetadata, tileData);
  return {
    geometry: "honeycomb",
    header1: [],
    metadata: {
      id: metadata.id,
      tileData,
      tooltipMetadata: metadata
    },
    series: [
      {
        name: metadata.name,
        color: metadata.color.Default,
        value: metadata.value.toString()
      }
    ]
  };
};
export {
  buildOverlayTooltipData
};
//# sourceMappingURL=build-overlay-tooltip-data.js.map
