import { isUndefined } from "lodash-es";
import {
  HOVERED_TILE_OPACITY,
  NOT_HOVERED_TILE_OPACITY
} from "../constants.js";
function calculateTileOpacity(hovered, value) {
  return !isUndefined(hovered) && hovered.name.toLowerCase() !== value.toLowerCase() ? NOT_HOVERED_TILE_OPACITY : HOVERED_TILE_OPACITY;
}
export {
  calculateTileOpacity
};
//# sourceMappingURL=calculate-tile-opacity.js.map
