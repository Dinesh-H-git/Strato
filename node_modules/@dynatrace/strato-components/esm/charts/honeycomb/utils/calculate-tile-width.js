import { calculateHexSizeBasedOnHeight } from "./shapes/hexagon/calculate-hex-size-based-on-width.js";
const calculateTileWidth = (height, shape) => {
  return shape === "hexagon" ? calculateHexSizeBasedOnHeight(height).width : height;
};
export {
  calculateTileWidth
};
//# sourceMappingURL=calculate-tile-width.js.map
