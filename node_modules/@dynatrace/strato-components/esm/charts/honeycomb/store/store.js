import { useContext } from "react";
import { HoneycombStoreContext } from "../context/honeycomb-store.context.js";
const useSetState = () => {
  const { setState } = useContext(HoneycombStoreContext);
  return setState;
};
const useSetStateSelection = () => {
  const setState = useSetState();
  return (newSelectionState) => {
    setState((prev) => ({
      ...prev,
      selection: {
        ...prev.selection,
        ...newSelectionState
      }
    }));
  };
};
const useSetStateOverlay = () => {
  const setState = useSetState();
  return (newOverlayState) => {
    setState((prev) => ({
      ...prev,
      overlay: {
        ...prev.overlay,
        ...newOverlayState
      }
    }));
  };
};
export {
  useSetStateOverlay,
  useSetStateSelection
};
//# sourceMappingURL=store.js.map
