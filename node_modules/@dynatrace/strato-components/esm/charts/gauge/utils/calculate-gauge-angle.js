import { scaleLinear } from "d3-scale";
import { ARC_END_ANGLE, ARC_START_ANGLE } from "../constants.js";
const calculateGaugeAngle = (value, max, min) => {
  const scale = scaleLinear().domain([min, max]).range([ARC_START_ANGLE, ARC_END_ANGLE]).clamp(true);
  return scale(value);
};
export {
  calculateGaugeAngle
};
//# sourceMappingURL=calculate-gauge-angle.js.map
