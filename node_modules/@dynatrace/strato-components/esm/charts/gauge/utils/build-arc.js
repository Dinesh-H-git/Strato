import { arc } from "d3-shape";
const buildArc = (innerRadius, outerRadius, startAngle, endAngle) => {
  const d3Arc = arc();
  return d3Arc({
    innerRadius,
    outerRadius,
    startAngle,
    endAngle
  }) ?? void 0;
};
export {
  buildArc
};
//# sourceMappingURL=build-arc.js.map
