const polarToCartesian = (radius, angle) => {
  return {
    x: Math.cos(angle - 0.5 * Math.PI) * radius,
    y: Math.sin(angle - 0.5 * Math.PI) * radius
  };
};
export {
  polarToCartesian
};
//# sourceMappingURL=polar-to-cartesian.js.map
