import { jsx } from "react/jsx-runtime";
import {
  GAUGE_VERTICAL_DEFAULT_OFFSET,
  GAUGE_VERTICAL_SMALL_OFFSET
} from "../constants.js";
import { useGaugeChartSizeContext } from "../hooks/useGaugeChartSizeContext.js";
const GaugeRootSVG = ({ children }) => {
  const dimensions = useGaugeChartSizeContext();
  const width = dimensions?.width ?? 0;
  const height = dimensions?.height ?? 0;
  const verticalOffset = height > width ? height * GAUGE_VERTICAL_SMALL_OFFSET : height * GAUGE_VERTICAL_DEFAULT_OFFSET;
  return /* @__PURE__ */ jsx(
    "svg",
    {
      width: "100%",
      height: "100%",
      viewBox: `0 0 ${width} ${height}`,
      style: { display: "block" },
      children: /* @__PURE__ */ jsx("g", { transform: `translate(${width / 2}, ${height / 2 + verticalOffset})`, children })
    }
  );
};
export {
  GaugeRootSVG
};
//# sourceMappingURL=GaugeRootSVG.js.map
