import { SCALE_FALLBACK_COLOR } from "../core/constants.js";
const DONUT_CHART_DISPLAY_NAME = "DonutChart";
const PIE_CHART_DISPLAY_NAME = "PieChart";
const CHART_VARIANT = Object.freeze({
  Donut: "donut",
  Pie: "pie"
});
const THRESHOLD_TYPE_OPTION = Object.freeze(
  {
    Absolute: "absolute",
    NumberOfSlices: "number-of-slices",
    Relative: "relative"
  }
);
const TRUNCATION_MODE = Object.freeze({
  End: "end",
  Middle: "middle",
  Start: "start"
});
const LABEL_TYPE_OPTION = Object.freeze({
  Absolute: "absolute",
  Relative: "relative"
});
const DEFAULT_SCALE_FALLBACK_COLOR = SCALE_FALLBACK_COLOR;
const DONUT_CHART_ARIA_LABEL = "Donut Chart";
const PIE_CHART_ARIA_LABEL = "Pie Chart";
const PAD_ANGLE = 0.01;
export {
  CHART_VARIANT,
  DEFAULT_SCALE_FALLBACK_COLOR,
  DONUT_CHART_ARIA_LABEL,
  DONUT_CHART_DISPLAY_NAME,
  LABEL_TYPE_OPTION,
  PAD_ANGLE,
  PIE_CHART_ARIA_LABEL,
  PIE_CHART_DISPLAY_NAME,
  THRESHOLD_TYPE_OPTION,
  TRUNCATION_MODE
};
//# sourceMappingURL=constants.js.map
