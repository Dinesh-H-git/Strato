import { useCallback } from "react";
import { useSetState } from "./use-set-state.js";
const useSetHighlightedIdState = () => {
  const setState = useSetState();
  const setHighlightedIdState = useCallback(
    (id) => {
      setState((prev) => {
        return {
          ...prev,
          highlightedId: id
        };
      });
    },
    [setState]
  );
  return setHighlightedIdState;
};
export {
  useSetHighlightedIdState
};
//# sourceMappingURL=use-set-highlighted-ids-state.js.map
