import { useSetState } from "./use-set-state.js";
const useSetOverlayState = () => {
  const setState = useSetState();
  const setOverlayStateInBounds = (mouseInBounds) => {
    setState((prev) => {
      return {
        ...prev,
        overlay: {
          mouseInBounds
        }
      };
    });
  };
  return {
    setOverlayStateInBounds
  };
};
export {
  useSetOverlayState
};
//# sourceMappingURL=use-set-overlay-state.js.map
