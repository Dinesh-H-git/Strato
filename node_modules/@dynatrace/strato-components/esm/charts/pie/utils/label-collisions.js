import { isInHorizontalBounds } from "../../core/utils/is-in-horizontal-bounds.js";
import { LABEL_FONT_SIZE } from "../types/default.js";
const COLLISION_THRESHOLD = LABEL_FONT_SIZE / 2;
const oneWayCollision = (a, b) => a.boxX >= b.boxX && a.boxX <= b.boxX + b.boxWidth + COLLISION_THRESHOLD && (a.boxY >= b.boxY && a.boxY <= b.boxY + b.boxHeight + COLLISION_THRESHOLD || a.boxY + a.boxHeight >= b.boxY - COLLISION_THRESHOLD && a.boxY + a.boxHeight <= b.boxY + b.boxHeight);
const collision = (a, b) => oneWayCollision(a, b) || oneWayCollision(b, a);
const isInVerticalBounds = (labelY, labelHeight, top) => labelY > -top && labelY + labelHeight < top;
const isInBounds = (label, left, top) => isInHorizontalBounds(label.boxX, label.boxWidth, left) && isInVerticalBounds(label.boxY, label.boxHeight, top);
export {
  collision,
  isInBounds,
  isInVerticalBounds
};
//# sourceMappingURL=label-collisions.js.map
