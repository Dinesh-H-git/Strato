import { isWithinInterval } from "date-fns";
const isDatapointWithinSelection = (xPosition, start, end) => isWithinInterval(xPosition, { start, end });
export {
  isDatapointWithinSelection
};
//# sourceMappingURL=is-datapoint-within-selection.js.map
