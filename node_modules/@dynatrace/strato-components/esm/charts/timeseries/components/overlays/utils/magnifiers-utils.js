import { getMagnifierLabel } from "../../../../core/components/crosshair/utils/get-magnifier-label.js";
function generateYMagnifierLabels(yScales, mousePosition, units, yFormatters) {
  const [leftAxisUnit, rightAxisUnit] = units;
  const [leftFormatter, rightFormatter] = yFormatters;
  const yLabelLeft = getMagnifierLabel(
    leftAxisUnit,
    "y",
    leftFormatter,
    mousePosition,
    yScales.left?.scale
  );
  const yLabelRight = getMagnifierLabel(
    rightAxisUnit,
    "y",
    rightFormatter,
    mousePosition,
    yScales.right?.scale
  );
  return {
    left: yLabelLeft,
    right: yLabelRight
  };
}
function generateXMagnifierLabels(formatter, date) {
  return formatter(date);
}
export {
  generateXMagnifierLabels,
  generateYMagnifierLabels
};
//# sourceMappingURL=magnifiers-utils.js.map
