const MAX_DATA_POINTS_DISTANCE = 1e6;
function getClampedTimeScale(timescale) {
  return timescale.copy().clamp(true);
}
function getValueWithBoundaries(timestamp, timeScale, clampedTimeScale) {
  const value = timeScale(timestamp) ?? 0;
  const clampedValue = clampedTimeScale(timestamp) ?? 0;
  const isClamped = clampedValue !== value;
  if (isClamped) {
    return clampedValue > value ? Math.max(-MAX_DATA_POINTS_DISTANCE, value) : Math.min(value, clampedValue + MAX_DATA_POINTS_DISTANCE);
  }
  return value;
}
export {
  MAX_DATA_POINTS_DISTANCE,
  getClampedTimeScale,
  getValueWithBoundaries
};
//# sourceMappingURL=clamp-value.js.map
