function extractValue(record, valueDimensionName) {
  if (valueDimensionName === void 0) {
    return void 0;
  }
  if (!record) {
    throw Error("Record has no values");
  }
  const value = record[valueDimensionName];
  return value;
}
export {
  extractValue
};
//# sourceMappingURL=extractValue.js.map
