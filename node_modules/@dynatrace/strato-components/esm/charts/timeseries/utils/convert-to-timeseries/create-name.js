import { isNil } from "lodash-es";
function createName(dimensions, valueName) {
  const name = [
    ...dimensions.map(
      (d) => typeof d.value === "object" ? JSON.stringify(d.value) : d.value?.toString()
    ).filter((x) => !isNil(x)),
    valueName
  ];
  return name;
}
export {
  createName
};
//# sourceMappingURL=create-name.js.map
