import { useMemo } from "react";
import { useTimeseriesFilteredData } from "./useTimeseriesFilteredData.js";
const useTimeseriesUnits = () => {
  const { units } = useTimeseriesFilteredData();
  return useMemo(() => [...new Set(units)].slice(0, 2), [units]);
};
export {
  useTimeseriesUnits
};
//# sourceMappingURL=useTimeseriesUnits.js.map
