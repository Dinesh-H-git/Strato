import { compact } from "lodash-es";
function isColoredRangeArray(colorRange) {
  return Array.isArray(colorRange) && compact(colorRange).length > 0 && Object.keys(colorRange[0]).includes("to") && Object.keys(colorRange[0]).includes("from");
}
export {
  isColoredRangeArray
};
//# sourceMappingURL=colored-range.js.map
