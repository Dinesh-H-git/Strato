function defineTrendDirection(directionProp, valueProp) {
  let direction = directionProp;
  if (!direction) {
    direction = "neutral";
    if (valueProp !== void 0) {
      if (valueProp > 0) {
        direction = "upward";
      } else if (valueProp < 0) {
        direction = "downward";
      }
    }
  }
  return direction;
}
export {
  defineTrendDirection
};
//# sourceMappingURL=define-trend-direction.js.map
