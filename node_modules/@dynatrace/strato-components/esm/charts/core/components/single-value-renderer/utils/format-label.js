import { normalize } from "./normalize.js";
function formatLabel(label) {
  if (!label) {
    return;
  }
  return normalize(label);
}
export {
  formatLabel
};
//# sourceMappingURL=format-label.js.map
