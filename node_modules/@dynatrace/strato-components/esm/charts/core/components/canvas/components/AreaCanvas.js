import { useCanvas } from "../hooks/useCanvas.js";
import {
  ORIENTATION_VERTICAL
} from "../types/canvas-shape.js";
import { printAreaBuilder } from "../utils/shapes/area-canvas.js";
function AreaCanvas(props) {
  const {
    radius,
    strokeWidth = 2,
    lineStrokeWidth = 2,
    enableDotOverflow = false,
    overlayPadding,
    orientation = ORIENTATION_VERTICAL,
    ...propsAreaCanvas
  } = props;
  const drawArea = printAreaBuilder(
    strokeWidth,
    radius,
    lineStrokeWidth,
    enableDotOverflow,
    overlayPadding
  );
  const drawAreaCanvas = (ctx, canvas) => drawArea(ctx, canvas, canvas.x, canvas.y0, canvas.y1, orientation);
  useCanvas(
    "area",
    (ctx) => drawAreaCanvas(ctx, propsAreaCanvas)
  );
  return null;
}
export {
  AreaCanvas
};
//# sourceMappingURL=AreaCanvas.js.map
