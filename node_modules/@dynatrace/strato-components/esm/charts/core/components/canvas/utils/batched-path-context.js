const PATH_BATCH_SIZE = 1e3;
const ARC_BATCH_SIZE = 1e3;
function createBatchedStrokeContext(ctx, batchSize) {
  let closePathCount = 0;
  const d3PathContext = {
    moveTo(x, y) {
      ctx.moveTo(x, y);
    },
    lineTo(x, y) {
      ctx.lineTo(x, y);
    },
    bezierCurveTo(cp1x, cp1y, cp2x, cp2y, x, y) {
      ctx.bezierCurveTo(cp1x, cp1y, cp2x, cp2y, x, y);
    },
    closePath() {
      ctx.closePath();
      closePathCount++;
      if (closePathCount >= batchSize) {
        ctx.stroke();
        ctx.beginPath();
        closePathCount = 0;
      }
    }
  };
  return d3PathContext;
}
export {
  ARC_BATCH_SIZE,
  PATH_BATCH_SIZE,
  createBatchedStrokeContext
};
//# sourceMappingURL=batched-path-context.js.map
