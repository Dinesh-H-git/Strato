import { useCanvas } from "../hooks/useCanvas.js";
import { printBarBuilder } from "../utils/shapes/bar-canvas.js";
function BarCanvas(props) {
  const {
    strokeWidth,
    isVertical = true,
    enableDotOverflow = false,
    overlayPadding,
    ...propsBarCanvas
  } = props;
  const drawBars = printBarBuilder(
    strokeWidth,
    enableDotOverflow,
    overlayPadding
  );
  const drawBarCanvas = (ctx, canvas) => {
    drawBars(ctx, canvas, canvas.x, canvas.y, canvas.width, canvas.height);
  };
  const barCanvas = {
    ...propsBarCanvas,
    isVertical
  };
  useCanvas(
    "bar",
    (ctx) => drawBarCanvas(ctx, barCanvas)
  );
  return null;
}
export {
  BarCanvas
};
//# sourceMappingURL=BarCanvas.js.map
