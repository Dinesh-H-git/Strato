const TRIANGLE_RADIUS_ADJUSTMENT = 0.5;
function drawSymbol(ctx, cx, cy, radius, shape = "circle") {
  switch (shape) {
    case "circle":
      ctx.arc(cx, cy, radius, 0, 2 * Math.PI);
      break;
    case "square": {
      const s = radius;
      ctx.rect(cx - s, cy - s, 2 * s, 2 * s);
      break;
    }
    case "triangle": {
      const r = radius - TRIANGLE_RADIUS_ADJUSTMENT;
      const R = 2 * r;
      const side = r * 2 * Math.sqrt(3);
      ctx.moveTo(cx, cy - R);
      ctx.lineTo(cx + side / 2, cy + r);
      ctx.lineTo(cx - side / 2, cy + r);
      ctx.closePath();
      break;
    }
    case "diamond": {
      const s = radius * Math.sqrt(2);
      ctx.moveTo(cx, cy - s);
      ctx.lineTo(cx + s, cy);
      ctx.lineTo(cx, cy + s);
      ctx.lineTo(cx - s, cy);
      ctx.closePath();
      break;
    }
    default:
      ctx.arc(cx, cy, radius, 0, 2 * Math.PI);
  }
}
export {
  drawSymbol
};
//# sourceMappingURL=symbol-canvas.js.map
