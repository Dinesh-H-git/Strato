import { useCanvas } from "../hooks/useCanvas.js";
import { printLineBuilder } from "../utils/shapes/line-canvas.js";
function LineCanvas(props) {
  const {
    radius,
    strokeWidth = 2,
    enableDotOverflow = false,
    overlayPadding,
    ...propsLineCanvas
  } = props;
  const drawLine = printLineBuilder(
    strokeWidth,
    enableDotOverflow,
    radius,
    overlayPadding
  );
  const drawLineCanvas = (ctx, canvas) => {
    drawLine(ctx, canvas, canvas.x, canvas.y);
  };
  useCanvas(
    "line",
    (ctx) => drawLineCanvas(ctx, propsLineCanvas)
  );
  return null;
}
export {
  LineCanvas
};
//# sourceMappingURL=LineCanvas.js.map
