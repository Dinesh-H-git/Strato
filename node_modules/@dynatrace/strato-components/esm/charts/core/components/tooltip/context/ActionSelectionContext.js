import { createContext, useContext } from "react";
const ActionSelectionContext = createContext(void 0);
const useActionSelection = () => {
  const context = useContext(ActionSelectionContext);
  if (context === void 0) {
    throw new Error(
      "useActionSelection must be used within ActionSelectionContext.Provider"
    );
  }
  return context;
};
ActionSelectionContext["displayName"] = "ActionSelectionContext";
export {
  ActionSelectionContext,
  useActionSelection
};
//# sourceMappingURL=ActionSelectionContext.js.map
