import { jsx, jsxs } from "react/jsx-runtime";
import clsx from "clsx";
import { container } from "./TooltipHeader.sty.js";
import { Divider } from "../../../../../../layouts/divider/Divider.js";
import { Flex } from "../../../../../../layouts/flex/Flex.js";
import { iterateHeaderSlots } from "../../utils/iterate-header-slots.js";
import { TooltipItem } from "../TooltipItem/TooltipItem.js";
const HEADER_SECTION_NAME = "header";
const TooltipHeader = (props) => {
  const { withoutDivider, children, ...rest } = props;
  const { header, subHeader } = iterateHeaderSlots(children);
  return /* @__PURE__ */ jsxs(
    Flex,
    {
      className: clsx(
        container({ withBorder: !withoutDivider }),
        HEADER_SECTION_NAME
      ),
      ...rest,
      children: [
        /* @__PURE__ */ jsx(Flex, { alignItems: "center", gap: 0, children: header }),
        /* @__PURE__ */ jsx(Flex, { alignItems: "center", gap: 0, children: /* @__PURE__ */ jsx(TooltipItem, { children: subHeader }) }),
        withoutDivider ? null : /* @__PURE__ */ jsx(Divider, { orientation: "horizontal" })
      ]
    }
  );
};
TooltipHeader["displayName"] = "TooltipHeader";
export {
  HEADER_SECTION_NAME,
  TooltipHeader
};
//# sourceMappingURL=TooltipHeader.js.map
