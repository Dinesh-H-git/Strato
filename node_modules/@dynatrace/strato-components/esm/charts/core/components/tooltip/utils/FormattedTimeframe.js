import { jsx, jsxs } from "react/jsx-runtime";
import clsx from "clsx";
import { memo } from "react";
import { useIntl } from "react-intl";
import { formatTimeframeToParts } from "@dynatrace-sdk/units";
const FormattedTimeframe = memo((props) => {
  const intl = useIntl();
  const {
    from,
    to,
    clampFutureToDateToNow,
    className: consumerClassName,
    style: consumerStyle,
    "data-testid": dataTestId
  } = props;
  const parts = formatTimeframeToParts(from, to, intl, clampFutureToDateToNow);
  return typeof parts === "string" ? /* @__PURE__ */ jsx(
    "span",
    {
      "data-testid": dataTestId,
      className: clsx("formatted-timeframe", consumerClassName),
      style: consumerStyle,
      children: parts
    }
  ) : /* @__PURE__ */ jsxs(
    "span",
    {
      "data-testid": dataTestId,
      className: clsx("formatted-timeframe", consumerClassName),
      style: consumerStyle,
      children: [
        /* @__PURE__ */ jsx("span", { className: "ft-prefix", children: parts[0] }),
        /* @__PURE__ */ jsxs("span", { className: "ft-separator", children: [
          "\xA0",
          parts[1],
          "\xA0"
        ] }),
        /* @__PURE__ */ jsx("span", { className: "ft-suffix", children: parts[2] })
      ]
    }
  );
});
export {
  FormattedTimeframe
};
//# sourceMappingURL=FormattedTimeframe.js.map
