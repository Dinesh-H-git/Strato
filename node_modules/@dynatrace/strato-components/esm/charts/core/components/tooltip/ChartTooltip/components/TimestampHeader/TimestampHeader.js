import { jsx } from "react/jsx-runtime";
import Spacings from "@dynatrace/strato-design-tokens/spacings";
import { formatDate } from "@dynatrace-sdk/units";
import { TooltipItem } from "../../../section-components/TooltipItem/TooltipItem.js";
import { TextHeader } from "../TextHeader.js";
const TimestampHeader = ({ date }) => {
  const formattedDate = formatDate(date, {
    day: "2-digit",
    month: "short",
    year: date.getFullYear() === (/* @__PURE__ */ new Date()).getFullYear() ? void 0 : "numeric",
    hour: "2-digit",
    minute: "2-digit",
    second: date.getSeconds() === 0 ? void 0 : "numeric"
  });
  return /* @__PURE__ */ jsx(TooltipItem, { style: { padding: `0 ${Spacings.Size2}` }, children: /* @__PURE__ */ jsx(TextHeader, { text: formattedDate }) });
};
TimestampHeader["displayName"] = "ChartTooltip.TimestampHeader";
export {
  TimestampHeader
};
//# sourceMappingURL=TimestampHeader.js.map
