import { jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import useResizeObserver from "use-resize-observer";
import { GridPlotContext } from "../context/GridPlotContext.js";
const GridPlotSizeProvider = ({ children }) => {
  const { width = 0, height = 0, ref } = useResizeObserver();
  const context = useMemo(() => ({ width, height, ref }), [width, height, ref]);
  return /* @__PURE__ */ jsx(GridPlotContext.Provider, { value: context, children });
};
GridPlotSizeProvider["displayName"] = "GridPlotSizeProvider";
export {
  GridPlotSizeProvider
};
//# sourceMappingURL=GridPlotSizeProvider.js.map
