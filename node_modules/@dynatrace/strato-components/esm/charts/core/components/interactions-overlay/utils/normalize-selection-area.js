const normalizeSelectionArea = ([positionA, positionB]) => {
  let start;
  let end;
  const draggingLeft = positionA.relative.x > positionB.relative.x;
  if (draggingLeft) {
    start = positionB;
    end = positionA;
  } else {
    start = positionA;
    end = positionB;
  }
  return [start, end];
};
export {
  normalizeSelectionArea
};
//# sourceMappingURL=normalize-selection-area.js.map
