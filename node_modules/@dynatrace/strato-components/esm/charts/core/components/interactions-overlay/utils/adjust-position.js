const adjustPosition = (position, delta) => {
  return {
    relative: {
      x: position.relative.x + delta,
      y: position.relative.y
    },
    absolute: {
      x: position.absolute.x + delta,
      y: position.absolute.y
    }
  };
};
export {
  adjustPosition
};
//# sourceMappingURL=adjust-position.js.map
