const getActiveMode = (mode, event, zoomDisabled = false, panDisabled = false) => {
  if (event) {
    if ((event.metaKey || event.ctrlKey) && !zoomDisabled) {
      return "zoom";
    } else if (event.altKey && !panDisabled) {
      return "pan";
    }
  }
  return mode;
};
export {
  getActiveMode
};
//# sourceMappingURL=get-active-mode.js.map
