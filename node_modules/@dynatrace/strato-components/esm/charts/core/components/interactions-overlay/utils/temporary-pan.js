const temporaryPan = ({
  deltaX,
  onPanChange
}) => {
  const horizontal = deltaX && deltaX !== 0;
  if (horizontal) {
    onPanChange?.({ triggeredBy: "pointer", delta: deltaX });
  }
};
export {
  temporaryPan
};
//# sourceMappingURL=temporary-pan.js.map
