const calculateCenterPositions = (width, height, top, left) => {
  const relativePosition = {
    x: width / 2,
    y: height / 2
  };
  const absolutePosition = {
    x: relativePosition.x + left,
    y: relativePosition.y + top
  };
  return {
    relative: relativePosition,
    absolute: absolutePosition
  };
};
export {
  calculateCenterPositions
};
//# sourceMappingURL=calculate-center-positions.js.map
