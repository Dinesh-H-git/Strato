import {
  charsFontWidth12pixelsSize400Map,
  charsFontWidth12pixelsSize500Map
} from "../../text-ellipsis/chars-font-width-map.js";
import { calculateWidth } from "../../text-ellipsis/truncate-string.js";
const calculateTotalWidth400 = (...texts) => {
  let totalWidth = 0;
  for (const text of texts) {
    if (text) {
      const width = calculateWidth(text, charsFontWidth12pixelsSize400Map);
      totalWidth += isFinite(width) ? width : 0;
    }
  }
  return totalWidth;
};
const calculateTotalWidth500 = (...texts) => {
  let totalWidth = 0;
  for (const text of texts) {
    if (text) {
      const width = calculateWidth(text, charsFontWidth12pixelsSize500Map);
      totalWidth += isFinite(width) ? width : 0;
    }
  }
  return totalWidth;
};
export {
  calculateTotalWidth400,
  calculateTotalWidth500
};
//# sourceMappingURL=width-calculation.js.map
