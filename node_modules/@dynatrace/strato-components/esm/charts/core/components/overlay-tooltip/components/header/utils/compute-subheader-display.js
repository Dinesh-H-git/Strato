import { getValue } from "./get-value.js";
import { getHeaderTruncatedText } from "../../../utils/text-truncation-header.js";
const computeSubheaderDisplay = ({
  subheader,
  formatStrategy,
  availableWidth,
  truncationMode
}) => {
  const subheaderValue = getValue(subheader);
  const subheaderContent = subheaderValue ? formatStrategy(subheaderValue) : void 0;
  const shownSubheaderText = subheaderContent ? getHeaderTruncatedText(subheaderContent, availableWidth, truncationMode) : void 0;
  return {
    subheaderContent,
    shownSubheaderText,
    isSubheaderTruncated: !!subheaderContent && shownSubheaderText !== subheaderContent
  };
};
export {
  computeSubheaderDisplay
};
//# sourceMappingURL=compute-subheader-display.js.map
