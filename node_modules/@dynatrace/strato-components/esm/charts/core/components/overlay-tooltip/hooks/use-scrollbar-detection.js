import { useCallback } from "react";
import { needsScrollbarFix } from "../utils/needs-scrollbar-fix.js";
const useScrollbarDetection = () => {
  const checkScrollbar = useCallback((element) => {
    const hasVerticalScrollbar = element.scrollHeight > element.clientHeight;
    const needsFix = hasVerticalScrollbar && needsScrollbarFix();
    element.style.setProperty("--needs-scrollbar-fix", needsFix ? "1" : "0");
  }, []);
  const refCallback = useCallback(
    (element) => {
      if (!element) {
        return;
      }
      checkScrollbar(element);
    },
    [checkScrollbar]
  );
  return refCallback;
};
export {
  useScrollbarDetection
};
//# sourceMappingURL=use-scrollbar-detection.js.map
