import { isNil } from "lodash-es";
import { createContext, useContext } from "react";
const TooltipDimensionsContext = createContext(null);
const useTooltipDimensions = () => {
  const context = useContext(TooltipDimensionsContext);
  if (isNil(context)) {
    return null;
  }
  return context;
};
export {
  TooltipDimensionsContext,
  useTooltipDimensions
};
//# sourceMappingURL=TooltipDimensionsContext.js.map
