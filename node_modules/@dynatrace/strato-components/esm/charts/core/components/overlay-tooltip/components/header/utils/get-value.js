import { isArray, isUndefined } from "lodash-es";
import { isFromTo, isSingleValue } from "./type-guards.js";
const getValue = (data) => {
  if (isUndefined(data)) {
    return void 0;
  }
  if (isSingleValue(data)) {
    return isArray(data) ? data[0] : data;
  } else if (isFromTo(data)) {
    return data;
  }
};
export {
  getValue
};
//# sourceMappingURL=get-value.js.map
