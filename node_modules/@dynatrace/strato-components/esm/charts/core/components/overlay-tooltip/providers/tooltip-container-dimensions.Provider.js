import { jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { TOOLTIP_WRAPPER_PADDING } from "../constants.js";
import { TooltipDimensionsContext } from "../contexts/TooltipDimensionsContext.js";
const TooltipContainerDimensionProvider = ({
  children,
  container
}) => {
  const value = useMemo(() => {
    if (!container) {
      return null;
    }
    const padding = TOOLTIP_WRAPPER_PADDING * 2;
    return {
      width: container.clientWidth - padding,
      height: container.clientHeight
    };
  }, [container]);
  return /* @__PURE__ */ jsx(TooltipDimensionsContext.Provider, { value, children });
};
export {
  TooltipContainerDimensionProvider
};
//# sourceMappingURL=tooltip-container-dimensions.Provider.js.map
