import { jsx } from "react/jsx-runtime";
import useIntl from "react-intl/src/components/useIntl.js";
import { OVERLAY_TOOLTIP_MESSAGES } from "../messages.js";
import { useTooltipSnapshot } from "../selectors.js";
import { actionsPlaceholderTextAnimated } from "./global-actions/actions.sty.js";
import { GlobalActions } from "./global-actions/GlobalActions.js";
const Footer = ({ actions, unpinnedPlaceholderText }) => {
  const intl = useIntl();
  const { pinned } = useTooltipSnapshot();
  if (pinned) {
    return actions.length > 0 ? /* @__PURE__ */ jsx(GlobalActions, { actions }) : null;
  }
  const placeholderText = unpinnedPlaceholderText ?? intl.formatMessage(OVERLAY_TOOLTIP_MESSAGES.unpinnedPlaceholderMessage);
  return /* @__PURE__ */ jsx("div", { className: actionsPlaceholderTextAnimated, children: placeholderText });
};
export {
  Footer
};
//# sourceMappingURL=Footer.js.map
