import { jsx } from "react/jsx-runtime";
import clsx from "clsx";
import { customSymbol, customSymbolLarge } from "./seriesSymbol.sty.js";
import { ShapeIcon } from "../../../shape/ShapeIcon.js";
const SeriesSymbol = (props) => {
  if (props.icon) {
    return /* @__PURE__ */ jsx(
      "span",
      {
        "aria-hidden": "true",
        className: clsx(
          customSymbol,
          props.iconStyle === "large" && customSymbolLarge
        ),
        children: props.icon
      }
    );
  }
  if (props.symbol && props.symbol.trim() !== "") {
    return /* @__PURE__ */ jsx(
      "span",
      {
        className: clsx(
          customSymbol,
          props.iconStyle === "large" && customSymbolLarge
        ),
        "aria-hidden": "true",
        children: props.symbol
      }
    );
  }
  if (!props.color) {
    return null;
  }
  return /* @__PURE__ */ jsx(ShapeIcon, { color: props.color, shape: props.shape || "single" });
};
export {
  SeriesSymbol
};
//# sourceMappingURL=SeriesSymbol.js.map
