import { createContext, useContext } from "react";
const ExternalConfigContext = createContext({
  defaultActionsConfig: /* @__PURE__ */ new Map()
});
const useExternalConfig = () => {
  const config = useContext(ExternalConfigContext);
  if (!config) {
    throw new Error("config not used within ConfigContext provider.");
  }
  return config;
};
export {
  ExternalConfigContext,
  useExternalConfig
};
//# sourceMappingURL=ExternalConfigContext.js.map
