import { formatSeriesNameForDisplay } from "./format-series-name-for-display.js";
const getIsTruncated = (isMultiLineCssClamped, name, multiLine, nameText, displayNameText) => {
  if (isMultiLineCssClamped) {
    return true;
  }
  if (Array.isArray(name)) {
    if (multiLine) {
      return name.some(
        (original, index) => original !== nameText[index]
      );
    }
    return formatSeriesNameForDisplay(name) !== nameText;
  }
  return nameText !== displayNameText;
};
export {
  getIsTruncated
};
//# sourceMappingURL=get-is-truncated.js.map
