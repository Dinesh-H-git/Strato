const needsScrollbarFix = () => {
  if (typeof navigator === "undefined") {
    return false;
  }
  const userAgent = navigator.userAgent.toLowerCase();
  const isSafari = /webkit/.test(userAgent) && !/chrome|chromium|crios/.test(userAgent);
  const isFirefox = /firefox/.test(userAgent);
  return isSafari || isFirefox;
};
export {
  needsScrollbarFix
};
//# sourceMappingURL=needs-scrollbar-fix.js.map
