import clsx from "clsx";
import {
  seriesItemText,
  seriesNameTextMultiLine,
  seriesNameTextMultiLineWrap
} from "../../../tooltip.sty.js";
const getNameTextStyle = (isSingleLine, multiLine, multiLineMode = "clamp") => {
  const multiLineClass = multiLineMode === "wrap" ? seriesNameTextMultiLineWrap : seriesNameTextMultiLine;
  if (!isSingleLine) {
    return clsx(seriesItemText, multiLineClass);
  }
  if (multiLine) {
    return multiLineClass;
  }
  return seriesItemText;
};
export {
  getNameTextStyle
};
//# sourceMappingURL=get-name-text-style.js.map
