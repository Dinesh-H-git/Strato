const buildRelativeValueText = (relativeValue, relativeValueUnit) => {
  if (!relativeValue) {
    return void 0;
  }
  const value = relativeValue?.toString();
  return `${value ?? ""}${relativeValueUnit ?? ""}`;
};
export {
  buildRelativeValueText
};
//# sourceMappingURL=build-relative-value-text.js.map
