import { isEqual } from "lodash-es";
import { TooltipContext, useTooltip } from "./contexts/TooltipContext.js";
import { useStoreSelector } from "../../../../core/store/selector.js";
const useSelector = (selector) => useStoreSelector(TooltipContext, selector, isEqual);
const useTooltipSnapshot = () => useTooltip().getState();
export {
  useSelector,
  useTooltipSnapshot
};
//# sourceMappingURL=selectors.js.map
