import { jsx } from "react/jsx-runtime";
import {
  createContext,
  useContext,
  useRef
} from "react";
import { buildOverlay } from "../overlay.js";
const OverlayContext = createContext({});
const useOverlay = () => {
  const overlayState = useContext(OverlayContext);
  if (!overlayState) {
    throw new Error("useOverlay not used within OverlayContext provider.");
  }
  return overlayState;
};
const OverlayProvider = (props) => {
  const overlayRef = useRef();
  const overlay = useRef();
  overlay.current ||= buildOverlay(overlayRef);
  return /* @__PURE__ */ jsx(OverlayContext.Provider, { value: overlay.current, children: props.children });
};
export {
  OverlayContext,
  OverlayProvider,
  useOverlay
};
//# sourceMappingURL=OverlayContext.js.map
