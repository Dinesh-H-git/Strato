import { isArray } from "lodash-es";
const handleCopyToClipboard = async (name) => {
  const parsedName = isArray(name) ? name.join("\u2022") : name;
  try {
    await navigator.clipboard.writeText(parsedName ?? "unknown name");
  } catch {
  }
};
export {
  handleCopyToClipboard
};
//# sourceMappingURL=handleCopyToClipboard.js.map
