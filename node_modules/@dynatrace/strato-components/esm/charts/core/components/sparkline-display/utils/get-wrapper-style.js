const getWrapperStyle = (width, height, style) => {
  if (width === void 0 && height === void 0 && style === void 0) {
    return void 0;
  }
  const wrapperStyle = { ...style };
  if (width !== void 0) {
    wrapperStyle.width = typeof width === "number" ? `${width}px` : width;
  }
  if (height !== void 0) {
    wrapperStyle.height = typeof height === "number" ? `${height}px` : height;
  }
  return wrapperStyle;
};
export {
  getWrapperStyle
};
//# sourceMappingURL=get-wrapper-style.js.map
