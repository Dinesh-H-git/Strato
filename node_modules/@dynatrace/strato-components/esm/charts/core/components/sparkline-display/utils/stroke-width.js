const STROKE_DEFAULT = 2;
const STROKE_BREAKPOINTS = [
  {
    breakpoint: 576,
    strokeWidth: 4
  }
];
const calculateStrokeWidth = (height, width) => {
  for (const { breakpoint, strokeWidth } of STROKE_BREAKPOINTS) {
    if (height >= breakpoint && width >= breakpoint) {
      return strokeWidth;
    }
  }
  return STROKE_DEFAULT;
};
export {
  STROKE_DEFAULT,
  calculateStrokeWidth
};
//# sourceMappingURL=stroke-width.js.map
