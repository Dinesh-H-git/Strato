import { AXIS_HEIGHT } from "../constants.js";
const getSparklineHeight = (containerHeight, showTicks) => {
  const height = showTicks ? containerHeight - AXIS_HEIGHT : containerHeight;
  return Math.max(0, height);
};
export {
  getSparklineHeight
};
//# sourceMappingURL=get-sparkline-height.js.map
