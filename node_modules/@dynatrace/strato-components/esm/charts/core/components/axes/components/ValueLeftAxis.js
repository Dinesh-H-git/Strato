import { jsx } from "react/jsx-runtime";
import { DivAxisWrapper } from "./DivAxisWrapper.js";
import { AxisLeft } from "../AxisLeft.js";
import { axisFontProps } from "../constants.js";
import { getTickLabelPropsForValueAxis } from "../utils/get-tick-label-props.js";
const TICK_LABEL_PROPS = getTickLabelPropsForValueAxis("left");
const ValueLeftAxis = (props) => {
  const {
    scale,
    containerHeight,
    containerWidth,
    label,
    stroke,
    ticks,
    formattedTicks
  } = props;
  const tickFormat = (_, index) => formattedTicks[index];
  return /* @__PURE__ */ jsx(
    DivAxisWrapper,
    {
      divAxisStyle: axisFontProps,
      ticks: formattedTicks,
      label,
      children: /* @__PURE__ */ jsx(
        AxisLeft,
        {
          role: "graphics-symbol",
          "aria-label": "value axis left",
          scale,
          containerSize: containerHeight,
          width: containerWidth,
          tickValues: ticks,
          tickLabelProps: TICK_LABEL_PROPS,
          tickFormat,
          label,
          stroke
        }
      )
    }
  );
};
ValueLeftAxis["displayName"] = "ValueLeftAxis";
export {
  ValueLeftAxis
};
//# sourceMappingURL=ValueLeftAxis.js.map
