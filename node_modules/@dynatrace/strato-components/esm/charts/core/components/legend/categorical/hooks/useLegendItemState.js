import { useEffect, useRef, useState } from "react";
import { useMouseHover } from "../../../../hooks/useMouseHover.js";
const useLegendItemState = (item, interactionsDisabled, hiddenItems, numberOfItems) => {
  const refIconButton = useRef(null);
  const refNameButton = useRef(null);
  const [open, setOpen] = useState(false);
  const [isHovered, setIsHovered] = useState(false);
  const isItemHidden = hiddenItems.has(item.id);
  const [visible, setVisible] = useState(!isItemHidden);
  const shouldShowActionsButton = open || isHovered;
  const isIconHovered = useMouseHover(refIconButton) && !interactionsDisabled && isHovered;
  const isLastSelectedItem = hiddenItems.size === numberOfItems - 1 && !isItemHidden;
  useEffect(() => setVisible(!isItemHidden), [isItemHidden]);
  return {
    refIconButton,
    refNameButton,
    open,
    shouldShowActionsButton,
    isIconHovered,
    visible,
    isLastItem: isLastSelectedItem,
    updateVisible: setVisible,
    updateOpenState: setOpen,
    updateHoveredState: setIsHovered
  };
};
export {
  useLegendItemState
};
//# sourceMappingURL=useLegendItemState.js.map
