const indexIsInsideBoundaries = (index, boundary1, boundary2) => index >= boundary1 && index <= boundary2 || index <= boundary1 && index >= boundary2;
const indexShouldBeIncludedInShiftClickedList = (index, newClick, pivot, previousLastClick, wasSelectedBefore) => {
  const isSelectedNow = indexIsInsideBoundaries(index, newClick, pivot);
  const wasSelectedWithShiftBefore = indexIsInsideBoundaries(
    index,
    pivot,
    previousLastClick
  );
  return isSelectedNow || wasSelectedBefore && !wasSelectedWithShiftBefore;
};
export {
  indexShouldBeIncludedInShiftClickedList
};
//# sourceMappingURL=index-should-be-included-in-shift-clicked-list.js.map
