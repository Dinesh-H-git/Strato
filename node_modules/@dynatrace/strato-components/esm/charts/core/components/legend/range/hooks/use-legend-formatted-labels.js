import { useFormatter } from "../context/FormatterContext.js";
const useLegendFormattedLabels = (items) => {
  const { formatter } = useFormatter();
  return items.map((item) => {
    return {
      ...item,
      labels: {
        from: formatter(+item.labels.from),
        to: formatter(+item.labels.to)
      }
    };
  });
};
export {
  useLegendFormattedLabels
};
//# sourceMappingURL=use-legend-formatted-labels.js.map
