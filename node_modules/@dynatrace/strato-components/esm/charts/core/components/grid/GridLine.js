import { jsx } from "react/jsx-runtime";
import { Line } from "@visx/shape";
import Colors from "@dynatrace/strato-design-tokens/colors";
const DEFAULT_STROKE_WIDTH = "1px";
const DEFAULT_STROKE_COLOR = Colors.Background.Field.Neutral.DefaultActive;
const HIGHLIGHTED_STROKE_WIDTH = "2px";
const HIGHLIGHTED_STROKE_COLOR = Colors.Text.Neutral.Subdued;
const GridLine = ({ from, to, highlight = false }) => {
  let stroke = DEFAULT_STROKE_COLOR;
  let strokeWidth = DEFAULT_STROKE_WIDTH;
  if (highlight) {
    stroke = HIGHLIGHTED_STROKE_COLOR;
    strokeWidth = HIGHLIGHTED_STROKE_WIDTH;
  }
  return /* @__PURE__ */ jsx(
    Line,
    {
      from,
      to,
      stroke,
      strokeWidth,
      style: {
        shapeRendering: "geometricPrecision"
      }
    }
  );
};
export {
  DEFAULT_STROKE_COLOR,
  DEFAULT_STROKE_WIDTH,
  GridLine,
  HIGHLIGHTED_STROKE_COLOR,
  HIGHLIGHTED_STROKE_WIDTH
};
//# sourceMappingURL=GridLine.js.map
