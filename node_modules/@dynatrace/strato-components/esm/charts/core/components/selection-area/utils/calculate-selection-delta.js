import {
  KeyboardControls,
  LARGE_STEP_FACTOR,
  SMALL_STEP_FACTOR
} from "../../interactions-overlay/constants.js";
const calculateSelectionDelta = (event, width) => {
  switch (event.key) {
    case KeyboardControls.LEFT_ARROW: {
      const factor = event.shiftKey ? -LARGE_STEP_FACTOR : -SMALL_STEP_FACTOR;
      return width * factor;
    }
    case KeyboardControls.RIGHT_ARROW: {
      const factor = event.shiftKey ? LARGE_STEP_FACTOR : SMALL_STEP_FACTOR;
      return width * factor;
    }
    default:
      return 0;
  }
};
export {
  calculateSelectionDelta
};
//# sourceMappingURL=calculate-selection-delta.js.map
