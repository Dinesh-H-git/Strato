const LEFT_EDGE_ID = "left-edge";
const RIGHT_EDGE_ID = "right-edge";
const changedStart = ([currentStart, currentEnd], [nextStart, nextEnd]) => {
  return nextStart.relative.x !== currentStart && nextEnd.relative.x === currentEnd;
};
const changedEnd = ([currentStart, currentEnd], [nextStart, nextEnd]) => {
  return nextStart.relative.x === currentStart && nextEnd.relative.x !== currentEnd;
};
const changedEdge = (currentRange, nextRange) => {
  if (changedStart(currentRange, nextRange)) {
    return LEFT_EDGE_ID;
  }
  if (changedEnd(currentRange, nextRange)) {
    return RIGHT_EDGE_ID;
  }
  return false;
};
export {
  LEFT_EDGE_ID,
  RIGHT_EDGE_ID,
  changedEdge
};
//# sourceMappingURL=utils.js.map
