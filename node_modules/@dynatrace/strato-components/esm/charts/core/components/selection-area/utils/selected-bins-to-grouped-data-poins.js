import { getGroupedDataPointsByCoordinate } from "../../../utils/group-by-threshold.js";
const selectedToGroupedDataPoints = (selected) => {
  let dataPoints = [];
  selected.forEach((value, key) => {
    dataPoints = dataPoints.concat(
      getGroupedDataPointsByCoordinate(value, "y")
    );
  });
  return dataPoints;
};
export {
  selectedToGroupedDataPoints
};
//# sourceMappingURL=selected-bins-to-grouped-data-poins.js.map
