const TOOLTIP_OFFSET_X = 6;
const TOOLTIP_OFFSET_Y = 12;
const getSelectionTooltipPosition = (selectState, chartBounds) => {
  return {
    x: Math.round(
      TOOLTIP_OFFSET_X + chartBounds.left + chartBounds.width * (selectState.selectionPercentage ? selectState.selectionPercentage[1] : 0) / 100
    ),
    y: Math.round(chartBounds.top + chartBounds.height / 2 - TOOLTIP_OFFSET_Y)
  };
};
export {
  getSelectionTooltipPosition
};
//# sourceMappingURL=get-selection-tooltip-position.js.map
