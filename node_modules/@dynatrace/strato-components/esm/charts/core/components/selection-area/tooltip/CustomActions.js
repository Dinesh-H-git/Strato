import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { cloneElement } from "react";
import { Prefix } from "../../../../../navigation/menu/Prefix.js";
import { Intent } from "../Intent.js";
const CustomActions = ({
  actions
}) => /* @__PURE__ */ jsx(Fragment, { children: actions?.map(
  (action, index) => cloneElement(action, { key: action.key ?? index })
) });
const CustomIntents = ({
  intents
}) => /* @__PURE__ */ jsx(Fragment, { children: intents?.map((intent, index) => /* @__PURE__ */ jsxs(Intent, { ...intent.props, children: [
  intent.props.icon && /* @__PURE__ */ jsx(Prefix, { children: intent.props.icon }),
  intent.props.children
] }, intent.key ?? index)) });
export {
  CustomActions,
  CustomIntents
};
//# sourceMappingURL=CustomActions.js.map
