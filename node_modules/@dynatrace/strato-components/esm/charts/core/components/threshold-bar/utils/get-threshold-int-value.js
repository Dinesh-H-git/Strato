import { THRESHOLD } from "../../../messages.js";
const getThresholdIntValue = (intl, values) => values ? values.map((value) => {
  return value === THRESHOLD.dynamic.defaultMessage ? intl.formatMessage(THRESHOLD.dynamic) : value;
}) : void 0;
export {
  getThresholdIntValue
};
//# sourceMappingURL=get-threshold-int-value.js.map
