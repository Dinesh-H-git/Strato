import { MARKER_RADIUS } from "../markers/constants.js";
function resolveInfinitePixel(y, plotSize) {
  if (isFinite(y)) {
    return y;
  }
  const isPositive = y > 0;
  return isPositive ? plotSize + MARKER_RADIUS : -MARKER_RADIUS;
}
export {
  resolveInfinitePixel
};
//# sourceMappingURL=resolve-infinite-pixel.js.map
