import { jsx } from "react/jsx-runtime";
import { useCurrentTheme } from "../../../../core/hooks/useCurrentTheme.js";
import { BrightnessFilter } from "../BrightnessFilter.js";
import {
  MARKER_LIGHT_BRIGHTNESS,
  MARKER_DARK_BRIGHTNESS
} from "./markers/constants.js";
const ThresholdBrightnessFilter = ({
  id
}) => {
  const theme = useCurrentTheme();
  const brightnessSlope = theme === "dark" ? MARKER_DARK_BRIGHTNESS : MARKER_LIGHT_BRIGHTNESS;
  return /* @__PURE__ */ jsx(BrightnessFilter, { id, slope: brightnessSlope });
};
export {
  ThresholdBrightnessFilter
};
//# sourceMappingURL=ThresholdBrightnessFilter.js.map
