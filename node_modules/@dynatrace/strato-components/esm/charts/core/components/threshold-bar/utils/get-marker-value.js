const MARKER_VALUE_SEPARATOR = "\u2013";
function getMarkerValue(marker, formatter = (value) => `${value}`) {
  return marker.y0 === marker.y1 ? formatter(marker.y0) : `${formatter(
    marker.y0
  )} ${MARKER_VALUE_SEPARATOR} ${formatter(marker.y1)}`;
}
export {
  MARKER_VALUE_SEPARATOR,
  getMarkerValue
};
//# sourceMappingURL=get-marker-value.js.map
