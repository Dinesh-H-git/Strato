import { useMemo } from "react";
import { processAnnotationTracksData } from "../utils/process-annotation-tracks-data.js";
const useGroupedAnnotationsData = (tracks, domain) => useMemo(() => {
  return tracks.map((track, index) => ({
    markers: processAnnotationTracksData(track, index, domain),
    label: track.label,
    hidden: track.hidden
  }));
}, [tracks]);
export {
  useGroupedAnnotationsData
};
//# sourceMappingURL=useGroupedAnnotationsData.js.map
