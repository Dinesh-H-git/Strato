import { TRACK_HEIGHT, TRACK_SEPARATOR_STROKE_WIDTH } from "../constants.js";
const getAnnotationsTracksHeight = (tracksCount) => {
  if (tracksCount > 0) {
    return tracksCount * TRACK_HEIGHT + TRACK_SEPARATOR_STROKE_WIDTH;
  }
  return 0;
};
export {
  getAnnotationsTracksHeight
};
//# sourceMappingURL=get-annotations-tracks-height.js.map
