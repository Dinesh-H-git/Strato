import { MARGIN_TOP_STROKE, TRACK_HEIGHT } from "../constants.js";
const inMarkerBounds = (mousePosition, trackIndex, markerCoords) => {
  const [start, end] = markerCoords;
  const top = trackIndex * TRACK_HEIGHT + MARGIN_TOP_STROKE;
  const bottom = (trackIndex + 1) * TRACK_HEIGHT + MARGIN_TOP_STROKE;
  return mousePosition.y >= top && mousePosition.y <= bottom && mousePosition.x >= start && mousePosition.x <= end;
};
export {
  inMarkerBounds
};
//# sourceMappingURL=in-marker-bounds.js.map
