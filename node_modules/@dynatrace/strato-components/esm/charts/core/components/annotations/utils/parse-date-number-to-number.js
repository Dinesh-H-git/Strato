const parseDateNumberToNumber = (value) => {
  if (typeof value === "number") {
    return value;
  }
  if (value instanceof Date) {
    return value.getTime();
  }
  return 0;
};
export {
  parseDateNumberToNumber
};
//# sourceMappingURL=parse-date-number-to-number.js.map
