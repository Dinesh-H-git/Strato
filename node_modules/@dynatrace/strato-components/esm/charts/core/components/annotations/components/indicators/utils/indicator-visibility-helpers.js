import { isUndefined } from "lodash-es";
import { buildMarkerBrightnessState } from "../../../utils/build-marker-brightness-state.js";
const isAlwaysShown = (markerIndicatorDisplay, trackIndicatorDisplay) => markerIndicatorDisplay === "always" || isUndefined(markerIndicatorDisplay) && trackIndicatorDisplay === "always";
const isShownByInteraction = (data, markerId, indicatorsDisplay) => indicatorsDisplay !== "never" && data?.some(({ key }) => key === markerId);
function buildIndicator(color, selection, markerId, start, end, priority) {
  const isListHovered = selection.hoverGroupListKey === markerId;
  const state = buildMarkerBrightnessState(selection, markerId, isListHovered);
  return {
    end,
    start,
    color,
    state,
    priority
  };
}
const isAlwaysHidden = (markerIndicatorVisibility, trackIndicatorsVisibility) => markerIndicatorVisibility === "never" || isUndefined(markerIndicatorVisibility) && trackIndicatorsVisibility === "never";
export {
  buildIndicator,
  isAlwaysHidden,
  isAlwaysShown,
  isShownByInteraction
};
//# sourceMappingURL=indicator-visibility-helpers.js.map
