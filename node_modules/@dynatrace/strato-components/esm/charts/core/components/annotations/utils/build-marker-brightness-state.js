import { isUndefined } from "lodash-es";
const buildMarkerBrightnessState = ({ data, hoveredGroup, hoveredListItem, pinnedGroup }, markerId, isHoveringListItem) => {
  let state = "default";
  if ((pinnedGroup ?? hoveredGroup) && data) {
    const markersIdWithinGroup = data.map(({ key }) => key);
    if (markersIdWithinGroup.includes(markerId)) {
      if (isUndefined(pinnedGroup)) {
        state = "hovered";
      } else if (isUndefined(hoveredListItem)) {
        state = "active";
      } else {
        state = isHoveringListItem ? "active" : "inactive";
      }
    }
  }
  return state;
};
export {
  buildMarkerBrightnessState
};
//# sourceMappingURL=build-marker-brightness-state.js.map
