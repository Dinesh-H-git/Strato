const extractTooltipMarkerProps = (marker) => {
  const { id: _id, priority: _priority, hidden: _hidden, ...rest } = marker;
  return rest;
};
const prepareCustomTooltipData = (data, hoveredItemKey) => {
  let hoveredMarkers = [];
  const closeMarkers = [];
  data?.forEach((annotationGroup) => {
    const { key, markers } = annotationGroup;
    if (key === hoveredItemKey) {
      hoveredMarkers = markers.map(
        (marker) => extractTooltipMarkerProps(marker)
      );
    } else {
      const closeMarkerGroup = markers.map(
        (marker) => extractTooltipMarkerProps(marker)
      );
      closeMarkers.push(closeMarkerGroup);
    }
  });
  return {
    hoveredMarkers,
    closeMarkers
  };
};
export {
  prepareCustomTooltipData
};
//# sourceMappingURL=prepare-custom-tooltip-data.js.map
