const renderIconContent = (ctx, icon, iconImages, x, y) => {
  if (iconImages.has(icon)) {
    const imageElement = iconImages.get(icon);
    if (imageElement) {
      ctx.drawImage(imageElement, x, y);
    }
  }
};
export {
  renderIconContent
};
//# sourceMappingURL=render-icon-to-canvas.js.map
