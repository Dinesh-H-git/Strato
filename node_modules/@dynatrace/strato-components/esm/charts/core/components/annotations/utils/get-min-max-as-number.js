import { isUndefined } from "lodash-es";
import { parseDateNumberToNumber } from "./parse-date-number-to-number.js";
const getMinMaxAsNumber = (firstValue, secondValue) => {
  const first = parseDateNumberToNumber(firstValue);
  const second = isUndefined(secondValue) ? first : parseDateNumberToNumber(secondValue);
  return {
    min: Math.min(first, second),
    max: Math.max(first, second)
  };
};
export {
  getMinMaxAsNumber
};
//# sourceMappingURL=get-min-max-as-number.js.map
