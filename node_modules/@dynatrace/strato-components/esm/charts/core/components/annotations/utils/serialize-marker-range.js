import { toFinite } from "lodash-es";
import { isNumericalMarker } from "./is-numerical-marker.js";
const serializeNumericalRange = ({ start, end }) => [start, end ?? start].join("_");
const serializeTimeRange = ({ start, end }) => [start.getTime(), end?.getTime() ?? start.getTime()].join("_");
const serializeMarkerRange = (marker, trackIndex) => {
  const markerString = isNumericalMarker(marker) ? serializeNumericalRange(marker) : serializeTimeRange(marker);
  return `${trackIndex}_${markerString}`;
};
const deserializeMarkerRange = (range) => {
  const [trackIndex, from, to] = range.split("_").map(toFinite);
  return [from, to, trackIndex];
};
export {
  deserializeMarkerRange,
  serializeMarkerRange
};
//# sourceMappingURL=serialize-marker-range.js.map
