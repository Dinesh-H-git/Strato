import { jsx } from "react/jsx-runtime";
import clsx from "clsx";
import {
  forwardRef,
  useRef
} from "react";
import {
  charsFontWidth12pixelsSize400Map
} from "./chars-font-width-map.js";
import * as styles from "./ChartTextEllipsis.sty.js";
import {
  truncationContainer,
  truncationText
} from "./ChartTextEllipsis.sty.js";
import { useTruncateMiddleEllipsis } from "./useTruncateMiddleEllipsis.js";
import { useMergeRefs } from "../../../../core/hooks/useMergeRefs.js";
const ComputedTextEllipsis = /* @__PURE__ */ forwardRef((props, forwardedRef) => {
  const {
    children,
    className: consumerClassName,
    style: consumerStyle,
    "data-testid": dataTestId,
    "data-dtrum-mask": dataDtrumMask,
    "data-dtrum-allow": dataDtrumAllow,
    charsFontWidthMap,
    truncationMode,
    ...containerProps
  } = props;
  const internalContainerRef = useRef(null);
  const mergedContainerRef = useMergeRefs([
    internalContainerRef,
    forwardedRef
  ]);
  const text = typeof children === "string" ? children : String(children);
  const displayText = useTruncateMiddleEllipsis(
    text,
    charsFontWidthMap ?? charsFontWidth12pixelsSize400Map,
    internalContainerRef
  );
  return /* @__PURE__ */ jsx(
    "div",
    {
      ref: mergedContainerRef,
      className: clsx(truncationContainer, consumerClassName),
      "data-testid": dataTestId,
      "data-dtrum-mask": dataDtrumMask,
      "data-dtrum-allow": dataDtrumAllow,
      "data-sourcetext": children,
      style: consumerStyle,
      ...containerProps,
      children: /* @__PURE__ */ jsx("span", { className: truncationText, children: displayText })
    }
  );
});
const CssTextEllipsis = /* @__PURE__ */ forwardRef((props, forwardedRef) => {
  const {
    children,
    truncationMode = "end",
    className: consumerClassName,
    style: consumerStyle,
    "data-testid": dataTestId,
    charsFontWidthMap,
    // important to exclude from  remaining props to avoid problems in DOM.
    ...remainingProps
  } = props;
  return /* @__PURE__ */ jsx(
    "span",
    {
      ref: forwardedRef,
      "aria-label": String(children),
      "data-testid": dataTestId,
      className: clsx(styles.ellipsis({ truncationMode }), consumerClassName),
      style: consumerStyle,
      ...remainingProps,
      children: /* @__PURE__ */ jsx("span", { children })
    }
  );
});
const ChartTextEllipsis = /* @__PURE__ */ forwardRef((props, forwardedRef) => {
  const cssOnlyEllipsis = props.truncationMode !== "middle";
  return cssOnlyEllipsis ? /* @__PURE__ */ jsx(CssTextEllipsis, { ref: forwardedRef, ...props }) : /* @__PURE__ */ jsx(ComputedTextEllipsis, { ref: forwardedRef, ...props });
});
ChartTextEllipsis.displayName = "ChartTextEllipsis";
export {
  ChartTextEllipsis
};
//# sourceMappingURL=ChartTextEllipsis.js.map
