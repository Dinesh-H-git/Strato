import { validateCSSSize } from "../../../utils/validate-css-size.js";
function coerceWidthValue(widthValue, horizontalPadding) {
  let layoutWidth;
  if (widthValue === void 0) {
    return void 0;
  } else if (typeof widthValue === "number") {
    layoutWidth = `${widthValue - horizontalPadding}px`;
  } else {
    const cssSizeValidated = validateCSSSize(widthValue);
    layoutWidth = cssSizeValidated ? `calc(${widthValue} - ${horizontalPadding}px)` : void 0;
  }
  return layoutWidth;
}
export {
  coerceWidthValue
};
//# sourceMappingURL=coerce-width-value.js.map
