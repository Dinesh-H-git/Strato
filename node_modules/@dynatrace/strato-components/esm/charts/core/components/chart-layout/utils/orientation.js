import { LEGEND_POSITION, ORIENTATION } from "../../../constants.js";
const LEGEND_MIN_WIDTH = 225;
const calculateAutoOrientation = (width, height) => {
  if (width > height + LEGEND_MIN_WIDTH) {
    return ORIENTATION.Horizontal;
  }
  return ORIENTATION.Vertical;
};
const getOrientation = (width, height, position) => {
  if (position === LEGEND_POSITION.Bottom) {
    return ORIENTATION.Vertical;
  }
  if (position === LEGEND_POSITION.Right) {
    return ORIENTATION.Horizontal;
  }
  return calculateAutoOrientation(width, height);
};
const isVertical = (orientation) => orientation === ORIENTATION.Vertical;
const getLegendOrientation = (layoutOrientation) => isVertical(layoutOrientation) ? ORIENTATION.Horizontal : ORIENTATION.Vertical;
export {
  getLegendOrientation,
  getOrientation,
  isVertical
};
//# sourceMappingURL=orientation.js.map
