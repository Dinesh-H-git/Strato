import { calculateLegendSizeBasedOnRatio } from "./calculate-legend-size-based-on-ratio.js";
import { calculateLegendSizeForAutoRatio } from "./calculate-legend-size-for-auto-ratio.js";
const calculateLegendSize = (orientation, containerWidth, containerHeight, ratio, ratioBoundaries, candidateSizes) => ratio === "auto" ? calculateLegendSizeForAutoRatio(
  orientation,
  containerWidth,
  containerHeight,
  ratioBoundaries,
  candidateSizes
) : calculateLegendSizeBasedOnRatio(
  orientation,
  containerWidth,
  containerHeight,
  ratio
);
export {
  calculateLegendSize
};
//# sourceMappingURL=calculate-legend-size.js.map
