import { isVertical } from "./orientation.js";
import { sanitizeLegendRatio } from "../../../utils/sanitize-legend-ratio.js";
const calculateLegendSizeBasedOnRatio = (orientation, containerWidth, containerHeight, ratio) => {
  const sanitizedRatio = sanitizeLegendRatio(ratio);
  const fraction = sanitizedRatio * 0.01;
  const width = isVertical(orientation) ? containerWidth : fraction * containerWidth;
  const height = !isVertical(orientation) ? containerHeight : fraction * containerHeight;
  return { width, height };
};
export {
  calculateLegendSizeBasedOnRatio
};
//# sourceMappingURL=calculate-legend-size-based-on-ratio.js.map
