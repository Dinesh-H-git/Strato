import { jsx, jsxs } from "react/jsx-runtime";
import { ErrorBoundary } from "react-error-boundary";
import { EmptyState } from "../../chart-states/EmptyState.js";
import { ErrorState } from "../../chart-states/ErrorState.js";
import { LoaderOverlay } from "../../loader-overlay/LoaderOverlay.js";
import { LoadingContext } from "../context/Loading.context.js";
const LayoutStateHandler = (props) => {
  const { children, empty, emptyState, errorState, height, showLoader } = props;
  if (empty) {
    return showLoader ? /* @__PURE__ */ jsx(LoaderOverlay, {}) : /* @__PURE__ */ jsx(EmptyState, { height, children: emptyState });
  }
  return /* @__PURE__ */ jsxs(
    ErrorBoundary,
    {
      FallbackComponent: ErrorState({
        height,
        children: errorState
      }),
      children: [
        /* @__PURE__ */ jsx(LoadingContext.Provider, { value: showLoader, children }),
        showLoader && /* @__PURE__ */ jsx(LoaderOverlay, { gridArea: "1/1/2/2" })
      ]
    }
  );
};
export {
  LayoutStateHandler
};
//# sourceMappingURL=LayoutStateHandler.js.map
