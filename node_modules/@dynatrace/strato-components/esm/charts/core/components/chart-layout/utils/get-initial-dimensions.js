import { calculateLegendSize } from "./calculate-legend-size.js";
const getInitialDimensions = (orientation, width, height, containerWidth, containerHeight, ratio, ratioBoundaries, legendCandidateSize, overrideRatio) => {
  const legendContainerDimensions = calculateLegendSize(
    orientation,
    containerWidth,
    containerHeight,
    ratio,
    ratioBoundaries,
    legendCandidateSize
  );
  const overriddenDimensions = overrideRatio?.(
    orientation,
    width,
    height,
    ratio,
    legendContainerDimensions
  );
  return overriddenDimensions ?? legendContainerDimensions;
};
export {
  getInitialDimensions
};
//# sourceMappingURL=get-initial-dimensions.js.map
