import { useCallback, useEffect, useState } from "react";
function useEventErrorHandler() {
  const [error, setError] = useState("");
  useEffect(() => {
    if (error) {
      throw new Error(error);
    }
  }, [error]);
  const handlerError = useCallback((callback) => {
    try {
      if (typeof callback === "function") {
        return callback();
      }
    } catch (error2) {
      setError(error2);
    }
  }, []);
  return handlerError;
}
export {
  useEventErrorHandler
};
//# sourceMappingURL=useEventErrorHandler.js.map
