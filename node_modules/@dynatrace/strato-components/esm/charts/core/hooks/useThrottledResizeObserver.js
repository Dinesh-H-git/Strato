import { throttle } from "lodash-es";
import { useMemo, useState } from "react";
import useResizeObserver from "use-resize-observer";
import { SIXTY_FPS } from "../constants.js";
function useThrottledResizeObserver(wait = SIXTY_FPS) {
  const [size, setSize] = useState({});
  const onResize = useMemo(() => throttle(setSize, wait), [wait]);
  const { ref } = useResizeObserver({ onResize });
  return { refCallback: ref, ...size };
}
export {
  useThrottledResizeObserver
};
//# sourceMappingURL=useThrottledResizeObserver.js.map
