import { useEffect } from "react";
const useApplyCallbackOnScroll = (disabled, callback) => {
  useEffect(() => {
    if (disabled) {
      return;
    }
    document.body.addEventListener("scroll", callback, true);
    return () => document.body.removeEventListener("scroll", callback, true);
  }, [disabled]);
};
export {
  useApplyCallbackOnScroll
};
//# sourceMappingURL=useApplyCallbackOnScroll.js.map
