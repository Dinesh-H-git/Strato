import { useMemo } from "react";
import useResizeObserver from "use-resize-observer";
import { throttleRaf } from "../utils/throttleRaf.js";
const useThrottledCallbackOnResize = (subject, callback, disabled = false) => {
  const throttledCallback = useMemo(
    disabled ? () => () => {
    } : () => throttleRaf(callback),
    [disabled]
  );
  useResizeObserver({
    ref: subject,
    onResize: throttledCallback
  });
};
export {
  useThrottledCallbackOnResize
};
//# sourceMappingURL=useThrottledCallbackOnResize.js.map
