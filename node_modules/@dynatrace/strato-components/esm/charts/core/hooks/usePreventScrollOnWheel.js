import { useEffect } from "react";
const isWheelEvent = (event) => "metaKey" in event || "ctrlKey" in event;
function usePreventScrollOnWheel(targetRef) {
  useEffect(() => {
    const handleWheel = (event) => {
      if (isWheelEvent(event) && (event.metaKey || event.ctrlKey)) {
        event.preventDefault();
      }
    };
    const targetRefCurrent = targetRef?.current ?? document;
    targetRefCurrent.addEventListener("wheel", handleWheel, {
      passive: false
    });
    return () => {
      targetRefCurrent.removeEventListener("wheel", handleWheel);
    };
  }, [targetRef]);
}
export {
  usePreventScrollOnWheel
};
//# sourceMappingURL=usePreventScrollOnWheel.js.map
