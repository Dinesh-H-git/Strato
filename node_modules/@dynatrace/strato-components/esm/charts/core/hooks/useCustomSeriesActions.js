import {
  useEffect,
  useState
} from "react";
import { getStratoLogger } from "../../../core/sdk/sdk-logger.js";
const useCustomSeriesActions = (actions, isOpen) => {
  const [customActions, setCustomActions] = useState(null);
  const logger = getStratoLogger("useCustomSeriesActions");
  useEffect(() => {
    if (actions && isOpen) {
      if (actions instanceof Promise) {
        actions.then((resolvedActions) => {
          setCustomActions(resolvedActions);
        }).catch((error) => {
          logger.error("Failed to load series actions", error);
        });
      } else {
        setCustomActions(actions);
      }
    } else {
      setCustomActions(null);
    }
  }, [actions, isOpen]);
  return customActions;
};
export {
  useCustomSeriesActions
};
//# sourceMappingURL=useCustomSeriesActions.js.map
