import { scaleQuantize } from "d3-scale";
import {
  isCustomColorPalette
} from "../types/color-palette.js";
import { colorPaletteMap } from "../types/color-palettes-definitions.js";
const buildQuantizeScaleFromColorPalette = (min, max, colorPalette) => {
  let colors;
  if (isCustomColorPalette(colorPalette)) {
    if (Array.isArray(colorPalette)) {
      colors = colorPalette;
    } else {
      colors = Object.values(colorPalette);
    }
  } else {
    colors = Object.entries(colorPaletteMap[colorPalette]).map(([_, color]) => color.Default).reverse();
  }
  return scaleQuantize([min, max], colors);
};
export {
  buildQuantizeScaleFromColorPalette
};
//# sourceMappingURL=build-quantize-scale-from-color-palette.js.map
