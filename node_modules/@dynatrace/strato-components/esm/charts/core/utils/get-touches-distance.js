const getEuclideanDistance = ({ x: xA, y: yA }, { x: xB, y: yB }) => Math.sqrt(Math.pow(xB - xA, 2) + Math.pow(yB - yA, 2));
const getTouchesDistance = (touches) => {
  if (touches.length < 2) {
    return 0;
  }
  const fingerOne = { x: touches[0].clientX, y: touches[0].clientY };
  const fingerTwo = { x: touches[1].clientX, y: touches[1].clientY };
  return getEuclideanDistance(fingerOne, fingerTwo);
};
export {
  getTouchesDistance
};
//# sourceMappingURL=get-touches-distance.js.map
