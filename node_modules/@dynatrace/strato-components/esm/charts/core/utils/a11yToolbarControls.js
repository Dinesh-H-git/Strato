function getTabIndexValue(tabIndex) {
  return Object.values(tabIndex)[0];
}
function getTabIndexKey(tabIndex) {
  return Object.keys(tabIndex)[0];
}
function handleControls(direction, controls) {
  const newControls = [...controls];
  const controlIndex = controls.findIndex(
    (c) => getTabIndexValue(c) === 0
  );
  const isRightLastControl = direction === "ArrowRight" && controlIndex === newControls.length - 1;
  const isLeftFirstControl = direction === "ArrowLeft" && controlIndex === 0;
  if (isLeftFirstControl || isRightLastControl) {
    return newControls;
  }
  const nextControlIndex = direction === "ArrowLeft" ? controlIndex - 1 : controlIndex + 1;
  const currentControl = newControls[controlIndex];
  const nextControl = newControls[nextControlIndex];
  newControls[controlIndex] = { [getTabIndexKey(currentControl)]: -1 };
  newControls[nextControlIndex] = { [getTabIndexKey(nextControl)]: 0 };
  return newControls;
}
export {
  getTabIndexKey,
  getTabIndexValue,
  handleControls
};
//# sourceMappingURL=a11yToolbarControls.js.map
