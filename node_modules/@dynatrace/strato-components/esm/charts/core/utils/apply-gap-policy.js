import { convertTimeThreshold } from "./timeThreshold.js";
function getThresholdValue(gapPolicy) {
  return isThresholdGapPolicy(gapPolicy) ? convertTimeThreshold(gapPolicy.threshold) ?? 0 : 0;
}
function isThresholdGapPolicy(gapPolicy) {
  return typeof gapPolicy === "object" && "threshold" in gapPolicy;
}
function getNumericalThresholdValue(gapPolicy) {
  return isNumericalThresholdGapPolicy(gapPolicy) && Number.isFinite(gapPolicy.threshold) ? gapPolicy.threshold : 0;
}
function isNumericalThresholdGapPolicy(gapPolicy) {
  return typeof gapPolicy === "object" && "threshold" in gapPolicy;
}
export {
  getNumericalThresholdValue,
  getThresholdValue
};
//# sourceMappingURL=apply-gap-policy.js.map
