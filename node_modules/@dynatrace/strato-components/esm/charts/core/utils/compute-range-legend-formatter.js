import { isFormatterOptions } from "../types/formatter.js";
const computeRangeLegendFormatter = (formatter, unit) => {
  if (!isFormatterOptions(formatter)) {
    return (value) => formatter(value, unit);
  }
  return formatter;
};
export {
  computeRangeLegendFormatter
};
//# sourceMappingURL=compute-range-legend-formatter.js.map
