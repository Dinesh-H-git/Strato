function isMouseOutsideElement(event, ref) {
  const isNodeType = event.relatedTarget instanceof Node;
  if (!isNodeType) {
    return true;
  }
  const relatedTarget = event.relatedTarget;
  const isOutsideRelatedTarget = !ref.current?.contains(relatedTarget) && ref.current !== event.relatedTarget;
  return isOutsideRelatedTarget;
}
export {
  isMouseOutsideElement
};
//# sourceMappingURL=is-mouse-outside-element.js.map
