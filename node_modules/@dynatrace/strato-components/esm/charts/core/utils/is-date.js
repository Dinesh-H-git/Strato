import { isDate, isUndefined } from "lodash-es";
const isDateString = (dateString) => {
  if (typeof dateString !== "string") {
    return false;
  }
  return isValidTimestamp(dateString);
};
const isValidTimestamp = (timestamp) => !isNaN(new Date(timestamp).getTime());
const isDateCompatibleToTimestamp = (input) => !isUndefined(input) && (isDateString(input) || isDate(input) || isValidTimestamp(input));
const isParseableDate = (value) => {
  if (value instanceof Date) {
    return true;
  }
  if (typeof value === "number") {
    return false;
  } else if (typeof value === "string") {
    if (!isNaN(Number(value))) {
      return !!new Date(Number(value)).getTime();
    } else {
      return !!new Date(value).getTime();
    }
  }
  return false;
};
export {
  isDateCompatibleToTimestamp,
  isDateString,
  isParseableDate,
  isValidTimestamp
};
//# sourceMappingURL=is-date.js.map
