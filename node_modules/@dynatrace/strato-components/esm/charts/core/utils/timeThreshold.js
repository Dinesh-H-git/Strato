const TIME_THRESHOLD_REGEX = /^(\d+)(y|mo|d|h|min|s|ms)$/;
const parseTimeThreshold = (value) => {
  const matched = value.match(TIME_THRESHOLD_REGEX);
  return matched ? [+matched[1], matched[2]] : void 0;
};
const MULTIPLIERS = {
  y: 31556926e3,
  mo: 26298e5,
  d: 864e5,
  h: 36e5,
  min: 6e4,
  s: 1e3,
  ms: 1
};
const convertTimeThreshold = (timeThresholdExpr) => {
  const parsedValue = parseTimeThreshold(timeThresholdExpr);
  if (!parsedValue) {
    return void 0;
  }
  const [value, unit] = parsedValue;
  return value * MULTIPLIERS[unit];
};
export {
  convertTimeThreshold,
  parseTimeThreshold
};
//# sourceMappingURL=timeThreshold.js.map
