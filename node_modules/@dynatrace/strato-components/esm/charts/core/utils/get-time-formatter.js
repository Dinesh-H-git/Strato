import { isFunction, isUndefined } from "lodash-es";
import { formatDate } from "@dynatrace-sdk/units";
import { formatDateTicks } from "./format-date-ticks.js";
const getTimeFormatter = (axisFormatter) => {
  if (isUndefined(axisFormatter)) {
    return (value) => formatDateTicks(new Date(value));
  } else if (isFunction(axisFormatter)) {
    return (value) => axisFormatter(new Date(value));
  } else {
    return (value) => formatDate(value, axisFormatter);
  }
};
export {
  getTimeFormatter
};
//# sourceMappingURL=get-time-formatter.js.map
