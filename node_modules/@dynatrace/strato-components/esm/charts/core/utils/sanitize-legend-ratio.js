import { clamp, isNumber } from "lodash-es";
import { defaultRatio } from "../constants.js";
const DEFAULT_PIE_LEGEND_RATIO = 80;
const LEGEND_RATIO_RANGE = {
  min: 10,
  max: 80
};
const sanitizeLegendRatio = (legendRatio = defaultRatio) => isNumber(legendRatio) ? clamp(legendRatio, LEGEND_RATIO_RANGE.min, LEGEND_RATIO_RANGE.max) : LEGEND_RATIO_RANGE.max;
export {
  DEFAULT_PIE_LEGEND_RATIO,
  LEGEND_RATIO_RANGE,
  sanitizeLegendRatio
};
//# sourceMappingURL=sanitize-legend-ratio.js.map
