import {
  format
} from "@dynatrace-sdk/units";
import { defaultUnitFormatter } from "./defaultUnitFormatter.js";
import { isFormatterOptions } from "../types/formatter.js";
function getFormatter(formatter) {
  if (!formatter) {
    return (value, unit) => defaultUnitFormatter(unit)(value);
  }
  if (isFormatterOptions(formatter)) {
    return (value) => format(value, formatter);
  }
  return formatter;
}
export {
  getFormatter
};
//# sourceMappingURL=get-formatter.js.map
