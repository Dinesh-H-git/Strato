import { isDate } from "lodash-es";
import { isDateString } from "./is-date.js";
const convertDateStringToTimestamp = (input) => new Date(input).getTime();
const convertDateToTimestamp = (input) => input.getTime();
const convertDateCompatibleToTimestamp = (input) => {
  if (isDateString(input)) {
    return convertDateStringToTimestamp(input);
  }
  if (isDate(input)) {
    return convertDateToTimestamp(input);
  }
  return input;
};
export {
  convertDateCompatibleToTimestamp,
  convertDateStringToTimestamp,
  convertDateToTimestamp
};
//# sourceMappingURL=convert-dates.js.map
