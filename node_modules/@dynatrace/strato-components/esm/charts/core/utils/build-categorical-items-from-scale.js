import { isUndefined } from "lodash-es";
import { getValidColor } from "./getValidColor.js";
const buildCategoricalItemsFromScale = (scale, overrides, id) => {
  const categories = scale.domain();
  const legendItems = [];
  categories.forEach((category) => {
    const color = overrides?.get(category) || scale(category);
    if (!isUndefined(color)) {
      legendItems.push({
        id: id ? id(category) : crypto.randomUUID(),
        color: { Default: getValidColor(color) },
        name: category
      });
    }
  });
  return legendItems;
};
export {
  buildCategoricalItemsFromScale
};
//# sourceMappingURL=build-categorical-items-from-scale.js.map
