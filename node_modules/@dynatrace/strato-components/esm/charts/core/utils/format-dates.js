import { formatDate } from "@dynatrace-sdk/units";
const formatSeconds = (date) => formatDate(date, {
  hour: "2-digit",
  minute: "2-digit",
  second: "2-digit",
  fractionalSecondDigits: 3
});
const formatMinute = (date) => formatDate(date, {
  hour: "2-digit",
  minute: "2-digit",
  second: "2-digit"
});
const formatHour = (date) => formatDate(date, {
  hour: "2-digit",
  minute: "2-digit"
});
const formatDay = (date) => formatDate(date, {
  hour: "2-digit"
});
const formatWeekday = (date) => formatDate(date, {
  month: "short",
  day: "numeric"
});
const formatMonth = (date) => formatDate(date, {
  month: "short",
  day: "numeric"
});
const formatYear = (date) => formatDate(date, {
  year: "numeric"
});
export {
  formatDay,
  formatHour,
  formatMinute,
  formatMonth,
  formatSeconds,
  formatWeekday,
  formatYear
};
//# sourceMappingURL=format-dates.js.map
