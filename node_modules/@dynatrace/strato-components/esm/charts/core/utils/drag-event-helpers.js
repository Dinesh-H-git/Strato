import { MIDDLE_BUTTON_ID } from "../constants.js";
import {
  PointerEventActions
} from "../types/pointer-events.js";
const getEndActionByPreviousAction = (prevGesture) => prevGesture.action === PointerEventActions.MiddleButtonDragMove ? PointerEventActions.MiddleButtonDragEnd : PointerEventActions.DragEnd;
const buildDragEndEvent = (prevGesture, startHoldingAt, releasedAt) => ({
  action: getEndActionByPreviousAction(prevGesture),
  startHoldingAt,
  releasedAt
});
const getMoveActionByButton = (button) => button === MIDDLE_BUTTON_ID ? PointerEventActions.MiddleButtonDragMove : PointerEventActions.DragMove;
const buildDragMoveEvent = (button, startHoldingAt, prevPosition, currentPosition) => {
  const delta = currentPosition.relative.x - prevPosition.relative.x;
  return {
    action: getMoveActionByButton(button),
    startHoldingAt,
    currentPosition,
    delta
  };
};
const getStartActionByButton = (button) => button === MIDDLE_BUTTON_ID ? PointerEventActions.MiddleButtonDragStart : PointerEventActions.DragStart;
const buildDragStartEvent = (button, startHoldingAt) => ({
  action: getStartActionByButton(button),
  startHoldingAt
});
export {
  buildDragEndEvent,
  buildDragMoveEvent,
  buildDragStartEvent
};
//# sourceMappingURL=drag-event-helpers.js.map
