import { SELECTION_RIGHT_OVERFLOW } from "../components/interactions-overlay/constants.js";
const calculateMousePositions = ({ clientY, clientX }, target) => {
  const { top, left, width } = target.getBoundingClientRect();
  return {
    absolute: {
      x: clientX,
      y: clientY
    },
    relative: {
      x: Math.min(
        Math.max(clientX - left, 0),
        width + SELECTION_RIGHT_OVERFLOW
      ),
      y: clientY - top
    }
  };
};
export {
  calculateMousePositions
};
//# sourceMappingURL=calculate-mouse-positions.js.map
