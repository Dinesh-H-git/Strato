import { isNumber, isString } from "lodash-es";
import { validateCSSSize } from "./validate-css-size.js";
const PX_UNIT = "px";
function coerceSizeValue(value) {
  if (isNumber(value)) {
    return `${value}${PX_UNIT}`;
  }
  if (isString(value)) {
    return validateCSSSize(value);
  }
  return void 0;
}
export {
  coerceSizeValue
};
//# sourceMappingURL=coerceSizeValue.js.map
