import { invertColorPalette } from "./invert-color-palette.js";
const prepareInvertedColorPalette = (coloringMap) => {
  return Object.keys(coloringMap).reduce((acc, key) => {
    acc[key] = coloringMap[key];
    acc[`${key}-inverted`] = invertColorPalette(acc[key]);
    return acc;
  }, {});
};
export {
  prepareInvertedColorPalette
};
//# sourceMappingURL=prepare-inverted-color-palette.js.map
