const getRelativeValue = (value, total, allowNegativeValues = true) => {
  if (total === 0) {
    return 0;
  }
  const numberValue = isNaN(value) ? 0 : value;
  const currentValue = allowNegativeValues ? numberValue : Math.abs(numberValue);
  const isNegativeValue = value < 0;
  const math = Math[isNegativeValue ? "ceil" : "floor"];
  return math(currentValue / total * 1e3) / 10;
};
export {
  getRelativeValue
};
//# sourceMappingURL=get-relative-value.js.map
