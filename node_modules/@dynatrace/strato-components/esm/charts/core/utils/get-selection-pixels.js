import { cloneDeep } from "lodash-es";
const getSelectionPixels = (percentage, width, position) => {
  if (!percentage) {
    return position;
  }
  return position.map((pos, i) => {
    const clone = cloneDeep(pos);
    clone.relative.x = width * percentage[i] / 100;
    return clone;
  });
};
export {
  getSelectionPixels
};
//# sourceMappingURL=get-selection-pixels.js.map
