import { formatUnit } from "@dynatrace-sdk/units";
import { isUnit } from "../types/formatter.js";
function getUnitAsString(unit) {
  if (!unit) {
    return "";
  }
  if (isUnit(unit)) {
    return formatUnit(unit);
  }
  return unit.toString();
}
export {
  getUnitAsString
};
//# sourceMappingURL=get-unit-as-string.js.map
