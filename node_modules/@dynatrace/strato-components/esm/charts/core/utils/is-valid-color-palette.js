import { isString } from "lodash-es";
import { colorPaletteOptions } from "../types/color-palette.js";
const isValidColorPalette = (colorPalette) => {
  return isString(colorPalette) && colorPaletteOptions.includes(colorPalette ?? "");
};
export {
  isValidColorPalette
};
//# sourceMappingURL=is-valid-color-palette.js.map
