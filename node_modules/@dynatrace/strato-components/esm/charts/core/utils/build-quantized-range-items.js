const buildQuantizedRangeItems = (scale, invertItems = false) => {
  const range = invertItems ? scale.range().reverse() : scale.range();
  return range.map((color) => {
    const [from, to] = scale.invertExtent(color);
    return {
      from,
      to,
      color
    };
  });
};
export {
  buildQuantizedRangeItems
};
//# sourceMappingURL=build-quantized-range-items.js.map
