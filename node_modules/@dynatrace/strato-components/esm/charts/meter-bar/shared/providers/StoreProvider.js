import { jsx } from "react/jsx-runtime";
import { useRef } from "react";
import { createStore } from "../../../../core/store/store.js";
import { INITIAL_STATE } from "../constants.js";
import { MeterBarChartTooltipContext } from "../context/MeterBarChartTooltipContext.js";
const StoreProvider = ({ children }) => {
  const storeRef = useRef(null);
  storeRef.current = createStore({ ...INITIAL_STATE });
  return /* @__PURE__ */ jsx(MeterBarChartTooltipContext.Provider, { value: storeRef.current, children });
};
export {
  StoreProvider
};
//# sourceMappingURL=StoreProvider.js.map
