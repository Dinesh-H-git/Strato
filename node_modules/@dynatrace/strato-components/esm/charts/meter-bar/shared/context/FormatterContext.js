import { createContext, useContext } from "react";
import {
  format
} from "@dynatrace-sdk/units";
import {
  isFormatterOptions
} from "../../../core/types/formatter.js";
const FormatterContext = createContext({});
const useFormatter = () => {
  const context = useContext(FormatterContext);
  if (context === void 0) {
    throw new Error("useFormatter must be within FormatterContext");
  }
  const { formatter, unit } = context;
  if (isFormatterOptions(formatter)) {
    return { formatter: (value) => format(value, formatter) };
  }
  return { formatter, unit };
};
export {
  FormatterContext,
  useFormatter
};
//# sourceMappingURL=FormatterContext.js.map
