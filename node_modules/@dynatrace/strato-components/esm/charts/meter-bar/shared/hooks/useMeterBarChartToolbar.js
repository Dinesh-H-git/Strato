import { useRef } from "react";
import { useMeterBarToolbarConfig } from "../context/MeterBarToolbarConfigContext.js";
const useMeterBarChartToolbar = () => {
  const toolbarRef = useRef(null);
  const toolbarConfig = useMeterBarToolbarConfig();
  const isToolbarEnabled = !toolbarConfig?.hidden && !!toolbarConfig?.intents?.length;
  const modifyHoverState = (hoverState) => {
    if (isToolbarEnabled) {
      if (hoverState) {
        toolbarRef.current?.showPopover();
      } else {
        toolbarRef.current?.hidePopover();
      }
    }
  };
  return {
    toolbarRef,
    isToolbarEnabled,
    modifyHoverState
  };
};
export {
  useMeterBarChartToolbar
};
//# sourceMappingURL=useMeterBarChartToolbar.js.map
