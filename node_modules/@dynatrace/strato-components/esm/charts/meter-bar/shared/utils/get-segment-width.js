import { scaleLinear } from "d3-scale";
const getSegmentWidth = (value, min, max, containerWidth) => {
  if (min === max) {
    return value >= max ? containerWidth : 0;
  }
  const segmentScale = scaleLinear().domain([min, max]).range([0, containerWidth]).clamp(true);
  return segmentScale(value);
};
export {
  getSegmentWidth
};
//# sourceMappingURL=get-segment-width.js.map
