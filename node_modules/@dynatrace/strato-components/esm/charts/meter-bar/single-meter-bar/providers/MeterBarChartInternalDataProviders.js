import { jsx } from "react/jsx-runtime";
import { DEFAULT_VALUE_ACCESSOR } from "../../../core/constants.js";
import { MeterBarChartDataContext } from "../context/MeterBarChartDataContext.js";
import { useMeterBarChartThresholds } from "../hooks/useMeterBarChartThresholds.js";
import { useMeterBarColorRule } from "../hooks/useMeterBarColorRule.js";
import { NAME_DEFAULT_VALUE, SEGMENT_COLOR_DEFAULT } from "../types/default.js";
const MeterBarChartInternalDataProviders = ({
  value,
  displayValue,
  valueAccessor = DEFAULT_VALUE_ACCESSOR,
  name = NAME_DEFAULT_VALUE,
  color,
  children
}) => {
  const matchingColorRule = useMeterBarColorRule(value, valueAccessor);
  const { thresholdIndicators, matchingThreshold, legendItems } = useMeterBarChartThresholds(displayValue);
  const meterBarChartData = {
    color: matchingColorRule?.Default ?? matchingThreshold?.color ?? color ?? SEGMENT_COLOR_DEFAULT,
    value,
    name,
    valueAccessor,
    thresholdIndicators,
    legendItems
  };
  return /* @__PURE__ */ jsx(MeterBarChartDataContext.Provider, { value: meterBarChartData, children });
};
export {
  MeterBarChartInternalDataProviders
};
//# sourceMappingURL=MeterBarChartInternalDataProviders.js.map
