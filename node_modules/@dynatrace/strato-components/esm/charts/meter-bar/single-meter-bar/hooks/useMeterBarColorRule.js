import { useColorRule } from "../../../core/color-rule/useColorRule.js";
import { DEFAULT_VALUE_ACCESSOR } from "../../../core/constants.js";
const useMeterBarColorRule = (value, valueAccessor) => {
  const seriesForColorRule = typeof value === "number" ? [{ value }] : [
    {
      ...value,
      _chartValueAccessor: valueAccessor ?? DEFAULT_VALUE_ACCESSOR
    }
  ];
  const colorRule = useColorRule(
    seriesForColorRule,
    "meterBar"
  );
  return colorRule.for(
    seriesForColorRule[0]
  );
};
export {
  useMeterBarColorRule
};
//# sourceMappingURL=useMeterBarColorRule.js.map
