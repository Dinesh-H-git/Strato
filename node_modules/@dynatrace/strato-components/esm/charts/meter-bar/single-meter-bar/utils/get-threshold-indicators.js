import { scaleLinear } from "d3-scale";
import { compact } from "lodash-es";
const getThresholdIndicators = (thresholds, min, max, containerWidth) => {
  const scale = scaleLinear().domain([min, max]).range([0, containerWidth]).clamp(true);
  return compact(
    thresholds.map(
      (threshold) => threshold.showIndicator ? scale(threshold.value) : void 0
    )
  );
};
export {
  getThresholdIndicators
};
//# sourceMappingURL=get-threshold-indicators.js.map
