import { getThresholdIndicators } from "./get-threshold-indicators.js";
import { findMatchingThreshold } from "../../../core/utils/find-matching-threshold.js";
function buildThresholds(value, min, max, width, thresholdsConfig) {
  const matchingThreshold = thresholdsConfig !== void 0 ? findMatchingThreshold(value, thresholdsConfig.data) : void 0;
  const thresholdIndicators = thresholdsConfig !== void 0 ? getThresholdIndicators(thresholdsConfig.data, min, max, width) : [];
  const legendItems = thresholdsConfig !== void 0 ? thresholdsConfig.data.map((threshold, index) => {
    const name = threshold.name ?? `Threshold ${index + 1}`;
    return {
      id: `item-${name}-${index}`,
      name,
      color: { Default: threshold.color }
    };
  }) : [];
  return { matchingThreshold, thresholdIndicators, legendItems };
}
export {
  buildThresholds
};
//# sourceMappingURL=build-thresholds.js.map
