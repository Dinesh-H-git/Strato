import { jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import { StoreProvider } from "./StoreProvider.js";
import { OverlayProvider } from "../../../core/components/overlay-tooltip/contexts/OverlayContext.js";
import { TooltipProvider } from "../../../core/components/overlay-tooltip/contexts/TooltipContext.js";
import { ChartSeriesActionContext } from "../../../core/components/series-action/ChartSeriesActionContext.js";
import { ColorRuleContext } from "../../../core/context/ColorRuleContext.js";
import { FormatterContext } from "../../shared/context/FormatterContext.js";
import { MeterBarChartLoadingContext } from "../../shared/context/MeterBarChartLoadingContext.js";
import { MeterBarToolbarConfigContext } from "../../shared/context/MeterBarToolbarConfigContext.js";
const MultiMeterBarChartProviders = ({
  colorPalette,
  formatter,
  unit,
  segments,
  colorRules,
  children,
  toolbar,
  seriesActions,
  loading
}) => {
  const formatterContextProps = useMemo(
    () => ({
      formatter,
      unit
    }),
    [formatter, unit]
  );
  const contextSeriesActionsValue = useMemo(
    () => ({
      chartSeriesActions: seriesActions
    }),
    [seriesActions]
  );
  return /* @__PURE__ */ jsx(ChartSeriesActionContext.Provider, { value: contextSeriesActionsValue, children: /* @__PURE__ */ jsx(FormatterContext.Provider, { value: formatterContextProps, children: /* @__PURE__ */ jsx(ColorRuleContext.Provider, { value: colorRules, children: /* @__PURE__ */ jsx(MeterBarChartLoadingContext.Provider, { value: loading, children: /* @__PURE__ */ jsx(MeterBarToolbarConfigContext.Provider, { value: toolbar, children: /* @__PURE__ */ jsx(
    StoreProvider,
    {
      colorPalette,
      segmentsSlot: segments,
      children: /* @__PURE__ */ jsx(OverlayProvider, { children: /* @__PURE__ */ jsx(TooltipProvider, { children }) })
    }
  ) }) }) }) }) });
};
export {
  MultiMeterBarChartProviders
};
//# sourceMappingURL=MultiMeterBarChartProviders.js.map
