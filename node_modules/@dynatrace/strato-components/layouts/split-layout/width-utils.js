var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var width_utils_exports = {};
__export(width_utils_exports, {
  clamp: () => clamp,
  convertToPx: () => convertToPx,
  resolveWidthPx: () => resolveWidthPx,
  toCssLength: () => toCssLength
});
module.exports = __toCommonJS(width_utils_exports);
const clamp = (min, value, max) => {
  return Math.max(min, Math.min(value, max));
};
const isPercentage = (value) => {
  return typeof value === "string" && value.trim().endsWith("%");
};
const convertToPx = (value, containerSize) => {
  if (typeof value === "number") {
    return value;
  }
  const numeric = Number.parseFloat(value);
  if (!Number.isFinite(numeric)) {
    throw Error("`value` must be finite");
  }
  if (isPercentage(value)) {
    return numeric / 100 * containerSize;
  }
  return numeric;
};
const resolveWidthPx = (widthValue, containerWidth, fallback) => {
  if (containerWidth <= 0) {
    return fallback;
  }
  return convertToPx(widthValue, containerWidth);
};
const toCssLength = (value) => {
  if (typeof value === "number") {
    return `${value}px`;
  }
  return value;
};
