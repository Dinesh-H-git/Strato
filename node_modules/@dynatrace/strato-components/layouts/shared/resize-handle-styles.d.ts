export declare const resizeHandleStyles: {
    backgroundColor: string;
    border: "none";
    borderRadius: string;
    cursor: "col-resize";
    height: string;
    left: "3px";
    margin: number;
    padding: number;
    touchAction: "none";
    transition: `height ${string} ${string}`;
    width: "2px";
    selectors: {
        '&:focus-visible': {
            width: "100%";
        };
        '&:hover&:focus-visible': {
            width: "2px";
        };
        '&:focus-visible, &:hover': {
            height: string;
            backgroundColor: string;
        };
        '&:after': {
            content: "\"\"";
            display: "block";
            height: "100%";
            left: "50%";
            position: "absolute";
            top: number;
            transform: "translateX(-50%)";
            userSelect: "none";
            width: "100%";
        };
    };
};
