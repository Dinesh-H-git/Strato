var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var useLayoutState_exports = {};
__export(useLayoutState_exports, {
  useLayoutState: () => useLayoutState
});
module.exports = __toCommonJS(useLayoutState_exports);
var import_useControllableState = require("../../../core/hooks/useControllableState.js");
var import_useBreakpointCollapse = require("../shared/useBreakpointCollapse.js");
function useLayoutState({
  details,
  containerRef
}) {
  const [preferredLayoutMode, setPreferredLayoutMode] = (0, import_useControllableState.useControllableState)({
    value: details.config.layout,
    defaultValue: details.config.defaultLayout,
    onChange: details.config.onLayoutChange
  });
  const { containerWidth, collapsed, setCollapsed, isBelowBreakpoint } = (0, import_useBreakpointCollapse.useBreakpointCollapse)({
    collapsed: {
      value: details.config.collapsed,
      defaultValue: details.config.defaultCollapsed ?? false,
      onChange: details.config.onCollapsedChange
    },
    breakpoint: details.config.breakpoint,
    containerRef
  });
  let mode = preferredLayoutMode ?? "split";
  if (collapsed) {
    mode = "collapsed";
  }
  if (isBelowBreakpoint) {
    mode = "drawer";
  }
  return {
    containerWidth,
    collapsed,
    setCollapsed,
    preferredLayoutMode,
    setPreferredLayoutMode,
    mode
  };
}
