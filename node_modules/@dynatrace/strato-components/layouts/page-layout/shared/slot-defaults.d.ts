import type { PanelWidth } from '../../split-layout/width-utils.js';
import type { LayoutMode } from '../details-layout/DetailsLayoutContext.js';
export declare const sidebarDefaults: {
    defaultWidth: PanelWidth;
    minWidth: PanelWidth;
    maxWidth: PanelWidth;
    resizable: boolean;
    defaultCollapsed: boolean;
    breakpoint: number;
};
export declare const contentDefaults: {
    minWidth: PanelWidth;
};
export declare const detailsDefaults: {
    minWidth: PanelWidth;
    maxWidth: PanelWidth;
    defaultWidth: PanelWidth;
    resizable: boolean;
    defaultCollapsed: boolean;
    defaultLayout: LayoutMode;
    breakpoint: number;
};
