/**
 * Generates a link that opens the specified document on current environment.
 *
 * *Note:* Due to varying document availability across environments, there's no guarantee the link will always successfully open the document.
 *
 * @param {string} documentId The unique identifier (UUID or external ID) of the document to open.
 * @returns {string} A URL for the document in the current environment.
 */
export declare function getDocumentLink(documentId: string): string;
