import { PathChangeHandler } from './types/path-change-handler';
/**
 * Registers a custom handler for changing the app's URL path.
 * It is used only for the changes that are triggered externally, outside the app.
 *
 * If a custom handler is not set, the application is reloaded
 * by replacing the current document location with a new path.
 *
 * @param {PathChangeHandler} handler Custom handler for changing the app's URL path.
 * @returns {void}
 * @see {@link PathChangeHandler}
 * @see {@link PathChangeRequest}
 */
export declare function setPathChangeHandler(handler: PathChangeHandler): void;
