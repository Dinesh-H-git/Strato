import { IntentPayload } from './types/intent-payload';
/**
 * Generates a link that launches the App Shell on current environment and lets it handle the specified intent.
 *
 * *Note:* There is no guarantee that the specified app will be launched since different sets of apps could be installed on different platform instances.
 * Relying on explicitly hardcoded app could lead to broken UX.
 * @param {IntentPayload} intentPayload Set of properties to pass to the platform.
 * @returns {string} URL to the specified intent on current environment.
 * @see {@link IntentPayload}
 * @see {@link sendIntent}
 */
export declare function getIntentLink(intentPayload: IntentPayload): string;
/**
 * Generates a link that launches the specified app on current environment and lets it handle the specified intent of given id.
 *
 * *Note:* There is no guarantee that the specified app will be launched since different sets of apps could be installed on different platform instances.
 * Relying on explicitly hardcoded app could lead to broken UX.
 * @param {IntentPayload} intentPayload Set of properties to pass to the specified app.
 * @param {string} [appId] ID of the app that will be launched to handle the intent.
 * @param {string} [intentId] ID of the action that will be passed to the app.
 * @returns {string} URL to the specified intent on current environment.
 * @see {@link IntentPayload}
 * @see {@link sendIntent}
 */
export declare function getIntentLink(intentPayload: IntentPayload, appId: string, intentId: string): string;
