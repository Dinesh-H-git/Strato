import { PermissionContext } from './permission-context';
/**
 * optional generic set of context data
 */
export interface SinglePermissionRequest {
    permission: 'app-settings:objects:read' | 'app-settings:objects:write' | 'app-settings:objects:admin';
    context?: PermissionContext;
}
