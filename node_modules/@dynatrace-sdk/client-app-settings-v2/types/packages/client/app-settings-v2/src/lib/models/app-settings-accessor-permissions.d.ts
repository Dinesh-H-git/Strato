import { AppSettingsPermissionsList } from './app-settings-permissions-list';
import { Identity } from './identity';
export interface AppSettingsAccessorPermissions {
    accessor: Identity;
    permissions: AppSettingsPermissionsList;
}
