/**
 * ErrorResponse
 */
import { ErrorResponseError } from '../error-response-error';
export declare class NotFound extends ErrorResponseError {
    readonly isNotFound = true;
    readonly name = "NotFound";
}
export declare function isNotFound(e: any): e is NotFound;
