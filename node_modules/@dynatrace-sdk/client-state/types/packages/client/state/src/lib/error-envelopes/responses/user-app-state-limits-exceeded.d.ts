/**
 * ErrorResponse
 */
import { ErrorResponseError } from '../error-response-error';
export declare class UserAppStateLimitsExceeded extends ErrorResponseError {
    readonly isUserAppStateLimitsExceeded = true;
    readonly name = "UserAppStateLimitsExceeded";
}
export declare function isUserAppStateLimitsExceeded(e: any): e is UserAppStateLimitsExceeded;
