/**
 * ErrorResponse
 */
import { ErrorResponseError } from '../error-response-error';
export declare class AppStateLimitsExceeded extends ErrorResponseError {
    readonly isAppStateLimitsExceeded = true;
    readonly name = "AppStateLimitsExceeded";
}
export declare function isAppStateLimitsExceeded(e: any): e is AppStateLimitsExceeded;
