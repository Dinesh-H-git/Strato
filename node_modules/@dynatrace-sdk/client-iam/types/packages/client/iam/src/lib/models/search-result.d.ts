import { ServiceUserDto } from './service-user-dto';
export interface SearchResult {
    /**
     * Collection containing the paginated result
     */
    results?: ServiceUserDto[];
    /**
     * Key for requesting the next page of the result
     */
    nextPageKey?: string;
    /**
     * Total count of records
     */
    totalCount?: number;
}
