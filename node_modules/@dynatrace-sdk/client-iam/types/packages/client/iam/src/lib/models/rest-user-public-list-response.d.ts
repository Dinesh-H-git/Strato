import { RestUserPublic } from './rest-user-public';
export interface RestUserPublicListResponse {
    /**
     * Collection containing the paginated result
     */
    results?: RestUserPublic[];
    /**
     * Key for requesting the next page of the result
     */
    nextPageKey?: string;
    /**
     * Total count of records
     */
    totalCount?: number;
}
