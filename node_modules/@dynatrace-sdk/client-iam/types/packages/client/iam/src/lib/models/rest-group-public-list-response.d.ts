import { RestGroupPublic } from './rest-group-public';
export interface RestGroupPublicListResponse {
    /**
     * Collection containing the paginated result
     */
    results?: RestGroupPublic[];
    /**
     * Key for requesting the next page of the result
     */
    nextPageKey?: string;
    /**
     * Total count of records
     */
    totalCount?: number;
}
