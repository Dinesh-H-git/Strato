import * as types from '../types';
export declare abstract class BaseError extends Error implements types.BaseError {
    readonly cause?: any;
    protected constructor(message: string, cause?: any);
}
