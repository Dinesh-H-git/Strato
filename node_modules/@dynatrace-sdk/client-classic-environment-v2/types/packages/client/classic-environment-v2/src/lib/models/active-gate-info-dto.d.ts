/**
 * ActiveGates in group.
 */
export interface ActiveGateInfoDto {
    /**
     * List of errors if Extension cannot be run on the ActiveGate
     */
    errors: string[];
    /**
     * ActiveGate ID.
     */
    id: number;
}
