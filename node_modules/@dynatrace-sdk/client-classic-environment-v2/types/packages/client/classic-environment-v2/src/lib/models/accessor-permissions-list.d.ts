import { AccessorPermissions } from './accessor-permissions';
/**
 * All configured permissions of an object (excluding owner).
 */
export interface AccessorPermissionsList {
    accessors?: AccessorPermissions[];
}
