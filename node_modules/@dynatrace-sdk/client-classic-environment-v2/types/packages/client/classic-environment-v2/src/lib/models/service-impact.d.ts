import { Impact } from './impact';
/**
 * Analysis of problem impact to a service.
 */
export interface ServiceImpact extends Impact {
    /**
     * The number of potentially impacted services.
     */
    numberOfPotentiallyAffectedServiceCalls: number;
}
