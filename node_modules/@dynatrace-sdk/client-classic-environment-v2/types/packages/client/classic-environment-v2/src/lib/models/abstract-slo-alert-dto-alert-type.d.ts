/**
 * Defines the actual set of fields depending on the value. See one of the following objects:
 *
 * * `BURN_RATE` -> BurnRateAlert
 * * `STATUS` -> StatusAlert
 *
 * @deprecated Use literal values.
 */
export declare enum AbstractSloAlertDtoAlertType {
    BurnRate = "BURN_RATE",
    Status = "STATUS"
}
