/**
 * When to reset datasource value in the UI on filter change.
 *
 * @deprecated Use literal values.
 */
export declare enum DatasourceDefinitionResetValue {
    Always = "ALWAYS",
    InvalidOnly = "INVALID_ONLY",
    Never = "NEVER"
}
