/**
 * Defines the actual set of fields depending on the value. See one of the following objects:
 *
 * * `BROWSER` -> BMAction
 * * `HTTP` -> MonitorRequestExecutionResult
 *
 * @deprecated Use literal values.
 */
export declare enum ExecutionStepMonitorType {
    Browser = "BROWSER",
    Http = "HTTP"
}
