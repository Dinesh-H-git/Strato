import { EntrypointPayload } from './entrypoint-payload';
import { TruncationInfo } from './truncation-info';
/**
 * A list of values that has possibly been truncated.
 */
export interface AttackEntrypointPayloadItem {
    /**
     * Information on a possible truncation.
     */
    truncationInfo?: TruncationInfo;
    /**
     * Values of the list.
     */
    values?: EntrypointPayload[];
}
