import { CredentialsResponseElement } from './credentials-response-element';
/**
 * A list of credentials sets for Synthetic monitors.
 */
export interface CredentialsList {
    /**
     * A list of credentials sets for Synthetic monitors.
     */
    credentials: CredentialsResponseElement[];
    nextPageKey?: string;
    pageSize?: number;
    totalCount?: number;
}
