import { Timeframe } from './timeframe';
/**
 * Parameters that are present in all analyzer input parameter definitions.
 */
export interface AnalyzerGeneralParameters {
    timeframe?: Timeframe;
    resolveDimensionalQueryData?: boolean;
    logVerbosity?: 'TRACING' | 'INFO' | 'WARNING' | 'SEVERE';
}
