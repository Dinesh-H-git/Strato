import { AnalyzerErrorDetails } from './analyzer-error-details';
/**
 * The result of the dry-execution of the analyzer.
 */
export interface AnalyzerValidationResult {
    /**
     * Whether the analyzer can be successfully executed with the given input data.
     */
    valid: boolean;
    details?: AnalyzerErrorDetails;
}
