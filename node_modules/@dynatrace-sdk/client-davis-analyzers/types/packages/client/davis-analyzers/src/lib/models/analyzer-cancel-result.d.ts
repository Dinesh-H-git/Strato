import { AnalyzerExecuteResult } from './analyzer-execute-result';
/**
 * Result of cancelling the analyzer execution. Only returned if the result completes while cancelling.
 */
export interface AnalyzerCancelResult extends AnalyzerExecuteResult {
}
