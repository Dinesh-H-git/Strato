import { UserSegmentsPresets } from './types/user-segments-presets';
/**
 * Unpins the segments preset in the current app scope.
 *
 * @param {string} id ID of the segments preset to unpin.
 * @returns {UserSegmentsPresets} User created segments presets in the current environment.
 */
export declare function unpinSegmentsPreset(id: string): Promise<UserSegmentsPresets>;
