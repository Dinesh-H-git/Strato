import { UserSegmentsPresets } from './types/user-segments-presets';
import { FilterSegment } from './types/filter-segment';
/**
 * Adds a new segments preset to the recently used in the current environment scope.
 *
 * @param {FilterSegment[]} segments List of segments to create a new preset.
 * @returns {UserSegmentsPresets} User created segments presets in the current environment.
 */
export declare function addRecentSegmentsPreset(segments: FilterSegment[]): Promise<UserSegmentsPresets>;
