import { FilterSegment, UserSegmentsPresets } from '@dynatrace-sdk/segments-presets';
declare const wrapper: {
    getSegmentsPresets: () => Promise<UserSegmentsPresets>;
    addRecentSegmentsPreset: (segments: FilterSegment[]) => Promise<UserSegmentsPresets>;
    pinSegmentsPreset: (segments: FilterSegment[]) => Promise<UserSegmentsPresets>;
    unpinSegmentsPreset: (id: string) => Promise<UserSegmentsPresets>;
};
export declare const setupTestingEnv: () => void;
type MockType = Partial<typeof wrapper>;
/***
 * Setup sdk-web for testing purposes
 * The sdk-web implementation will be overridden with passed mocked values or defaults mocks
 * @param setup - configuration with mocked values / function
 */
export declare const mockSegmentsPresets: (setup?: MockType) => void;
/***
 * Setup sdk-web testing environment with default values
 */
export declare const clearSegmentsPresetsMocks: () => void;
export {};
