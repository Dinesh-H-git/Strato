import { FilterSegmentVariableDefinition } from './filter-segment-variable-definition';
/**
 * Configuration of a single segment.
 *
 * The segment is identified by ID. The configuration may include a set of variables.
 */
export interface FilterSegment {
    id: string;
    variables?: FilterSegmentVariableDefinition[];
}
