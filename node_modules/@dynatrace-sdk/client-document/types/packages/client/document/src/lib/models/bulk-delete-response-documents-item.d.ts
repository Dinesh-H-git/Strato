import { Error } from './error';
export interface BulkDeleteResponseDocumentsItem {
    /**
     * ID of the document.
     */
    id: string;
    /**
     * HTTP code the single delete endpoint would return for a request with this Document ID
     */
    code: number;
    /**
     * An error response as defined [here](https://dt-url.net/platform-services-error-handling).
     */
    error?: Error;
}
