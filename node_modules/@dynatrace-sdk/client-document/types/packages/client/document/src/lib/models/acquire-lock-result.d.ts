export interface AcquireLockResult {
    /**
     * The latest version of the document.
     */
    documentVersion: number;
    /**
     * Timestamp until the document remains locked.
     */
    lockedUntil: Date;
}
