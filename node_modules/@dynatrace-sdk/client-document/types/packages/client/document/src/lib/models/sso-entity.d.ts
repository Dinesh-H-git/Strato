/**
 * A SSO user or SSO group.
 */
export interface SsoEntity {
    /**
     * SSO id of a user or group.
     */
    id: string;
    /**
     * Type of the SSO entity - either `user` or `group`.
     */
    type: string;
}
