/**
 * Envelope object for a list of claimers.
 */
export interface EnvironmentShareClaimerList {
    claimers: string[];
    /**
     * Use this as page-key query param to get the next page
     */
    nextPageKey?: string;
    /**
     * The total amount of results for this query
     */
    totalCount: number;
}
