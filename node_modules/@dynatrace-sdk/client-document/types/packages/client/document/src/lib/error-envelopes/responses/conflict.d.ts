/**
 * ErrorEnvelope
 */
import { ErrorEnvelopeError } from '../error-envelope-error';
export declare class Conflict extends ErrorEnvelopeError {
    readonly isConflict = true;
    readonly name = "Conflict";
}
export declare function isConflict(e: any): e is Conflict;
