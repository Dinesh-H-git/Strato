import { CreatedSnapshot } from './created-snapshot';
import { DocumentMetaData } from './document-meta-data';
export interface UpdateDocumentMetadata {
    documentMetadata: DocumentMetaData;
    createdSnapshot?: CreatedSnapshot;
}
