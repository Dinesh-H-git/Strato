/**
 * Input to acquire the lock.
 */
export interface AcquireLock {
    /**
     * The latest version of the document.
     */
    documentVersion: number;
    /**
     * Duration specified in seconds to acquire the lock on the document.
     */
    lockDurationInSeconds?: number;
}
