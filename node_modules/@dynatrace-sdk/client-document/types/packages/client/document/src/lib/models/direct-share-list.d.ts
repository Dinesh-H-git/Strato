import { DirectShare } from './direct-share';
/**
 * A list of direct-shares.
 */
export interface DirectShareList {
    'direct-shares': DirectShare[];
    nextPageKey?: string;
    totalCount: number;
}
