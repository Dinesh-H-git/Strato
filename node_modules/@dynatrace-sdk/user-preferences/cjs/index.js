/**
 * @license
 * Copyright 2023-2026 Dynatrace LLC
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/platform/user-preferences/src/index.ts
var src_exports = {};
__export(src_exports, {
  getLanguage: () => getLanguage,
  getRegionalFormat: () => getRegionalFormat,
  getTheme: () => getTheme,
  getTimezone: () => getTimezone
});
module.exports = __toCommonJS(src_exports);

// packages/platform/user-preferences/src/lib/utils.ts
var isRunningInServerless = (global) => {
  return global?.document === void 0;
};

// packages/platform/shared/utils/get-global-with-dt-runtime.ts
var getGlobalWithDtRuntime = () => typeof globalThis !== "undefined" ? globalThis : window;

// packages/platform/user-preferences/src/lib/get-theme.ts
function getTheme() {
  const global = getGlobalWithDtRuntime();
  const runtimeFunc = global.dtRuntime?.userPreferences?.getTheme;
  if (typeof runtimeFunc === "function") {
    return runtimeFunc();
  } else {
    if (!isRunningInServerless(global)) {
      console.warn("Missing getTheme function from web runtime.");
    }
    return "auto";
  }
}

// packages/platform/user-preferences/src/lib/get-language.ts
function getLanguage() {
  const global = getGlobalWithDtRuntime();
  const runtimeFunc = global.dtRuntime?.userPreferences?.getLanguage;
  if (typeof runtimeFunc === "function") {
    return runtimeFunc();
  } else {
    if (!isRunningInServerless(global)) {
      console.warn("Missing getLanguage function from web runtime.");
    }
    return "en";
  }
}

// packages/platform/user-preferences/src/lib/get-regional-format.ts
function getRegionalFormat() {
  const global = getGlobalWithDtRuntime();
  const runtimeFunc = global.dtRuntime?.userPreferences?.getRegionalFormat;
  if (typeof runtimeFunc === "function") {
    return runtimeFunc();
  } else {
    if (!isRunningInServerless(global)) {
      console.warn("Missing getRegionalFormat function from web runtime.");
    }
    return "en";
  }
}

// packages/platform/user-preferences/src/lib/get-timezone.ts
function getTimezone() {
  const global = getGlobalWithDtRuntime();
  const runtimeFunc = global.dtRuntime?.userPreferences?.getTimezone;
  if (typeof runtimeFunc === "function") {
    return runtimeFunc();
  } else {
    if (!isRunningInServerless(global)) {
      console.warn("Missing getTimezone function from web runtime.");
    }
    return "UTC";
  }
}
