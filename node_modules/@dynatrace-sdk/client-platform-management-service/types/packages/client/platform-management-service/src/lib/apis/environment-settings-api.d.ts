import { AbortSignal, HttpClient } from '@dynatrace-sdk/http-client';
import { ApiClientOptions } from '@dynatrace-sdk/shared-client-utils';
import { SettingsResponse } from '../models/settings-response';
export declare class EnvironmentSettingsClient {
    private httpClient;
    private shouldTransformDates;
    /**
     * @param {HttpClient} httpClientImplementation - You can provide custom http client as a parameter to constructor of a EnvironmentSettingsClient. Custom http client should implement {HttpClient} interface.
     * @param {ApiClientOptions} [options] - Optional configuration for SDK Client.
     * @example
     * const environmentSettingsClientNodeJs = new EnvironmentSettingsClient(yourCustomImplementation);
     */
    constructor(httpClientImplementation: HttpClient, options?: ApiClientOptions);
    /**
     * Get settings for the environment
     *
     * **One of the following scopes is required:**
     * + app-engine:apps:run
     * + app-engine:functions:run
     *
     * <br/><br/>Gets settings for environment.
     *
     * @returns {Promise<SettingsResponse>} The settings for the requested environment
     *
     * @example <caption>Code example</caption>
     * import { environmentSettingsClient } from "@dynatrace-sdk/client-platform-management-service";
     *
     * const data =
     *   await environmentSettingsClient.getEnvironmentSettings();
     *
     * @throws {BadRequest} Bad request
     * @throws {Unauthorized} Authentication failed
     * @throws {NotFound} Resource not found
     * @throws {ServiceUnavailable} There is a temporary problem in the backend.
     * @throws {DefaultErrorResponse} Unexpected error
     * @throws {InvalidResponseError}
     * @throws {ApiClientError}
     * @throws {ClientRequestError}
     */
    getEnvironmentSettings(config?: {
        abortSignal?: AbortSignal;
    } | AbortSignal): Promise<SettingsResponse>;
}
export declare const environmentSettingsClient: EnvironmentSettingsClient;
