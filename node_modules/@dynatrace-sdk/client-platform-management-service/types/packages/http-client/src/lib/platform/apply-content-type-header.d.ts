import { Headers, RequestBodyTypes } from '../types';
export declare function applyContentTypeHeader<T extends keyof RequestBodyTypes>(headers: Headers | undefined, requestBodyType: T): Headers | undefined;
