/**
 * Specifies when to trigger based on maintenance window status.
 *
 * @deprecated Use literal values.
 */
export declare enum MaintenanceWindowTriggerBehaviorType {
    Always = "always",
    Inside = "inside",
    Outside = "outside"
}
