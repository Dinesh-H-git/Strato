export interface EventQuery {
    /**
     * DQL matcher expression defining which events to match.
     */
    query: string;
    /**
     * Grail event type.
     */
    eventType?: 'events' | 'bizevents' | 'dt.system.events' | 'security.events';
}
