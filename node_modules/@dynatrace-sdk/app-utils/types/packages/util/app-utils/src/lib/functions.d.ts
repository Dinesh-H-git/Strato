/**
 * Utility layer for communicating with app functions.
 */
export declare const functions: {
    call: typeof call;
};
/**
 * Makes HTTP requests to app functions with convenient error handling
 *
 * @param functionName - Name of the function to call
 * @param options - Request configuration:
 *   - data: Optional payload for POST requests
 *   - abortSignal: Optional AbortSignal for request cancellation
 *
 * @returns Promise resolving to fetch Response object
 *
 * @throws Error with properties:
 *   - message: Descriptive error message (e.g., 'Function getData has failed')
 *   - cause: Original Response object
 *
 * @example
 * // GET request
 * const response = await functions.call('getData')
 * const data = await response.json()
 *
 * @example
 * // POST request with data
 * await functions.call('processData', {
 *   data: { key: 'value' }
 * })
 *
 * @example
 * // Request with abort signal
 * const controller = new AbortController()
 * try {
 *   await functions.call('longOperation', {
 *     abortSignal: controller.signal
 *   })
 * } catch (error) {
 *   if (error.name === 'AbortError') {
 *     console.log('Request was cancelled')
 *   }
 * }
 */
declare function call(functionName: string, options?: {
    data?: unknown;
    abortSignal?: AbortSignal;
}): Promise<Response>;
export {};
