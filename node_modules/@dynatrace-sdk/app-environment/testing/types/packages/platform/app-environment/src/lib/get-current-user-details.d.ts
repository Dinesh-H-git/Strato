import { UserDetails } from './types/user-details';
/**
 * Retrieves the information about currently logged user
 *
 * @returns {UserDetails} id, name and email of currently logged user.
 *
 * *Note:* If the Dynatrace JavaScript runtime isn't available, a default UserDetails placeholder object with values
 * ('dt.missing.user.id', 'dt.missing.user.name', 'dt.missing.user.email') will be returned and a warning will be logged to the console.
 *
 * @example <caption>Code example</caption>
 * import { getCurrentUserDetails } from '@dynatrace-sdk/platform/app-environment';
 *
 * // Get the details of the currently logged-in user
 * const userDetails = getCurrentUserDetails();
 *
 * // Use destructuring to extract specific properties
 * const { id, name, email } = getCurrentUserDetails();
 */
export declare function getCurrentUserDetails(): UserDetails;
