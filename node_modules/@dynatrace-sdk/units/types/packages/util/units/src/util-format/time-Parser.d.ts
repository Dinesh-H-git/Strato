export declare const TIMEFRAME_EXPRESSION: RegExp;
/**
 * Structured representation of the parsed time.
 */
export interface TimeDetails {
    /** Input candidate trimmed of whitespaces. */
    normalized: string;
    /** Date representation of the converted input candidate. */
    date: Date;
    /**
     * Type of the given time, which can either be an ISO string
     * or a relative time expression (e.g. Last 30 minutes).
     */
    type: 'expression' | 'iso8601';
}
/**
 * Converts a string representation of time into a structured TimeDetails object.
 *
 * @deprecated Use the `parseTimeAsTimeValue` function instead
 *
 * @param candidate - Input string to parse. Supports:
 *   - ISO 8601 format (e.g., '2025-05-27T14:30:00Z')
 *   - Relative expressions (e.g., 'now-30m', 'now+1h')
 *   - Date formats (e.g., '2025-05-27', '14:30')
 *   - 'now' keyword
 * @param relativeDate - Reference timestamp for relative expressions (defaults to current time)
 *
 * @returns TimeDetails object containing:
 *   - normalized: Cleaned and standardized input string
 *   - date: JavaScript Date object
 *   - type: 'expression' for relative times or 'iso8601' for absolute times
 *   Returns null if parsing fails
 *
 * @example
 * // Parse ISO datetime
 * parseTime('2025-05-27T14:30:00Z')
 * // Returns: {
 * //   normalized: '2025-05-27T14:30:00Z',
 * //   date: Date('2025-05-27T14:30:00.000Z'),
 * //   type: 'iso8601'
 * // }
 *
 * @example
 * // Parse relative time
 * parseTime('now-30m')
 * // Returns: {
 * //   normalized: 'now-30m',
 * //   date: [Date 30 minutes before current time],
 * //   type: 'expression'
 * // }
 *
 * @example
 * // Parse date format
 * parseTime('2025-05-27 14:30')
 * // Returns: {
 * //   normalized: '2025-05-27T14:30:00Z',
 * //   date: Date('2025-05-27T14:30:00.000Z'),
 * //   type: 'iso8601'
 * // }
 */
export declare const parseTime: (candidate?: string | null, relativeDate?: number) => TimeDetails | null;
