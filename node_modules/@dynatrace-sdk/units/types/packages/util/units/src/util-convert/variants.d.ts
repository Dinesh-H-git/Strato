import { Unit } from './types';
type Variants<U extends Unit> = string[];
type VariantNames<U extends Unit> = string[];
type VariantUnits<U extends Unit> = Unit[];
export declare function isConvertibleUnit(unit: Unit): boolean;
export declare function variants<U extends Unit>(unit: U): Variants<U>[];
/**
 * Returns the names of all units to which the provided unit can be converted.
 *
 * @param unit - The source unit to find convertible variants for (e.g., units.length.meter)
 * @returns Array of unit names that are valid conversion targets
 *
 * @example
 * // Get all length unit names
 * variantNames(units.length.meter)
 * // Returns:
 * // ['meter', 'kilometer', 'centimeter', 'millimeter', ...]
 *
 * @example
 * // Get temperature unit names
 * variantNames(units.temperature.degree_celsius)
 * // Returns:
 * // ['degree_celsius', 'degree_fahrenheit', ...]
 */
export declare function variantNames<U extends Unit>(unit: U): VariantNames<U>;
/**
 * Returns all units to which the provided unit can be converted.
 *
 * @param unit - The source unit to find convertible variants for (e.g., units.length.meter)
 * @returns Array of unit objects that are valid conversion targets
 *
 * @example
 * // Get all length units
 * variantUnits(units.length.meter)
 * // Returns:
 * // [
 * //   [{ 'group': 'meter', 'index': -15, 'exponent': 1 }],
 * //   [{ 'group': 'meter', 'index': 30, 'exponent': 1 }],
 * //   ...
 * // ]
 *
 * @example
 * // Get convertible temperature units
 * variantUnits(units.temperature.degree_celsius)
 * // Returns:
 * // [
 * //   [{ 'group': 'degree_celsius', 'index': 0, 'exponent': 1 }],
 * //   [{ 'group': 'degree_fahrenheit', 'index': 0, 'exponent': 1 }],
 * //   ...
 * // ]
 */
export declare function variantUnits<U extends Unit>(unit: U): VariantUnits<U>;
export {};
