import { Unit } from '../util-convert/types';
import { units } from '../util-units/units';
export interface GrailUnitEquivalency {
    namespace: string;
    unitName: string;
    unit: Unit;
}
export type GrailToUnitDefinition = keyof typeof units extends infer L ? L extends keyof typeof units ? keyof (typeof units)[L] extends infer M ? M extends keyof (typeof units)[L] ? {
    namespace: L;
    unitName: M;
    unit: (typeof units)[L][M];
} : never : never : never : never;
