import { ConvertibleUnit, ConvertibleTarget } from './types';
/**
 * Converts a numeric value from one unit to another within the same measurement system
 *
 * @param input - The numeric value to convert
 * @param from - Source unit (e.g., units.length.meter, units.time.second)
 * @param to - Target unit for conversion (must be compatible with source unit)
 * @param unsafe - Must be `true` to bypass type checking. Useful when unit types are
 * not statically known (e.g., from runtime data like outputUnit from getFormatting)
 * @returns Converted numeric value
 *
 * @throws Error if units are incompatible (different measurement systems)
 *
 * @example
 * // Convert length
 * import { convert, units } from "@dynatrace-sdk/units";
 * convert(1500, units.length.meter, units.length.kilometer)
 * // Returns: 1.5
 *
 * @example
 * // Convert temperature
 * convert(32, units.temperature.degree_fahrenheit, units.temperature.degree_celsius)
 * // Returns: 0
 *
 * @example
 * // Convert time
 * convert(3600, units.time.second, units.time.hour)
 * // Returns: 1
 *
 * @example
 * // Using unsafe mode to convert with runtime-determined unit
 * convert(100, outputUnit, units.data.mebibyte, true)
 */
export declare function convert<FromUnit extends ConvertibleUnit, ToUnit extends ConvertibleUnit>(input: number, from: FromUnit, to: ConvertibleTarget<FromUnit, ToUnit>): number;
export declare function convert(input: number, from: ConvertibleUnit, to: ConvertibleUnit, unsafe: true): number;
