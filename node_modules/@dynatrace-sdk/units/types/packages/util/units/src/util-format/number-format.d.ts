export declare let numberFormatCache: {
    [key: string]: Intl.NumberFormat;
};
/** Clears the cache of an entry. If the key is omitted the whole cache store will be cleared */
export declare function clearNumberFormatCache(key?: string): void;
/**
 * Generates a cache key based on the passed locales and options
 */
export declare function generateCacheKey(locales?: Intl.LocalesArgument, options?: Intl.NumberFormatOptions): string;
/**
 * Returns a cached instance of Intl.NumberFormat based on passed locales and options
 */
export declare function getNumberFormat(locales?: Intl.LocalesArgument, options?: Intl.NumberFormatOptions): Intl.NumberFormat;
